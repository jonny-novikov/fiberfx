# trd-9-1-1 — AAW scope ledger

## {trd-9-1-1-thinking} Thinking

### T-1 — UNDERSTAND/EXPAND: the transport fix as its own rung (Flat-L2 + Apollo, HIGH risk)

5W. WHAT: fix echo/apps/investex so the venue client can actually dial the live sandbox, and add a re-runnable 3-way live harness that re-proves 9.1's G6 and unblocks 9.2's live floor. WHY: the as-built transport cannot complete a verifying handshake to the venue — two real defects — so the 9.1/9.2 live floors were never genuinely met and 9.1's G6 "STANDING result" record (trd.9.1.specs.md:368-383, with per-seed ms timings) is a FALSE-GREEN. WHERE: config.ex + client.ex + a vendored root PEM + their tests + the live harness + the rung gate; the spec slice + the 9.1 reconcile + the ledger. WHO: Venus (spec) → Mars (code, never the Director — LAW-1a) → Director review → Mars harden + live harness → Apollo (mandatory) → Director ship. WHEN: before 9.2 can re-run its live floor.

THE TWO DEFECTS (grounded). A — config.ex:20 hardcodes the stale sandbox-invest-public-api.tinkoff.ru:443 and resolve/1 (config.ex:89-91) lifts only INVEST_TOKEN, ignoring INVEST_API_URL/INVEST_API_PORT; the live host is sandbox-invest-public-api.tbank.ru:443 (the T-Bank rebrand). B — client.ex:184-193 dials verify_peer against :public_key.cacerts_get() (the OS bundle, 0 Russian roots), but the venue chains leaf → Russian Trusted Sub CA → Russian Trusted Root CA (a self-signed root absent from the host store), so every verifying handshake is rejected → the {:stop,{:dial_failed,_}} path.

SOLUTION SPACE. V-baseline (do nothing; 9.2 ships pure-only, live floor permanently deferred) — REJECTED: the live floor is the Operator's hard gate and the false-green stays uncorrected. V-verify_none (sandbox tier) — REJECTED by the Operator (SF-1): weakens a financial client even in sandbox. V-ca-path (configurable INVEST_CA_BUNDLE) — viable, NOT chosen. V-CHOSEN — vendor the pinned Russian Trusted Root CA in-repo + read INVEST_API_URL/INVEST_API_PORT (default tbank.ru:443): the Operator's pick, out-of-box + pinned/auditable.

INVARIANTS AS RUNNABLE CHECKS. INV-10 (endpoint env-resolved): grep INVEST_API_URL config.ex non-empty; Config.new([]).endpoint == "sandbox-invest-public-api.tbank.ru:443". INV-11 (vendored+pinned root, verify_peer kept): the vendored PEM SHA-256 == D2:6D:2D:02:31:B7:C3:9F:92:CC:73:85:12:BA:54:10:35:19:E4:40:5D:68:B5:BD:70:3E:97:88:CA:8E:CF:31; grep verify_none client.ex EMPTY; the vendored root's DER ∈ the cacerts the client builds (the G-TLS network-free trust proof, with a mutation that kills it). INV-9 (secret hygiene) held byte-for-byte. DIRECTOR DE-RISK (verified this turn): trusting ONLY that root → ssl_verify_result=0 against the venue's real IP 178.130.128.33; the served chain is leaf→Sub→Root so only the root is vendored (existing depth:3 builds the path). Userspace egress completed a full TLS handshake (curl), so gun has a real shot — Q2's 3-way harness reports honestly (PASS / TLS-trust-FAIL=BLOCK / egress-BLOCK=reproduced-defer; the classifier must never collapse a trust FAIL into an egress BLOCK — that is the exact false-green class being closed).

SMALLEST CORRECT CHANGE (reductive minimization): read 2 env vars + flip the default; read+append one vendored PEM, keep verify_peer; one network-free trust proof + one 3-way live harness + the rung gate. NO new dep (mix.lock unchanged, SF-4), NO shim (the doctest/config_test update with the surface), NO 9.2 file touched (SF-3). Settled forks SF-1..SF-4 carry no open Operator decision → the Stage-1 gate is reachable.

### T-2 — Venus reconcile (deterministic core): 3 STALE deltas in trd.9.1.specs.md, all confirmed by pure grep against the as-built code

The lag-1 reconcile of the transport spine trd.9.1.specs.md against echo/apps/investex as-built. Three claims probed deterministically (grep/read — unrationalizable), three STALE, each owned by this rung's correction:

DELTA-1 (endpoint). Spec :196 pins the endpoint default `sandbox-invest-public-api.tinkoff.ru:443`; config.ex:20 `@endpoint_default` == that stale literal, and resolve/1 (config.ex:89-91) reads ONLY System.fetch_env!("INVEST_TOKEN") — no INVEST_API_URL/INVEST_API_PORT read anywhere in config.ex. VERDICT STALE → corrected by INV-10 (env-resolved endpoint, default …tbank.ru:443). Owner: Venus reconciles the spec; Mars builds the code.

DELTA-2 (TLS posture). Spec :207-217 + the client.ex:24,181-187 moduledoc/comment claim "verify_peer against the OTP system trust store"; client.ex:187 == `cacerts: :public_key.cacerts_get()` (the OS bundle only). The venue chains leaf → Russian Trusted Sub CA → Russian Trusted Root CA, a self-signed root absent from that bundle (0 Russian roots on this host). VERDICT STALE → corrected by INV-11 (vendored+pinned root appended to cacerts; verify_peer kept). Owner: Venus reconciles the spec; Mars builds the code.

DELTA-3 (G6 false-green). Spec :368-383 records "G6 ACTUALLY DIALS … a STANDING result" with per-seed ms timings (0: 1224ms/409ms; 1: 795ms/775ms; default: 825ms/446ms). The Director de-risk (T-1) proved a verifying TLS handshake to the sinkholed …tinkoff.ru host with 0 Russian roots is STRUCTURALLY IMPOSSIBLE — so a non-empty account_id from open_account could not have returned; the recorded dial could not have completed. VERDICT FALSE-GREEN → marked [RECONCILE] corrected-by-TRD.9.1.1. The twin assertion in trd.progress.md:239-254 ("a standing result across seeds") is the narrative twin, corrected in the same pass.

BOUNDARY FACTS for the build topology (Mars): priv/ does NOT exist (the vendored PEM at priv/certs/russian_trusted_root_ca.pem is genuinely new); test/ holds config_test.exs + sandbox_live_test.exs but NOT client_test.exs nor transport_live_test.exs (Mars creates what it touches; the pathspec lists both live-test paths, stage only the written one); echo/rungs/exchange/trd_9_1_check.{exs,out} is the rung-gate precedent for trd_9_1_1_check.

GATE: every reconcile claim is MATCH or [RECONCILE]-marked after the corrections land; no INVENTED/MISSING; the slice is build-grade once authored. The reconcile is the executable form of /reconcile's deterministic core — the three greps are cheap, fast, and unrationalizable.

### T-3 — CA provenance + fingerprint-match (the pin is the authority, not the source). Pulled the venue's served chain network-side with `openssl s_client -connect sandbox-invest-public-api.tbank.ru:443 -servername … -showcerts` (3 certs: leaf `CN=*.tbank.ru` → `Russian Trusted Sub CA` → `Russian Trusted Root CA`). Isolated the self-signed root by subject==issuer (cert_3). `openssl x509 -noout -sha256 -fingerprint` on it = `D2:6D:2D:02:31:B7:C3:9F:92:CC:73:85:12:BA:54:10:35:19:E4:40:5D:68:B5:BD:70:3E:97:88:CA:8E:CF:31` — EXACTLY the spec pin (SF-1/INV-11). Subject `C=RU, O=The Ministry of Digital Development and Communications, CN=Russian Trusted Root CA`. Only the root is vendored; the existing `depth: 3` builds leaf→sub→root from the wire. The served bytes are acceptable because they fingerprint-matched the pin BEFORE trust — never blind (per spec §SF-1 + the runbook). It is a PUBLIC CA cert, not a secret (INV-9 unaffected).

### T-4 — Stage-2 gate closure (build + network-free proof). Compile `--warnings-as-errors` clean (umbrella). Tier-1 `cd echo/apps/investex && TMPDIR=/tmp mix test` = 16 doctests, 6 properties, 42 tests, 0 failures, 3 excluded (the :sandbox tier stays excluded — network-free). Determinism loop 100/100 GREEN (the loopback binds sockets + config tests mutate env = process-touching, so ≥100 per the charter). G-TLS both assertions land: (1) real-root presence — the vendored DER (sha256 = the pin) ∈ tls_opts/0's :cacerts, absent from the OS bundle; (2) loopback mechanism — a self-CA leaf clears with verify_peer + cacerts++[test_ca_der] and is REJECTED (Unknown CA / {:tls_alert,_}) without it (the two stderr TLS [notice] lines are that negative case succeeding). MUTATION SELF-CHECK (Mars, reverted net-zero per LAW-1a): dropping the ++ append → the (1) teeth test FAILS (exit 2); revert byte-identical, suite GREEN — the gate is not a tautology. Boundary held: only client.ex + config.ex + config_test.exs (M) + client_test.exs (new) touched in apps/investex; no other transport module (Retry/Money/Error/Caller/Users/Sandbox), no 9.2 file, no live harness, no rung gate (Stage 4). echo/mix.lock UNCHANGED (SF-4). `grep -n verify_none client.ex` EMPTY (the explanatory comments reworded to not trip the literal gate). No token-shaped literal anywhere (G7).

### T-5 — Stage-4 gate closure (harden + G6′ live harness + rung gate). No Stage-3 REMEDIATE to close (Director Y-3 = BUILD-GRADE, no blocking findings; the only "REMEDIATE" was L-3 build-guidance, honored). BUILT: (1) echo/apps/investex/test/transport_live_test.exs (NEW, @moduletag :sandbox) — the 3-way classifier per D-4/D-5: a fail-fast :ssl.connect discriminator using the client's REAL Investex.Client.tls_opts/0 + SNI → {:tls_alert,_} ⇒ TLS-trust FAIL=BLOCK (flunk) / other {:error,_} ⇒ egress-BLOCK=reproduced-defer (named, not a suite-fail) / {:ok} ⇒ the gRPC round-trip open→get_accounts→close asserting a non-empty account_id (PASS, the INV-8 dialed-proof, re-proves 9.1 G6). (2) echo/rungs/exchange/trd_9_1_1_check.{exs,out} (NEW) — the --no-start network-free gate, 6 lines: G-TLS-presence (the teeth) + G-TLS-mechanism (positive-only loopback, no stderr noise) + endpoint-env-resolved (INV-10) + no-verify_none + CA-provenance (the pin) + G7. GATES: compile clean; Tier-1 16 doctests/6 properties/43 tests/0 failures/4 excluded (the live harness correctly excluded); 100/100 determinism GREEN; the rung gate PASS 6/6 exit 0, reproducible (run twice byte-identical), the committed .out record == fresh stdout; gate-teeth mutation proven (drop the ++ append → G-TLS-presence FAIL exit 1; revert net-zero). LIVE: G6′ ran with .env.test sourced → PASS (the venue verified against the vendored root, the round-trip dialed, a non-empty account_id; 9.1 G6 re-proven, 9.2 unblocked; secret hygiene held — log scrubbed + deleted, env clean). BOUNDARY: only config.ex + client.ex (lib), config_test.exs (M) + client_test.exs + transport_live_test.exs (test), the rung gate — sandbox_live_test.exs UNCHANGED (the 9.2 file untouched, SF-3); mix.lock unchanged (SF-4); verify_none absent from client.ex; no token-shaped string anywhere. The pre-existing untracked trd_9_2_check.{exs,out} are NOT mine (a prior rung) — excluded from my stage. FORWARD to the Director: the L-2 .gitignore negation is STILL REQUIRED in the Stage-5 pathspec (the PEM is ignored by repo-root *.pem) — the live PASS depended on the on-disk PEM, but the COMMIT will drop it without the scoped negation.

### T-6 — Apollo evaluation opened: read-first complete, independent fingerprint + gitignore confirmed

Read the authoritative spec (trd.9.1.1.specs.md), the runbook (trd.9.1.1.prompt.md, the ◇ Apollo block + Acceptance), the ledger (trd-9-1-1.progress.md T-1..T-5/D-1..D-13/Y-1..Y-4/L-1..L-4), and the as-built (config.ex, client.ex, client_test.exs, transport_live_test.exs, config_test.exs, the rung gate .exs/.out). This is a trd.* rung (Exchange platform), NOT emq.* — the generic §11.2 charter governs, NOT echo-mq-evaluator.

INDEPENDENT CONFIRMATIONS this turn (not trusting Mars):
(1) CA fingerprint: ran `openssl x509 -in priv/certs/russian_trusted_root_ca.pem -noout -sha256 -fingerprint` MYSELF → D2:6D:2D:02:31:B7:C3:9F:92:CC:73:85:12:BA:54:10:35:19:E4:40:5D:68:B5:BD:70:3E:97:88:CA:8E:CF:31 — byte-for-byte the spec pin (D-1/INV-11). subject==issuer = C=RU, O=The Ministry of Digital Development and Communications, CN=Russian Trusted Root CA → the self-signed root (not the Sub CA). The pin is verified, not asserted.
(2) gitignore trap (L-2): `git check-ignore -v echo/apps/investex/priv/certs/russian_trusted_root_ca.pem` → `.gitignore:29:*.pem`. The scoped negation is NOT yet in .gitignore (grep found none). CONFIRMED: a Stage-5 commit without `!echo/apps/investex/priv/certs/russian_trusted_root_ca.pem` SILENTLY DROPS the trust anchor — a structural false-green of the security fix.
(3) boundary: working tree dirty with UNRELATED program work (emq.2.4, fsharp, mercury live_admin, aob, .mcp.json) — the SF-3 contingency; the rung's own files are config.ex/client.ex/config_test.exs (M) + client_test.exs/transport_live_test.exs/the rung gate (??). The untracked trd_9_2_check.{exs,out} are a PRIOR rung's, NOT 9.1.1 — must be excluded from the Stage-5 stage (Mars flagged this in Y-3/T-5).
(4) the 9.1 G6 false-green correction IS present in trd.9.1.specs.md (:378-394 + the DoD twin :425-428), [RECONCILE]-marked, original L-9/L-10 history preserved verbatim.

NEXT: re-run the network-free gate independently; run the drop-append + corrupt-PEM mutations MYSELF to confirm G-TLS has teeth (RED); grep verify_none; then the independent live re-dial.

## {trd-9-1-1-decisions} Decisions

### D-1 — SF-1 locked (Operator-chosen): vendor the Russian Trusted Root CA in-repo, append to cacerts, keep verify_peer

TLS trust posture. The venue's leaf chain roots at the self-signed Russian Trusted Root CA, absent from the OTP system trust store. The Operator chose: vendor the root PEM under echo/apps/investex/priv/certs/russian_trusted_root_ca.pem (sourced from the OFFICIAL distribution) and APPEND its DER to :public_key.cacerts_get(); keep verify: :verify_peer + depth: 3 + the hostname check. NO verify_none, ever, in any tier. The root identity is PINNED: C=RU, O=The Ministry of Digital Development and Communications, CN=Russian Trusted Root CA, SHA-256 D2:6D:2D:02:31:B7:C3:9F:92:CC:73:85:12:BA:54:10:35:19:E4:40:5D:68:B5:BD:70:3E:97:88:CA:8E:CF:31. Mars MUST verify the vendored bytes match this fingerprint (never vendor the server-served bytes blind — that trusts the thing being authenticated). The venue serves the Sub CA on the wire, so ONLY the root is vendored; the existing depth:3 builds leaf→sub→root. Alternatives rejected: V-verify_none (weakens a financial client even in sandbox — Operator-rejected); V-ca-path (a configurable INVEST_CA_BUNDLE — viable, not chosen; the in-repo pin is out-of-box + auditable). Director de-risk (T-1): trusting ONLY this root yields ssl_verify_result=0 against the venue's real IP 178.130.128.33. Grounding: runbook SF-1; trd-9-1-1.progress.md T-1.

### D-2 — SF-2 locked (Operator-chosen): ship the fix + a re-runnable 3-way live harness; the classifier MUST NOT collapse a trust-FAIL into an egress-BLOCK

No-egress posture. The live verification is a re-runnable harness, not a one-shot. It classifies the live leg into exactly ONE of three outcomes: (i) PASS — the TLS handshake clears AND a decoded gRPC response returns (a non-empty account_id from open_account, then close_account) → 9.1 G6 re-proven, 9.2's live floor unblocked; (ii) TLS-trust FAIL — the handshake is rejected (a cert / unknown_ca / {:tls_alert,_} shape) → FAIL LOUD, this BLOCKS the ship (the vendored root is wrong, a real bug); (iii) egress BLOCK — a TCP timeout / refused / sinkhole BEFORE any TLS exchange (the 198.18.x split-tunnel may block gun even though curl reached the real IP) → a named, reproduced environment-BLOCK, ship-with-deferred (the correctness fix ships, the live leg stands unproven-here for an egress-capable run), NEVER a false-green. THE CLASSIFIER MUST NOT COLLAPSE (ii) INTO (iii) — that collapse is the exact false-green class this rung closes (a real trust bug masquerading as "just the environment"). Alternative rejected: V-baseline (ship pure-only, live floor permanently deferred) — the live floor is the Operator's hard gate and the false-green stays uncorrected. Grounding: runbook SF-2; trd-9-1-1.progress.md T-1.

### D-3 — SF-3 locked: the slice boundary is the transport only (config.ex, client.ex, the vendored cert, their tests, the live harness, the rung gate) + the spec slice + the 9.1 reconcile + the ledger

Scope. trd.9.1.1 touches ONLY: echo/apps/investex/lib/investex/config.ex (DEFECT A), echo/apps/investex/lib/investex/client.ex (DEFECT B), echo/apps/investex/priv/certs/russian_trusted_root_ca.pem (NEW), the touched tests (config_test.exs, client_test.exs, the live harness in sandbox_live_test.exs and/or transport_live_test.exs, test_helper.exs), echo/rungs/exchange/trd_9_1_1_check.{exs,out}, and the docs (trd.9.1.1.specs.md, trd.9.1.1.md, the trd.9.1.specs.md/trd.9.1.md reconcile, trd.progress.md, the ledger). It does NOT touch any 9.2 spec file, any read-service / order / stream module, nor the transport modules' OTHER public surfaces (Retry, Money, Error, Caller, Users, Sandbox are unchanged). The Director's Stage-3 reconcile greps this boundary: any 9.2 / read-service / order / stream file in the diff is a violation. 9.2 ships separately AFTER this rung re-proves the dial. Grounding: runbook SF-3, §Map.

### D-4 — SF-4 locked: no new dependency; echo/mix.lock is NOT touched by this rung

Reading a PEM (File.read! + :public_key.pem_decode → DER) and appending DER to :public_key.cacerts_get() are OTP stdlib (:public_key, :file) — already in the runtime, NOT a new hex dep. The spec must NOT imply a mix.lock change. The Director's Stage-5 precondition greps `echo/mix.lock NOT in the diff`; if it appears, foreign lock churn was swept and the commit is wrong. Mars confirms `git status` shows no mix.lock change before reporting. The 9.1 deps (grpc 0.11.5 + protobuf 0.17.0 + stream_data + echo_data) are reused unchanged. Grounding: runbook SF-4.

### D-5 — INV-10 locked: the endpoint is env-resolved (INVEST_API_URL + INVEST_API_PORT), default sandbox-invest-public-api.tbank.ru:443; precedence explicit-opt > env > default

The new invariant correcting DEFECT A. Investex.Config.resolve/1 reads INVEST_API_URL and INVEST_API_PORT from the environment and composes `host:port` into :endpoint; @endpoint_default (and the new/1 :endpoint default) becomes sandbox-invest-public-api.tbank.ru:443 (the T-Bank rebrand). The precedence is decided and documented: an explicit :endpoint opt to new/1 wins; else the composed env (URL+PORT); else the default. The doctest (config.ex:53-55) and config_test.exs are updated to the new default — a DELIBERATE surface change, NO shim, NO backward-compat alias to the old tinkoff.ru literal. INV-9 token discipline is kept byte-for-byte (resolve/1 still lifts INVEST_TOKEN; the value appears nowhere written). LIVENESS as a runnable check (the acceptance must EXERCISE the outcome, not satisfy its letter): Config.new([]).endpoint == "sandbox-invest-public-api.tbank.ru:443"; with INVEST_API_URL/INVEST_API_PORT set, resolve/1's endpoint reflects them; grep INVEST_API_URL in config.ex is non-empty. Woven into the existing INV-5/8/9. Parity note: the Go SDK default (client.go:120-121) is ALSO the stale …tinkoff.ru; only its env/.env.test override (INVEST_API_URL) points at tbank.ru — so env-resolution matches the Go SDK's actual runtime behavior. Grounding: config.ex:20,89-91; client.go:72-73,120-121; runbook Stage-1 INV-10.

### D-6 — INV-11 locked: the venue root is vendored under priv/certs/ + pinned by SHA-256; verify_peer KEPT; never verify_none

The new invariant correcting DEFECT B. Investex.Client.tls_opts/0 reads the vendored PEM at runtime (:code.priv_dir(:investex) + File.read! + :public_key.pem_decode → DER) and APPENDS it to :public_key.cacerts_get(); it keeps verify: :verify_peer + depth: 3 + the customize_hostname_check. The vendored root is PINNED to SHA-256 D2:6D:2D:02:31:B7:C3:9F:92:CC:73:85:12:BA:54:10:35:19:E4:40:5D:68:B5:BD:70:3E:97:88:CA:8E:CF:31 (C=RU, O=The Ministry of Digital Development and Communications, CN=Russian Trusted Root CA); Mars records the provenance in a comment and verifies the bytes match before vendoring (never server-served bytes blind). LIVENESS as a runnable check: the vendored root's DER ∈ the cacerts the client builds (the G-TLS network-free trust proof, with a mutation — drop the append, or corrupt the PEM — that turns it RED); grep -n verify_none client.ex is EMPTY. Woven into the existing INV-5/8/9. Security/supply-chain note: this is why the rung is HIGH risk and Apollo is mandatory — it changes TLS peer-verification trust and vendors a foreign state-operated root CA into the repo. Grounding: client.ex:181-193; runbook SF-1, Stage-1 INV-11; trd-9-1-1.progress.md T-1 (the ssl_verify_result=0 de-risk).

### D-7 — the harness 3-way contract locked (G6′): PASS / TLS-trust-FAIL=BLOCK / egress-BLOCK=reproduced-defer; a no-op cannot satisfy any branch

The live transport harness (G6′) is the genuine replacement for the corrected false-green. With INVEST_TOKEN + INVEST_API_URL/INVEST_API_PORT sourced into the env, the harness dials the venue and classifies the outcome into exactly one of three, and the classifier MUST distinguish them (it must NOT collapse a trust-FAIL into an egress-BLOCK):

(i) PASS — the TLS handshake clears AND a decoded gRPC response returns: the chain open_account → get_accounts → close_account, asserting a NON-EMPTY account_id (the INV-8 positive dialed-proof). This re-proves 9.1's G6 and unblocks 9.2's live floor.

(ii) TLS-trust FAIL — the handshake is rejected: a cert / unknown_ca / {:tls_alert, _} shape (TLS-layer rejection AFTER TCP connect). → fail LOUD; this BLOCKS the ship. The vendored root is wrong — a real correctness bug, never deferred.

(iii) egress BLOCK — a TCP timeout / connection refused / sinkhole BEFORE any TLS exchange (no ClientHello answered). → a NAMED, reproduced environment-BLOCK; the correctness fix ships with the live leg marked unproven-here (ship-with-deferred, SF-2/D-2); NEVER a pass. The harness stands to prove the live leg from an egress-capable run.

The discriminator is the TLS layer: a failure WITH a TLS alert / cert shape is (ii); a failure with NO TLS bytes exchanged (TCP-layer) is (iii). The own-liveness rule (INV-8, the venus-charter "a gate must specify its OWN liveness"): a PRESENT token under --include sandbox MUST run the dial with a positive proof (the non-empty account_id assertion in (i)), and an ABSENT token under --include sandbox is a LOUD flunk (the keyless case), never a silent skip-or-pass. Secret hygiene (INV-9): no token / account-id values logged. Grounding: runbook SF-2, Stage-4 G6′, the §"Acceptance"; trd.9.1.specs.md INV-8 (the liveness contract sandbox_live_test.exs realizes); trd-9-1-1.progress.md T-1.

### D-8 — the G6 false-green correction locked: trd.9.1.specs.md:368-383 is marked [RECONCILE] corrected-by-TRD.9.1.1; the live floor was never genuinely met

The prior "RECONCILED — G6 ACTUALLY DIALS … STANDING result" block in trd.9.1.specs.md:368-383, with per-seed ms timings, is a FALSE-GREEN. The as-built (before this rung) dialed the stale sinkholed sandbox-invest-public-api.tinkoff.ru:443 with verify_peer against a cacerts bundle holding 0 Russian roots, so: (a) the host was a sinkhole/rebrand-stale name (DEFECT A), and (b) even reaching the real venue, the verifying TLS handshake would be rejected for an untrusted root (DEFECT B). A non-empty account_id from open_account was therefore STRUCTURALLY IMPOSSIBLE, and the recorded per-seed dials could not have completed a verifying round-trip. The prior 9.1 Apollo loop fixed a DIFFERENT false-green (the async OS-env token clobber, L-9/L-10) and correctly hardened the gate's own-liveness — but it could not have observed the transport defects, because the gate's letter (assert a non-empty account_id) was being satisfied by a dial that the de-risk proof shows could not have run as recorded; the env was masking the transport failure as a different shape. The correction: a third-person [RECONCILE] marker on the :368-383 block stating the standing result is corrected by TRD.9.1.1 (the false-green), and the same correction on the narrative twin in trd.progress.md:239-254 ("a standing result across seeds"). The genuine G6 is re-proven by D-7's 3-way harness AFTER the A+B fix lands. LESSON (a learning, surfaced for the Operator's mentor grant): a live gate that asserts a positive proof can still false-green when the SUBSTRATE it dials is wrong in a way the assertion shape does not reach — own-liveness on the gate is necessary but not sufficient; the transport itself must be independently de-risked (the ssl_verify_result=0 proof). Grounding: trd.9.1.specs.md:364-383; trd.progress.md:239-254; trd-9-1-1.progress.md T-1; runbook Stage-1 "the important one".

### D-9 — DEFECT A seam + precedence (the env-compose lives in resolve/1; explicit>env>default via a default-sentinel compare). Per the spec ("the realization is Mars's … the precedence enforced is Mars's call to document"): endpoint resolution lives in `resolve/1` beside the token lift, NOT in `new/1`. Rationale: `new/1` stays a pure function of its opts → `new([]).endpoint` is the deterministic `@endpoint_default` and the doctest holds with NO env present (a bare Tier-1 `mix test` sets no INVEST_API_URL — the determinism caveat the brief flagged). The env is read in exactly one place. Precedence explicit>env>default is realized by a private `resolve_endpoint/1`: an explicit endpoint (struct.endpoint != @endpoint_default) is kept verbatim (explicit beats env); otherwise INVEST_API_URL (+ INVEST_API_PORT, else :443) composes host:port; else the default stands. Documented edge: a caller passing the LITERAL default string is indistinguishable from no-opt, so env would override it — the only value for which explicit is not honored over env; harmless (passing default → default is observably correct) and the spec's contract exercises an explicit endpoint distinct from the default ("host:443"), so every pinned assertion passes. @endpoint_default flipped tinkoff.ru→tbank.ru; added @endpoint_port_default "443". INV-9 token discipline byte-unchanged.

### D-10 — DEFECT B append shape (plain DER, not {:cert, der}) + read-each-dial. tls_opts/0 appends the vendored root as the BARE DER binary: `cacerts: :public_key.cacerts_get() ++ [russian_root_ca_der()]`. Per L-1, cacerts_get/0 on OTP 28.5.0.1 returns mixed entries (mostly {:cert, der}, not uniformly so) and `:ssl` accepts a raw DER binary in the same cacerts list, so the bare DER is the robust realization-over-literal (the brief's "append the der binary" intent, made shape-correct). The DER is obtained via `:code.priv_dir(:investex) |> Path.join("certs/russian_trusted_root_ca.pem") |> File.read! |> :public_key.pem_decode` → `[{:Certificate, der, :not_encrypted}]` → der. Read once per dial (the dial is once per client) — no compile-time embedding of the cert bytes. verify_peer + depth:3 + the hostname check KEPT; verify_none nowhere.

### D-11 — G-TLS realization-over-literal (two assertions; loopback uses a SELF-CA, not the real root) + tls_opts/0 promoted to @doc false. The spec's G-TLS strong form ("a loopback :ssl handshake against a cert chained to the vendored root clears") is LITERALLY UNREALIZABLE: signing a test leaf with the real Russian Trusted Root CA needs that root's PRIVATE KEY, which is not ours — the loopback cannot chain to the REAL vendored root. Realization (per the runbook's own "realization-over-literal" note): (1) REAL-ROOT PRESENCE (the floor with teeth) — assert the vendored root's DER ∈ the cacerts `Investex.Client.tls_opts/0` actually builds; dropping the `++` append in client.ex turns this RED (the gate's teeth, exercised by the Stage-3 mutation). (2) MECHANISM, STRONG FORM — a network-free loopback :ssl handshake with a SELF-GENERATED test CA + server leaf: verifying with verify_peer + `cacerts: cacerts_get() ++ [test_ca_der]` CLEARS, and WITHOUT the test CA appended is REJECTED ({:tls_alert,_}/unknown_ca). (2) proves the append→trust mechanism AND that verify_peer genuinely rejects an untrusted chain, so (1) is not a tautology. To give (1) teeth against the REAL tls_opts/0 (a defp), tls_opts/0 is promoted to `@doc false def tls_opts` — public to the VM (test-callable), excluded from docs, signaling internal; the test asserts on its real output rather than reconstructing the bundle (a reconstruction would not fail when the client drops the append). Both assertions land; network-free (loopback only).

### D-12 — G6′ classifier = a fail-fast :ssl.connect TLS probe (the discriminator) + the gRPC round-trip (the PASS proof). Realization-over-literal of L-3 (forced by L-4): the harness classifies the venue dial in TWO channels. (1) THE DISCRIMINATOR — a direct `:ssl.connect(host, port, tls_opts, timeout)` using EXACTLY Investex.Client.tls_opts/0's trust set (cacerts_get() ++ [vendored_root], verify_peer, depth:3, hostname check) with a SHORT timeout, bypassing gun's retry:100. Proven against the LIVE venue: with the vendored root the handshake CLEARS; with the OS bundle only it returns `{:error, {:tls_alert, {:unknown_ca, …}}}`; a closed/absent host returns `{:error, :econnrefused}` / `{:error, :nxdomain}` / `{:error, :timeout}`. So the disjoint, IMMEDIATE rule is: `{:error, {:tls_alert, _}}` (TLS-layer, AFTER TCP connect) ⇒ TLS-trust FAIL = BLOCK; any other `{:error, reason}` (TCP-layer, no TLS bytes) ⇒ egress-BLOCK = reproduced-defer; `{:ok, sock}` ⇒ proceed. The classifier CANNOT collapse the two (the tls_alert tag is structural, not a timeout). (2) THE PASS PROOF — only if (1) cleared: Investex.Client.start_link/1 → Sandbox.open_account → Sandbox.get_accounts → Sandbox.close_account, asserting a non-empty account_id (the INV-8 dialed-proof; re-proves 9.1 G6, unblocks 9.2). LIVE FINDING: the BEAM reaches the venue from THIS machine and the vendored root verifies — so the expected live verdict is PASS, not the SF-2 egress-BLOCK contingency (that contingency stands in the harness for an egress-incapable run). The discriminator uses the client's REAL tls_opts so it genuinely tests the shipped trust against the live wire.

### D-13 — the live harness is a FOCUSED new file `echo/apps/investex/test/transport_live_test.exs`, NOT an extension of sandbox_live_test.exs. The spec/runbook offer both; L-3 says "reuse the existing sandbox_live_test infra". Realization-over-literal: sandbox_live_test.exs already carries the 9.2 READ REPRESENTATIVE SUBSET (Instruments/MarketData/Operations/Money) — that is 9.2 scope, and SF-3 forbids this rung touching a 9.2 surface. The 3-way TRANSPORT harness (does the dial VERIFY?) is a distinct concern from the read subset (does Money decode live?). So a focused transport_live_test.exs isolates the transport classifier and keeps the diff OFF the 9.2-bearing file; the Director's Stage-5 pathspec lists both filenames, so staging only the file written is clean (drop sandbox_live_test.exs from the stage — it is NOT touched). "Reuse the infra" is honored by reusing the MECHANISMS — the @moduletag :sandbox default-exclude (test_helper.exs), the GRPC.Client.Supervisor started there, the keyless-flunk setup idiom, INV-9 secret hygiene — not by editing the 9.2 file. The harness also re-proves 9.1 G6 (open→get_accounts→close) so the genuine replacement for the corrected false-green lives in the transport file, where it belongs.

### D-14 — Apollo verdict: BUILD-GRADE. Every §11.2 prompted check PASSES on independent re-run; the genuine live PASS reproduces from a second context; the L-2 gitignore negation is the ONE precondition the Director must add to the Stage-5 pathspec.

The ten prompted checks, each PASS with Apollo-independent evidence (not Mars's word):
(1) DEFECT A realized — config.ex:35 @endpoint_default = tbank.ru:443; resolve/1 (config.ex:117-118) composes :endpoint from INVEST_API_URL+INVEST_API_PORT via resolve_endpoint/2 (config.ex:134-143); precedence explicit>env>default proven by edge probe (a distinct explicit endpoint beats env; env beats default); doctest (config.ex:70-72) updated to tbank.ru; no shim. PASS.
(2) DEFECT B realized — client.ex:213 cacerts: :public_key.cacerts_get() ++ [russian_root_ca_der()]; verify_peer (212) + depth:3 (214) + the hostname check (215-217) KEPT; russian_root_ca_der/0 (228-236) reads the PEM at runtime. PASS.
(3) the vendored root pinned to SHA-256 D2:6D:…:CF:31 — Apollo ran `openssl x509 -sha256 -fingerprint` on the on-disk PEM ITSELF → byte-for-byte the pin; subject==issuer = the self-signed Russian Trusted Root CA. NOT the server-served bytes blind. PASS.
(4) verify_none NOWHERE — grep apps/investex/lib/ EMPTY; the only two hits are client_test.exs asserting its ABSENCE. PASS.
(5) G-TLS has TEETH — Apollo independently ran THREE mutations: drop the ++ append → gate G-TLS-presence FAIL exit 1; swap a DIFFERENT valid CA → CA-provenance FAIL exit 1 (the pin fires on a valid-but-wrong cert — the anti-"trust the thing authenticated" defense); break the PEM body → client_test.exs MatchError, 3 tests/1 failure. ALL reverted byte-identical (client.ex == pre-mutation backup; PEM fingerprint back to the pin). Kill-rate 3/3 effective mutations killed. PASS.
(6) the live 3-way classifier CANNOT confuse a TLS-trust-FAIL with an egress-BLOCK — the discriminator (transport_live_test.exs:154-169) is a direct :ssl.connect with Client.tls_opts/0; {:error,{:tls_alert,alert}} → :tls_alert (FAIL=BLOCK) is a STRUCTURAL match distinct from {:error,reason} → :tcp_error (egress). Apollo PROVED the FAIL branch NON-VACUOUS by an independent LIVE :ssl.connect with the OS bundle ONLY → {:tls_alert, :unknown_ca} (a real verify-reject AFTER TCP connect). So the tag is a structural property, never a timeout heuristic; the collapse this rung closes cannot occur. PASS.
(7) the 9.1 G6 false-green corrected — trd.9.1.specs.md:378-394 [RECONCILE]-marked corrected-by-9.1.1, original L-9/L-10 history preserved; the DoD twin :425-428 + trd.progress.md corrected. PASS.
(8) INV-9 secret hygiene structural — G7 grep of the FULL surface (code + docs + ledger + gate .out) for JWT-shaped / Bearer-opaque / INVEST_TOKEN=value / raw account-id → NONE; the token value is in nothing the rung writes; .env.test is doubly gitignored (*.local). PASS.
(9) echo/mix.lock unchanged — git status mix.lock empty (SF-4). PASS.
(10) the boundary held — sandbox_live_test.exs UNCHANGED (the 9.2 file untouched, D-13); only config.ex/client.ex/config_test.exs (M) + client_test.exs/transport_live_test.exs/the rung gate (??) in the rung surface; the pre-existing untracked trd_9_2_check.{exs,out} are a PRIOR rung's, correctly NOT staged (SF-3). PASS.

THE INDEPENDENT LIVE RE-DIAL (the crux, NOT trusting Y-4): Apollo sourced .env.test into a subshell only (token never echoed/written), ran `mix test --include sandbox test/transport_live_test.exs` on a FRESH seed (566368), observed [G6′ PASS] — TLS verified against the vendored Russian root, open_account→get_accounts→close_account round-tripped, a non-empty account_id decoded (value redacted in Apollo's output), 1 test/0 failures; then unset the env. The genuine live PASS REPRODUCES from a second independent context. The SF-2 egress contingency did NOT fire (the BEAM egresses here). Secret hygiene held end-to-end.

VERDICT: BUILD-GRADE. The single Director precondition: add the L-2 scoped negation `!echo/apps/investex/priv/certs/russian_trusted_root_ca.pem` to .gitignore AND include .gitignore in the Stage-5 LAW-4 pathspec — else the commit silently drops the trust anchor the live PASS depends on. Grounding: this whole evaluation pass; the §11.2 charter; trd.9.1.1.specs.md; trd-9-1-1.progress.md.

## {trd-9-1-1-report} Report

### Y-1 — Venus Stage-1: the spec slice + the 9.1 reconcile are authored and build-grade

VERDICT: BUILD-GRADE. The Stage-1 gate is reachable — the slice is settled, internally consistent, every reconcile claim is MATCH or [RECONCILE]-marked, no open Operator fork, the harness 3-way contract is specified, the vendored-root provenance + fingerprint are in the spec.

AUTHORED (NEW):
- docs/exchange/trd.9.1.1.specs.md — authoritative; mirrors the trd.9.2.specs.md slice form. The two defects grounded at file:line; the A+B fix as the smallest correct change; SF-1..SF-4 + INV-10 + INV-11 as locked decisions; INV-10/INV-11 woven into INV-5/8/9; the gates G-TLS / G6′ / G5 / G7 each with a Given/When/Then + Liveness; the endpoint + CA provenance; the harness 3-way contract (PASS / TLS-trust-FAIL=BLOCK / egress-BLOCK=reproduced-defer, the TLS-vs-TCP discriminator, never collapse ii into iii); the Coverage table; the Definition of done.
- docs/exchange/trd.9.1.1.md — the concise chapter narrative (the two defects, the fix, the proofs, the corrected record, the boundary).

RECONCILED (6 [RECONCILE] markers; do-no-harm, history preserved):
- trd.9.1.specs.md ×4: the endpoint default (:196 → tbank.ru + env-resolution, INV-10); the Client §TLS-posture surface-pin (vendored-root reality, INV-11); the G6 record (:378-389, FALSE-GREEN corrected-by-9.1.1, the original L-9/L-10 prose kept verbatim below the marker); the DoD live-PASS line (:424, conditioned).
- trd.9.1.md ×1: the ":129 live-sandbox-verified" claim marked false-green.
- trd.progress.md ×2: the §Build-status "standing result across seeds" twin marked false-green + a new TRD.9.1.1 OPEN ledger entry (re-proves 9.1 G6, unblocks 9.2's live floor).

D-n LOCKED (8): D-1 SF-1 (vendor+pin+append, verify_peer, no verify_none) · D-2 SF-2 (3-way harness, classifier must not collapse) · D-3 SF-3 (transport-only boundary) · D-4 SF-4 (no new dep, mix.lock untouched) · D-5 INV-10 (env-resolved endpoint, default tbank.ru, precedence opt>env>default) · D-6 INV-11 (vendored+pinned root, verify_peer kept) · D-7 the harness 3-way contract · D-8 the G6 false-green correction (with the L-learning: own-liveness is necessary but not sufficient — the substrate must be independently de-risked).

KEY RECONCILE DELTA — THE G6 FALSE-GREEN (D-8): the as-built dialed the stale sinkholed …tinkoff.ru with verify_peer against an OS bundle holding 0 Russian roots, while the venue chains to a self-signed Russian Trusted Root CA — so a non-empty account_id was structurally impossible and the recorded per-seed ms timings could not have measured a completed verifying round-trip. The earlier 9.1 Apollo loop fixed a DIFFERENT false-green (the async OS-env token clobber) and correctly hardened own-liveness, but the gate's letter was satisfied by a dial the de-risk shows could not have run. The genuine G6 is re-proven by D-7's 3-way harness after A+B lands.

BOUNDARY (LAW-1a held): Venus edited ONLY docs/exchange/{trd.9.1.1.specs.md, trd.9.1.1.md, trd.9.1.specs.md, trd.9.1.md, trd.progress.md} + this ledger. ZERO echo/apps/investex/** files touched (git status echo/apps/investex/ empty). The echo_mq/** + trd_9_2_check.exs dirty state is pre-existing emq.2.2 program work, out of scope SF-3. No git run.

OPEN QUESTION for the Director's Stage-1 gate: NONE blocking — no open Operator fork (SF-1..SF-4 all settled in the runbook). One item to flag forward (not a gate-blocker): the mentor-diff lesson in D-8 (own-liveness ≠ sufficient; de-risk the substrate) is a candidate for the Mars/Apollo/Venus charters under an explicit Operator grant only (per the runbook Stage-6 fold) — surfaced, not applied.

### Y-2 — Mars-1 Stage-2 complete (build + network-free proof). DEFECT A (INV-10): Investex.Config @endpoint_default flipped tinkoff.ru→tbank.ru; resolve/1 now composes :endpoint from INVEST_API_URL(+INVEST_API_PORT, else 443) via a private resolve_endpoint/1, precedence explicit>env>default (D-1); new/1 kept pure so new([]).endpoint is the deterministic default (the doctest holds with no env); doctest + config_test.exs updated, no shim. DEFECT B (INV-11): Investex.Client.tls_opts/0 appends the vendored root's bare DER to :public_key.cacerts_get() (D-2 — bare DER not {:cert,der}, per L-1: cacerts_get/0 is mixed-shape on OTP 28.5.0.1 and :ssl accepts a raw DER alongside; the brief's "DER binaries" guess was wrong, verified against running OTP); verify_peer + depth:3 + the hostname check KEPT; moduledoc §TLS-posture + the dial comment rewritten to the vendored-root reality; tls_opts/0 promoted to @doc false so G-TLS asserts on its real output (D-3). VENDORED CERT: echo/apps/investex/priv/certs/russian_trusted_root_ca.pem, obtained from the venue's served chain and FINGERPRINT-MATCHED on disk to SHA-256 D2:6D:2D:02:31:B7:C3:9F:92:CC:73:85:12:BA:54:10:35:19:E4:40:5D:68:B5:BD:70:3E:97:88:CA:8E:CF:31 (the pin — the self-signed root, subject==issuer; provenance+pin in a header comment). G-TLS realized as both assertions (D-3 realization-over-literal: the loopback uses a SELF-CA, since the real root's private key is not ours). GATE: compile clean; Tier-1 16 doctests/6 properties/42 tests/0 failures/3 excluded; 100/100 determinism; mutation self-check proves the teeth (revert net-zero); verify_none absent from client.ex; mix.lock unchanged; boundary held. TWO BRIEF GAPS for the Director: (L-2) the PEM path is GITIGNORED by repo-root `*.pem` (.gitignore:29) — the Stage-5 pathspec MUST add a scoped negation `!echo/apps/investex/priv/certs/russian_trusted_root_ca.pem` or the commit silently drops the trust anchor; (Stage-4 note) the loopback negative case emits 2 stderr TLS [notice] lines — suppress for the clean reproducible rung .out. Stage 4 (live harness G6′ + rung gate) pending resume.

### Y-3 — Director Stage-3 solo review: BUILD-GRADE (Mars-1 Stage-2).

RECONCILE (as-built vs trd.9.1.1.specs.md). DEFECT A: @endpoint_default → sandbox-invest-public-api.tbank.ru:443; resolve/1 composes :endpoint from INVEST_API_URL+INVEST_API_PORT with precedence explicit>env>default, env read in ONE place (resolve/1) so new/1 stays pure + the doctest deterministic; the one edge (an explicit literal-default is indistinguishable from no-opt → env overrides it) is documented honestly. config_test.exs grew 5 precedence cases with save/restore of all 3 env vars (the L-9 discipline extended). DEFECT B: moduledoc TLS-posture corrected; cacerts: cacerts_get() ++ [russian_root_ca_der()]; verify_peer + depth:3 + hostname-check KEPT; russian_root_ca_der/0 reads the PEM at runtime via :code.priv_dir. Realizations (documented, accepted): tls_opts/0 exposed as @doc false def for the G-TLS test; G-TLS realized as DER-membership of the REAL root + a SELF-CA loopback (the realization-over-literal — the real Russian root's private key is not ours, so a loopback cannot chain to it; (1) proves the specific real root is wired in, (2) proves the append→trust mechanism generically + that verify_peer rejects an untrusted chain).

INDEPENDENT GATE (re-run, not Mars's word). cd echo && mix compile --warnings-as-errors: clean. cd apps/investex && mix test: 16 doctests, 6 properties, 42 tests, 0 failures, 3 excluded (the :sandbox tier); network-free (loopback only; the [notice] Unknown CA is the mechanism test correctly rejecting an un-appended chain). The held 9.2 tests (parity/fidelity/money) are in that suite and green — 9.1.1 did not disturb 9.2.

G-TLS TEETH — PROVEN by 2 mutation spot-checks (Edit-in, observe kill, revert net-zero, LAW-1a). (1) drop the ++ append → the presence test RED (assert vendored_der in cacerts; INV-11), 3 tests/1 failure. (2) corrupt the vendored PEM → MatchError no match of [] on the PEM-dependent tests, 3 tests/2 failures. Both reverted byte-identical (client.ex diff vs backup empty; the PEM fingerprint back to D2:6D:…:CF:31).

BOUNDARY held: only config.ex, client.ex, priv/certs/russian_trusted_root_ca.pem, config_test.exs, client_test.exs touched; echo/mix.lock unchanged (SF-4); verify_none absent (the two grep hits are the test ASSERTING its absence). Secret hygiene: a marker token, no real token; the PEM is a public CA cert (INV-9 unaffected). The vendored PEM fingerprint == the pin.

NO blocking findings. The forward work is Stage 4 (the 3-way live harness G6′ + the rung gate trd_9_1_1_check). The Director authored NO production code (the 2 probes reverted net-zero).

### Y-4 — G6′ LIVE VERDICT: PASS (a genuine live round-trip; NOT the SF-2 egress contingency). Ran `cd echo/apps/investex && set -a; . .env.test; set +a; TMPDIR=/tmp mix test test/transport_live_test.exs --include sandbox; unset …` (the token sourced into the subshell only, unset immediately after, never echoed/written). Result: `[G6′ PASS] venue=sandbox-invest-public-api.tbank.ru:443 — TLS verified against the vendored Russian root, open_account → get_accounts → close_account round-tripped, a non-empty account_id decoded (value not logged). 9.1 G6 re-proven; 9.2's live floor unblocked.` — 1 test, 0 failures, exit 0. So: (1) the vendored Russian Trusted Root CA VERIFIES the live venue chain from this machine (INV-11 confirmed against the real wire, not just the network-free G-TLS); (2) the BEAM egresses to the venue here — the SF-2 split-tunnel egress-BLOCK contingency did NOT fire (it remains in the harness for an egress-incapable run); (3) the 9.1 G6 false-green is now replaced by a GENUINE live PASS (the dialed-proof = a real non-empty account_id); (4) TRD.9.2's live floor is unblocked. Secret hygiene held: the harness never prints the token; the live log scrubbed clean of any token-shaped string and then deleted (transient, never committed). The committed rung-gate .out is generated network-free with NO token in env.

### Y-5 — Apollo §11.2 evaluation: BUILD-GRADE (one Director precondition: the L-2 .gitignore negation)

VERDICT: BUILD-GRADE. The TRD.9.1.1 transport fix satisfies every spec promise; the genuine live PASS reproduces from a second independent Apollo context; the build is shippable IFF the Director adds the L-2 scoped .gitignore negation to the Stage-5 pathspec (else the trust anchor silently does not ship).

POST-BUILD RECONCILE DELTA TABLE (promise → as-built file:line → verdict):
- INV-10 endpoint env-resolved, default tbank.ru → config.ex:35,37,117-118,134-143 → MATCH (independently probed: default, env-composed, explicit-opt-wins, empty-fallback all correct).
- INV-11 vendored+pinned root appended, verify_peer+depth:3 kept → client.ex:208-236 + priv/certs/russian_trusted_root_ca.pem → MATCH (fingerprint Apollo-verified = the pin; subject==issuer = self-signed root).
- G-TLS network-free trust proof (presence + mechanism) → client_test.exs:35-65,75-88 + the rung gate → MATCH (teeth proven by 3 independent mutation kills).
- G6′ 3-way live harness → transport_live_test.exs:79-201 → MATCH (Apollo re-ran live → PASS; FAIL branch proven non-vacuous).
- G5 pure suite network-free + reproducible gate → trd_9_1_1_check.exs/.out → MATCH (gate PASS 6/6 exit 0, two runs byte-identical, == committed .out; suite 16 doctests/6 properties/43 tests/0 failures/4 excluded).
- G7 no token anywhere → full surface grep → MATCH (no JWT/Bearer-opaque/INVEST_TOKEN=value/raw-id).
- D-8 9.1 G6 false-green correction → trd.9.1.specs.md:378-394,425-428 + trd.progress.md → MATCH ([RECONCILE]-marked, history preserved).
- SF-3 boundary → sandbox_live_test.exs UNCHANGED; only the rung's own files dirty → MATCH.
- SF-4 no new dep → echo/mix.lock not in the diff → MATCH.
- INV-5 client owns the channel → mix.exs no mod: → MATCH (undisturbed; only tls_opts/0 edited).
EVERY promise MATCH; zero STALE/INVENTED/MISSING-blocking.

THE INDEPENDENT LIVE RE-DIAL (the crux): Apollo sourced .env.test into a subshell only (token never echoed/written), ran the harness on a FRESH seed 566368 → [G6′ PASS]: TLS verified against the vendored Russian root, open_account→get_accounts→close_account round-tripped, a non-empty account_id decoded (value redacted), 1 test/0 failures; env unset after. 3-WAY VERDICT OBSERVED: PASS (not TLS-FAIL, not egress-BLOCK). The genuine live PASS REPRODUCES from a second context — Y-4 is independently confirmed, not taken on faith.

ADVERSARIAL CHECKS RUN:
- MUTATION KILL-RATE: 3/3 effective mutations killed — (i) drop the ++ append → gate G-TLS-presence FAIL exit 1; (ii) swap a DIFFERENT valid CA → CA-provenance pin FAIL exit 1; (iii) break the PEM body → client_test.exs MatchError 3/1. A 4th attempt (mangle one base64 line) was INEFFECTIVE (pem_decode tolerates it — counted as not-a-mutation, see L-5-B), and the SHA-256 pin catches the decoded-DER class anyway. All reverts byte-identical (client.ex == pre-mutation; PEM fingerprint back to the pin).
- ATTACK-THAT-HELD #1 (the decisive one): a LIVE :ssl.connect to the venue with the OS bundle ONLY (no vendored root) → {:tls_alert, :unknown_ca} — proving the same venue that CLEARS with tls_opts/0 is REJECTED without the vendored root, so (a) the FAIL branch is non-vacuous, (b) the classifier cannot collapse a trust-FAIL into an egress-BLOCK (the tag is structural), (c) the vendored root is load-bearing against the live wire.
- ATTACK-THAT-HELD #2: a different VALID CA vendored → the pin assertion fires (CA-provenance FAIL) — the anti-"trust the thing being authenticated" defense holds; a well-formed but wrong cert is rejected.
- ATTACK-THAT-HELD #3: keyless --include sandbox → the setup flunks loudly (1 test/1 failure, "INVEST_TOKEN is not set") — a no-op self-skip cannot satisfy the live gate (INV-8).
- DETERMINISM: 100/100 GREEN on the touched suite (client_test + config_test), uncontended.

UN-PROMPTED FINDINGS (L-5, none blocking): (A) a whitespace-only INVEST_API_URL=" " composes a malformed " :443" host — the nil/"" guard misses whitespace; NEGLIGIBLE (self-inflicted misconfig, fails the dial cleanly as an egress-block, no trust/security impact; INV-10 promises no trimming) — a candidate follow-up, surfaced not applied. (B) pem_decode tolerates a single mangled base64 line (a mutation-probe caveat). (C) function_exported? under --no-start under-reports tls_opts/0 (a probe artifact; force-load → exported: true).

SPEC-SYNC: NO [RECONCILE] needed. The spec body (Venus) is authored at the CONTRACT level and explicitly grants the realization latitude the as-built used: the harness discriminator (spec:274 "matching the gun/:ssl error term, OR a staged TCP-connect-then-TLS probe — Mars's"); the live-test file (spec:238-248 offers both, "Mars realizes ONE"); the append-shape (spec:202,210 "the DER, APPENDED"). Mars's D-9..D-13 realizations all fall WITHIN the spec's stated contract; the ledger is their correct home. Good Venus hygiene — pinned contracts, not premature implementations.

THE ONE PRECONDITION (L-2, structural, MUST be in Stage-5): `git check-ignore -v echo/apps/investex/priv/certs/russian_trusted_root_ca.pem` → `.gitignore:29:*.pem`. The Stage-5 LAW-4 commit WITHOUT a scoped negation SILENTLY DROPS the trust anchor — INV-11/SF-1 would ship without the cert the live PASS depends on (a structural false-green of the security fix). The fix Apollo verified is SAFE: a scoped negation `!echo/apps/investex/priv/certs/russian_trusted_root_ca.pem` un-ignores ONLY this one public CA cert; every other *.pem stays ignored (Apollo confirmed the blanket still catches other .pem paths). RECOMMENDATION: add that exact line to .gitignore AND add .gitignore to the Stage-5 pathspec.

FRAMING (LAW-3.1): third person, no gendered pronouns, no perceptual/interior-state verbs, no first-person narration — enforced throughout. Grounding: the full evaluation pass; trd.9.1.1.specs.md; trd.9.1.1.prompt.md; trd-9-1-1.progress.md; the §11.2 charter.

## {trd-9-1-1-learnings} Learnings

### L-1 — OTP cacerts_get/0 shape contradicts the brief; append plain DER. The brief assumed `:public_key.cacerts_get()` returns "a list of DER binaries; append the der binary". On the running OTP 28.5.0.1 it returns 167 entries that are mostly `{:cert, der}` tuples (is_binary(hd(...)) == false), and the list is NOT uniformly that shape (a probe found `ALL elements are {:cert, binary}` == false — mixed plain-DER + tuple). `:ssl`'s `cacerts` option accepts BOTH a raw DER binary and a `{:cert, der}` tuple in the same list, so the realization-over-literal is: append the vendored root as the plain DER binary obtained from `:public_key.pem_decode` (which yields `[{:Certificate, der, :not_encrypted}]` — extract `der`). The G-TLS presence assertion checks DER-membership decoupled from the wrapper, so it is robust to the mixed shape. Verified against the running OTP per the charter ("verify the exact shapes; do not guess").

### L-2 — the vendored PEM path is GITIGNORED by a repo-root blanket `*.pem` (.gitignore:29). `git check-ignore -v echo/apps/investex/priv/certs/russian_trusted_root_ca.pem` → `.gitignore:29:*.pem`. So the file lives on disk and Stage-2 builds against it, but the Stage-5 LAW-4 commit would SILENTLY DROP it — INV-11/SF-1 would ship without the trust anchor (a structural false-green of the security fix). The fix is a scoped negation in `.gitignore` (`!echo/apps/investex/priv/certs/russian_trusted_root_ca.pem`) so this one PUBLIC CA cert is committable while every other `*.pem` (private keys/secrets) stays ignored. `.gitignore` is OUTSIDE the Stage-2 edit boundary (the pathspec names config.ex/client.ex/the PEM/the two tests), so this is reported to the Director as a brief gap to fold into the Stage-5 pathspec — Mars does not edit `.gitignore` this stage.

### L-3 — REMEDIATE for Mars-2 (the G6′ classifier crux). The live harness classifies on Investex.Client.start_link/1's return: {:ok, pid} → open_account → get_accounts → close_account = PASS (a non-empty account_id, the INV-8 dialed-proof; re-proves 9.1 G6, unblocks 9.2). {:error, {:dial_failed, reason}} (init's {:stop, {:dial_failed, reason}} surfaces here) → classify `reason`: a TLS-LAYER shape ({:tls_alert, _} / unknown_ca / a cert error AFTER TCP connect) = TLS-trust FAIL = BLOCK (the vendored root is wrong — NEVER deferred); a TCP-LAYER shape (:timeout / :closed / :econnrefused / :nxdomain / gun down before any TLS) = egress-BLOCK = reproduced-defer (SF-2). The classifier MUST NOT collapse a TLS-FAIL into an egress-BLOCK — that collapse is the exact false-green class this rung closes. CAVEAT: client_test.exs's tls_rejection?/1 string-matches "certificate" loosely — fine for the network-free loopback (a known :ssl shape), but the LIVE classifier must match the actual gun/grpc {:dial_failed, reason} term — VERIFY the real shape the as-built dial returns (a probe / an inspect of the reason) BEFORE asserting on it, do not guess. Reuse the existing sandbox_live_test infra: the GRPC.Client.Supervisor started in test_helper.exs, the @tag :sandbox default-exclude, the keyless setup flunk, INV-9 (no raw account-id/token in the .out or ledger).

### L-4 — the as-built Investex.Client.start_link/1 is NOT a clean 3-way classification seam (L-3's assumption falsified by probe). grpc 0.11.5's gun adapter (deps/grpc/lib/grpc/client/adapters/gun.ex:63-79) dials with `retry: @max_retries` (100) + an exponential `retry_fun` (1.6^n seconds, capped 120s), and reads the result via `:gun.await_up/1`. Consequences proven by probe: (a) BOTH a TCP failure (closed port 127.0.0.1:9) AND a TLS verify-reject (example.com:443 with empty cacerts) surface from GRPC.Stub.connect as the SAME outer exit `{:noproc, {:gen_server, :call, [:gun_conns_sup, {:start_child, …}], :infinity}}` under --no-start, and as an `(EXIT from #PID)` that CRASHES the caller — start_link does not cleanly return `{:error, {:dial_failed, tls_alert}}` vs `{:error, {:dial_failed, econnrefused}}`; (b) under retry:100 a verify-reject is retried like a connection failure, so a bounded-timeout wait would TIME OUT — which would COLLAPSE a TLS-trust-FAIL into an egress-BLOCK, the exact false-green this rung closes. Therefore the classifier's TLS-vs-TCP discriminator CANNOT come from the client's dial term. Verified against running OTP/grpc per the Director's L-3 caveat ("verify the real shape, do not guess").

### L-5 — Apollo un-prompted findings (none blocking): the whitespace-URL edge, the pem_decode tolerance, the function_exported? artifact

THREE findings the prompted-checks table did not name, surfaced per the §11.2 charter (≥1 un-prompted finding). None blocks; each carries its cost.

(A) THE WHITESPACE-ONLY INVEST_API_URL EDGE (a real robustness gap, NEGLIGIBLE severity, a candidate follow-up — NOT a blocker). resolve_endpoint/2 (config.ex:139-143) guards `nil` and `""` (falls back to @endpoint_default), but a WHITESPACE-ONLY value slips through: Apollo probed `INVEST_API_URL=" "` → endpoint composed as `" :443"` (a space host). COST, named: it requires an operator to DELIBERATELY set the URL to a single space (a self-inflicted misconfiguration, not an attack surface, not a security issue); a `" "` host yields a clean dial failure (:nxdomain/:timeout) that the harness correctly classifies as an egress-BLOCK — never a silent wrong-venue dial and never a trust bypass. The spec's INV-10 contract pins nil/empty fallback + the resolved-env + explicit-opt cases; it does NOT promise whitespace-trimming, so this is MISSING-from-spec robustness, not a STALE or a contract violation. Candidate fix (a later rung's nicety): trim, or treat a whitespace-only URL as empty (`String.trim(host) == ""` → default). Surfaced, not applied (Apollo edits no production code).

(B) :public_key.pem_decode TOLERANCE (a measurement caveat for future mutation-probes). Apollo's first corrupt-PEM mutation (mangle a single mid-body base64 line) did NOT change the decoded DER — the gate still PASSED 6/6. pem_decode re-frames / skips a single malformed base64 segment, so a one-line edit is an INEFFECTIVE mutation. The EFFECTIVE kills are: a wholesale cert SWAP (a different valid CA → CA-provenance pin FAIL) or a BODY break that makes pem_decode return [] (→ the [{:Certificate,der,_}] match MatchError). LESSON for the evaluator craft: a mutation must be VERIFIED to actually alter the decoded artifact before counting it — a mutation that the parser silently tolerates proves nothing about the gate. (This strengthens, not weakens, the gate finding: the SHA-256 pin catches ANY decoded-DER change, which is why the wrong-CA swap fired.)

(C) function_exported?/3 UNDER --no-start IS A PROBE ARTIFACT, NOT A DEFECT. Apollo's first export check read `function_exported?(Investex.Client, :tls_opts, 0)` → false under `mix run --no-start -e`, which could be misread as "tls_opts/0 is not VM-public" (undercutting the G-TLS teeth). It is an artifact: the module was loaded-but-not-ensured in that -e context. After `Code.ensure_loaded(Investex.Client)` → exported: true, and a direct `Investex.Client.tls_opts()` returns a keyword list. tls_opts/0 IS genuinely `@doc false def` (client.ex:208-210) and VM-public; the gate's first act calls it successfully. LESSON: `function_exported?` under-reports on a not-yet-loaded module — force-load before asserting non-export, or the artifact reads as a false defect.

Grounding: this evaluation pass; config.ex:139-143; client.ex:208-210; the §11.2 un-prompted-finding mandate.
