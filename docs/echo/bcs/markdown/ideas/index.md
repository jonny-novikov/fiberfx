# B1 · Ideas Behind — the conceptual floor

> Route: `/bcs/ideas` (chapter landing, B1). The route-mirror source-of-record for the chapter landing. Part I
> of the BCS manuscript, taught as six modules; every figure on every page is quoted verbatim from a committed
> output. Build stamp: `BCS0NtM2LGoYoC`.

## Hero

Kicker: `B1 · IDEAS BEHIND — manuscript Part I`. Title: **The conceptual floor.** Lede — Part I is the floor
every later Part stands on: the law in three clauses, the identity contract read as architecture, the storage
economics that priced the contract, the ECS arc that distribution completes, the time inside the name, and the
encode trial that closes it. Heronote — the manuscript states Part I as assertion and argument; Part II begins
demonstrating. Each module here teaches one written chapter, with its committed evidence on the page.

### The Part I arc (interactive SVG)

Six nodes — B1.1 → B1.6 — connected as the reading order. Select a node to read its thesis in the readout; the
SVG dims the rest. Degrades to a static labelled arc without JavaScript.

## §1 · The arc (#arc)

- **B1.1 · The System Substrate** (`system-substrate`) — the law, executable: a boundary gate, a private-ETS
  property store, a supervisor; six gates refuse the canonical crimes on stage; the transcript reads `PASS 6/6`.
- **B1.2 · The Identity Contract, Read as Architecture** (`identity-contract`) — the contract property by
  property: namespace, order theorem, hash32 placement, canon — each read against the failure it retires.
- **B1.3 · Choosing the ID System** (`id-system`) — the chooser, settled by measurement on Valkey 8.1's new
  hash table: seven key shapes, two engines, a million keys each; the branded form ties binary UUID-16 at 65
  bytes and beats its own decimal.
- **B1.4 · From ECS to BCS** (`ecs-to-bcs`) — what distribution changes: the index-handle's three deaths (save
  file, socket, foreign store), each a missing contract property; the ECS→BCS translation table.
- **B1.5 · The Time Inside the Name** (`time-inside-the-name`) — the 41-bit horizon (September 2093), the law of
  two clocks, the monotonic mint floor, the store's TTL as a third clock.
- **B1.6 · Branding Beats Its Own Integer** (`branding-beats-its-own-integer`) — the encode trial in five
  runtimes: C 2.8×, Rust 4.2×, the BEAM a tie, the one V8 loss where the contract prescribes the wasm crossing.

## §2 · The floor under the floor (#evidence)

Part I is fully written, and every chapter is backed by committed evidence — the ethic the course inherits page
by page. One sample, from the first rung (`content/bcs1.1.md`, quoting `bcs_rung_1_1_check.out`): the smallest
faithful system refused the architecture's six canonical crimes on stage, `PASS 6/6`.

## §3 · Up next (#upnext)

The chapters that follow, built as the manuscript's Parts ship: B2 · The Elixir BCS Core (Part II) · B3 · The
Bus (Part III) · B4 · EchoCache · B5 · Go · B6 · Node 22+ · B7 · Production on Fly · B8 · The Trading System.
(Non-anchor `soon` cards.)

## §4 · The doors (#doors)

- `/echomq` — the bus protocol B3 narrates, taught rung by rung.
- `/redis-patterns` — the substrate patterns under the bus.
- `/elixir` — the Portal engine and the umbrella where `echo_data` lives.

## References (#refs)

Sources: Erlang/OTP ets docs · the Go share-memory codewalk · Söderqvist — A new hash table · Lamport — Time,
Clocks, and the Ordering of Events · King — Announcing Snowflake.
Related: `/bcs` (course home) · `/echomq` · `/redis-patterns` · `/elixir`.

## Pager

Previous: `/bcs` — the course landing. Next: B1.1 · The System Substrate (`/bcs/ideas/system-substrate`, once
built; a non-anchor until then).
