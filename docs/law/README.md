Я продолжаю работу над курсом III «Право повседневной жизни» (право РФ, на русском). К сообщению приложен архив `law-course-toolkit.zip` — это весь комплект и текущее состояние курса.

## Шаг 0 — распаковать и прочитать
Распакуй архив и в начале **прочитай incrementally**:
1. `law-status.md` — текущее состояние, метрики, дорожная карта (что уже построено).
2. `law-course-design.md` — архитектура 6 глав × 6 модулей, поглавный разбор с практиками, палитра, и **§6 «Честность и дисклеймеры» (критично для юр-курса)**.
3. `course-build-playbook.md` — ОБЩИЕ механики (CSS-токены `:root`, шрифты, движок квиза, KaTeX-правила, Quality Gates, паттерн навигации). Не дублируй их — бери оттуда.
4. `law-references.md` — реальные источники по главам.

## Что это за курс
Тезис: школьное обществознание + читательская грамотность + базовая арифметика — аппарат, чтобы не подписывать невыгодные договоры, защищать права потребителя и понимать семейное право. Одно предложение: «взрослый подписывает ~30 юридически значимых документов в год, не читая большинства». URL-namespace `/law`. 6 глав × 6 модулей = 36. Слаги глав и темы: `dogovor`(slate) · `potrebitel`(gold) · `trud`(copper) · `semya`(burgundy) · `nasledstvo`(plum) · `pretenziya`(jade, капстоун курса).

## Что уже готово (HTML 14, core-модулей 1/36)
- Главная курса `law/index.html` (`/law`).
- 6 лендингов глав `law/{глава}/index.html`. **Гл.2 `potrebitel` и Гл.3 `trud` — интерактивные** (липкая навигация по секциям + виджет: дерево «можно ли вернуть?» и калькулятор сверхурочных). Остальные 4 — сгенерированы (статичные), их можно так же апгрейдить.
- 6 страниц «Квиз главы» `law/{глава}/kviz.html`.
- Модуль 1.1 `law/dogovor/sila.html` «Зачем читать договор».

## Утилиты (в `law/`)
- `verify_numbers.py` — **арифметика калькуляторов** (алименты 1/4·1/3·1/2, обязательная доля 1/2, сверхурочные 1,5×/2×, отпуск 28, сроки) + справочные правовые константы. Перед любым модулем с расчётом: проверь формулу здесь, потом зеркаль в JS. Запуск: `python3 law/verify_numbers.py`.
- `build_chapter_quiz.py`, `build_chapter_landing.py` — генераторы (тема + конфиги). Генерируй в рабочую папку: `OUT_BASE=/home/claude/work/law python3 law/build_chapter_quiz.py`, затем `cp` в выдачу.
- `preflight.py` — скан: `python3 law/preflight.py 'law/*.html' 'law/*/*.html'`.

## Рабочий цикл на каждый модуль/страницу (обязательно)
verify_numbers.py (если есть калькулятор) → сборка → `preflight.py` → **toolkit-валидация** → синк в выдачу → `present_files` → обновить `law-status.md` (+ запись в журнал).

## Валидация — 0 image budget (важно)
НЕ открывай PNG через `view` (бюджет изображений почти исчерпан). Проверяй через headless-валидатор по DOM/`computedStyle` (вывод в stdout, без скриншотов):
```
BASE_URL="file:///abs/path/law" NODE_PATH="<...>/node_modules" node suite.X.js
```
Класс `Validator` в `toolkit/validator.js`: `open/title/noKatexErrors/noHorizontalOverflow/expectText/expectCount(sel,op,n)/expectVisible/fill/click/settle/report`. Пример — `toolkit/suite.example.js`. Playwright ставится в окружении (`npm i -D playwright@1.56 && npx playwright install chromium` при необходимости).

## Критичные правила (не нарушать)
- **Честность юр-курса (главное):** на КАЖДОЙ странице — дисклеймер «образовательный материал, не юридическая консультация; право РФ; нормы по состоянию на момент — сверяйте действующую редакцию и при необходимости обращайтесь к юристу». Ссылайся только на реальные кодексы (ГК/ТК/СК РФ, ЗоЗПП №2300-1, ГПК РФ) и официальные порталы (pravo.gov.ru, КонсультантПлюс, Гарант, Роспотребнадзор, нотариат, ФНС). Не выдумывай номера статей. Калькуляторы — иллюстративные (явный `.disclaimer`). Госпошлину/ставки не хардкодить — ссылка на актуальный калькулятор. Шаблоны — «образец».
- **KaTeX strict:** `strict:false, throwOnError:false`. Внутри `$...$` НЕЛЬЗЯ символы `№ « » " " „ … ₽` и нелатинский текст-пунктуацию. Проценты в HTML — `\%`, в JS-литералах — `\\%`; вне математики — слово или обычный «%». Запятая в числах — `{,}`. ₽ — только в обычном тексте (лучше «руб.»).
- **Вложенные `<a>` запрещены:** карточки/тайлы-обёртки с внутренними ссылками — это `<div>`, не `<a>` (иначе браузер дублирует DOM). Проверяй: число `.ch-card`/`.tile` должно быть ровно 6.
- **Десктоп-шрифт:** на каждой странице `@media (min-width:1024px){html{font-size:18px}body{font-size:18.5px}} @media (min-width:1440px){html{font-size:19px}}`.
- **Навигация:** дидактические модули и интерактивные лендинги — липкая `.section-nav` под топбаром (`top:72px`, подсветка активной секции через IntersectionObserver; секции `id="s1.."` + `scroll-margin-top:126px`) + внизу `.nav-prev-next` и «↑ Наверх» (`<body id="top">`).
- **localStorage:** префикс ключей в этом курсе — `law-c{N}-...` (НЕ `lg-...`, чтобы не было коллизии с курсом логики).
- **Окружение:** запись напрямую в выдачу иногда даёт OSError errno 5 — генерируй в `/home/claude/work/law`, валидируй там, потом `cp` в выдачу. `create_file` падает, если файл существует — сначала `rm`.

## Что делать дальше
Темп — **2 модуля на запрос** (я смотрю между итерациями). Дидактический модуль = hero → 6 секций `.sect` → интерактив `.task-block` (+`.disclaimer`) → `.quote-block` → `.takeaway` (5 пунктов I–V) → `.quiz` (5, с `.law-note`) → `.references` (3 реальных, с пометкой редакции) → нижняя навигация; сверху — навигация по секциям. Тема главы из палитры. Числа — сперва в `verify_numbers.py`.

Начни с подтверждения, что прочитал `law-status.md` и `law-course-design.md`, кратко перечисли текущее состояние и предложи следующую пару модулей (по плану — **1.2 «Анатомия договора» + 1.3 «Красные флаги»** Главы 1; их интерактивы — разметка частей договора и чек-лист «красных флагов»). Жди моего «ок», затем строй.