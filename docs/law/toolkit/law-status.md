# Курс III · «Право повседневной жизни» — статус сборки

**Обновлён:** старт проекта · **построено core-модулей:** $1/36$ · лендинги глав и квизы — готовы · **глав завершено:** $0/6$

> Живой трекер. Спецификация — `law-course-design.md`; источники — `law-references.md`; общие механики — `course-build-playbook.md`. Утилиты — в `law/`.
>
> **Дисклеймер курса:** образовательный материал, не юридическая консультация; право РФ; нормы — по состоянию на момент подготовки, сверять действующую редакцию.

---

## Сводка

| Метрика | Значение |
|---|---|
| Core-модулей построено | $1 / 36$ ($3\%$) |
| Глав полностью готовых | $0 / 6$ |
| Лендингов глав | $6 / 6$ ✓ (лендинг курса `/law` — позже) |
| Секций «Источники» | $1$ |
| Квизов модулей | $1$ |
| Страниц «Квиз главы» | $6 / 6$ ✓ — сгенерированы и валидированы |
| HTML-файлов курса | $13$ (6 лендингов + 6 квизов + 1 модуль) |
| Утилит готово | $4$ (`verify_numbers.py` ✓, `build_chapter_quiz.py`, `build_chapter_landing.py`, `preflight.py`) |

---

## Карта глав (цвет / слаг / статус)

| Гл. | Слаг | Тема | Статус |
|----|------|------|--------|
| $1$ | `dogovor` | slate | ◐ $1/6$ ($1.1$ ✓) |
| $2$ | `potrebitel` | gold | ☐ $0/6$ |
| $3$ | `trud` | copper | ☐ $0/6$ |
| $4$ | `semya` | burgundy | ☐ $0/6$ |
| $5$ | `nasledstvo` | plum | ☐ $0/6$ |
| $6$ | `pretenziya` | jade | ☐ $0/6$ (капстоун курса → баннер завершения) |

Обозначения: ☐ не начато · ◐ в работе · ★ глава завершена.

> **Лендинги всех 6 глав** (`/law/{слаг}`) и страницы **«Квиз главы»** сгенерированы и валидированы (toolkit $102/102$, $0$ изображений, дисклеймеры на месте). Ждут лендинга курса `/law` и модулей.

---

## Готовый стартовый комплект (утилиты)

- **`law/verify_numbers.py`** ✓ — арифметика калькуляторов (алименты, обязательная доля, сверхурочные, отпуск, сроки) $+$ справочные правовые константы. Прогон: **$15$ проверок**. Константы помечены «сверять действующую редакцию».
- **`law/build_chapter_quiz.py`** ✓ — генератор страниц «Квиз главы» ($6$ вопросов по модулям, тема главы, цепочка).
- **`law/build_chapter_landing.py`** ✓ — генератор лендингов глав (hero $+$ 3 intro $+$ 6 тайлов $+$ цепочка).
- **`law/preflight.py`** ✓ — pre-flight скан по `law/*.html` и `law/*/*.html`.

---

## Открытые вопросы / правила

- **Актуальность норм.** Перед публикацией каждого модуля — сверить нормы и сроки с действующей редакцией кодекса (pravo.gov.ru, КонсультантПлюс/Гарант). Это главный риск юр-курса.
- **Госпошлина и ставки** (Гл. $6.4$): не хардкодить — вести на актуальный калькулятор суда/ФНС.
- **Дисклеймеры обязательны** в каждом модуле и калькуляторе (см. §6 дизайн-дока).
- **Переиспользование из курса II**: навыки критического чтения (Гл. $5$ курса логики) — для «красных флагов» договора (Гл. $1$).

---

## Дорожная карта (темп — $2$ модуля на запрос)

1. Лендинг курса `/law` $+$ $6$ лендингов глав.
2. Гл. $1$ `dogovor` ($1.1$–$1.6$).
3. Гл. $2$ `potrebitel` → Гл. $3$ `trud`.
4. Гл. $4$ `semya` → Гл. $5$ `nasledstvo`.
5. Гл. $6$ `pretenziya` (капстоун курса).
6. Страницы «Квиз главы» (генератором) $+$ ссылки в лендингах.

Цикл модуля: `verify_numbers.py` (если есть калькулятор) → сборка с дисклеймером и источниками → `preflight.py` → toolkit ($0$ изображений) → синк → `present_files` → обновить этот файл.

---

## Журнал (новое сверху)

- **Исправлены устаревшие ссылки на курс логики.** В сгенерированных лендингах/квизах были: цепочка глав логики внизу (ссылки `/law/iskazheniya` и т. п.), текст «Логика» в хлебных крошках и `Курс · Логика` в шапке — из-за несработавшей регулярки `CHAIN` и хардкода в шаблоне. Оба генератора поправлены точными заменами (CHAIN → главы права, «Логика» → «Право», `/logic/` → `/law/`); $12$ страниц перегенерированы. Проверка: $0$ логик-ссылок, цепочка ведёт на `/law/{глава}`, крошки «Право». Также исправлен **префикс localStorage** квизов: `lg-c{N}-kviz` → `law-c{N}-kviz` (была коллизия прогресса с курсом логики на одном домене). Toolkit $102/102 + 5/5$ анти-регрессия.
- **Модуль $1.1$ «Зачем читать договор» построен ✓ (Гл. $1$ → $1/6$).** `/law/dogovor/sila` — дидактический, тема slate: $6$ секций (сила подписи, ~30 документов в год, что прячется в непрочитанном, привычка читать), интерактив-**самоаудит** «что подписал не глядя» ($9$ пунктов $+$ рефлексия), квиз ($5$), источники (ГК РФ). Обе фичи навигации: **секции в шапке** (подсветка активной) и **нижняя навигация** (пред./след. $+$ «↑ Наверх»). **Правовой дисклеймер**-баннер сверху и в квизе. prev → лендинг главы, next → $1.2$. Toolkit $17/17$, $0$ изображений.
- **Лендинги и квизы 6 глав сгенерированы ✓.** Запущены `build_chapter_landing.py` → `/law/{слаг}/index.html` (hero с цитатой-принципом, 3 intro, 6 тайлов-модулей, цепочка глав, **дисклеймер-баннер**) и `build_chapter_quiz.py` → `/law/{слаг}/kviz.html` (6 вопросов по модулям, **дисклеймер**). Темы: slate/gold/copper/burgundy/plum/jade. Тайлы ведут на модули (пути готовы, страницы — позже); ссылки на квизы работают. Pre-flight $0$ проблем, toolkit $102/102$, $0$ изображений. HTML-файлов: $12$.
- **Старт проекта.** Создан стартовый комплект: спецификация (`law-course-design.md` — архитектура $6\times6$, поглавный разбор с практиками, палитра, усиленный раздел честности/дисклеймеров), источники (`law-references.md` — кодексы $+$ официальные порталы), этот трекер; утилиты (`verify_numbers.py` — $15$ проверок ✓, `build_chapter_quiz.py`, `build_chapter_landing.py`, `preflight.py`). Курс ещё не верстался — готов фундамент.
