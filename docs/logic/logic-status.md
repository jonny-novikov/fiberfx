# Курс II · «Логика и принятие решений» — статус сборки

**Обновлён:** старт проекта · **построено core-модулей:** $23/36$ ($64\%$) · **глав завершено:** $3/6$

**Публикация:** опубликовано на https://jonnify.fly.dev/logic — часть серии **jonnify** (девять курсов: школьная математика и геометрия, ЕГЭ, финансы `/edu`, здоровье, право, логика, Elixir, «Образование будущего»; карта — `llms.txt`, полный список — `/sitemap.xml`). Этот архив — авторский исходник и инструментарий для продолжения.

> Живой трекер. Спецификация — `logic-course-design.md`; источники — `logic-references.md`; общие механики — `course-build-playbook.md`. Утилиты — в `logic/`.

---

## Сводка

| Метрика | Значение |
|---|---|
| Core-модулей построено | $23 / 36$ ($64\%$) |
| Глав полностью готовых | $0 / 6$ |
| Лендингов глав | $6 / 6$ ✓ · главная курса `/logic` ✓ |
| Секций «Источники» | $23$ |
| Квизов модулей | $20$ |
| Страниц «Квиз главы» | $6 / 6$ ✓ — сгенерированы и валидированы ($42/42$, $0$ изображений) |
| HTML-файлов курса | $38$ (главная + 6 лендингов + 6 квизов глав + 23 модуля + финальные квизы Гл. 1 и курса) |
| Утилит готово | $4$ (`verify_math.py`, `build_chapter_quiz.py`, `build_chapter_landing.py`, `preflight.py`) |

---

## Карта глав (цвет / слаг / статус)

| Гл. | Слаг | Тема | Статус |
|----|------|------|--------|
| $1$ | `iskazheniya` | burgundy | ✓ $6/6$ ($1.1$–$1.6$ ✓) |
| $2$ | `veroyatnost` | gold | ✓ $6/6$ ($2.1$–$2.6$ ✓) |
| $3$ | `bayes` | blue | ◐ $5/6$ ($3.1$–$3.5$ ✓) |
| $4$ | `igry` | plum | ☐ $0/6$ |
| $5$ | `dannye` | slate | ☐ $0/6$ |
| $6$ | `resheniya` | jade | ✓ $6/6$ ($6.1$–$6.6$ ✓; капстоун курса + баннер завершения ✓) |

Обозначения: ☐ не начато · ◐ в работе · ★ глава завершена.

> **Лендинги всех $6$ глав** (`/logic/{слаг}`) и страницы **«Квиз главы»** (`logic/{глава}/kviz.html`, $6$ вопросов по модулям, тема главы) и валидированы. Они ждут лендингов глав, чтобы заработала навигация «К Главе N».

---

## Готовый стартовый комплект (утилиты)

- **`logic/verify_math.py`** ✓ — верификатор математики курса ($E[X]$, дисперсия, Байес, $ARR/RRR/NNT$, дилемма заключённого $+$ итеративная игра, комбинаторика, $CI$, дерево решений). Прогон: **$22$ проверки пройдены**.
- **`logic/build_chapter_landing.py`** ✓ — генератор лендингов глав (hero $+$ 3 intro-карточки $+$ 6 тайлов-модулей $+$ цепочка глав), тема главы, крупный шрифт. Сгенерировал все $6$ лендингов.
- **`logic/build_chapter_quiz.py`** ✓ — генератор страниц «Квиз главы» ($6$ вопросов по модулям, тема главы, цепочечная навигация). Засеян $6$ главами; генерирует `/logic/{глава}/kviz.html`.
- **`logic/preflight.py`** ✓ — pre-flight скан (KaTeX strict-чистота, смешанные скрипты, кросс-ссылки, JS-парс) по `logic/*/*.html`.

---

## Открытые вопросы / выравнивания

- **Гл. $2$, модуль $2.3$ «$9$ классов решений»**: ядро ссылается на $9$ классов из **финкурса** — при сборке выровнять с фактическим списком финкурса. До выравнивания держать как «$9$ архетипов ставок/решений».
- **Переиспользование из курса I**: Гл. $5$ модуль $5.4$ (относительный/абсолютный, $NNT$) и Гл. $3$ (Байес, sens/spec) — переносить компоненты из `health/risk/*`, не изобретая заново.

---

## Дорожная карта (темп — $2$ модуля на запрос)

1. Лендинг курса `/logic` $+$ $6$ лендингов глав.
2. Гл. $1$ `iskazheniya` ($1.1$–$1.6$).
3. Гл. $2$ `veroyatnost` → Гл. $3$ `bayes` (математическое ядро).
4. Гл. $4$ `igry` → Гл. $5$ `dannye`.
5. Гл. $6$ `resheniya` (капстоун курса).
6. Страницы «Квиз главы» (генератором) $+$ ссылки в лендингах.

Цикл модуля: `verify_math.py` → сборка → `preflight.py` → toolkit-валидация ($0$ изображений) → синк → `present_files` → обновить этот файл.

---

## Журнал (новое сверху)

- **Глава 3: построены модули $3.4$ и $3.5$ — глава на пороге закрытия (◐ $5/6$).** **$3.4$ «Последовательное обновление»** `/logic/bayes/posledovatelnoe` (blue): в quote-row **аккумулятор улик** — снэп-слайдер по четырём стопам ($x = 40/112/184/256$, центр однозначно у стопа «+2»): тот же тест $90\%/9\%$ из $3.3$, применённый трижды, ведёт $1\% \to 9{,}2\% \to 50{,}3\% \to 91{,}0\%$ (шансы $1{:}99 \to 10{:}99 \to 100{:}99 \to 1000{:}99$); секции: цепочка (апостериорная = новый приор) / тест трижды + **шансовая форма** $O \times LR$ ($LR = 10$, $4$ чипа) / интерактив / независимость улик (двойной счёт, эхо-камеры, проверка «узнал бы второй, если бы первый молчал?») / две патологии (упрямство-консерватизм Эдвардса и капитуляция) / итог; refs Jaynes 2003, Edwards 1968, Tetlock–Gardner 2015; `lg-c3-posledovatelnoe-*`. **$3.5$ «Сила улик»** `/logic/bayes/sila-ulik`: в quote-row **весы правдоподобий** — фиксировано $P(E \mid H) = 90\%$, бегунок задаёт $P(E \mid \neg H)$ ($1$–$90\%$): живые $LR = 90/x$, вердикт (тяжёлая $\ge 10$ / умеренная $\ge 3$ / слабая $\ge 1{,}5$ / почти пустая) и сдвиг от равных шансов $LR/(LR+1)$; старт $5\% \to LR = 18$, «тяжёлая», $94{,}7\%$; центр $45\% \to LR = 2$, «слабая», $66{,}7\%$; секции: громкость не вес / $LR$ + чипы (зонт $18$, тест $10$, «не ответил» $\approx 1$) / интерактив / пустышки и «собака, которая не лаяла» ($LR^- = 0{,}1/0{,}91 \approx 0{,}11$ — ослабление в $9$ раз) / ошибка прокурора до винтиков (Thompson–Schumann 1987) / итог; refs Good 1950, Kass–Raftery 1995, Thompson–Schumann 1987; `lg-c3-sila-ulik-*`. Вся математика в `verify_math.py` (Гл.3.4, Гл.3.5: цепочка, коммутативность, снэп-стопы, $LR$-вердикты). Toolkit $41/41$, $0$ изображений. Гл. $3$ → ◐ $5/6$, core $23/36$ ($64\%$), HTML: $38$. Остался капстоун $3.6$ «Синтез: менять мнение».

- **Глава 3: построены модули $3.2$ и $3.3$.** **$3.2$ «Формула Байеса»** `/logic/bayes/formula` (blue): слайдер приора с живым пересчётом — пример с мокрым зонтом (приор $30\%$, правдоподобия $90\%/5\%$ → апостериорная $0{,}27/0{,}305 = 88{,}5\%$; центр слайдера $50\% \to 94{,}7\%$), бары «вера до / вера после»; секции: зачем динамика после $3.1$ / формула $P(H|E)=P(E|H)P(H)/P(E)$ со знаменателем полной вероятности / интерактив / натуральные частоты Гигеренцера ($270/35$ из $1000$ дней → те же $88{,}5\%$) / инверсная ошибка $P(E|H) \ne P(H|E)$ (мост к $3.3$ и $3.5$) / итог; refs Bayes–Price 1763, Gigerenzer–Hoffrage 1995, McGrayne 2011; `lg-c3-formula-*`. **$3.3$ «Базовая ставка»** `/logic/bayes/bazovaya-stavka`: слайдер базы $0$–$50\%$ (тест $90\%/9\%$) с живым счётом тревог из $1000$ (база $1\%$: $9$ истинных против $89$ ложных → $9{,}2\%$; центр $25\%$: $225/68 \to 76{,}9\%$) и засечкой порога фифти-фифти $b = 0{,}09/0{,}99 \approx 9{,}1\%$; секции: парадокс (Эдди: врачи ошибались на порядок) / разбор в частотах / интерактив / порог доверия и группы риска / база в жизни (детектор лжи, такси-задача Канемана–Тверски, бытовая версия) / итог (мост к $3.4$); refs Eddy 1982, Kahneman–Tversky 1973, Gigerenzer 2002; `lg-c3-bazovaya-stavka-*`. Вся математика в `verify_math.py` (Гл.3.2, Гл.3.3). Toolkit $40/40$, $0$ изображений. Гл. $3$ → ◐ $3/6$, core $21/36$ ($58\%$), HTML: $36$.

- **Глава 6 ЗАКРЫТА ($6/6$): построен модуль $6.1$ «Зачем фреймворки»** `/logic/resheniya/zachem` (jade). В quote-row — **шкала веса решения**: drag-слайдер $0$–$100$ переключает зону рекомендации (Интуиция → Один инструмент → Полный конвейер, пороги $33/66$) с примерами, и живой бар «цена ошибки» против постоянной «цены анализа» (порог окупаемости $25$) с вердиктом. Секции: два режима (интуиция = сжатый опыт Системы 1) / условия Канемана–Клейна 2009 (регулярная среда + быстрая обратная связь) / интерактив «Чутьё или процедура» / критерий окупаемости (цена ошибки × P > цена анализа; мост к $2.2$–$2.3$) / простые правила (Meehl 1954, Dawes 1979) / итог + карта главы. Refs Kahneman–Klein 2009, Meehl 1954, Dawes 1979; storage `lg-c6-zachem-*`; математика зон/порогов в `verify_math.py` (Гл.6.1). Toolkit $19/19$, $0$ изображений. **Сквозная проверка замыкания главы:** все $8$ страниц (лендинг, квиз, $6$ модулей) + финал курса на месте; цепочка prev/next двунаправленно цела по всем пяти парам; $6$ тайлов лендинга бьют в существующие файлы; $6.6$ → `/logic/final` ✓. Итог: глав завершено $3/6$ (Гл. $1$, $2$, $6$), core $19/36$ ($53\%$), HTML: $34$.

- **Глава 6: построены модули $6.4$–$6.6$ + финальный квиз курса.** «Финальный квиз» интерпретирован как **квиз всего курса** (по образцу `final.html` Гл. 1, но уровнем выше): страница `/logic/final`, $12$ вопросов ($2$ на главу), storage `lg-final-quiz`, плашка результата с градациями, чипы повторения глав; на него ведёт CTA капстоуна. **$6.4$ «Инверсия Мангера»** `/logic/resheniya/inversiya`: флип-карта — drag-слайдер сжимает карточку по $|\cos|$ и на середине переворачивает ЦЕЛЬ → АНТИ-ЦЕЛЬ; секции: асимметрия (провал конкретнее успеха) / приём в $3$ шага / интерактив / premortem Клейна (проспективный взгляд: $\approx +30\%$ причин, Mitchell–Russo–Pennington 1989) / анти-чеклист / итог; refs Munger 2005, Klein 2007 HBR, Mitchell et al. 1989; `lg-c6-inversiya-*`. **$6.5$ «Predict &amp; verify»** `/logic/resheniya/predict-verify`: слайдер уверенности с живыми штрафами Брайера $(1-p)^2$ / $p^2$ ($80\% \to 0{,}04/0{,}64$; $50\% \to 0{,}25/0{,}25$); журнал из $4$ полей, пример счёта $0{,}22$ против базы $0{,}25$, демонстрация proper scoring (вера $0{,}7$: честность $0{,}21$, бравада и трусость по $0{,}25$); refs Brier 1950, Tetlock–Gardner 2015, Mellers et al. 2014; `lg-c6-predict-*`. **$6.6$ «Синтез: личная система»** `/logic/resheniya/sintez` (капстоун: $5$ секций, без takeaway/квиза): конвейер из $5$ снэп-шагов (классифицируй → инвертируй → дерево → три горизонта → прогноз), рекап-грид $6$, интерактив «какой инструмент?» ($5$ ситуаций × $5$ опций), границы системы, финальная CTA (квиз главы + финальный квиз курса) и **баннер завершения курса** с чипами $6$ глав; refs HKR 1999, Kahneman 2011, Tetlock–Gardner 2015; `lg-c6-sintez-game`. Вся математика в `verify_math.py` (Гл.6.4–6.6 + финал). Toolkit: $39/39$ ($6.4$–$6.5$) + $27/27$ ($6.6$ + финал), $0$ изображений. Гл. $6$ → $5/6$ (остался только $6.1$), core $18/36$ ($50\%$), HTML: $33$.

- **Глава 6: построены модули $6.2$ «Дерево решений» и $6.3$ «Правило 10/10/10»** (jade; $6.1$ пропущен — построим позже). Оба — по новому стандарту интерактивности из $3.1$ (drag-SVG $+$ один текстовый интерактив). **$6.2$** `/logic/resheniya/derevo`: в quote-row **живое дерево решений** — экзаменационный пример (задача А: $90\% \cdot 2 = E\,1{,}8$; задача Б: $p \cdot 6$) со слайдером $P(\text{решить Б})$: тянешь — $E$ пересчитывается, на пороге $p = 1{,}8/6 = 0{,}3$ (засечка на шкале) выгодная ветвь и рекомендация переключаются (классы win-a/win-b). Секции: узлы ■●× / счёт ветвей $+$ сворачивание с конца / интерактив «Сосчитайте дерево» ($0{,}25 \cdot 8000 = 2000$; $E = 2000 > 1500$; fold back; порог) / анализ чувствительности / пределы ($\sigma$ из $2.4$, необратимость из $2.3$) / итог. Источники Raiffa 1968, Hammond–Keeney–Raiffa 1999, Clemen 1996; storage `lg-c6-derevo-*`. **$6.3$** `/logic/resheniya/pravilo-101010`: в quote-row **машина времени** — таймлайн с тремя снэп-стопами ($10$ минут / $10$ месяцев / $10$ лет; drag и клик прыгают к ближайшему), два живых бара: эмоция $90 \to 30 \to 5$, последствия $10 \to 60 \to 95$, подпись меняется. Секции: горячка момента (Система 1) / правило Уэлч ($3$ карточки горизонтов, $10$ мес $\approx 432\,000$ мин) / интерактив «Машина времени» / где работает (и где не заменяет дерево $6.2$) / чек-лист применения ($4$ шага) / итог. Источники Welch 2009, Kross 2021, Kahneman 2011; storage `lg-c6-pravilo-*`. Числа обоих модулей в `verify_math.py` (Гл.6.2, Гл.6.3). Toolkit $41/41$, $0$ изображений. Гл. $6$ → $2/6$, core $15/36$ ($42\%$), HTML: $29$.

- **Глава 3: построен модуль $3.1$ «Мышление в вероятностях»** `/logic/bayes/myshlenie` (blue) — **новый стандарт интерактивности SVG**: вместо кнопки-тумблера в `.quote-row` живой **слайдер уверенности** (drag pointer-событиями $+$ клик по шкале $+$ стрелки клавиатуры, `role="slider"` с `aria-value*`); процент и словесная оценка («почти исключено» … «почти наверняка») обновляются вживую; старт $65\%$, так что клик по центру видимо меняет состояние ($\to 50\%$ «фифти-фифти») — это и проверяет валидатор. $6$ секций: шкала вместо «да/нет» (мнение $=$ ставка) / слова обманывают — **шкала Кента** ($5$ чипов диапазонов, «вероятно» $= 63$–$87\%$) / интерактив / калибровка (из «$90\%$» сбывается ~$9$ из $10$; один исход не приговор) / готовность обновлять (мост к $3.2$) / итог. Один текстовый интерактив **«Мыслить в процентах»** ($4$ вопроса: сухой день при «$70\%$ дождя», диапазон Кента, калибровка, байесовский сдвиг). Квиз ($5$); источники Kent 1964, Tetlock–Gardner 2015, Duke 2018. Числа проверены в `verify_math.py` (Гл.3.1). Storage `lg-c3-myshlenie-*`. Toolkit $20/20$, $0$ изображений. Гл. $3$ → $1/6$, core $13/36$, HTML: $27$.

- **Глава 2 завершена ($6/6$) $+$ интерактивный лендинг Главы 3.** Капстоун $2.6$ «Синтез: считать ставку» `/logic/veroyatnost/sintez` (gold, зеркало $1.6$): `.quote-row` — слева **анатомия ставки** (исходы $-100$/$+200$; кнопка «Разобрать ставку» открывает $E[X]=+50$ и стрелку разброса $\sigma=150$), справа принцип качества решения; $5$ секций — recap-сетка ($6$ карточек $2.1$–$2.6$ со ссылками), **метод из $5$ шагов** (класс → вероятности/счёт → $E[X]$ → разброс → решение по двум числам), интерактив **«Какой инструмент решает?»** ($5$ ситуаций: страховка → $E[X]$; «скорее всего» → шкала; вклады с равным средним → $\sigma$; переезд → класс; лотерея $C_{36}^5=376\,992$ → комбинаторика), «судите ставку, а не исход» (resulting), CTA → квиз главы `/logic/veroyatnost/kviz`; next → `/logic/bayes`; источники Duke 2018, Kahneman 2011, Blitzstein–Hwang 2019; storage `lg-c2-sintez-game` (без инлайн-квиза — как $1.6$). Лендинг Гл. $3$ `/logic/bayes` (blue) переделан в **интерактивный**: CSS лендинга Гл. $2$ с точечной заменой палитры gold→blue; тезис «мнение — это вероятность» $+$ принцип соразмерности; $3$ опоры; **демо «тест и базовая ставка»** (тест ловит $90\%$, ложная тревога $9\%$; базы $1\%/10\%/50\%$ → постериоры $9{,}2\%/52{,}6\%/90{,}9\%$, проверено в `verify_math.py`); $6$ тайлов ($3.1$ myshlenie / $3.2$ formula / $3.3$ bazovaya-stavka / $3.4$ posledovatelnoe / $3.5$ sila-ulik / $3.6$ sintez); цепочка глав (current $=3$). Toolkit $28/28$, $0$ изображений. **Глав завершено: $2/6$**, core $12/36$, HTML: $26$.

- **Глава 2: построены модули $2.4$ и $2.5$ — остался капстоун.** $2.4$ «Дисперсия и риск» `/logic/veroyatnost/dispersiya` (gold): `.quote-row` — слева **разброс** (один центр, узкая ставка А vs широкая Б; кнопка «Показать разброс»), справа принцип риска; $6$ секций (среднее не всё — две ставки ±10 vs ±100 с $E=0$ / дисперсия $\mathrm{Var}(X)=\sum p_i(x_i-E)^2$ $+$ $\sigma$ / интерактив / риск $=$ разброс / снизить — диверсификация / итог); интерактив **«Какая ставка рискованнее?»** ($4$ пары с равным $E$, выбрать больший $\sigma$); квиз ($5$); источники Markowitz 1952, Blitzstein–Hwang 2019, Bernstein 1996. $2.5$ «Комбинаторика» `/logic/veroyatnost/kombinatorika` (gold): `.quote-row` — слева **дерево правила умножения** ($2\times3=6$ путей; кнопка «Показать пути»), справа принцип счёта; $6$ секций (зачем считать / правило умножения $N=n_1 n_2\cdots$, ПИН $10^4$ / интерактив / порядок — размещения $A_n^k$ vs сочетания $C_n^k$ / связь с вероятностью — лотерея $C_{45}^6=8\,145\,060$ / итог); интерактив **«Сколько способов?»** ($4$ задачи: ПИН $10\,000$, $C(5,2)=10$, $5!=120$, $C(45,6)=8\,145\,060$); квиз ($5$); источники Виленкин 1969, Graham–Knuth–Patashnik 1994, Blitzstein–Hwang 2019. Математика Гл.2.4 (дисперсия/$\sigma$) и Гл.2.5 (счёт) проверена в `verify_math.py`. Storage `lg-c2-dispersiya-*`, `lg-c2-kombinatorika-*`. Toolkit $32/32$, $0$ изображений. Гл. $2$ → $5/6$, HTML-файлов: $25$.

- **Глава 2: построены модули $2.2$ и $2.3$.** $2.2$ «Ожидаемая стоимость» `/logic/veroyatnost/ozhidaemaya` (gold): паттерн `.quote-row` — слева **числовая прямая** с исходами $-100$/$+200$ и средним $E[X]=+50$ (кнопка «Найти среднее»), справа принцип; $6$ секций (что это / как считать $\sum x_i p_i$ / калькулятор / правило / подвохи / итог); **интерактивный калькулятор $E[X]$** (выигрыш/проигрыш/шанс → $E[X]$ $+$ вердикт; $3$ пресета: честная монета $0$, выгодная $+50$, казино $-4$); квиз ($5$); источники Bernoulli 1738, Blitzstein–Hwang 2019, Hacking 2001. $2.3$ «Классы решений» `/logic/veroyatnost/klassy` (gold, концептуальный): `.quote-row` — слева **развилка** «вероятности известны?» → риск (считай $E[X]$) / неопределённость (взвесь сценарии) (кнопка «Показать ветви»), справа принцип; $6$ секций (зачем классифицировать / риск vs неопределённость [Найт 1921] / интерактив / повторяемость $+$ обратимость / памятка из $3$ вопросов / итог); интерактив **«Какой подход?»** ($4$ ситуации → выбор подхода из $4$); квиз ($5$); источники Knight 1921, Luce–Raiffa 1957, Savage 1954. Математика $E[X]$ проверена в `verify_math.py` (блок Гл.2.2); $2.3$ — концептуальный. Storage `lg-c2-ozhidaemaya-*`, `lg-c2-klassy-*`. Toolkit $30/30$, $0$ изображений. Гл. $2$ → $3/6$, HTML-файлов: $23$.

- **Старт Главы 2: лендинг поднят до интерактивного + построен модуль $2.1$.** Лендинг `/logic/veroyatnost` (gold) переделан из сгенерированного в **интерактивный** (как лендинги Права): навигация по секциям $+$ scroll-spy, тезис «решение — это ставка» $+$ принцип качества решения, $3$ опоры, **демо-виджет «вероятность события»** (маркер едет по шкале $0$–$1$; $4$ события: монета $1/2$, кубик $1/6$, две шестёрки $1/36$, туз $1/13$), $6$ тайлов модулей, цепочка глав. Модуль $2.1$ «Вероятность с нуля» `/logic/veroyatnost/osnovy` (gold): применяет паттерн **`.quote-row`** — слева интерактивная **шкала $0$–$1$** (кнопка «Показать события» открывает маркеры монета/кубик с миганием), справа принцип; $6$ секций (мера $0$–$1$; три подхода — классический/частотный/субъективный $+$ формула $P=m/n$; интерактив; два правила — диапазон, сумма $=1$, дополнение; зачем; итог); интерактив **«Какова вероятность?»** ($4$ задачи: $P(6)=1/6$, $P($не $6)=5/6$, $P($чёт$)=1/2$, $P($два орла$)=1/4$); квиз ($5$); источники (Колмогоров 1933, Blitzstein–Hwang 2019, Tijms 2012). Математика проверена в `verify_math.py` (блок Гл.2.1). Storage `lg-c2-osnovy-*`. Toolkit $26/26$, $0$ изображений. Гл. $2$ → $1/6$, HTML-файлов: $21$.

- **Глава 1 ЗАВЕРШЕНА ✓ ($6/6$): построен капстоун $1.6$ «Синтез» + страница финального квиза + обновлён toolkit.** Toolkit: в `course-build-playbook.md` добавлен **новый паттерн `.quote-row`** — двухколоночный блок «интерактивный/анимированный SVG слева + цитата справа», на мобиле (≤760px) сворачивается в две строки (CSS, разметка, пример анимации+интерактива, правила валидации). $1.6$ `/logic/iskazheniya/sintez` (burgundy, $5$ секций): применяет `.quote-row` — слева **анимированная иллюзия Мюллера-Лайера** (две равные линии кажутся разными; направляющие равенства пульсируют по CSS-циклу, кнопка «Измерить» фиксирует и показывает «длины равны»), справа принцип главы; рекап-сетка пяти искажений; интерактив **«Определите искажение»** ($5$ ситуаций → выбрать искажение из $5$); общая защита-чеклист; мост к Главе 2; заметный CTA на финальный квиз; источники (Tversky–Kahneman 1974, Kahneman 2011, Gilovich–Griffin–Kahneman 2002). Финальный квиз `/logic/iskazheniya/final` (burgundy, отдельная страница): **$10$ вопросов** по всем пяти искажениям + синтез, метки тем, мгновенный разбор, липкая шкала прогресса, блок результата с оценкой ($10$/$\ge7$/иначе) и кнопками «к Главе 2» / «пройти заново». Storage `lg-c1-sintez-game`, `lg-c1-final-quiz`. Toolkit $26/26$ ($0$ изображений): иллюзия-переключатель, $5$ рекап-задач, $10$ вопросов до результата $10/10$. HTML-файлов: $20$. **Глав завершено: $1/6$.**

- **Модули $1.4$ «Ошибка выжившего» и $1.5$ «Потери» построены ✓ (Гл. $1$ → $5/6$).** $1.4$ `/logic/iskazheniya/vyzhivshiy` (burgundy): $6$ секций, интерактив «Пропавшие данные» — $3$ задачи, включая **самолёты Вальда с inline-SVG** (вектор, $13$ пробоин на крыльях/фюзеляже/хвосте, двигатели и кабина чисты; ответ — усилить чистые зоны), плюс провалившиеся бросившие и «выжившие» старые вещи; квиз ($5$); источники (Mangel–Samaniego 1984 о Вальде, Brown et al. 1992, Kahneman 2011 WYSIATI). $1.5$ `/logic/iskazheniya/potery` (burgundy): $6$ секций, интерактив «Поймайте неприятие потерь» — $3$ задачи (невозвратные затраты, фрейминг $500-200=300$, коэффициент $\approx2$); квиз ($5$); источники (Kahneman–Tversky 1979 теория перспектив, Tversky–Kahneman 1991, Kahneman 2011). Честность: ошибка выжившего концептуальна (SVG — иллюстрация идеи Вальда); вычислимое для $1.5$ (EV монеты $=0$, тождество $500-200=300$, при выигрыше $2000$ EV$=+500$) проверено в `verify_math.py`; $\lambda\approx2$ помечен как эмпирический. Storage `lg-c1-vyzhivshiy-*`, `lg-c1-potery-*`. Toolkit $25/25$, $0$ изображений. HTML-файлов: $18$.

- **Модуль $1.3$ «Доступность» построен ✓ (Гл. $1$ → $3/6$).** `/logic/iskazheniya/dostupnost` (burgundy): $6$ секций (что такое доступность, почему — Система 1 и роль СМИ, где встречается, защита через базовую частоту), интерактив **«Что чаще?»** — $4$ пары, где редкое-яркое переоценивается (машина vs самолёт, собаки vs акулы, болезни сердца vs терроризм, молния vs джекпот) с выводом про доступность. Навигация по секциям $+$ нижняя; квиз ($5$); источники (Tversky–Kahneman 1973, Slovic 1987, Kahneman 2011). Честность: сравнения частот помечены как иллюстративные (направление по данным ВОЗ), вычислимая часть (комбинаторика лотереи $C(45,6)=8\,145\,060$) проверена в `verify_math.py`. Storage `lg-c1-dostupnost-*`. Toolkit $13/13$, $0$ изображений. HTML-файлов: $16$.

- **Курс опубликован — `jonnify.fly.dev/logic`.** Toolkit синхронизирован с фактом публикации: статус и README отмечают живой адрес и контекст серии из девяти курсов (см. карту `llms.txt`). В архиве — авторские страницы `/logic` (главная, 6 лендингов глав, 6 «Квиз главы», модули 1.1–1.2), утилиты (`verify_math.py`, генераторы, `preflight.py`), дизайн-док, общий `course-build-playbook.md` и валидатор.

- **Модули $1.1$ и $1.2$ построены ✓ (Гл. $1$ → $2/6$).** `sistemy` ($1.1$ «Две системы мышления» — интерактивный тест когнитивной рефлексии: ракетка-мяч, машины, кувшинки) и `yakorenie` ($1.2$ «Якорение» — интерактив «колесо фортуны»: случайный якорь → оценка доли Африки в ООН → реальность $28\%$ и опыт Тверски–Канемана). Обе фичи запроса: **навигация по секциям в шапке** (липкая, подсветка активной секции) и **удобная нижняя навигация** (пред./след. $+$ «↑ Наверх»). Числа проверены в `verify_math.py`. Дидактические, с квизом и источниками. Toolkit $23/23$, $0$ изображений. HTML-файлов: $15$.
- **Лендинги всех 6 глав готовы ✓.** Сгенерированы `build_chapter_landing.py` → `/logic/{слаг}/index.html` для всех глав (burgundy/gold/blue/plum/slate/jade): hero с римской цифрой и цитатой-принципом, 3 intro-карточки «зачем глава», сетка из 6 тайлов-модулей (последний — капстоун), цепочка из 6 глав с подсветкой текущей, CTA на квиз главы. Тайлы ведут на модули (пути готовы, страницы строятся позже); ссылки на квизы уже работают. Карточки/тайлы — standalone `<a>` (без вложенных анкоров). Toolkit $54/54$, $0$ изображений. HTML-файлов курса: $13$.
- **Главная страница курса `/logic` ✓.** Создана `logic/index.html`: hero (тезис + «как не быть обманутым собственным мозгом»), цели обучения ($6$), программа ($6$ карточек глав в темах глав), целевая аудитория, начальные требования, «как устроен курс». Карточки глав — `<div>` (не `<a>`), чтобы не было вложенных анкоров. Toolkit $10/10$, $0$ изображений.
- **Десктоп-шрифт увеличен.** В базовый CSS квизов и главной добавлен медиазапрос `@media (min-width:1024px){html{font-size:18px}body{font-size:18.5px}}` ($19px$ при $\geq1440px$) — масштабирует все rem-размеры. Квизы перегенерированы; переполнения нет (проверено на десктоп-вьюпорте). Правило внесено в дизайн-док как сквозное.
- **Квиз-страницы сгенерированы.** Запущен `build_chapter_quiz.py` → $6$ страниц «Квиз главы» (`/logic/{глава}/kviz`), по $6$ вопросов на главу (по одному на модуль), в теме главы. Pre-flight ($0$ проблем) $+$ toolkit ($42/42$, $0$ изображений). HTML-файлов курса: $6$. Ждут лендингов глав.
- **Старт проекта.** Создан стартовый комплект: спецификация (`logic-course-design.md` — архитектура $6\times6$, поглавный разбор с интерактивами, палитра), источники (`logic-references.md` — $3$ реальных на главу), этот трекер; утилиты (`verify_math.py` — $22$ проверки ✓, `build_chapter_quiz.py`, `preflight.py`). Курс ещё не верстался — готов фундамент для сборки.
