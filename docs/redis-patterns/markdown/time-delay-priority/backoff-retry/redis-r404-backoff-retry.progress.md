# redis-r404-backoff-retry — AAW scope ledger

## {redis-r404-backoff-retry-progress} Progress

### P-1

R4.04 backoff-retry DEEP reconcile COMPLETE — hub + 3 dives all STATUS: PASS / A+ (10/10 gates). Re-skinned dark→contract-sheet (R1 model). Re-grounded off the DELETED Go port: retryJob/promoteDelayedJobs/getPriorityScore + frozen echo/apps/echomq (EchoMQ.Scripts/Keys, retryJob-11.lua, getDelayedScore) → real echo/apps/echo_mq: EchoMQ.Backoff.delay_ms/2 (the :exponential `base*2^(attempts-1)` clamp + :jitter full-jitter wrapper), EchoMQ.Jobs.retry/7 (verbatim @retry non-terminal arm), EchoMQ.Jobs.promote/3 (verbatim @promote sweep), key emq:{q}:schedule, attempts HINCRBY fencing token — all re-found on disk. Scrubs CLEAN: 0 Go-port/frozen-tree, 0 BullMQ/Dragonfly, 0 Portal/Exchange/EchoCache, 0 fonts.googleapis/Cormorant, 0 bcs/content, 0 .out. Doors: → /echomq/queue + /echo-persistence. md mirrors updated. 8 hover-select interactives (2/dive + 1 hub) all node --check clean. Edited ONLY html/redis-patterns/time-delay-priority/backoff-retry/ + its md mirror.
