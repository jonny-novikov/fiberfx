# Прогресс: курс «Электричество и устройства»

Маршрут `/physics` · итерация: **модули Главы 3 «Счёт за электричество» (3.1–3.4)**. Остаётся квиз главы.

## Что построено в этой итерации (4 новых модуля)

**Глава 3 «Счёт за электричество»** (`schet`, зелёный):

| Модуль | Маршрут | Интерактив | Формула |
|---|---|---|---|
| 3.1 Из чего складывается счёт | `schet/iz-chego` | калькулятор счёта | $S = E \cdot p$ |
| 3.2 Тарифы и зоны | `schet/tarify` | переключатель «одна зона / день-ночь» + сравнение | $S = E_1 p_1 + E_2 p_2$ |
| 3.3 Что реально тратит деньги (флагман) | `schet/chto-tratit` | таблица 6 приборов + тариф → счёт + рейтинг | $S = p \sum P_i t_i$ |
| 3.4 Как сэкономить | `schet/ekonomiya` | калькулятор «до и после» (руб/год) | $S = \Delta E \cdot p$ |

Каждый модуль — 4 раздела со scroll-spy, интерактив, квиз из 5 вопросов, навигация пред./след., «↑ Наверх», breadcrumb, safety-banner. Навигация: 3.1 → 3.2 → 3.3 → 3.4 → квиз главы.

Флагман 3.3 переиспользует логику таблицы приборов из модуля 2.3 и добавляет тариф: считает счёт по каждому прибору за месяц, суммирует и показывает главного «пожирателя». Валюта — рубли (везде обычным текстом, в математику не попадает).

## Изменения в инструментарии

- **`build_module.py`** — +4 интерактива (`IZCHEGO`, `TARIFY`, `CHTOTRATIT`, `EKONOMIYA`) и +4 конфигурации модулей (`M31`–`M34`, поле `chnum='3'`).
- **`verify_physics.py`** — +12 проверок для калькуляторов 3.1–3.4 (всего 56/56).
- **Регрессии нет** — вывод всех 13 ранее собранных модулей байт-в-байт идентичен (`md5sum -c`).
- **`suite.ch3.js`** — новый сьют для модулей 3.1–3.4 (интерактивы + квизы).

## Цикл сборки — результаты

| Этап | Результат |
|---|---|
| `verify_physics.py` | **56/56** примеров проходят |
| `preflight.py` (KaTeX strict + вложенные `<a>`) | **0 проблем** на всех 26 страницах |
| Playwright-валидатор (0 изображений) | **506 PASS / 0 FAIL** |

Разбивка валидатора: `home` 24 · `landings` 90 · `modules` 74 · `sample` 18 · `quizzes` 48 · `ch2` 86 · `ch3` 82 · `ch5` 84.

## Проверка математики калькуляторов

- Счёт (3.1): 200 кВт·ч по 5 руб = 1000 руб; за год около 12000 руб.
- Тарифы (3.2): одна зона (100+50) по 6 руб = 900 руб; день-ночь 100 по 6 + 50 по 2 = 700 руб; разница 200 руб.
- Флагман (3.3): по умолчанию сумма 378 кВт·ч в месяц = 1890 руб; верх рейтинга — обогреватель 180 кВт·ч = 900 руб.
- Экономия (3.4): снижение на 50 кВт·ч в месяц при тарифе 5 руб = 250 руб в месяц = 3000 руб в год.

## A+ Apollo gates

- KaTeX strict: формулы только латиница/команды ($S = E \cdot p$, $S = E_1 p_1 + E_2 p_2$, $S = p \sum P_i t_i$, $S = \Delta E \cdot p$); единицы (кВт·ч, руб) и валюта — текстом вне `$...$`. Результаты калькуляторов выводятся без `$`-разметки. Скан `\$[^$\n]+?\$` по страницам и докам — чисто.
- 0 изображений (бюджет валидатора 0; только DOM/computedStyle).
- Ссылки root-relative; вложенных `<a>` нет; localStorage с префиксом `phys-schet-`.
- На каждой странице: полоса прогресса, scroll-spy section-nav, карточки пред./след., «↑ Наверх», breadcrumb, safety-banner.

## Снимок прогресса курса

| Глава | Слаг | Тема | Статус |
|---|---|---|---|
| 1 | `tok` | Ток, напряжение, сопротивление | ✓ завершена (5 модулей + квиз) |
| 2 | `moshchnost` | Мощность и энергия | ✓ завершена (4 модуля + квиз) |
| 3 | `schet` | Счёт за электричество | ◐ модули 3.1–3.4 ✓ · квиз главы ☐ |
| 4 | `bezopasnost` | Безопасность | ◐ лендинг ✓ · модули ☐ |
| 5 | `ustroystva` | Устройства и батареи | ✓ завершена (4 модуля + квиз) |
| ★ | `final` | Физика для жизни (Проект 6) | ☐ спроектирован |

Собрано HTML-страниц: **26**.

## Что дальше

1. Квиз Главы 3 `schet/kviz` — завершить Главу 3.
2. Модули Главы 4 `bezopasnost`: `pochemu-bjot`, `zazemlenie`, `uzo-avtomaty`, `peregruzka` (флагман), `pervaya-pomoshch` + квиз — только модели, без опытов с сетью.
3. Финальный проект 6: хаб + энергоаудит + финальный квиз + печатные материалы.

> Образовательный материал по школьной физике. Электричество опасно: соблюдать ПУЭ, не работать под напряжением.
