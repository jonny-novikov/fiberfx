# Прогресс: курс «Электричество и устройства»

Маршрут `/physics` · итерация: **лендинги Глав 3, 4, 5**. Теперь у всех пяти глав есть лендинги; дальше — модули Глав 3–5 и квизы.

## Что построено в этой итерации

Три лендинга глав на самодостаточном статическом HTML дизайн-системы jonnify (dark luxury-editorial), каждый со своей палитрой, через `style.py` (`head(slug=…)` + `NAV_JS`).

| Глава | Маршрут | Палитра | Формула главы | Плиток модулей | Квиз-CTA |
|---|---|---|---|---|---|
| 3 · Счёт за электричество | `/physics/schet` | зелёный | $S = E \cdot p$ | 4 | `schet/kviz` |
| 4 · Безопасность | `/physics/bezopasnost` | красный | $I = \frac{U}{R}$ | 5 | `bezopasnost/kviz` |
| 5 · Устройства и батареи | `/physics/ustroystva` | фиолетовый | $E = QU$ | 4 | `ustroystva/kviz` |

Каждый лендинг: герой с формулой главы, секция «о главе» (4 тезиса), сетка плиток-модулей, карточка квиза главы, навигация пред./след., scroll-spy, safety-banner. Единицы и пояснения — обычным текстом вне формул; формулы героев — только латиница и команды (ASCII-подстрочники) ради KaTeX strict.

Плитки модулей (ссылки вперёд, сами модули — следующие итерации):
- **Гл.3:** `iz-chego`, `tarify`, `chto-tratit` (флагман), `ekonomiya`.
- **Гл.4:** `pochemu-bjot`, `zazemlenie`, `uzo-avtomaty`, `peregruzka` (флагман), `pervaya-pomoshch`. В тексте подчёркнуто: только модели и правила, без опытов с сетью.
- **Гл.5:** `dc-ac`, `emkost`, `zaryadka-kpd`, `ukhod`.

## Изменения в инструментарии

- **`build_chapter_landing.py`** — добавлены три конфигурации (`CH3`, `CH4`, `CH5`); список `CHAPTERS` расширен. Навигация связала Гл.2 → Гл.3 → Гл.4 → Гл.5 → Финальный проект.
- **Регрессии нет** — вывод лендингов Гл.1 и Гл.2 остался байт-в-байт идентичным (проверено `md5sum -c`, 0 расхождений).
- **`suite.landings.js`** расширен на три новые главы (число плиток, ссылки модулей, цепочка навигации).

## Цикл сборки — результаты

| Этап | Результат |
|---|---|
| `verify_physics.py` | **37/37** примеров проходят (новых формул в этой итерации нет) |
| `preflight.py` (KaTeX strict + вложенные `<a>`) | **0 проблем** на всех 16 страницах |
| Playwright-валидатор (0 изображений) | **308 PASS / 0 FAIL** |

Разбивка валидатора: `home` 24 · `landings` 90 · `modules` 74 · `sample` 18 · `quizzes` 16 · `ch2` 86.

## A+ Apollo gates

- KaTeX strict: формулы героев $S = E \cdot p$, $I = \frac{U}{R}$, $E = QU$ — только латиница/команды; единицы и расшифровки — текстом вне `$...$`. Скан `\$[^$\n]+?\$` по страницам и докам — чисто.
- 0 изображений (бюджет валидатора 0; только DOM/computedStyle).
- Ссылки root-relative (`/physics/…`); хост не захардкожен. Вложенных `<a>` нет.
- На каждом лендинге: полоса прогресса, scroll-spy section-nav (3 якоря), сетка плиток, карточка квиза, карточки пред./след., «↑ Наверх», breadcrumb, safety-banner.

## Снимок прогресса курса

| Глава | Слаг | Тема | Статус |
|---|---|---|---|
| 1 | `tok` | Ток, напряжение, сопротивление | ✓ завершена (5/5 модулей + квиз главы) |
| 2 | `moshchnost` | Мощность и энергия | ◐ модули 2.1–2.4 ✓ (4/4) · квиз главы ☐ |
| 3 | `schet` | Счёт за электричество | ◐ лендинг ✓ · модули ☐ |
| 4 | `bezopasnost` | Безопасность | ◐ лендинг ✓ · модули ☐ |
| 5 | `ustroystva` | Устройства и батареи | ◐ лендинг ✓ · модули ☐ |
| ★ | `final` | Физика для жизни (Проект 6) | ☐ спроектирован |

Собрано HTML-страниц: **16** (Главная + 5 лендингов + модули 1.1–1.5 + квиз Гл.1 + модули 2.1–2.4).

## Что дальше

1. Квиз Главы 2 `moshchnost/kviz` — завершить Главу 2.
2. Модули Глав 3–5 по директивам §8 + квизы глав.
3. Финальный проект 6: хаб + энергоаудит + финальный квиз + печатные материалы (§9).

> Образовательный материал по школьной физике. Электричество опасно: соблюдать ПУЭ, не работать под напряжением. Глава безопасности — только модели и правила, без опытов с сетью.
