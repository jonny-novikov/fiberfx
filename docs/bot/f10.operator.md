# F10 · Operator's guide · Telegram Bot in Elixir

## Actionables [RECONCILE]

1. One manual step remains for full DoD — the live Telegram demo (D9): set a real token in ECHO_BOT_HELLO_TOKEN, iex -S mix in dev (polling), send /start, confirm a reply. It can't be
   automated (no live Telegram in the suite, by design).
2. One open fork before F10.3 — the games web server. The roadmap recommends B (Plug.Router on Bandit + WebSock, isolated from portal_web); it's recorded but awaits your confirmation
   before the F10.3 build. F10.2 (multi-bot) has no open forks and is ready whenever you want to run its pipeline.