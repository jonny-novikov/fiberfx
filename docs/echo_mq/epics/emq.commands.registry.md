# EchoMQ command registry — the v1→v3 capability surface, by feature

> **Dev-support reference, NOT the catalogue.** All **50** v1 BullMQ Lua commands mapped v1→v3, sorted by the **12-feature taxonomy** ([`emq.epic.1.md`](emq.epic.1.md) §The feature index). This is the feature-sorted, ≤300-line-per-file successor to the former 1290-line `emq.1.specs.md` matrix — *why* each v3 form re-derives its v1 analogue, never the forward-only DSL catalogue itself.

**How to read.** This file is the **summary matrix** (one table per feature). A command's **full detail + the v1→v3 side-by-side** lives exactly once in its feature slice under [`emq.commands/features/`](../specs/emq.commands/features/), anchored `#{command}`; the Command cell links there. A command serving two features is filed under its **primary** feature and cross-referenced (`→ feature`) from the secondary. The raw v1 sources are [`emq.commands/registry/*.lua`](../specs/emq.commands/registry/); the flat agent index is [`emq.commands/llms.txt`](../specs/emq.commands/llms.txt).

**Columns:** Command · v1 (purpose) · v2 status (grounded — token · rung `sha` · module) · v3 decision (PROPOSED) · BCS · EchoMesh. **Voice:** v2 is as-shipped present tense, traced to the rung that shipped it; every v3 claim is forward / PROPOSED; `[RECONCILE]` marks a claim ahead of as-built code. Status tokens: **PORTED** (shipped) · **PARTIAL** (shipped in part) · **NOT YET** (proposed) · **RETIRED** (dropped by design) · **[RECONCILE]** (building).

Per-rung commits: emq.0 `a2d599c8` · emq.1 `e0fa9b03` · emq.2.1 `7d98ef86` · emq.2.2 `76fc947c` · emq.2.3 `3c6461ff` · emq.2.4 `3298e4bc` · emq.3.4 `4c401479` · emq.3.5 building.

## admission

The three v1 enqueue entry points — immediate, delayed, priority-ordered. (`addDelayedJob` → **scheduling**; `addPrioritizedJob` → **groups**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`addStandardJob-9`](../specs/emq.commands/features/admission/addStandardJob-9.md) | Immediate admission: INCR id, store row + push to wait/paused LIST, marker | **PORTED** — emq.0/1 `a2d599c8`/`e0fa9b03`; `Jobs.enqueue/4` (`@enqueue`) | Keep `@enqueue`; re-add the parent/flow edge as a **declared** dependents-set, never a data-rooted `parentKey` | Order theorem (byte order = mint order) — the deterministic-admission floor | Availability-first: idempotent at-least-once admission survives a partition |
| [`addDelayedJob-6`](../specs/emq.commands/features/scheduling/addDelayedJob-6.md) → **scheduling** | Delayed admission: pack run-at into a `delayed` ZSET score (12-bit tiebreak) | **PORTED** — emq.1 `e0fa9b03`; `Jobs.enqueue_at/5`+`enqueue_in/5` (`@schedule`) | → see **scheduling** (visibility-fence re-derivation) | One `schedule` set, one timing source | Server-clock fence, never the caller's clock |
| [`addPrioritizedJob-9`](../specs/emq.commands/features/groups/addPrioritizedJob-9.md) → **groups** | Priority admission: `priority*2^32 + INCR(pc)` packed score, global `prioritized` ZSET | **PORTED (re-aimed)** — emq.1 `e0fa9b03`; `Lanes.enqueue/5` (`@genqueue`), no `prioritized` set (D-9) | → see **groups** (fair lanes replace the priority number) | Fair lanes give many-tenants isolation a priority number cannot | Consistency-first: fair per-identity service is a server-side invariant |

## scheduling

Run-at / run-in on one schedule set; in-place re-score; targeted promote. (`addDelayedJob` is the primary home for delayed-admission detail, shared with **admission**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`addDelayedJob-6`](../specs/emq.commands/features/scheduling/addDelayedJob-6.md) | Delayed admission: pack run-at into a `delayed` ZSET score (12-bit tiebreak) | **PORTED** — emq.1 `e0fa9b03`; `Jobs.enqueue_at/5`+`enqueue_in/5` (`@schedule`, `enqueue_in` scores from server `TIME`) | Keep `@schedule` as a **visibility fence**; mint-ordered id stays sort key once promoted; server clock prices the delay | Scheduled and retry jobs re-score onto one set — one timing source | Trade-staleness-for-availability dial: a server-clock fence |
| [`changeDelay-4`](../specs/emq.commands/features/scheduling/changeDelay-4.md) | Re-score a job already in `delayed` to a new delay; emit `delayed`, re-arm marker | **NOT YET** — no `change_delay`; `schedule`+`@schedule`+`@promote` exist, no in-place re-score (grep-confirmed) | `reschedule/4` — re-`ZADD` the `schedule` member under a `TIME`-derived run-at, declared-keys, branded id at the builder | Push out / pull in a scheduled job without drop-and-re-add | CP-side admin write; staleness is a *budget knob* (M4 dial) |
| [`promote-9`](../specs/emq.commands/features/scheduling/promote-9.md) | Promote ONE specific delayed job (by id) → wait/prioritized; strip marker, re-enqueue, emit `waiting` | **PARTIAL** — emq.1 `e0fa9b03`; `Jobs.promote/3` (`@promote`) promotes *all due* members (batch, server clock), not a targeted single-id | Add a sibling `promote_now/3` (declared `[schedule, pending]`) — `ZREM` one id + `ZADD pending 0`, beside the due-sweep | Operator/early-trigger of a scheduled job | Consistency-first: targeted single-slot move, no clock-skew dependence |

## repeat

The repeatable registry; fresh-mint per occurrence; the pump cadence. The whole v1 job-scheduler family ports to **`EchoMQ.Repeat`** (emq.1 `e0fa9b03`).

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`addRepeatableJob-2`](../specs/emq.commands/features/repeat/addRepeatableJob-2.md) | Register/override a repeatable; on override rebuild the stale delayed id from data | **PORTED** — emq.1 `e0fa9b03`; `Repeat.register/6` over `emq:{q}:repeat`+`:<name>` (`repeat.ex:58`); cadence = `Pump` | Keep registry + fresh-mint-per-occurrence; extend the record with the v1 `pattern`/`tz`/`endDate` cron fields | A daily sweep registers once; every run is a first-class, mint-ordered job | Consistency-side — one queue owns its `{q}` repeat slot; the pump is the single writer |
| [`addJobScheduler-11`](../specs/emq.commands/features/repeat/addJobScheduler-11.md) | Upsert a scheduler + materialize the next occurrence; evict previous, slot collisions `-10`/`-11` | **PARTIAL** — emq.1 `e0fa9b03`; `Repeat.register/6`+`Pump` cover registration + cadence; collision codes have no port (fresh mint moots them) | Keep `register/6`; add `upsert`; fresh `JOB` id per occurrence; next-millis from server `TIME` at the pump | Each materialized run a browsable `JOB` on the queue's slot | Consistency-side: a partition isolates one queue's cadence, never the mesh's |
| [`getJobScheduler-1`](../specs/emq.commands/features/repeat/getJobScheduler-1.md) | Read a scheduler record: `ZSCORE` next-run + `HGETALL` template hash | **PARTIAL** — emq.1 `e0fa9b03`; `count/2`/`due/3` read depth + due records; no single-name point read | Add `get/3` → `{score, %{every_ms, template}}` over the two declared keys; honest-row `:absent` | Inspect one cadence by name without walking the due set | Read-side; a point read of one slot's registry, partition-tolerant |
| [`removeJobScheduler-3`](../specs/emq.commands/features/repeat/removeJobScheduler-3.md) | Remove a scheduler + its one next-programmed job | **PORTED** — emq.1 `e0fa9b03`; `Repeat.cancel/3` (`@repeat_cancel`): `ZREM` member + `DEL` record | Keep `cancel/3`; the "delete next programmed job" half is **not lifted** (next occurrence is a fresh `JOB`) | Cancelling a cadence stops future mints; in-flight occurrences drain | Consistency-side; single-slot, partition-local |
| [`updateJobScheduler-12`](../specs/emq.commands/features/repeat/updateJobScheduler-12.md) | Iterate a scheduler: recompute next-millis, add next, guard re-entry via producer-id | **PORTED** — emq.1 `e0fa9b03`; `Repeat.advance/4` (`@repeat_advance`): re-score to now+`every_ms`; sweep dangling; `Pump` drives | Keep `advance/4`+pump; the producer-id guard is **superseded** by the pump's single sweep | The pump advances each cadence idempotently; a mid-sweep cancel is swept | Availability dial: cadence flows per slot with no cross-slot coordination |
| [`updateRepeatableJobMillis-1`](../specs/emq.commands/features/repeat/updateRepeatableJobMillis-1.md) | Legacy: re-score an existing repeatable by custom or legacy key | **PARTIAL** — emq.1 `e0fa9b03`; `Repeat.advance/4` re-scores a registration; no legacy re-score-by-arbitrary-key verb | Fold into `advance/4`; the v1 legacy/custom-key branch is **dropped** | One re-score path; no legacy key-shape branching | Consistency-side; single-slot re-score, partition-local |
| [`removeRepeatable-3`](../specs/emq.commands/features/repeat/removeRepeatable-3.md) | Legacy + new removal: `ZREM` registration, `DEL` hash, remove next programmed job | **PORTED** — emq.1 `e0fa9b03`; `Repeat.cancel/3` covers the **new** form; the legacy concat-key path has no v2 equivalent | `cancel/3` is the single removal verb; dual legacy/new branches **collapse to one** | Removing a repeatable is one verb regardless of vintage | Consistency-side; single-slot removal |

## claim

Single-writer fetch; server-clock lease; attempts fence. (`moveJobsToWait` → **batches**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`moveToActive-11`](../specs/emq.commands/features/claim/moveToActive-11.md) | Worker fetch: wait→active, lock, fetch data; promote due delayed; rate-limit + pause gates | **PORTED** — emq.0/1 `a2d599c8`/`e0fa9b03`; `Jobs.claim/4` (`@claim`); `ZPOPMIN pending`→`active` lease-scored on `TIME`, token = `HINCRBY attempts`; grouped `Lanes.claim/3` | Keep `@claim`; make the **lane-aware claim the mesh-facing default**; no client-side `LMPOP` (§12.2) | The single-writer claim — the lease the only ownership proof | Consistency-first (§4 row 24): one claimant per slot, refuse rather than admit a second writer |
| [`moveJobFromActiveToWait-9`](../specs/emq.commands/features/claim/moveJobFromActiveToWait-9.md) | Worker-initiated requeue: return a still-leased active job to wait (lock-token-fenced) | **PARTIAL** — emq.0 `a2d599c8`; crash-recovery return is `Jobs.reap/2`+`Stalled.check/3`; a lock-fenced *voluntary* active→pending has no dedicated verb | Add a token-fenced `requeue` verb (no attempts bump, no morgue), declared `[active, pending, job_key]`; lock-token → `EMQSTALE` attempts fence | A worker yielding work it cannot finish without consuming an attempt | Consistency-first: the give-back stays on the owning slot |
| [`moveStalledJobsToWait-8`](../specs/emq.commands/features/claim/moveStalledJobsToWait-8.md) | Periodic stalled sweep: move stalled active→wait, bump `stc`, dead-letter past max | **PORTED** — emq.2.3 `3c6461ff`; `Stalled.check/3` (`@sweep_stalled`): `ZRANGEBYSCORE active` by `TIME`, recover < max else dead-letter; lane-aware; opt-in `:transient` | Formalize the sweep: the v1 two-scan mark-then-sweep collapses to one server-clock lease scan (the lease deadline IS the staleness signal) | The crash-recovery net — a dead worker's leases return; repeated stalls dead-letter | Consistency-first recovery: fold-to-state restart, the lease the only liveness fact |
| [`moveJobsToWait-8`](../specs/emq.commands/features/batches/moveJobsToWait-8.md) → **batches** | Bulk move completed/failed/delayed → wait by score window; "no priorities" | **PARTIAL** — emq.2.2; single-job `reprocess_job/3`+`promote/3` exist; a bulk count-windowed verb is NOT YET | → see **batches** (the bulk `requeue_set` re-derivation) | Operator bulk-recovery: replay a window of dead/scheduled work in mint order | Consistency-first with an availability lever — bulk replay onto the owning slot |

## retry

Retry/backoff; the dead-letter morgue; reprocess.

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`retryJob-11`](../specs/emq.commands/features/retry/retryJob-11.md) | Move a failed job active→wait/prioritized now, releasing its lock — the operator "retry now" | **PARTIAL** — split across `Jobs.retry/7` (`@retry`, worker active→scheduled/dead) + `reprocess_job/3` (operator dead→pending); no single "active→pending now, lock-released" verb | A declared-keys `requeue_active/4` (active→pending, lease-retired, lane-aware); **no** data-value lock token | The operator "kick a stuck claim back to the front" control | Consistency-first (CP-side) operator action — manual, audited, single-writer-serialized |
| [`reprocessJob-8`](../specs/emq.commands/features/retry/reprocessJob-8.md) | Reprocess a finished/failed job: remove from state, drop finish fields, re-add to wait, mend parent links | **PORTED** — emq.2.2 `76fc947c`; `Jobs.reprocess_job/3` (`@reprocess`): `dead`→`pending`, clears `last_error`, refuses non-dead `EMQSTATE` | Extend `@reprocess` with the A-1-clean flow fan-in re-link via declared §6 parent subkeys | The post-fix "re-run a dead job" recovery | CP-side recovery verb — deterministic, single-slot, audited |
| [`moveToDelayed-8`](../specs/emq.commands/features/retry/moveToDelayed-8.md) | Move a locked active job → delayed ZSET at a baked score; release lock, bump attempts, set delay | **PORTED (as retry-reschedule)** — emq.1 `e0fa9b03`; `Jobs.retry/7`+`@retry` non-terminal arm: `state=scheduled`, `ZADD schedule now+delay` on server `TIME` | Retain `@retry`'s scheduled arm; if a host-initiated defer is needed, add a thin `defer/…` routing the same `schedule` ZADD | Backoff-driven retry; visibility-fence reschedule | Consistency-first: the schedule set is single-slot, server-clock-scored |

## flows

Parent/child fan-in; child-result reads; failure-policy + bulk. The flow family is **`EchoMQ.Flows`**, A-1-clean (the graph in declared §6 parent subkeys, never a data-value `parent_key`); flat core shipped emq.3.1–3.4. (`removeChildDependency` + `removeUnprocessedChildren` → **lifecycle**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`addParentJob-6`](../specs/emq.commands/features/flows/addParentJob-6.md) | Add a flow **parent** held until children finish; register the child in a parent dependency SET (data-value `parent_key`) | **PORTED** — emq.3.1–3.4; `Flows.add/3`+`add_bulk/3`+`children_values/3`+`ignored_failures/3`+`dependencies/3`; `@enqueue_flow`/`@hold_parent`/`@enqueue_flow_child` | Keep the atomic same-queue + parent-first cross-queue add; add a **roster subkey** (Fork R2.B) to list which legs remain | The flow IS the BCS multi-leg work unit — parent = a job, children = legs | Consistency-side same-queue (atomic fan-in); availability-side cross-queue (outbox, INV5/INV7) — CAP segmented per flow |
| [`moveToFinished-14`](../specs/emq.commands/features/flows/moveToFinished-14.md) | Move a locked active job → completed/failed; release lock, run parent fan-in, trim/keep, fetch next + promote | **PORTED** — emq.0/1 `a2d599c8`/`e0fa9b03`; `Jobs.complete/5` (`@complete`, completion-deletes); fan-in via the hook + `Flows`; fail arm = `@retry` dead-letter | Keep `complete/5`; finalize-status the host-read seam; keep fetch-next + promote-on-finish **decomposed** into `claim`/`promote` | The terminal write — single-writer, fenced, completion-deletes | Consistency-first: refuse-on-stale (`EMQSTALE`); same-slot fan-in atomic |
| [`moveToWaitingChildren-7`](../specs/emq.commands/features/flows/moveToWaitingChildren-7.md) | Worker parks its active job → waiting-children IF deps remain (or a named child) and no failed | **PARTIAL** — emq.3.1; the *fan-in* (parent held, released at zero) is in `Flows`/`@complete`; the worker-initiated self-park is not a distinct port | Add an explicit `await_children/…`; host reads dep count host-side; parks parent on its slot; honest-row `{:awaiting,n}`/`{:ready,0}`/`{:failed_children}` | Dynamic flows where a parent discovers children at runtime | Consistency-first within a slot; the await is a same-slot guarded transition |
| [`getDependencyCounts-4`](../specs/emq.commands/features/flows/getDependencyCounts-4.md) → also **metrics** | Counts per child state (processed/unprocessed/ignored/failed) for a flow parent | **PARTIAL (split, not aggregated)** — emq.3.2/3.4; count is `Flows.dependencies/3`; per-state reads `children_values/3`+`ignored_failures/3`; no single aggregate | A `Flows.child_counts/3` composing the declared counter + subkeys on the parent slot — NOT a SET-cardinality lift | The fan-in progress a saga parent reads before proceeding | Consistency-first: on the strong-consistency (state-of-record) axis |
| [`removeChildDependency-1`](../specs/emq.commands/features/lifecycle/removeChildDependency-1.md) → **lifecycle** | Break one parent↔child link; move parent to wait if last child | **NOT YET** as a named verb — closest is the automatic fan-in `DECR` in `@flow_deliver`, not an explicit detach | → see **lifecycle** (`Flows.drop_dependency/3`, counter+record, no member SET) | Cancel one leg of a multi-leg flow without failing the parent | Consistency-first single-slot same-queue; cross-queue rides `flow:outbox` |
| [`removeUnprocessedChildren-2`](../specs/emq.commands/features/lifecycle/removeUnprocessedChildren-2.md) → **lifecycle** | Recursively remove a job's children, ignoring processed & locked | **NOT YET** — no recursive child-removal verb; `Flows` ships fan-in + failure-policy, not a teardown | → see **lifecycle** (`Flows.remove_children/3`, FLAT first; grandchildren → emq.3.5) | Tear down a cancelled fan-out's child legs without touching the parent | Consistency-first same-queue; cross-queue teardown availability-leaning |
| [`(grandchildren)`](../specs/emq.commands/features/flows/grandchildren-recursive-flow-tree.md) — emq.3.5 | Recursive flow tree: recursive add + multi-level fan-in + recursive failure propagation | **[RECONCILE]** — building (emq.3.5); flat family propagates ONE level (`4c401479`); recursive depth is the sole remaining slice; **Arm A APPROVED 2026-06-15, building** | **[RECONCILE]** Arm A — host/sweep-orchestrated re-emit over the byte-frozen failure machinery; only the recursive FAILURE hook is new | A multi-leg job whose legs are themselves sub-jobs (a tree) | Consistency-first per same-queue subtree; availability-leaning across queues (latency × depth) |

## groups

Fair lanes (per-player/per-tenant); the rotating ring — priority's replacement. (`addPrioritizedJob` is the primary detail home, shared with **admission**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`addPrioritizedJob-9`](../specs/emq.commands/features/groups/addPrioritizedJob-9.md) | Priority admission: `priority*2^32 + INCR(pc)` packed score, global `prioritized` ZSET | **PORTED (re-aimed)** — emq.1 `e0fa9b03`; no `prioritized` set; priority is `Lanes.enqueue/5` (`@genqueue`, D-9); the rotating ring is the rota | Keep fair lanes as the priority model; intra-group priority = a non-zero lane score, no new key family (emq.4) | Fair lanes give many-tenants isolation a priority number cannot | Consistency-first/coordinated: bounded, fair, per-identity service, server-side |
| [`changePriority-7`](../specs/emq.commands/features/groups/changePriority-7.md) | Re-position a job in `wait`/`prioritized` at a new priority score | **RETIRED** — no `change_priority`; the `prioritized` ZSET is retired by design (§6; mint-order IS the order theorem, **Lanes** replace priority) | **No priority re-score**; the equivalent is `Lanes` group re-assignment / weighted rotation (emq.4) — re-aim, not re-implement | Per-player fairness replaces per-job priority for the work surface | Lane fairness is the AP-leaning "keep all lanes answering" dial (emq.4) |
| [`getCountsPerPriority-4`](../specs/emq.commands/features/groups/getCountsPerPriority-4.md) → also **metrics** | Counts per priority band over the `prioritized` ZSET | **RETIRED** — no `prioritized` set; priorities are `Lanes`; `Metrics.lane_depth/3` reads per-lane depth | Re-derive as per-lane depth — `Metrics.lane_depths/3` over `g:<group>:pending`, branded-group-gated | Per-lane backlog is the fair-lane / per-player depth | Availability-first: per-segment lane depth, observational only |

## batches

Bulk add (`add_bulk` — shipped); bulk move/clean by window. (`add_bulk` parity ships with the **flows** `addParentJob` detail; `cleanJobsInSet` → **lifecycle**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`add_bulk`](../specs/emq.commands/features/batches/add_bulk.md) (flow parity) | Submit many flows at once | **PORTED** — emq.3.4 `4c401479`; `Flows.add_bulk/3` (fail-closed per flow); bulk job-enqueue is `Jobs.enqueue_many/3` (emq.1) | Keep `add_bulk/3` + `enqueue_many/3`; both already declared-keys, branded, honest-row | Bulk submission of N multi-leg flows in one call | Consistency-side per flow; availability-side for any cross-queue leg |
| [`moveJobsToWait-8`](../specs/emq.commands/features/batches/moveJobsToWait-8.md) | Bulk move completed/failed/delayed → wait by score window; "no priorities" | **PARTIAL** — emq.2.2; single-job `reprocess_job/3`+`promote/3` exist; a bulk count-windowed verb is NOT YET; `completed` is gone (completion-deletes) | Add a bulk `requeue_set(state, count)` over `schedule`/`dead`→`pending`, batched, declared keys; row cleared via a grammar-derived `job:` key | Operator bulk-recovery: replay a window of dead/scheduled work in mint order | Consistency-first with an availability lever — bulk replay onto the owning slot |
| [`cleanJobsInSet-3`](../specs/emq.commands/features/lifecycle/cleanJobsInSet-3.md) → **lifecycle** | Bulk-remove from one set older than a timestamp, up to a limit, skipping locked + scheduler jobs | **PARTIAL** — emq.2.2; `Admin.drain/3` (`@drain`) wipes `pending` (+ optional `schedule`), predicate-free; `obliterate/3` wipes a paused queue whole | → see **lifecycle** (`Admin.clean/4` with a server-clock age grace + limit) | Age-based queue hygiene for work lanes during an incident | Availability-first maintenance: a bounded sweep that degrades a backlog, not blocks |

## locks

Lease extend (single + batch); release; stalled recovery. The lock plane shipped at **emq.2.3** (`3c6461ff`). (`moveStalledJobsToWait` → **claim**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`extendLock-2`](../specs/emq.commands/features/locks/extendLock-2.md) | Renew one job's lease — `SET …:lock PX` if token matches, drop it from the stalled set | **PORTED** — emq.2.3 `3c6461ff`; `Jobs.extend_lock/5` (`@extend_lock`): re-scores the `active` member under server `TIME`, token-fenced `EMQSTALE` | Hold the shipped form; extend with an explicit returned `lease_deadline` so a consumer reasons about headroom | Token-fence is the single-writer guarantee for a long handler holding a row | Best-Effort-Availability corner: a consistency-first lease, correct-always (CP) |
| [`extendLocks-1`](../specs/emq.commands/features/locks/extendLocks-1.md) | Batch-renew many leases — loop over msgpacked ids/tokens, `SET …:lock PX` per match | **PORTED** — emq.2.3 `3c6461ff`; `Jobs.extend_locks/4` (`@extend_locks`): one `TIME`, variadic ARGV id/token pairs slot-rooted off `KEYS[1]=active`, returns `failed` | Keep the variadic A-1 form (slot-rooted-ARGV ruling, §1 S-6); surface `Locks.extend/1`'s `{extended, dropped}` telemetry | One server-clock read amortizes the held set — bulk-fence for a draining worker | The single-`TIME` batch keeps every lease on one consistency clock, fleet-wide |
| [`releaseLock-1`](../specs/emq.commands/features/locks/releaseLock-1.md) | Release a lease — `DEL …:lock` if token matches | **PORTED (re-split)** — emq.2.3 `3c6461ff`; `Locks.untrack_job/2` DELs the `:lock` *marker*; the lease releases by natural `active`-score expiry / `complete` | Keep the L-3 two-part release (marker DEL + self-expiring marker `PX`); make the DEL idempotent + return prior held-state | Releasing the marker unblocks `remove_job` (`EMQLOCK` lifts) | Marker self-expiry is the partition-healing escape hatch: a dead holder can't pin the row |
| [`moveStalledJobsToWait-8`](../specs/emq.commands/features/claim/moveStalledJobsToWait-8.md) → **claim** | Periodic stalled sweep → recover/dead-letter | **PORTED** — emq.2.3 `3c6461ff`; `Stalled.check/3` (`@sweep_stalled`) | → see **claim** (one server-clock lease scan) | The crash-recovery net for the single-writer model | Consistency-first recovery: a lost lane's leases recover on a survivor |

## metrics

Counts · state · throughput · introspection · membership. The read plane is **`EchoMQ.Metrics`** (emq.2.1, `7d98ef86`). (`getCountsPerPriority` → **groups**; `getDependencyCounts` → **flows**.)

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`getCounts-1`](../specs/emq.commands/features/metrics/getCounts-1.md) | Counts per requested state name | **PORTED** — emq.2.1 `7d98ef86`; `Metrics.get_counts/3` (`@counts`, closed-registry) | Keep `@counts`; add a one-shot multi-set snapshot + an honest `as_of` server-`TIME` stamp | Per-state depth feeds the operator dashboard | Availability-first: a queue-health read, stale-tolerant, nearest-replica |
| [`getState-8`](../specs/emq.commands/features/metrics/getState-8.md) | A job's state across 8 sets | **PORTED** — emq.2.1 `7d98ef86`; `Metrics.get_job_state/3` (`@state_lookup`; 4 sets + the row-field branch) | Keep the four-set + `awaiting_children` probe; the closed `@lookup_states` table is the honest-row guard; add `as_of` on a replica read | A runbook reads a job's state before any mutate | Consistency-first: the authoritative state-of-record read for one branded id |
| [`getStateV2-8`](../specs/emq.commands/features/metrics/getStateV2-8.md) | The `LPOS`-based state read (newer-Valkey variant) | **PORTED (subsumed)** — emq.2.1 `7d98ef86`; folded into `get_job_state/3` (no wait/paused LISTs, all four states are ZSETs) | No separate verb — the ZSET keyspace makes the LIST-vs-ZSET split moot; one `get_job_state/3` | Same as `getState` — one canonical state read | Consistency-first: same single-id read; the variant collapses |
| [`getMetrics-2`](../specs/emq.commands/features/metrics/getMetrics-2.md) | Completed/failed throughput series | **PARTIAL** — emq.2.1 `7d98ef86`; `Metrics.get_metrics/3` reads the `count`; the `:data` time-series + Prometheus export are unbuilt (emq.8) | Keep the counter read; build the `metrics:<which>:data` ring honestly behind it; Prometheus *format* stays emq.8 | Terminal-outcome throughput for capacity/SLO dashboards | Availability-first: a metered observation surface, never on the write path |
| [`getRanges-1`](../specs/emq.commands/features/metrics/getRanges-1.md) | Job-ids per requested states over a window (`LRANGE`/`ZRANGE`); v6-marker-aware | **NOT YET** — no `get_ranges`; the v1 state structures don't exist (§6 retires wait/paused/completed/failed/prioritized) | Re-aim to a per-state windowed browse: `pending` via REV BYLEX (`Jobs.browse/3`) + `ZRANGE` over active/schedule/dead; forward `XRANGE` window (emq3.6) | Read a queue's backlog/morgue by state with no second index | Consistency-first (CP) read of the regulated bus state |
| [`getRateLimitTtl-2`](../specs/emq.commands/features/metrics/getRateLimitTtl-2.md) | Remaining limiter TTL in ms (`PTTL`; `max` from meta) | **PORTED** — emq.2.1 `7d98ef86`; `Metrics.get_rate_limit_ttl/3` (`@rate_ttl`, declared `[limiter, meta]`) | Hold as-shipped; forward: extend to the per-group/per-player limiter window (`EMQRATE`-class) | How long until the rate window reopens — a throttle a consumer consults | Consistency-first read against the regulated window; exact, never best-effort |
| [`isMaxed-2`](../specs/emq.commands/features/metrics/isMaxed-2.md) | Is the queue at its concurrency ceiling → boolean | **PORTED** — emq.2.1 `7d98ef86`; `Metrics.is_maxed/2` (`@is_maxed`): read-and-refuse `:ok`/`{:error,:rate}` (`EMQRATE`) over `ZCARD active` | Hold as-shipped (v2 evolves the boolean into a typed `EMQRATE` refusal); forward: per-lane ceilings (`Lanes`) | The scale-out gate: consult the ceiling before claiming | Consistency-first admission gate — the ceiling is a hard refuse (CP) |
| [`isFinished-3`](../specs/emq.commands/features/metrics/isFinished-3.md) | Is a job finished (`completed`→1 / `failed`→2 ZSET); `-1` missing; optional result | **PARTIAL** — emq.2.1 `7d98ef86`; subsumed by `get_job_state/3`; the gap: no `completed` set (completion-deletes), only `dead` retained | A thin `finished?/3` over `get_job_state/3` — `:dead` the only retained terminal; "completed" via `metrics:completed`; forward: durable history via stream replay | "Did this job terminate?" — `dead` is the retained answer | Consistency-first point read; durable finished-history (emq3.5) serves the AP retention surface |
| [`isJobInList-1`](../specs/emq.commands/features/metrics/isJobInList-1.md) | Is an id a member of a LIST (`LRANGE`+linear scan) → 1/nil | **NOT YET** — no `in_list`; the bus has no membership LISTs (claiming is `ZPOPMIN`) | Re-aim to O(log n) set membership: a declared-key `ZSCORE` (the `@state_lookup` mechanism), never a linear `LRANGE` | Confirm a job's set residency cheaply without pulling the whole set | Consistency-first membership probe — exact, partition-refusing (CP) |
| [`paginate-1`](../specs/emq.commands/features/metrics/paginate-1.md) | Stable-cursor page of a set or hash (`SSCAN`/`HSCAN`, optional job fetch) | **NOT YET** — no `paginate`; listing is `Jobs.browse/3` REV-BYLEX + `pending_size/3` ZCARD (sorted-set windowing) | Primary listing is the order-theorem browse (REV BYLEX, stable because mint-ordered); set/hash via declared-key `HSCAN`; forward: `XRANGE`/`XAUTOCLAIM` over the stream tier | Page a large backlog/event log for recorded-runs + backtest replay | Availability-first (AP) read — nearest-replica, staleness-bounded |

## data

Update data/progress; logs; failure record. The job-mutation surface shipped at **emq.2.2** (`76fc947c`).

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`updateData-1`](../specs/emq.commands/features/data/updateData-1.md) | Replace a job's `data` hash field | **PORTED** — emq.2.2 `76fc947c`; `Jobs.update_data/4` (`@update_data`) | Keep the one-declared-key form; `data`→`payload`; gate id at `Keyspace.job_key/2`; immutability for stream-tier records (emq3.2) | Re-arms a job's payload before claim | Consistency-first: a single-slot HSET on the owning node, refuses (`:gone`) on a missing row |
| [`updateProgress-3`](../specs/emq.commands/features/data/updateProgress-3.md) | Write `progress` + XADD a `progress` event to the queue stream | **PORTED** — emq.2.2 `76fc947c`; `Jobs.update_progress/4` (`@update_progress`; the v2 form is a `PUBLISH emq:{q}:events`, not `XADD` — D-5/D-6) | Keep the field-write + the `PUBLISH` progress event; promote to a retained stream record at emq3.2 | A long-running job's heartbeat the dashboard watches | Availability-first observability edge: a subscriber-less `PUBLISH` is a no-op; degrades, never blocks |
| [`addLog-2`](../specs/emq.commands/features/data/addLog-2.md) | Append a line to a job's `:logs` list, keep-N trim | **PORTED** — emq.2.2 `76fc947c`; `Jobs.add_log/5`+`get_job_logs/3` (`@add_log`; both keys co-located by the braced root) | Keep two-declared-keys, keep-N `LTRIM`, honest count; forward: retention-as-policy for trimmed lines (emq3.5) | Per-job audit trail for a worker's diagnostics | Consistency-first but bounded: `:logs` co-locates by hashtag; a staleness-budget archive candidate |
| [`saveStacktrace-1`](../specs/emq.commands/features/data/saveStacktrace-1.md) | HMSET `stacktrace` + `failedReason` onto the job hash | **NOT YET (folded)** — no `save_stacktrace`; the failure record is the single `last_error` field set by `@retry`'s dead-letter arm, cleared by `reprocess_job/3` | A dedicated `@save_stacktrace` — separate `stacktrace`/`failed_reason` fields beside `last_error`, written on dead-letter, server-clock-stamped | Rich post-mortem on a dead job before reprocess | Consistency-first morgue: failure detail single-writer on the owning row |

## lifecycle

Remove · dedup release · drain · obliterate · pause/resume. The lifecycle/destructive plane shipped at **emq.2.2** (`76fc947c`); recursive-remove and clean verbs re-derive forward.

| Command | v1 (purpose) | v2 status (grounded) | v3 decision (PROPOSED) | BCS | EchoMesh |
|---|---|---|---|---|---|
| [`removeJob-2`](../specs/emq.commands/features/lifecycle/removeJob-2.md) | Remove one job from every state + all data; refuse if active/locked; optionally recurse into children | **PORTED** — emq.2.2 `76fc947c`; `Jobs.remove_job/4` (`@remove_job`): refuses locked `EMQLOCK`, `-1`→`:gone`, ZREM x4, DEL row + `:logs`, releases the dedup key iff it matches | Keep single-job `@remove_job`; add a declared-keys **recursive** variant walking the `Flows` subkeys, never a data-value `parent_key`; FLAT first (grandchildren → emq.3.5) | The removal verb behind an operator runbook / job cleanup | Operator-plane write on the consistency-first side — gated `EMQLOCK`, single-slot atomic |
| [`removeChildDependency-1`](../specs/emq.commands/features/lifecycle/removeChildDependency-1.md) | Break one parent↔child link; `SREM` child from the parent SET, `HDEL parent`; move parent to wait if last | **NOT YET** as a named verb — closest is the automatic `DECR` in `@flow_deliver`; v2 `:dependencies` is a STRING counter (Fork R2.A), no member to `SREM` | A new `Flows.drop_dependency/3` — `DECR` the declared counter + record in `:processed` (HSETNX), at-zero release; declared parent subkeys only | Cancel one leg of a multi-leg flow without failing the parent | Consistency-first single-slot same-queue; cross-queue detach rides `flow:outbox` |
| [`removeDeduplicationKey-1`](../specs/emq.commands/features/lifecycle/removeDeduplicationKey-1.md) | Release a dedup key iff it still points at this job: `GET`, compare, `DEL` on match | **PARTIAL / folded** — emq.2.2 `76fc947c`; the GET-compare-DEL is inlined into `@remove_job`'s `de:` branch; `get_deduplication_job_id/3` reads it; no standalone verb | Surface a standalone `Jobs.release_dedup/3` reusing the `de:` branch verbatim — declared `KEYS[1]=emq:{q}:de:<did>`, value-compare against the branded receipt | The idempotency-key release behind a producer that retired a window early | Consistency-first local key op; the dedup key is a single-slot per-queue structure |
| [`removeUnprocessedChildren-2`](../specs/emq.commands/features/lifecycle/removeUnprocessedChildren-2.md) | Recursively remove a job's children, ignoring processed & locked | **NOT YET** — no recursive child-removal verb; `Flows` ships fan-in + failure-policy, not a teardown | A bounded `Flows.remove_children/3` walking the declared `:dependencies`/`:processed`/`:failed`/`:unsuccessful` subkeys; per-slot same-queue, `flow:outbox`-hopped; FLAT first (grandchildren = emq.3.5) | Tear down a cancelled fan-out's child legs without touching the parent | Consistency-first same-queue; cross-queue teardown availability-leaning |
| [`cleanJobsInSet-3`](../specs/emq.commands/features/lifecycle/cleanJobsInSet-3.md) | Bulk-remove from one set older than a timestamp, up to a limit, skipping locked + scheduler jobs | **PARTIAL** — emq.2.2 `76fc947c`; `Admin.drain/3` (`@drain`) wipes `pending` (+ optional `schedule`), predicate-free; `obliterate/3` wipes a paused queue whole | A `Admin.clean/4` over the four v2 sets with a server-clock age grace + limit + honest count; per-set dispatch folds away | Age-based queue hygiene for work lanes during an incident | Availability-first maintenance: a bounded sweep that degrades, not blocks; single-slot |
| [`drain-5`](../specs/emq.commands/features/lifecycle/drain-5.md) | Empty wait+paused(+delayed)+prioritized, skipping job-scheduler-owned delayed; leaves active/completed/failed | **PORTED** — emq.2.2 `76fc947c`; `Admin.drain/3` (`@drain`): `KEYS[1]`=base, `KEYS[2]`=pending, optional `KEYS[3]`=schedule | Keep `@drain`; the v1 scheduler-skip re-derives as the repeat REGISTRY surviving (D-4), no per-job backref | Clears a queue's pending backlog during an incident without killing active jobs | A control-plane (CP) act on a queue surface — single-slot atomic, no availability claim |
| [`obliterate-2`](../specs/emq.commands/features/lifecycle/obliterate-2.md) | Destroy a *paused* queue iteratively: refuse if not paused / active (unless force), delete every set + key, bounded by `count` | **PORTED** — emq.2.2 `76fc947c`; `Admin.obliterate/3` (`@obliterate`); the obliterate fix landed at emq.2.4 `3298e4bc` | Keep the bounded `:more`/`:ok` form; `EMQSTATE`/`active jobs present` refusals; the v1 set-list collapses to four braced sets + lanes + `repeat`/`metrics:*`/`meta` | A control plane tears down ephemeral/test queues, no trace left | The CP edge of the dial — a destroy is correct-always; bounded budget keeps each call sound |
| [`pause-7`](../specs/emq.commands/features/lifecycle/pause-7.md) | Pause/resume globally: RENAME wait↔paused, set/clear `meta.paused`, manage the marker, XADD a `paused`/`resumed` event | **PORTED** — emq.2.2 `76fc947c`; `Admin.pause/2`+`resume/2` (`@pause`/`@resume`): a `paused` FIELD on `emq:{q}:meta`, claim paths read it → `:empty`; no RENAME, `@claim`/`@gclaim` byte-frozen | Keep FORM b (the `paused` field); the event PUBLISH rides the emq.2.3 watch plane | Quiesce a runaway lane's claiming without moving the backlog | A control-plane (CP) gate; pausing trades availability (`:empty`) for control — segmentation by operation |

---

*Lineage:* `emq.1.specs.md` (1290-line matrix) → this feature-sorted registry → the per-feature slices under [`emq.commands/features/`](../specs/emq.commands/features/) (≤300 lines each, with the v1→v3 side-by-side). Taxonomy: [`emq.epic.1.md`](emq.epic.1.md). Agent index: [`emq.commands/llms.txt`](../specs/emq.commands/llms.txt).
