# Two configs, described the same way: {script, keys, fields}. Both on the v1 wire.
# Config A — fixed reference
  script  moveToActive-11
  keys    emq:{q}:wait → emq:{q}:active + emq:{q}:{jobId}:lock
  fields  atm · ats · stc   # compressed (attemptsMade, attemptsStarted, stalledCounter)
# Config B — toggle its attemptsMade field above
  script  moveToActive-11          # same
  keys    emq:{q}:wait → emq:{q}:active + lock   # same
  fields  atm | attemptsMade · ats · stc   # the one part that can diverge
# verdict = compare the three parts (computed below, not hard-coded)
