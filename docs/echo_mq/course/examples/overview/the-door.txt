# the door, near side -> far side (the canonical door map)
  redis-patterns R0.2  ->   echomq E3            # the facade seam, the reserved tier
  redis-patterns R0.3  ->   echomq E1            # patterns become a protocol
  redis-patterns R1    ->   echomq E7            # caching → the near-cache
  redis-patterns R2    ->   echomq E1 / E3       # coordination & atomicity
  redis-patterns R3    ->   echomq E2 / E5 / E6  # reliable queues & the lifecycle
  redis-patterns R4    ->   echomq E6 / E4       # time, delay & priority
  redis-patterns R5    ->   echomq E6 / E7       # streams & events
  redis-patterns R6    ->   echomq E4 / E5       # flow control: groups & batches
  redis-patterns R8    ->   echomq E8            # production & conformance
# and back: every echomq chapter links to its redis-patterns R[N]
