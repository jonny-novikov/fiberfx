# The atm field across the runtimes — read from the real source, three files
# Go · the Redis hash path writes and reads the contract field "atm"
  queue_impl.go:102    "atm": job.AttemptsMade        # HSET → the hash
  script_runner.go:160 data["atm"]                  # HGETALL → read back
# Go · only the EXTERNAL JSON surface (L4) spells it out
  job.go:22            AttemptsMade int `json:"attemptsMade"`
# Elixir · the reader defends both at the boundary
  job.ex:220           Map.get(data, "attemptsMade") || Map.get(data, "atm", "0")
# on the WIRE (the hash): both use atm. The divergence is Go's JSON only.
