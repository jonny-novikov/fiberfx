# EMQ3.5 — the Mars brief (S3 the memory, part 1 — THE ARCHIVE: a store-side fold consumer, a merge-read over `W`, box-loss restore)

> The compact build brief — CALIBRATED to the as-built (lag-0). The body [`emq3.5.md`](emq3.5.md) is authoritative; the acceptance is [`emq3.5.stories.md`](emq3.5.stories.md); the run scope is [`emq3.5.prompt.md`](emq3.5.prompt.md). **This is a TWO-APP rung — built across `echo/apps/echo_mq` + `echo/apps/echo_store` (Operator-ruled D-1, HIGH risk).** Net-new fold/landing/merge-read code landed STORE-SIDE (`echo_store`: `EchoStore.StreamArchive` + `.Core` + `.Driver`); the `emq:{q}:stream:<name>:archived` polyglot cache key landed bus-side (`echo_mq`: `EchoMQ.Stream.put_archived/4`/`get_archived/3`/`clear_archived/3`), never the source of truth. The third-app rule bars `echo_graft` / `codemojex` / `echo_bot` / `mix.lock`. The fold targets the engine's PUBLIC `commit/3` (the engine internals UNTOUCHED — COEXIST, EBV3-2); the merge-read split is a branded `EVT` id (`W`), NEVER the integer `head_lsn` (F-1). **The DESIGN was RULED (the six convergences + EBV3-9/10 — [`../../kb/echo-bus-v3/echo-bus-v3.consolidated.md`](../../kb/echo-bus-v3/echo-bus-v3.consolidated.md) §2/§8) and the five Arms ruled D-3 (placement → SUPERVISED · trigger → TICK · posture → the store-side suite · the `:archived` key → BUILD, 77→78 · the box-loss cut → offline in-suite + live-Tigris tagged); the increment is BUILT to the rulings.**
>
> **Framing law (propagated).** Third person for any agent; no gendered pronouns for agents; no perceptual or interior-state verbs for agents or software (components read, compute, refuse, return); no first-person narration. Bind this same clause in any sub-brief.

## References (read first — the exact upstream, links/paths first)

1. **The body** — [`emq3.5.md`](emq3.5.md): Goal · §1 the fold contract (the fold-THEN-trim cycle, INV1 the no-loss invariant) · §2 the archive landing (`@archive_base` disjoint from every business page, INV4) · §3 the merge-read over `W` (a branded `EVT` id, NOT `head_lsn`, INV3/INV6) · §4 the fold consumer + box-loss restore (INV5) · §5 the two-app conformance/test posture (Arm 3) · the testing posture (the ≥100 loop) · INV1–10 · the closed error set · the Arms · DoD.
2. **The ruled decisions + the reconcile findings** — [`../progress/emq3-5.progress.md`](../progress/emq3-5.progress.md): **T-1** (the §0 5W + the solution space + INV1–5 as runnable checks + the HIGH-risk Squad triage); **D-1** (the TWO-APP boundary — net-new code STORE-SIDE, the optional `:archived` cache key bus-side, the third-app rule); **D-2** (the HIGH-risk Squad formation — Venus author · Mars two-pass · Director DEEPENED verify · Apollo MANDATORY); **T-2** (the two NO-INVENT findings — F-1 `W` is a branded id not `head_lsn`; F-2 `XTRIM` returns only a count, so fold-THEN-trim with the consumer owning the trim). Read for the LOCKED contract; do NOT re-frame D-1/D-2 as Arms.
3. **The RULED design (the six convergences + EBV3-9/10 — read for the locked floor, NOT to re-open)** — [`../../kb/echo-bus-v3/echo-bus-v3.consolidated.md`](../../kb/echo-bus-v3/echo-bus-v3.consolidated.md): §2 the build-ready decision set (F-LS-A the `@archive_base` page-index landing · F-ENG-A the native engine via `commit/3` · F-ENG-B the store-side fold consumer, no injected bus callback · F-ENG-C native alone, eg.6 deferred · F-PLAT-B the engine-derived `W` · F-PLAT-C distinct vocabulary) + §8 the Operator rulings (EBV3-9 Graft-canonical durability · EBV3-10 the archive GC watermark DISTINCT from the stream MINID — no MINID-coupling this rung). The convergences are the build surface; the Arms (placement/trigger/posture/cache/box-loss) are the Director's.
4. **The tier contract** — [`../../emq.streams.md`](../../emq.streams.md): the Stream Tier ladder, the three milestones (S1 writer · S2 readers · **S3 memory**), the emq3.5 row (`:72` — *"the archive: a group consumer folding trimmed segments into the `Graft` engine (CubDB → Tigris); deep reads = segment + live-tail merge"*; gate sketch *"segment fold == stream slice; box-loss restore; the merge-read property"*), the need (`:37` — *"Deep history without resident memory — stream segments folded to local CubDB and streamed to Tigris by the `EchoStore.Graft` engine (emq3.5)"*), and §The durable-archive answer (`:51-59` — the fold into the engine's CubDB → Tigris, one knob `remote_cfg`, box-loss = the engine's lazy `segments/{SEG}` fetch). emq3.5 is the FIRST rung of S3.

### Bus-side (`echo/apps/echo_mq`) — the slice SOURCE, byte-frozen this rung (READ, do not edit)

5. **The writer + retention to READ from (SHIPPED — BYTE-FROZEN this rung)** — `echo/apps/echo_mq/lib/echo_mq/stream.ex`:
   - `read/3..6` (`stream.ex:270`) — `read(conn, queue, name, from \\ "-", to \\ "+", count \\ nil)` → `{:ok, [{branded, fields_map}]}` in mint order; `parse_entry/1` (`:294`) recovers the stored `"id"` field as the branded receipt. **THE slice-read the fold consumer uses** (read the about-to-expire span as `{branded, fields}` tuples).
   - `trim/4` (`stream.ex:213`, two heads — `{:maxlen, count, approx?}` `:215` / `{:minid, %DateTime{}, approx?}` `:222`) → `{:ok, removed_count} | {:error, term}`; `minid_floor/1` (`:239`) the public `"<ms>-0"` floor helper. **THE trim the consumer drives AFTER the fold** (F-2 — `XTRIM` returns only a count; fold first).
   - `stream_key/2` (`:287`) — `emq:{q}:stream:<name>` via `Keyspace.queue_key(queue, "stream:" <> name)`. `append/4` (`:115`)/`append_id/5` (`:132`)/`append_batch/4` (`:167`) — the writer (the test SETUP mints slices with `append/4`). **ALL byte-FROZEN this rung (INV7).**
6. **The A1 id ↔ XADD-bound mapping (SHIPPED — the merge split; NO edit)** — `echo/apps/echo_mq/lib/echo_mq/stream/id.ex`: `xadd_id/1` (`:87`) maps a branded `EVT` id → `"<ms>-<tail22>"` where `<ms> = Snowflake.unix_ms(snow)` (the REAL Unix-ms). **`W` (a branded id) maps to an `XRANGE`/`XTRIM` bound via `xadd_id/1`** — the merge-read reads records > `W` live by setting the `XRANGE` lower bound to `W`'s xadd id (exclusive). `kind/0` (`:76`) → `"EVT"`; `evt?/1` (`:102`) — the kind predicate (assert `W` is an `EVT` id, INV6).
7. **The fold-then-trim driver PRECEDENT (SHIPPED — the SHAPE; FROZEN, NOT the archive's path)** — `echo/apps/echo_mq/lib/echo_mq/stream_retention.ex` + `stream_retention/core.ex`: the emq3.4 named/opt-in BARE-trim driver (`:transient` child) + its pure core (`decide/2`/`resolve/2`, exhaustive + disjoint over `{:maxlen,…}`/`{:minid,…}`/`{:minid,{:ago,ms},…}`). The archive's fold-then-trim consumer MIRRORS this shape (the `:transient` opt-in child + the pure decision core) but its destination is the ENGINE and it folds BEFORE trimming (F-2 — the bare driver folds nothing). **`git diff stream_retention.ex` MUST be EMPTY** (the archive is a NEW store-side process, not an edit to this one).
8. **The conformance harness (AS-BUILT 78 — the Arm-4 scenario landed)** — `echo/apps/echo_mq/lib/echo_mq/conformance.ex`: `scenarios/0`, **78** live (the prior 77 byte-unchanged + the `stream_archived` addition after `stream_retention` [`:207`] with its `apply_scenario(:stream_archived, …)` clause [`:2893`]; the moduledoc "seventy-eight" ×3; `run/2 → {:ok, 78}`); the pins re-pinned `conformance_run_test.exs:79` `{:ok, 78}` + `conformance_scenarios_test.exs` `@run_order` (78 atoms ending `:stream_archived` `:137`). The scenario is BUS-PURE (a `put_archived`→`get_archived`→overwrite→`clear_archived`→`:empty` round-trip — the cache CONTRACT, not the cross-app fold).
9. **The generic command path any optional `:archived` cache rides (SHIPPED — NO edit)** — `echo/apps/echo_wire/lib/echo_mq/connector.ex`: `command/3` (`:49`). An optional `:archived` cache is a stock `SET emq:{q}:stream:<name>:archived <W>` / `GET …` over `command/3` (no new script — declared-keys VACUOUS). `@wire_version "echomq:2.4.2"` — byte-unchanged (INV7/INV10). **`git diff echo/apps/echo_wire/` MUST be EMPTY.**
10. **The keyspace (SHIPPED, NO edit)** — `echo/apps/echo_mq/lib/echo_mq/keyspace.ex`: `queue_key/2` (`:14`). Any optional `:archived` key rides the existing `emq:{q}:stream:<name>:<sub>` form — NO grammar edit. **`git diff keyspace.ex` MUST be EMPTY.**

### Store-side (`echo/apps/echo_store`) — where the net-new fold/landing/merge code LANDS

11. **The native engine facade the fold TARGETS (SHIPPED — the PUBLIC surface; the engine internals UNTOUCHED, INV8)** — `echo/apps/echo_store/lib/echo_store/graft.ex`:
    - `open_volume/2` — starts a Volume's writer + store under `EchoStore.Graft.VolumeSup` (the `DynamicSupervisor`, NOT auto-started; the host/test starts `EchoStore.Graft.Supervisor` first). `new_volume_id/0` — a fresh `VOL` GID.
    - `begin/1`/`commit/3`/`snapshot/1`/`head_lsn/1`/`read_at/3` (the `defdelegate` surface to `VolumeServer`). `read/2` — a lock-free head read. **The fold commits via `commit/3`; the archive reads via `read_at/3` over a `snapshot/1`.**
12. **The single-writer commit (SHIPPED — `commit/3`, the fold target; NO edit)** — `echo/apps/echo_store/lib/echo_store/graft/volume_server.ex`:
    - `begin/1` (`:41`) → `{:ok, base_lsn}`; `commit/3` (`:50`) — `commit(volume_id, base_lsn, staged)` where `staged :: %{page_idx => binary}` → `{:ok, lsn} | {:error, {:conflict, head}} | {:error, term}` (OCC — a stale base retries upstream, re-`begin/1`-ing for the new head). `snapshot/1` (`:55`) → `%EchoData.Graft.Snapshot{volume_id, lsn, index}`, `index :: %{page_idx => {lsn, seg}}`; `head_lsn/1` (`:57`) — the INTEGER cursor (**F-1 — this is NOT `W`; it addresses pages, never the merge split**). With `remote_cfg`, the writer starts a `Streamer` shipping segments to Tigris (`:90`). **DO NOT edit this module (INV8) — the fold is a NEW consumer of `commit/3`.**
13. **The reserved-high-range landing (AS-BUILT — the realized mechanism in `stream_archive.ex` itself; the engine's flat page axis is the anchor)** — `echo/apps/echo_store/lib/echo_store/stream_archive.ex` over the engine's public `commit/3`:
    - `@archive_base = :erlang.bsl(1, 49)` (`stream_archive.ex:69`) — the archive's reserved high page range on the engine's flat page axis (`EchoStore.Graft.Store` keys `{:page, page_idx, lsn}`, `store.ex:14`). `bsl(1, 49)` is far above any realistic business page count, so an archive page is unique and never overwrites a business page (a low index a workload writes). **The archive's `@archive_base` is DISJOINT from every business page** (INV4 — the literal is pinned; the disjointness is the invariant). *(D-5: the prior outbox adapter `EchoStore.Durability.Graft` — `@obx_base = :erlang.bsl(1, 48)`, `plugins/graft.ex` — was the original reserved-range exemplar but is DROPPED this window (no backward compat); the archive owns the mechanism natively.)*
    - `fold/3` (`:106`) lands each record as a page: `page_idx = @archive_base + n; bytes = :erlang.term_to_binary({branded, fields}); EchoStore.Graft.VolumeServer.commit(vol, base, %{page_idx => bytes})` (`:121`). `read_archive/2` (`:188`) reads the span back via `read_at/3` over a `Store.index_at/3` snapshot (the `idx >= @archive_base` range, bounded by `folded_count/1` `:244`). The page-index seq `:arc_seq` is PEEKED before the commit (`peek_seq/1` `:273`) and advanced ONLY on a clean commit (`commit_seq/3` `:282`, a single CubDB transaction — the R-1 fix: a failed commit never leaves the seq ahead of the committed pages).
14. **The fold-CONSUMER SHAPE precedent (SHIPPED — mirror the subscribe/drain/frontier/via; NO edit)** — `echo/apps/echo_store/lib/echo_store/graft/committer.ex`: `EchoStore.Graft.Committer` — a `use GenServer` that `Sync.subscribe_commits/2`s at init, handles `{:emq_push, ["message", _channel, payload]}` live notices (`:67`), has a `:drain` catch-up (`:74`), persists a frontier (`SyncPoint` via `Store.{get,put}_syncpoint`, `advance/2` `:110`), and is `via/1`-Registry-named (`:41` — `{:via, Registry, {@registry, {:committer, vol}}}`). **The archive's fold consumer is THIS shape** with: the SOURCE = the archived stream's about-to-expire tail (read via `EchoMQ.Stream.read/3..6`), the DESTINATION = the engine's `@archive_base` range, the FRONTIER = `W` (a branded `EVT` id, persisted like the SyncPoint but as the archive frontier), the trigger per Arm 2. **`git diff committer.ex` EMPTY** (a NEW consumer module, not an edit).
15. **The CubDB store layer (SHIPPED — the archive's landing reuses it; NO edit)** — `echo/apps/echo_store/lib/echo_store/graft/store.ex`: `head_lsn/1` (`:35`, the integer cursor); `append/3` (`:42`, the atomic commit-row + pages transaction — what `commit/3` calls); `page_at/3` (`:60`); `index_at/3` (`:81`); `commits/3` (`:104`). **DO NOT edit (INV8).**
16. **The box-loss restore READ path (SHIPPED — the engine's lazy Tigris fetch; NO edit)** — `EchoStore.Graft.read_at/3` (the `Reader`) over a re-opened Volume's CubDB dir, or (with `remote_cfg`) a lazy re-fetch of `segments/{SEG}` from Tigris via `EchoStore.Graft.Streamer` (`streamer.ex`) / `EchoStore.Graft.Remote.Tigris` (`remote/tigris.ex`). The archive reads its records back by enumerating the `@archive_base` range of a snapshot. **The box-loss proof (INV5) drops the local CubDB dir and re-opens** (the offline path, the `durability_test.exs` precedent) **or re-fetches from Tigris** (the live path, `:tigris`-tagged, Arm 5).
17. **The supervision surface + the read-context the archive resolves (SHIPPED — NO edit)** — `echo/apps/echo_store/lib/echo_store/graft/supervisor.ex`: `EchoStore.Graft.Registry` (`:21`, a unique-keys Registry) + `EchoStore.Graft.VolumeSup` `DynamicSupervisor` (`:22`). The archive's Volume starts under `VolumeSup`; the archive's `store_for/1` (`stream_archive.ex:224`) READS the engine's published read context via `Registry.lookup(EchoStore.Graft.Registry, {:ctx, volume_id})` → `%{db: db}` (the `:db` pid `VolumeServer` registers at `volume_server.ex:107` via `Map.take(state, [:volume_id, :db, …])`, the same context `EchoStore.Graft.Reader` reads) — a CONSUMER of `{:ctx, vol}`, NOT a registrant of its own `via/1` name (the `Driver` carries its OWN optional `:name` for supervision). **NOT auto-started — the host/test starts `EchoStore.Graft.Supervisor`.**
18. **The OFFLINE engine-test precedent (SHIPPED — the determinism posture's offline floor)** — `echo/apps/echo_store/test/durability_test.exs`: `async: true`, no Valkey/Snowflake — exercises the engine's pure rollup/frontier/fencing/divergence (`Segment.build`, `SyncPoint.advance_local`, `Epoch.fence`, `Divergence.check`). **The box-loss LOCAL-restore path (INV5) follows this offline precedent**; the live-Tigris path is `:tigris`-tagged. There is NO live-`VolumeServer` test on disk today (the only live graft test is the RUST `graft_backend/live_round_trip_test.exs`, the coexisting peer) — so the archive's fold/restore/merge suite is NET-NEW live-engine scaffolding, store-side.
19. **The echo_store deps + version (SHIPPED — the boundary)** — `echo/apps/echo_store/mix.exs`: `version: "2.0.0"` (`:7`); `deps/0` (`:24`) declares `echo_data` + `echo_mq` + `echo_wire` + `exqlite` + `cubdb ~> 2.0` (`:26-35`). **`cubdb` (the engine dep) is already declared — NO new dependency, `mix.lock` unchanged (INV8).** The arrow is `echo_store → echo_mq` (one-way; `echo_mq` has no `echo_store` dep) — which FORCES the fold store-side (the bus cannot call the store, D-1/EBV3-3).

### Engine facts (cited valkey.io, NEVER memory)

20. `valkey.io/commands/xrange` — the mint-ordered slice read the fold consumes (`XRANGE <key> <start> <end> [COUNT n]`, the nested-array reply). `valkey.io/commands/xtrim` — the destructive trim the consumer drives AFTER the fold (`MINID [~|=] <id>` drop-below-id / `MAXLEN [~|=] <count>` keep-newest-N; **the integer removed-count-only reply — F-2, the reason fold MUST precede trim**). `valkey.io/topics/streams-intro` — capped streams, the archive-past-the-window pattern, `MINID` mint-time trimming.

## Requirements (numbered; each traced back to a story and forward to an invariant)

1. **The store-side fold consumer — the fold-THEN-trim cycle (INV1, the no-loss invariant) — AS-BUILT.** `EchoStore.StreamArchive.Driver` (`stream_archive/driver.ex`, a SUPERVISED `:transient` GenServer — Arm 1) over the pure `EchoStore.StreamArchive.Core` (`stream_archive/core.ex` — the `EchoMQ.StreamRetention.Core` analogue), on a TICK cadence (Arm 2), runs ONE `cycle/3` (`:190`): (a) read the about-to-expire slice via `EchoMQ.Stream.read/3..6` (`about_to_trim_slice/5` `:220`/`:234` — the span `(W, floor]`, the `from_bound/1` `:257` reading `(<W.xadd_id>` or `-`); (b) `StreamArchive.fold/3` (`:101`) commits a staged page map into the Volume at `@archive_base`, each page `{branded, fields}`; (c) `fold/3` advances `W` to the branded `EVT` id of the highest-folded record (`:arc_frontier`); (d) cache the bus seam (`cache_seam/4` `:282`); (e) ONLY THEN `trim/4` the now-archived span (the `with` chain trims LAST). Fold BEFORE trim (F-2 — `XTRIM` returns only a count; the consumer OWNS both calls; NO injected bus callback, EBV3-3). *(US1 → INV1, INV2.)*
2. **The archive landing — `@archive_base` disjoint from every business page (INV4, F-LS-A) — AS-BUILT.** `fold/3` lands each record as a CubDB page at `@archive_base + n` (`stream_archive.ex:121`) via the engine's public `commit/3`. `@archive_base = :erlang.bsl(1, 49)` (`stream_archive.ex:69`) — a reserved high page range on the engine's flat page axis, DISJOINT from every business page (a low index a workload writes); `bsl(1, 49)` is far above any realistic business page count, the archive owning `[bsl(1, 49), ∞)`. The page payload is `:erlang.term_to_binary({branded, fields})`; the page index is branded-id-MONOTONE (the `Enum.with_index(base_n)` fold; the seq peeked via `peek_seq/1` `:273`, advanced on success via `commit_seq/3` `:282` — the R-1 split). *(D-5: the dropped outbox adapter's `@obx_base = :erlang.bsl(1, 48)` was the original reserved-range exemplar — gone this window; the archive owns the discipline natively.) (US3 → INV4, INV2.)*
3. **The merge-read over `W` (INV3, INV6, F-1) — AS-BUILT.** `merge_read/5` (`stream_archive.ex:186` — `merge_read(conn, queue, name, volume_id, db)`) returns archived ∪ live-tail, split on `W` = the branded `EVT` id of the highest-folded record. Records with branded id ≤ `W` from the engine's `@archive_base` range (`read_archive/2` `:157` — `read_at/3` over a `Store.index_at/3` snapshot, NOT `VolumeServer.snapshot/1`, the empty-index trap avoided); records with branded id > `W` live (`EchoMQ.Stream.read/6` with the `XRANGE` lower bound `"(" <> W.xadd_id`, exclusive — `w_bound/1` `:222`). `W` is read from the `:arc_frontier` CubDB key the fold advances via `archive_frontier/1` (`:140`) → `{:ok, branded_W} | :empty`. **NEVER the integer `head_lsn` (F-1).** No-gap/no-overlap is a CONSEQUENCE of INV1 + the order theorem (not a per-read boundary check). *(US2 → INV3, INV6.)*
4. **Box-loss restore (INV5, the engine's Tigris property, EBV3-9).** Prove drop-local-CubDB → re-open → identical archive reads. The OFFLINE local-restore path (drop the dir, re-open the Volume from the same dir, assert identical reads) follows the `durability_test.exs` precedent (the engine's pure restore, no live S3); the LIVE-Tigris path (`:tigris`-tagged) drops the dir and re-fetches `segments/{SEG}`. The cut (in-suite vs tagged) is **Arm 5**. No new durability mechanism — the engine's existing `Streamer` + `Remote.Tigris` carry it. *(US4 → INV5.)*
5. **The two-app conformance/test posture (INV9, Arm 3) — AS-BUILT.** `EchoStore.StreamArchiveTest` (`echo/apps/echo_store/test/echo_store/stream_archive_test.exs`) proves INV1–INV6 POSITIVELY (the store has no `EchoMQ.Conformance`). The engine starts under `EchoStore.Graft.Supervisor` in `setup` (`:49`); `:valkey`-tagged for the slice round-trip on Valkey 6390 (suite default excludes `:valkey`), `:tigris`-tagged for the live-S3 restore (`:369`). The Arm-4 `:archived` cache scenario landed → `EchoMQ.Conformance` 77 → **78** (both pins re-pinned, the prior 77 byte-unchanged). *(US-GATE → INV9.)*
6. **The bus-side `:archived` cache key (Arm 4 ruled BUILD) — AS-BUILT.** Three stock `EchoMQ.Stream` fns over `EchoMQ.Connector.command/3` (NO new script, the key rides the existing `emq:{q}:stream:<name>:<sub>` form via `archived_key/2` `stream.ex:363`): `put_archived/4` (`:306`, a `SET`, written by `Driver.cache_seam/4` after advancing `W`) → `{:ok, "OK"}`; `get_archived/3` (`:326`, a `GET`) → `{:ok, w} | :empty | {:error, term}`; `clear_archived/3` (`:347`, a `DEL` — the NAMED cleanup on stream obliterate) → `{:ok, n}`. A polyglot cache of `W` (never source-of-truth — the engine's frontier is) + the additive `stream_archived` conformance scenario (Req 5/8). *(US2, US-GATE → INV3, INV9.)*
7. **The bus-side byte-freeze + the engine COEXIST boundary (INV7, INV8, INV10).** The shipped stream verbs / `EchoMQ.Stream` / `EchoMQ.StreamRetention` / every `@`-script byte-identical to HEAD (`git diff` of `stream.ex`/`stream_retention.ex`/`stream_consumer.ex`/`stream/id.ex` EMPTY — the archive READS/TRIMS via the existing public verbs); `echo_wire` UNTOUCHED; `@wire_version` `echomq:2.4.2`; `keyspace.ex` unedited; `grep -c redis.call` on the bus-side `lib/` diff = 0. The engine internals (`volume_server.ex`/`store.ex`/`reader.ex`/`streamer.ex`/`segment.ex`) EMPTY-diff (the fold targets the PUBLIC `commit/3` — a NEW consumer, not an engine edit); the Rust `echo_graft_backend` untouched; `mix.lock` unchanged (`cubdb` already declared). The moved label(s) reflect ONLY the changed app(s). *(US5 → INV7, INV8, INV10.)*
8. **The determinism posture — the ≥100 loop over the store-side fold suite (NOT a multi-seed sweep).** emq3.5 is a PROCESS + an at-rest engine write; the suite SETUP mints `EVT` ids (to build a slice) — so the same-millisecond mint hazard IS live. RUN the ≥100 loop over the store-side fold suite (`for i in $(seq 1 150); do TMPDIR=/tmp mix test || break; done` inside `echo_store`); the loop OWNS the machine (no concurrent liveness server). STATE the posture: the fold itself mints no ids (it folds the writer's); box-loss restore is unit-testable offline. *(US-GATE → all.)*

> **Dependency note (AS-BUILT — no dep moved).** `echo_store`'s `mix.exs` `deps/0` already declares `cubdb ~> 2.0` + `echo_mq` + `echo_data` + `echo_wire` — the fold needed NO new dep edge (the engine + the bus reads already compile-visible). `echo_mq`'s `deps/0` declares only `echo_data` + `echo_wire` (the bus side added a stock `SET`/`GET`/`DEL`, no dep). `mix.lock` is unchanged; the store-side suite uses no StreamData (the ≥100 loop is the determinism proof).

## Execution topology (the runtime shape, the build-order DAG, the EXACT files touched)

**Runtime shape (two apps).** The net-new code is STORE-SIDE: a `Committer`-shaped fold consumer (`EchoStore.*`, Arm 1 — supervised or host-wired) owns the fold-THEN-trim cycle for a declared archived stream; the archive lands as CubDB pages at `@archive_base` (a reserved high range on the engine's flat page axis, disjoint from every business page); a merge-read function returns archived ∪ live-tail split on `W` (a branded `EVT` id read from a NEW `archive_frontier/1` reader); box-loss restore is the engine's existing lazy Tigris fetch. The fold targets the engine's PUBLIC `commit/3` (the engine internals UNTOUCHED). BUS-SIDE: the shipped `EchoMQ.Stream.read/3..6` (the slice source) + `trim/4` (the trim, driven AFTER the fold) are READ byte-frozen; at most an OPTIONAL `:archived` cache key (Arm 4) lands as a stock `SET`/`GET`. The consumer mints NO ids (it folds the writer's `EVT` ids); the test SETUP mints `EVT` ids (to build a slice) — the ≥100-loop hazard.

**Build-order DAG (each step gated before the next):**

1. **Re-confirm the as-built on disk** (the lag-1 law — line numbers drift): `EchoMQ.Stream.read/3..6` + `trim/4` + `stream_key/2`; `Stream.Id.xadd_id/1` + `evt?/1`; `EchoStore.Graft.{open_volume,commit,snapshot,read_at}` + `VolumeServer.commit/3` + `Store.{head_lsn,page_at,index_at}/3` (the flat `{:page, page_idx, lsn}` page axis); `EchoStore.Graft.Committer` (the shape); `EchoStore.Graft.Supervisor` (the Registry + VolumeSup); the conformance count (77 → 78 as-built, Arm-4 `stream_archived`); both apps' `deps/0`. *(D-5: `plugins/graft.ex` + `@obx_base` are DELETED this window — the archive's reserved-range discipline is native, anchored on the engine's flat page axis.)*
2. **The archive landing** (store-side) — `@archive_base = :erlang.bsl(1, 49)` (a reserved high range on the engine's flat page axis, far above any business page; PINNED in the body §2) + the page-index seq `:arc_seq` peeked-before-commit / advanced-on-success (`peek_seq/1` + `commit_seq/3`, the R-1 split — a failed commit leaves the seq untouched) + the page payload (`{branded, fields}`). Prove INV4 (disjointness, with a Volume carrying BOTH archive pages AND low-index business pages via the public `commit/3`).
3. **The `W` frontier reader** (store-side) — `archive_frontier(vol_or_stream)` → `{:ok, branded_W} | :empty` (a CubDB frontier key the fold advances and/or the max id in the `@archive_base` range). Prove INV6 (`W` is a branded `EVT` id, never `head_lsn`).
4. **The fold consumer** (store-side, Arm 1/Arm 2) — the `Committer`-shaped module: read-slice (`Stream.read/3..6`) → `commit/3` at `@archive_base` → advance `W` → `trim/4`. Fold BEFORE trim (INV1). The placement + trigger per the Director's ruling.
5. **The merge-read** (store-side) — split on `W`: archived (`read_at/3` over a snapshot, the `@archive_base` range) ∪ live-tail (`Stream.read/3..6` with the `XRANGE` lower bound = `W`'s `xadd_id`). Prove INV3 (no-gap/no-overlap, `W` mid-stream).
6. **Box-loss restore** (store-side, Arm 5) — drop the local CubDB → re-open → identical reads (offline); the `:tigris`-tagged live re-fetch. Prove INV5.
7. **The store-side suite** (Arm 3) — `test/.../<archive>_test.exs` proving INV1–INV6 positively; start the engine under `EchoStore.Graft.Supervisor` in setup; `:valkey`/`:tigris` tags.
8. **IFF Arm 4** — the bus-side `:archived` cache key (`SET`/`GET` over `command/3`, cleanup NAMED) + the additive conformance scenario (77→78, both pins).
9. **The label** — the moved app's `mix.exs` version (within-family; settle which app moves + PIN in the body §INV10); `@wire_version` frozen `echomq:2.4.2`.
10. **The gate ladders + the ≥100 loop** (both apps) before reporting.

**The EXACT files touched:**

*Store-side (`echo/apps/echo_store`) — the net-new code (AS-BUILT):*
- `lib/echo_store/stream_archive.ex` (NEW — `EchoStore.StreamArchive`: the `@archive_base = :erlang.bsl(1, 49)` landing + `fold/3` + `archive_frontier/1` the `W` reader + `read_archive/2` + `merge_read/5` + `folded_count/1` + `store_for/1`; the pure no-process half).
- `lib/echo_store/stream_archive/core.ex` (NEW — `EchoStore.StreamArchive.Core`: the pure decision core `decide/2`/`resolve/2`, the `EchoMQ.StreamRetention.Core` analogue).
- `lib/echo_store/stream_archive/driver.ex` (NEW — `EchoStore.StreamArchive.Driver`: the SUPERVISED `:transient` fold-then-trim consumer — `child_spec/1`/`start_link/1`/the `:tick` cadence/`sweep/1`/`cycle/3`).
- `test/echo_store/stream_archive_test.exs` (NEW — `EchoStore.StreamArchiveTest`, the fold/restore/merge suite; `:valkey`/`:tigris` tags).
- `mix.exs` — UNCHANGED `2.0.0` (the store carried net-new modules but no label step; INV10).
- **UNTOUCHED (INV8):** `volume_server.ex`, `store.ex`, `reader.ex`, `streamer.ex`, `segment.ex`, `committer.ex`, `supervisor.ex` (the engine internals — READ, not edited). *(D-5: `plugins/graft.ex`/`plugins/postgres.ex` are DELETED this window — a SEPARATE pre-existing-cleanup concern, committed apart from the archive; emq3.5 never depended on them.)*

*Bus-side (`echo/apps/echo_mq`) — the additive `:archived` cache (Arm 4, AS-BUILT):*
- `lib/echo_mq/stream.ex` — ADDITIVE: `put_archived/4` (`:306`) + `get_archived/3` (`:326`) + `clear_archived/3` (`:347`) + `archived_key/2` (`:363`); the shipped fns byte-identical (0 deletions).
- `lib/echo_mq/conformance.ex` (+1 `stream_archived` scenario + helpers + moduledoc count) + `test/conformance_run_test.exs` + `test/conformance_scenarios_test.exs` (re-pin 77→78) + `mix.exs` (`2.6.3` → `2.6.4`).
- **UNTOUCHED (INV7):** `stream.ex` (the SHIPPED fns — append/read/trim/`stream_key`), `stream_retention.ex`, `stream_consumer.ex`, `stream/id.ex`, `keyspace.ex`, all `@`-scripts, `echo_wire/` (the whole app).

## Agent stories (each a Directive + an Acceptance gate — the contract IS the acceptance)

- **AS-1 · The fold-then-trim cycle (INV1, the no-loss invariant).** *Directive:* build the store-side fold consumer's ONE-cycle path — read-slice (`Stream.read/3..6`) → `commit/3` at `@archive_base` → advance `W` → `trim/4`; fold BEFORE trim, the consumer owning both calls (F-2). *Acceptance gate:* the store-side suite appends K records, runs one cycle on a prefix, and asserts every trimmed record reads back from the archive (the `@archive_base` range) AND is gone from the live stream AND the union == the original K (no loss). A trim-before-fold REORDER (the Director's mutant) is CAUGHT. *(precondition: a started Volume + K appended records; postcondition: prefix archived + trimmed, no loss; invariant: INV1, fold-before-trim.)*
- **AS-2 · The archive landing (INV4, disjointness).** *Directive:* land each folded record as a CubDB page at `@archive_base + n` (a reserved high range on the engine's flat page axis, disjoint from every business page), the payload `{branded, fields}`, branded-id-monotone. *Acceptance gate:* a Volume carrying BOTH low-index business pages (via the PUBLIC `commit/3`) AND archive pages reads each range back un-corrupted (`idx >= @archive_base` selects the archive; low indices select the business pages); an `@archive_base`-dropped-into-the-business-range mutant is CAUGHT (a collision). *(precondition: a Volume with business pages + archive pages; postcondition: each range correct; invariant: INV4 + INV2 monotone.)*
- **AS-3 · The `W` frontier reader (INV6, F-1).** *Directive:* build `archive_frontier/1` → `{:ok, branded_W} | :empty` — the branded `EVT` id of the highest-folded record, NOT the integer `head_lsn`. *Acceptance gate:* an assertion that `W` is a 14-byte branded `EVT` id (`BrandedId.valid?/1` + `Stream.Id.evt?/1` true); a `W`-as-integer-`head_lsn` mutant is CAUGHT by the merge-read no-gap/no-overlap assertion. *(precondition: a folded prefix; postcondition: `W` a branded id; invariant: INV6.)*
- **AS-4 · The merge-read (INV3).** *Directive:* build the merge-read returning archived ∪ live-tail, split on `W` (≤ `W` from the engine, > `W` from `XRANGE` with the lower bound = `W`'s `xadd_id`). *Acceptance gate:* fold-then-trim a prefix so `W` is MID-STREAM; a merge-read over the full range returns EXACTLY the K records in mint order, none missing (no gap), none doubled (no overlap); a fully-below-`W` range hits only the engine, a fully-above-`W` range only the wire. *(precondition: `W` mid-stream; postcondition: the union exact; invariant: INV3, no-gap/no-overlap a consequence of INV1 + the order theorem.)*
- **AS-5 · Box-loss restore (INV5).** *Directive:* prove drop-local-CubDB → re-open → identical archive reads (offline); the `:tigris`-tagged live re-fetch (Arm 5). *Acceptance gate:* the suite folds K records, DROPS the local CubDB dir, re-opens the Volume, and asserts identical reads (the same K branded ids + payloads in mint order); a merge-read after restore == before. *(precondition: a folded Volume; postcondition: identical reads after a real drop; invariant: INV5.)*
- **AS-6 · The bus-side byte-freeze + the engine COEXIST boundary (INV7, INV8, INV10).** *Directive:* keep the bus-side stream surface + the engine internals UNTOUCHED; fold via the PUBLIC `commit/3`; any optional `:archived` cache (Arm 4) a stock `SET`/`GET` (cleanup NAMED). *Acceptance gate:* `git diff` of the bus stream fns + the engine internals EMPTY (additive-only where a change lands); `grep -c redis.call` on the bus-side `lib/` diff = 0; `echo_wire`/`keyspace.ex` diffs EMPTY; `@wire_version` `echomq:2.4.2`; `mix.lock` unchanged; the moved label(s) reflect ONLY the changed app(s). *(precondition: the shipped surfaces; postcondition: byte-frozen + the engine a new consumer; invariant: INV7, INV8, INV10.)*
- **AS-GATE · The two-app gate + the ≥100 loop (INV9, all).** *Directive:* run BOTH app gate ladders + the ≥100 loop over the store-side fold suite + box-loss (offline + `:tigris`-tagged). *Acceptance gate:* the bus's `mix test --include valkey` + `Conformance.run/2 → {:ok, 78}` (the Arm-4 scenario landed); the store's `mix test` + `EchoStore.StreamArchiveTest` (INV1–INV6 positive); the ≥100 loop green (the loop owns the machine); box-loss proven; honest-row reporting (Valkey 6390). *(precondition: the as-built across both apps; postcondition: both ladders + the loop green; invariant: INV9 + the standing two-app gate.)*

A short comprehensive prompt (CALIBRATED — the build is done): the DESIGN was RULED (the six convergences + EBV3-9/10) and the five Arms ruled D-3; the two findings are F-1 (`W` is a branded `EVT` id, never `head_lsn`) + F-2 (`XTRIM` returns only a count, so fold-THEN-trim with the consumer owning the trim); the net-new code is STORE-SIDE (`EchoStore.StreamArchive` + `.Core` + `.Driver`; the engine's PUBLIC `commit/3` the fold target, the internals UNTOUCHED); the bus side is ADDITIVE-ONLY (`put/get/clear_archived` + the `stream_archived` scenario, 77→78, label 2.6.4). The precedents honored: the engine's flat page axis (a reserved high `@archive_base` range, the realized mechanism in `stream_archive.ex` itself) for the page-range landing, `EchoStore.Graft.Committer` for the consumer shape, `EchoMQ.StreamRetention` for the driver cadence, `EchoMQ.Stream.read/3..6`/`trim/4` for the slice + trim; the engine is the native `EchoStore.Graft`; the engine facts are cited valkey.io. *(D-5: the prior `EchoStore.Durability.Graft` outbox adapter — the original reserved-range exemplar — is DROPPED this window, no backward compat.)* The Director verifies BOTH gate ladders + the ≥100 loop; the Director commits by pathspec spanning both apps.
