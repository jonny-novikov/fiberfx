# EMQ3.6 · llms — the Mars build brief (TIME-TRAVEL + HYDRATION, the S3-memory closer)

> The agent brief Mars builds from. It DERIVES from the body [`emq3.6.md`](emq3.6.md) (authoritative) + the acceptance [`emq3.6.stories.md`](emq3.6.md). Build to THIS; cite the spec line for every public call; invent nothing. **This is a TWO-APP rung (`echo_mq` + `echo_store`).** **The Arms (Arm 1 scope · Arm 2 the read surface · Arm 3 the hydration mechanism [TIER-SETTING] · Arm 4 the hydration source · Arm 5 the conformance posture) are the Director's to rule via AskUserQuestion BEFORE you build — build to the RULINGS, never to an unruled Arm.** The risk tier (NORMAL vs HIGH) and whether the ≥100 loop + Apollo are mandatory turn on Arm 3 (one-shot vs supervised).
>
> **Framing law (propagated).** Third person for any agent; no gendered pronouns for agents; no perceptual or interior-state verbs for agents or software (components read, compute, refuse, return); no first-person narration. Bind this same clause in any sub-brief.

## References (read first — the exact upstream, both apps)

**The contract + acceptance:**
- [`emq3.6.md`](emq3.6.md) — the body (§0 the slice + the two-app boundary · §1 time-travel/INV-TT/INV-BOUND · §2 hydration/INV-HYDRATE/INV-NOCOMPACTOR · §3 the fence composition/INV-FENCE/INV-CURSOR · §4 the two-app posture/INV-BOUNDARY/INV-ADDITIVE · §5 the v2 master invariant · the INV summary · the closed error set · the Arms · DoD).
- [`emq3.6.stories.md`](emq3.6.stories.md) — US1–US6 + EMQ3.6-US-GATE + the Coverage map (the no-vacuous-pass liveness law per story).
- [`emq3.6.prompt.md`](emq3.6.prompt.md) — the run runbook (the pipeline stages + the two gate ladders + the risk tier).

**Bus-side surfaces (`echo/apps/echo_mq`) — the time-travel read lands here; the read path is reused:**
- `EchoMQ.Stream.read/3..6` (`lib/echo_mq/stream.ex:270`) — `XRANGE <key> <from> <to> [COUNT n]`, raw STRING bounds (default `"-"`/`"+"`), parses `{branded, fields_map}` in mint order via `parse_entry/1` (`:370`). The time-travel window read DELEGATES to this with mint-instant-derived bounds. **Byte-frozen** — the window read is a NEW fn that CALLS `read/6`, never an edit to it.
- `EchoMQ.Stream.minid_floor/1` (`stream.ex:239`) — `DateTime` → `"<ms>-0"` LOWER bound via `EchoData.Snowflake.unix_ms(min_for(dt))`. **Reused byte-frozen** for the window's `from`. The UPPER bound (the new inverse) does NOT exist — `trim/4` uses only this lower floor.
- `EchoMQ.Stream.Id.xadd_id/1` (`lib/echo_mq/stream/id.ex:87`) — a branded `EVT` id → `"<ms>-<tail22>"` (the real Unix-ms); `kind/0` (`:76`) → `"EVT"`; `evt?/1` (`:102`). For the upper-bound math + the mint-instant recovery.
- `EchoData.Snowflake.min_for/1` (`apps/echo_data/lib/echo_data/snowflake.ex:116`) — the smallest snowflake at-or-after an instant (`(unix_ms - epoch) <<< 22`, a RAW integer — NEVER to the wire as-is); `unix_ms/1` (`:107`); `to_datetime/1` (`:109`, a snowflake → its mint `DateTime` — for STATING the id-filter equivalence in the test).
- `EchoMQ.Conformance.scenarios/0` (`lib/echo_mq/conformance.ex:128`) — **78** scenarios as-built (`grep -c 'defp apply_scenario(' = 78`; the moduledoc comment at `:214` says "77" and is STALE by one). The two pins: `test/conformance_run_test.exs:79` `{:ok, 78}` + `test/conformance_scenarios_test.exs` `@run_order` (78 atoms ending `:stream_archived`). Re-pin to 79 IFF Arm 5 lands a bus-side time-travel scenario.
- `EchoMQ.Connector.command/3` (`apps/echo_wire/lib/echo_mq/connector.ex:49`) — the generic RESP path `read/6` rides; `@wire_version "echomq:2.4.2"` (FROZEN). `echo_wire` is UNTOUCHED.
- `EchoMQ.Stream.trim/4` (`stream.ex:213`), `append/4` (`:115`), `put_archived/4` (`:306`) — **byte-FROZEN this rung** (the cross-check the byte-freeze gate runs).

**Store-side surfaces (`echo/apps/echo_store`) — the hydration lands here; the Table is the target:**
- `EchoStore.Table.put/4` (`lib/echo_store/table.ex:97`) — the versioned write: `SET <l2> version<>value PX ttl` + `:ets.insert {id, value, exp, version}`. The hydration writes each tail record keyed on its entity id, versioned by the branded `EVT` id. `put/3` (`:90`) mints a version itself — the hydration uses `put/4` with the record's branded `EVT` id AS the version (so newer-wins is mint-order over the same id space).
- `EchoStore.Table.fetch/3` (`table.ex:63`) — the read-back the hydration suite asserts against.
- `EchoStore.Table.apply_coherence/4` (`table.ex:115`) — DROPS the L1 row iff `version` newer (a cache INVALIDATION — "the writer already placed the newer value in L2"). **NOT a fold mechanism** — hydration writes via `put/4`, not `apply_coherence/4`. The private `apply_newer_wins/3` (`:513`) is the comparison primitive (reached only via `{:coherence,...}`).
- `EchoStore.Coherence.newer?/2` (`lib/echo_store/coherence.ex:52`) — the mint-order comparison over the 11-byte base62 payload (`pa > pb`). The newer-wins SEMANTIC — assert per-key newest-mint-id wins via this.
- `EchoStore.Table` `:tracking` lane (`table.ex:256`, `arm_tracking/2` `:564`) — the RESP3 server-assisted staleness fence (BCAST over `ecc:{table}:`). The fence the hydrate-then-fence composition rides; hydration adds NO new fence.
- `EchoMQ.Stream.read/3..6` (read via `echo_store`'s `echo_mq` dep) — the tail SOURCE; `EchoStore.StreamArchive.merge_read/5` (`lib/echo_store/stream_archive.ex:191`) the deep source (archived ∪ live-tail) IFF Arm 4 rules the merge-read source.
- `EchoStore.Graft.Supervisor` (`lib/echo_store/graft/supervisor.ex:22`, `VolumeSup`) — started in the suite setup IFF Arm 4 rules the merge-read deep source (it needs a Volume); a tail-only hydrate needs NO engine.
- `EchoStore.StreamArchive.Driver` (`lib/echo_store/stream_archive/driver.ex`) — the SUPERVISED `:transient` consumer + pure `.Core` SHAPE precedent IFF Arm 3 rules a supervised hydrator.

**Dep arrows (the two-app law):** `echo_store/mix.exs:24-32` declares `echo_data` + `echo_mq` + `echo_wire` + `exqlite` + `cubdb`; the arrow is `echo_store → echo_mq` (one-way). So the time-travel READ lives bus-side; the hydration FROM the bus stream INTO a Table lives store-side. `echo_mq` has NO `echo_store` dep — a store-side time-travel read is structurally impossible, and a bus-side hydrate is structurally impossible.

**The program law + the as-built map:** `.claude/skills/echo-mq-program.md` (the v2 laws, the gate ladder, the conformance additive-minor law) · `.claude/skills/echo-mq-surface.md`.

## Requirements (numbered; each traced back to a story + forward to an invariant)

1. **The time-travel window read** (bus-side, `EchoMQ.Stream`) — a NEW fn (Arm 2's name/arity) that takes a `DateTime` window and returns the in-window entries in mint order, by DELEGATING to the shipped `read/6` with mint-instant-derived bounds. ZERO new Lua. (US1 → INV-TT, INV-ADDITIVE.)
2. **The window bound math** — the `from` reuses the shipped `minid_floor/1`; the `to` is the NEW upper-bound inverse (the largest id at-or-before `t1`, `"<ms>-<maxseq>"`). NEVER a raw `min_for/1` integer to the wire. The inclusive/exclusive edge per Arm 2. (US2 → INV-BOUND.)
3. **The stream-tail hydration into a Table** (store-side) — a NEW surface (Arm 3 — a one-shot `EchoStore.Table.hydrate_from_stream/_` OR a supervised hydrator module) that reads a tail (Arm 4 — `read/6` live OR `merge_read/5` deep) and folds it into a Table by newer-wins (`put/4` per key, versioned by the branded `EVT` id). NO background compaction; the source read-only. (US3 → INV-HYDRATE, INV-NOCOMPACTOR.)
4. **The fence composition** — the hydrate SEEDS the Table; a subsequent admission through the `:tracking` fence (a newer `put/4` OR an invalidation push + reload) wins over a stale hydrated value; a stale admission loses. Hydration adds NO new fence. (US4 → INV-FENCE.)
5. **The supervised hydrator's two-phase write** (CONDITIONAL — only if Arm 3 rules supervised) — the cursor advances ONLY after the Table write; a crash re-hydrates harmlessly (newer-wins idempotent). VACUOUS + dropped if one-shot. (US5 → INV-CURSOR.)
6. **The two-app boundary + the byte-freeze** — the time-travel read additive over the byte-frozen bus stream surface; `echo_wire` EMPTY-diff; `@wire_version` `echomq:2.4.2`; `grep -c redis.call` on the bus diff = 0; the hydration through the PUBLIC Table API; the engine internals + `echo_graft` + `mix.lock` EMPTY-diff; no third app; the label additive per touched app. (US6 → INV-BOUNDARY, INV-ADDITIVE.)
7. **The conformance posture** (Arm 5) — a NEW store-side hydration ExUnit suite (forced — `echo_store` has no conformance) proving INV-HYDRATE/NOCOMPACTOR/FENCE positively (+ INV-CURSOR iff supervised); a bus-side time-travel conformance scenario (78 → **79**, both pins re-pinned, the prior 78 byte-unchanged) IFF Arm 5 rules it lands, else the bus conformance FROZEN at 78. (EMQ3.6-US-GATE → INV-ADDITIVE.)
8. **The gate ladders + the determinism posture** — BOTH app ladders green on Valkey 6390; the ≥100 loop over the store-side hydration suite IFF Arm 3 rules supervised (id-minting setup + a process), else a multi-seed sweep + an honest posture statement; honest-row reporting. (EMQ3.6-US-GATE.)

## Execution topology (the runtime shape + the build-order DAG + the EXACT files touched)

**Runtime shape.** Two additive surfaces, no shared state:
- BUS-SIDE: a pure additive read fn on `EchoMQ.Stream` — `DateTime` window → mint-instant bounds (the shipped `minid_floor/1` + the new upper inverse) → `read/6` → `{:ok, [{branded, fields_map}]}`. No process, no Lua, no lease.
- STORE-SIDE: the hydration — Arm 3 rules the shape (a one-shot fn that reads a bounded tail + folds it into a Table via `put/4`, OR a supervised `:transient` hydrator over a pure `.Core`, the emq3.5 `.Driver` shape, with a persisted cursor).

**Build-order DAG (build to the Arm RULINGS):**
1. **Re-confirm the as-built on disk** (the lag-1 law) — `read/6`, `minid_floor/1`, `Stream.Id.xadd_id/1`, `Snowflake.min_for/1`/`unix_ms/1`/`to_datetime/1`, `Table.put/4`/`fetch/3`, `Coherence.newer?/2`, the `:tracking` lane, the conformance count (78).
2. **The upper-bound math** (bus-side) — the inverse of `minid_floor/1`: a `DateTime` → the LARGEST entry id at-or-before it (`"<ms>-<maxseq>"`), or the Arm-2 inclusive/exclusive form. Pure, host-side, ZERO Lua. (Req 2.)
3. **The time-travel window read** (bus-side) — the NEW `EchoMQ.Stream` fn (Arm 2's name) that computes `from`/`to` from the window and delegates to `read/6`. (Req 1.)
4. **The hydration surface** (store-side, Arm 3) — the NEW one-shot fn OR the supervised hydrator: read a tail (Arm 4 — `read/6` or `merge_read/5`), fold per key via `put/4` versioned by the branded `EVT` id. (Req 3.)
5. **The fence composition** (store-side) — verify the hydrate-then-fence path (`:tracking` admission wins over stale); this is mostly a TEST over the shipped fence, not new code (the fence exists). (Req 4.)
6. **The cursor + two-phase write** (store-side, CONDITIONAL — Arm 3 supervised) — the persisted cursor (the emq3.5 `:arc_seq`/`SyncPoint` precedent), advanced only after the Table write. (Req 5.)
7. **The store-side hydration suite** (Arm 5) — INV-HYDRATE/NOCOMPACTOR/FENCE positive (+ INV-CURSOR iff supervised); the engine started under `EchoStore.Graft.Supervisor` in setup IFF Arm 4 deep source; `:valkey`-tagged for the live-wire slice read. (Req 7.)
8. **IFF Arm 5 the bus-side time-travel scenario** — an additive `EchoMQ.Conformance` scenario (a window read over a known set of appends, asserting window == id-filter; 78 → 79, both pins re-pinned). (Req 7.)
9. **The label** (the moved app's `mix.exs`) — `echo_mq` 2.6.4 → 2.6.5 (the time-travel read is always a bus touch); `echo_store` 2.0.0 → 2.0.1 IFF a new public hydration surface. (Req 6.)
10. **BOTH gate ladders + the determinism posture** (Arm 3 — the ≥100 loop iff supervised, else a sweep). (Req 8.)

**The EXACT files touched:**
- BUS-SIDE (`echo/apps/echo_mq`): `lib/echo_mq/stream.ex` (ADDITIVE — the new time-travel read fn + the upper-bound math; the append/trim/archive-cache surface byte-FROZEN) + IFF Arm 5: `lib/echo_mq/conformance.ex` (+1 scenario) + `test/conformance_run_test.exs` + `test/conformance_scenarios_test.exs` (the two pins, 78 → 79) + `test/stream_test.exs` (the time-travel read tests) + `mix.exs` (2.6.4 → 2.6.5). NO `echo_wire` edit. NO new Lua. NO `keyspace.ex` edit.
- STORE-SIDE (`echo/apps/echo_store`): the NEW hydration surface (`lib/echo_store/table.ex` ADDITIVE for a one-shot `hydrate_from_stream/_`, OR a NEW `lib/echo_store/stream_hydrator.ex` (+ `.Core`) for a supervised hydrator) + `test/echo_store/<hydration>_test.exs` (the new suite) + `mix.exs` (2.0.0 → 2.0.1 IFF a new public surface). The engine internals UNTOUCHED (hydration reads through the public Table API + iff Arm 4 `merge_read/5`).
- UNTOUCHED: `echo_wire` (the whole app), the native engine internals (`volume_server.ex`/`store.ex`/`reader.ex`/`streamer.ex`/`segment.ex`), the Rust `echo_graft`, `codemojex`, `echo_bot`, `mix.lock` (no dep moves).

## Agent stories (each a Directive + an Acceptance gate; the contract IS the acceptance criterion)

- **AS-1 · The upper-bound math (bus-side).** *Directive:* build the inverse of the shipped `minid_floor/1` — a `DateTime` → the `XRANGE` upper bound `"<ms>-<maxseq>"` (the largest entry id at-or-before the instant), reusing `Snowflake.unix_ms/1`; the inclusive/exclusive edge per Arm 2. NEVER a raw `min_for/1` integer to the wire. *Acceptance gate:* `precondition` a `DateTime`; `postcondition` an `ms-seq` string bound; `invariant` the exact-`ms` edge holds (a `t1` record IN an inclusive `to`, a `t1+1ms` record OUT). INV-BOUND.
- **AS-2 · The time-travel window read (bus-side).** *Directive:* build the NEW `EchoMQ.Stream` fn (Arm 2's name/arity) — a `DateTime` window → `from` (the shipped `minid_floor/1`) + `to` (AS-1) → `read/6` → `{:ok, [{branded, fields_map}]}` in mint order; a malformed name raises `ArgumentError` before any wire (the `append_id/5` precedent); ZERO new Lua. *Acceptance gate:* `precondition` N records across known instants; `postcondition` the in-window entries in mint order; `invariant` the result EQUALS `Enum.filter(full_read, &(mint_instant in window))` — the window ACTUALLY excludes out-of-window entries (US1 straddle). INV-TT, INV-ADDITIVE.
- **AS-3 · The stream-tail hydration (store-side, Arm 3).** *Directive:* build the NEW hydration surface (Arm 3 — a one-shot `hydrate_from_stream/_` OR a supervised hydrator over a pure `.Core`) that reads a tail (Arm 4 — `read/6` live OR `merge_read/5` deep) and folds it into a Table via `put/4` per key, versioned by the record's branded `EVT` id; NO compaction; the source read-only. *Acceptance gate:* `precondition` K records / D keys, ≥2 per key at distinct known instants; `postcondition` the Table holds per key the value with the MAX branded `EVT` mint id; `invariant` newer-wins by mint order (`Coherence.newer?/2`) — a key receiving a second, newer record reads back the NEWER value. INV-HYDRATE, INV-NOCOMPACTOR.
- **AS-4 · The fence composition (store-side).** *Directive:* verify hydrate-then-fence — after a hydrate, a newer admission through the `:tracking` fence (`put/4` newer, OR a `:tracking` invalidation + reload) wins over the stale hydrated value; a stale admission loses. Hydration adds NO new fence. *Acceptance gate:* `precondition` a key hydrated to V1(m1); `postcondition` admitting V2(m2>m1) → `fetch` = V2, admitting V0(m0<m1) → `fetch` = V1; `invariant` hydrate-then-fence equals loader truth (INV-FENCE).
- **AS-5 · The supervised cursor (store-side, CONDITIONAL — Arm 3 supervised).** *Directive:* IFF Arm 3 rules supervised, persist a cursor (the emq3.5 `:arc_seq`/`SyncPoint` precedent) advanced ONLY after the Table write; a crash re-hydrates harmlessly. SKIP entirely if one-shot. *Acceptance gate:* `precondition` a supervised hydrator mid-fold; `postcondition` a crash between the write and the advance → restart → the Table holds the same per-key latest value; `invariant` cursor-after-write + newer-wins idempotence (INV-CURSOR). VACUOUS if one-shot.
- **AS-6 · The hydration suite (store-side, Arm 5).** *Directive:* build the NEW store-side ExUnit suite (the `EchoStore.StreamArchiveTest` precedent) — INV-HYDRATE/NOCOMPACTOR/FENCE positive (+ INV-CURSOR iff supervised); the engine started under `EchoStore.Graft.Supervisor` in setup IFF Arm 4 deep source; `:valkey`-tagged for the live-wire slice read; the no-vacuous-pass conditions per US3/US4 (≥2 per key; a real fence admission). *Acceptance gate:* `precondition` Valkey 6390 up; `postcondition` the suite green proving the three (four) invariants positively; `invariant` no scenario is a no-op (each exercises its outcome).
- **AS-GATE · The two-app gate + the byte-freeze + the determinism posture.** *Directive:* run BOTH app ladders on Valkey 6390; verify the byte-freeze (`echo_wire` EMPTY-diff, `grep -c redis.call` on the bus diff = 0, `@wire_version` `echomq:2.4.2`, the bus stream append/trim surface byte-frozen, the engine internals EMPTY-diff, `mix.lock` unchanged, no third app); run the ≥100 loop over the store-side suite IFF Arm 3 supervised (id-minting setup), else a multi-seed sweep + an honest posture statement; re-pin the conformance count IFF Arm 5 (78 → 79, both pins, the prior 78 byte-unchanged); the label per touched app. *Acceptance gate:* `precondition` both apps built; `postcondition` both ladders green + the determinism posture met + the boundary clean; `invariant` INV-BOUNDARY + INV-ADDITIVE; honest-row reporting. **Apollo MANDATORY IFF Arm 3 supervised.**

## The prompt (a short comprehensive directive that leaves no decision the spec has not fixed)

Build emq3.6 — TIME-TRAVEL + HYDRATION, the S3-memory closer — to the body [`emq3.6.md`](emq3.6.md) and the RULED Arms (the Director rules Arm 1 scope · Arm 2 the read surface · Arm 3 the hydration mechanism · Arm 4 the source · Arm 5 the conformance posture BEFORE you build — build to the rulings, never an unruled Arm). TWO apps: the time-travel window read is an ADDITIVE `EchoMQ.Stream` fn (bus-side — a `DateTime` window → mint-instant bounds via the shipped `minid_floor/1` + a NEW upper-bound inverse → the byte-frozen `read/6`; ZERO new Lua; the window read EQUALS the id-filtered full read, INV-TT); the hydration is the NEW store-side surface (Arm 3 — a one-shot `hydrate_from_stream/_` OR a supervised hydrator; it reads a tail [Arm 4 — `read/6` live OR `merge_read/5` deep] and folds it into a Table via `put/4` versioned by the branded `EVT` id, holding per key the newest-mint-id value, newer-wins, no compactor, INV-HYDRATE; then the `:tracking` staleness fence keeps it fresh — hydrate-then-fence equals loader truth, INV-FENCE). IFF Arm 3 rules supervised, the cursor advances only after the Table write and a crash re-hydrates harmlessly (INV-CURSOR; VACUOUS if one-shot). The bus stream append/trim/archive surface is byte-FROZEN; `echo_wire` UNTOUCHED; `@wire_version` `echomq:2.4.2`; `grep -c redis.call` on the bus diff = 0; the engine internals + `echo_graft` + `mix.lock` EMPTY-diff; the diff is purely `{echo_mq, echo_store}`; the label climbs additively per touched app. The store-side hydration suite proves the invariants positively (no vacuous pass — ≥2 records per key, a real fence admission); IFF Arm 5 a bus-side time-travel conformance scenario lands (78 → 79, both pins re-pinned, the prior 78 byte-unchanged). Run BOTH gate ladders on Valkey 6390 + the ≥100 loop IFF Arm 3 supervised (else a multi-seed sweep + an honest posture); honest-row reporting; cite the spec line for every public call; invent nothing.
