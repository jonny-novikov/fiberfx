# EMQ3.4 — the Mars brief (S2 the readers, part 2 — RETENTION AS POLICY: `EchoMQ.Stream.trim/4` + a declared per-stream window + the named/opt-in trim driver)

> The compact build brief. The body [`emq3.4.md`](emq3.4.md) is authoritative; the acceptance is [`emq3.4.stories.md`](emq3.4.stories.md); the run scope is [`emq3.4.prompt.md`](emq3.4.prompt.md). Build ONLY inside `echo/apps/echo_mq` (the trim rides the shipped `echo_wire` connector `command/3` — NO `echo_wire` edit). Cite the spec line for every public call; `XTRIM` is issued DIRECT as a `parts` list (NO new `Script.new/2` — emq3.4 is a no-new-Lua rung); the conformance additive-minor mechanics (76→77). **The forks are RULED (the Operator ruled the two open emq3.4 forks — D-2, D-3 in [`../progress/emq3-4.progress.md`](../progress/emq3-4.progress.md)); build to the ruling, do NOT re-open.**
>
> **Framing law (propagated).** Third person for any agent; no gendered pronouns for agents; no perceptual or interior-state verbs for agents or software (components read, compute, refuse, return); no first-person narration. Bind this same clause in any sub-brief.

## References (read first — the exact upstream, links/paths first)

1. **The body** — [`emq3.4.md`](emq3.4.md): Goal · §1 the trim contract (the two window forms, the blast radius bounded) · §2 the `MINID` floor derived from `min_for/1` (INV6, the rung's one piece of id-math) · §3 the declared policy + the named/opt-in driver (D-2, D-3) · the testing posture (NO ≥100 loop) · INV1–10 · the closed error set · the forks RULED · DoD. The forks are the Operator's ruling: D-1 the TRIO formation · D-2 the trim cadence = NAMED/OPT-IN (the `StreamConsumer` loop FROZEN) · D-3 the policy storage = BEAM-side declared config (no keyspace subkey).
2. **The ruled decisions + the T-1 derivation** — [`../progress/emq3-4.progress.md`](../progress/emq3-4.progress.md): the T-1 5W + solution space + INV1–6 as runnable checks + the topology triage (HIGH-risk, TRIO, the multi-seed sweep NOT the ≥100 loop); D-1 formation; **D-2** (the trim cadence = named/opt-in — the consequence: NO edit to the frozen emq3.3 `StreamConsumer` loop); **D-3** (the policy storage = BEAM-side declared config — no `emq:{q}:` subkey, no at-rest cleanup obligation). Read for the LOCKED contract; do NOT re-frame the decisions as Arms.
3. **The tier contract** — [`../../emq.streams.md`](../../emq.streams.md): the Stream Tier ladder (emq3.1–3.6), the three milestones (S1 writer · S2 readers · S3 memory), the emq3.4 row (`:71` — *"retention as policy: per-stream `MAXLEN` (approx) and mint-time `MINID` windows, declared not defaulted"*; gate sketch *"trim honors the window; a read inside the window never misses; outside, it answers truthfully"*), the need (`:34` — *"Bounded retention as policy — the compliance window and memory truth, via `XTRIM` `MAXLEN` (approx) and `MINID` (mint time)"*). emq3.4 is the SECOND rung of S2; the archive fold is emq3.5.
4. **The design KB (the RULED forks — read for the argument, NOT to re-open)** — [`../../kb/streams-tier/streams.synthesis.md`](../../kb/streams-tier/streams.synthesis.md) §2 (the build-ready emq3.4 decision set — the converged `trim/4` verb + the declared policy) + §3 (F3.4-A, the ONE divergence — the trim cadence, ruled D-2 = Lens B + the Director's reconciliation) + §6 (F3.4-B the policy storage, ruled D-3 = BEAM-side). The CONVERGED half (a dedicated public `trim/4` + a declared policy, `MAXLEN ~` approx default + mint-instant `MINID`) is what Mars builds; the cadence + storage forks are RULED (D-2, D-3) — there is NO open decision for Mars.
5. **The writer it EXTENDS (SHIPPED — `trim/4` is an ADDED function; the existing ones BYTE-FROZEN)** — `echo/apps/echo_mq/lib/echo_mq/stream.ex`:
   - the pre-declared retention slot (`stream.ex:50`): *"Retention (`MAXLEN`/`MINID`) is emq3.4; `append/_` does not trim."* **emq3.4 lands `trim/4` HERE** (a new public function in `EchoMQ.Stream`, beside `append/4`).
   - `append/4` (`stream.ex:77`) + `append_id/5` (`:94`) + `append_batch/4` (`:129`) + `read/3..6` (`:156`) + `stream_key/2` (`:173`) — **ALL byte-FROZEN this rung** (INV5; `trim/4` is added, these are untouched). `read/3..6` is the read-back the conformance scenario uses to prove survival/deletion (`parse_entry/1` `:180-183` recovers the stored `"id"` field as the branded receipt).
   - `stream_key/2` (`stream.ex:173`) — `emq:{q}:stream:<name>` via `Keyspace.queue_key(queue, "stream:" <> name)`. **`trim/4` addresses the stream via THIS key** (no grammar edit, INV8).
6. **The mint-instant math (SHIPPED — the `MINID` floor; NO edit)** — `echo/apps/echo_data/lib/echo_data/snowflake.ex`:
   - **`min_for/1` (`snowflake.ex:116`)** — `(DateTime.to_unix(dt, :millisecond) - @epoch_ms) <<< 22` — the smallest snowflake mintable at-or-after a `DateTime` instant (for half-open time-range scans). **THE floor source (INV6).**
   - `unix_ms/1` (`snowflake.ex:107`) — `(snow >>> 22) + @epoch_ms` — the inverse, recovering a snowflake's Unix-ms. **`unix_ms(min_for(dt))` == `DateTime.to_unix(dt, :ms)`** (the floor's ms component).
   - `to_datetime/1` (`:110`); `epoch_ms/0` (`:122`) → `1_704_067_200_000`. **The `MINID` floor id is `"<ms>-0"`, `ms = unix_ms(min_for(dt))`** — derived from `min_for/1`, NEVER raw epoch arithmetic, NEVER `min_for/1`'s 63-bit integer handed to `XTRIM` (the wire wants `ms-seq`, not a snowflake int).
7. **The A1 id mapping the floor mirrors (SHIPPED — NO edit)** — `echo/apps/echo_mq/lib/echo_mq/stream/id.ex`: `xadd_id/1` (`:87`) maps a branded id to its EXPLICIT XADD id `"<ms>-<tail22>"` where `ms = unix_ms(snow)` (the writer's A1 mapping, `stream.ex:18-20`). **The `MINID` floor `"<ms>-0"` is the smallest entry at that ms** (tail `-0` the lowest sequence) — the same `ms` domain the writer's ids live in, so `MINID "<ms>-0"` removes exactly the entries minted in an earlier ms. `kind/0` (`:76`) → `"EVT"`.
8. **The opt-in cadence PRECEDENT (SHIPPED — the SHAPE to follow for the driver; NO edit)** — `echo/apps/echo_mq/lib/echo_mq/pump.ex`: `EchoMQ.Pump` + `EchoMQ.Pump.Core` — a `:transient` OPT-IN child + a PURE tick/decision core, owner-started (`no mod:` — never a default-on background sweep; `.claude/skills/echo-mq-surface.md`: *"the opt-in cadence … a pure tick/batch decision core … owner-started, no `mod:`"*). **The trim driver is the SAME shape:** a named, opt-in, owner-started `:transient` child whose PURE decision core (given the declared policy + the injected clock) answers WHICH `trim/4` call to make (or `:noop`), exhaustive + disjoint over the policy forms; a thin process router applies it. The pure core is testable WITHOUT a live process (the `Pump.Core`/`BatchShaper.Core` injected-clock precedent — the architect rule "a verdict/shaping/decision surface is a pure module, not a process `defp`").
9. **The generic command path to RIDE (SHIPPED — NO edit)** — `echo/apps/echo_wire/lib/echo_mq/connector.ex`: `command/3` (`:49`) — `command(conn, parts, timeout \\ 5_000)`, `parts` a `[binary() | integer() | atom()]` list. **`trim/4` rides THIS:** `command(conn, ["XTRIM", key, "MAXLEN", "~", count])` or `command(conn, ["XTRIM", key, "MINID", "~", floor])` (the `=` opt-in drops the `~`). `@wire_version "echomq:2.4.2"` — byte-unchanged (INV7/INV10). **The `echo_wire` diff MUST be EMPTY.** (Re-probe `command/3` at the reconcile; cite by surface name.)
10. **The frozen emq3.3 reader (NOT touched — D-2)** — `echo/apps/echo_mq/lib/echo_mq/stream_consumer.ex`: the BEAM consumer group loop. **`git diff stream_consumer.ex` MUST be EMPTY** (D-2 — the trim is NOT folded into the consumer beat; retention is decoupled from consumer liveness). Read its moduledoc `:85` (*"No retention … emq3.4; the consumer does not trim"*) + `:28` (*"the at-rest group-removal surface stays UNFROZEN for the retention/archive family"*) — emq3.4 lands the deferred destructive surface as `trim/4`, NOT inside this loop.
11. **The conformance harness (SHIPPED — extend additively)** — `echo/apps/echo_mq/lib/echo_mq/conformance.ex`: `scenarios/0` (`:118`) — the registered keyword list, **76** live (re-probed: the moduledoc reads "seventy-six runnable scenarios" ×3; `:195` is the emq3.3 `:stream_group` tail); `apply_scenario/3` clauses (`:252+`, one `defp apply_scenario(:name, conn, q)` per scenario); `run/2` (`:228`) → `{:ok, n}`. The pins: `echo/apps/echo_mq/test/conformance_run_test.exs:69` `{:ok, 76}` → `{:ok, 77}`; `echo/apps/echo_mq/test/conformance_scenarios_test.exs:127` `@run_order` (`:66-123`, ends at `:stream_group`) gains `:stream_retention`; the moduledoc "seventy-six" → "seventy-seven". **Re-probe the live count at the reconcile** (76 as of emq3.3 — verify against `conformance.ex` before pinning).
12. **The keyspace (SHIPPED, NO edit)** — `echo/apps/echo_mq/lib/echo_mq/keyspace.ex`: `queue_key/2` (`:14`) — `emq:{q}:<type>`. **The trim addresses the existing stream key; NO new application subkey** (D-3 — the policy is BEAM-side, INV8). `git diff keyspace.ex` MUST be EMPTY.
13. **The engine (cited valkey.io, NEVER memory)** — `valkey.io/commands/xtrim` (the `MAXLEN [~|=] <count>` keep-newest-N form; the `MINID [~|=] <id>` drop-below-id form; the `~` approximate — trims in whole macro-nodes, keeps AT LEAST the window, cheaper, the recommended default — vs `=` exact; the integer removed-count reply); `valkey.io/topics/streams-intro` (capped streams, `MINID` mint-time trimming, the approximate-vs-exact safety semantics).
14. **The program law** — `.claude/skills/echo-mq-program.md` (the v2 laws, the gate ladder, the additive-minor conformance law, the determinism posture — for a no-mint/no-lease rung a MULTI-SEED sweep + an honest statement, NOT the ≥100 loop) + the as-built map `.claude/skills/echo-mq-surface.md`.

## Requirements (numbered; each traced back to a story and forward to an invariant)

1. **The `EchoMQ.Stream.trim/4` verb.** A NEW public function in `EchoMQ.Stream` (beside `append/4`, at the pre-declared slot `stream.ex:50`) over `XTRIM` issued DIRECT through `Connector.command/3`. The two window forms: `MAXLEN [~|=] <count>` (keep the newest N) and `MINID [~|=] <floor>` (drop below the mint-instant floor); `~` approximate the DEFAULT, `=` exact the OPT-IN. Returns `{:ok, removed_count}` (the integer `XTRIM` answers) or `{:error, term}` (any connector/server fault verbatim — a `WRONGTYPE` surfaced, not swallowed). Raises BEFORE any wire on a malformed queue/stream name (`FunctionClauseError` via the `is_binary` guard AS-BUILT, matching `append/4`'s precedent — the raise-before-wire holds). SETTLED (AS-BUILT): a single `trim/4` with two window-tuple heads `{:maxlen, count, approx?}` / `{:minid, %DateTime{}, approx?}` — ONE public entry the driver + a manual caller both use; the `MINID` floor via the public `minid_floor/1` helper. *(US1, US2, US3 → INV1, INV2, INV3, INV6.)*
2. **The `MINID` floor derived from `Snowflake.min_for/1` (INV6).** The mint-instant window's floor id is `"<ms>-0"` where `ms = EchoData.Snowflake.unix_ms(EchoData.Snowflake.min_for(dt))` == `DateTime.to_unix(dt, :millisecond)`. Derive it from `min_for/1` (its ms component), NEVER raw epoch arithmetic, NEVER `min_for/1`'s integer to the wire. A unit assertion: the floor ms == `unix_ms(min_for(dt))`. *(US3 → INV6.)*
3. **The declared per-stream policy, BEAM-side (D-3).** A declared retention window per stream — `{:maxlen, count, approx?}` / `{:minid, horizon, approx?}` — held BEAM-side (an ETS map keyed by `{queue, stream}`, OR the driver's own config keyword list), re-applied at start. NO `emq:{q}:stream:<name>:policy` keyspace subkey; NO at-rest cleanup obligation; NO reader-visible policy (a polyglot reader reads ENTRIES). A malformed declared policy RAISES (or returns a typed `:invalid`) at decision time — never a silent skip. *(US4 → INV8.)*
4. **The named/opt-in trim driver (D-2).** A NEW named, opt-in, owner-started `:transient` OTP child (the `EchoMQ.Pump` shape — settle the module name, e.g. `EchoMQ.StreamRetention`, in the body §3): a PURE tick/decision core (`<Module>.Core` — given the declared policy + the injected clock, answers WHICH `trim/4` call to make, or `:noop`; exhaustive + disjoint over the policy forms) + a thin process router that applies the core's decision via `trim/4`. DECOUPLED from consumer liveness (NOT the `StreamConsumer` beat — `git diff stream_consumer.ex` EMPTY). A manual `trim/4` call is the equally-supported cadence (the driver is sugar over the verb). *(US4 → INV5, D-2.)*
5. **The conformance scenario (`+1 stream_retention`, 76→77).** Register `stream_retention` in `scenarios/0` (the keyword pair) WITH its `apply_scenario(:stream_retention, conn, q)` clause in the same change; the prior 76 byte-unchanged; re-pin 76→77 in BOTH pinning tests (`conformance_run_test.exs` `{:ok, 76}` → `{:ok, 77}` + `conformance_scenarios_test.exs` `@run_order` gains `:stream_retention` after `:stream_group`) + the moduledoc "seventy-six" → "seventy-seven". The scenario is a POSITIVE proof: append entries INSIDE and BELOW a window via `EchoMQ.Stream.append/4`, call `trim/4`, assert the below-window entries are GONE (a `read/3..6` no longer returns them) AND the in-window entries SURVIVE (their branded receipts still read back) AND the removed-count is correct. A vacuous pass (trim a non-existent window, delete nothing) is a LOUD failure. *(US5 → INV9, INV4.)*
6. **The destructive blast-radius bounded — POSITIVELY (INV4, load-bearing).** Every retention proof asserts, in the SAME check, a real DELETION (below-window entries removed) AND a real SURVIVAL (in-window entries kept) — over BOTH window forms (`MAXLEN` and `MINID`). Over-deletion (an in-window entry removed) is a LOUD test failure. A no-op (delete nothing) does NOT satisfy INV4. *(US1, US5 → INV4.)*
7. **Byte-freeze + the label.** `echo_wire` UNTOUCHED (`git diff` empty); NO new/edited Lua (`grep -c redis.call` on the `lib/` diff = 0; `append/4` + every shipped `@`-script byte-identical — `XTRIM` is issued DIRECT); `keyspace.ex` UNTOUCHED (`git diff` empty; no new subkey); `stream_consumer.ex` UNTOUCHED (`git diff` empty — D-2); `@wire_version` frozen `echomq:2.4.2`; `mix.exs` `version: "2.6.2"` → `"2.6.3"` (a within-family patch). *(US5 → INV5, INV7, INV8, INV10.)*
8. **The determinism posture — a MULTI-SEED sweep + an honest statement (NOT the ≥100 loop).** The `:valkey` retention suite green under a handful of distinct seeds (`mix test --seed 0 --include valkey`, then 2–3 more). STATE that the rung mints NO ids and opens NO lease — so the same-ms branded-id mint hazard is ABSENT, and the ≥100 loop is NOT run (running it would forge a load the rung does not introduce). The driver's decision core is a PURE function of the injected clock + the policy, tested directly. *(US-GATE → all.)*

> **Dependency note (a reconcile finding carried from emq3.2/3.3 — do NOT add a dep silently).** `echo_mq`'s `mix.exs` `deps/0` declares only `echo_data` + `echo_wire` (re-probed). The retention proofs are **deterministic ExUnit integration tests** (append-inside-and-below + trim + assert-survive-and-gone) + the pure decision-core unit tests + the multi-seed sweep — needing NO new dep edge. No StreamData arm is needed; if ever wanted, it is the one-line `deps/0` add the Operator rules — surface it, do not add it silently.

## Execution topology (the runtime shape, the build-order DAG, the EXACT files touched)

**Runtime shape.** `EchoMQ.Stream.trim/4` is a STATELESS public function (a thin host-side wrapper over `XTRIM` on the shipped `Connector.command/3`) — no process, no lease, no `TIME`, no Lua. The named trim driver (`EchoMQ.StreamRetention` or the settled name) is an OPT-IN `:transient` OTP child (the `EchoMQ.Pump` shape) — a pure decision core + a thin process router that on each tick re-applies the declared policy via `trim/4`. It is owner-started (never a default-on sweep), decoupled from any `StreamConsumer`. The policy is BEAM-side state (an ETS map or the driver's config). The `MINID` floor is derived from the SHIPPED `Snowflake.min_for/1`; the trim mints NO id and opens NO lease.

**Build-order DAG (each step gated before the next):**

1. **The `trim/4` verb** — `lib/echo_mq/stream.ex` (at the pre-declared slot `:50`, beside `append/4`): the `XTRIM` wrapper over `Connector.command/3`, the two window forms (`MAXLEN`/`MINID`, `~`/`=`), the `MINID` floor derived from `Snowflake.min_for/1` (`"<ms>-0"`, INV6), `{:ok, removed_count} | {:error, term}`, the malformed-name raise. *(Gate: `mix compile --warnings-as-errors` clean; a unit test of the floor derivation.)*
2. **The declared policy carrier** — the BEAM-side policy shape (`{:maxlen, …}` / `{:minid, …}`) + its validation (a malformed policy raises / returns `:invalid` at decision time). *(Gate: the policy-form unit tests pass; no keyspace subkey created.)*
3. **The pure decision core** — `<Module>.Core` (the `Pump.Core` shape): given the declared policy + the injected clock, answer the `trim/4` call to make (or `:noop`), exhaustive + disjoint over the policy forms. *(Gate: the core's doctests/unit tests pass as a PURE function of the injected clock — no live process needed.)*
4. **The named/opt-in driver** — `<Module>` (the `EchoMQ.Pump` shape): the `:transient` child + the thin process router applying the core's decision via `trim/4`. Owner-started, no `mod:`. *(Gate: `mix compile --warnings-as-errors` clean; a trim applied on a tick with NO consumer present.)*
5. **The `:valkey` retention proof** — `test/stream_retention_test.exs` (NEW): the window honored + the blast radius bounded over BOTH forms (append-inside-and-below + trim + assert-survive-and-gone, `MAXLEN` AND `MINID`); the `MINID`-floor edge (`dt − 1ms` trimmed, `dt` survives); the truthful-read-after-trim (an emptied range reads `[]`); the driver's pure-core decisions + a trim with no consumer present. The `on_exit` per-queue purge (the `stream_verbs_test.exs` idiom). *(Gate: `mix test --include valkey` green.)*
6. **The conformance scenario + the re-pins** — `lib/echo_mq/conformance.ex` (the `stream_retention` keyword pair + the `apply_scenario(:stream_retention, …)` clause + the moduledoc count) + `test/conformance_run_test.exs` (`{:ok, 77}`) + `test/conformance_scenarios_test.exs` (`@run_order` gains `:stream_retention`). *(Gate: `Conformance.run/2` → `{:ok, 77}`; both pins updated; the prior 76 git-verified byte-unchanged.)*
7. **The label** — `mix.exs` `version: "2.6.3"`. *(Gate: the byte-freeze greps — `echo_wire`/`keyspace.ex`/`stream_consumer.ex` diffs EMPTY, `grep -c redis.call` on `lib/` diff = 0, `@wire_version` unchanged.)*

**The EXACT files touched (the whole diff):**
- `echo/apps/echo_mq/lib/echo_mq/stream.ex` — `trim/4` ADDED (the existing `append/4`/`append_id/5`/`append_batch/4`/`read/3..6`/`stream_key/2` BYTE-FROZEN).
- `echo/apps/echo_mq/lib/echo_mq/stream_retention.ex` (or the settled name) — NEW: the named/opt-in driver (a `:transient` child + a thin router).
- `echo/apps/echo_mq/lib/echo_mq/stream_retention/core.ex` (or the settled name) — NEW: the pure tick/decision core.
- `echo/apps/echo_mq/lib/echo_mq/conformance.ex` — the `stream_retention` scenario + the moduledoc count (76→77).
- `echo/apps/echo_mq/test/stream_retention_test.exs` — NEW: the `:valkey` retention proof + the pure-core unit tests.
- `echo/apps/echo_mq/test/conformance_run_test.exs` — `{:ok, 76}` → `{:ok, 77}`.
- `echo/apps/echo_mq/test/conformance_scenarios_test.exs` — `@run_order` gains `:stream_retention`.
- `echo/apps/echo_mq/mix.exs` — `version: "2.6.2"` → `"2.6.3"`.
- **UNTOUCHED (the byte-freeze):** `echo/apps/echo_wire/**` (the connector), `echo/apps/echo_mq/lib/echo_mq/keyspace.ex` (no grammar edit, no subkey), `echo/apps/echo_mq/lib/echo_mq/stream_consumer.ex` (D-2 — the frozen emq3.3 loop), `echo/apps/echo_data/**` (`min_for/1` reused, not edited), `apps/echomq` (the frozen v1 reference), the `EchoStore.Graft` engine (the COEXIST law).

## Agent stories (each a Directive + an Acceptance gate — the contract IS the acceptance)

- **AS1 — the `trim/4` verb (the converged mechanism).** *Directive:* add `trim/4` to `EchoMQ.Stream` over `XTRIM` on `Connector.command/3` — the two window forms (`MAXLEN`/`MINID`, `~`/`=`), the `MINID` floor from `Snowflake.min_for/1` (`"<ms>-0"`), `{:ok, removed_count} | {:error, term}`, the malformed-name raise. *Acceptance:* a `:valkey` test trims a flooded stream by `MAXLEN N` AND by `MINID floor`, asserting the below-window entries gone + the in-window entries surviving + the removed-count correct; `append/4` byte-frozen; `grep -c redis.call` on the `lib/` diff = 0. *(INV1, INV2, INV3, INV6.)*
- **AS2 — the `MINID` floor exactness.** *Directive:* derive the floor `"<ms>-0"` from `unix_ms(min_for(dt))` == `DateTime.to_unix(dt, :ms)`; never raw epoch arithmetic, never the snowflake integer to the wire. *Acceptance:* a unit test asserts the floor ms == `unix_ms(min_for(dt))`; an integration test asserts a `dt − 1ms` entry is trimmed by `MINID(dt)` and a `dt` entry survives (the exact half-open `[dt, ∞)` edge). *(INV6.)*
- **AS3 — the declared policy + the named/opt-in driver.** *Directive:* hold the per-stream policy BEAM-side (an ETS map or the driver's config — NO keyspace subkey); build the named, opt-in, owner-started `:transient` driver (the `EchoMQ.Pump` shape) with a PURE decision core re-applying the policy via `trim/4`; decouple it from the `StreamConsumer` (frozen). *Acceptance:* the pure core's decisions are tested as a function of the injected clock + the policy (exhaustive + disjoint, `:noop` when nothing is due); a trim applies on a tick with NO consumer present; `git diff stream_consumer.ex` EMPTY; no keyspace subkey. *(D-2, D-3, INV5, INV8.)*
- **AS4 — the destructive blast-radius bounded (POSITIVELY).** *Directive:* every retention proof asserts a real deletion AND a real survival in the same check, over BOTH window forms; over-deletion is a LOUD failure; a no-op does not satisfy INV4. *Acceptance:* the `stream_retention` scenario + the deep integration proofs append-inside-and-below + trim + assert-survive-and-gone; the Director's blast-radius mutation battery (mutate bound / approx-exact / floor) is caught. *(INV4.)*
- **AS5 — the conformance scenario + the re-pins (76→77).** *Directive:* register `stream_retention` with its `apply_scenario` probe in the same change; re-pin both pinning tests + the moduledoc; keep the prior 76 byte-unchanged. *Acceptance:* `Conformance.run/2` → `{:ok, 77}` on the truth row; the git-diff of `scenarios/0` shows ONLY the addition; both pins updated; a vacuous pass is a LOUD failure. *(INV9, INV4.)*
- **AS6 — the gate (the determinism posture + the byte-freeze).** *Directive:* run the `:valkey` retention suite + the multi-seed sweep (NOT the ≥100 loop — the rung mints no ids, opens no lease); prove the byte-freeze (the `echo_wire`/`keyspace.ex`/`stream_consumer.ex` diffs EMPTY, `grep -c redis.call` on `lib/` diff = 0, `@wire_version` unchanged); state the determinism posture honestly. *Acceptance:* EMQ3.4-US-GATE holds — compile clean, the suite green, conformance `{:ok, 77}`, the byte-freeze greps 0/EMPTY, the multi-seed sweep green + the honest statement, the label `2.6.3`, honest-row (Valkey on 6390). *(all.)*

A short, comprehensive prompt that leaves no decision the spec has not already fixed — the cadence (D-2 named/opt-in), the storage (D-3 BEAM-side), the determinism posture (multi-seed, NOT the ≥100 loop), and the byte-freeze are all RULED. The one shape Mars settles is the `trim/4` arity and the driver module name — pin both in the body §1/§3 at build time. **Risk HIGH (a DESTRUCTIVE at-rest `XTRIM`); Apollo is MANDATORY** (post-build — the destructive op).
