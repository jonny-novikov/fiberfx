# EMQ3.3 — the Mars brief (S2 the readers, part 1 — THE READER LAW: `EchoMQ.StreamConsumer`, the BEAM consumer group + the polyglot seam)

> The compact build brief. The body [`emq3.3.md`](emq3.3.md) is authoritative; the acceptance is [`emq3.3.stories.md`](emq3.3.stories.md); the run scope is [`emq3.3.prompt.md`](emq3.3.prompt.md). Build ONLY inside `echo/apps/echo_mq` (the consumer rides the shipped `echo_wire` connector `command/3` on its OWN private lane — NO `echo_wire` edit). Cite the spec line for every public call; the group verbs (`XGROUP`/`XREADGROUP`/`XACK`/`XAUTOCLAIM`) are issued DIRECT as `parts` lists (NO new `Script.new/2` — emq3.3 is a no-new-Lua rung); the conformance additive-minor mechanics (75→76). **The forks are RULED (the design-phase convergence — D-2..D-7); build to the convergence.**
>
> **Framing law (propagated).** Third person for any agent; no gendered pronouns for agents; no perceptual or interior-state verbs for agents or software (components read, compute, refuse, return); no first-person narration. Bind this same clause in any sub-brief.

## References (read first — the exact upstream, links/paths first)

1. **The body** — [`emq3.3.md`](emq3.3.md): Goal · §1 the order theorem under a group (the PEL exception, D-6) · §2 the recovery design (two complementary mechanisms, D-4) · Scope · INV1–10 · the closed error set · the forks RULED · DoD. The forks are the design-phase convergence: D-2 the SETTLED sibling shape · D-3 the lazy-ensure group door (`:group_start` declared, no destructive verb) · D-4 drain-PEL-first + the exact-mirror handler + the NAMED `attempts`↔delivery-count invariant · D-5 `+1 stream_group` (75→76) · D-6 the order-theorem PEL exception NAMED · D-7 `2.6.2` label, HIGH risk, Apollo mandatory.
2. **The tier contract** — [`../../emq.streams.md`](../../emq.streams.md): the Stream Tier ladder (emq3.1–3.6), the three milestones (S1 writer · S2 readers · S3 memory), the emq3.3 row (`:70` — *"a BEAM consumer and one non-BEAM reader on the same group … at-least-once with idempotent handlers; crash → `XAUTOCLAIM` re-delivery"*), the seams (`:82-91` — exactly-once NOT claimed, `:89`). emq3.3 is the FIRST rung of S2; retention is emq3.4.
3. **The design KB (the RULED forks — read for the argument, NOT to re-open)** — [`../../kb/streams-tier/streams.synthesis.md`](../../kb/streams-tier/streams.synthesis.md) §2 (the build-ready decision set) + §4 (the PEL re-claim exception) · [`../../kb/streams-tier/streams.design.A-consumer-lens.md`](../../kb/streams-tier/streams.design.A-consumer-lens.md) §emq3.3 · [`../../kb/streams-tier/streams.design.B-steward-lens.md`](../../kb/streams-tier/streams.design.B-steward-lens.md) §1. Every emq3.3 fork CONVERGED — there is NO open decision for Mars.
4. **The supervised-loop precedent (SHIPPED — the SHAPE to follow, NO edit to it)** — `echo/apps/echo_mq/lib/echo_mq/consumer.ex`:
   - `child_spec/1` (`consumer.ex:28`) — a `:permanent` child (the loop restarts whole, its self-started lane dies and returns with it). The `StreamConsumer` mirrors this.
   - `start_link/1` (`consumer.ex:52`) — a `spawn_link` loop (NOT a GenServer), `Process.flag(:trap_exit, true)` (`:58`), the `:conn`/`:connector` lane option (`:60-68` — `:conn` an exclusive lane, or `:connector` opts to `Connector.start_link/1` linked to the loop). The `StreamConsumer` takes the SAME lane option.
   - `stop/2` (`consumer.ex:101`) — drain-and-stop, synchronous (`Process.monitor` + `{:emq_stop, …}` + the `{:DOWN, …}` receive). The `StreamConsumer` mirrors this exactly.
   - the `loop/1` beat (`consumer.ex:114-121`): `check_control` → `reap` → `promote` → `drain` → `park` → recurse. The `StreamConsumer`'s loop is `check_control` → (PEL-drain on first pass) → `XAUTOCLAIM` reclaim → `XREADGROUP … >` BLOCK → settle each → recurse.
   - the handler (`consumer.ex:147`): `s.handler.(%{id: id, payload: payload, attempts: att, group: group})`. **The `StreamConsumer` handler is BYTE-IDENTICAL in SHAPE** (INV3).
   - the rescue/catch raise→retry that SURVIVES the loop (`consumer.ex:148-153`): a raising handler converts to `{:error, ...}`, the loop continues. The `StreamConsumer` reuses this discipline (on `{:error, _}` the entry is LEFT un-acked instead of `Jobs.retry`).
   - `check_control/0` (`consumer.ex:127-135`) — honors `{:emq_stop, …}` → `exit(:normal)`, `{:EXIT, _, :shutdown}` → `exit(:shutdown)` at the settle points (between entries, never inside one).
   - the private lane for the blocking `BLPOP` park (`consumer.ex:167-172`, the `park/1` runs `["BLPOP", wake, secs]` on the consumer's OWN lane) + the moduledoc "blocking verbs get their own lane (Appendix B)" (`consumer.ex:1-12`). **The `StreamConsumer`'s blocking `XREADGROUP … BLOCK ms` rides `command/3` on the consumer's OWN lane THE SAME WAY** — this is why the single-owner socket does not stall (the design tension retired one tier down).
5. **The SIBLING precedent (SHIPPED — why a sibling, NOT a mode; NO edit)** — `echo/apps/echo_mq/lib/echo_mq/batch_consumer.ex`: `:10-16` (emq.5.2-D1) — a DIFFERENT claim path + a DIFFERENT handler contract earns a SIBLING, not a mode on `EchoMQ.Consumer`. The `StreamConsumer` is the same call: a different claim path (`XREADGROUP` group read, not `Lanes.claim`) + a different settle (`XACK`/leave-un-acked, not `Jobs.complete`/`Jobs.retry`). The same `child_spec`/`start_link`/`stop` + `:conn`/`:connector` lane shape (`:57-104`), the same trap-exits + settle-point control (`:48-51`).
6. **The writer it reads (SHIPPED — the branded `id` field; NO edit)** — `echo/apps/echo_mq/lib/echo_mq/stream.ex`:
   - `append/4` (`stream.ex:77`) — mints an `EVT`-branded id, stores the 14-byte branded string as the stream `id` FIELD (`stream.ex:100`, `["XADD", key, xadd_id, "id", branded | …]`). **The consumer reads THIS `id` field back as the branded receipt** (the polyglot seam, INV5).
   - `read/3..6` (`stream.ex:156`) — the un-grouped `XRANGE` read-back parsing `[xadd_id, [field, value, …]]` into `{branded, fields_map}` via `parse_entry/1` (`:180-183`: the stored `"id"` field IS the branded id, the rest the payload map). **The consumer's group-read parser reuses THIS shape** (the same nested-array reply parse, applied to the `XREADGROUP` reply).
   - `stream_key/2` (`stream.ex:173`) — `emq:{q}:stream:<name>` via `Keyspace.queue_key(queue, "stream:" <> name)`. **The consumer addresses the group via THIS key** (no grammar edit, INV8).
   - the pure id core `echo/apps/echo_mq/lib/echo_mq/stream/id.ex`: `kind/0` (`:75`) → `"EVT"`; `evt?/1` (`:101`); `xadd_id/1` (`:87`) over `BrandedId.parse/1` → `{:ok, ns, snow}`. The branded id is the canonical receipt; the consumer mints NO id (the writer did).
7. **The generic command path to RIDE on a PRIVATE lane (SHIPPED — NO edit)** — `echo/apps/echo_wire/lib/echo_mq/connector.ex`: `command/3` — `command(conn, parts, timeout \\ 5_000)`, `parts` a `[binary() | integer() | atom()]` list. **The group verbs ride THIS on the consumer's OWN lane** — `command(conn, ["XREADGROUP", "GROUP", group, consumer, "BLOCK", ms, "COUNT", n, "STREAMS", key, ">"], block_ms + buffer)`. `pipeline/3` for any batched settle. `Connector.start_link/1` for the consumer's own lane (the `:connector` opt, the `consumer.ex:66` idiom). `@wire_version "echomq:2.4.2"` — byte-unchanged (INV7/INV10). **The `echo_wire` diff MUST be EMPTY.** (Line numbers in the lens docs drift — re-probe `command/3`/`pipeline/3` at the rung's reconcile; cite by surface name.)
8. **The emq3.1 verb floor (SHIPPED — the group verbs round-trip; the deferred BLOCK)** — `echo/apps/echo_mq/test/stream_verbs_test.exs`:
   - `XGROUP CREATE key grp <start>` → `{:ok, "OK"}` (`:93`); the group reads via `XREADGROUP GROUP grp c1 COUNT 10 STREAMS key >` (`:97-108`); `XACK key grp id` → `{:ok, 1}` (`:115`); `XAUTOCLAIM key grp c2 "0" "0"` → `{:ok, [cursor, claimed, deleted]}` (`:135-139`, the claimed entry positively re-assigned to c2). **The group verbs already round-trip — emq3.3 wires them into the consumer loop.**
   - the DEFERRED `XREADGROUP BLOCK` (`stream_verbs_test.exs:21-23`): *"NO XREADGROUP BLOCK -- the blocking consumer-group read holds the single-owner socket and lands at emq3.3 with the readers' blocking-read design."* **emq3.3 is where the BLOCK lands — on the consumer's PRIVATE lane** (the `BLPOP`-on-its-own-lane precedent, `consumer.ex:170`).
   - the per-queue purge idiom (`stream_verbs_test.exs:39-65`): a DISPOSABLE connection sweeps `emq:{q}:*` on `on_exit` (the stream key shares the `{q}` hashtag). **The consumer suite reuses THIS purge.**
9. **The conformance harness (SHIPPED — extend additively)** — `echo/apps/echo_mq/lib/echo_mq/conformance.ex`: `scenarios/0` (`:110`) — the registered list, **75** live (the moduledoc reads "seventy-five runnable scenarios"; `:84` names the emq3.2 `stream_append`); `run/2` → `{:ok, n}`. The pins: `echo/apps/echo_mq/test/conformance_run_test.exs:65` `{:ok, 75}` → `{:ok, 76}`; `echo/apps/echo_mq/test/conformance_scenarios_test.exs:43` `@run_order` (ends at `:stream_append`, `:118`) gains `stream_group`; the assertion at `:122`. **Re-probe the live count at the reconcile** (it is 75 as of emq3.2 — verify against `conformance.ex` before pinning).
10. **The keyspace (SHIPPED, NO edit)** — `echo/apps/echo_mq/lib/echo_mq/keyspace.ex`: `queue_key/2` (`:14`) — `emq:{q}:<type>`; `slot/1` (`:44`). The group state is server-side stream state on the stream key's `{q}` slot — NO new application subkey (INV8).
11. **The engine (cited valkey.io, NEVER memory)** — `valkey.io/commands/xreadgroup` (the `>` new-vs-`0` PEL read; the `BLOCK ms` form; the consumer name); `valkey.io/commands/xgroup` (`CREATE … MKSTREAM`; the `BUSYGROUP` reply on an existing group; `$` new-only vs `0` from-head start); `valkey.io/commands/xautoclaim` (min-idle reclaim; the `[cursor, claimed, deleted]` triple); `valkey.io/commands/xack`; `valkey.io/commands/xpending` (the per-entry DELIVERY-COUNT — the `attempts` mapping, INV3); `valkey.io/topics/streams-intro` (consumer groups, the PEL, at-least-once delivery).
12. **The program law** — `.claude/skills/echo-mq-program.md` (the v2 laws, the gate ladder, the additive-minor conformance law, the ≥100 determinism loop) + the as-built map `.claude/skills/echo-mq-surface.md`.

## Requirements (numbered; each traced back to a story and forward to an invariant)

1. **The `EchoMQ.StreamConsumer` supervised sibling.** A NEW module `lib/echo_mq/stream_consumer.ex` — a `spawn_link` loop (NOT a GenServer), the `EchoMQ.Consumer`/`BatchConsumer` shape: `child_spec/1` (a `:permanent` child), `start_link/1` (`Process.flag(:trap_exit, true)`, the `:conn`/`:connector` lane option), `stop/2` (drain-and-stop, synchronous, the `Process.monitor` + `{:emq_stop, …}` + `{:DOWN, …}` idiom). Reads `XREADGROUP GROUP <group> <consumer> BLOCK <ms> COUNT <n> STREAMS <key> >` via `Connector.command/3` on its OWN lane. *(US1, US2 → INV1, INV2.)*
2. **The lazy-ensure group door.** On `start_link`, issue `XGROUP CREATE <key> <group> <start> MKSTREAM` via `Connector.command/3`; swallow ONLY the `BUSYGROUP` reply (a `WRONGTYPE` or any other error is LOUD — the consumer fails to start). The start position is the DECLARED `:group_start` option (`:new` → `$` / `:head` → `0`), no default — a missing/malformed `:group_start` RAISES at start. NO `group_destroy`/`XGROUP DESTROY` verb (the destructive surface stays UNFROZEN — the emq.4.1 `drain/3` precedent). *(US5 → INV4.)*
3. **The loop — drain-PEL-first → `>` → `XAUTOCLAIM` reclaim on the beat.** On the FIRST pass, drain the consumer's OWN PEL via `XREADGROUP GROUP g <self> COUNT n STREAMS key 0` to exhaustion, settling each; THEN switch to `>` (a cold start's `0` returns empty → straight to `>`). On each beat, reclaim entries idle past `:min_idle_ms` via `XAUTOCLAIM <key> <group> <self> <min_idle_ms> <cursor>`, settling each (the dead PEERS). The blocking `XREADGROUP … BLOCK <beat_ms>` parks on the consumer's OWN lane (the `BLPOP` precedent). `check_control` honors stop/shutdown at the settle points; a raising handler converts to `{:error, reason}` and the loop survives; `stop/2` drains the entry in hand. *(US1, US2 → INV1, INV2.)*
4. **The exact-mirror handler + the NAMED `attempts`↔delivery-count invariant.** The handler is `fun(%{id, payload, attempts, group}) :: :ok | {:error, reason}` — BYTE-IDENTICAL in SHAPE to `consumer.ex:147`. On the stream side `attempts` carries the `XPENDING` per-entry DELIVERY-COUNT (read from the `XREADGROUP`/`XPENDING` reply — the number of times THIS entry has been delivered), NOT a handler-failure count (INV3 — specced, not assumed). `group` is the consumer-group name; `id` the stored branded record id; `payload` the entry's fields map. On `:ok` → `XACK <key> <group> <xadd_id>`; on `{:error, reason}` (or a raise) → LEAVE un-acked (it survives in the PEL → re-deliverable). *(US3 → INV3.)*
5. **The polyglot parity test.** An in-suite `:valkey` test: `append/4` an entry (capturing the branded receipt R); read the group with RAW `XREADGROUP GROUP grp c STREAMS key >` through the BARE `Connector` (no `EchoMQ.Stream`/`StreamConsumer` helpers); assert the entry's stored `id` field == R; a raw `XACK` through the bare `Connector` settles it (the BEAM consumer does not re-deliver it) — the BEAM and non-BEAM sides share ONE group state. *(US4 → INV5.)*
6. **The order-theorem PEL exception, NAMED + EXERCISED.** The body §1 NAMES the two regimes (the stream stays id-ordered; a re-claimed entry returns out of delivery order — the honest at-least-once cost). A `:valkey` integration assertion EXERCISES it: a re-claimed entry's branded id is LOWER than entries already delivered to the same consumer (delivery out of mint order). The handler's required idempotence is stated (the branded id is the dedup key). *(US6 → INV6.)*
7. **The conformance scenario (`+1 stream_group`, 75→76).** Register `stream_group` in `scenarios/0` with its probe in the same change; the prior 75 byte-unchanged; re-pin 75→76 in BOTH pinning tests (`conformance_run_test.exs` `{:ok, 76}` + `conformance_scenarios_test.exs` `@run_order` gains `stream_group` after `:stream_append`). The scenario is a POSITIVE proof of RE-DELIVERY: append → group read → `XACK` one, LEAVE one un-acked → an idle-window/forced `XAUTOCLAIM` → assert the SAME entry returns. A vacuous ack-everything pass is a LOUD failure. *(US7 → INV9, INV1.)*
8. **Byte-freeze + the label.** `echo_wire` UNTOUCHED (`git diff` empty); NO new/edited Lua (`grep -c redis.call` on the `lib/` diff = 0; every shipped `Script.new/2` byte-identical — the group verbs are issued DIRECT); `keyspace.ex` UNTOUCHED (`git diff` empty; the group state is server-side stream state on the `{q}` slot, no new application subkey); `@wire_version` frozen `echomq:2.4.2`; `mix.exs` `version: "2.6.1"` → `"2.6.2"` (a within-family patch). *(US8 → INV7, INV8, INV10.)*
9. **The ≥100 determinism loop (MANDATORY — a NEW supervised process).** The full `:valkey` suite green over `for i in $(seq 1 100); do TMPDIR=/tmp mix test --include valkey || break; done` — emq3.3 builds a supervised process (a `spawn_link` loop + a private blocking lane + a lease-like PEL recovery), so both a same-ms branded-id mint hazard AND a process-timing race are present. The loop OWNS the machine. *(US1, EMQ3.3-US-GATE → INV1.)*

> **Dependency note (a reconcile finding carried from emq3.2 — do NOT add a dep silently).** `echo_mq`'s `mix.exs` `deps/0` declares only `echo_data` + `echo_wire` (re-probed); `stream_data` is locked transitively but NOT a declared edge. The deep proofs (every-entry-≥1, recovery-completeness) are **deterministic ExUnit integration tests** + the ≥100 loop — needing NO new dep edge. IF the Operator ever wants a StreamData property arm, it needs the one-line `deps/0` add `{:stream_data, "~> 1.3", only: :test}` — surface it, do not add it silently.

## Execution topology (the runtime shape, the build-order DAG, the EXACT files touched)

**Runtime shape.** `EchoMQ.StreamConsumer` is a SUPERVISED PROCESS (a `spawn_link` loop, the `EchoMQ.Consumer` shape — NOT a GenServer), holding a PRIVATE connector lane (its own `Connector` started via the `:connector` opt, or an exclusive `:conn`). The blocking `XREADGROUP … BLOCK` parks on that PRIVATE lane (so the single-owner socket of the rest of the system is never stalled — the `consumer.ex:170` `BLPOP`-on-its-own-lane precedent). The consumer reads the stored branded `id` field as the receipt, mints NO id (the writer did). No lease key, no `TIME` script (the PEL idle-time is the server's clock, evaluated by `XAUTOCLAIM` server-side), no new Lua.

**Build-order DAG (each step gated before the next):**

1. **The consumer skeleton** — `lib/echo_mq/stream_consumer.ex`: `child_spec/1` + `start_link/1` (the `:conn`/`:connector` lane, `:queue`/`:stream`/`:group`/`:consumer`/`:group_start`/`:handler`/`:min_idle_ms`/`:beat_ms`/`:count` opts) + `stop/2`, the trap-exits + `check_control` settle-point discipline (the `consumer.ex` shape). *(Gate: `mix compile --warnings-as-errors` clean.)*
2. **The lazy-ensure group door** — `XGROUP CREATE … MKSTREAM` on start, `BUSYGROUP`-only swallow, the declared `:group_start` raise. *(Gate: a fresh group creates; a re-start is a no-op; a `WRONGTYPE` is LOUD; a missing `:group_start` raises.)*
3. **The loop** — drain-PEL-first (`… 0`) → `>` BLOCK on the private lane → `XAUTOCLAIM` reclaim on the beat → settle each (the handler verdict → `XACK` or leave-un-acked, the raise→survive discipline). *(Gate: `mix compile --warnings-as-errors` clean.)*
4. **The `:valkey` consumer proof** — `test/stream_consumer_test.exs`: the group drain (every entry delivered ≥1), the crash/PEL recovery (kill holding un-acked → restart → recover SELF first), the `XAUTOCLAIM` reclaim (a dead PEER's backlog), the handler exact-mirror + the `attempts`↔delivery-count assertion, the polyglot parity (raw-connector read recovers the receipt), the order-theorem PEL exception (a re-claimed id lower than already-delivered). The `on_exit` per-queue purge (the `stream_verbs_test.exs:39-65` idiom). *(Gate: `mix test --include valkey` green.)*
5. **The conformance scenario** — add `stream_group` to `conformance.ex` `scenarios/0` with its probe (a POSITIVE re-delivery proof); re-pin `conformance_run_test.exs` `{:ok, 75}` → `{:ok, 76}` + `conformance_scenarios_test.exs` `@run_order` gains `stream_group`. *(Gate: `Conformance.run/2` → `{:ok, 76}`; both pins green; the prior 75 byte-unchanged, git-verified.)*
6. **The label** — `mix.exs` `version: "2.6.1"` → `"2.6.2"`. *(Gate: the label reads `2.6.2`; `@wire_version` frozen `echomq:2.4.2`.)*
7. **The full gate ladder + the ≥100 loop** — `mix compile --warnings-as-errors` + `mix test --include valkey` + `Conformance.run/2 → {:ok, 76}` + the **≥100 determinism loop** (MANDATORY) + the byte-freeze checks (`echo_wire` diff empty, `grep -c redis.call` on the `lib/` diff = 0, `keyspace.ex` diff empty).

**Files touched (the complete set):**

| File | Change |
|---|---|
| `echo/apps/echo_mq/lib/echo_mq/stream_consumer.ex` | **NEW** — the `EchoMQ.StreamConsumer` supervised sibling (`child_spec`/`start_link`/`stop`, the lazy-ensure door, the drain-PEL-first → `>` → `XAUTOCLAIM` loop, the exact-mirror handler) |
| `echo/apps/echo_mq/test/stream_consumer_test.exs` | **NEW** — the `:valkey` consumer proof (group drain · crash/PEL recovery · `XAUTOCLAIM` reclaim · the `attempts`↔delivery-count · the polyglot parity · the order-theorem PEL exception) |
| `echo/apps/echo_mq/lib/echo_mq/conformance.ex` | the `stream_group` scenario + its probe (additive; the prior 75 byte-unchanged) |
| `echo/apps/echo_mq/test/conformance_run_test.exs` | re-pin `{:ok, 75}` → `{:ok, 76}` (`:65`) |
| `echo/apps/echo_mq/test/conformance_scenarios_test.exs` | `@run_order` gains `stream_group` (`:43`/`:118`); the assertion (`:122`) |
| `echo/apps/echo_mq/mix.exs` | the label `version: "2.6.1"` → `"2.6.2"` |

**UNTOUCHED (load-bearing):** `echo/apps/echo_wire/**` (the connector — the consumer rides the shipped `command/3` on a private lane); `echo/apps/echo_mq/lib/echo_mq/keyspace.ex` (the stream key + its server-side group state ride the total `queue_key/2`); `echo/apps/echo_mq/lib/echo_mq/consumer.ex` + `batch_consumer.ex` + `stream.ex` + `stream/id.ex` (precedents read, not edited); every shipped `Script.new/2` (no new/edited Lua); `apps/echomq` (the frozen v1 reference); `echo/apps/echo_store/**` (the `EchoStore.Graft` engine — the COEXIST law, the archive fold is emq3.5, untouched here); `mix.lock` (no dep moved).

## Agent stories (each a Directive + an Acceptance gate — the contract IS the acceptance criterion)

- **AS-1 · the consumer skeleton + the lazy-ensure door.** *Directive:* build `EchoMQ.StreamConsumer` — the `spawn_link` loop (the `consumer.ex` shape: `child_spec/1`, `start_link/1` with the `:conn`/`:connector` lane + `:group`/`:consumer`/`:group_start`/`:handler`/`:min_idle_ms`/`:beat_ms`/`:count` opts, `stop/2`, trap-exits + `check_control`); on start issue `XGROUP CREATE <key> <group> <start> MKSTREAM`, swallow ONLY `BUSYGROUP`, raise on a missing/malformed `:group_start` (`:new`→`$`/`:head`→`0`). NO destructive verb. *Acceptance gate:* `mix compile --warnings-as-errors` clean; a fresh group creates (`MKSTREAM`); a re-start is a no-op (`BUSYGROUP` swallowed); a `WRONGTYPE` start fails LOUD; a missing `:group_start` raises; `grep "XGROUP.*DESTROY\|group_destroy" lib/` == 0 (INV4).
- **AS-2 · the loop — drain-PEL-first → `>` → `XAUTOCLAIM`.** *Directive:* build the loop — on the first pass drain own PEL (`XREADGROUP … 0`) to exhaustion, then `>` BLOCK on the private lane; on each beat reclaim idle entries via `XAUTOCLAIM <key> <group> <self> <min_idle_ms> <cursor>`; settle each via the handler verdict; `check_control` at the settle points; a raise converts to `{:error, _}` and the loop survives. *Acceptance gate:* a killed consumer holding un-acked entries restarts and handles its OWN backlog FIRST (the PEL-drain, asserted by the recovered branded ids); the `XAUTOCLAIM` beat recovers a dead PEER's backlog; a cold start passes straight to `>`; both mechanisms present (INV1, INV2).
- **AS-3 · the exact-mirror handler + the NAMED invariant.** *Directive:* the handler takes `%{id, payload, attempts, group}` (byte-identical SHAPE to `consumer.ex:147`); `attempts` is the `XPENDING` delivery-count; `:ok` → `XACK`, `{:error, reason}`/raise → leave un-acked. *Acceptance gate:* the handler map has exactly `{id, payload, attempts, group}`; a re-delivered entry's `attempts` == the `XPENDING` delivery count (asserted); a `:ok` `XACK`s, an `{:error, _}` leaves un-acked (re-deliverable); a raising handler does NOT crash the loop (INV3).
- **AS-4 · the polyglot parity + the order-theorem exception.** *Directive:* build the `:valkey` proofs — the raw-connector parity (read the same group through the bare `Connector`, assert the stored `id` field == the `append/4` receipt; a raw `XACK` settles it); the order-theorem PEL exception (a re-claimed entry's branded id is LOWER than already-delivered ids). *Acceptance gate:* `mix test --include valkey` green; the raw read recovers the EXACT receipt; the re-claim is delivered out of mint order (the exception exercised, not asserted in prose) (INV5, INV6).
- **AS-5 · the conformance scenario + the count.** *Directive:* add `stream_group` to `conformance.ex` `scenarios/0` with its probe (a POSITIVE re-delivery proof — append, group-read, ack one, leave one un-acked, force `XAUTOCLAIM`, assert the SAME entry returns); re-pin 75→76 in both pinning tests. *Acceptance gate:* `Conformance.run/2` → `{:ok, 76}` on Valkey 6390; the git-diff of `scenarios/0` shows only the addition; both pins green; the scenario asserts re-delivery (a positive proof, not ack-everything) (INV9).
- **AS-6 · byte-freeze + the label + the ≥100 loop.** *Directive:* step `mix.exs` to `"2.6.2"`; run the full gate ladder + the ≥100 determinism loop. *Acceptance gate:* `git diff echo/apps/echo_wire/` EMPTY; `grep -c redis.call` on the `lib/` diff = 0; every shipped `Script.new/2` byte-identical; `git diff keyspace.ex` EMPTY (no new application subkey); `@wire_version` `echomq:2.4.2`; `mix.exs` `2.6.2`; the ≥100 loop green owning the machine (INV7, INV8, INV10, INV1).

## The short comprehensive prompt (no decision the spec has not fixed)

Build the EchoMQ 3.0 Stream Tier **reader law** inside `echo/apps/echo_mq` to the RULED convergence (D-2..D-7). Add a NEW supervised sibling `EchoMQ.StreamConsumer` (a `spawn_link` loop, the `EchoMQ.Consumer`/`BatchConsumer` shape — NOT a mode on the job `Consumer`; `child_spec`/`start_link`/`stop/2`, trap-exits, `check_control` settle-points) holding a PRIVATE connector lane for a BLOCKING `XREADGROUP GROUP <group> <consumer> BLOCK <ms> COUNT <n> STREAMS <key> >` (the `consumer.ex:170` `BLPOP`-on-its-own-lane precedent — this is why the single-owner socket does not stall, the design tension retired one tier down). On `start_link` ensure the group lazily: `XGROUP CREATE <key> <group> <start> MKSTREAM`, swallow ONLY `BUSYGROUP` (a `WRONGTYPE` is LOUD), the start position a DECLARED `:group_start` option (`:new`→`$`/`:head`→`0`, no default — raise if missing); NO destructive `group_destroy`/`XGROUP DESTROY` verb. The loop drains the consumer's OWN PEL first (`XREADGROUP … 0` to exhaustion — recover SELF), THEN reads `>` (new), and on each beat reclaims dead PEERS' idle entries via `XAUTOCLAIM <key> <group> <self> <min_idle_ms> <cursor>` (both mechanisms NAMED — PEL-drain SELF, beat PEERS). The handler is BYTE-IDENTICAL in SHAPE to `consumer.ex:147` — `%{id, payload, attempts, group}` → `:ok | {:error, reason}` — with `attempts` carrying the `XPENDING` per-entry DELIVERY-COUNT (specced, NOT assumed); `:ok` → `XACK`, `{:error, reason}`/raise → leave un-acked (re-deliverable; the raise→survive discipline `consumer.ex:148-153`). Prove the polyglot seam with a raw-connector parity test (read the same group through the bare `Connector`, assert the stored `id` field == the `append/4` branded receipt; a raw `XACK` settles it). NAME the order-theorem PEL exception in the body (the stream stays id-ordered; a re-claimed entry returns out of real-time delivery order — the honest at-least-once cost) and EXERCISE it (a re-claimed id lower than already-delivered). Add `+1 stream_group` to conformance (75→76, the prior 75 byte-unchanged, both pins re-pinned) — a POSITIVE proof of re-delivery (an un-acked entry actually re-handed), never ack-everything. NO new Lua (the group verbs are issued DIRECT — `grep -c redis.call` on the `lib/` diff = 0; `echo_wire` UNTOUCHED; every shipped `Script.new/2` byte-identical; `keyspace.ex` UNTOUCHED — the group state is server-side stream state on the `{q}` slot, no new application subkey; `@wire_version` frozen `echomq:2.4.2`). Step `mix.exs` to `2.6.2`. Gate: per-app `compile --warnings-as-errors` + `test --include valkey` + `Conformance.run/2 → {:ok, 76}` + the **≥100 determinism loop** (MANDATORY — a NEW supervised process + a same-ms mint hazard) + the byte-freeze checks. Cite the spec line for every public call; invent nothing (the precedent is `EchoMQ.Consumer`/`BatchConsumer`; the writer is `EchoMQ.Stream`; the engine is cited valkey.io — `xreadgroup`/`xgroup`/`xautoclaim`/`xack`/`xpending`); do NOT add a dep silently (the deep proofs are deterministic ExUnit integration tests + the ≥100 loop). Risk HIGH; Apollo MANDATORY (post-build).
