# emq3-5 — AAW scope ledger

## {emq3-5-thinking} Thinking

### T-1 — emq3.5 the archive: §0 derivation (two-app, store-side substance)

5W. WHY: deep stream history without resident memory — segments trimmed past the emq3.4 retention window must survive durably and read beside the live tail (the never-deleted problem; emq.streams.md §"the durable-archive answer"). WHAT: a Committer-shaped fold consumer reads trimmed slices off the bus and commits them into the native EchoStore.Graft engine at a reserved high page range (@archive_base disjoint from @obx_base), + a merge-read over an engine-derived watermark W, + box-loss restore (engine lazy-fetch of segments/{SEG} from Tigris). WHO: the echo_mq Stream Tier program, rung emq3.5, built across echo_mq + echo_store (Operator-ruled two-app boundary, D-1). WHEN: now — active near-term delivery, gated on emq.0 (met); emq3.1–3.4 shipped (conf 77). WHERE: net-new code in echo/apps/echo_store (archive landing in plugins/graft.ex + a fold consumer mirroring graft/committer.ex + the merge-read), with at most an optional emq:{q}:stream:<name>:archived polyglot cache key in echo/apps/echo_mq.

Solution space (incl. do-nothing). Do-nothing: trimmed segments lost past the window — deep history unsolved. Ruled design = the six echo-bus-v3 convergences (KB §2): F-LS-A reserved high-range page index · F-ENG-A native Graft via VolumeServer.commit/3 · F-ENG-B store-side Committer-shaped consumer (NO injected bus callback — the bus cannot verify the no-loss invariant) · F-ENG-C native alone (eg.6/Rust deferred) · F-PLAT-B engine-derived W merge-read · F-PLAT-C distinct vocabulary; + EBV3-9 Graft-canonical durability · EBV3-10 distinct watermarks. Rejected: bus-side callback; per-stream Graft Volume as default (A2 opt-in for a few deep streams); MINID-coupled GC (EBV3-10 — later measured additive).

Invariants as runnable checks. (1) segment fold == stream slice: folded page payload's branded ids equal the trimmed slice's ids, in mint order (order theorem extended to the archive). (2) box-loss restore: drop the local CubDB, restore from Tigris, archive reads identical. (3) merge-read property: a read spanning the trim boundary returns archived ∪ live-tail, no gap / no overlap (consequence of fold-before-trim + W = the engine's committed frontier). (4) @archive_base disjoint from @obx_base (=erlang.bsl(1,48)) — no page collision with the outbox. (5) the v2 laws bind any bus-side surface unchanged (braced keys, branded ids, declared Lua keys, server clock).

Smallest change that preserves correctness. A new store-side fold consumer (Committer-shaped) + the archive page-index landing in plugins/graft.ex + a merge-read fn (RE-CONFIRM head_lsn on disk — the KB cited graft.ex:40-44 but the symbol did not grep there: a NO-INVENT flag for Venus's reconcile), gated by a new echo_store test (fold/restore/merge properties) + (iff a bus-side verb lands) an additive EchoMQ.Conformance scenario for the optional :archived key. Shipped stream verbs / EchoMQ.Stream byte-frozen; the emq3.4 trim is the upstream that produces the slice.

Risk tier: HIGH — new cross-app surface + new supervised process + durable at-rest engine write + first stream rung to reach store-side. → deepened Director verify (≥100 determinism loop on the engine/process suite + mutation battery + the two-app boundary grep) + mandatory Apollo.

### T-2 — Director independent reconcile: the NO-INVENT flag resolved + the fold-before-trim hazard

(1) Merge-read watermark symbol RESOLVED. The KB's "head_lsn/1 at plugins/graft.ex:40-44" was imprecise — the symbol does NOT live there. The real public engine-frontier reader is `EchoStore.Graft.VolumeServer.head_lsn/1` (volume_server.ex:57-58, `@spec head_lsn(BrandedId.t()) :: non_neg_integer()`), with `Store.head_lsn/1` the internal call (used in plugins/graft.ex:177/201/213, committer.ex:77, reader.ex:30, streamer.ex:64). CRUCIAL: head_lsn returns a commit **LSN (integer)**, NOT a branded id. So the archive merge boundary `W` (a branded id, F-LS-A: the page payload carries the id) is **archive-semantic** — the highest branded id whose record has been folded — derived from the folded page payloads, distinct from the raw engine LSN. Mars must not conflate the two. NO-INVENT flag for Venus: cite VolumeServer.head_lsn/1, and write W's branded-id derivation precisely (forward-tense if a new archive-frontier reader is needed).

(2) The fold-before-trim HAZARD (the load-bearing design problem → Arm-2). emq3.4's `EchoMQ.Stream.trim/4` (stream.ex:215 MAXLEN / 222 MINID) is a DESTRUCTIVE XTRIM; `EchoMQ.StreamRetention` (a GenServer over a pure Core) re-applies a declared policy via Stream.trim on a tick. There is NO fold step today. If emq3.5's fold consumer runs independently of the retention tick, the trim can DELETE a slice before the consumer commits it to Graft → silent segment loss (violates invariant #1 segment-fold==slice). The structurally-sound mechanism: **clamp the effective trim floor to the folded watermark W (fold-then-trim)** — retention PROPOSES a horizon, the trim can only ever delete what is already durably folded. This matches the manuscript ("coupled only by fold-then-trim; a slice is folded durably before the stream may trim past it", checkout-and-gc.md §2) AND EBV3-10 (the two watermarks keep DISTINCT values, coupled by a SAFETY RULE not a shared number). Director's lean on Arm-2: trim clamped to W, pending Venus's framing.

(3) The fold-consumer precedent is exact. `EchoStore.Graft.Committer` (committer.ex:73-77 catch-up drain "every commit above the announced frontier, oldest first"; :109 "Move the announced frontier forward; persisted so a restart resumes, never re-floods") already implements restart-safe catch-up-drain-from-a-persisted-announced-frontier — the precise shape the store-side fold consumer mirrors. Box-loss restore path is also real: graft/remote/tigris.ex (put_commit/list_commits), graft/reader.ex (get_at), graft/streamer.ex (commit_ready → Tigris).

## {emq3-5-decisions} Decisions

### D-1 — Boundary: emq3.5 is a TWO-APP rung (echo_mq + echo_store), Operator-ruled

The seam fork: emq3.5 = "the archive", but its ruled build surface (page-index landing, the commit/3 fold, the merge-read, the Committer-shaped fold consumer) lives in echo_store, not echo_mq — forced by the one-way dependency echo_store → echo_mq (echo_mq/mix.exs has no echo_store dep; the bus cannot call the store; echo-bus-v3 F-ENG-B / EBV3-3). /echo-mq-ship's default boundary is echo_mq-only. Surfaced via AskUserQuestion BEFORE any spawn (the bootstrap STOP-on-open-seam rule).

RULED (Operator, Arm A — Two-app rung): keep emq3.5 a Stream-Tier rung (emq3-5 ledger/ladder), but EXPLICITLY widen the build boundary to echo_mq + echo_store for this rung. Net-new fold/landing/merge-read code lands in echo_store; at most the optional emq:{q}:stream:<name>:archived polyglot cache key lands in echo_mq. Verify runs BOTH app gate ladders; the rung commit spans both apps, pathspec-scoped, as the rung's measured surface. Rejected: Arm B (relocate to a store-tier rung, out of the echo_mq vehicle) and Arm C (bus-side enabling seam only, store-side fold deferred — under-delivers "the archive" this run).

Consequence: the boundary grep for this rung is {echo_mq, echo_store} (not echo_mq alone); the third-app rule still bars echo_graft / codemojex / echo_bot / mix.lock. The dependency law is honored (code where it must live), not bent.

### D-2 — Formation: HIGH-risk Squad, single architect, two-pass Mars, mandatory Apollo

Topology Router triage (risk × design-width). RISK = HIGH (new cross-app surface; a new supervised fold-consumer process; a durable at-rest engine write; first stream rung to reach store-side). DESIGN-WIDTH = NARROW-but-real: the echo-bus-v3 dual-architect vision debate already ran and the Operator ruled the forks (six convergences + EBV3-9/10), so the remaining design is bounded — fold-consumer placement (store-side process vs host-side wiring, the "or host-side" of F-ENG-B), the two-app conformance/test posture, the box-loss proof, the merge-read contract + the optional cache key.

RULED: a Squad, right-sized — Venus (1, authors the two-app triad + carves the remaining Arms; a SECOND architect is over-ceremony, the vision debate is done) → Director rules Arms (AskUserQuestion) → Mars-1 (build across both apps) → Director DEEPENED verify (≥100 determinism loop on the engine/process suite + full mutation battery + the two-app boundary grep + a box-loss adversarial probe) → Mars-2 (harden) → Apollo (MANDATORY on HIGH: spec-reconcile + BUILD-GRADE verdict + mentoring) → Director ship (pathspec commit spanning both apps + the Stage-6 roadmap fold). aaw_status(emq3-5) must then show exactly: Director, Venus, Mars, Apollo.

### D-3 — The five Arm rulings (Operator via AskUserQuestion + Arm 3 Director-ruled), pre-build

Arm 1 · fold-consumer placement → SUPERVISED store-side GenServer (Operator). A new OPT-IN EchoStore.* GenServer in echo_store's supervision tree (the EchoStore.Graft.Committer / EchoMQ.StreamRetention :transient-opt-in precedent: persisted frontier, restart-safe, never re-floods). The no-loss invariant is OWNED + verifiable where the store can see it (EBV3-3); box-loss restore is provable. REJECTED: host-wired (scatters no-loss ownership into host code the store cannot verify).

Arm 2 · the fold-then-trim trigger → TICK cadence in a NEW store-side fold-then-trim driver (Operator). The StreamRetention :transient-opt-in + pure-decision-core shape — NOT an edit to the frozen bare emq3.4 driver (INV7). For an archived stream THIS driver IS the retention path: the bare emq3.4 trim-only StreamRetention driver MUST NOT also run on an archived stream (else it trims before the fold → loss). REJECTED: hand-off (would edit the frozen driver, INV7); depth-watch (a future additive optimization once the tick path is proven).

Arm 3 · the two-app conformance/test posture → NEW store-side fold/restore/merge ExUnit suite (Director-ruled — FORCED: echo_store has no EchoMQ.Conformance harness). Proves INV1–INV6 POSITIVELY (never a no-op); the engine started under EchoStore.Graft.Supervisor (VolumeSup + Registry) in setup; :valkey-tagged for the slice round-trip on Valkey 6390.

Arm 4 · the optional bus-side :archived cache key → BUILD IT this rung (Operator — DEPARTS from the Director's defer recommendation). A stock SET/GET of emq:{q}:stream:<name>:archived = W (NO new Lua, rides the existing emq:{q}:stream:<name>:<sub> key form, NO keyspace.ex grammar edit; cleanup NAMED — overwritten on each fold, DELETEd on stream obliterate). The store-side fold consumer, after advancing W, writes this bus cache via a small NEW echo_mq function (put/get the archived watermark). + an ADDITIVE EchoMQ.Conformance scenario that is BUS-PURE (a put/get round-trip of the :archived key proving the W-cache contract a polyglot reader reads — NOT a cross-app fold; conformance stays bus-only), prior 77 BYTE-UNCHANGED → 78, probe-registered, BOTH pins re-pinned (conformance_run_test.exs + conformance_scenarios_test.exs) + the moduledoc + the echo_mq mix.exs label 2.6.3 → 2.6.4 (@wire_version FROZEN echomq:2.4.2). CONSEQUENCE: a genuine TWO-APP EDIT rung — the commit spans BOTH apps' code; INV7 RELAXES from "bus EMPTY-diff" to "bus ADDITIVE-ONLY" (the :archived put/get + the +1 conformance + the label; the shipped stream verbs / EchoMQ.Stream / EchoMQ.StreamRetention / every @-script still BYTE-FROZEN, echo_wire UNTOUCHED, keyspace.ex UNEDITED, grep -c redis.call on the bus lib diff still 0).

Arm 5 · the box-loss restore / determinism cut → OFFLINE local-restore IN-SUITE + live-Tigris :tigris-TAGGED (Operator). The offline path (drop the CubDB dir, re-open — the durability_test.exs precedent, deterministic, always-runs) is the gate's proof; the live-Tigris segments/{SEG} re-fetch is :tigris-tagged (runs only when a bucket is configured). Honest determinism posture stated. REJECTED: both-in-suite (an infra-dependent, flakier always-run gate).

The build surface is now fully ruled. Mars-1 builds to D-3 (the rulings), NOT to an unruled Arm. The ≥100 determinism loop runs over the store-side fold suite (id-minting setup). Apollo is a SHIP PRECONDITION (HIGH risk).

## {emq3-5-report} Report

### Y-1

T-2 — Venus reconcile findings (the NO-INVENT flags resolved on disk, pre-authoring)

The lag-1 reconcile against the as-built echo_mq + echo_store tree is COMPLETE. Verdict: every cited surface either MATCHES or is named forward-tense as net-new. TWO load-bearing findings reshape the build surface vs the KB §2 — both flagged by the Director's brief as NO-INVENT, now resolved:

**F-1 (load-bearing) — the merge-read watermark W is NOT `head_lsn` and NOT a branded id "read from the engine".** The KB §2 #5 cited `head_lsn/1 @ graft.ex:40-44` and called W "a branded id read from the engine's folded frontier." On disk: (a) `head_lsn` does NOT live in `plugins/graft.ex` — it is `EchoStore.Graft.Store.head_lsn/1` (`store.ex:35` → `CubDB.get(db, :head, 0)`, an INTEGER commit counter) and `EchoStore.Graft.VolumeServer.head_lsn/1` (`volume_server.ex:57-58`, the GenServer call). The KB conflated the call-site file (`plugins/graft.ex` calls `Store.head_lsn` at :177/:201/:213) with the defining module. (b) `head_lsn` is an engine LSN (a monotone commit integer), NOT a stream-record branded id — it is the WRONG TYPE to compare against live-tail `EVT` ids for a merge-read. RESOLUTION: emq3.5 must derive W as the **branded `EVT` id of the highest-folded record**, persisted by the fold (a CubDB archive-frontier key and/or in the archive page payload), and the merge-read splits on W: archived records ≤ W from the engine, live-tail records > W from `XRANGE`. `head_lsn` is the engine's commit cursor (used to address pages), NOT W. Named forward-tense in the body §the-merge-read; the build ADDS the frontier-id reader.

**F-2 (load-bearing) — `XTRIM` returns only a removed-COUNT, never the deleted entries, so fold-before-trim CANNOT react to the emq3.4 trim.** `EchoMQ.Stream.trim/4` (`stream.ex:213-226`) issues `XTRIM` direct and returns `{:ok, removed_count}` (`xtrim/2`, `stream.ex:252-258`) — an integer. `XTRIM` does not return what it deleted; the trimmed slice is GONE the instant the trim fires. So "the trimmed slice the fold consumer reads off the bus" (KB §2 #3 / the §0 derivation) does NOT exist as a post-trim artifact. RESOLUTION: the no-loss invariant forces FOLD-THEN-TRIM with the consumer OWNING the trim — read the about-to-expire slice via `XRANGE` (the surviving `Stream.read/6` path, which recovers each record's branded id from the stored `id` field, `stream.ex:294-297`), `commit/3` it to Graft, ADVANCE W, and ONLY THEN `trim/4` the now-archived span. The emq3.4 `EchoMQ.StreamRetention` bare driver (which calls `trim/4` with no fold) must NOT be the archive's retention path — the archive needs a fold-then-trim driver. This crosses Arm 1 (placement) × Arm 2 (trigger): a store-side consumer driving read-slice→fold→advance-W→trim is the ONLY shape where fold-before-trim is provable. (The emq3.4.prompt.md §the-rung-in-one-paragraph already anticipated this: "emq3.5's archive fold rides this proven retention (fold-before-trim — the fold consumer commits a segment, THEN trims it)".)

**The MATCH set (cited surfaces confirmed on disk, by re-probe):**
- `@obx_base = :erlang.bsl(1, 48)` CONFIRMED at `plugins/graft.ex:46` (the outbox reserved range; doc :16 "a unique page index in a reserved high range"; filters `idx >= @obx_base` at :161/:189). The archive's `@archive_base` mirrors this, disjoint from `@obx_base` (F-LS-A / EBV3-1). NOTE: `@obx_base = bsl(1,48)` and a CubDB page idx is a non_neg_integer with no stated ceiling — `@archive_base` must be a SECOND disjoint high range; a candidate is `bsl(1,49)` or `bsl(3,47)` (disjointness is the invariant, the exact constant is Mars's to settle + pin; the body states the disjointness law, not the literal).
- `EchoStore.Graft.VolumeServer.commit/3` CONFIRMED `volume_server.ex:50` → `{:ok, lsn} | {:error, {:conflict, head}} | {:error, term}`; `begin/1` :41 → `{:ok, base_lsn}`; `snapshot/1` :55 → `%Snapshot{volume_id, lsn, index}` where `index :: %{page_idx => {lsn, seg}}`. The facade `EchoStore.Graft.commit/3`/`begin/1`/`snapshot/1`/`read_at/3` (`graft.ex` delegates) is the public surface; `open_volume/2` (`graft.ex`) starts a Volume under `EchoStore.Graft.VolumeSup` (the DynamicSupervisor in `EchoStore.Graft.Supervisor`, NOT auto-started — the host/test starts it). `read_at(vol, snap, idx)` → `{:ok, bin} | :absent`.
- `EchoStore.Graft.Committer` CONFIRMED `committer.ex` — the precedent the fold consumer mirrors: a `use GenServer`, `Sync.subscribe_commits/2` at init, `{:emq_push, ["message", _channel, payload]}` handler, a `:drain` catch-up, a persisted frontier (`SyncPoint` via `Store.{get,put}_syncpoint`), `via/1` Registry naming. The fold consumer is THIS shape with the destination = the Graft engine and the source = a stream's trimmed-tail rather than the commit channel.
- `EchoStore.Durability.Graft` (`plugins/graft.ex`) — the OUTBOX adapter using `@obx_base` page-range commits via `VolumeServer.commit/3` (`record/4` :83). The archive's page-range landing is the SAME pattern at `@archive_base` — a strong build precedent for "commit a page carrying a branded id at a reserved high range."
- `EchoMQ.Stream.read/3..6` CONFIRMED `stream.ex:270` → `{:ok, [{branded, fields_map}]}` in mint order (the branded id recovered from the stored `id` field). This is the slice-read the fold consumer uses. `Stream.Id.xadd_id/1` (`stream/id.ex:87`) maps a branded id → `"<ms>-<tail22>"` where `<ms>` is REAL Unix-ms — so W (a branded id) maps to an `XRANGE` bound for the merge split.
- Conformance is **77** as-built (`conformance.ex` moduledoc "seventy-seven" ×3 + `run/2 → {:ok, 77}` :264; `conformance_run_test.exs:74` `{:ok, 77}`; `conformance_scenarios_test.exs` `@run_order` 77 atoms). The KB §0 wrote "conf 77" — MATCHES.
- echo_store deps (`mix.exs:24-35`): `echo_data` + `echo_mq` + `echo_wire` + `exqlite` + `cubdb ~> 2.0` — all in-umbrella declared; `cubdb` (the engine dep) lives in echo_store, NOT echo_mq → the fold/landing/merge code MUST live store-side (confirms D-1; a bus-side fold is structurally impossible — echo_mq has no `cubdb`, no `echo_store` dep).
- The native engine is tested OFFLINE today (`durability_test.exs`, `async: true`, no Valkey/Snowflake — pure rollup/frontier/fencing/divergence). There is NO live-`VolumeServer` test on disk (the only live graft test is the RUST `graft_backend/live_round_trip_test.exs`, the coexisting peer). So emq3.5's fold/restore/merge suite is NET-NEW live-engine test scaffolding, store-side.

**msh baseline:** `msh specs echo_mq` (error) shows 5 PRE-EXISTING findings, NONE in `specs/emq3/` (4× `KEYS[n](word)` false-links in `emq-5-4.progress.md`; 1× a stale `emq3-4.progress.md` link to a non-existent `echo_mq/stream_retention.md`). These are OUT of my edit scope; my authored emq3.5 triad must add ZERO new findings.

Next: author the four spec files (emq3.5.{md,stories.md,llms.md,prompt.md}) + carve the 5 Arm-sets for the Director. The two findings above are the spine of Arms 1/2 (placement × trigger — they collapse into one decision) and Arm 4 (the W contract).

### Y-2 — Venus deliverable: the emq3.5 two-app spec triad AUTHORED + the 5 Arms carved (BUILD-GRADE pending the Director's Arm rulings)

**The four files written** (all at docs/echo_mq/specs/emq3/, house-style = emq3.4 template):
- emq3.5.md — the authoritative body: §0 the slice (the never-deleted problem, the store-side fold-then-trim, the two-app boundary D-1, the engine COEXIST D-1/EBV3-2) · §1 the fold contract (read-slice → commit/3 at @archive_base → advance W → THEN trim, INV1 fold-before-trim) · §2 the @archive_base landing (disjoint from @obx_base, INV4) · §3 the merge-read over W (a branded EVT id, NOT head_lsn, INV3/INV6) · §4 the fold consumer + box-loss restore (INV5) · §5 the two-app conformance/test posture · the testing posture (the ≥100 loop — a process + at-rest write + id-minting setup) · INV1–10 as runnable checks · the closed error set · the 5 Arms · DoD. Forward-tense for everything net-new; present-tense (re-probed) for shipped surfaces.
- emq3.5.stories.md — US1 (fold-before-trim/no-loss) · US2 (the merge-read no-gap/no-overlap, W a branded id) · US3 (@archive_base disjointness) · US4 (box-loss restore) · US5 (the bus byte-freeze + engine COEXIST) · EMQ3.5-US-GATE (the two-app gate + the ≥100 loop) + the liveness-no-vacuous-pass discipline + the Coverage map.
- emq3.5.llms.md — the two-app Mars brief: References (both apps' surfaces, links/paths first) · Requirements 1–8 (each traced to a story + an invariant) · the build-order DAG + the EXACT files touched (store-side net-new; bus-side at most the optional :archived cache) · AS-1..AS-6 + AS-GATE (each a Directive + an Acceptance gate).
- emq3.5.prompt.md — the runbook: HIGH-risk Squad mode (Venus → Director-rules-Arms → Mars-1 → Director DEEPENED verify → Mars-2 → Apollo MANDATORY → Director ship), the THREE HIGH triggers + their gate components, the 5 Arms, the 6 pipeline stages, the TWO gate ladders (bus + store), the two-app boundary, DoD.

**The reconcile (T-2 detail) — every claim MATCH or forward-tense; TWO load-bearing NO-INVENT findings resolved on disk:**
- F-1: W is NOT head_lsn (Store.head_lsn/1 store.ex:35 + VolumeServer.head_lsn/1 volume_server.ex:57 return an INTEGER engine LSN, the page cursor — not a stream-record branded id; the KB conflated the call-site file with the defining module). W = the branded EVT id of the highest-folded record, persisted by the fold + read from the engine's archive frontier. emq3.5 ADDS the W reader (archive_frontier/1). The integer LSN addresses pages, never the merge split.
- F-2: XTRIM returns only a removed-COUNT (stream.ex:252), never the deleted entries — so fold-before-trim CANNOT react to the emq3.4 trim. The no-loss invariant forces fold-THEN-trim with the consumer OWNING the trim: read-slice (Stream.read/3..6, which recovers each record's branded id from the stored id field) → commit/3 → advance-W → THEN trim/4. The bare emq3.4 StreamRetention driver (it trims with no fold) is NOT the archive's path.
- MATCH set: @obx_base = :erlang.bsl(1,48) (plugins/graft.ex:46); commit/3 (volume_server.ex:50) → {:ok,lsn}|{:error,{:conflict,head}}; the Committer subscribe/drain/frontier/via shape (committer.ex); Stream.read/3..6 (stream.ex:270) + trim/4 (:213); Stream.Id.xadd_id/1 <ms>=real Unix-ms (stream/id.ex:87); conf 77 (run/2→{:ok,77}); cubdb already an echo_store dep (mix.exs:35 — no new dep); the engine tested OFFLINE today (durability_test.exs async:true — no live native-VolumeServer test exists; the only live graft test is the RUST graft_backend peer); the arrow echo_store→echo_mq (echo_mq has no echo_store dep) FORCES the fold store-side.

**msh specs:** CLEAN over my 4 files (0 findings in any emq3.5.* file; all 4 cross-link depths verified resolving on disk — the KB at ../../kb/, the store design at ../../store/, the approach at ../../../elixir/, the ledger at ../progress/). The 11 tree-wide findings are ALL pre-existing + out of my edit scope (echo_mq.md, emq4.roadmap.md, kb/emq-durability-design.md, emq-5-4.progress.md KEYS[n](word) false-links, emq3-4.progress.md stale link). No KEYS[n](x) footgun in my files (grep clean).

**The 5 Arms carved (four-part framed in the body §the-Arms + the prompt §the-Arms; SURFACED not ruled — the Director rules via AskUserQuestion BEFORE Mars-1):**
1. Fold-consumer placement — supervised EchoStore.* GenServer vs host-wired (F-ENG-B's "store-side OR host-side"; no injected bus callback either way).
2. The fold-then-trim trigger — tick (StreamRetention shape) / depth-watch / hand-off-from-emq3.4-driver. The invariant (fold before trim, consumer owns both calls) is fixed; the mechanism is the Arm.
3. The two-app conformance/test posture — a new store-side fold/restore/merge suite (the store has no EchoMQ.Conformance) + iff a bus verb lands, an additive conformance scenario (77→78).
4. The merge-read surface + the optional :archived cache key — built this rung (+ conformance 77→78) vs deferred (W store-side only, bus conformance frozen at 77). W fixed as a branded EVT id (F-1).
5. The box-loss restore proof / determinism cut — offline local-restore in-suite + live-Tigris :tigris-tagged, vs both in-suite. The honest posture stated.

VERDICT: the triad is BUILD-GRADE once the Director rules the 5 Arms (D-3..D-7). Every surface is real or forward-tense; the two-app boundary is explicit; the no-loss invariant (fold-before-trim) is the spine; the determinism posture is the ≥100 loop (a process + at-rest write + id-minting setup), NOT a multi-seed sweep. Sending the Director the decision-ready Arms digest next.

## {emq3-5-progress} Progress

### P-1 — Mars-1 lag-1 reconfirm COMPLETE (DAG step 1); building to D-3 rulings

Re-probed every cited surface across both apps on disk. All MATCH the body; two as-built precision points I will build to:

- VolumeServer.snapshot/1 (volume_server.ex:119-120) returns %Snapshot{volume_id, lsn} with index DEFAULTING to %{} — it does NOT fold the commit log into an index. Only Store.index_at/3 (store.ex:80) builds a populated index. So the archive enumeration of the @archive_base range MUST NOT depend on snap.index from VolumeServer.snapshot/1; the robust public path is Store.page_at/3 (store.ex:58, the reverse-select {:page,idx,0}..{:page,idx,head}) or read_at/3 over a Store.index_at snapshot. EchoStore.Durability.Graft.replay/2 reads snap.index (plugins/graft.ex:160) — but that is the outbox's path; the archive owns its own folded-count frontier and reads @archive_base+0..n-1 by index, sidestepping the empty-index trap. Stays on PUBLIC engine surface (INV8).
- echo_store has NO application.ex supervision tree → EchoStore.Graft.Supervisor is NOT auto-started (host/test starts it; spec-confirmed). The fold-consumer suite starts EchoStore.Graft.Supervisor in setup.

Confirmed: @obx_base = :erlang.bsl(1,48) (plugins/graft.ex:46); next_seq/1 atomic allocator (plugins/graft.ex:239); VolumeServer.commit/3 (:50) → {:ok,lsn}|{:error,{:conflict,head}}; Committer shape (committer.ex — use GenServer, via/1 Registry, persisted frontier, :drain); Stream.read/3..6 (stream.ex:270) → {:ok,[{branded,fields_map}]} mint order; Stream.trim/4 (:213) → {:ok,removed_count}; Stream.minid_floor/1 (:239); Stream.Id.xadd_id/1 (id.ex:87) + evt?/1 (:102) + kind/0 (:76); Snowflake.next_branded/1 + unix_ms/1 + min_for/1; BrandedId.parse/1 + valid?/1; conf 77 (run/2→{:ok,77} conformance.ex; conformance_run_test.exs:74; @run_order 77 atoms ending stream_verbs/stream_append/stream_group/stream_retention); echo_store deps already declare cubdb+echo_mq (no new dep); echo_store test_helper excludes :valkey by default. Engine internals (volume_server/store/reader/streamer/segment/committer/plugins/graft/supervisor) READ-only (INV8).

Building store-side: the @archive_base landing + W reader + fold consumer (supervised, tick — Arm 1/2) + merge-read + box-loss suite; bus-side: the :archived put/get + the +1 conformance (Arm 4, 77→78). Next: write the code.

### P-2 — BUS SIDE (echo_mq) COMPLETE + GREEN (Arm 4). Store side held on E-1 ruling.

BUS LADDER (echo_mq) all green on Valkey 6390:
- asdf: elixir 1.18.4 / erlang 28.5.0.1 (re-probed from app dir).
- TMPDIR=/tmp mix compile --warnings-as-errors → EXIT=0 (clean; the lone echo_data champ_view warning is a pre-existing upstream-dep warning, not echo_mq's gate).
- TMPDIR=/tmp mix test --include valkey → 18 doctests, 533 tests, 0 failures (the [error] GenServer-killed log lines are pre-existing deliberate-kill tests + the pre-existing StreamConsumer WRONGTYPE test; summary 0 failures authoritative).
- Conformance.run/2 → {:ok, 78} (CONFORMANCE 78/78; stream_archived passes). The prior 77 BYTE-UNCHANGED (only stream_archived appended after stream_retention in scenarios/0 + @run_order). Both pins re-pinned: conformance_run_test.exs {:ok,77}→{:ok,78} + "seventy-seven"→"seventy-eight" ×2; conformance_scenarios_test.exs @run_order +:stream_archived + count prose. conformance.ex moduledoc "seventy-seven"→"seventy-eight" ×2 + the emq3.5 narrative.

BUS BYTE-FREEZE (INV7, relaxed to ADDITIVE-ONLY by Arm 4) PROVEN:
- grep -c redis.call on echo_mq lib/ diff = 0 (no new Lua).
- echo_wire EMPTY-diff; keyspace.ex EMPTY-diff.
- stream.ex diff = 76 added / 0 removed (purely the new put_archived/4 + get_archived/3 + clear_archived/3; the shipped append/4/read/6/trim/4/stream_key/2 byte-identical — 0 deletions). stream_retention.ex / stream_consumer.ex / stream/id.ex UNCHANGED (no diff).
- echo_mq/mix.exs label 2.6.3 → 2.6.4 (@wire_version FROZEN echomq:2.4.2 — connector.ex untouched).

Bus-side files touched: lib/echo_mq/stream.ex (+3 fns), lib/echo_mq/conformance.ex (+1 scenario+helpers+moduledoc count), test/conformance_run_test.exs (re-pin), test/conformance_scenarios_test.exs (re-pin), mix.exs (label).

STORE SIDE: written (EchoStore.StreamArchive + .Core + .Driver) but UNGATEABLE until E-1 (the pre-existing echo_store duplicate-module CompileError) is ruled. Holding the store gate for the Director's ruling on E-1 (A: Operator removes orphan / B: I delete lib/echo_store/core/adapter.ex as a scoped prerequisite).

## {emq3-5-escalations} Escalations

### E-1 — BLOCKER: echo_store is RED at HEAD (pre-existing duplicate-module collision), independent of emq3.5

STOP-and-escalate (program law: a spec⇄as-built contradiction stops, does not invent; do-no-harm on a committed file outside the rung surface).

THE FACT. echo_store cannot compile AT HEAD, before any emq3.5 change. Two COMMITTED files define the SAME module `EchoStore.Durability.Adapter`:
  - lib/echo_store/core/adapter.ex (committed 303f1653 "[echo_data] [echo_store] plugins"; 12 @callback; the `core/` dir but declares the `Durability.Adapter` module)
  - lib/echo_store/durability/adapter.ex (committed 59a76913 "[echo_store] durability, graft"; 9 @callback)
Elixir refuses: "cannot define module EchoStore.Durability.Adapter because it is currently being defined in lib/echo_store/core/adapter.ex:1" → CompileError. This blocks ALL echo_store compilation, the existing store suites, and my emq3.5 store-side modules alike.

PROOF IT IS PRE-EXISTING (the stash-and-recompile-at-HEAD test). I moved my NEW files (stream_archive.ex + stream_archive/{core,driver}.ex) ASIDE and recompiled echo_store: the SAME CompileError fired with my files absent. So HEAD is red on its own gate; my code compiles cleanly INTO a tree that itself does not build. Neither adapter file is in my edit scope (both committed before this rung; not staged by me).

THE RECOMMENDED RESOLUTION (Director's call — I will not delete a committed file on my own authority). Delete lib/echo_store/core/adapter.ex (the orphan), keep lib/echo_store/durability/adapter.ex (the canonical). Evidence the canonical is `durability/adapter.ex`:
  - EVERY live implementor sits in durability/ and references it: durability.ex:20 `@behaviour EchoStore.Durability.Adapter` + alias; durability/sqlite.ex + durability/memory.ex use `@impl EchoStore.Durability.Adapter`; plugins/graft.ex:34 `@behaviour EchoStore.Durability.Adapter`.
  - NOTHING in lib/ references an `EchoStore.Core.*` namespace (grep clean) — core/adapter.ex is a misplaced/stale copy under `core/` declaring a module that belongs to `durability/`.
  - The two bodies DIFFER (core/ has 12 callbacks incl. extras; durability/ has the 9 the implementors @impl). Keeping durability/adapter.ex matches the @impl set the suite compiles against.
This is a docs-clean DELETE of one orphan file — but it is OUTSIDE the emq3.5 boundary (a third concern in echo_store), so it needs a Director ruling: (A) the Director/Operator removes the orphan out-of-band (cleanest — keeps emq3.5's diff purely the archive); OR (B) the Director rules I may delete lib/echo_store/core/adapter.ex as a same-tree prerequisite and the ship commit carries it as a separate scoped concern (NOT folded into the emq3.5 archive commit — the LAW-4 split-by-concern rule). Either unblocks the store ladder.

STATUS of emq3.5 meanwhile:
  - BUS SIDE (echo_mq) is COMPLETE + COMPILES CLEAN (warnings-as-errors EXIT=0): EchoMQ.Stream.put_archived/4 + get_archived/3 + clear_archived/3 added (additive-only, the shipped verbs byte-frozen, no new Lua, keyspace.ex unedited, echo_wire untouched — Arm 4). The +1 conformance scenario (stream_archived, 77→78) + both pins + the label 2.6.3→2.6.4 are NEXT (I can finish + gate the bus side now; the bus ladder does not depend on echo_store).
  - STORE SIDE (echo_store) is WRITTEN (EchoStore.StreamArchive [@archive_base=bsl(1,49), the fold, the W frontier reader, read_archive, merge_read] + StreamArchive.Core [pure decide/resolve] + StreamArchive.Driver [the supervised :transient fold-then-trim consumer, Arm 1/2]) but UNGATEABLE until the duplicate-module blocker is resolved.

ASK: rule (A) Operator removes the orphan out-of-band, or (B) I delete lib/echo_store/core/adapter.ex as a scoped prerequisite (separate commit concern). I proceed on the bus side regardless; I need the ruling to gate the store side.
