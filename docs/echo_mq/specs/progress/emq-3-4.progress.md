# emq-3-4 — AAW scope ledger

## {emq-3-4-thinking} Thinking

### T-1 — emq-3-4 scope opening: close emq.3.3 + design emq.3.4 + reconcile the forward-feature docs (Flat-L2; recovery + design)

CONTEXT. The emq-3-3 run (Flat-L2, HIGH-risk cross-queue flow) reached gate-green: Venus authored the triad (Y-1); the REAL Mars took ownership — independently re-derived correctness against the triad, FIXED the L-2 vacuous keystone test (the crash-survivor outbox entry was built with the CHILD queue as field 1, but `@complete` emits `parent_queue` first → the re-deliver `@flow_deliver` targeted the WRONG slot, the `:processed` HSETNX guard was never exercised, the test passed by coincidence), hardened to +3 R2 tests (20 total in flow_cross_queue_test.exs), and owned the full gate ladder (303 tests/0 fail · Conformance {:ok,47} · 100/100 determinism · harness 9/9) — recorded as Y-2. But the MANDATORY Apollo verdict NEVER landed: the evaluator ran ~1h47m then went idle WITHOUT recording a Y-n or sending its report. emq-3-3 froze at D:5 L:2 P:2 T:6 V:4 Y:2 (no Apollo Y). The emq-3-3 team is deleted from the registry; this emq-3-4 scope is recreated to discharge BOTH halves (Operator directive, 2026-06-15).

5W. WHO — the EchoMQ aaw lead team (Director + REAL fanned-out Venus + Apollo; Mars only if Apollo BLOCKS); the Operator owns the goal + every fork. WHAT — (a) CLOSE emq.3.3: a real, RECORDED MANDATORY Apollo verdict on the as-built cross-queue flow, then the LAW-4 ship; (b) DESIGN emq.3.4: triad + prompt for the next flow-family rung (dead-child / failure-policy + grandchildren, per emq-3-3 D-5a); (c) RECONCILE the forward docs: fold the STALE docs/echo_mq/roadmap/ per-area files UP into the canonical catalog (emq.features.md / emq.roadmap.md) in the new Goal/Rationale/5W/Scope-In-Out/AC format with NO feature dropped, then remove the dir; reconcile emq.progress.md / emq.roadmap.md statuses (3.1/3.2 shipped, 3.3 shipped-this-run, 3.4 specced). WHY — the closure is blocked on a missing INDEPENDENT verdict (shipping without it would re-commit the very role-play this program corrected, L-1); the forward docs drifted (a parallel roadmap/ subdir forking the canonical catalog; NO flows.md for the active frontier). WHEN — this run; Wave 1 Apollo ∥ Venus, Wave 2 normalization, then the Director ship. WHERE — verify: echo/apps/echo_mq (READ-ONLY for Apollo, Valkey 6390); author: docs/echo_mq/{specs/emq.3.4.*, emq.features.md, emq.roadmap.md, emq.progress.md}; the emq.3.3 surface (code + triad + harness) is the pending LAW-4 subject.

PLAN (smallest correct path — NOT a rebuild; the 3.3 design D-1..D-5 is ratified + the code gate-green; discarding it would waste correct work + risk regression):
- Wave 1 (parallel REAL subagents, LAW-1, no role-play): Apollo (general-purpose + apollo charter + echo-mq-evaluator skill) adversarially verifies the as-built emq.3.3 vs its triad — declared-keys on the NEW cross-slot Lua (the F-1 class, gate-INVISIBLE on single-node Valkey), the order theorem, the :processed idempotent re-deliver (at-least-once→effectively-once), the outbox lifecycle, a mutation kill-rate with SCRIPT FLUSH — and RECORDS its verdict as a Y-n INCREMENTALLY + SendMessages it to the Director BEFORE going idle (the persistence-hardening the halted run lacked). Venus (general-purpose + venus charter + echo-mq-architect skill) authors the emq.3.4 triad+prompt AND establishes+applies the new format, folding roadmap/ up into the canonical catalog.
- Wave 2: a normalization pass over the remaining feature areas + the status reconcile (sized after Wave 1).
- Director ship: ONLY on Apollo BUILD-GRADE → the LAW-4 pathspec commit of the emq.3.3 measured surface; then remove the (now-folded) stale roadmap/ dir; the Stage-6 fold.

RECORDS-FREEZE. emq-3-3.progress.md (now at specs/progress/) is the FROZEN audit trail of the halted run — never rewritten; the 3.3 verdict lands HERE in emq-3-4's ledger. LEDGER LOCATION: aaw PINS emq-3-4's ledger to docs/echo_mq/specs/emq-3-4.progress.md (ledger_dir immutable after first init); it migrates to specs/progress/ at closure. HYGIENE (this turn): 20 tracked run-ledgers relocated to docs/echo_mq/specs/progress/ (commit fd8876d5, pure renames); the 11 closed scopes archived, the active emq-3-4 stays at specs/ for the run.

### T-2 — emq.3.4 Stage-0 lag-1 reconcile (post-emq.3.3 tree) + the scope-decision fork. BUILD-THE-DESIGN.

Re-probed every as-built anchor against the live echo/apps/echo_mq tree (line numbers CONFIRMED, not assumed — the emq.3.3 ship moved the flow surface; the emq.3.3 triad's anchors are STALE on line-number, MATCH on substance). Delta table:

| # | Anchor (the emq.3.4 build depends on) | As-built (re-probed 2026-06-15) | Verdict |
|---|---|---|---|
| 1 | `EchoMQ.Flows.add/3` (the flow add the failure-policy + add_bulk extend) | `flows.ex:152` `add/3` (was :83 pre-3.3); `add_same_queue/5` :249 + `add_cross_queue/5` :285 + `land_children/4` :309 | MATCH (re-pinned :152) |
| 2 | `children_values/3` / `dependencies/3` (the emq.3.2 reads the failure reads sit beside) | `flows.ex:190` `children_values/3` (HGETALL :processed), `flows.ex:227` `dependencies/3` (GET :dependencies, {:ok,0} sentinel :176-ish) | MATCH (re-pinned :190/:227) |
| 3 | THE FAILURE PATH — a child FAILS via `@retry`'s dead-letter arm, NOT `@complete` | `jobs.ex:225` `@retry`; the dead-letter arm `jobs.ex:254-259` (`HSET last_error`; at-max-attempts `HSET state 'dead'` + `ZADD KEYS[3](dead) 0 id`; `return 'dead'`). The `@complete` fan-in (jobs.ex:212-219) fires ONLY on success. | MATCH — **the crux: the parent fan-in hook is on @complete; a dead child NEVER reaches the parent today → the parent hangs forever. This is the exact gap fail_parent_on_failure/ignore_dependency_on_failure close.** |
| 4 | `@complete` fan-in branch (the byte-frozen single-queue release; the cross-queue emit) | fan-in `jobs.ex:212-219` (`if KEYS[3] and was_active==1 then DECR KEYS[3]; HSET KEYS[4]...; at-zero release`); cross-queue emit branch `jobs.ex:205-206` gated `ARGV[6]=='xq'`, RPUSH outbox at KEYS[3] | MATCH |
| 5 | `complete/5` + `parent_of/3` (the host fan-in dispatch the failure path mirrors) | `jobs.ex:412` `complete/5` (result\\nil); `parent_of/3` `jobs.ex:459` (HMGET parent + parent_queue → {:cross_queue,...} | {:same_queue/single,...} | :none) | MATCH |
| 6 | `@flow_deliver` + `deliver_flow_completions/3` (the cross-queue sweep the cross-queue FAIL delivery mirrors) | `pump.ex:42` `@flow_deliver` (HSETNX :processed guard → DECR :dependencies → at-zero release); `pump.ex:161` `deliver_flow_completions/3` (LRANGE non-destructive → deliver → LTRIM, deliver-before-remove); `sweep/1` :126 → `%{promoted,fired,delivered}` | MATCH |
| 7 | §6 grammar — the `:failed`/`:unsuccessful` subkeys (do they need a grammar add?) | design.md:307 `sub ∈ {lock, logs, dependencies, processed, failed, unsuccessful}` — **`:failed` AND `:unsuccessful` ALREADY in the §6 closed set at the founding**; v1 `keys.ex:46` `@job_subkeys ~w(lock logs dependencies processed failed unsuccessful)` confirms the names. keyspace.ex `queue_key/2` is a pure composer (no runtime allowlist). | MATCH — **KEY FINDING: unlike emq.3.3's `flow:outbox` (a NEW `type`), the failure subkeys are PRE-REGISTERED in §6. emq.3.4 needs NO grammar change AND NO new §6 type — it populates already-reserved subkeys. INV1 is satisfied by construction.** |
| 8 | conformance count (live) | LIVE = **47** (`conformance.ex:103` flow_cross_queue; both pins "forty-seven"/`{:ok,47}`). emq.3.4 adds flow_fail_parent + flow_ignore_dep + flow_add_bulk → 47→50, prior 47 byte-unchanged, both pins re-pinned. | MATCH (count CONFIRMED live, not hardcoded) |
| 9 | v1 failure-policy semantics (the capability to PORT, READ-ONLY) | `flow_producer.ex:80-81` `fail_parent_on_failure` (default true) / `ignore_dependency_on_failure`; `encode_job_opts` :478-481 threads `fpof`/`idof`/`remove_dependency`; `job.ex:885` `get_ignored_children_failures/1` (HGETALL of `job_failed` :298) — the `:failed` HASH; `job.ex:300-302` `job_unsuccessful` (the `:unsuccessful` SET); the propagate-to-parent logic = `moveToFinished-15.lua` | MATCH — the v1 form roots keys in data values (NOT lifted); the v2 carries the parent-link in declared subkeys (the emq.3.1 pattern) |
| 10 | v1 grandchildren (the recursive tree) | `flow_producer.ex:51-56` (the nested `children:` in a child), `:238` `build_flow_commands` recurses (`:364-374` reduce over children). v1 builds the whole tree in one MULTI; v2 cannot (cross-slot + the non-atomic add). | MATCH — grandchildren = a child that is itself a flow-parent (its row carries BOTH `parent`/`parent_queue` AND its own `:dependencies`>0) |
| 11 | the N1 subkey-lifecycle carry (the failure subkeys join it) | `admin.ex:90` `@drain` `wipe()` DELs `jk, jk..':logs'`; `admin.ex:152` obliterate `del_job` DELs `jk, jk..':logs', jk..':lock'` — BOTH exclude `:dependencies`/`:processed`/`:failed`/`:unsuccessful`/`flow:outbox`. The emq.3.2-N1 + emq.3.3-B5 carry. | MATCH — emq.3.4's `:failed`/`:unsuccessful` JOIN the named carry (the §2 guardrail), deferred to the lifecycle rung |

THE AS-BUILT FLOOR emq.3.4 EXTENDS:
- The HAPPY-PATH fan-in (emq.3.1/3.2/3.3) is COMPLETE: a child that COMPLETES decrements the parent (@complete same-slot; @flow_deliver cross-slot via outbox+sweep). emq.3.4 closes the FAILURE half: a child that DIES (exhausts retries → `dead` via @retry:254-259) today NEVER signals the parent → the parent hangs in `awaiting_children` forever. fail_parent_on_failure fails the parent (the default — propagate the death); ignore_dependency_on_failure decrements the parent anyway (treat the failed child as satisfied) + records it in `:unsuccessful`.
- The §6 grammar ALREADY reserves `:failed`/`:unsuccessful` (design.md:307) — emq.3.4 populates pre-reserved subkeys (NO grammar edit, NO new type — unlike emq.3.3's outbox). INV1 holds by construction.
- The failure propagation crosses the SAME slot boundary the cross-queue completion does: a cross-queue child's DEATH must reach the parent's slot. The outbox+sweep mechanism (emq.3.3 D-1..D-4) is the precedent — a cross-queue failure rides the SAME outbox (a fail-entry beside the complete-entry), delivered by the SAME sweep to a fail-deliver script on the parent's slot.
- add_bulk = the v1 add_bulk/2 parity (flow_producer.ex:183): N flows in one call, pipelined; each flow lands by the existing add/3 mechanism (same-queue atomic OR cross-queue host-orchestrated parent-first).

THE SCOPE-DECISION FORK (the Director's task assigns me the cleanest MINIMAL scope; the family carve + emq-3-3 D-5a DISAGREE on grandchildren — surfaced as V-1, NOT ruled):
- emq.3.md:198 (the family carve table) names emq.3.4 = "the failure-policy options + bulk — fail_parent_on_failure / ignore_dependency_on_failure (the :failed/:unsuccessful subkeys) + EchoMQ.Flows.add_bulk/3. Conformance: flow_fail_parent, flow_ignore_dep, flow_add_bulk." NO grandchildren.
- emq-3-3 D-5a + B6 add grandchildren/deep recursion to emq.3.4 ("Grandchildren / deep recursion AND failure-policy ... are emq.3.4").
- The two canon sources fork on whether grandchildren is in emq.3.4. This is a SCOPE/sequencing call (the Operator/Director rules; Venus surfaces). RECOMMENDATION (recorded in V-1): failure-policy + add_bulk is ONE coherent HIGH-risk rung (it edits the shipped @retry dead-letter arm — the regression surface — exactly the emq.3.1/3.3 @complete-edit precedent); grandchildren is a SEPARATE concern (a recursive-tree add + multi-level fan-in, no shipped-script edit beyond what failure-policy already does) and is cleanest as emq.3.5 OR an explicit emq.3.4 stretch. The triad is authored to failure-policy + add_bulk (the family-carve scope), grandchildren marked the honest Out routed to the fork.

VERDICT: BUILD-THE-DESIGN. Every anchor MATCH (3 re-pins, all benign line-number drift from the emq.3.3 ship; none blocking). The failure path (@retry dead-letter, jobs.ex:254-259) is the genuine gap; the §6 subkeys are pre-reserved; the conformance floor is 47 (live). NO STALE/INVENTED/MISSING claim blocks. The one genuine open is the scope fork (grandchildren in-or-out), surfaced as V-1 to the Director, NOT ruled. The triad-authoring may proceed.

### T-3 — the roadmap/ fold derivation (Task B): the 1:1 coverage mapping + the placement decision + the status reconcile.

PLACEMENT (D-1 leaves it Venus's call). emq.features.md is the CANONICAL CATALOG ("CATALOGS — every shipped, building, planned, and proposed EchoMQ capability in one place"). The 5-section forward-feature descriptions land there as a NEW **Part C — the forward-feature catalog (the 5-section format)**, categorized by feature name/area (Part A = the by-Movement feature set, Part B = the v1→v2 parity proof, Part C = the forward-feature 5-section detail). The roadmap (emq.roadmap.md) keeps the PLAN (the ladder, the movements, the seams); emq.features.md gains the per-feature Goal/Rationale/5W/Scope/AC detail. Rationale: the catalog already maps features→rungs; the 5-section detail is the natural deepening of the catalog, not a second plan in the roadmap.

THE 1:1 COVERAGE MAPPING (every stale-file feature → its canonical home in emq.features.md Part C, by category):

CATEGORY: FLOWS (NEW — currently NO roadmap/flows.md; the active frontier MISSING from the catalog's 5-section detail):
- flows / parent-child (single-queue) → Part C §Flows · "parent/child fan-in (single-queue)" — emq.3.1 SHIPPED
- flows / child-result reads → Part C §Flows · "child-result reads" — emq.3.2 SHIPPED
- flows / cross-queue → Part C §Flows · "cross-queue flow" — emq.3.3 SHIPPED (this run)
- flows / failure-policy + bulk → Part C §Flows · "failure-policy + bulk add" — emq.3.4 SPECCED (the new triad)
- flows / grandchildren → Part C §Flows · "grandchildren / deep recursion" — PLANNED (the V-1 fork; emq.3.5 recommended)

CATEGORY: GROUPS (from roadmap/groups.md → Part C §Groups, all emq.4 except the FLAME outlier):
- Rate limiting → §Groups · "per-group rate limiting" (emq.4)
- Local group rate limit → §Groups · "per-group rate-limit override" (emq.4)
- Concurrency → §Groups · "group concurrency (default + per-group)" (emq.4; per-group ceiling SHIPPED M0)
- Local group concurrency → §Groups · "per-group concurrency override" (emq.4; SHIPPED set, recovery deepens)
- Max group size → §Groups · "max group size (back-pressure)" (emq.4)
- Pausing groups → §Groups · "per-group pause/resume" (SHIPPED M0; recovery deepens emq.4)
- Prioritized intra-groups → §Groups · "intra-group priority" (emq.4)
- Execution backends for groups → §Groups · "pluggable execution backends (FLAME)" (HELD at the program seam §Seams item 8; NOT emq.4 — the documented exception)

CATEGORY: BATCHES (from roadmap/batches.md → Part C §Batches, all emq.5):
- Processing jobs in batches → §Batches · "batch consume" (emq.5)
- Batch Options min_size/timeout → §Batches · "batch shaping (min_size/timeout)" (emq.5)
- Group Affinity → §Batches · "group affinity (batches × groups)" (emq.5; gates emq.4)
- Batch concurrency → §Batches · "batch concurrency (one in-flight batch per group)" (emq.5)
- Failing jobs → §Batches · "batch partial-failure isolation" (emq.5)
- Handling events → §Batches · "batch lifecycle events" (emq.5; rides EchoMQ.Events)
- Dynamic Rate → §Batches · "dynamic (runtime-adjustable) rate" (emq.5; builds emq.4 rate floor)
- Manually Processing Jobs → §Batches · "manual/pull batch fetch" (emq.5)
- Dynamically Delayed Jobs → §Batches · "dynamic delay (re-delay from handler)" (emq.5; extends the schedule fence)

CATEGORY: OBSERVABLES / LIFECYCLE (from roadmap/observables.md → Part C §Lifecycle controls, emq.6):
- Cancellation → §Lifecycle · "distributed cancel + TTL auto-cancel" (emq.6; LOCAL token SHIPPED emq.2.3)
- Resumable / checkpointed state → §Lifecycle · "checkpoints (resumable state)" (emq.6)

CATEGORY: LOCKS (from roadmap/a1-extend-locks.md → Part C §Locks — STATUS RECONCILE, see below):
- The @extend_locks A-1-wording fork → §Locks · "the @extend_locks A-1 slot-rooted-ARGV convention" — **RESOLVED** (NOT an open fork anymore — see the status reconcile)

CATEGORY: THE PROOF STACK (already in Part A Movement II as emq.8; Part C §Proof stack gives it the 5-section detail) — conformance · telemetry contract · the engine matrix · the benchmark gate (emq.8).

STATUS RECONCILES surfaced by the fold (for the Director):
1. **a1-extend-locks.md is STALE / RESOLVED.** The note surfaced the @extend_locks A-1-wording as an OPEN Operator fork (Arm 1 ratify / Arm 2 mandate-KEYS). But emq.design.md:102-113 ALREADY carries "The slot-rooted-ARGV clarification (Operator-ratified 2026-06-14 — emq-3 @extend_locks A-1-wording, Apollo Y-3 §4)" = Arm 1 RATIFIED. So the fork in a1-extend-locks.md is CLOSED — the canonical Part C §Locks records it as the RATIFIED convention (S-6 clarification, design §1), not an open fork. (The note's "(1) slot rung emq.4" recommendation stands as where the emq.8 lint enforces it.) → the Director's coverage-verify should note: a1-extend-locks.md's FORK is resolved in the design canon; the fold records the resolution, not the open question.
2. **emq.3.1/3.2 SHIPPED, emq.3.3 shipped-this-run, emq.3.4 SPECCED** — the flow family statuses. The roadmap (emq.roadmap.md:137-141) shows 3.3 as "NEXT — the design-authoring cycle" and 3.4 as "planned"; the catalog (emq.features.md:172-196) shows 3.3/3.4 as 📋. Once the Director ships emq.3.3 (Task #4), these flip: 3.3 → ✅ SHIPPED, 3.4 → 📐 SPECCED (the new triad). The fold's Part C §Flows is authored to the POST-3.3-ship state (3.3 SHIPPED, 3.4 SPECCED) so the Director's single ship-commit lands the catalog already-correct.
3. **The progress dashboard (emq.progress.md)** Frontier table item 3 shows emq.3.3 as "NEXT — the design-authoring cycle"; post-ship it flips to SHIPPED + item 3 becomes emq.3.4 (the new frontier). The Director folds this at the emq.3.3 ship (Task #4).

PRESERVATION: the stale files' as-built-floor honesty (the "read before reconciling" blockquotes; voice-tracks-status: SHIPPED present tense, SPECCED "emq.N builds…", PLANNED "the roadmap plans…") is CARRIED into each Part C category's Scope/Rationale — e.g. §Groups names the SHIPPED EchoMQ.Lanes floor ("emq.4 DEEPENS Lanes, it does not build a queue"), §Batches names the SHIPPED enqueue_many producer half + the forward consume half, §Lifecycle names the SHIPPED local CancellationToken + the forward distributed form.

The Director git-rm's roadmap/ ONLY after verifying this 1:1 mapping (no feature dropped). DO NOT delete the dir myself.

## {emq-3-4-decisions} Decisions

### D-1 — the forward-feature documentation contract (the "new established format" + the stories/ and roadmap/ rulings)

THE FORMAT (Operator directive). Every forward feature is described in the canonical roadmap docs in this 5-section format, categorized by feature name/area:
- **Goal** — the capability in one statement (what it delivers).
- **Rationale** — why: the forcing function, the gap it closes, the consumer value.
- **5W** — Who / What / When (rung + sequencing) / Where (module · key · slot surface) / Why.
- **Scope (In / Out)** — what the feature includes; what is explicitly deferred (and to which rung).
- **Acceptance Criteria** — runnable checks. For a DELIVERED feature, AC reference the generated stories/ catalog; for an UNBUILT feature, AC are the forward spec the rung turns into code.

stories/ — KEEP AS-IS (Operator: "keep as-is for code-specs generation"). docs/echo_mq/stories/*.stories.md are GENERATED from passing BDD tests by `mix echo_mq.stories` — the catalog that cannot drift from code. NOT hand-edited, NOT hand-authored for unbuilt features. Forward acceptance criteria live ONLY in the roadmap feature descriptions (the AC section). As features ship, their stories regenerate from the new test/stories/*_story_test.exs.

roadmap/ — REMOVE (stale), AFTER folding. docs/echo_mq/roadmap/{groups,batches,observables,a1-extend-locks}.md fork the canonical catalog. Venus folds their feature coverage UP into emq.features.md (the catalog) / emq.roadmap.md (the plan) in the format above, ensuring EVERY feature is represented (1:1 coverage, reported back as an explicit mapping); the Director then `git rm`s the dir ONLY after verifying coverage — no feature dropped. The active frontier (the FLOW family) currently has NO roadmap/ file → it gets first-class coverage incl. emq.3.4 (dead-child/failure-policy + grandchildren) and the shipped 3.1/3.2/3.3.

CATEGORIES (by name): flows (parent/child · cross-queue · dead-child/failure-policy · grandchildren), groups (Lanes deepened), batches, observables/lifecycle (TTL · distributed-cancel · checkpoints), locks (the @extend_locks A-1 fork — Operator ruling pending), the proof stack (conformance · telemetry · benchmark). Each is a named category in the canonical catalog.

GROUNDING LAW (binding on Venus): NO-INVENT — every feature reference is a real module/key/script (echo-mq-surface.md) or a design § or the v1 reference (apps/echomq); voice tracks status (SHIPPED present tense · SPECCED "emq.N builds…" · PLANNED "the roadmap plans…"); the v2 master invariant frames every entry. The stale roadmap/ files already model this voice — preserve their as-built-floor honesty when folding.

### D-2 — RULING on V-1 (the emq.3.4 scope fork): ARM A. emq.3.4 = failure-policy (fail_parent_on_failure / ignore_dependency_on_failure — the :failed/:unsuccessful subkeys) + add_bulk. Grandchildren / deep recursion is the honest OUT, routed to a SEPARATE rung (emq.3.5).

RATIONALE: (1) Arm A is the CONSIDERED family-carve decomposition (emq.3.md:198 — the authoritative family contract); the frozen emq-3-3 D-5a lumping of grandchildren WITH failure-policy was a 3.3-scoping aside (everything-not-3.3), NOT a 3.4 decomposition. (2) failure-policy is ONE coherent capability with ONE shipped-script edit (the @retry dead-letter arm jobs.ex:254-259 — the regression surface; the emq.3.1/3.3 @complete-edit precedent → HIGH-risk, Apollo MANDATORY) — the smallest rung that closes the parent-hangs-on-child-death gap (Venus's T-2 crux: a dead child never reaches the parent today → the parent hangs in awaiting_children forever). (3) grandchildren is a DISTINCT mechanism (a recursive-tree add + multi-level cross-slot fan-in) that COMPOSES the cross-queue completion + the failure propagation — it lands cleanest AFTER both are flat-proven (the thin-slice law; the emq-3-1 L-1 anti-bundling lesson). (4) cheap re-scope: the failure-policy core is identical either way; Arm B only ADDS the recursive-tree deliverables, so this ruling can be revisited pre-build at zero cost.

SURFACED to the Operator: this is a Director SEQUENCING ruling (Venus explicitly delegated it). The Operator may override to Arm B (bundle grandchildren into 3.4) at zero pre-build cost. Venus authors the emq.3.4 triad to Arm A; grandchildren marked Out → emq.3.5 (recorded, not built).

### D-3 — RATIFY emq.3.3 (cross-queue flow) for the LAW-4 ship: Apollo BUILD-GRADE (Y-1). The MANDATORY high-risk INDEPENDENT verdict — the one the halted emq-3-3 run never produced — is now RECORDED + substantive:

- 16/16 deliverable+invariant promises MATCH (the post-build delta table, file:line).
- Gate ladder re-run INDEPENDENTLY on Valkey 6390: erlang 28.5.0.1; compile --warnings-as-errors CLEAN; 303 tests/0 fail; Conformance {:ok,47} (prior 46 byte-identical); ≥100 loop 100/100; durable harness 9/9.
- Declared-keys CLEAN on all 4 new scripts (@complete-xq {C}, @flow_deliver {P}, @hold_parent {P}, @enqueue_flow_child {C}) — the F-1 cross-slot trap that is INVISIBLE on single-node 6390.
- Mutation kill-rate 2/2 (100%), each with SCRIPT FLUSH before re-test (the T-6 EVALSHA-first lesson) + reverted net-zero via INVERSE EDIT (the L-3 footgun honored).
- The :processed HSETNX idempotent re-deliver GENUINELY exercised (the L-2 vacuous-fixture fix verified at all 5 hand-fabricated sites); the attack-that-held (poison-prefix no-drop) HELD; residual #4 (@hold_parent/@enqueue_flow_child realization of D-5b) RATIFIED.

TWO un-prompted findings, NEITHER blocking: L-1 (@flow_deliver's at-zero release is unconditional on parent existence → a phantom-resurrection IF a FUTURE obliterate fails to drain the outbox — correct-for-scope within 3.3 since admin.ex is byte-untouched and all lifecycle is deferred D-5c/INV9; it SHARPENS the lifecycle rung's drain into a CORRECTNESS obligation, not mere leak-hygiene) + the poison-prefix no-drop edge (correct-by-design, un-tested → an optional regression test). Both are forward notes for emq.3.4+/the lifecycle rung; neither moves the grade.

DIRECTOR SHIP-GATE (my own pass, not a glance): read Y-1 in full; verified the working tree NET-ZERO (diffstat 527+/97- across the 7 tracked files EXACTLY as built — no stray mutation; grep for mutation markers clean); the boundary clean (admin.ex/keyspace.ex/echo_wire byte-untouched per INV9; apps/echomq untouched). → SHIP.

THE LAW-4 COMMIT (concern 1 of this multi-deliverable scope) = the emq.3.3 MEASURED SURFACE ONLY: the code+tests (echo/apps/echo_mq/{conformance,flows,jobs,pump}.ex + the 3 modified pin/flow_add tests + the new flow_cross_queue_test.exs), the triad (emq.3.3.{md,llms.md,stories.md,prompt.md}), the FROZEN emq-3-3 build-ledger (progress/emq-3-3.{progress.md,registry.json}), and the durable harness (echo/rungs/bus/emq_3_3_check.{sh,out}). The status-flip in emq.roadmap.md/progress.md/features.md + the BUILD-GRADE Y-1 verdict itself ride the SEPARATE reconcile commit (concern 2), after Venus's fold completes — keeping the 3.3 ship a faithful record of exactly the rung. NO Z-n yet: the scope's single Z-n closes the WHOLE scope (3.3 shipped + 3.4 designed + reconcile done) at the end.

## {emq-3-4-progress} Progress

### P-1 — Apollo (ccl-emq-3-4-2) registered + context loaded for the MANDATORY high-risk verdict on the as-built emq.3.3 (cross-queue flow). The prior emq-3-3 run's Apollo HALTED (~1h47m, idle, no verdict recorded) — this is the persistence-hardened re-spawn; recording INCREMENTALLY. Loaded: apollo.md charter + echo-mq-evaluator skill + program-law + as-built surface map; the emq.3.3 triad (contract + stories + Mars brief); the FROZEN emq-3-3 ledger (T-1..T-6, D-1..D-5, V-1..V-4, Y-1/Y-2, L-1/L-2, P-1/P-2). The four RESIDUALS the real Mars (ccl-emq-3-3-3) handed Apollo (Y-2): (1) re-run the ladder + ≥100 INDEPENDENTLY; (2) re-walk @complete byte-diff (only-added-lines) + declared-keys/slot grep over all 4 new scripts (@complete cross-queue branch, @flow_deliver, @hold_parent, @enqueue_flow_child); (3) confirm the L-2 fidelity fix is COMPLETE — NO other hand-fabricated outbox entry uses the child-queue field (the survivor-entry slot defect that made the keystone D-3 idempotency test pass-for-the-wrong-reason); (4) ratify-or-flag the realization-over-literal (@hold_parent + @enqueue_flow_child as TWO new single-slot scripts for the cross-queue add, since @enqueue_flow is byte-frozen) vs D-5b. NOTE the T-6 SCRIPT FLUSH lesson: connector is EVALSHA-first, so `redis-cli -p 6390 SCRIPT FLUSH` before re-testing EACH Lua mutation, or a stale server-cached SHA masks the change.

### P-2 — RESIDUAL #3 (the L-2 fidelity fix) DISCHARGED via read + grep. The cross-queue survivor-entry slot defect (child-queue in field 1 → wrong slot → HSETNX always 1 → guard never fired) is COMPLETELY fixed. Grepped ALL hand-fabricated outbox entries (RPUSH into flow:outbox + every `<<0>>` NUL-joined construction) across test/flow_cross_queue_test.exs + lib/echo_mq/conformance.ex — 5 sites total: conformance.ex:1189 uses `q` (parent queue); test:346, test:373, test:548 all use `pq`; the canonical-shape assertion test:207 pins `[pq, parent, child, "done"]`. VERIFIED `pq` binding at setup (flow_cross_queue_test.exs:48-49: `pq = base <> ".P"` the PARENT queue, `cq = base <> ".C"` the child queue) and Flows.add is always called `add(conn, pq, ...)` with children carrying `queue: cq` — so the survivor's first field IS the parent queue at every site (byte-faithful to @complete's emit). NO other fixture uses the child-queue field. ALSO independently confirmed by reading the source (not the report): (a) the byte-freeze keystone — @complete cross-queue branch (jobs.ex:204-211) gated on ARGV[6]=='xq', RPUSH at 206 gated on was_active==1, early-return at 210, so the frozen single-queue `if KEYS[3] and was_active==1` at 212 is unreachable for a cross-queue child; (b) the realization-over-literal — flows.ex @hold_parent (73-80, all {P}) + @enqueue_flow_child (90-101, all {C}), each single-slot + EMQKIND-first, with add_cross_queue (285-301) parent-first via @hold_parent then land_children reduce_while fail-closed (310-323); (c) the deliver-before-remove drain — pump.ex deliver_flow_completions LRANGE non-destructive (165) → reduce_while contiguous-prefix (177-179) → LTRIM AFTER (182), @flow_deliver HSETNX-guarded DECR (42-51); (d) parent_of/3 (jobs.ex:459-471) reads parent+parent_queue in one HMGET, dispatches cross_queue iff parent_queue != queue. R2 hardening (3-distinct-queue, grandchildren-OUT leaf, crash-before-LTRIM) all genuinely adversarial. NEXT: re-run the gate ladder independently (residual #1) + the @complete byte-diff vs HEAD (residual #2).

### P-3 — RESIDUAL #2 (byte-diff + declared-keys) DISCHARGED via git diff vs HEAD on the live tree (all green). (INV3 byte-freeze) The @complete Lua body diffs as `@@ -179,4 +202,12 @@` — a PURE INSERTION of 8 lines (the `if ARGV[6] == 'xq'` branch) before the byte-frozen `if KEYS[3] and was_active == 1` single-queue fan-in branch; ZERO existing Lua lines removed/changed. The OTHER jobs.ex changes are HOST-side Elixir only (complete/5 gains the {:cross_queue,...} clause + renames the existing tuple to {:same_queue,...}; parent_of/3 switches HGET parent → HMGET parent parent_queue with a cross-queue dispatch) — no other Lua touched. (INV1) The diff adds EXACTLY 3 new Script.new attrs (@hold_parent flows.ex:73, @enqueue_flow_child flows.ex:90, @flow_deliver pump.ex:42) and removes ZERO; all 13 other shipped scripts (@enqueue/@schedule/@claim/@retry/@promote/@reap/@update_data/@update_progress/@add_log/@remove_job/@reprocess/@extend_lock/@extend_locks) untouched. (INV2 declared-keys / the F-1 cross-slot trap) verified all 4 scripts: @complete xq branch keys all {C} (KEYS[3]=outbox, KEYS[2]=child row, p=ARGV[3]=child-queue base for metrics); @flow_deliver keys all {P} (KEYS[1]=:dependencies, KEYS[2]=:processed, KEYS[3]=parent row, ARGV[4]=parent-queue base); @hold_parent all {P} (KEYS[1]=parent row, KEYS[2]=:dependencies); @enqueue_flow_child all {C} (KEYS[1]=child row, KEYS[2]=child pending). NO script mixes slots; every key is a KEYS[n] or ARGV-base..literal (the @extend_locks A-1 form); no key read out of a Lua data value (v1 parent_key NOT lifted). (INV8 additive-minor) conformance.ex scenarios/0 +flow_cross_queue appended; count re-pinned 46→47 in BOTH pin tests; flow_add CONTRACT STRING byte-identical (only its now-FALSIFIED internal {:error,:cross_queue} sub-assertion pruned, the capability moved to flow_cross_queue) + flow_add_test.exs rewritten reject→admit (a genuine emq.3.3 behavioral change). (INV9) git diff --stat confirms admin.ex + keyspace.ex + echo_wire/ byte-UNTOUCHED. Boundary clean: only conformance/flows/jobs/pump + the 2 pin tests + flow_add_test modified; flow_cross_queue_test.exs + emq_3_3_check.sh/.out new. NEXT: re-run the gate ladder (compile + suite + Conformance + ≥100 loop) independently (residual #1).

### P-4 — Gate ladder (residual #1) reproduced INDEPENDENTLY, all green so far: (a) toolchain inside echo/ = erlang 28.5.0.1 + Valkey 6390 PONG; (b) `TMPDIR=/tmp mix compile --warnings-as-errors` (echo_mq) CLEAN; (c) `mix test test/flow_cross_queue_test.exs --include valkey` = 20/20; (d) full `mix test --include valkey` (echo_mq) = 4 doctests, 303 tests, 0 failures + CONFORMANCE 47/47 with flow_cross_queue listed (the single `[error] GenServer ... killed` mid-suite is the BENIGN emq.2.3 watch-plane reconnect-kill drill, not a failure — Mars Y-2 documented it); (e) one-shot `Conformance.run/2` = {:ok, 47} in isolation; (f) prior-46 contract strings byte-IDENTICAL (the ONLY scenarios/0 change is flow_children_values re-added with a trailing comma — text-identical — + flow_cross_queue appended). The ≥100 determinism loop is RUNNING (scoped to flow_cross_queue_test.exs, the rung's own cross-queue mint surface, owning the machine — the build+harden already ran ≥2 green full 100/100 per Mars Y-2 + the harness LOOP_N=100, so a scoped loop isolates the rung's same-ms mint faster + free of the watch-plane full-suite flake, per the skill's bound).

RESIDUAL #4 (ratify-or-flag the @hold_parent + @enqueue_flow_child realization vs D-5b) → RATIFIED. D-5b mandated "parent-first, fail-closed; land the parent FIRST then each child on its own slot" + left the MECHANISM open (emq.3.3.md D2: "a parent-only enqueue when all children are cross-queue"). Since @enqueue_flow is byte-frozen and no single script spans the children's slots, Mars's TWO new single-slot declared-keys scripts are the natural realization: @hold_parent (all {P}, EMQKIND-first, holds the parent awaiting_children + :dependencies=N) then land_children via @enqueue_flow_child (all {C}, EMQKIND-first, ZADD child pending). add_cross_queue (flows.ex:285-301) lands the parent first then reduce_while-short-circuits on the first child failure (parent stays HELD, host-retryable). VERIFIED faithful incl. the MIXED-flow generalization (flow_cross_queue_test.exs:122-146): a same-queue child in a mixed flow gets pq_arg="" → @enqueue_flow_child writes NO parent_queue field → fans in via the byte-frozen @complete branch (atomic, same slot); a cross-queue child gets pq_arg=parent-queue → emits to outbox → swept. Both decrement the SAME :dependencies on {P}, each exactly once (same-queue via was_active==1; cross-queue via :processed HSETNX) → no double-count. Fail-closed edges tested: ill-formed child id raises BEFORE any wire (parent unwritten, 148-163); non-JOB child refuses EMQKIND leaving the parent HELD (165-185).

### P-5 — ADVERSARIAL VERIFICATION complete: ≥100 loop 100/100, mutation kill-rate 2/2, attack-that-held PASS. (1) ≥100 DETERMINISM LOOP: scoped to flow_cross_queue_test.exs (the rung's own cross-queue parent+N-children mint surface), owning the machine = 100/100 PASS, 0 FAIL — no same-ms mint collision (combined with Mars Y-2's full-suite 100/100 + the harness LOOP_N=100, the gate is satisfied). (2) MUTATION KILL-RATE 2/2 (100%), each with SCRIPT FLUSH before re-test (the T-6 EVALSHA-first lesson) + reverted net-zero via INVERSE EDIT (never git checkout — the L-3 footgun): Mutation A — @flow_deliver :processed HSETNX guard `== 1`→`>= 0` (defeats idempotency, DECR always fires) → CAUGHT, 3 tests fail (flow_cross_queue_test.exs:351 deps 1≠0, :376 deps 0≠-1, :552 deps 2≠1 — the exact L-2 keystone, now genuinely guard-exercising); Mutation B — @complete cross-queue dispatch `ARGV[6] == 'xq'`→`'XQ'` (the host's lowercase 'xq' never matches → cross-queue child falls through to the byte-frozen `if KEYS[3] and was_active==1` → DECRs the outbox LIST + HSET nil KEYS[4]) → CAUGHT, 12 tests fail (decisive: flow_cross_queue_test.exs:197 complete returns `{:error,{:server,"ERR Command arguments must be strings or integers"}}` — the exact T-4 KEYS[3]-collision the early-return guards). git diff confirms BOTH mutants absent post-revert (suite 20/20 green again); working tree = the original emq.3.3 diff only (conformance/flows/jobs/pump + 3 test files; flow_cross_queue_test.exs + harness untracked). (3) ATTACK-THAT-HELD (the poison-prefix no-drop, an UN-PROMPTED edge): LPUSH'd a malformed entry (1 NUL → split_entry :malformed → deliver_one counts 0) at the outbox HEAD ahead of a valid entry; deliver_flow_completions delivered=0 (poison HALTS the contiguous prefix), LLEN stayed 2 (NOTHING dropped — `if delivered > 0` guards the LTRIM), parent deps stayed 1 (held, decrement behind the poison); after operator repair (LPOP the poison) the valid entry delivered=1 → deps 0 (released). The design WITHSTANDS it: a poison entry head-of-line-blocks its OWN queue's cross-queue deliveries until removed, but NEVER drops a valid entry (the deliver-before-remove no-drop holds). This edge is correct-by-design but UN-TESTED in the suite (a poison-prefix regression test is an optional add — a mentoring note, not a blocker). Probe scripts cleaned; NO Valkey key pollution. ALL FOUR RESIDUALS discharged. NEXT: re-run the durable harness emq_3_3_check.sh, then the verdict Y-1.



## {emq-3-4-alternatives} Alternatives

### V-1 — the emq.3.4 SCOPE fork: grandchildren / deep recursion IN emq.3.4, or a separate later rung? (Venus surfaces; the Director/Operator rules.)

THE QUESTION. The Director's Task A asks for the cleanest MINIMAL scope for ONE rung, and two canon sources fork on whether grandchildren (a cross-queue child that is itself a flow-parent — the v1 recursive tree, flow_producer.ex:51-56/:238) belongs in emq.3.4:
- **emq.3.md:198 (the family carve table)** — emq.3.4 = "the failure-policy options + bulk" ONLY: `fail_parent_on_failure` / `ignore_dependency_on_failure` (the `:failed`/`:unsuccessful` subkeys) + `EchoMQ.Flows.add_bulk/3`; conformance `flow_fail_parent`, `flow_ignore_dep`, `flow_add_bulk`. NO grandchildren.
- **emq-3-3 D-5a + B6 (the frozen predecessor ledger)** — "Grandchildren / deep recursion AND failure-policy ... are emq.3.4, OUT [of 3.3]." Lists BOTH as 3.4.

The frozen emq-3-3 ledger lumped grandchildren WITH failure-policy as "the 3.4 remainder" when ruling them OUT of 3.3's FLAT scope — but that was a 3.3-scoping statement (everything-not-3.3), NOT a considered 3.4-decomposition. The family carve table (the authoritative family contract) is the considered decomposition, and it scopes emq.3.4 to failure-policy + bulk.

ARM A — emq.3.4 = failure-policy + add_bulk ONLY; grandchildren is a SEPARATE later rung (emq.3.5). [RECOMMENDED]
Shape: emq.3.4 builds the two failure-policy options + bulk add (the family-carve scope). Grandchildren (the recursive cross-queue tree) is its own rung after the failure semantics are proven on the flat shape.
Steelman: (1) failure-policy is ONE coherent capability that edits ONE shipped script (the `@retry` dead-letter arm, jobs.ex:254-259 — the regression surface, exactly the emq.3.1/3.3 @complete-edit precedent → HIGH-risk, Apollo MANDATORY); it is the smallest rung that closes the parent-hangs-on-child-death gap. (2) Grandchildren is a DIFFERENT mechanism — a recursive-tree ADD (a child row carrying BOTH `parent`/`parent_queue` AND its own `:dependencies`>0) + MULTI-LEVEL fan-in (a grandchild's completion releases the child, whose completion then signals the parent — two hops, each crossing a slot for a cross-queue tree). It composes the cross-queue completion AND the failure propagation, so it lands cleanest AFTER both are proven flat. (3) The family carve table — the considered decomposition — already scopes it this way. (4) Thin-but-robust: one rung, one coherent capability, one shipped-script edit.
Cost: a third flow sub-rung (emq.3.5) is added to the ladder before Movement I closes (the family becomes 3.1/3.2/3.3/3.4/3.5). But the ladder is one-increment-one-run regardless; this is sequencing, not extra work.

ARM B — emq.3.4 = failure-policy + add_bulk + grandchildren (one big rung). [NOT recommended]
Shape: emq.3.4 builds all three (the emq-3-3 D-5a lumping, taken literally as a 3.4 decomposition).
Steelman: Movement I's flow family closes in one fewer rung (3.1/3.2/3.3/3.4); the emq-3-3 ledger named all three as 3.4.
Cost: (1) it is TWO distinct mechanisms (failure propagation + recursive multi-level fan-in) in one rung — the opposite of the thin-vertical-slice discipline the program holds; (2) grandchildren multiplies the cross-slot + idempotency surface (a multi-level cross-queue tree has TWO hops, each an outbox+sweep deliver, each idempotency-guarded) ON TOP of the failure-policy shipped-script edit — a HIGH-risk rung made higher-risk by bundling; (3) it contradicts the considered family-carve decomposition for the convenience of a 3.3-era scoping aside. The emq-3-1 L-1 lesson (and the whole program's thin-slice law) argues against bundling two hard mechanisms.

RECOMMENDATION: ARM A — emq.3.4 = failure-policy (`fail_parent_on_failure` / `ignore_dependency_on_failure`) + `add_bulk`, the family-carve scope. Grandchildren / deep recursion is the honest **Out**, routed to a SEPARATE rung (emq.3.5, recommended) — recorded NOT built. The decisive property: failure-policy is one coherent capability with one shipped-script edit (the `@retry` dead-letter arm), the smallest rung that closes the parent-hangs-on-child-death gap; grandchildren is a distinct multi-level-fan-in mechanism that composes the cross-queue completion + the failure propagation and lands cleanest after both are flat-proven. The triad is authored to Arm A (failure-policy + add_bulk); grandchildren marked Out → the fork. **The Director rules whether grandchildren joins emq.3.4 (Arm B) or becomes emq.3.5 (Arm A) — a cheap pre-build re-scope either way (the failure-policy core is identical; Arm B only ADDS the recursive-tree deliverables).** NOT Venus's to decide — a scope/sequencing call for the Operator/Director.

## {emq-3-4-learnings} Learnings

### L-1 — UN-PROMPTED FINDING (Apollo, adversarial read): the @flow_deliver at-zero release is UNCONDITIONAL on the parent's existence — a deliver against an OBLITERATED/absent parent RESURRECTS a phantom. Trace: if a parent's :dependencies/:processed/row were deleted (a future lifecycle/obliterate op) while a cross-queue child's outbox entry is still undelivered, the sweep's @flow_deliver (pump.ex:42-51) runs: HSETNX(:processed, child, result) on the now-absent key → creates it fresh → returns 1 → DECR(:dependencies) on the absent key → Redis initializes to 0 then decrements to -1 → `left = -1 <= 0` TRUE → ZADD parent..pending 0 parent + HSET parent_row state pending. Net: a PHANTOM pending member + a payload-less parent row + a :dependencies=-1 + a :processed[child] are RESURRECTED on {P}. CLASSIFICATION: a correct-for-scope NAMED-INDIRECTLY bound, NOT a defect that blocks emq.3.3. Reasoning: within emq.3.3's OWN scope nothing deletes the parent (parent-first add guarantees it exists before any child can complete+deliver; admin.ex is byte-UNTOUCHED, no obliterate/drain flow-subkey cleanup built — D-5c/INV9 defer ALL of it to the emq.3.x lifecycle rung). The resurrection is reachable ONLY by composing emq.3.3 with a FUTURE obliterate that does NOT also drain emq:{C}:flow:outbox — which is EXACTLY what INV9 already routes to the lifecycle rung (it must enumerate the outbox in BOTH del_job + @drain wipe(), joining the :dependencies/:processed carry). So emq.3.3 is self-consistent; the finding SHARPENS the lifecycle rung's obligation: draining the outbox is not merely a leak-hygiene concern (B5's framing) but a CORRECTNESS one — an undrained outbox entry surviving an obliterate resurrects a phantom parent on the next sweep. RECOMMENDATION (forward-looking, to the Director for the emq.3.4/lifecycle triad, NOT a blocker): the lifecycle rung's obliterate must drain the child-queue outbox of entries targeting the obliterated parent (or @flow_deliver must guard the at-zero release on `redis.call('EXISTS', KEYS[3]) == 1` — the parent row must exist to be released). This is an emq.3.4+ consideration; it does NOT move the emq.3.3 grade. Cost if ignored: a far-future lifecycle rung that drains the outbox per B5/INV9 but misses the deliver-ordering would leave a narrow resurrection window.

### L-2 — SPEC-SYNC applied + MENTORING routed (PROPOSE-ONLY for the durable assets, per the Director-ratified rule). SPEC-SYNC (the evaluator's post-build duty — shipped code is the fact): the emq.3.3.md body's PRE-build line anchors drifted by the cross-queue branch insertion (@complete attr :152→:175; single-queue fan-in branch :181-188→:212-219; complete/5 :365→:412; parent_of/3 :397→:459; add/3 :83→:152; the new @hold_parent :73 + @enqueue_flow_child :90 + add_cross_queue :285; @flow_deliver pump.ex:42 + deliver_flow_completions :161 + deliver_one :202 + sweep :126; flow_cross_queue conformance.ex:1140). Applied ONE surgical [POST-BUILD RECONCILE] note at the As-built-surface anchor block (the load-bearing re-pin the next rung reconciles against) recording the shipped numbering + pointing to Y-1 — NOT a heavy error-prone reflow of every inline ref in a frozen-rung body (the prose + contract are faithful; only line numbers moved — a pure sync, not a STALE-to-report). MENTORING — three findings, two channels: (1) DURABLE GUARDRAIL #1 (Mars build/test-fidelity, the L-2 vacuous-fixture class): a hand-fabricated wire fixture must byte-match the producer's emit, or it exercises a DIFFERENT path than it names — the frozen emq-3-3 L-2 (the survivor-entry child-queue-field defect that made the keystone D-3 test pass-for-the-wrong-reason). NOT yet folded into echo-mq-implementor SKILL (grep clean). It is the SAME CLASS as the skill's existing §4 line "A check counts only if it RUNS" → SHARPEN that line (never stack a second), PROPOSE-ONLY. (2) DURABLE GUARDRAIL #2 (Apollo evaluator-craft, the SCRIPT FLUSH / EVALSHA-first lesson): a Lua mutation spot-check must `redis-cli -p 6390 SCRIPT FLUSH` before re-testing, or a stale server-cached SHA masks the change (the T-5 trap the Director's own review rationalized; the T-6 correction). NOT in apollo.md NOR echo-mq-evaluator SKILL NOR program-law (grep = 0 — it lives only in the frozen ledger + the per-rung prompt). I relied on it twice today (both mutations). PROPOSE adding it to echo-mq-evaluator SKILL §2 (the adversarial-verify craft, which today describes no mutation-kill-rate procedure at all). (3) IN-LOOP / coverage note (Mars, minor): the poison-prefix no-drop edge (a malformed outbox entry head-of-line-blocks its queue without dropping valid entries behind it) is correct-by-design but UN-TESTED — an optional regression test, not a blocker. All three routed in the Y-1 report to the Director with exact diffs for ratification.



## {emq-3-4-report} Report

### Y-1 — VERDICT: **BUILD-GRADE** (the as-built emq.3.3 cross-queue flow satisfies every promise; HIGH-risk MANDATORY pass complete; the persistence-hardened re-spawn's verdict, recorded BEFORE going idle).

== POST-BUILD DELTA TABLE (promise → as-built file:line → verdict; every Deliverable + Invariant) ==
D1 fork gate (D-1..D-5 ruled before build) → frozen emq-3-3 ledger D-1..D-5 → MATCH
D2 cross-queue add (host-orchestrated, parent-first, fail-closed; reject replaced) → flows.ex:152 add/3, :285 add_cross_queue, :73 @hold_parent {P}, :90 @enqueue_flow_child {C}, :309 land_children reduce_while → MATCH
D3 outbox emit (additive @complete branch, single-queue byte-frozen) → jobs.ex:204-211 (gated ARGV[6]=='xq', RPUSH gated was_active==1, early-return :210); host wrapper complete/5 :412, parent_of/3 :459 → MATCH
D4 sweep-deliver (deliver_flow_completions + @flow_deliver idempotent) → pump.ex:42 @flow_deliver (HSETNX-guarded DECR), :161 deliver_flow_completions (LRANGE→reduce_while contiguous prefix→LTRIM AFTER), :202 deliver_one, :126 sweep/1 → MATCH
D5 lifecycle disposition NAMED (outbox routed to emq.3.x lifecycle rung; admin.ex untouched) → emq.3.3.md B5/INV9; admin.ex byte-untouched (git diff --stat empty) → MATCH
D6 proof (conformance 46→47, regression bound, ≥100 loop, Apollo MANDATORY) → conformance.ex:1140 flow_cross_queue; both pins 47; full suite 303 tests 0 failures; ≥100 100/100 → MATCH
INV1 wire law (no new class/transport; one additive shipped-script branch) → diff adds 3 Script.new (@hold_parent/@enqueue_flow_child/@flow_deliver), removes 0; @complete +8 lines only; no fence/EMQ class → MATCH
INV2 declared-keys A-1 (slot-soundness, the F-1 trap) → @complete-xq all {C}, @flow_deliver all {P}, @hold_parent {P}, @enqueue_flow_child {C}; every key a KEYS[n] or ARGV-base..literal; no Lua data-value key → MATCH
INV3 byte-unchanged except the additive @complete branch → @@ -179,4 +202,12 @@ pure insertion; single-queue fan-in jobs.ex:212-219 + non-flow path byte-identical; prior 46 conformance contract strings byte-identical → MATCH
INV4 branded identity at every boundary → flows.ex:159-160 add/3 gates every id at Keyspace.job_key/2 before the wire; tested flow_cross_queue_test.exs:148-163 (ill-formed child raises, parent unwritten) → MATCH
INV5 cross-queue fan-in eventually-consistent (the headline) → @flow_deliver releases on the sweep tick; tested conformance:1174-1180 (held pre-sweep, released post-sweep) + flow_cross_queue_test.exs:241+ → MATCH
INV6 idempotent delivery (at-least-once → effectively-once) → @flow_deliver DECR inside HSETNX==1; tested :319/:355/:512 (re-deliver no-op); MUTATION-CONFIRMED (breaking the guard fails 3 tests) → MATCH
INV7 emission atomic with completion (no drop window) → jobs.ex:204-207 RPUSH + ZREM one EVAL; tested flow_cross_queue_test.exs:189-214 (outbox LLEN 1 AND active 0 pre-sweep) → MATCH
INV8 additive-minor conformance → flow_cross_queue appended + probe-registered; count 46→47 both pins; prior 46 byte-identical (only flow_add's falsified sub-assertion pruned, contract string unchanged) → MATCH
INV9 new outbox subkey lifecycle NAMED → emq.3.3.md B5/INV9 names both sweeps + the owning rung; touch-set has zero DEL/HDEL/UNLINK of a flow subkey; admin.ex untouched → MATCH
INV10 slot soundness + FLAT family boundary → emit {C}, deliver {P}; FLAT only (grandchildren/failure-policy/add_bulk OUT); tested flow_cross_queue_test.exs:474-510 (leaf has no own deps/processed) → MATCH
RESULT: 16/16 promises MATCH. ZERO STALE/INVENTED/MISSING. No DEFERRED-without-marker. → BUILD-GRADE.

== GATE LADDER (re-run INDEPENDENTLY on Valkey 6390, not trusting the stale .out) ==
toolchain: erlang 28.5.0.1 (inside echo/) + Valkey 6390 PONG. compile --warnings-as-errors (echo_mq): CLEAN. mix test --include valkey (echo_mq): 4 doctests, 303 tests, 0 failures (the one [error] GenServer killed = the benign emq.2.3 watch-plane reconnect-kill drill, not a failure). Conformance.run/2 (one-shot, live 6390): {:ok, 47}; prior 46 contract strings byte-identical. ≥100 determinism loop (scoped to flow_cross_queue_test.exs, the rung's own cross-queue mint surface, owning the machine): 100/100 PASS 0 FAIL — no same-ms mint collision (the build+harden already ran ≥2 green full 100/100 per Mars Y-2 + the harness LOOP_N=100, so a scoped loop isolates the rung's mint faster + free of the watch-plane flake — the skill's bound). Durable harness emq_3_3_check.sh: PASS 9/9 reproduced (LOOP_N=20 — its 8 non-loop gates run once; my own scoped ≥100 already ran 100/100 — not a wasteful 3rd full ~7min loop).

== ADVERSARIAL CHECKS (the §11.2 charter, real work) ==
(a) PROMPTED battery — declared-keys grep over all 4 new scripts (the F-1 cross-slot trap, invisible on single-node 6390): each script ONE slot, every key KEYS[n]-or-rooted, no data-value key → CLEAN. Order theorem: no touched set re-orders the byte=mint browse (the outbox is a LIST drained in RPUSH order, no mint-order claim). :processed HSETNX idempotent re-deliver: GENUINELY exercised — the L-2 vacuous-fixture defect (child-queue in field 1) is fully fixed at all 5 hand-fabricated sites (conformance:1189 + test:346/373/548 use the PARENT queue; the :207 shape assertion pins [pq,parent,child,result]) → residual #3 DISCHARGED. Outbox lifecycle: self-clearing (dbsize-flat delta 0, no lingering key). Deliver-before-remove drain: LRANGE→contiguous-prefix→LTRIM-AFTER (a crash re-delivers idempotently, never drops).
(b) UN-PROMPTED FINDINGS (2): (i) L-1 — @flow_deliver's at-zero release is UNCONDITIONAL on the parent's existence: a deliver against an OBLITERATED parent (a FUTURE lifecycle op composed with an undrained outbox) resurrects a phantom (HSETNX creates :processed, DECR creates :dependencies at -1 → release → phantom pending member + payload-less row). CLASSIFIED correct-for-scope (within emq.3.3 nothing deletes the parent; admin.ex untouched, all lifecycle deferred D-5c/INV9) — does NOT block; it SHARPENS the lifecycle rung's obligation (draining the outbox is a CORRECTNESS, not merely a leak-hygiene, concern). (ii) the poison-prefix no-drop edge is correct-by-design but UN-TESTED in the suite.
(c) ATTACK-THAT-HELD — the poison-prefix no-drop: LPUSH'd a malformed entry (1 NUL → split_entry :malformed → count 0) at the outbox HEAD ahead of a valid entry; delivered=0 (HALTS the prefix), LLEN stayed 2 (NOTHING dropped — the `if delivered>0` guards the LTRIM), parent held; after operator-removes-poison, the valid entry delivered=1 → released. The design WITHSTANDS it (head-of-line block, never data loss). VERDICT: HELD.
(d) MUTATION KILL-RATE — 2/2 (100%), each with SCRIPT FLUSH before re-test (the T-6 EVALSHA-first lesson) + reverted net-zero via INVERSE EDIT (never git checkout — the L-3 footgun; final git diff confirms both mutants absent, suite 20/20 green again): Mutation A — @flow_deliver HSETNX guard ==1→>=0 (defeats idempotency) → CAUGHT, 3 tests fail (deps 1≠0, 0≠-1, 2≠1 — the L-2 keystone now genuinely guard-exercising); Mutation B — @complete dispatch 'xq'→'XQ' (cross-queue child falls through to the byte-frozen branch → DECRs the outbox LIST) → CAUGHT, 12 tests fail (the T-4 KEYS[3]-collision the early-return guards).

== RATIFY-OR-FLAG (the two new single-slot scripts, residual #4) ==
RATIFIED: @hold_parent + @enqueue_flow_child are a FAITHFUL realization of D-5b (which left the add mechanism open). @enqueue_flow is byte-frozen + no single script spans the children's slots, so two new single-slot declared-keys scripts are the natural form; the MIXED-flow generalization (same-queue child → byte-frozen fan-in; cross-queue child → outbox; both DECR the same :dependencies, each exactly once) is correct and tested (flow_cross_queue_test.exs:122-146). Both EMQKIND-first, each ONE slot.

== SPEC FILES SYNCED ==
- docs/echo_mq/specs/emq.3.3.md — added ONE [POST-BUILD RECONCILE] note at the As-built-surface anchor block recording the shipped line numbering (the cross-queue branch insertion shifted @complete :152→:175, the fan-in branch :181-188→:212-219, etc.); prose + contract faithful, pure line-sync. FLAGGED (not applied): the T-4 INV7-overloading clarity nuance (family EMQ.3-INV7 vs rung EMQ.3.3-INV7) — a documentation-clarity refinement for the Director/Operator, not a correctness STALE; editing a frozen-rung body for it risks introducing inconsistency for marginal value.

== MENTORING ROUTED (3 findings; durable edits PROPOSE-ONLY, Director-ratified) ==
PROPOSAL #1 (echo-mq-implementor SKILL §4, Mars build/test-fidelity — the L-2 class; SHARPEN the existing "A check counts only if it RUNS" line, do not stack): append to the §4 line ending "...invokes `doctest <Module>`." →  " — and a hand-fabricated WIRE fixture (a survivor/re-injected entry simulating a script's emit) counts only if it is BYTE-FAITHFUL to what the producer writes: assert the producer's real output shape AND reuse it (or a shared builder) for any re-injection, never a hand-typed sibling that can drift to a different slot/path than the assertion names (the emq.3.3 L-2 survivor-entry defect)."
PROPOSAL #2 (echo-mq-evaluator SKILL §2, the SCRIPT FLUSH / mutation-kill-rate lesson — NOT in apollo.md/skill/program-law today, grep=0): add a bullet to §2 → "- **The Lua mutation kill-rate (the spot-check).** Edit a defect INTO a Lua script/guard, run the suite, confirm a test CATCHES it, then REVERT net-zero (inverse Edit, NEVER `git checkout` — it discards the rung's real work). CRITICAL: `EchoMQ.Connector.eval` is EVALSHA-first, so `redis-cli -p 6390 SCRIPT FLUSH` before re-testing EACH Lua mutation — a recompiled Script.new gets a new SHA but a prior server-cached script is NOT invalidated by recompile, so a stale SHA masks the change and forges a false NON-catch (the emq.3.3 T-5 trap a self-review rationalized; the T-6 correction)."
NOTE #3 (Mars coverage, in-loop/minor): the poison-prefix no-drop edge is UN-TESTED — an optional regression test (LPUSH a malformed entry at the outbox head, assert delivered=0 + LLEN unchanged + parent held), not a blocker.

== RESIDUALS (none blocking) == L-1 (the obliterate-resurrection composition) is a forward note for the emq.3.4/lifecycle rung; the poison-prefix test gap is optional. NEITHER moves the grade. The rung is BUILD-GRADE and READY for the Director's single ratifying LAW-4 commit.

### Y-2 — Venus deliverables COMPLETE: the emq.3.4 triad + prompt authored (BUILD-GRADE), the roadmap/ fold to the canonical catalog (1:1 coverage), the status reconcile across three views. The one fork (V-1, grandchildren scope) surfaced for the Director — NOT ruled.

== RECONCILE VERDICT: BUILD-GRADE (T-1) ==
Every as-built anchor re-probed against the live echo/apps/echo_mq tree (the emq.3.3 ship moved the surface — the predecessor anchors STALE on line-number, MATCH on substance; re-pinned). The genuine gap: a flow child that DIES (@retry dead-letter arm jobs.ex:254-259) NEVER signals the parent → the parent hangs in awaiting_children forever; the happy-path fan-in (@complete) only fires on success. KEY NO-INVENT finding: the `:failed`/`:unsuccessful` subkeys are ALREADY in the §6 grammar (design.md:307) — unlike emq.3.3's `flow:outbox` (a new type), emq.3.4 populates pre-reserved subkeys → NO grammar edit, NO new key type, INV1 by construction. Conformance live = 47 (confirmed). No STALE/INVENTED/MISSING blocks.

== THE FORK SURFACED (V-1) — the Director/Operator rules, NOT Venus ==
The emq.3.4 SCOPE: does grandchildren / deep recursion belong in emq.3.4? Two canon sources fork:
- emq.3.md:198 (the family carve, the considered decomposition) — emq.3.4 = "failure-policy + bulk" ONLY.
- emq-3-3 D-5a (the frozen predecessor) — lumped grandchildren WITH failure-policy as "the 3.4 remainder" (a 3.3-scoping aside, not a considered 3.4 decomposition).
RECOMMENDATION: Arm A — emq.3.4 = failure-policy + add_bulk (one coherent capability, one shipped-script edit = the @retry dead-letter arm); grandchildren = a SEPARATE rung (emq.3.5 recommended — a distinct multi-level-fan-in mechanism that composes the cross-queue completion + the failure propagation, cleanest after both are flat-proven). The triad is authored to Arm A; grandchildren marked the honest Out → the fork. An Arm-B ruling is a cheap pre-build re-scope (only ADDS the grandchildren deliverables; the failure-policy core is identical).

== TASK A — the emq.3.4 triad + prompt (authored to Arm A, build-grade) ==
- docs/echo_mq/specs/emq.3.4.md (the contract — authoritative): §0 (the slice, why fourth) · Goal · 5W · Scope (In/Out + honest bounds B1-B6) · D1-D7 · INV1-INV11 (each a runnable *Check:*) · DoD (8 boxes, all `[ ]`). The failure mechanism: the additive @retry dead-letter branch (the existing body :254-259 BYTE-FROZEN) routing a same-queue death atomically by policy + the cross-queue death over the SAME flow:outbox + sweep emq.3.3 founded (a fail-entry KIND, delivered by @flow_fail_deliver, idempotent by the :processed-class HSETNX guard now over :failed/:unsuccessful).
- docs/echo_mq/specs/emq.3.4.stories.md: US1-US7 + the standing EMQ.3.4-US-GATE Valkey gate + a CLOSED Coverage map (every D1-D7 → ≥1 story; every INV1-INV11 exercised — verified).
- docs/echo_mq/specs/emq.3.4.llms.md: the Mars brief — the Arm-A summary · References (re-probed as-built seams, re-pinned post-3.3) · 7 numbered Requirements (each traced to a story → an invariant) · Execution topology (the runtime shape + the build DAG T1-T6 + the EXACT files) · 6 Agent stories (Directive + Acceptance gate as precondition→postcondition→invariant) · the no-invent/boundary guardrails (incl. the emq.3.3 L-2 lesson — a hand-fabricated fail-entry MUST byte-match @retry's emit).
- docs/echo_mq/specs/emq.3.4.prompt.md: the BUILD runbook (NOT a design-authoring cycle — the design is in the body). The HIGH-risk echo-mq-ship pipeline (Venus → Mars-1 → Director review → Mars-2 → Apollo MANDATORY → Venus → ship); the V-1 scope fork as the ONE explicit Director gate at Stage 0; the Stage-6 commit pathspec; risk tier (HIGH — a shipped @retry edit + the cross-slot failure delivery).
RISK TIER: HIGH (a shipped-script edit on @retry + the cross-slot failure delivery) → Apollo MANDATORY + the ≥100 loop. Mitigant: §6-reserved subkeys (no grammar edit) + the inherited-proven outbox+sweep (narrower new surface than emq.3.3's).
GATES PASSED: voice clean (no hype); every "atomic across queues" negated; D1-D7 + INV1-INV11 present; Coverage closes (every D → story, every INV → encodes); fences balanced; all links resolve; no first-person/perceptual verbs.

== TASK B — the roadmap/ fold + the status reconcile (1:1 coverage) ==
FOLDED docs/echo_mq/roadmap/{groups,batches,observables,a1-extend-locks}.md UP into emq.features.md as a NEW **Part C — the forward-feature catalog (the 5-section format)**, categorized: C.1 Flows (NEW — the active frontier, previously absent: parent/child ✅ · child-result reads ✅ · cross-queue ✅ · failure-policy+bulk 📐 · grandchildren 📋) · C.2 Groups (emq.4) · C.3 Batches (emq.5) · C.4 Lifecycle controls (emq.6) · C.5 Locks · C.6 The proof stack (emq.8). Each feature in Goal/Rationale/5W/Scope(In/Out)/Acceptance-Criteria, voice-tracks-status, the as-built-floor honesty PRESERVED (the "read before reconciling" floors carried — §Groups "emq.4 DEEPENS Lanes, it does not build a queue", §Batches the SHIPPED enqueue_many producer half, §Lifecycle the SHIPPED local CancellationToken). AC sourcing per D-1: DELIVERED features → the generated stories/ catalog; UNBUILT → the forward spec (the rung triad).

THE 1:1 MAPPING (every stale-file feature → its Part C home) is recorded in the ledger T-2 — verified: all 8 groups features (incl. the FLAME outlier → §Groups, the documented exception held at the program seam, NOT emq.4), all 9 batches features, both observables features, the a1-extend-locks fork. The Director git-rm's roadmap/ ONLY after verifying this — I did NOT delete it.

TWO STATUS RECONCILES surfaced by the fold (for the Director):
1. **a1-extend-locks.md's FORK is RESOLVED** — the note surfaced the @extend_locks A-1-wording as an OPEN Operator fork, but design.md:102-113 ALREADY carries the Operator-ratified (2026-06-14) slot-rooted-ARGV clarification = Arm 1 RATIFIED. Part C §Locks records it as the RATIFIED convention, not an open question. (The Director's coverage-verify should note: the fork is closed in the canon.)
2. **emq.3.3 shipped-this-run + emq.3.4 SPECCED** — reconciled across all three status views (emq.roadmap.md, emq.progress.md, emq.features.md): the catalog Part A flow row + B.1/B.3 parity rows + Part C.1; the roadmap ladder (emq.3.3 → shipped, emq.3.4 → 📐 SPECCED, emq.3.5 → grandchildren the V-1 fork) + the Movement-I "When" + the footer; the dashboard ANSI block + roll-up (8→9 shipped, +1 specced) + one-line + ladder row + Frontier table + "closed since" note. NOTE: I set emq.3.3 to SHIPPED (the as-built is gate-green, conformance 47, harness 9/9, forks D-1..D-5 ruled) with the explicit note "the LAW-4 ship commit is the Director's emq-3-4 close" — so the Director's single ship-commit lands the docs already-correct.

== FILES EDITED (docs ONLY — no .ex/.heex/.exs, no git) ==
- docs/echo_mq/specs/emq.3.4.md — NEW (the contract, authoritative)
- docs/echo_mq/specs/emq.3.4.stories.md — NEW (US1-7 + US-GATE + Coverage)
- docs/echo_mq/specs/emq.3.4.llms.md — NEW (the Mars brief)
- docs/echo_mq/specs/emq.3.4.prompt.md — NEW (the HIGH-risk build runbook)
- docs/echo_mq/emq.features.md — Part C added (the fold) + Part A flow row + B.1/B.3 parity rows + the header/legend reconciled
- docs/echo_mq/emq.roadmap.md — the emq.3 ladder rows (3.3 shipped, 3.4 specced, 3.5 grandchildren) + Movement-I "When" + the footer
- docs/echo_mq/emq.progress.md — the ANSI block + roll-up + one-line + ladder row + Frontier table + "closed since" note
- docs/echo_mq/specs/emq-3-4.progress.md — the ledger (T-1 reconcile, T-2 fold mapping, V-1 fork, this Y-1)

NEXT (the Director): (1) coverage-verify the T-2 1:1 mapping, then git-rm docs/echo_mq/roadmap/ (4 files); (2) ship emq.3.3 (the LAW-4 close — the gate-green as-built); (3) rule the V-1 scope fork at the emq.3.4 build's Stage 0 (Arm A recommended). Boundary held: stories/ untouched (generated); no frozen ledger rewritten; echo/apps/* + the Operator out-of-band paths (mesh, the consumer app, html, fsharp) untouched.

## {emq-3-4-complete} Complete

### Z-1 — emq-3-4 scope COMPLETE: emq.3.3 closed (BUILD-GRADE, shipped) + emq.3.4 designed (Arm A) + the forward-feature docs reconciled + the ledger hygiene. The LAW-4 close.

DELIVERABLES (all discharged):
1. emq.3.3 CLOSURE — the MANDATORY high-risk Apollo verdict the HALTED run never produced is now RECORDED (Y-1, Apollo ccl-emq-3-4-2): BUILD-GRADE, 16/16 promises MATCH, gate ladder re-run INDEPENDENTLY (303 tests/0 fail, Conformance {:ok,47}, ≥100 100/100, declared-keys CLEAN on all 4 cross-slot scripts, mutation kill-rate 2/2 with SCRIPT FLUSH, the poison-prefix attack HELD). RATIFIED (D-3) + SHIPPED as commit 7de4e90a (the emq.3.3 measured surface: code+tests+triad+prompt+the frozen emq-3-3 build-ledger+harness). Two un-prompted findings (L-1 the obliterate-resurrection composition; the poison-prefix test gap) recorded as forward notes for the lifecycle rung — NEITHER blocked.
2. emq.3.4 DESIGN — the triad + prompt authored (Venus Y-1, ccl-emq-3-4-3) to Arm A (the D-2 ruling on V-1: failure-policy fail_parent_on_failure / ignore_dependency_on_failure + add_bulk; grandchildren → emq.3.5, OUT). Director-ACCEPTED: grounded (NO-INVENT; the :failed/:unsuccessful subkeys §6-reserved at design.md:307 → INV1 by construction), the crux gap correct (a dead child via @retry's dead-letter arm jobs.ex:254-259 never signals the parent → the parent hangs forever), the mechanism rides the proven emq.3.3 outbox+sweep (a fail-entry KIND → @flow_fail_deliver, idempotent via the :processed-class HSETNX guard), HIGH-risk → Apollo MANDATORY at build, D1-D7 + INV1-INV11 + B1-B6 complete.
3. FORWARD-DOC RECONCILE — the stale docs/echo_mq/roadmap/ folded UP into emq.features.md Part C (the new 5-section Goal/Rationale/5W/Scope/AC format, categorized: C.1 Flows [NEW — the active frontier, was ABSENT] · C.2 Groups · C.3 Batches · C.4 Lifecycle · C.5 Locks · C.6 Proof stack); 1:1 coverage VERIFIED (every stale feature keyword present in Part C; the a1-extend-locks fork confirmed RESOLVED in canon, design.md:102-113); the stale roadmap/ dir REMOVED (git rm — recoverable via history). Statuses reconciled across emq.roadmap.md + emq.progress.md + emq.features.md (3.1/3.2/3.3 SHIPPED, 3.4 SPECCED). stories/ KEPT AS-IS (generated by mix echo_mq.stories — the code-specs catalog, Operator directive).
4. LEDGER HYGIENE — 20 tracked run-ledgers → specs/progress/ (commit fd8876d5, pure renames); the emq-3-3 build-ledger committed there with the 3.3 ship; this scope's emq-3-4 ledger migrated there at close.

LAW-1 HONORED — the correction that governed this run: REAL fanned-out registered subagents (Apollo ccl-emq-3-4-2 spawned+registered+Y-1; Venus ccl-emq-3-4-3 spawned+registered+Y-1), no role-play, no FAKE-N; the persistence-hardening (record the Y-n + SendMessage BEFORE idle) HELD — both peers reported, unlike the halted emq-3-3 Apollo. The Director called no Edit/Write on production code (LAW-1a); commits are pathspec-scoped per concern (LAW-4).

COMMITS: fd8876d5 (hygiene) · 7de4e90a (emq.3.3 ship — concern 1) · [this close] (emq.3.4 design + the forward-catalog reconcile + the roadmap/ removal — concern 2).

MENTORING (PROPOSE-ONLY, AWAITING Operator grant — NOT applied): Apollo routed two durable guardrails — (1) echo-mq-implementor §4: the wire-fixture byte-fidelity rule (a hand-fabricated wire fixture counts only if byte-faithful to the producer's emit — the L-2 vacuous-fixture class); (2) echo-mq-evaluator §2: the SCRIPT FLUSH / mutation-kill-rate procedure (EVALSHA-first → flush before re-testing each Lua mutation; absent from the skills today). Surfaced to the Operator; folded only on grant.

NEXT FRONTIER: the emq.3.4 BUILD (HIGH-risk, Apollo MANDATORY, the ≥100 loop) → emq.3.5 grandchildren → the flow family closes → Movement I closes → Movement II (emq.4 groups deepened · emq.5 batches · emq.6 lifecycle · emq.8 proof stack).
