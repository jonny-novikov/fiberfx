# emq-0 — AAW scope ledger

## {emq-0-thinking} Thinking

### T-1 — UNDERSTAND/EXPAND: the §0 derivation for rung emq-0 (mode: Flat-L2, declared). 5W — WHO: the Operator (jonny, Product Owner of the BCS + echo projects) ordered the run; the aaw lead team executes (Venus architect → Mars implementor ×2 passes → Apollo evaluator, Director-supervised). WHAT: found the "EchoMQ in Three Movements" program surface at docs/echo_mq (reconcile the moved emq.design.md, rewrite emq.roadmap.md as the engineering program with epic + per-movement 5W, author the emq.0 + emq.1 spec triads) and ship rung emq.0 = complete Movement 0: the PENDING echo2-migration.md §5 test/coverage pass over apps/echo_mq (9 modules), apps/echo_store (7 modules + Directory), apps/echo_data test/bcs (5 modules), plus tracking echo/rungs/ (10 untracked gate scripts). WHEN: now — the file import landed 2026-06-12 (commits c55673380-era c55071f4 bus + b4b81eda cache), the same day's supersession pointer promises the triad port that does not yet exist (docs/echo_mq/specs/ is empty). WHERE: the echo umbrella + docs/echo_mq. WHY: the Operator's pivot — state-of-the-art EchoMQ converges in apps/echo_mq (the measured BCS 2.0 Valkey-native code); apps/echomq is the v1 line frozen at 1.3.0, the push source, dissolving at program end; the aborted prior rung left Movement 0 built-but-untested and uncontracted. SOLUTION SPACE: (a) do-nothing baseline — Movement 0 stays untested, the supersession pointer dangles — rejected; (b) orchestrator-solo spec authoring — the V-SOLO-4 violation — rejected; (c) build emq.1 in the same run — breaks one-rung-one-increment-one-commit — deferred (emq.1 is SPECCED this run, built next); (d) CHOSEN: the Flat-L2 pipeline with an Operator checkpoint between Venus's specs and Mars's build. INVARIANTS AS RUNNABLE CHECKS: per-app `mix compile --warnings-as-errors` exit 0 (echo_mq, echo_store, echo_data); per-app suites green pure AND `--include valkey` (Valkey live on 6390, precondition `redis-cli -p 6390 ping` → PONG, probed UP); rung gates 3_1..3_5 + 4_1..4_4 PASS via `mix run --no-compile --no-deps-check --no-start` from echo root (3_6 conditional per the ratified Q1 — its stand-in is the :valkey `Conformance.run/2 → {:ok, 14}` test); lock delta = exqlite-only (`git diff --stat -- echo/mix.lock` = 1 insertion, verified); the D7 umbrella-test ban (never bare `mix test` from echo root); every mix command prefixed `ASDF_ERLANG_VERSION=28.1` (the pinned 28.5.0.1 is not installed) and tests `TMPDIR=/tmp`. SMALLEST CHANGE PRESERVING CORRECTNESS: test files + the two new-app mix.exs `test_coverage: [summary: [threshold: 0]]` keys + the rungs/ tracking + the spec/doc surface — zero production lib edits, apps/echomq untouched. Known environment hazards banked for Mars: the fence-mismatch test mutates the shared {emq}:version key (async:false + snapshot/restore in on_exit); Ring/Journal raise in init (trap_exit assertion pattern); named singletons EchoStore.Directory + EchoData.Bcs.Supervisor force async:false; per-test sub-queues + the purge idiom; Litestream binary absent → only replica_url/2 is testable, report partial coverage honestly (the no-fake-100% law); the mercury_cms phantom dep can kill the umbrella root compile (pre-existing, operator-owned — per-app compiles bind).

### T-2 — Venus Stage-1 evidence base + the pre-build reconcile derivation. SOURCES READ IN FULL: docs/echo_mq/emq.design.md (510 lines, §0–§12); docs/echo_mq/emq.roadmap.md (52-line course-flavored skeleton); docs/echo_mq/emq.references.md header (the consolidated BCS bibliography, 4 source families); docs/echo/migration/echo2-migration.md (673 lines — the Movement-0 contract-turned-record; §5 lifecycle map + §6 gate ladder = the triad's assertion sources); docs/elixir/specs/specs.approach.md (triad templates + the six quality gates); docs/echo/code/ROADMAP.md (the drop's 2.1/2.2/2.3/3.0 proposal ladder + standing dials — the dials name the superseded second-engine target; the design's S-5/Valkey canon wins in everything authored this stage); git show 1f03d579: the pre-move design (provenance header + §13 conformance architecture + §14 consolidation record — ALL THREE trimmed at the move, sharpening fork F-§13) and the old emq.roadmap.md (the four-cluster delivery arc, the per-rung status table emq.1–emq.6, the displaced-drafts ledger, seams). AS-BUILT PROBES (all MATCH): echo/apps/echo_mq = 9 lib modules (EchoMQ.{RESP,Script,Keyspace,Connector,Jobs,Lanes,Consumer,Pool,Conformance}) + keyspace_test/resp_test/test_helper (ExUnit.start(exclude: [:valkey])), mix.exs = EchoMq.MixProject, extra_applications [:logger,:crypto], deps [{:echo_data, in_umbrella}]; echo/apps/echo_store = 7 lib files (Directory nested at echo_store.ex:41) + coherence_test/helper, deps echo_data+echo_mq in_umbrella + {:exqlite, "0.23.0"}; echo/apps/echo_data lib/echo_data/bcs.ex + bcs/{property_store,archetypes,edge_store,supervisor}.ex (5 files; NO test/bcs yet — the open §5 work); echo/rungs = 6 bus + 3 cache + 1 journal scripts (10, untracked); mix.lock delta = exqlite-only 1 insertion (verified via git diff --stat). Anchor spot-checks: keyspace version_key/0="{emq}:version" (:11,:29-30), valid?/1 gate (:19), slot vector 12739 doc (:41) + def slot (:44); connector map_script_reply (:84-88), telemetry function_exported guard (:499-500), push_command requires_resp3 (:156-157), wire_version/0 (:111); conformance purge/2 (:271-275), scenarios/0 = EXACTLY 14 names (:18-35), run/2 (:42); consumer child_spec (:18) spawn_link loop (:40) stop/2 (:78); pool persistent_term+atomics (:25,:60-65); bcs gate/gate!/NamespaceError, branded_id valid?/1 (:95); coherence alias EchoMQ.{Connector,Lanes,Script} (:23); table init starts own Connector (:207); journal alias Exqlite.Sqlite3 (:34); litestream replica_url/2 (:90). LOAD-BEARING REALIZATION: the as-built echo_mq ALREADY EMBODIES the BCS state machine (three-field row, REV BYLEX order, attempts-as-token EMQSTALE, completion-deletes, server-clock reap, lanes rotation — the 14 scenarios name them) — the design §4 matrix's as-built column is dated at D1 against the PRE-MOVE apps/echomq tree, so the new convergence target closes much of cluster 1 by construction; the program roadmap (not the design) carries that mapping. DESIGN LINK SWEEP (grep, complete): 8 STALE link/claim sites (lines 80, 204, 206, 307, 311, 369, 380+383, 430 — all targeting the deleted docs/echomq/specs/emq/ home or the moved roadmap); §8's engine_hygiene_test.exs claim probed ABSENT from apps/echomq/test/conformance/ (7 other test files present) → DEFERRED living-status (Track B in flight, "Ships at" is forward-tense); prose cites bcs.progress.md (2 real homes) + aaw-mcp.progress.md verified existing → MATCH. v1 frozen line confirmed @version "1.3.0" (apps/echomq/mix.exs:4).

### T-3 — Stage-1c evidence base: the drop CHANGELOG delta analyzed file-by-file + the consumer ground read in full. THE CHANGELOG (docs/echo/code/CHANGELOG.md): "IMPORTANT CODE: Movement 0. NOT COMPLETED" — pending = NEW echo_wire app · MOVED {connector,resp,script}.ex + resp_test.exs out of the bus app into echo_wire (lib/echo_mq/ path KEPT — namespace frozen) · NEW echo_store shadow.ex + shadow/copy.ex + MOD litestream.ex · MOD 3 mix.exs (echo_wire dep) · NEW rungs/journal/bcs_rung_shadow_check.{exs,out} · MOD rung loaders dual-path · MOD 4 doc files (drop-internal, not imported). DEEP-DIFF RESULTS: (1) drop echo_wire/lib/echo_mq/{connector,resp,script}.ex + test/resp_test.exs are BYTE-IDENTICAL to production echo/apps/echo_mq copies — the "enhanced connector" is ALREADY in production (committed at 6a7d3655 [echo] umbrella); the production delta is therefore a pure FILE RELOCATION, and the functional delta is vs the §5 LIFECYCLE MAP (authored pre-enhancement): + noreply_pipeline/3 (CLIENT REPLY OFF..ON suppressed replies, answers :ok — connector.ex:110), + transaction_pipeline/3 (MULTI/EXEC wrap, {:ok, exec_replies} — :115), + subscribe/2 (:104, rides push_command/RESP3) — three wire verbs the map's Connector row never named, plus the EchoWire facade surface (defdelegate start_link/command/pipeline/noreply_pipeline/transaction_pipeline/eval/push_command/subscribe/stats → Connector; script/2 → Script.new — echo_wire.ex:19-30). (2) echo_wire/mix.exs: EchoWire.MixProject, app :echo_wire, version 2.0.0, extra_applications [:logger, :crypto], deps = [] — DEPENDENCY-FREE → ZERO lock implication (D10 exqlite-only law HOLDS); :crypto travels with Script. Drop echo_mq/mix.exs: app renamed :echo_mq, module EchoMQ.MixProject (STILL collides with v1's in production → the §3.6 EchoMq.MixProject as-built exception SURVIVES), extra_applications down to [:logger], deps + {:echo_wire}. Drop echo_store/mix.exs carries a STALE DEP ATOM {:echomq, in_umbrella} (no app :echomq exists in the drop — a drop source defect; production's {:echo_mq} stands, only + {:echo_wire}). lab gains echo_wire too — lab stays DEFERRED (D6), recorded not imported. (3) Shadow subsystem: EchoStore.Shadow = behaviour (start_link/1, restore/1 RESTORE-IF-MISSING law, status/1, stop/1) + pure dispatcher (start_link(:none) → :ignore; restore(:none) → {:ok, :no_replica}; child_spec both arms) — fully pure-testable; EchoStore.Shadow.Copy = GenServer, VACUUM INTO snapshots via Exqlite (NO wire, NO sidecar binary, NO creds — pure-suite eligible like Journal): sync/1 forced snapshot, status/1 map (db/dir/every_ms/syncs/last_error), restore/1 three arms, replica_path/2 pure, periodic tick re-arm, tmp-then-rename, no-live-file no-op; litestream.ex MOD = ADDITIVE ONLY (@behaviour + 4 @impl). (4) NEW rung bcs_rung_shadow_check.exs: gates SH1..SH4, tail PASS 4/4, compiled-module rung (alias only, like 4_4 → byte-identical copy), NO server NO creds; SH4 also asserts the EchoWire facade exports. (5) Dual-path loaders: 9 self-loading rungs differ (6 bus + 3 cache; 4_4 identical) — the wire-file loader becomes Enum.find([apps/echo_wire/lib/echo_mq/.., apps/echomq/lib/echo_mq/..], &File.exists?/1); the drop's fallback arm spells the drop's OLD dir name (vestigial there); in production the literal arm would name the FROZEN V1 APP dir — probed: production apps/echomq has NO lib/echo_mq/ (hazard dead today) — adaptation decision: re-spell the fallback segment apps/echomq/ → apps/echo_mq/ (the D12 one-segment precedent; production rungs never name the v1 dir). (6) OUTSIDE the delta, observed in the drop and NOT imported: rungs/bus/bcs_rung_busobjects_check.{exs,out} (G1..G6 "objects on the bus", Valkey), rungs/canon/bcs_rung_serialization_check.{exs,out} (S1..S6, needs priv/echo_native.so + gcc/cargo), the referee family (already §7-deferred) — surfaced in the report. CONSUMER GROUND (the consumer docs read in full): the consumer's §Jobs surface names retriable follow-up jobs on EchoMQ.Jobs with branded job ids, drained by EchoMQ.Consumer, shaped by EchoMQ.Lanes with one group per lane, and states "scheduled and repeatable jobs remain the EchoMQ roadmap's 2.1 row, with this consumer's needs recorded there as an input"; the consumer's §Dependencies repeats the trace (scheduled jobs trace to the EchoMQ roadmap's 2.1 row); the Work shape = retriable follow-ups; the consumer's patterns name reconciliation as a periodic-sweep consumer; the client is "EchoMQ.Connector via EchoWire" throughout — the emq.0 expansion imports exactly the inventory the consumer's §Where-this-starts lists (EchoWire, the pluggable Shadow with Litestream + Copy). Engine framing note: the consumer's "Where" carries its own superseded-engine clause — NOT inherited into anything authored here (design S-5/S-4 canon governs the EMQ program's engine line).

### T-4

T — Mars Stage-2 derivation: the echo_wire strict-compile fork. FACT: `cd apps/echo_wire && mix compile --warnings-as-errors` FAILS — connector.ex:417 (`fence/2`) calls `Keyspace.version_key()` via the :31 alias `EchoMQ.{Keyspace, RESP, Script}`; `keyspace.ex` stays in echo_mq per manifest F; echo_wire's deps are `[]` by design → the compiler emits undefined-module for the cross-app runtime reference → strict gate exits nonzero. The brief's three clauses (connector byte-identical · mix.exs deps:[] byte-identical · per-app strict compile exit 0) are jointly unsatisfiable as written. EVIDENCE this is a drop source defect, not a production artifact: the drop's CHANGELOG declares Movement 0 NOT COMPLETED (its own strict per-app echo_wire compile never ran — no .out exists for it); the drop's README claims "Depends on: Nothing"; the drop's own codebase handles exactly this pattern with `@compile {:no_warn_undefined, EchoData.BrandedId}` (echo_data/snowflake.ex:2). ALTERNATIVES weighed: (a) @compile annotation in connector.ex — in-drop precedent but breaks US7's "lib trio byte-identical to the files that left echo_mq"; (b) `elixirc_options: [no_warn_undefined: [EchoMQ.Keyspace]]` in echo_wire/mix.exs — keeps ALL lib files cmp-identical to the drop, preserves every property the manifest names for mix.exs (EchoWire.MixProject, app, version 2.0.0, umbrella keys, extra_applications [:logger,:crypto], deps: [], zero lock implication), and mix.exs is already the ADAPTED file class in this import (echo_mq + echo_store mix.exs both adapted); (c) circular dep — mix refuses; (d) moving/editing keyspace or connector — manifest/INV5/INV9 violations; (e) gating on a no-op compile after a root build — an inert check, dishonest green, rejected. RULING: realization (b), per the mars charter's realization-over-literal clause; flagged as BUILT-DIFFERENTLY in the Stage-2 report for Director ratification; the runtime-only Keyspace edge (echo_wire functionally needs the echo_mq sibling at fence time despite "depends on nothing") is surfaced to Venus for a spec-side resolution.

### T-5

T — Mars Stage-2 derivation: the echo_wire per-app wire-suite fork (the RUNTIME half of the Keyspace edge). FACT: `cd apps/echo_wire && mix test --include valkey` fails 13/22 — every Connector.start_link crashes UndefinedFunctionError EchoMQ.Keyspace.version_key/0 (connector.ex:417 ← do_connect:338 ← init:161): a per-app run loads only echo_wire's dep closure (deps: []), keyspace.ex lives in echo_mq, so the relocated connector CANNOT fence — and therefore cannot connect — inside its own app's test run. The brief's R4 placement (Connector wire suite + facade-live test in apps/echo_wire/test/) and its acceptance gate (per-app --include valkey green in echo_wire) are jointly unsatisfiable against the as-imported byte-identical sources; the extraction in the drop is INCOMPLETE on this edge (consistent with CHANGELOG NOT COMPLETED). ALTERNATIVES: (a) move keyspace.ex into echo_wire — manifest-F violation (the dual-path loaders' fallback arm is designed around keyspace staying in echo_mq); (b) {:echo_mq} dep — circular, mix refuses; (c) test_helper Code.prepend_path of the DEV-env echo_mq ebin — env-coupled hack + deviates the D13-specced helper line; (d) test-only dep — cycle in :test env; (e) stand-in Keyspace module in tests — API-redefinition drift, forbidden; (f) RELOCATE the two wire-bound suites (connector_test.exs, echo_wire_live_test.exs) to apps/echo_mq/test/ where the runtime closure exists (echo_mq depends on echo_wire, both apps load) — assertions byte-equal, placement-only deviation. RULING: (f). echo_wire's per-app suite stays pure-only (green standalone; its --include valkey ladder line stays green, vacuously). Deviation #3 flagged for Director ratification; the underlying lib defect (the dependency-free wire app's runtime need of EchoMQ.Keyspace — both the compile warning and this) is surfaced to Venus as the spec-side question: inline the fence key constant in the connector, or move version_key/0 into echo_wire (emq.1 territory; NOT built here under INV9/INV5).

### T-6

T-n — Venus-2 Stage-1d evidence base for docs/echo_mq/echo_mq.md (the program front door, D-11). THE "current,2,3" RESOLUTION: the directive's "emq*.roadmap.md current,2,3" matches THREE REAL FILES in docs/echo_mq/ — emq.roadmap.md (current: the ratified program, Movements 0/I/II, ladder emq.0–emq.8), emq2.roadmap.md (the EchoMQ 2.x line view, status ACTIVE-aligned 2026-06-12, mirrors the program ladder verbatim — its substance is the same ladder), emq3.roadmap.md (the EchoMQ 3.x STREAM TIER, status PROPOSED awaiting Operator slot ratification: rungs emq3.1–emq3.6, milestones S1 writer/S2 readers/S3 memory; hard dependency = emq.0 — the stream verbs land on the extracted echo_wire, the archive under the pluggable Shadow; the consumer retargets its event log to emq3.2, gates a consumer group on emq3.3, and gates run-id replay on emq3.4 windows + emq3.5 archive) — so the team-lead's fallback reading ("rungs 2..8") is superseded by the literal glob; both siblings emq2.specs.md/emq3.specs.md read for grounding. MILESTONE GROUNDS (per source): the consumer's roadmap milestones A=the walking skeleton ("no unbuilt dependency: every component it stands on carries a committed record today"), B=stream lanes (gates on conn.1–conn.2, promoted into emq3.1–emq3.2 per emq3.specs.md §needs-table), C=placement/sharding (gates on the Placement primitive, consumer-side); the scheduled-jobs trace to the EchoMQ roadmap's 2.1 row (§Dependencies) = emq.1; the consumer's §Jobs follow-up work via Jobs/Consumer/Lanes one-group-per-lane ("Batched follow-ups map to the as-built one-flush pipeline posture … until a batching rung earns its own record" = emq.5's slot); the consumer's lane-verb patterns — a control lane verb (3.4 pause/resume/limit per lane) + promotion-as-lane-wiring (shadow→canary→full) = emq.4's deepened-groups consumers; the consumer's §bus (RESP3 tracking, 4.5 lag rows) + §read-path (Tables) = emq.7's consumers; no consumer line names the parent/flow family (emq.3) or checkpoints/TTL/cancel (emq.6) — those mappings stay HONESTLY OPEN in the doc rather than invented. GO SDK (public surfaces only; .env untouched): module github.com/tinkoff/invest-api-go-sdk (go.mod:1, go 1.20, gRPC via google.golang.org/grpc); entry investgo.NewClient/3 (investgo/client.go:34); streaming Client.NewMarketDataStreamClient (client.go:137) → MarketDataStream() (md_stream_client.go:20) → channel-typed SubscribeCandle (md_stream.go:47) / SubscribeOrderBook (:89) / SubscribeTrade (:129) / SubscribeInfo (:168) / SubscribeLastPrice (:207) + Listen (:253) / Stop (:306); unary NewMarketDataServiceClient (client.go:175 — GetCandles/GetLastPrices/GetOrderBook/GetTradingStatus(es)/GetLastTrades/GetClosePrices/Get(All)HistoricCandles, marketdata.go); own-fill/ops feeds NewOrdersStreamClient (client.go:247) + NewOperationsStreamClient (client.go:259); token setup documented in the SDK README (referenced, never reproduced). WIRE MEETING POINT (forward-honest): no v2 Go client exists — the work slots at the program roadmap's seam 8 ("the Go-driven conformance harness and the Go store/keyspace ports", unslotted, slotted only by a checkpoint ruling per emq2.roadmap.md §Dependencies); the 3.x polyglot READ seam is separately named (emq3.3's non-BEAM reader on ordinary Redis clients over claims-only payloads). echo/apps/ probed: echo_wire/echo_mq/echo_store/echo_data present (Mars's import landed), echomq frozen, NO consumer app — forward tense binds.

### T-7

Apollo Stage-4 evidence inventory + reconcile/charter trace (post-build reconcile per reconcile.md post-mode; charter per x.md §11.2). READ IN FULL: the triad emq.0.{md,stories.md,llms.md}; ledger D-6..D-12, L-1..L-4, V-1, Y-1..Y-5, P-1..P-7; the record echo2-migration.md §status/:1-60 + section map; the drop CHANGELOG; apollo.md + x.md §11 + reconcile.md. ENVIRONMENT RE-PROBED: asdf erlang 28.5.0.1 + elixir 1.18.4 resolve, NO override; redis-cli -p 6390 PONG. GATES REPRODUCED INDEPENDENTLY (not the 530-loop): four per-app strict compiles exit 0 (echo_wire, echo_mq, echo_data, echo_store) + umbrella root compile exit 0; pure suites echo_wire 18/0 · echo_mq 63 total/0 (34 excluded) · echo_store 68 total/0 (20 excluded) · echo_data 65+3props/0; echo_mq --include valkey 63/0 with Conformance.run/2 → {:ok, 14} asserted live (conformance_run_test.exs:33, traced run prints the fence line echomq:2.0.0); shadow rung re-run PASS 4/4 with derive lines. RECONCILE EVIDENCE (file:line): D-10(a) echo_wire/mix.exs:18 elixirc_options no_warn_undefined [EchoMQ.Keyspace] + :19 test_coverage threshold 0 (permitted key); D-10(b) connector.ex diff vs drop = exactly 32 lines = the 16-line defp send_pipe/5 block relocated from :221-236 to below the last handle_call (:243 {:bump}), send_pipe now :248, all handle_call clauses contiguous :171-:243 in original relative order — resp.ex/script.ex/echo_wire.ex/resp_test.exs cmp-IDENTICAL to drop; D-10(c) the seven :valkey-tagged wire files (incl. connector_test.exs + echo_wire_live_test.exs) live in apps/echo_mq/test/, echo_wire/test carries ZERO :valkey tags (sole grep hit = a comment echo_wire_facade_test.exs:7) — AS3's echo_wire tagged clause confirmed VACUOUS. Manifests: six bus modules cmp-IDENTICAL to drop post-move; shadow.ex/copy.ex/litestream.ex/shadow-rung/4_4 cmp-IDENTICAL; litestream delta vs HEAD = exactly +5 lines (@behaviour + 4 @impl); echo_mq mix.exs EchoMq.MixProject + [:logger] + echo_data/echo_wire deps; echo_store mix.exs + echo_wire dep, stale {:echomq} atom NOT copied; helper ExUnit.start(exclude: [:valkey]); loaders 3_1/4_1/3_6 spot-verified dual-path echo_wire-first, grep "apps/echomq" over rungs/ = ZERO hits; resp_extend_test.exs = Stage-3 AUTHORED delta (Director note), in the test-tree fence, lifts RESP 42.37→93.22 and echo_wire pure 8→18. COVERAGE REPRODUCED to the basis point: echo_wire Connector 0.00 / EchoWire 10.00 / RESP 93.22 / Script 100; echo_store Litestream 7.25 / Table 83.04 / Copy 91.43 / Directory 94.44 / six modules 100 / total 79.14 — partials real, no fake-100. BOUNDARY: git status+diff on apps/echomq EMPTY; echo/mix.lock status+diff+cached EMPTY; full fence walk classifies every changed path to manifest/test-trees/operator-named out-of-band churn (live_svelte, mercury_cms statics, mercury_live_admin site). CONFORMANCE PIN: test @run_order == as-built scenarios/0 keys == the stories' 14 names, same order. UN-PROMPTED FINDINGS: (1) echo/rungs/ is ALREADY TRACKED at HEAD (operator out-of-band commit 6a7d3655) — the triad's D8/INV7 "(untracked today) enters tracking at the Director's pathspec commit" is OVERTAKEN: the Stage-5 pathspec ships 9 loader MODIFICATIONS + 1 NEW file (the shadow rung), not a whole-dir first add; (2) the generated artifacts inside fence dirs (echo_wire/cover/*.html, rungs/.DS_Store) are git-ignored (.gitignore:193 cover/, :28 .DS_Store — git check-ignore verified) — no pathspec hazard. MUTATION CAMPAIGN 5/5 KILLED, 0 survivors, every revert verified (git diff --stat empty for tracked; cmp-identical-to-drop for untracked; post-campaign re-green 63/0 · 68/0 · 18/0): M1 keyspace.ex:11 "version"→"versionX" KILLED (keyspace suites 12/1F); M2 jobs.ex:260 retry verdict :scheduled→:dead KILLED (jobs_test +valkey 8/1F); M3 shadow.ex:46 restore(:none)→{:ok,:restored} KILLED (shadow_test 6/1F); M4 resp.ex:22 array lead ?*→?% KILLED by the relocated floor (resp_test 3/1F); M5 ring.ex:150 applied counter +1 over-count KILLED (ring_test 7/1F — the L-3 settled-counter predicate is load-bearing: it kills the over-count deterministically). ATTACKS THAT HELD: (1) the INV5 fence attack — the full git-status walk over echo/ hunting an out-of-fence agent write found none; (2) the D-10(b) smuggle attack — diff arithmetic + clause-order analysis hunting a behavior change hidden in the relocation found pure reordering only; (3) the pin-triangulation attack — pin test vs scenarios/0 vs stories found zero drift. REMAINING: record :21 parenthetical ruling + spec-sync edits + retrospective + verdict (next entries).

### T-8

T-n — Venus-2 canon editorial pass (the D-13 grant): evidence + changes. PRE-EDIT PROBES: echo/apps/echo_mq/mix.exs — echo_data dep at :25, echo_wire at :26 (the design's :72 anchor STALE post-import-adaptation); keyspace.ex job_key/2 gate verified at :17-24 in production (the §2 anchor MATCHES — no edit); EMQ.1-US-GATE verified at specs/emq.1.stories.md:123. RECONCILE EDITS (7): (1) NEW 3-line orientation note under the title — rung numbers in the file name the pre-program ladder, read through the roadmap's old→new bridge; "deleted with the old spec home" = recoverable from git history, stated once (not a restoration of the trimmed provenance header — that ruling stands); (2) §1 S-4 stories carriage: emq.0 AND emq.1 now authored; (3) §2 mix.exs anchor :72 → :25; (4) §4 cluster 2: the groups slot flipped open-seam → RULED emq.4 (Stage-1b; roadmap seam 2 CLOSED); (5) §4 cluster 4: the as-landed note — the connector-substrate adoption LANDED via Movement 0's echo_wire extraction (EchoMQ.{RESP,Connector,Script} names frozen, the EchoWire facade), the fence's Keyspace read staying in echo_mq with the cross-app seam carried to the scheduler rung's design gate (ledger D-10); (6) §7: EMQ.1-US-GATE added beside EMQ.0-US-GATE; (7) §10 seams: intro re-titled to live-status-in-roadmap, seam 1 marked OPEN, seam 2 flipped RULED with the interim reading rule retired. CONCISION EDITS (6, style-only): §0 third-problem passage tightened; §1 intro tightened; §1 S-1 cluster-posture run-on split in two (clarity); §4 cluster 1 + §10 R-1 per-site "recoverable from git history" repetitions dropped (the orientation note states it once); §11 intro tightened; §11.11 de-duplicated against §10 seam 1 — with the no-release-precondition clause RESTORED after a self-caught regression (next entry). POST-EDIT VERIFICATION: §0–§12 + S-1…S-7 numbering/titles unchanged (header sweep); emq.1.md's three citations re-resolve — §5 (the wire-class ADR, :265), §11.10 (Deferral families, :434), §4 row 30 (backoff HOLD, :255); the roadmap's + emq2.specs.md's §-reference sweep all land (incl. §13 which the roadmap cites AS deliberately trimmed); fences 4 markers/2 blocks even; voice/person/perceptual/pronoun gates clean on every added line; diff 40 insertions/36 deletions; 516 → 520 lines (net +4: the two reconcile additions outweigh ~10 lines of prose trim — the canon is dense, not bloated; the trims are real but the as-landed truth is new content).

### T-9

T-n — Venus-2 task-#8 evidence: the EMQ.1-D1 scheduler design's as-built ground, re-verified at writing time (not recalled). THE FLOOR IS RICHER THAN THE BANKED ANCHORS: (1) the schedule set EXISTS as-built — emq:{q}:schedule, run-at-scored in server-clock ms: @retry parks into it (jobs.ex:153-154, keys :245) and @promote sweeps it (ZRANGEBYSCORE '-inf' now LIMIT 0 batch, script :158-185, verb promote/3 :268, group-aware); (2) retry/7 (jobs.ex:242) is the complete WIRE half — literal delay_ms + max_attempts in, {:ok,:scheduled}|{:ok,:dead} out, last_error kept, dead set + state='dead' at the cap — so emq.1's D4 is exactly the missing HOST half (the policy module) plus the drill; (3) the row-state vocabulary already carries 'scheduled' (jobs.ex:153) — scheduled-at-admission adds no new state; (4) the connector holds NO subscription registry (init state map connector.ex:129-158: push_to/pushes, no subs field), subscribe/2 :104 rides push_command/RESP3, the :reconnect success arm :284-287 re-issues nothing and already emits [:emq,:connector,:reconnect] telemetry :286, NO unsubscribe verb exists — the 2.1 gap verified at the exact seam; (5) THE SCRIPT CONVENTION IS INLINE Script.new/2 module attributes (echo_wire/lib/echo_mq/script.ex; @enqueue jobs.ex:14-24), NOT priv/ files — the emq.1.md §Where line "new Lua under priv/" is flagged STALE-vs-convention for the build run's reconcile; (6) the as-built key-construction convention: structure keys declared in KEYS[], the queue base as an ARGV, in-script derivation ONLY of the registered shapes (base..'job:'..id in claim/promote/retry/reap; the lane family g:<g>:pending/gactive/glimit/paused/ring/wake) — slot-sound, grammar-closed — and the S-6 LETTER ("declared KEYS[n] root") vs this ARGV-base FORM is a genuine reading fork the A-1 lint needs fixed, surfaced as fork 2; (7) Consumer process-shape precedent re-pinned: child_spec :18, start_link :35, stop/2 :78, loop :91; (8) the 2.1 row quoted VERBATIM from docs/echo/code/ROADMAP.md §2.1 (the visibility-fence constraint; the max-attempts blind spot + poison drill; "today the table's restart is the resubscription"); (9) the 14 conformance scenario names re-pinned from conformance.ex (fence mint duplicate kind order claim stale complete retry dead reap rotate pause limit). CROSSING NOTE: the Director's roadmap-row addendum was OBE — the row was already edited (stronger post-commit wording "shipped … ratified in the one-commit-full-pivot") by the Director's own hand seconds before Venus-2's edit would have landed; Venus-2 did NOT overwrite it; the commit a2d599c8 then fired, making the wording true.

### T-10

T-n — Venus-2 under /effort max: the /x-mode "Ship emq.0 and emq.1" assessment + the fence-safe lag-kill it justified. THE ASSESSMENT (3 blockers, each tree-verified, none clearable by an architect seat): (1) emq.0 SHIPPED a2d599c8 (HEAD since moved to c03202bb via Operator out-of-band) — nothing to ship; (2) emq.1 GATED by EMQ.1-INV7 ("no build artifact until EMQ.1-D1 Operator-approved") — the design draft sits DRAFT with 6 open forks, so a Mars build now = the V-SOLO/gate-skip x-mode exists to prevent; (3) x-mode is a DIRECTOR ceremony (init+spawn-director+TeamCreate+sole-LAW-4-commit) consuming a <rung>.prompt.md that DID NOT EXIST — an architect cannot stand up the team or commit (LAW-1/1a). RESOLUTION: surfaced the blockers + the 3-step path (Operator rules EMQ.1-D1 → author emq.1.prompt.md → Director runs x-mode), then under max effort took the one fence-safe step that collapses an authoring round-trip out of the critical path: AUTHOR docs/echo_mq/specs/emq.1.prompt.md in DRAFT (the same lag-kill logic the Operator endorsed for emq.1.design.md). THE RUNBOOK derives from authoritative material (the emq.1 triad's brief already carries the B0–B7 DAG, AS1–AS7 gates, the comprehensive prompt) re-shaped into x-mode's 6-stage structure; it DECIDES NO FORK (the 6 forks enter as the BLOCKING "Settled forks" gate). MAX-EFFORT ANCHOR RE-PROBE (post-emq.0, the import moved the wire trio): retry/7 jobs.ex:242 ✓, promote/3 :268 ✓, subscribe/2 echo_wire/.../connector.ex:104 ✓, :reconnect :282/:297 ✓, EchoWire facade 9 defdelegates+script/2 ✓, conformance scenarios == 14 ✓, Consumer child_spec:18/start_link:35/stop:78/loop:91 ✓, and CRITICALLY no echo_mq/priv/ dir exists → the triad's "new Lua under priv/" is STALE-vs-convention (inline Script.new/2) — captured as a Stage-1 RECONCILE FLAG in the runbook. SELF-CHECK: 1 real broken link caught+fixed (./emq2.roadmap.md → ../emq2.roadmap.md — the 2.x roadmap lives in docs/echo_mq/, not specs/); the x-mode SKILL.md relative path (../../../.claude/skills/x-mode/SKILL.md) probed-resolves; voice/perceptual/fences clean; the 2 "person" grep hits = false positives on "Movement I" (roman numeral); links 0 broken on re-run. File untracked, uncommitted; zero team spawned, zero code touched, zero git mutations.

## {emq-0-decisions} Decisions

### D-1 — Run scope locked: this run ships rung emq.0 ONLY (Movement 0 completion: the §5 test/coverage pass + rungs/ tracking + the spec surface). emq.1 is SPECCED in this run (triad authored by Venus after the Operator checkpoint rules its contract) but NOT built — one rung, one increment, one commit; the emq.1 build is the next run. Subject to Operator override at the mid-run checkpoint.

### D-2 — Spec home + freeze locks: (1) the program spec home is docs/echo_mq/ — emq.design.md (Operator-approved canon, reconcile-only, never redesigned), emq.roadmap.md (the engineering program roadmap), emq.references.md (read-first bibliography), specs/emq.N.{md,stories.md,llms.md} FLAT triads, ledger specs/emq-0.progress.md; the canonical triad template + six quality gates = docs/elixir/specs/specs.approach.md (the deleted docs/echomq/specs/reference copy was byte-identical). (2) apps/echomq stays UNTOUCHED and frozen at 1.3.0 this run (the push source per migration-doc D1 + the supersession pointer; dissolution timing operator-owned). (3) docs/echo/migration/echo2-migration.md remains the Movement-0 build RECORD — the triad becomes the authoritative contract; the record gets only the §5 status flip post-build plus reconcile-grade factual corrections.

### D-3 — The design pre-reconcile is APPLIED (apply-mode, link/claim repair ONLY — zero redesign): 8 sites corrected in docs/echo_mq/emq.design.md — §1 S-4 (GWT carriers re-pointed forward to ./specs/emq.N.stories.md as authored), §4 cluster 1 (the deleted emq.2.prompt.md runbook marked deleted-with-old-home/recoverable/re-issued-when-scheduled), §4 cluster 2 + §10 seam 2 (../emq.roadmap.md → ./emq.roadmap.md), §7 (the GWT block's home phrased living-status: formerly the pre-move emq.md, re-homes in ./specs/ triads; the EMQ.N-US-GATE twin "was present in all six pre-move files, verified 6/6 at the old home" + rides new triads as authored, emq.0 carries EMQ.0-US-GATE), §10 R-1 (the rewritten emq.md noted since-deleted), §10 seam 1 + §11.11 (../emq.2.md DoD → ./emq.roadmap.md seams, formerly the old emq.2 triad's DoD). NOT restored: the provenance header, §13 (conformance architecture), §14 (consolidation record) — FORK F-§13 surfaced to the Operator (recoverable via git show 1f03d579:docs/echomq/specs/emq/design/emq.design.md; the trimmed §13 is the requirements seed for the proposed emq.8). Verdicts: 8 STALE corrected, 1 DEFERRED (§8 engine_hygiene_test.exs — absent from apps/echomq/test/conformance/, claim is forward-tense Track-B living-status), prose ledger cites MATCH, all as-built §5 anchors MATCH.

### D-4 — The program roadmap is REWRITTEN at docs/echo_mq/emq.roadmap.md as the engineering program "EchoMQ in Three Movements": (1) THE EPIC with full 5W — why from-scratch convergence beats extending v1 in place (the two structural flaws unfixable under compatibility per design §0; the fork happens once per S-3; the BCS drop is measured rung-gated code born braced/branded/declared — converge on proofs, not debt), what dissolves when (apps/echomq dissolves when the movements complete, timing operator-owned, course re-grounding a dissolution-time concern); (2) per-movement rationale + 5W (M0 BCS Migration — build pass complete, test pass = emq.0; MI The Core — the v1-capability push named ONLY from sources: drop ROADMAP 2.1 retry-vocabulary/scheduled-repeatable/auto-resubscribe + design §11.10 scheduler+flow families + S-3/§2 migration path; MII The Extension — groups/batches/lifecycle/cache-deepened/conformance from the old roadmap's clusters + drop 2.2/§12.3 knobs); (3) THE RUNG LADDER (proposed, checkpoint-ratified): emq.0=M0 in flight · emq.1=MI proposal pending (recommended: the drop-2.1 scheduler+retry cluster) · emq.2=migration path · emq.3=parent/flow · emq.4=groups (the displaced family's proposed slot = seam-2 resolution) · emq.5=batches · emq.6=lifecycle · emq.7=cache (pull-forward operator call carried) · emq.8=conformance/proof; the OLD-emq.1 lineage recorded in one line + an old→new bridge table (old emq.2 the state machine = largely CLOSED BY THE DROP, residue at MI reconciles); (4) SEAMS 1-9 (design §10's two + F-§13 + commit packaging + dissolution timing + mercury_cms + carried knobs + drop 2.3/3.0 unslotted items + the two fence residuals); (5) THE NUMBERING BRIDGE Movements ↔ emq.N ↔ E0–E8 with the course bridge subordinated (course TEACHES, rungs SHIP; E0–E2 teach the frozen v1 line; E3 bridges the break as landed; E4–E8 ⇄ emq.4–emq.8). The read-first pointer to ./emq.references.md retained as line 3.

### D-5 — The emq.0 triad is AUTHORED and gate-self-checked at docs/echo_mq/specs/emq.0.{md,stories.md,llms.md} — the AAW port of echo2-migration.md per specs.approach.md. Spec body: six sections exactly; 5W Rationale (5 bold bullets); deliverables SPLIT — EMQ.0-D1..D4 = the recorded COMPLETE build pass (re-proven, not rebuilt) vs EMQ.0-D5..D8 = the open work (pure suites per the §5 map · :valkey suites + the ratified-Q1 stand-in Conformance.run/2 → {:ok, 14} with the pinned 14-name scenario list · honest per-module coverage with reasoned partials/Litestream · closure = the record's §5 flip + rungs/ tracked at the Director's pathspec commit); invariants EMQ.0-INV1..INV8 encode the §6 gates + the D-laws (INV1 strict compiles; INV2 = D7 per-app-only + TMPDIR=/tmp + ASDF_ERLANG_VERSION=28.1 prefixes; INV3 suite duality + 6390 PONG precondition + purge idiom; INV4 rung gates re-PASS under --no-compile --no-deps-check --no-start with 3_6 conditional per Q1; INV5 diff fence — zero lib/ edits, apps/echomq untouched; INV6 lock fence exqlite-only; INV7 no agent git; INV8 no-invent/no-fake-coverage). Stories: 6 Connextra stories with GWT, INVEST + encodes, Priority/Size/Implements, incl. the standing EMQ.0-US-GATE (US4 — the design §7 twin: the {:ok, 14} stand-in + honest-row reporting); Coverage line D1→US6 · D2→US6 · D3→US2 · D4→US6 · D5→US1,US2 · D6→US3,US4 · D7→US5 · D8→US6 (all 8 deliverables mapped; all 8 invariants story-reachable). Brief: References → Requirements R1–R8 [US-traced] → Execution topology (runtime shapes from record §3.4/§4.4 + hazards bank + task DAG T0–T5 + the complete touched-files fence) → the §5 lifecycle map PORTED VERBATIM (the assertion source, cited per module) → 5 agent stories [implements-traced] → first-two plan → the comprehensive Mars prompt embedding the §6 ladder verbatim-executable (the two standing env prefixes added and reasoned — the only delta from §6). Gate self-check: voice CLEAN ×5 files; fences even (0/4/0/0/6); links zero broken; structure 6/6 + 5/5; traceability closed (encodes 6/6 after the US4 wrap fix, implements 5/5, US-tags 8/8). The record's supersession pointer corrected at BOTH stale-path sites (line 5 + the closing note): docs/echo_mq/specs/emq.roadmap.md → docs/echo_mq/emq.roadmap.md; the record otherwise untouched per D-2.

### D-6

D-n — Stage-1b Operator checkpoint RULINGS (2026-06-12): (1) emq.1 = Candidate B (scheduler + retry vocabulary), with the consumer target REFRAMED — Portal is NO LONGER a target; codemoji is the named consumer, and emq.1's stories/value framing cite it. (2) The emq.N ladder CONFIRMED with two amendments: drop the old-emq.1 lineage line from the roadmap ("no history needed"), and emq.0/Movement 0 is NOT FINISHED — scope expands per the drop CHANGELOG (next decision). (3) F-§13 RULED: leave trimmed — §13 re-derives inside the emq.8 triad when that rung is specced. (4) F-PKG RULED: ONE commit, full pivot — the LAW-4 pathspec commit carries the rung files + docs/echo_mq/emq.references.md + the docs/echomq/specs/reference/* deletions + the re-added (post-rewrite) roadmap.

### D-7

D-n — emq.0 SCOPE EXPANSION (Operator directive + docs/echo/code/CHANGELOG.md, "IMPORTANT CODE: Movement 0. NOT COMPLETED."): Movement 0 additionally imports the drop's pending delta — (a) NEW library echo_wire (drop src/echo/apps/echo_wire/{mix.exs, README.md, lib/echo_wire.ex, test/test_helper.exs}; EchoMQ.* module names FROZEN, EchoWire is the facade; rationale in the app README) absorbing the wire layer MOVED out of echo_mq: lib/echo_mq/{connector,resp,script}.ex → echo_wire/lib/echo_mq/ plus resp_test.exs — the connector arrives ENHANCED, so the import is preceded by a deep diff vs the production echo/apps/echo_mq copies; (b) echo_store gains shadow.ex + shadow/copy.ex and a MODIFIED litestream.ex; (c) apps/{echo_mq, echo_store} mix.exs gain {:echo_wire, in_umbrella: true} (the drop also wires lab — lab stays deferred per the migration D6); (d) NEW rungs/journal/bcs_rung_shadow_check.exs (its .out record exists in the drop — bands derivable); (e) the 10 existing rung scripts become dual-path echo_wire-first loaders. Landing default MIRRORS the drop (echo/apps/echo_wire as a new umbrella app); Venus surfaces any structural fork instead of deciding one. The §5 test map extends to the echo_wire surface + the shadow subsystem. Environment superseded mid-run (Operator): erlang 28.5.0.1 now INSTALLED and mercury is FIXED (mercury_cms restored, mercury core dep dropped) — the umbrella root compile is viable again; Mars RE-PROBES the toolchain (asdf current erlang) instead of hardcoding the ASDF_ERLANG_VERSION=28.1 prefix, and a toolchain switch implies a full rebuild before gates.

### D-8 — The emq.0 triad is EXTENDED per the Stage-1b rulings (the Stage-1c scope expansion) and gate-self-checked. Spec body: deliverables now D1–D11 (D1–D4 the recorded first pass = the import's input state; D9 echo_wire import incl. the FILE-MOVE realization — drop copies verified byte-identical to production, so the relocation is fs-mv + the EchoMq.MixProject exception SURVIVING (the drop's renamed EchoMQ.MixProject still collides with frozen v1 in production) + extra_applications :crypto traveling with Script; D10 the shadow subsystem incl. the litestream ADDITIVE-ONLY behaviour delta + the drop's stale {:echomq} dep atom recorded as a source defect NOT imported + the shadow rung; D11 the 9 dual-path loader re-adaptations with the fallback re-spell apps/echomq/→apps/echo_mq/ so no production rung names the v1 dir; D5–D8 the proof over the as-landed 4-app shape); invariants now INV1–INV9 (INV2 REWRITTEN: toolchain RE-PROBE asdf current erlang replaces the hardcoded ASDF prefix, switch⇒full-rebuild; INV4 gains the shadow rung PASS 4/4; INV5 re-scoped to the exact D9–D11 manifest + test trees; INV6 re-checked under echo_wire deps=[]; INV9 NEW = the namespace law: EchoMQ.* frozen under apps/echo_wire/lib/echo_mq/, EchoWire the only new module, ZERO call-site edits). Stories: US1/US3/US6 re-scoped to the as-landed placement + the three Stage-1c connector verbs; NEW US7 (the wire extraction, consumer-cited) + US8 (the pluggable shadow, consumer-cited); Coverage line closes all 11 deliverables. Brief: the file-by-file import manifest E/F/G/H (record §3/§4 style, modes byte-identical vs ADAPTED, landing shape mirrors the drop = new umbrella app, NO structural fork found); the §5 lifecycle map kept verbatim + THE EXTENSION ROWS (EchoWire facade · the Connector Stage-1c verbs noreply_pipeline/3 :110 CLIENT-REPLY-OFF..ON, transaction_pipeline/3 :115 MULTI/EXEC, subscribe/2 :104 · Shadow dispatcher · Shadow.Copy full surface incl. SH2/SH3/SH4 shapes + sync-not-tick determinism + init KeyError · Litestream behaviour conformance); agent stories AS1–AS8 with AS6=the import FIRST in the task DAG (T1); the gate ladder extended (echo_wire compile/test gates + the shadow rung, expected tail PASS 4/4 per the drop's frozen .out) and DE-HARDCODED (the T0 toolchain re-probe replaces every ASDF prefix; mercury fixed ⇒ root compile viable; rung-gate flags stand). The record gains ONE delta-note line in the supersession pointer (the drop moved post-record; the triad carries D9–D11; the record stays the first pass's frozen account) — otherwise frozen. Gate self-check: voice CLEAN ×7, fences even, perceptual-verbs clean, traceability closed (encodes 8/8, implements 8/8, US-tags 12/12, INV1–INV9 all story-reachable), links 0 broken.

### D-9 — The emq.1 triad is AUTHORED to the ratified Candidate-B contract at docs/echo_mq/specs/emq.1.{md,stories.md,llms.md} — status SPECCED, NOT BUILT (built next run, after emq.0 closes), every surface written "emq.1 builds". Contract: the scheduler + retry vocabulary + connector auto-resubscribe (the drop ROADMAP 2.1 cluster, constraint verbatim: mint-ordered ids stay the sort key, a delay is a visibility fence not a new queue), CONSUMER = codemoji with the T-3-banked citations (the consumer's §Jobs follow-up work + the "remain the EchoMQ roadmap's 2.1 row" trace; the consumer's §Dependencies + its per-occurrence-or-batched trigger feedback; the consumer's reconciliation-as-a-periodic-sweep pattern; the consumer's Work shape "retriable follow-ups"); Portal appears nowhere. THE DESIGN GATE OPENS THE RUNG (D1/US6/AS1): the A-1-compatible scheduler design per design §11.10 (the v1 family roots key operands in data values — structurally inexpressible under declared keys), ≥2 steelmanned alternatives + ADR, every key spelled against the §6 grammar, Operator approval BEFORE any build artifact (INV7). Deliverables D1–D7 (design gate · run-at/run-in over the schedule set · repeatables = fresh JOB mint per occurrence · backoff vocabulary over the as-built Jobs.retry/7 jobs.ex:242 + the poison-job drill · the supervised OPT-IN promote pump over Jobs.promote/3 jobs.ex:268 · Connector resubscribe at the :reconnect seam connector.ex:282-297/subscribe :104 · conformance additions under the additive-minor law w/ the prior 14 scenarios byte-unchanged); invariants INV1–INV7 (wire law · declared keys · identity/visibility-fence · backoff-above-the-wire per §4 row 30 HOLD · pump opt-in supervised pure-cored · the carried emq.0 fences · specced-not-built + design-gate order); stories US1–US7 incl. the standing EMQ.1-US-GATE; deeper key/registry anchors explicitly deferred to the build run's pre-build reconcile (lag-1), never invented. Scope-Out fences: stream lanes = the consumer's recorded dependency on the connector's forward spec (conn.1–conn.2), NOT this rung; batched-follow-up shaping = emq.5; flow family = emq.3. Gate self-check: structure 6 sections + 5 5W bullets; traceability closed (encodes 7/7, implements 7/7, US 8/8, INV1–7 reachable, Coverage D1–D7 complete); voice/fences/links clean. The engine framing throughout = the design canon (Valkey the gate); the consumer docs' own superseded-engine clause is NOT inherited.

### D-10

Stage-2 deviations RATIFIED (Director gate, Stage 2→3): Mars's three flagged realizations are within-charter (realization-over-literal; behavior-identical; minimal; each resolves a genuine unsatisfiability born of the drop's never-strict-compiled echo_wire extraction): (1) apps/echo_wire/mix.exs gains elixirc_options: [no_warn_undefined: [EchoMQ.Keyspace]] — the connector reads EchoMQ.Keyspace.version_key/0 at fence time while deps stay []; in-drop precedent echo_data/snowflake.ex:2; every named mix.exs property preserved, zero lock implication. (2) connector.ex defp send_pipe/5 relocated below the last handle_call clause — pure definition reordering; the ungrouped-clauses warning made byte-identity and strict-compile jointly unsatisfiable. (3) The Connector :valkey suite + the facade-live test live in apps/echo_mq/test — the dependency-free wire app's compile closure cannot reach the wire (probed: 13/22 UndefinedFunctionError), so AS3's echo_wire tagged gate is vacuous-green; assertions unchanged, placement only. CONSEQUENCES ROUTED: Apollo's Stage-4 spec-sync records all three as as-built in the emq.0 triad (incl. the AS3 vacuity note); the wire-app↔Keyspace fence-time dependency question (inline the "{emq}:version" constant beside @wire_version vs move version_key/0 into echo_wire) is a SEAM carried to emq.1's design gate, plus the drop README "Depends on: Nothing" recorded as a source defect. LOCK-LAW UPDATE: the exqlite insertion sits at HEAD via an operator out-of-band commit — the Stage-5 pathspec DROPS echo/mix.lock unless the tree shows new movement at commit time. Operator out-of-band working-tree changes confirmed in apps/live_svelte/** + apps/mercury_cms/priv/static — excluded from the pathspec as planned.

### D-11

Operator directive (mid-run, parallel to Stage 3): the program gains its FRONT-DOOR document docs/echo_mq/echo_mq.md — the complete roadmap with per-rung MILESTONES binding the EchoMQ movements to the consuming app's delivery: each Movement done = a concrete consumer ship; MOVEMENT I COMPLETE (fully functional EchoMQ) ⇒ the consumer app scaffold WITH Go workers for external feed processing built on the local SDK github.local/invest-api-go-sdk. Authored by a SECOND Venus seat (Venus-2, fanned out per the Operator's wording) in parallel with Mars's harden pass — disjoint surfaces (Venus-2 writes ONLY the new echo_mq.md; doc reads + light greps, no compiles, so Mars's determinism loop keeps owning the machine). The Go-worker↔EchoMQ wire meeting point is phrased FORWARD (the v2 Go client does not exist yet — roadmap, never asserted-as-shipped). Credentials hygiene: the SDK dir carries a .env — never read or quote credential files into any doc.

### D-12

D-n — The milestone layer LOCKED in docs/echo_mq/echo_mq.md (Venus-2, per the Operator's D-11 directive verbatim — the document records, the Operator can correct at the next checkpoint): M0 = Movement 0 done (emq.0) — the certified foundation (EchoMQ 2.0 + EchoWire + EchoStore with the pluggable Shadow + the EchoData BCS subtree imported, tested, rung-gated in the production umbrella); no consumer code; un-blocks the consumer roadmap's entire §Where-this-starts inventory AND the 3.x tier's hard dependency. M1 = Movement I done (emq.1–emq.3; fully functional EchoMQ) — THE CONSUMER APP SCAFFOLD lands (the umbrella app skeleton + supervision + the EchoWire-fed consumers the consumer docs name, scoped from the consumer's first surfaces) WITH GO WORKERS for external feed processing built on github.com/tinkoff/invest-api-go-sdk (the streaming MarketDataStream subscription surfaces + the orders/operations stream clients, cited file:line); the Go-side→EchoMQ-keyspace meeting point stays ROADMAP-FORWARD at seam 8 (no v2 Go client exists; slotted only by a checkpoint ruling). M2 = Movement II done (emq.4–emq.8) — the consumer's operational depth rides the extension families (deepened consumer groups = the lane control/throttle/promotion verbs of the consumer's patterns; batched follow-ups = emq.5, the rung the consumer's §Jobs defers to; the near-cache deepened for the hot read/fan-out path = emq.7; the proof stack certifying the substrate = emq.8), each mapping grounded in a named consumer line, with emq.3/emq.6 consumer claims recorded OPEN (no consumer line names them — honesty over forced mapping). INTERPRETATION RECORDED in the doc: "current,2,3" = the three emq*.roadmap.md files (current program + 2.x line view + 3.x stream tier); the 3.x tier enters the complete roadmap as PROPOSED-awaiting-slot-ratification, never re-decided. The doc carries (never re-decides) the standing seams: wire-app↔Keyspace fence → emq.1's design gate (D-10), the Go-client slot (seam 8), apps/echomq dissolution timing (seam 5), the emq.7 pull-forward call, the emq3.1–emq3.2 pull-forward call (3.x roadmap's own recorded Operator call).

### D-13

Two Operator rulings at the mid-run checkpoint (the canon fork + the commit-packaging delta), both applied. (1) CANON RESTORED + RECONCILE GRANTED: docs/echo_mq/emq.design.md — found staged-DELETED at ~02:32 (an out-of-band index action, verified alongside unrelated mercury_live_admin site additions; surfaced by Venus-2 as L-5) — ruled "Restore and reconcile, make it clear and concise." The Director ran the granted narrow restore (git restore --staged + --worktree for exactly that path; 516 lines back on disk, path clean vs HEAD 29f37f7f, the mercury additions left untouched in the index). The reconcile + clarity/concision editorial pass is granted to Venus-2 as a NEW one-file fence with the hard constraint: section numbering and anchors STABLE (emq.1.md's footer cites §5/§11.10/§4-row-30; the roadmap's binding-design links and emq2.specs.md defer by name), every S-1…S-7 lock, every ADR, and the deferred-families register preserved SEMANTICALLY — prose trimmed, normative content never. (2) THE FOUR OPERATOR-DROPPED FILES ENTER THE RUNG COMMIT: emq2.specs.md + emq2.roadmap.md + emq3.specs.md + emq3.roadmap.md (untracked, mode 0600) join the one-commit-full-pivot pathspec, resolving echo_mq.md's spec-home links for repo readers; their internal sibling links to consumer/bcs docs remain a known later re-point (Venus-2's Stage-1d finding 1), recorded not fixed. Pathspec re-baseline noted: the Operator committed mid-run (29f37f7f design+roadmap+early-triad, e61483a8 tracked echo/rungs), so the Stage-5 commit takes the REMAINING tree delta — the triad extension + Apollo sync, the ledger/registry, the record flip, the code import surfaces, echo_mq.md, the emq.1 triad, the reconciled canon, and the four 0600 files; the staged mercury_live_admin site additions are operator out-of-band and stay out.

## {emq-0-alternatives} Alternatives

### V-1 — Shapes considered and rejected at Stage 1. (1) Restoring the trimmed §13/§14/provenance header during the design reconcile — REJECTED: the trim may be a deliberate Operator cut at the move; restoring would decide a fork (charter violation) — surfaced as F-§13 instead. (2) Re-numbering the design's own §4/§10 rung references (old emq.2/emq.6) to the new ladder — REJECTED: the design is Operator-approved canon, reconcile-only; the roadmap carries an old→new bridge table instead, so every §-citation stays valid as history. (3) Embedding the §5 lifecycle map by reference only ("read echo2-migration.md §5") in emq.0.llms.md — REJECTED: the rung delta mandates a faithful port (the map IS the assertion source; a pointer is a drift surface when the record evolves); ported verbatim with the §5 citation in the table title. (4) Nesting the §6 command ladder as a fenced block inside the comprehensive prompt's fence — REJECTED (fence-gate violation: same-length nesting); the ladder is indented text inside the one prompt fence. (5) Folding rungs/-tracking into a Mars deliverable — REJECTED: tracking is a git mutation (D8/INV7); it is specified as the Director's pathspec-commit closure step. (6) emq.1 candidate shapes — THREE steelmanned (A cluster-1 alignment/verification · B v1-capability parity scheduler+retry · C proof/conformance stack), recommendation B; full text in the Stage-1 proposal to the Director (the ruling is the Operator's, not locked here).



## {emq-0-learnings} Learnings

### L-1 — Stage-1 surprises. (a) The move trimmed MORE than the named fork: beside §13 (the conformance architecture), the pre-move design carried a provenance header (the D-3 consolidation note + status block) AND a §14 consolidation record — all three absent from docs/echo_mq/emq.design.md; F-§13 is therefore a three-part ruling (header + §13 + §14), and the trimmed §13 is the requirements seed for the proposed emq.8 (proof stack). (b) The as-built echo/apps/echo_mq ALREADY embodies the BCS state machine the design's §4 re-aimed at old-emq.2 (three-field row, four sets, attempts-as-token EMQSTALE, completion-deletes, server-clock reap, REV BYLEX browse — asserted by the 14 conformance scenarios): the §4 matrix's as-built column is dated at D1 against the PRE-MOVE tree, so the new convergence target closes most of cluster 1 by construction — the roadmap's old→new bridge table records this, and Movement-I reconciles re-disposition the residue. (c) docs/echo_mq/emq.roadmap.md was operator-STAGED (git M) before this rewrite — a staged copy predating the working-tree rewrite means a plain commit would ship the stale skeleton; flagged for re-add in the commit-packaging fork (the known AM-status hazard). (d) The deterministic traceability grep caught a real defect the eye missed: US4's INVEST line wrapped between "encodes" and its invariant ids, breaking the gate pattern — fixed; the gate works. (e) The §8 engine-hygiene test claim ("Ships at test/conformance/engine_hygiene_test.exs") probes ABSENT in apps/echomq — Track B remains in flight on the frozen line; the claim is forward-tense living-status, classified DEFERRED, no edit.

### L-2

L — Mars Stage-2 learnings (4). (1) THE OTP PARENT-EXIT PROTOCOL REAPS TEST-STARTED GENSERVERS: a GenServer started by the test process via start_link dies WITH the test even on :normal exit (gen_server intercepts the parent's EXIT before handle_info) — so on_exit cleanup that reuses the test's connector calls a dead pid (22 failures, one mechanism). The buildable rule: on_exit cleanup rides its OWN disposable connection; never carry a test-started conn into on_exit. Corollary proven same pass: supervisors (Bcs.Supervisor, Pool, Table) follow the parent down the same way → every teardown stop wraps try/catch :exit. Non-trapping processes (Journal, Ring applier, Copy) IGNORE the parent's :normal exit and survive → those DO need the explicit on_exit stop. (2) THE DROP'S "NOT COMPLETED" IS LOAD-BEARING: the extracted echo_wire app had never been strict-compiled or test-run in the drop — two unsatisfiable-contract forks (the Keyspace undefined-module warning; the ungrouped handle_call clauses) + one runtime fork (per-app echo_wire tests cannot connect, version_key/0 unresolvable) were all latent in byte-identical sources. A byte-identity clause and a strict-gate clause can contradict; the realization-over-literal escape needs the deviation ledger. (3) The compiler's type system now PROVES Snowflake.start/1 returns :ok-only — the floor's defensive {:error, :already_started} arm draws a typing-violation warning; new suites use the plain :ok = match. (4) ExUnit alphabetical pin order: :sweeps < :swept (position-4 'e' < 'p') — sort the expected list mechanically, never by eye.

### L-3

L — Mars Stage-3 surprise: THE TORN STATS SNAPSHOT (the harden pass's one remediation). EchoStore.Ring's drain advances the head sequence (occupancy → 0) BEFORE adding to the applied/batches counters, and Ring.stats/1 assembles its map from independent reads in the order counters → occupancy → the synchronizing :max_batch call — so a test polling stats for quiescence on `occupancy == 0` alone can capture counters read BEFORE the final increments (observed: applied == 1 with occupancy == 0; fired 1/68 in the G4 ladder run, reproduced 2/30 in a capture loop). The buildable rule: a quiescence poll over a multi-source stats snapshot must predicate on the SETTLED COUNTER VALUES the assertions need, never on the gauge alone — the gauge and the counters are not one atomic read. Test-side fix (predicate occupancy==0 AND applied==2 AND batches==2) verified 30/30. The lib's semantics (monotonic counters, eventually-consistent snapshot) judged acceptable for an observability surface — surfaced to Apollo as a doc-comment candidate on stats/1, not claimed a defect. Confirms the charter's law that one green run is not proof: the Stage-2 build pass ran this suite green twice; only the harden ladder + loop exposed the race.

### L-4

L-n — Venus-2 Stage-1d surprises (4). (1) THE GLOB IS LITERAL: the directive's "emq*.roadmap.md current,2,3" resolves to three REAL files — emq.roadmap.md + emq2.roadmap.md + emq3.roadmap.md (with emq2.specs.md/emq3.specs.md siblings, operator-dropped 2026-06-12 22:21, mode 0600) — so the spawn brief's fallback interpretation ("the current ladder projected through rungs 2..8") was superseded by reading the directory before interpreting; the 3.x stream tier (emq3.1–emq3.6, S1–S3, PROPOSED) is a whole roadmap the fallback reading would have missed. Buildable rule: when a directive names a glob, LIST THE DIRECTORY first — the filesystem is the interpretation authority. (2) BROKEN SIBLING-RELATIVE LINKS in the dropped pair: emq2.roadmap.md/emq2.specs.md/emq3.roadmap.md/emq3.specs.md link the consumer's spec + roadmap docs, `bcs.toc.md`, `bcsI.md`, `bcsH.md`, `bcsH.specs.md` as SIBLINGS — none exists in docs/echo_mq/ (the files were authored for the BCS content home; the consumer docs live in their own home) — every such link 404s from the new placement. Surfaced, not edited (Venus-2's fence = echo_mq.md only); echo_mq.md routes around it (links the consumer docs directly; names "the appendix the spec cites" without a dead link). (3) THE SUPERSEDED-ENGINE CLAUSE rides the dropped pair: emq3.specs.md's one-wire row and the consumer's Where both carry the second-engine-primary framing the program canon supersedes (design S-4 Valkey-as-gate / S-5 eradication; T-3's non-inheritance note) — echo_mq.md names no engine beyond the canon's Valkey line. (4) A POSSIBLE RUNG MISMATCH in emq3.roadmap.md §Dependencies: the event log retargets to emq3.2 — but the consumer's roadmap places the log-move at a later milestone (milestone B; an earlier rung is feed-as-claims, a later rung consumes the log). echo_mq.md quotes the claim ATTRIBUTED to its source ("the 3.x roadmap's own recorded consumer line") rather than asserting it cross-doc; flagged for the Operator's next checkpoint on the dropped pair.

### L-5

L-n — Venus-2 post-stage discovery (2026-06-13 ~02:32, during the continuation sweep): THE DESIGN CANON IS STAGED-DELETED MID-RUN. docs/echo_mq/emq.design.md (516 lines, the Operator-approved single source of truth) is gone from the working tree and its deletion is STAGED — the staged set mixes it with three unrelated echo/apps/mercury_live_admin/site/* additions, so this is Operator out-of-band action (S-7's standing clause; agents run no git — no peer can have done it). Timeline: the file existed at this seat's Stage-1d reads and at the echo_mq.md link self-check (0 broken at check time); the directory mtime moved to 02:32 after the 02:22 write. The file exists NOWHERE else under docs/ (find-verified — deleted, not moved); content recoverable via git show 29f37f7f:docs/echo_mq/emq.design.md ([emq] design, roadmap, migration — the same out-of-band commit that landed the references/roadmap edits dirty at session start). BLAST RADIUS if the deletion stands: emq.roadmap.md (binding-design links), both triads (../emq.design.md citations: emq.1.md footer cites §5/§11.10/§4-row-30), emq2.specs.md (the BCS mirror defers to the canon BY NAME: "the binding design canon … live[s] with the production tree"), echo_mq.md (header + spec-home row + Map), and Apollo's in-flight Stage-4 link/authority gates — every one dangles. SURFACED to the team-lead with the two Operator options (restore staged+worktree from 29f37f7f, or name the canon's new home and grant a link-re-point pass); NOTHING edited, NO git run by this seat. Buildable rule confirmed twice this run: re-probe the tree at every stage boundary — an Operator out-of-band mutation can land between a self-check and the next stage's gate, so a passed link gate is a timestamped fact, not a standing one.

### L-6

Apollo Stage-4 retrospective — what rung emq.0 earned (evidence: T-7 + the D-10/L-2/L-3/P-6/P-7 trail). (1) THE REPORT BOUNDARY IS THE FRAGILE SEAM, FILES ARE THE RECOVERY: the boundary dropped three times this run (Mars's background determinism shell died with its turn; Apollo idled three turns pre-resume with no durable artifact), and every recovery rode the same pattern — probe the tree/ledger for what actually landed (files-are-truth), salvage tallies from tee'd logs (/tmp/emq0_determinism.log → the 530-iteration tally), then resume from evidence. The buildable rule: durable artifacts (ledger entries, tree edits, tee'd logs) land BEFORE the turn boundary; plain text output is not a deliverable; the report message is the FIRST action after the verdict artifact, never deferred behind cleanup. (2) JOINTLY-UNSATISFIABLE SPEC CLAUSES ARE A CLASS, NOT AN INCIDENT: a byte-identity clause and a strict-compile clause contradict whenever the imported source never passed the target gate (the drop's CHANGELOG says NOT COMPLETED — load-bearing, per L-2); emq.0 hit it three independent ways (the Keyspace undefined-module warning; the ungrouped handle_call clauses; the unreachable wire at fence time) and resolved each through the realization-over-literal escape + the D-10 deviation ledger + Stage-4 spec-sync. The forward fix is contractual: a brief importing never-gated sources pre-authorizes the escape (which gate wins; where deviations are ledgered) instead of ratifying mid-run. (3) THE VACUOUS-GATE CLASS: an acceptance gate phrased per-app ("tagged run green in echo_wire") can pass with ZERO assertions exercised once placement legitimately moves — green-by-vacuity is indistinguishable from green-by-proof in the tally line. Stage-4 confirmed AS3's echo_wire clause vacuous (zero :valkey tags in the app; sole grep hit a comment) and wrote the vacuity note into the triad; the rule: no acceptance clause should be satisfiable by an empty test set without NAMING that emptiness. (4) THE OTP PARENT-EXIT REAP CLASS (L-2, confirmed in force): test-started GenServers die with the test even on :normal exit — on_exit cleanup rides its OWN disposable connection (jobs_test.exs:24 carries the rule as a comment); supervisors follow the parent down (try/catch :exit teardown); non-trapping processes survive and need the explicit stop. (5) THE TORN-SNAPSHOT CLASS (L-3, now mutation-graded): a multi-source stats snapshot (counters → occupancy → synchronizing call) is not one atomic read — quiescence polls predicate on settled counters, never the gauge alone; Stage-4's M5 mutation (over-counting the applied increment) was KILLED by exactly that settled-counter predicate, proving the L-3 remediation is load-bearing test logic, not decoration; the Ring.stats/1 doc-comment is the carried item (emq.0.md Carried-forward, production code outside the evaluator fence). (6) MID-RUN OPERATOR OUT-OF-BAND COMMITS OVERTAKE SPEC PREMISES: the triad's "echo/rungs/ (untracked today)" premise was false by verify time (commit 6a7d3655 tracked the 10 scripts) — a verify pass re-probes filesystem AND git state for every tracked-state premise instead of trusting the brief's snapshot; the Stage-5 commit shape changed accordingly (9 modifications + 1 new file, not a whole-dir first add). (7) THE MUTATION CAMPAIGN EARNS ITS COST: 5 targeted mutations, 5 killed, every revert verified byte-clean (git diff --stat for tracked; cmp-to-drop for untracked), total added wall time under four minutes on per-app scoped runs — the kill-rate converts "the suites are green" into "the suites BITE", which is the §11.2 charter's whole point.

### L-7

L-n — Venus-2: DE-DUPLICATION MUST BE CITATION-AWARE (self-caught in the canon concision pass). The §11.11 ↔ §10-seam-1 duplication looked like a textbook DRY trim — the same in-place-v2→v2 statement in two places — so the first edit reduced §11.11 to a pointer at §10 seam 1. The post-edit §-reference sweep caught the regression: the ROADMAP cites "design §11.11" specifically FOR the no-release-precondition clause ("the v2 line has never shipped — design §11.11"), and §10 seam 1's own internal citation of that clause ALSO points at §11.11 — so the trim had moved a clause out of the exact address external documents bind it to, leaving a circular pointer with the statement living only in a parenthetical at the other end. Fix: keep the de-duplication of the un-cited detail (drain-vs-converter mechanics) but RESTORE the cited clause at its address. The buildable rule: before trimming a clause as "duplicated," grep WHO CITES THE SECTION carrying it — a §-citation binds a clause to its address, and concision that moves a cited clause is a semantic edit wearing a style edit's clothes. Corollary: run the citation sweep AFTER the concision pass, not only before — the sweep is what catches the regression the trim's author cannot see while trimming.

## {emq-0-report} Report

### Y-1 — Venus Stage-1 report (deliverables A–C complete; D proposed, awaiting the Operator checkpoint). A · DESIGN RECONCILE (apply-mode): docs/echo_mq/emq.design.md — 8 STALE sites corrected (all link/claim repairs for the old home; zero redesign); 1 DEFERRED (§8 engine-hygiene test, Track-B living-status); as-built anchors MATCH across the board (T-2); F-§13 surfaced NOT restored (broadened: header + §13 + §14). B · ROADMAP REWRITTEN: docs/echo_mq/emq.roadmap.md = "EchoMQ in Three Movements" — the epic 5W, per-movement 5W (M0 migration / MI the core / MII the extension, every capability source-named), the proposed rung ladder emq.0–emq.8 with the old-emq.1 lineage line and the old→new bridge table, seams 1–9, the numbering bridge Movements ↔ emq.N ↔ E0–E8 with the course bridge subordinated; the ./emq.references.md read-first pointer retained (D-4). C · TRIAD AUTHORED: specs/emq.0.{md,stories.md,llms.md} — the AAW port of the migration record; deliverables split recorded-vs-open; INV1–INV8 encode the §6 gates + D-laws; the standing EMQ.0-US-GATE story; the §5 lifecycle map ported verbatim into the brief; the §6 ladder embedded verbatim-executable with the two env prefixes; six quality gates self-checked PASS (voice/structure/traceability/fences/links/format — one wrap defect found by the grep and fixed) (D-5). The record's two stale supersession-pointer paths corrected; otherwise untouched. D · emq.1 PROPOSAL sent to the Director: three steelmanned candidates (A cluster-1 alignment-verification · B v1-capability parity = the drop-2.1 scheduler + retry vocabulary + auto-resubscribe [RECOMMENDED] · C the proof/conformance stack), plus the consolidated fork list for the checkpoint (F-§13 three-part · F-PKG commit packaging incl. the staged-M roadmap hazard · F-SEAM-2 groups slot = emq.4 proposed · F-LADDER ratification incl. the emq.7 pull-forward call). emq.1 triad authoring BLOCKED on the ruling by design. Working tree state: all changes uncommitted for Director ratification; Venus ran zero git mutations. Stage-2 readiness: Mars builds from specs/emq.0.llms.md's comprehensive prompt; Valkey 6390 probed UP this session.

### Y-2 — Venus Stage-1c report (the rulings applied; the expansion specced; emq.1 authored; NOTE the triad-lock decisions landed as ledger D-8 [extended emq.0] and D-9 [emq.1] — their bodies carry the drafted D-6/D-7 prefixes, content authoritative). RULINGS APPLIED: (1) the roadmap is the codemoji-consumer program (Portal absent; the consumer's §Jobs/§Dependencies traces quoted in Movement I + the ladder's emq.1 row); (2a) the old-emq.1 lineage line + bridge row + E3 clause REMOVED (grep-verified absent); (2b) Movement 0 = NOT FINISHED with the CHANGELOG delta in the epic, the movement section, and the emq.0 ladder row; (3) F-§13 CLOSED leave-trimmed (roadmap seam 3; §13 re-derives in the emq.8 triad); (4) F-PKG closed one-commit-full-pivot (seam 4); seam 6 rewritten to the superseded environment facts (28.5.0.1 installed; mercury fixed; re-probe doctrine). THE EXPANSION (D-8): emq.0 now D1–D11/INV1–INV9/US1–US8 with the file-by-file import manifest E/F/G/H in the brief, the §5 map's EXTENSION ROWS, AS6=import-first task DAG, the extended de-hardcoded gate ladder (+echo_wire gates, +the shadow rung PASS 4/4, toolchain re-probe replaces every ASDF prefix), and the record's one delta-note line. Named deltas as specced: the enhanced connector = noreply_pipeline/3 (:110) + transaction_pipeline/3 (:115) + subscribe/2 (:104) — already in production (6a7d3655), so the import is a pure relocation; the EchoWire facade (9 delegated verbs + script/2); Shadow = behaviour + pure dispatcher; Shadow.Copy = VACUUM-INTO laptop shadow, wire-free binary-free, the SH1–SH4 shapes as suite content; litestream delta additive-only; echo_wire deps=[] keeps the lock law; the drop's {:echomq} dep atom + apps/echomq/ loader fallback = recorded drop vestiges, corrected at import (the fallback re-spell). emq.1 (D-9): SPECCED to ratified Candidate B, codemoji consumer, the A-1 design gate opening the rung, as-built seeds anchor-cited, deeper anchors deferred to the build run's lag-1 reconcile. GATES: voice CLEAN ×7 files · fences even · perceptual-verbs clean · traceability closed both triads (emq.0: 8 encodes/8 implements/12 US-tags, INV1–9 reachable, Coverage D1–D11; emq.1: 7/7/8, INV1–7, Coverage D1–D7) · links 0 broken · emq.1.md = 6 sections + 5 5W bullets. SURFACED, NOT IMPORTED (for the Operator's awareness, outside the CHANGELOG delta): DROP rungs busobjects (bus, G1..G6 "objects on the bus", has .out) + serialization (canon, S1..S6, needs echo_native.so) + the referee family (already §7-deferred) — no fork raised; the CHANGELOG is the Operator's own scope declaration. NO NEW FORKS. Stage-2 readiness: Mars-1 imports per manifest E/F/G/H then builds the §5 pass from specs/emq.0.llms.md's prompt; the emq.1 build run opens with the design gate. Venus ran zero git mutations; all changes in the working tree for the one-commit-full-pivot ratification.

### Y-3

Y — Mars Stage-2 BUILD pass report (rung emq.0). IMPORT: manifests E/F/G/H executed exactly — echo_wire created (wire trio + resp_test relocated by filesystem mv, pre-verified byte-identical to the drop; facade + mix.exs copied; helper ADAPTED to exclude :valkey), echo_mq re-shaped (six bus modules verified identical to the drop's post-move tree; deps + extra_applications adapted; EchoMq.MixProject name kept), cache shadow landed (shadow.ex + shadow/copy.ex + additive-only litestream replacement + shadow rung, all cmp-identical; mix.exs + echo_wire dep), 9 loaders re-adapted dual-path with the apps/echo_mq fallback re-spell (normalization diff proves byte-equality to the drop modulo the re-spell + prior header adaptations), 4_4 untouched. deps.get: lock ZERO movement (exqlite already at HEAD). SUITES: 4 pure suites green (8 + 29 + 48 + 65+3props, 0 failures); 3 tagged suites green (echo_mq 63/0 incl. CONFORMANCE 14/14 live; echo_store 68/0; echo_wire tag-vacuous); 10/10 rung gates PASS at exact expected tails incl. the NEW shadow rung 4/4. THREE FLAGGED REALIZATIONS (all rooted in the drop's NOT-COMPLETED state; Director ratification + Venus spec-side resolution requested): (1) echo_wire/mix.exs + no_warn_undefined for EchoMQ.Keyspace (the fence's cross-app runtime read, connector.ex:417); (2) connector.ex send_pipe/5 relocated verbatim below the handle_call group (deterministic ungrouped-clauses warning fails any fresh strict compile of the byte-identical file); (3) the Connector wire suite + facade-live test placed in apps/echo_mq/test/ (a per-app echo_wire run cannot resolve EchoMQ.Keyspace → cannot connect; assertions unchanged). LEFT FOR MARS-2 (harden): the --cover runs + per-module coverage table (AS4), the determinism loop, the record's §5 flip + closure note (AS5). Tree left uncommitted; operator out-of-band live_svelte/mercury_cms changes present and untouched.

### Y-4

Y-n — Venus-2 Stage-1d report: docs/echo_mq/echo_mq.md AUTHORED (196 lines, the program front door, D-11 directive → D-12 lock). SECTION MAP: status header (living document, Operator-governed; this file PLANS, design+triads DEFINE) → The program in one screen (three movements one-line each + THE DELIVERY THESIS: the consuming app delivered as movements complete, §Jobs trace quoted) → The spec home (8-row read-me table: canon · program roadmap · 2.x pair · 3.x pair · references · triads · ledger · migration record) → The complete roadmap with milestones (the interpretation note, one visible line: "current,2,3" = the three emq*.roadmap.md files; the program ladder emq.0–emq.8 with a per-rung "unblocks for the consumer" column, every cell a named consumer line or an honest "no line names this — open" for emq.2/emq.3/emq.6; the 3.x tier section S1/S2/S3 with the consumer's event-log/consumer-group/replay lines, status PROPOSED carried) → MILESTONE M0 (the certified foundation, no consumer code; closes the consumer's §Where-this-starts inventory claim against production + the 3.x tier's two hard deps) → MILESTONE M1 (fully functional EchoMQ ⇒ the consumer app scaffold — skeleton + supervision + EchoWire-fed consumers scoped from the consumer's first rungs; Go workers for external feed processing on github.com/tinkoff/invest-api-go-sdk with the REAL surfaces cited file:line — investgo.NewClient client.go:34, NewMarketDataStreamClient :137 → MarketDataStream md_stream_client.go:20 → SubscribeCandle/OrderBook/Trade/Info/LastPrice md_stream.go:47/89/129/168/207 + Listen/Stop :253/:306, unary MarketDataServiceClient :175, OrdersStream :247 + OperationsStream :259; token per the SDK README, no value reproduced; THE WIRE MEETING POINT honest: no v2 Go client exists, the work slots at seam 8, checkpoint-ruled, handoff fixed at the scaffold's first rung; the emq3.3 Go-or-Python adjacency recorded not decided; all forward tense) → MILESTONE M2 (5-row family→consumer-ship table, each grounded: a control-lane-verb pattern + promotion-as-lane-wiring → emq.4; §Jobs batching deferral → emq.5; emq.6 open; Tables/RESP3-budget → emq.7; figures-verbatim law → emq.8; milestone B = the 3.x tier's business, milestone C = the consumer's own Placement rung) → Seams carried not re-decided (5: wire-app↔Keyspace → emq.1 design gate per D-10 · Go-client slot seam 8 · echomq dissolution seam 5 · emq.7 pull-forward · 3.x slot + emq3.1–3.2 pull-forward) → Map. SELF-CHECK PASS: links 0 broken (deterministic loop over every relative target) · voice-gate banned words 0 · gendered pronouns 0 · first/second person 0 (roman-numeral I only) · fences 0 (even) · perceptual verbs 0 · forward tense for all unbuilt surfaces · credentials untouched (.env never opened). RECONCILE-GRADE FINDINGS SURFACED NOT EDITED (L-4): the dropped emq2/emq3 pair's sibling-relative links 404 from docs/echo_mq/; their superseded-engine clause vs the canon; the emq3.roadmap event-log retarget vs the consumer roadmap's later log-move. File UNCOMMITTED (zero git mutations); zero compiles/tests run (Mars owns the machine). Task #7 complete.

### Y-5

Y — Mars Stage-3 HARDEN report (rung emq.0; the gate-by-gate detail is P-6, the loop tally + hygiene is P-7, the torn-snapshot learning is L-3). VERDICT: every ladder gate PASS; one remediation used of three (the ring torn-stats-snapshot test fix, verified 30/30 then folded into the 530-iteration loop). LADDER: G0 toolchain (erlang 28.5.0.1, no override) + PONG · G1 four strict compiles GREEN · G2 umbrella GREEN · G3 pure suites 8/29/48/65+3props all 0-fail · G4 tagged suites 18 (echo_wire, incl. the new resp_extend) / 63 incl. CONFORMANCE 14/14 (echo_mq) / 68 (echo_store) all 0-fail · G5 coverage per module with reasoned partials (Litestream 7.25 binary-deferred; Connector 0.00 + EchoWire 10.00 in echo_wire's own attribution — the wire proof lives in echo_mq's suite per the ratified placement; RESP lifted 42.37→93.22 by resp_extend_test.exs; echo_data's 44.67 app total = pre-existing out-of-scope modules, all five rung Bcs modules 100 except EdgeStore 95.74) · G6 rungs 3_1 5/5 · 3_2 5/5 · 3_3 6/6 · 3_4 8/8 · 3_5 6/6 · 4_1..4_4 6/6×4 · shadow 4/4 (3_6 conditional per Q1; its {:ok,14} stand-in green) · G7 hygiene: 101 stale Stage-2-era keys swept; post-loop steady state ZERO leaked keys (TTL'd flight-straggler mechanism recorded), fence intact, lock EMPTY. DETERMINISM: 530 iterations across pure + named tagged file groups, seeds 0/1/42/312540/999999 + random, 0 failures. CLOSURE: §5 flipped COMPLETE (2026-06-13) with in-style tallies; echo/rungs/ tracking noted for the Director's pathspec commit. APOLLO-FLAGGED (2): the record's :18 role parenthetical still reads "(PENDING)" (outside the one-line fence); Ring.stats/1 doc-comment candidate — the snapshot is assembled from independent reads (counters → occupancy → the synchronizing call), so quiescence polls must predicate on settled counters. Tree left uncommitted; operator out-of-band churn (live_svelte, mercury_cms statics) untouched and listed separately.

### Y-6

Apollo Stage-4 VERDICT: rung emq.0 is BUILD-GRADE. The post-build reconcile finds every triad promise MATCH, ratified-DEVIATION (D-10 ×3, now synced), or ratified harden-delta (resp_extend_test.exs, now synced); zero unratified STALE, zero INVENTED, zero MISSING. The full evidence trail is T-7; the retrospective is L-6.

RECONCILE DELTA SUMMARY (promise → as-built → verdict): D1 echo_mq six bus modules cmp-identical to drop post-move, EchoMq.MixProject, helper excludes :valkey → MATCH. D2 echo_store deps {echo_data, echo_mq, echo_wire, exqlite 0.23.0}, Directory nested → MATCH. D3 bcs subtree five lib files, echo_data/lib untouched → MATCH. D4+D11 rungs: 9 dual-path loaders echo_wire-first with the apps/echo_mq fallback re-spell (grep "apps/echomq" over rungs/ = ZERO), 4_4 cmp-identical, spot-verified 3_1/4_1/3_6 + the alias-only shadow rung → MATCH. D9 echo_wire app → MATCH-as-ratified: resp.ex/script.ex/echo_wire.ex/resp_test.exs cmp-IDENTICAL to drop; connector.ex differs by EXACTLY the 16-line defp send_pipe/5 relocation (32 diff lines = one block moved below the last handle_call :243, send_pipe :248, clause order preserved) = D-10(b); mix.exs carries elixirc_options: [no_warn_undefined: [EchoMQ.Keyspace]] (mix.exs:18) = D-10(a) + the permitted coverage key. D10 shadow: shadow.ex/copy.ex/litestream.ex/shadow-rung cmp-IDENTICAL; litestream vs HEAD = +5 lines only (@behaviour + 4 @impl); stale {:echomq} atom NOT copied → MATCH. D5/D6 suites: every §5 map row incl. the EXTENSION ROWS has its suite (RESP floor+resp_extend; Script; Keyspace floor+extend; Jobs/Lanes guards+wire; Consumer spec+wire; Pool; Conformance pin+run; Directory; cache Keyspace; Coherence pure+wire; Ring; Table; Journal pure+wire; Litestream pin+behaviour; Bcs ×5; EchoWire facade pure+live; Shadow; Shadow.Copy) — placement per D-10(c): the seven :valkey wire files incl. connector_test.exs + echo_wire_live_test.exs live in apps/echo_mq/test/ → MATCH-as-ratified. D7 coverage → MATCH (re-run, identical to the basis point). D8 closure → MATCH with one OVERTAKEN premise (rungs already tracked — finding U1).

PROMPTED CHECKS (12/12 PASS): (1) D-10(a) mix.exs:18 no_warn_undefined verified; triad now matches as-built (sync applied) — PASS. (2) D-10(b) diff vs drop = ONLY the relocation; resp/script byte-identical — PASS. (3) D-10(c) placement verified in tree; triad placement text synced — PASS. (4) AS3 vacuity CONFIRMED (zero :valkey tags in echo_wire/test; sole grep hit = comment echo_wire_facade_test.exs:7); vacuity note written into the brief's AS3 + stories US3 — PASS. (5) Record §5 status line: one sentence, in-style, COMPLETE (2026-06-13), P-7 tallies present (echo2-migration.md:14-18); the :21 role parenthetical RULED + AMENDED "(PENDING)" → "built §5 (SHIPPED 2026-06-13)" — it sits inside the living Status block the flip itself edits; leaving it would carry an internal contradiction — PASS. (6) INV1–INV9 each re-run as a check: INV1 four strict compiles + umbrella root all exit 0; INV2 honored procedurally (per-app only, TMPDIR=/tmp, toolchain re-probed erlang 28.5.0.1/elixir 1.18.4, no override); INV3 pure runs green with :valkey excluded + tagged green on 6390 (PONG) + per-test sub-queues/purge idiom spot-verified (jobs_test.exs:21-30, conformance_run_test.exs:33); INV4 shadow rung re-run PASS 4/4 with derive lines (3_1..4_4 per P-6 at exact tails); INV5 full fence walk classifies every changed path to manifest/test-trees/operator-named churn; INV6 mix.lock status+diff+cached EMPTY; INV7 zero agent git mutations (Stage-4 used read-only git + inverse-Edit reverts); INV8 coverage partials reproduce exactly — no fake-100; INV9 grep: EchoWire the only EchoWire* module, EchoMQ.{RESP,Script,Connector} defined under echo_wire/lib/echo_mq/, zero call-site renames (lib diff vs HEAD = 3 moved-out deletions + litestream +5 + mix.exs ×2) — PASS. (7) Manifests E/F/G/H file-by-file vs tree: every listed file present in its destination; extras inside fence dirs are git-ignored artifacts (cover/ .gitignore:193, .DS_Store :28 — check-ignore verified) + the ratified resp_extend_test.exs — PASS. (8) §5 map rows incl. EXTENSION ROWS each covered by a named suite (list above) — PASS. (9) Conformance: pin test @run_order == as-built scenarios/0 == the stories' 14 names, same order; live run {:ok, 14} reproduced (fence line echomq:2.0.0 printed) — PASS. (10) Coverage honesty: re-run reproduces G5 exactly — echo_wire Connector 0.00/EchoWire 10.00 (attribution honest — wire proof lives in echo_mq per ratified placement)/RESP 93.22/Script 100; echo_store Litestream 7.25 (binary-deferred)/six modules 100/total 79.14 — PASS. (11) Boundary: git status AND diff on apps/echomq EMPTY; mix.lock EMPTY → the Stage-5 pathspec DROPS mix.lock per D-10 — PASS. (12) Dual-path loaders spot-verified ×3 + shadow rung; zero v1-dir references — PASS.

UN-PROMPTED FINDINGS (2): U1 — echo/rungs/ is ALREADY TRACKED at HEAD (operator out-of-band commit 6a7d3655): the triad's D8/INV7 "(untracked today)" premise is overtaken; the Stage-5 pathspec ships the 9 loaders as MODIFICATIONS + ONE new file (the shadow rung), 4_4 rides unmodified — synced into emq.0.md D8. U2 — the in-fence generated artifacts (echo_wire/cover/*.html, rungs/.DS_Store) are confirmed git-ignored — no pathspec hazard. Swept additionally: Valkey hygiene steady state independently re-verified AFTER Stage-4's own tagged + mutation wire runs — {emq}:version = echomq:2.0.0, zero emq0*/emq:*/ecc:* keys.

ATTACKS THAT HELD (3): A1 the INV5 fence attack — the full git-status walk over echo/ hunting any out-of-fence agent write found none (all residue = operator-named live_svelte/mercury_cms/mercury_live_admin churn). A2 the D-10(b) smuggle attack — diff arithmetic (32 = 16+16, content-identical block) + clause-order analysis hunting a behavior change hidden inside the ratified relocation found pure reordering. A3 the pin-triangulation attack — three independent sources of the 14-scenario list (test pin, as-built registry, stories) hunted for drift; zero.

MUTATION KILL-RATE: 5/5 KILLED, 0 survivors (no to-do list). M1 keyspace.ex:11 fence-key string → KILLED (keyspace suites, 1F). M2 jobs.ex:260 retry verdict swap :scheduled→:dead → KILLED (jobs_test +valkey, 1F). M3 shadow.ex:46 restore(:none) → {:ok,:restored} → KILLED (shadow_test, 1F). M4 resp.ex:22 array lead ?*→?% → KILLED by the relocated floor itself (resp_test, 1F). M5 ring.ex:150 applied-counter over-count → KILLED by the L-3 settled-counter predicate (ring_test, 1F) — the harden remediation is proven load-bearing. Every revert verified: git diff --stat empty (tracked) / cmp-identical-to-drop (untracked); post-campaign re-green 63/0 · 68/0 · 18/0.

GATES REPRODUCED INDEPENDENTLY (not the 530-loop, per the scoped-loop charter — build+harden already ran it uncontended): toolchain re-probe; PONG; 4 strict compiles + umbrella exit 0; pure 18/0 · 29-run/0 · 48-run/0 · 65+3props/0; echo_mq --include valkey 63/0 incl. Conformance {:ok, 14} live; shadow rung PASS 4/4; echo_wire + echo_store cover runs reproducing G5 exactly.

SPEC-SYNC APPLIED (9 edits, all additive as-built notes): emq.0.md — D9 note (deviations a+b), D6 note (placement c + resp_extend provenance), D8 note (rungs-tracking overtaken + the :21 ruling), Carried-forward block (Ring.stats/1 doc-comment for the next Mars pass — production code, outside the Stage-4 fence). emq.0.stories.md — US3 placement+vacuity note; US7 as-built note (connector delta, mix.exs keys, 18-test count). emq.0.llms.md — manifest-E as-built note; lifecycle-map placement note; AS3 VACUITY NOTE (incl. the wire-app↔Keyspace seam carried to emq.1's design gate). echo2-migration.md — the :21 parenthetical amendment (in-style, one line).

MENTORING (PROPOSE-ONLY — no Operator grant given; diffs for Director ratification): VENUS (contract-fidelity, 2): V1 add to venus.md: "When a brief imports sources that never passed the target gate (a drop marked NOT COMPLETED, generated code, a foreign toolchain), pair every byte-identity clause with the pre-authorized escape — which gate wins on conflict and where the deviation is ledgered (emq.0: byte-identity and --warnings-as-errors were jointly unsatisfiable three ways; D-10 had to ratify mid-run)." V2 add: "Write no acceptance gate satisfiable by an empty test set — a per-app tagged-run clause names at least one test expected to RUN there, or names the vacuity (emq.0 AS3: the echo_wire --include valkey clause sat green over zero tagged tests)." MARS (build-fidelity, 1): M1 add to mars.md: "Tee every long-running gate's output to a file and report from the file — the turn boundary kills background shells, and a tally that lives only in the turn is lost (emq.0: the determinism loop's shell died with the turn; the 530-iteration tally survived only because it was tee'd to /tmp/emq0_determinism.log and salvaged)." In-loop channel: none needed — no Mars/Venus seat is live; the findings ride this report + L-6.

FOR STAGE 5 (Director): the pathspec ships — the emq.0 triad (3 files, synced) + the record (2 lines total: Mars's §5 flip + the :21 amendment) + the ledger + echo/apps/echo_wire/{lib,test,mix.exs} + apps/echo_mq {mix.exs, 3 lib deletions, test/} + apps/echo_store {mix.exs, litestream.ex, shadow.ex, shadow/, test/} + apps/echo_data/test/bcs/ + echo/rungs/ (9 M + 1 new); DROPS echo/mix.lock (no movement); cover//.DS_Store are git-ignored; the operator out-of-band churn (live_svelte, mercury_cms statics, mercury_live_admin site — the latter STAGED) stays excluded. Task #5 closes with this report.

### Y-7

Apollo Stage-4 ADDENDUM (the Director's three-item mid-flight update; the Y-6 BUILD-GRADE verdict STANDS — nothing here touches the rung's build surface). ITEM 1, CANON ATTRIBUTION — OVERTAKEN BY LIVE RESOLUTION: docs/echo_mq/emq.design.md was observed staged-deleted at ~02:32 (git diff --cached showed D beside the unrelated mercury_live_admin site adds — operator out-of-band, attributed per the Director's ruling); by 03:12, DURING this addendum pass, the file returned to disk (45989 bytes) and the staged deletion left the index (git diff --cached -- docs/echo_mq/ now empty; git status on the file clean) — the fork resolved on disk as accidental-deletion-restored. Final link state: NO dangle anywhere. ITEM 2 acknowledged: HEAD re-baselined mid-run (29f37f7f carries the pre-D-8 triad — the tree's triad delta vs HEAD is the expected uncommitted extension + Stage-4 sync, not a finding; rungs tracked, loaders ride as M). ITEM 3, THE echo_mq.md ADVERSARIAL PASS (additional charter rows): LINKS 18/18 resolve (deterministic walk over every relative target incl. the consumer docs, the 2.x/3.x pairs, specs.approach.md, the record, the triads, the ledger). QUOTES — 15 spot-checked against the consumer docs + the emq*.roadmap/specs sources: 11 MATCH verbatim under whitespace normalization (the §Jobs work-surface sentence; the 2.1-row trace; the walking-skeleton §Dependencies claim; the control-pattern span; the promotion-as-lane-wiring pattern; per-occurrence-or-batched; the batching-rung deferral; MAXLEN/MINID retention; the AAW figures law; the RESP3 budget; the natural-start line; the one-group-per-lane phrase); 2 MATCH with conventional case/markup adjustment, recorded not defects (the reconciliation-as-a-periodic-sweep pattern — source bold + sentence-initial capital, the consumer's patterns:106-107; the Tables read-path phrase lowercase mid-sentence at :74 vs the source's bold sentence-opener at the consumer's patterns:119, quoted exact-case at :159); 1 relocated to its true source (the non-BEAM Go-or-Python feedback line lives at emq3.roadmap.md:41, not emq3.specs.md — the doc's own attribution names no file, so no defect); 2 QUOTE-FIDELITY DEFECTS FOUND AND FIXED in-fence (Director item-3 grant): F1 the §Where-this-starts inventory quote (echo_mq.md:67) closed parentheses early without marking the intra-paren elisions — the source (the consumer's roadmap:12-18) reads "(`EchoMQ.Connector` over `EchoWire`, refereed in Appendix H)" etc.; FIXED by marking each elision ("over `EchoWire` …)", "(`EchoStore.Ring` …)", "(`EchoStore.Journal` …)"); F2 the replay-discipline quote (echo_mq.md:91) substituted "the" for the source's possessive inside the quoted span (emq3.roadmap.md:51-52, §Dependencies-recorded); FIXED by starting the quote at the contiguous source words ("run-id replay discipline gates on emq3.4's windows and emq3.5's archive"). SDK CITES — all nine verified exact at github.local/invest-api-go-sdk (the Director asked 2–3; nine cost nothing): investgo.NewClient client.go:34, NewMarketDataStreamClient :137, MarketDataStream md_stream_client.go:20, SubscribeCandle md_stream.go:47, Listen :253, Stop :306, NewMarketDataServiceClient client.go:175, NewOrdersStreamClient :247, NewOperationsStreamClient :259; go.mod module = github.com/tinkoff/invest-api-go-sdk; no .env or credential file opened. VOICE GATE: banned-marketing 0; gendered pronouns 0; first/second person 0 (bare "I" hits are the table's roman-numeral Movement column); perceptual verbs 0; fences 0 (even); exclamations 0; one honest-family hit ("stated honestly", :138) RULED in-register — the honest/honesty ban is the BCS ARTICLE gate (docs/echo/code/CLAUDE.md scopes it to docs/bcs/ articles); the program's spec-home register uses the family deliberately (D-12 "honesty over forced mapping") — recorded, not a defect. NET: echo_mq.md emerges link-sound, quote-sound after two mechanical fixes, SDK-grounded, voice-clean — the Stage-1d self-check held everywhere except the two elision-mechanics spans an adversarial source-diff catches and a self-check cannot. Sync ledger for the Director's pathspec: echo_mq.md now carries 2 Stage-4 fix edits beside Venus-2's authorship.

### Y-8

Y-n — Venus-2 canon editorial pass COMPLETE (the D-13 one-file grant: docs/echo_mq/emq.design.md, reconcile + clarity + concision). BEFORE/AFTER: 516 → 520 lines; diff 40 insertions / 36 deletions (76 changed lines, surgical Edits, one coherent sequence — no Write churn). RECONCILE HALF (7 sites, detail T-8): the orientation note (pre-program rung numbers → the roadmap bridge; git-history recoverability stated once); S-4 + §7 stories carriage updated to emq.0 AND emq.1 authored (EMQ.1-US-GATE verified at emq.1.stories.md:123); the §2 mix.exs anchor :72 → :25 (probe-verified); §4 cluster 2 + §10 seam 2 flipped open → RULED emq.4 (Stage-1b, roadmap seam 2 CLOSED) with the interim reading rule retired; §4 cluster 4 gains the as-landed note (the connector substrate adoption LANDED via Movement 0's echo_wire extraction; the Keyspace fence read stays in echo_mq; the cross-app seam carried to the scheduler rung's design gate per D-10); the §2 keyspace.ex:17-24 anchor probed MATCH (no edit). CONCISION HALF (6 sites, style-only): §0/§1-intro/§11-intro tightened; the S-1 cluster-posture run-on split; the per-site recoverability repetitions dropped; §11.11 de-duplicated against §10 seam 1. CONSTRAINT COMPLIANCE: §0–§12 + S-1…S-7 numbering and titles byte-stable (header sweep); the three emq.1.md citations re-resolve (§5 :265 · §11.10 :434 · §4 row 30 :255); the roadmap + emq2.specs.md §-reference sweep all land; every lock/ADR/deferred-family/genesis clause semantically intact (one regression self-caught and repaired — the roadmap-cited no-release clause briefly moved out of §11.11; restored; lesson L-7); §13/§14/provenance stay trimmed; fences even (2 blocks); voice/person/perceptual/pronoun gates clean on all added lines; zero git mutations; the one file only. NET READING: the canon proved dense rather than bloated — the honest concision is ~10 trimmed prose lines plus repetition removal; the net +4 is reconcile content (the bridge note + the as-landed truth). File in the working tree for the Stage-5 pathspec (the Operator ruled the emq2/emq3 four INTO the rung commit; this canon edit joins the same commit's docs/echo_mq surface).

### Y-9

Y-n — Venus-2 task-#8 COMPLETE: docs/echo_mq/specs/emq.1.design.md AUTHORED (228 lines, DRAFT — awaiting the Operator's EMQ.1-D1 approval; one new file, untracked, correctly OUTSIDE the emq.0 commit a2d599c8). SECTION MAP: status header (DRAFT, decides no fork, anchors re-verified 2026-06-13, Valkey engine line) → §0 Context (the §11.10 problem · the 2.1 constraint VERBATIM · the codemoji consumer · the as-built floor in 8 verified bullets — the schedule set, the retry wire half, the promote verb, the 'scheduled' state, the registry-less connector, the inline-script convention, the ARGV-base key convention) → §1 the carried D-10 Keyspace seam as a design INPUT (arms A inline-constant / B move-version_key / C keep-annotated, consequences each, ADR-5 interaction noted, undecided) → §2 ADR-1 scheduled enqueue (do-nothing + delayed-queue REJECTED-on-the-constraint-verbatim + chosen-proposed park-on-schedule: one new script, kind-law first act, state='scheduled', declared [job, schedule] keys, wire-side TIME for run-in, the order-theorem-under-the-fence stated explicitly: ordering attaches to MINT, never visibility) → §3 ADR-2 repeatables (do-nothing + template-id-reuse REJECTED-on-identity + chosen-proposed registration: PROPOSED emq:{q}:repeat zset + repeat:<name> record hash — a registry/grammar extension, additive minor; fresh host-side mint per occurrence) → §4 ADR-3 backoff (wire-side REJECTED on §4 row 30 HOLD; chosen-proposed pure EchoMQ.Backoff feeding retry/7 unchanged; the poison drill closes the blind spot) → §5 ADR-4 the pump (engine-eventing deferred-as-augmentation; chosen-proposed supervised OPT-IN cadence on the Consumer precedent child_spec :18/stop :78/loop :91, pure core, both sweeps) → §6 ADR-5 auto-resubscribe (caller-side-telemetry arm steelmanned; chosen-proposed connector-recorded subscription set re-issued at the :284-287 reconnect arm post-RESP3; the at-most-once gap stated honestly; unsubscribe companion flagged) → §7 THE A-1 ARGUMENT (the heart): the as-built declared-keys + ARGV-base derivation convention named precisely; the S-6 letter-vs-form reading fork FLAGGED not decided; the new scripts introduce no new derivation power; data never becomes a key; the 4 proposed conformance scenarios (schedule/repeat/backoff/resubscribe) under the additive-minor law with the prior 14 byte-unchanged → §8 SIX OPEN FORKS gathered for the Operator (Keyspace seam arm · A-1 lint reading · repeat key shape · run-at admission · pump shape/restart · unsubscribe verb) → §9 scope fences carried → the map footer. SELF-CHECK PASS: links 0 broken (loop-verified incl. the ../../echo/code path) · voice 0 · person/pronouns 0 · perceptual verbs 0 · fences 0 (even) · forward tense throughout ("emq.1 builds", PROPOSED markers). PROCESS: zero git mutations; zero compiles/tests; the roadmap-row addendum was OBE (the Director's own stronger wording landed first — not overwritten; the commit then made it true). NET EFFECT: the emq.1 build run can open at Mars the moment the Operator approves/amends this draft at the closing checkpoint — the lag-kill the E-1 lever B named.

### Y-10

Y-n — Venus-2 /x-mode response COMPLETE (under /effort max): the assessment surfaced + ONE fence-safe deliverable authored. VERDICT: "/x-mode Ship emq.0 and emq.1" cannot execute as issued from this seat — emq.0 already shipped (a2d599c8), emq.1 is gated shut (EMQ.1-INV7, 6 unruled forks), and x-mode is a Director ceremony with a missing runbook input. Routed to team-lead with the 3-step unblock path; no team spawned, no code touched (architect fence held). DELIVERABLE: docs/echo_mq/specs/emq.1.prompt.md (DRAFT, 210 lines, untracked) — the x-mode orchestration runbook for emq.1, pre-staged so the Director launches the instant the Operator rules EMQ.1-D1. SECTION MAP: status header (DRAFT, blocked-on-the-EMQ.1-D1-gate) → the rung in one paragraph → Mode (Flat-L2, slug emq-1, NOT the design-phase variant) → Settled forks (the 6 BLOCKING forks from the design draft + what's already resolved) → the as-built floor (8 re-verified anchors + the priv/-vs-inline reconcile flag) → the 5-stage pipeline with a per-stage Directive+Gate each (Stage 1 Venus reconcile+fold-the-ruling, Stage 2 Mars-1 build AS2–AS6, Stage 3 Mars-2 harden the 14+4 conformance ladder + the pump determinism loop, Stage 4 Apollo §11.2 charter+BUILD-GRADE, Stage 5 Director one LAW-4 commit, Stage 6 feedback) → risk tier (standard single-Apollo; the pump's determinism loop is the elevated-dimension gate) → the Stage-5 commit pathspec (the rung surface + the Operator-out-of-band EXCLUDE list: live_svelte/mercury_cms/mercury_live_admin) → acceptance. GROUNDING: derived from the emq.1 brief's own B0–B7 DAG + AS1–AS7 gates (re-shaped, not re-invented); decides none of the 6 forks. SELF-CHECK PASS: links 0 broken (1 fixed: ../emq2.roadmap.md), the x-mode SKILL path resolves, voice/perceptual/fences clean, forward-tense + DRAFT throughout. PROCESS: zero git, zero compiles, zero spawns. NET: the moment the Operator rules EMQ.1-D1's six forks, the emq.1 x-mode run is launch-ready — Stage 1 opens at a Venus reconcile, not at an authoring gap. Both Venus-2 granted deliverables (the canon pass in a2d599c8; the design draft) plus this pre-staged runbook now sit ready for the closing checkpoint.

## {emq-0-progress} Progress

### P-1

P — Mars Stage-2 T0 toolchain ruling: `asdf current erlang` resolves erlang 28.5.0.1 (Installed=true, source echo/.tool-versions); elixir 1.18.4 resolves. RULING: run WITHOUT any ASDF_ERLANG_VERSION override; a full _build rebuild is expected (prior artifacts were 28.1-built) and accepted. Valkey precondition green: `redis-cli -p 6390 ping` → PONG. TMPDIR=/tmp will prefix every test command. Import survey: production echo_mq/lib has 9 modules (wire trio still in place), echo_store/lib has 7 files (no shadow yet), rungs = bus 3_1..3_6 + cache 4_1..4_3 + journal 4_4 (no shadow rung yet) — the tree is in the expected pre-import state.

### P-2

P — Mars Stage-2 T1 import complete: manifests E/F/G/H all executed. E: wire trio + resp_test relocated by filesystem mv (pre-verified byte-identical to the drop's echo_wire copies, so the move == the drop shape), echo_wire.ex + mix.exs copied byte-identical, helper written ADAPTED to ExUnit.start(exclude: [:valkey]) (D13). F: echo_mq lib reduced to exactly the six bus modules (each pre-verified identical to the drop's post-move tree); mix.exs deps → [{:echo_data},{:echo_wire}] in_umbrella, extra_applications → [:logger]; module name EchoMq.MixProject UNCHANGED (the §3.6 collision exception survives). G: shadow.ex + shadow/copy.ex + litestream.ex (pre-verified additive-only: @behaviour + 4 @impl lines) + the shadow rung — all four cmp-IDENTICAL to the drop post-copy; echo_store mix.exs gains {:echo_wire, in_umbrella: true} (the drop's stale {:echomq} dep atom NOT copied — recorded source defect). H: 9 loaders re-adapted to the drop's dual-path Enum.find form via exact-string replacement (count==1 enforced per file), fallback re-spelled apps/echomq/→apps/echo_mq/; normalization diff proves 3_1..3_5 byte-identical to drop modulo the re-spell and 3_6/4_1..4_3 retain only their already-adapted header comment lines; 4_4 untouched (pre-verified identical). Lock law: mix deps.get ran; git status/diff on echo/mix.lock EMPTY — the exqlite insertion is already committed at HEAD, zero further movement (INV6 strongest form). Next: T2 per-app strict compiles (full rebuild under erlang 28.5.0.1 accepted).

### P-3

P — Mars Stage-2 T2 compiles ALL GREEN: per-app `mix compile --warnings-as-errors` exit 0 for echo_wire (forced full, 4 files), echo_mq, echo_data, echo_store; umbrella root `mix compile` green (mercury confirmed fixed — live_svelte/mercury_cms/portal_web/mercury_live_admin all generated). TWO flagged realizations were required to get echo_wire past its strict gate — both rooted in the drop's CHANGELOG "NOT COMPLETED" status (the drop never strict-compiled the extracted app): (1) mix.exs gains `elixirc_options: [no_warn_undefined: [EchoMQ.Keyspace]]` — connector.ex:417 fence/2 reads EchoMQ.Keyspace.version_key/0 at runtime while keyspace.ex stays in echo_mq and deps stay [] (in-drop precedent: echo_data/snowflake.ex:2); (2) connector.ex: the `defp send_pipe/5` block relocated VERBATIM from between handle_call clauses (:221-235, splitting the group after the Stage-1c verb clauses) to below the last handle_call clause — pure definition reordering, all handle_call clauses keep their relative order, zero behavior change; fixes the deterministic ungrouped-clauses warning the byte-identical file can never pass fresh. Both deviations flagged for Director ratification + Venus spec-side resolution. Note: _build was largely reusable under erlang 28.5.0.1 (OTP 28 major unchanged → beams forward-compatible); no phantom failures observed. Next: T3 suite authoring (echo_mq pure first), module-read-before-assert.

### P-4

P — Mars Stage-2 T3 pure suites GREEN per app: echo_wire 8 tests/0 fail (floor resp_test relocated + script_test + facade delegation suite), echo_mq 29/0 (floor keyspace_test + keyspace_extend + jobs/lanes guards + consumer child_spec + the scenarios/0 14-name pin), echo_store 48/0 (floor coherence_test + ring [gated-applier choreography for occupancy/drop/edge-wake/one-batch-drain] + directory [singleton reset per test] + cache keyspace + coherence_extend + journal pure [seq/record_many/mark/stats/last_applied/persistence/compact] + litestream [replica_url pin + behaviour conformance] + shadow dispatcher [both child_spec arms + stub dispatch] + shadow_copy [SH2 cycle/SH3 law/SH4 follow/no-op snapshot/KeyError refusals]), echo_data 65 tests+3 properties/0 fail ×3 consecutive runs (pre-existing suite green beside the 5 new test/bcs/ files). One flake found+fixed in-pass: Bcs.Supervisor traps exits → follows the dying test process down → on_exit Supervisor.stop raced :noproc — teardown now catches the already-dead exit. Observation (floor, untouchable): coherence_test.exs:8's defensive {:error, :already_started} arm draws a type-checker warning (Snowflake.start/1 is typed :ok-only); new files use the proven :ok = form. Next: T6 :valkey suites (connector/pool/table reads first).

### P-5

P — Mars Stage-2 T5 EXIT GATE GREEN + harden-pass smoke beyond it. Compiles: per-app strict (echo_wire/echo_mq/echo_data/echo_store) + umbrella root ALL GREEN after the T4 coverage keys (test_coverage threshold 0 added to the three new apps' mix.exs only; echo_data untouched). Pure suites: echo_wire 8/0 · echo_mq 63 total 0 fail (34 valkey-excluded) · echo_store 68 total 0 fail (20 excluded) · echo_data 65+3 properties 0 fail. Tagged suites (time-permitted, run clean): echo_mq --include valkey 63/0 incl. the standing gate CONFORMANCE 14/14 (Conformance.run/2 → {:ok, 14} live on 6390) · echo_store --include valkey 68/0 · echo_wire 8/0 (tag-vacuous by the T-5 placement ruling). Rung gates (time-permitted smoke of the re-adapted loaders): 3_1 PASS 5/5 · 3_2 PASS 5/5 · 3_3 PASS 6/6 · 3_4 PASS 8/8 · 3_5 PASS 6/6 · 4_1/4_2/4_3/4_4 PASS 6/6 each · shadow PASS 4/4 — every expected tail exact; the dual-path loaders resolve the wire trio through the echo_wire arm and keyspace through the fallback arm. INV checks read-only: INV5 fence holds (out-of-fence residue = operator out-of-band live_svelte/mercury_cms work, untouched); INV6 lock clean (exqlite already at HEAD, zero movement); INV9 lib diff = exactly 3 moved-out deletions + litestream +5 additive, zero call-site edits. NOT in this pass (Mars-2 harden scope): --cover runs + the per-module coverage table (AS4/T7), the record's §5 PENDING→COMPLETE flip + closure note (AS5/T8), the determinism loop.

### P-6

P — Mars Stage-3 (harden) G0–G7 status. G0: erlang 28.5.0.1 + elixir 1.18.4 resolve, PONG on 6390. G1: four per-app strict compiles GREEN. G2: umbrella compile GREEN. G3: pure suites GREEN (8 · 29-run · 48-run · 65+3props, 0 failures). G4: tagged suites — echo_wire 8/0, echo_mq 63/0 + CONFORMANCE 14/14; echo_store flaked 1/68 on first run → REMEDIATE-1: reproduced 2/30 in a capture loop — MY ring_test full-ring assertion raced Ring's drain (the head advances → occupancy 0 BEFORE the applied/batches counters move, and Ring.stats/1 assembles counters-then-occupancy from independent reads → a quiescence poll keyed on occupancy alone can return a torn snapshot). Test-side fix: the poll predicate now requires the settled counters (occupancy==0 AND applied==2 AND batches==2); verification loop 30/30 green. Lib semantics judged acceptable (monotonic counters, eventually-consistent stats snapshot) — noted for Apollo, not claimed a defect. G5 coverage (--cover, +valkey where wired): echo_wire Script 100 / RESP 42.37→93.22 AFTER authoring resp_extend_test.exs (the §5 map's RESP row names the full RESP2/3 type surface; the floor alone left it uncovered — error_reply/map/set/double+inf/-inf/nan/bool/null/bignum/verbatim/$-1/*-1/encode-part-kinds/split-frame continuations all pinned; 18 tests 0 fail) / Connector 0.00 (wire proof lives in echo_mq's suite per the T-5 placement; per-app attribution cannot see it — honest undershoot) / EchoWire 10.00 (delegation heads; proven live from echo_mq; same attribution reason); echo_mq Keyspace 100 / Conformance 96.12 / Jobs 90 / Pool 90 / Consumer 85.71 / Lanes 83.87, total 91.73 (every map-named happy+refusal path covered; residuals are unnamed passthrough/telemetry arms); echo_store EchoStore+Coherence+Journal+Keyspace+Ring+Shadow 100 / Directory 94.44 / Shadow.Copy 91.43 / Table 83.04 / LITESTREAM 7.25 — the named partial (the runtime demands the deferred binary; replica_url/2 + the behaviour exports are the reachable surface, record §7), total 79.14; echo_data Bcs+NamespaceError+PropertyStore+Archetypes+Supervisor 100 / EdgeStore 95.74 (residual: the empty-select limit arm), suite 65+3props/0 — the 44.67 app total is PRE-EXISTING modules at 0% (BrandedMap/Tree/Buckets/ChampServer/Edges/FrozenIndex/Timeline/Web), out of rung scope, and the canon mix.exs keeps Mix's default 90 threshold → the cover command exits 3 by design (report-only law; no number padded). G6: all ten rung gates PASS at exact tails (5/5 · 5/5 · 6/6 · 8/8 · 6/6 · 6/6×4 · shadow 4/4). G7 hygiene: ecc:* clean, fence = echomq:2.0.0; 101 stale emq0.* keys found — debris from the Stage-2 pre-fix run whose on_exit purges crashed on dead conns — ALL mine, swept to zero; post-loop re-scan pending to prove the post-fix purge discipline. AS5 closure: echo2-migration.md §5 status flipped PENDING → COMPLETE (2026-06-13), one line (the :15-16 Status sentence; the :18 role parenthetical left untouched per the one-line fence — flagged for Apollo). Determinism loops (≥100 per suite + tagged) running in background; report follows their tally.

### P-7

P — Mars Stage-3 determinism tally + post-loop hygiene (closes the harden ladder). THE LOOP (salvage + foreground continuation per the team lead's ruling — the background shell died with the turn after logging three loops): SALVAGED from /tmp/emq0_determinism.log: echo_wire pure ×100/0 · echo_mq pure ×100/0 · echo_store pure ×100/0 (ExUnit-random seeds each run). FOREGROUND SEEDED (the named process-touching files, --include valkey, seeds 0/1/42/312540/999999 explicit then $RANDOM): echo_mq consumer+connector+pool+conformance_run ×100/0 · echo_store table+journal+journal_wire+ring ×100/0. Plus the REMEDIATE-1 verification ×30/0 (echo_store full tagged). TOTAL post-fix: 530 loop iterations, 0 failures; seed coverage = 10 explicit (0/1/42/312540/999999 × 2 app groups) + ~190 $RANDOM + 300 ExUnit-random. echo_data ran 3 consecutive full-suite greens + the cover run (NOT in the lead's named loop scope — reported as such, not padded into the tally). G7 POST-LOOP RE-SCAN: transient singles in emq:*/ecc:* observed immediately post-loop, then ZERO on re-scan — mechanism understood and recorded: a Table flight is spawn_monitor'd (monitored, never linked), so a final in-flight L2 write can land AFTER the test's purge; the row carries the test's PX 60000 and self-expires — post-fix purge discipline holds, steady state is zero emq0*/emq:*/ecc:* keys, fence = echomq:2.0.0, mix.lock diff EMPTY. The §5 flip amended in-style with the tallies (suites + CONFORMANCE 14/14 + rungs + coverage note) per the Stage-3 instruction — still the one Status sentence, no other record edit.

### P-8

Commit-gate finding and disposition (Director, Stage 5): echo_wire's README.md — listed in the Operator's CHANGELOG scope declaration ("NEW src/echo/apps/echo_wire/{mix.exs, README.md, …}") — was absent from the as-built app. Root cause: the triad's import manifest named it in prose only ("README rationale", emq.0.llms.md:18), not in the file-by-file list, so Mars built faithfully past it and Apollo's manifest check correctly passed the list-as-written — a spec-level omission only the commit gate's CHANGELOG cross-check caught. Disposition: the file is a self-contained 27-line naming/dependency rationale with zero path references; imported VERBATIM from the drop at the commit gate (cmp byte-identical), staged into the rung pathspec. Classed as ratification mechanics (completing the declared import scope with a byte-exact relocation), the same class as the granted canon restore — no authorship, no adaptation. Mentoring adjacency for the retrospective: a manifest's prose mentions are not import rows; deliverable lists enumerate every file the scope declaration names.

## {emq-0-escalations} Escalations

### E-1

E-n — Venus-2: SHIP-FASTER lever set (Operator velocity directive). The engineering is no longer the critical path — emq.0 is build+harden green (530-loop/0); the time now goes to OPERATOR ROUND-TRIPS. The compression, surfaced for ratification: (A) BATCH EVERY PENDING RULING INTO ONE CHECKPOINT instead of N round-trips — the agenda: 1) ratify emq.0 + the LAW-4 pathspec commit; 2) rule the canon staged-deletion (restore from 29f37f7f vs name the new home — the only Stage-5 stall risk); 3) approve the EMQ.1-D1 A-1 scheduler design IF drafted by then (lever B); 4) exercise the two RECORDED pull-forward calls — emq.7 (roadmap's own least-machine-coupled note) and emq3.1–emq3.2 (the 3.x roadmap's own note; directly shortens the consumer's milestone B path since a consumer rung gates on emq3.3); 5) rule fork D below; 6) slot seam 8 (the v2 Go keyspace/store port) if M1 velocity matters — the Go workers' EchoMQ-side handoff design depends on it; 7) emq2*/emq3* file housekeeping (tracked vs private) + the L-4 findings. (B) KILL THE NEXT DESIGN-GATE LAG: EMQ.1-D1 (the A-1-compatible scheduler design + ADR, Operator-gated BEFORE any emq.1 build) currently sits SEQUENTIALLY after emq.0 closes — drafting it NOW, parallel to Apollo/Director stages, lets the Operator approve it at the SAME checkpoint that ratifies emq.0, collapsing one full round-trip; spec-side work, machine-free, grounding already banked (design §11.10's named problem, the 2.1 constraint verbatim, S-6, the triad's as-built anchors). Needs ONE Director grant (Venus-2's charter was one-file); staged as a pending board task. (C) PIPELINE DISCIPLINE forward: one spec seat drafts rung N+1's design/reconcile during rung N's build+harden (this run proved the disjoint-surface pattern, Venus-2 ∥ Mars-2); builds stay serial on the machine; one-rung-one-commit holds. (D) THE PROGRAM-LEVEL FORK (Operator's alone, surfaced with grounds): the M1 binding (the consumer scaffold AFTER Movement I) is conservative by the consumer docs' own record — "The walking skeleton (A) has no unbuilt dependency: every component it stands on carries a committed record today" (the consumer's roadmap §Dependencies); only one consumer rung's scheduled/batched follow-ups trace to emq.1. FAST ARM: start the consumer's first rung (its pure-parse gateway — ids at the edge) as its own AAW ladder right after emq.0 ratifies, parallel to the emq.1 build; M1 then marks the scaffold COMPLETE-with-Go-workers rather than scaffold-STARTS; machine contention small (the early consumer rungs are pure-core + local Ring/Journal). SAFE ARM: hold the current binding (one narrative, zero contention). Re-ruling the binding is strictly the Operator's call — D-12's milestone layer records the current one.

## {emq-0-complete} Complete

### Z-1

Rung emq.0 COMPLETE at Apollo BUILD-GRADE — Movement 0 (BCS Migration) of "EchoMQ in Three Movements" ships in the one-commit-full-pivot ratification (Stage-1b ruling). THE RUN: Venus (design reconcile + roadmap rewrite + emq.0 triad authored then D-8-extended + emq.1 triad specced) → Operator checkpoint (D-6/D-7: Candidate B ratified, codemoji = the consumer, Movement 0 reopened by the CHANGELOG delta) → Mars build (echo_wire extraction, the pluggable cache shadow, manifests E/F/G/H, three D-10 ratified deviations) → Mars harden (G0–G7 all PASS, 530 determinism iterations/0 failures, REMEDIATE 1/3 — the L-3 torn-snapshot test fix, proven load-bearing by mutation M5) → Apollo Stage 4 (BUILD-GRADE Y-6; 12/12 prompted checks, 5/5 mutation kills, zero unratified deviations, 9 triad sync edits + the record's :21 amendment; Y-7 addendum: echo_mq.md adversarial pass — 2 quote-fidelity fixes; mentoring PROPOSE-ONLY, no grant) → Venus-2 (echo_mq.md program front door with consumer milestones M0/M1/M2; the D-13 canon editorial pass — 516→520 lines, citations §5/§11.10/§4-row-30 verified stable, one self-caught citation-aware-trim regression repaired). MID-RUN OPERATOR ACTIONS folded: commits 29f37f7f/e61483a8 re-baselined the pathspec (early triad + rungs tracking already at HEAD); the canon staged-deletion ruled "Restore and reconcile" (D-13, restored + reconciled); the four emq2/emq3 line views ruled INTO the commit. THE COMMIT (pathspec, Director-only): the triad + emq.1 triad + ledger/registry + canon + roadmap (emq.0 row → shipped) + echo_mq.md + emq2/emq3 ×4 + the migration record + echo/apps/{echo_wire NEW, echo_mq, echo_store} + echo_data/test/bcs + echo/rungs (9 M + the shadow rung); EXCLUDED: echo/mix.lock (unmoved), the staged mercury_live_admin site files, all operator out-of-band churn, specs/emq.1.design.md (task #8, post-rung). CARRIED FORWARD: the wire-app↔Keyspace fence-time seam → emq.1's opening design gate; Ring.stats/1 doc-comment → next Mars pass; seam 8 (Go client) unslotted; the emq.7 + emq3.1–2 pull-forward calls + the consumer's-first-rung fork → the closing checkpoint. Decisions D-1..D-13; reports Y-1..Y-8; learnings L-1..L-7; the v1 line apps/echomq frozen untouched throughout.
