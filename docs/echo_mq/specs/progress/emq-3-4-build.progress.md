# emq-3-4-build — AAW scope ledger

## {emq-3-4-build-thinking} Thinking

### T-1 — the build §0 derivation + bootstrap (Director, Flat-L2, HIGH-risk; the design scope emq-3-4 closed Z-1, this is the BUILD scope)

SCOPE SPLIT (why a new slug). The design scope `emq-3-4` is CLOSED (Z-1 — triad+prompt authored to Arm A, V-1 ruled D-2, the forward-feature docs reconciled). Its registry ledger_path is the pre-hygiene `specs/emq-3-4.progress.md` (stale + immutable-after-init), while the canonical 69664-byte design ledger lives at `specs/progress/emq-3-4.progress.md` (T-1 reconcile · V-1 fork · D-1 doc-contract · D-2 Arm-A ruling · Y-1/Y-2 · Z-1, all FROZEN). To avoid re-splitting that ledger this BUILD opens a fresh scope `emq-3-4-build` (ledger `specs/progress/emq-3-4-build.progress.md`), cross-referencing the frozen design trail. Records-freeze honored.

THE RUNG. emq.3.4 = the flow family's FAILURE half + bulk add. The gap (a correctness gap, not a nicety): a flow parent is released ONLY by a child COMPLETING; a child that DIES — exhausts retries, lands in the morgue via @retry's dead-letter arm (jobs.ex:254-259, GROUND-TRUTH CONFIRMED: HSET last_error :254 → HSET state 'dead' :256 → ZADD KEYS[3] :257 → return 'dead' :259) — never touches the parent's :dependencies, so the parent HANGS in awaiting_children forever. The v1 flow API promises the opposite as its DEFAULT.

5W (compressed from the body). WHY: close the hang — a flow must terminate either way. WHAT: failure-policy (fail_parent_on_failure default true / ignore_dependency_on_failure) over the §6-RESERVED :failed/:unsuccessful subkeys (design.md:307 — no grammar edit, INV1 by construction) + add_bulk/3 (the last v1 producer verb) + ignored_failures/3. WHO: the program + bus consumers (robust flows) + a downstream consumer (prospective). WHEN: Movement I's flow family, the 4th slice (after 3.1 same-queue / 3.2 readable / 3.3 cross-queue). WHERE: echo/apps/echo_mq ONLY (flows.ex, jobs.ex, pump.ex, conformance.ex + 1 new test + 2 pin re-pins); NO echo_wire, NO keyspace.ex, NO admin.ex.

SOLUTION SPACE. (a) do-nothing — rejected (the parent hangs forever, a real consumer stall). (b) CHOSEN — the additive @retry dead-letter branch (existing body :254-259 BYTE-FROZEN) routing a same-queue death atomically (one EVAL, one slot) + a cross-queue death over the SAME flow:outbox + sweep emq.3.3 founded (a fail-entry KIND → @flow_fail_deliver on the parent's slot, idempotent by the :processed-class HSETNX guard now over :failed/:unsuccessful). (c) Arm B (grandchildren folded in) — NOT chosen (V-1 → D-2 Arm A; grandchildren the locked Out → emq.3.5; a zero-cost later Operator option).

SMALLEST CHANGE preserving correctness: the additive @retry branch + @flow_fail_deliver + the policy flags + parent_policy on the cross-queue child + add_bulk/3 + ignored_failures/3 + 3 conformance scenarios (47→50). NO §6 key type, NO new wire class, NO new transport, NO grammar edit. The genuinely-new surface is narrower than emq.3.3's (the cross-queue signal mechanism is INHERITED proven).

INVARIANTS AS RUNNABLE CHECKS (the gate, INV1-INV11): INV1 no grammar/wire-class/transport, additive shipped-script only (git-diff every @… Script.new shows only ADDED @retry lines + the NEW @flow_fail_deliver); INV2 declared-keys/slot — the F-1 cross-slot trap INVISIBLE on single-node 6390 (the same-queue branch + cross-queue fail-emit keys ALL on child slot {C}; @flow_fail_deliver keys ALL on parent slot {P}); INV3 byte-frozen @retry body :254-259 + schedule arm + @complete (fan-in :212-219, xq emit :205-206) + @flow_deliver (pump.ex:42); INV5/INV6 the gap closed (fail → parent dead; ignore → parent proceeds); INV7 idempotent (HSETNX, a re-delivered fail is a no-op); INV8 no-drop (the fail-emit RPUSH + morgue transition one EVAL on {C}); INV9 additive-minor 47→50 both pins; INV4 branded-id gate; INV10 lifecycle NAMED-deferred; INV11 FLAT one parent level.

BOOTSTRAP STATE. Valkey 6390 → PONG. erlang 28.5.0.1 (re-probed echo/.tool-versions; the echo/CLAUDE.md "28.1" is stale). Boundary CLEAN — git dirty/untracked = Dockerfile/Makefile/main.go + .claude/…/mesh-* + html/mesh/ (all Operator out-of-band, EXCLUDED by the runbook); nothing in echo/apps/echo_mq or docs/echo_mq touched. Conformance floor = 47 (CONFIRMED: {:ok,47} both pins, flow_cross_queue: the last scenario conformance.ex:103). The triad anchors are on-mark (minimal benign drift) — Venus re-pins precisely at Stage 0.

PIPELINE (Flat-L2, HIGH-risk → Apollo MANDATORY + ≥100 loop): Venus reconcile lag-1 (Stage 0) → Mars-1 build + STRONG adversarial self-verify as PRIMARY code-quality gate (the 2026-06-15 rebalance) → Director Stage-2 review (independent re-run + declared-keys grep + mutation spot-check net-zero) → Mars-2 harden → Apollo (fast finisher: story coverage + independent HIGH-risk verdict) → Venus post-build reconcile → Director LAW-4 pathspec commit + frontier fold. Build agents run NO git. NOTE the runbook's Stage-6 pathspec is STALE on two paths (the reorg moved the triad to specs/emq.3/emq.3.4.*; the hygiene moved the ledger to specs/progress/) — derive the real pathspec from the on-disk locations at ship.

### T-2 — Anchors re-pinned (lag-1) against current tree; design locked. Erlang 28.1 (asdf current, not hardcoded); Valkey 6390 PONG.

CURRENT ANCHORS (confirmed):
- jobs.ex @retry :225-266; dead-letter morgue body :254-260 (FIVE statements: HSET last_error :254; if att>=max :255; HSET state dead :256; ZADD dead :257; HINCRBY metrics:failed :258; return 'dead' :259; end :260); schedule arm :261-265. @complete :175-223 (xq emit :204-211, fan-in :212-219). parent_of/3 :459-471 (HMGET parent,parent_queue). retry/7 :474-497.
- flows.ex add/3 :152; @enqueue_flow_child :90-101 (writes parent_queue at :95 IF ARGV[4]!=''); add_cross_queue :285; land_children :309-323 (builds argv=[child.id,payload,parent_id,pq_arg]); children_values/3 :190 (<>":processed" precedent); add_same_queue :249.
- pump.ex @flow_deliver :42-51 (FROZEN); deliver_flow_completions :161-190; deliver_one :202-229; split_entry :236-244 (peels 3 NULs → [pq,pid,cid,result]).
- conformance.ex scenarios/0 :55-105 (last = flow_cross_queue :103, 47 entries); run/2 doc says n==47 + "forty-seven".

KIND-DISCRIMINATOR DECISION (Director point 3): complete-entry = parent_queue\0parent_id\0child_id\0result, parent_queue is a NON-EMPTY §6 name [A-Za-z0-9._-]{1,128}, never empty. fail-entry = LEADING EMPTY field + "fail" tag: ""\0"fail"\0parent_queue\0parent_id\0child_id\0error\0policy (7 fields, 6 NULs). The empty first field is ILLEGAL for a complete-entry (queue name never empty), so split_entry dispatches: first :binary.split on NUL → if head=="" it is a fail-entry (KIND="fail" the 2nd field), else the legacy 3-NUL complete peel (BYTE-UNCHANGED). Cannot collide.

@retry FAILURE BRANCH SHAPE: inserted INSIDE the morgue if-block, AFTER HINCRBY metrics:failed (:258) and BEFORE return 'dead' (:259). Gated on host-supplied keys/markers the shipped callers never pass. Same-queue arm: KEYS[5]=parent :failed, KEYS[6]=parent :unsuccessful, KEYS[7]=parent :dependencies, KEYS[8]=parent row, all on dead child's slot {C}; ARGV[7]=policy, ARGV[8]=parent bare id, ARGV[9]=parent-queue base p. Cross-queue arm: KEYS[5]=child own-slot flow:outbox {C}; ARGV[7]=policy, ARGV[8]=parent_queue (data), ARGV[9]=parent bare id; RPUSH the fail-entry. Dispatch marker: ARGV[6]='xqf' (cross-queue fail) vs same-queue (KEYS present, no xqf). Non-flow retry passes neither → branch unreached → byte-identical net diff (only ADDED lines).

@flow_fail_deliver (pump.ex, beside @flow_deliver): KEYS[1]=parent :dependencies, KEYS[2]=parent :failed, KEYS[3]=parent :unsuccessful, KEYS[4]=parent row (all {P}); ARGV[1]=child_id, ARGV[2]=error, ARGV[3]=policy, ARGV[4]=parent-queue base, ARGV[5]=parent id. fail_parent: if HSETNX(KEYS[2],child,error)==1 then move parent dead end. ignore_dep: if HSETNX(KEYS[3],child,error)==1 then DECR(KEYS[1]); at<=0 release end. HSETNX = idempotent guard.

SLOTS (F-1): same-queue branch keys ALL {C} (parent same-queue → same hashtag as dead child). cross-queue fail-emit keys ALL {C} (morgue + own outbox). @flow_fail_deliver keys ALL {P}. No script mixes slots.

### T-3 — Apollo Stage-4 HIGH-risk verification: the independent re-run + byte-freeze proof + slot soundness + story coverage

TOOLCHAIN HONEST-ROW (P-1, S-4): my context resolves `asdf current erlang` → **28.5.0.1** (source echo/.tool-versions, from the app dir echo/apps/echo_mq) — the CANONICAL line, matching the Director's context. Mars's context resolved 28.1 (the global default from the repo root, the cwd-reset trap, L-2). Both OTP 28. I ran the gate on 28.5.0.1. The asdf-context inconsistency is the documented L-2 lesson (probe from the app dir, never the repo root); not a code issue.

GATE REPRODUCED INDEPENDENTLY (Valkey 6390 PONG):
- compile --warnings-as-errors: CLEAN (EXIT 0, forced recompile of all 4 edited lib files).
- full per-app `TMPDIR=/tmp mix test --include valkey`: 4 doctests, 328 tests, 0 failures (matches Y-3). The one `[error] GenServer killed` log = the resubscribe scenario's deliberate CLIENT KILL (documented non-failure, P-3).
- Conformance: 50/50 (CONFORMANCE 50/50 tally + all 50 CONF lines ok incl. flow_fail_parent/flow_ignore_dep/flow_add_bulk).
- SCOPED determinism loop OWNING THE MACHINE (the rebalance — build+harden already ran 2× green 120/120 uncontended, so a 3rd full marathon is waste): flow_failure_test + conformance_run × 100 = **100/100 green (340s)**. The cross-queue failure scenario mints a parent + N children across queues every iteration; zero same-ms mint flake.

BYTE-FREEZE PROOF (per-attr HEAD-vs-working diff, the HIGH-risk headline):
- @retry: ZERO removed Lua lines / 17 added (the 4-arm failure branch sq:fp/sq:id/xq:fp/xq:id, inserted AFTER `HINCRBY metrics:failed` and BEFORE `return 'dead'`; the 5-statement morgue body + the schedule arm byte-identical). INV3 HIGH-risk regression bound HOLDS.
- @complete (incl. fan-in + xq emit): BYTE-IDENTICAL.
- @flow_deliver: BYTE-IDENTICAL.
- @enqueue_flow / @enqueue_flow_child / @hold_parent: BYTE-IDENTICAL (R1 confirmed — the parent_policy ARGV edit reverted; @enqueue_flow_child frozen, parent_policy via host HSET in land_children, symmetric with add_same_queue).

SLOT SOUNDNESS (the F-1 trap, empirical CRC16 via Keyspace.slot/1 — invisible on single-node 6390): same-queue @retry arm ALL one slot [3181]; cross-queue fail-emit + morgue ALL one slot [11041] = {C}; @flow_fail_deliver ALL one slot [3181] = {P}; {C}≠{P} true. No script mixes slots. Declared-keys grep: every redis.call key is KEYS[n] or a declared-root derivation (p.. / ARGV[4]..); no key read from a data value (S-6/INV2).

IDEMPOTENCY + R2-REORDER FIDELITY (the emq.3.3 L-2 lesson): producer (@retry xq emit) = `'' \0 fail \0 pq \0 pid \0 cid \0 policy \0 error`; consumer (split_fail_entry) peels leading `'' \0` then 5 NULs → {:fail, pq, pid, cid, error, policy}; the test helpers (conformance.ex fail_entry/5 + flow_failure_test.exs:893 fail_entry/5) = `Enum.join(["", "fail", pq, pid, cid, policy, error], <<0>>)` — ALL byte-identical (policy before error, error last/remainder). flow_failure_test.exs:302 asserts the byte-shape directly. The HSETNX guard fires on the REAL wire shape, not a phantom — the crash-window re-delivery tests pass for the RIGHT reason.

JOB 1 (story coverage): the story-gen mechanism IS wired (lib/mix/tasks/echo_mq.stories.ex + test/support/echo_mq/story.ex + test/stories/). emq.3.4 shipped its failure capability WITHOUT a story (only flows + groups existed, both pre-dating this build). Wrote test/stories/flows_failure_story_test.exs (NEW, :valkey, 4 passing BDD scenarios driving the real Flows/Jobs surface: fail-parent default, ignore-dep proceed + ignored_failures/3 + children_values disjoint, empty-parent read, add_bulk N flows). `mix echo_mq.stories` regenerated docs/echo_mq/stories/flow-failure-handling.stories.md + README.md (3 features / 12 scenarios). Same-queue forms only (the DSL's flat scenario shape; the cross-queue eventually-consistent path is exhaustively covered in flow_failure_test.exs/conformance).

FINDING (non-blocking-to-contract, contradicts D-1 R3): conformance.ex carries ~36 lines of mix-format reflow on PRE-EXISTING apply_scenario probe bodies (e.g. the `dead` scenario's `with` clauses single-line at HEAD → multi-line now) that R3 (a locked Director REMEDIATE) required reverted; Y-3 claimed R3 fully cleared but it was only PARTIAL (the scenario keyword-list registry + flow_cross_queue entry were restored; the older probe bodies were not). The prior-47 CONTRACT registry (the keyword-list keys + contract strings) IS byte-identical, both pins re-pinned 47→50, and every prior scenario drives identical wire commands + asserts identical verdicts — so INV9's behavioral guarantee and INV3's Lua byte-freeze teeth HOLD. echo_mq has NO .formatter.exs (the formatter is NOT supposed to run here). Routed to the Director (R3 was the Director's lock + the Director owns the pathspec).

## {emq-3-4-build-report} Report

### Y-1 — Stage-0 lag-1 reconcile VERDICT: **BUILD-GRADE** (the emq.3.4 triad is build-ready against the CURRENT echo/apps/echo_mq tree; Arm A; no open fork). The third probe in the lag-1 chain (after the design-cycle T-2 + the build-bootstrap T-1) — caught one substance-relevant re-pin, no STALE/INVENTED/MISSING blocker.

== GROUNDING DELTA (anchor → as-built file:line re-probed 2026-06-15 → verdict) ==
flows.ex:  add/3 :152 MATCH · children_values/3 :190 MATCH (the `<> ":processed"` precedent for ignored_failures/3, confirmed :191) · dependencies/3 :227 MATCH · add_same_queue/5 :249 / add_cross_queue/5 :293-ish / land_children/4 :309-ish MATCH · @enqueue_flow :39 / @hold_parent :73 / @enqueue_flow_child :90 MATCH (the child HSET writes 'parent'+'parent_queue' at :95-ish — parent_policy ADDS here)
jobs.ex:  @retry :225 MATCH · @complete :175 MATCH (fan-in `if KEYS[3] and was_active==1` :212, xq emit `if ARGV[6]=='xq'` :204 + RPUSH :206 — byte-frozen) · complete/5 :412 MATCH · retry/7 :474 MATCH · parent_of/3 :459 MATCH (HMGET parent+parent_queue — EXTEND to parent_policy) · @extend_locks :730 MATCH (the A-1 `base..'job:'..id` slot-rooted-ARGV precedent confirmed)
  ** THE DEAD-LETTER ARM (the byte-frozen bound) — [RECONCILE re-pinned, substance]: range :254-259 MATCH, but the body is FIVE statements, not the three the brief quoted — HSET last_error :254 / HSET state 'dead' :256 / ZADD KEYS[3] :257 / **HINCRBY p..'metrics:failed' count 1 :258** / return 'dead' :259. The brief's `HSET last_error; HSET state 'dead'; ZADD <dead>` abbreviation OMITTED the :258 HINCRBY. Re-pinned in all three triad files so the byte-freeze + Apollo's only-added-lines check cover ALL FIVE. **
pump.ex:  @flow_deliver :42 MATCH (byte-frozen) · sweep/1 :126 MATCH · deliver_flow_completions/3 :161 MATCH · deliver_one/2 :202 MATCH (was unpinned in brief; confirmed) · split_entry/1 :236 MATCH
keyspace.ex:  queue_key/2 :14 + job_key/2 :18 MATCH — gates BrandedId.valid?/1 + RAISES (INV4); UNEDITED, the subkeys compose via job_key(q,parent)<>":failed"/":unsuccessful"
conformance.ex:  scenarios/0 :55; live count **47** CONFIRMED (last scenario flow_cross_queue :103; run/2 doc/n==47 :110; both pins {:ok,47} run_test:42 + scenarios_test 47-name @run_order:22) — floor 47, post-build 50
admin.ex:  @drain wipe() :84-90 (DEL jk, jk..':logs') + del_job :150-152 (DEL jk, jk..':logs', jk..':lock') — the FIXED enumerations; UNTOUCHED (the N1/B6 carry, deferred)
design canon §6:  design.md:307 `sub ∈ {lock, logs, dependencies, processed, failed, unsuccessful}` CONFIRMED — :failed/:unsuccessful PRE-RESERVED at the founding → emq.3.4 adds NO grammar edit, NO new §6 type (INV1 by construction)

== LIVE CONFORMANCE FLOOR == 47 CONFIRMED (re-probed, not hardcoded). emq.3.4 → 50 (flow_fail_parent + flow_ignore_dep + flow_add_bulk; prior 47 byte-unchanged; both pins re-pinned 47→50).

== OUT-OF-BAND (lag-1 law) == git: NO Operator landing touched echo/apps/echo_mq/lib since the emq.3.3 ship (7de4e90a). Tree dirt in-region = only the two untracked emq-3-4-build ledger files (this run's). Operator OOB work = docs/echo/mesh/** + .claude/skills/echo-mq-writer/* — outside the boundary, correctly excluded.

== TRIAD EDITS (re-pin only — the one substance drift) ==
- emq.3.4.md: §0 morgue-body quote + D3 byte-frozen quote → the FULL five-statement branch (HINCRBY metrics:failed :258 named).
- emq.3.4.stories.md: US3 acceptance byte-frozen list → five statements.
- emq.3.4.llms.md: the jobs.ex anchor block (Mars's build target) → the five-statement morgue branch, HINCRBY :258 bolded as "freeze ALL five".

== VERDICT == BUILD-GRADE. Every anchor MATCH or [RECONCILE re-pinned]; the one re-pin (the omitted HINCRBY metrics:failed in the byte-frozen body) tightens the freeze target — it does not block. Arm A locked (D-2), no fork re-opened. The dead-child gap (the parent hangs when a child dies via @retry's dead-letter arm) is the genuine target; the §6 subkeys are pre-reserved; the cross-queue mechanism (outbox+sweep) is inherited proven. Mars may build to the body.

NOTE (aaw): agent_register returned fake_n_signal=true (registered=2 > spawned=1) — a benign spawn-then-register double-count for the single architect, NOT a phantom agent.

### Y-2 — emq.3.4 BUILD (Stage 1) COMPLETE, BUILD-GRADE. The flow failure-policy + bulk add, Arm A (failure-policy + bulk; grandchildren=Out→emq.3.5, NOT built).

CHANGE LIST (echo/apps/echo_mq only — NO echo_wire, NO git):
- flows.ex (EDIT): add/3 per-child fail_parent_on_failure(def true)/ignore_dependency_on_failure(def false); @enqueue_flow_child carries parent_policy ARGV[5]; same-queue policy via host HSET after the BYTE-FROZEN @enqueue_flow (E-1 realization); add_bulk/3 NEW (fail-closed per flow, {:ok,[{pid,[cid]}]}); ignored_failures/3 NEW (:unsuccessful HGETALL); policy_token/1 helper.
- jobs.ex (EDIT): @retry ADDITIVE failure branch (sq:fp/sq:id/xq:fp/xq:id via ARGV[7] marker), inserted after HINCRBY metrics:failed, before return 'dead' — morgue body + schedule arm BYTE-FROZEN (git: 0 removed Lua lines, 19 added); parent_fail_of/3 + policy_arm/1 NEW; retry/7 host extension appends per-arm keys/ARGV.
- pump.ex (EDIT): @flow_fail_deliver NEW (HSETNX-guarded, all {P}); split_entry/1 KIND-dispatch (leading-empty-field 'fail' tag → split_fail_entry; else byte-unchanged complete parse via split_complete_entry); deliver_one/2 fail branch. @flow_deliver BYTE-FROZEN (0 removed).
- conformance.ex (EDIT): flow_fail_parent/flow_ignore_dep/flow_add_bulk + 3 probes + helpers (fail_entry byte-faithful to @retry, hgetall/hlen/claim_id); count→50; moduledoc/run doc.
- test/conformance_run_test.exs + test/conformance_scenarios_test.exs (EDIT): re-pinned 47→50 both.
- test/flow_fanin_test.exs (EDIT — E-2): the ONE prior-suite test changed — emq.3.1 "dead child does not decrement, parent hangs" updated to the emq.3.4-INV5 contract (default policy → parent dead, child in :failed); the gap this rung closes.
- test/flow_failure_test.exs (NEW, :valkey, 20 tests).

REALIZATION-OVER-LITERAL (2, both reported as escalations):
- E-1: the SAME-QUEUE child's parent_policy. The brief (llms Req1) says "no new row field"; the body D2 says "host-read, never a data-rooted Lua key". Built (A): write parent_policy on EVERY flow child (same-queue via host HSET, @enqueue_flow byte-frozen; cross-queue via @enqueue_flow_child ARGV). Honors the body's intent; deviation = same-queue child ALSO carries parent_policy (flows.ex add_same_queue + @enqueue_flow_child). @enqueue_flow_child IS edited (brief explicitly names it the write site; supersedes the stale "untouched as scripts").
- E-2: INV3⇄INV5. flow_fanin_test.exs:139 asserted the exact gap emq.3.4 closes; updated to the new contract (a this-change-staled-it debt). INV3's byte-freeze (@complete/@flow_deliver/@retry-morgue) intact; every non-failure prior path byte-identical.

KIND-DISCRIMINATOR: fail-entry = ""\0"fail"\0parent_queue\0parent_id\0child_id\0error\0policy. The LEADING EMPTY field is illegal for a §6 queue name (complete-entry's first field), so cannot collide; split_entry peels the first NUL — empty head → fail-entry, else the byte-unchanged complete parse.

GATES (Erlang 28.1 [asdf current], Valkey 6390 PONG): compile --warnings-as-errors clean; full per-app TMPDIR=/tmp mix test --include valkey = 4 doctests, 323 tests, 0 failures; Conformance.run/2 → {:ok,50}; both pins 50; mix format clean; 25-iter sanity loop green (full ≥100 = Stage 3).

ADVERSARIAL BATTERY (Mars = primary gate): DECLARED-KEYS + SLOT (F-1): same-queue @retry arm ALL {C} (13053), cross-queue fail-emit ALL {C} (13053), @flow_fail_deliver ALL {P} (15113); {C}≠{P}; no script mixes slots; every key KEYS[n] or grammar-rooted. BYTE-DIFFS: @retry 0 removed/19 added; @complete + @flow_deliver + @enqueue_flow bodies byte-identical (post-format re-verified). MUTATION KILL-RATE 5/5 (SCRIPT FLUSH before each): M1 sq:fp row-state [SURVIVED first → exposed a coverage gap (get_job_state reads set membership) → strengthened test to assert row state → CAUGHT]; M2 sq:id DECR→INCR CAUGHT; M3 xq:fp KIND CAUGHT; M4' id-arm HSETNX→HSET CAUGHT (double-DECR); M5 split_entry dispatch CAUGHT. FINDING: M4 (fp-arm HSETNX→HSET) SURVIVED correctly — fail_parent's morgue is terminal/idempotent by nature; the load-bearing guard is the id-arm's (M4'). All reverted net-zero by inverse Edit.

INV1-INV11: INV1 no new key type (:failed/:unsuccessful §6-reserved)/no wire class/no transport/one additive @retry branch ✓; INV2 declared-keys + single slot per script ✓; INV3 @retry only-added-lines + @complete/@flow_deliver byte-unchanged + prior 47 byte-unchanged + 322 prior tests green ✓; INV4 every id gated at Keyspace.job_key/2 (add/add_bulk/ignored_failures raise on ill-formed) ✓; INV5 fail_parent → parent dead (same-queue atomic + cross-queue sweep) ✓; INV6 ignore_dep → DECR + :unsuccessful + proceed, disjoint reads ✓; INV7 idempotent (HSETNX, re-deliver no-op) ✓; INV8 emit atomic with morgue (one EVAL, no drop) ✓; INV9 conformance +3 additive, prior 47 byte-unchanged, both pins 50 ✓; INV10 :failed/:unsuccessful lifecycle NAMED in body, admin.ex untouched, zero cleanup added ✓; INV11 FLAT one parent level, no grandchildren ✓.

UNTOUCHED (proven): keyspace.ex 0, admin.ex 0, echo_wire 0 files, echomq frozen v1 untouched, @complete/@flow_deliver/@enqueue_flow/@hold_parent bodies byte-frozen.

HONESTY: same-queue failure IS atomic; cross-queue failure is EVENTUALLY-CONSISTENT (sweep tick) — never "atomic across queues" anywhere. No brief gap unresolved (E-1/E-2 resolved by realization, reported). Stage-3 owes the full ≥100 loop + Apollo MANDATORY (HIGH-risk).

### Y-3 — Stage 3 (harden) COMPLETE. All 3 REMEDIATE items cleared, full ladder + ≥100 loop green, 3 hardening shapes added. NO git used (L-3; the proposed git checkout was correctly DENIED — all restores by surgical inverse Edit).

REMEDIATE (D-1) cleared:
- R1 (INV1 restored): @enqueue_flow_child reverted to BYTE-FROZEN (4-ARGV, parent_policy ARGV[5] removed; Lua body 0 added/removed lines). Cross-queue parent_policy now written by a host HSET on the child row after the @enqueue_flow_child EVAL (land_children), symmetric with add_same_queue. ONLY shipped-script edit = @retry; only new script = @flow_fail_deliver. Crash-in-window defaults to fp (policy_arm/1 floor) + the cross-queue add is fail-closed/retried — same safety as same-queue.
- R2 (fail-entry payload): reordered policy BEFORE error, error LAST/the remainder ('' \0 fail \0 pq \0 pid \0 cid \0 policy \0 error), mirroring the complete-entry's result-last design. Updated @retry emit (both xq arms) + split_fail_entry parse + both byte-faithful fail_entry test helpers + the byte-exact LRANGE assertion + every shape comment. Round-trips green.
- R3 (mix-format churn revert): restored every PRE-EXISTING line mix format reflowed — jobs.ex parent_of HMGET + browse ZRANGE → single-line; pump.ex fire_repeats Logger → single-line; conformance.ex scenarios/0 → one-line-per-scenario (the PRIOR-47 byte-identity restored: 0 removed prior-scenario lines; flow_cross_queue gained only the required trailing comma + 3 new appended). Did NOT run mix format on echo_mq lib/test; also un-wrapped the 5 format-split test signatures (the E-2 test + 4 in flow_failure_test) back to the suite's inline `test "...", %{` convention.

TOOLCHAIN re-probe (MY machine, reporting the probe not a doc — N1): asdf current erlang → 28.1; live VM → OTP 28 / ERTS 16.1; Valkey 6390 PONG. (The Director's 28.5.0.1/ERTS 16.4.0.1 is a different machine; both are OTP 28.)

3 HARDENING SHAPES added to flow_failure_test.exs (the Director's named list):
- Multi-child: (a) a same-queue parent + 3 children (one completes → :processed, one ignore-dies → :unsuccessful+DECR, one fail-dies → :failed+parent dead); the fail-parent death is decisive (parent DEAD); the three subkeys disjoint. (b) a cross-queue parent + 3 children across 3 distinct queues; two ignore-dep proceed, the LAST (fail-parent) deliver kills the parent (eventually-consistent across independent sweeps).
- Crash-window re-deliver: (a) fail_parent — @flow_fail_deliver applied (parent dead) then the SAME fail-entry re-injected (crash before LTRIM) + re-drained → :failed HSETNX guard fires, parent dead exactly once, :failed HASH 1 entry. (b) ignore-dep multi-child — re-injected survivor does NOT double-DECR (deps stays 2), then the rest die → deps 0, parent released, 3 :unsuccessful entries exactly once.
- fail_parent ONE-LEVEL B3 boundary: a failed flow PARENT does NOT auto-propagate to a grandparent — modelled the grandparent→parent upward edge by hand, failed the parent's child (parent → dead), asserted the GRANDPARENT untouched (deps still 1, not dead, not in :failed; the upward `parent` edge never walked). Grandchildren/recursion stays OUT (V-1 → emq.3.5).

FULL GATE LADDER (all run before reporting):
- compile --warnings-as-errors clean.
- Full per-app suite (INV3 no-regression): 4 doctests, 328 tests, 0 failures (was 323 Stage-1 + 5 hardening).
- Conformance.run/2 → {:ok, 50}; both pins 50; the prior 47 byte-unchanged (R3 restored their byte-identity).
- Prior emq.3.{1,2,3} flow + emq.1/2 representative suites: 95 tests 0 failures (flow_fanin passes its E-2-updated test).
- ≥100 DETERMINISM LOOP OWNING THE MACHINE: 120 iterations of flow_failure_test + conformance_run, ALL GREEN (402s) — no same-ms mint flake across the cross-queue failure scenario (which mints a parent + N children across queues).
- BYTE-FREEZE per-attr git diff: @retry 0 removed Lua (morgue 5 statements + schedule arm byte-identical, only the failure branch added); @complete 0 changes; @flow_deliver 0 removed; @enqueue_flow/@hold_parent/@enqueue_flow_child 0 removed Lua (all byte-frozen). keyspace.ex/admin.ex/echo_wire untouched (0 diff).

TOUCHED (the rung's surface, 8 files): flows.ex/jobs.ex/pump.ex/conformance.ex (EDIT), conformance_run_test.exs/conformance_scenarios_test.exs (EDIT, pins 50), flow_fanin_test.exs (EDIT, E-2 ratified), flow_failure_test.exs (NEW :valkey, 25 tests). Ready for Apollo (Stage 4, MANDATORY HIGH-risk).

### Y-4 — Apollo Stage-4 HIGH-risk verification VERDICT: **BUILD-GRADE** (one non-blocking FINDING routed to the Director; one PROPOSED guardrail diff for ratification)

== VERDICT == **BUILD-GRADE.** Every Deliverable D1–D7 and Invariant INV1–INV11 is MATCH against the as-built tree; the HIGH-risk byte-freeze + slot-soundness + idempotency-fidelity are independently PROVEN; the gate reproduced clean. The one finding (R3 partial-revert, L-3) is COSMETIC — it does not break an invariant, change a scenario contract, or touch the byte-frozen Lua — so it does not BLOCK the grade; it is routed to the Director because R3 was a Director lock and the Director owns the pathspec (the residual ~36 reflow lines are the Director's call to require-revert-or-accept before commit).

== POST-BUILD DELTA TABLE (promise → as-built file:line → verdict) ==
D1 scope gate Arm A (V-1→D-2)            → ledger D-2 + triad authored to Arm A        → MATCH (no grandchildren built)
D2 policy flags + add_bulk/3             → flows.ex add/3 :177, add_bulk/3 :212        → MATCH (both-flags-true→id synced; add_bulk SEQUENTIAL not pipelined synced)
D2 parent_policy (host HSET, scripts frozen) → flows.ex :391 (sq) + :462 (xq)          → MATCH (R1: @enqueue_flow + @enqueue_flow_child BYTE-IDENTICAL)
D3 same-queue @retry failure branch      → jobs.ex :286-298 (sq:fp/sq:id arms)         → MATCH (morgue 5-stmt + schedule arm byte-frozen; 0 removed/17 added)
D3 host parent_fail_of/3 + retry/7        → jobs.ex :527 + :590                          → MATCH
D4 cross-queue fail-emit (xq arms)       → jobs.ex :299-302 (one EVAL on {C})           → MATCH (INV8 no-drop)
D4 @flow_fail_deliver + KIND dispatch    → pump.ex :53 + split_entry :316 + deliver_one → MATCH (@flow_deliver BYTE-IDENTICAL)
D5 lifecycle NAMED, admin.ex untouched   → body §D5/B6 + admin.ex 0 diff                → MATCH
D6 ignored_failures/3                    → flows.ex :286                                → MATCH
D7 conformance 47→50 + both pins         → conformance.ex :110-112 + 2 pins → {:ok,50}  → MATCH (contract registry byte-identical)
D7 :valkey suite + ≥100 loop + byte-proof → flow_failure_test.exs (25) + loops + diffs   → MATCH
INV1 no grammar/wire-class/transport, 1 additive script → git diff (only @retry + @flow_fail_deliver) → MATCH
INV2 declared-keys + single slot/script  → grep + empirical CRC16 (sq=[3181], xq+morgue=[11041]={C}, ffd=[3181]={P}) → MATCH
INV3 byte-unchanged except @retry branch → @complete/@flow_deliver/@enqueue_flow/_child/@hold_parent ALL byte-identical → MATCH
INV4 branded-id gate everywhere          → add/add_bulk/ignored_failures gate Keyspace.job_key/2 → MATCH
INV5 fail_parent → parent dead           → flow_fail_parent scenario + flow_failure_test (sq atomic + xq sweep) → MATCH
INV6 ignore_dep → parent proceeds        → flow_ignore_dep scenario + :unsuccessful XOR :processed disjoint → MATCH
INV7 idempotent (HSETNX, re-deliver no-op) → crash-window tests + byte-faithful fail_entry re-injection → MATCH
INV8 emit atomic w/ morgue (one EVAL)    → jobs.ex xq arm RPUSH inside the morgue block → MATCH
INV9 additive-minor 47→50 both pins      → contract registry byte-identical; pins 50    → MATCH (probe-body reflow = the L-3 finding, non-blocking)
INV10 :failed/:unsuccessful lifecycle NAMED → body D5; admin.ex untouched (0 diff)        → MATCH
INV11 FLAT one parent level, grandchildren OUT → B3 boundary test (grandparent untouched) → MATCH

== GATE REPRODUCED (independent, Valkey 6390, erlang 28.5.0.1 the canonical) ==
compile --warnings-as-errors CLEAN (EXIT 0, forced recompile); full per-app `mix test --include valkey` = 4 doctests + 328 tests + 0 failures; Conformance 50/50 (all 50 CONF lines ok); SCOPED determinism loop (flow_failure_test + conformance_run) OWNING THE MACHINE = 100/100 green (340s) — plus Mars's 2× 120/120 uncontended (Y-2/Y-3); no 3rd full marathon (the rebalance).

== ADVERSARIAL BATTERY (each + what it found) ==
- BYTE-FREEZE (per-attr HEAD-vs-working): @retry 0 removed / 17 added (the 4-arm branch between HINCRBY metrics:failed and return 'dead'; the 5-statement morgue + schedule arm byte-identical). @complete + @flow_deliver + @enqueue_flow + @enqueue_flow_child + @hold_parent ALL byte-identical. HIGH-risk regression bound HOLDS.
- DECLARED-KEYS/SLOT (F-1, empirical CRC16): same-queue @retry arm ALL slot 3181; cross-queue fail-emit + morgue ALL slot 11041 = {C}; @flow_fail_deliver ALL slot 3181 = {P}; {C}≠{P} true. No script mixes slots; every key KEYS[n] or declared-root (p.. / ARGV[4]..); no data-value key.
- IDEMPOTENCY + R2-REORDER FIDELITY (the emq.3.3 L-2 keystone): producer @retry xq emit = consumer split_fail_entry peel = both test helpers (conformance.ex + flow_failure_test.exs:893) = `'' \0 fail \0 pq \0 pid \0 cid \0 policy \0 error` byte-identical (policy before error, error last). flow_failure_test.exs:302 asserts the byte-shape. The HSETNX guard fires on the REAL wire shape — crash-window re-delivery passes for the RIGHT reason.
- NO-DROP (INV8): the cross-queue fail-emit RPUSH + the morgue transition are one EVAL on {C} (verified in the @retry body + the flow_fail_parent pre-sweep assertion LLEN=1 + child dead).
- GAP-CLOSED: fail_parent_on_failure child death → parent dead (INV5, flow_fanin E-2 + flow_failure_test); ignore_dependency_on_failure death → parent proceeds (INV6). The hang is no longer reachable.
- HONESTY: grep for "atomic across queues" finds ONLY negations (code comments + triad state "NOT atomic across queues"). No false atomicity claim. Same-queue IS atomic; cross-queue eventually-consistent — both stated honestly.
- BOUNDARY: admin.ex / keyspace.ex / echo_wire UNTOUCHED (0 diff).

== STORY-GEN COVERAGE (Job 1) == The mechanism IS wired (mix echo_mq.stories + test/support/echo_mq/story.ex + test/stories/). emq.3.4 shipped WITHOUT a failure story (only flows + groups pre-dated this build). WROTE test/stories/flows_failure_story_test.exs (NEW, :valkey, 4 PASSING BDD scenarios on the real Flows/Jobs surface: fail-parent default, ignore-dep proceed + ignored_failures/3 + children_values disjoint, empty-parent read, add_bulk N flows). `mix echo_mq.stories` regenerated docs/echo_mq/stories/flow-failure-handling.stories.md + README.md (3 features / 12 scenarios). Same-queue forms (the DSL's flat shape); the cross-queue path is exhaustively covered in flow_failure_test.exs/conformance.

== SPEC FILES SYNCED == emq.3.4.md: all 8 DoD boxes flipped [ ]→[x] with as-built file:line; 2 realization syncs recorded in the DoD (both-flags-true→ignore-dep; add_bulk SEQUENTIAL not pipelined); the L-3 finding noted on the D7 box; a 9th DoD box added for the acceptance story (Job 1). (Stage 5 Venus does the full body forward-tense→present-tense sync + the remaining anchor re-pins.)

== TOOLCHAIN HONEST-ROW == My context = erlang 28.5.0.1 (echo/.tool-versions, from the app dir) — the CANONICAL line, matching the Director. The asdf-context inconsistency Mars hit (28.1 from the repo root) is the L-2 cwd-reset trap; probe from the app dir. Both OTP 28.

== MENTORING ROUTED ==
- Mars (build-fidelity, DURABLE — L-3 in the ledger): a "REMEDIATE/byte-freeze cleared" CLAIM is proven by re-running the diff to CLOSURE over the WHOLE touched file, never by the lines re-touched (R3 was partial; this is the 3rd self-report-vs-independent-proof gap this rung after L-2's erlang-probe + M4' kill-rate). PROPOSED guardrail diff for the echo-mq-implementor §6 battery (Director-ratified, sharpen-don't-stack — see the SendMessage). Not applied.
- The N1/N2 D-1 NOTES CONFIRMED: N1 — Mars now reports the probe (Y-3 reported his real 28.1 probe, not the stale doc); the practice held. N2 — the M4' kill-rate was correctly re-characterized in L-2 (HSETNX→HSET keeps the ==1 guard sound → SURVIVES; guard-removal is the load-bearing mutation, caught). The shipped guard is HSETNX (byte-correct, independently re-confirmed).

== EXCLUDE FROM THE RUNG COMMIT (Operator/other-track OOB, flagged) == docs/echo_mq/specs/emq.1.llms.md, emq.1/emq.1.design.md, emq.1/emq.1.prompt.md, epics/emq.epic.{0,1}.md, testing/emq.1.testing.md, testing/emq.testing.progress.md, emq.3.4.todo.md (Task #8 emq1 v1→v3 matrix + epics/testing churn — NOT emq.3.4). INCLUDE the 2 new story artifacts (test/stories/flows_failure_story_test.exs + docs/echo_mq/stories/flow-failure-handling.stories.md) + the README.md change in the pathspec.

### Y-5 — R3 RESIDUAL CLOSED (Apollo's pre-commit finding). The last code touch before the LAW-4 commit.

THE FINDING: Apollo found R3 was only PARTIALLY cleared — conformance.ex still carried ~36 mix-format-reflow lines on PRE-EXISTING apply_scenario probe BODIES (the `dead`/`backoff`/`obliterate`/`telemetry`/`order`/`rate`/`lane_depth`/etc. probes: single-line with-clauses at HEAD → multi-line after my Stage-1 mix format). My Y-3 R3 restored the scenarios/0 registry + the flow_cross_queue entry but missed the probe bodies BELOW it (≈:55-112 region down through the probes). Genuinely cosmetic (the prior-47 CONTRACT registry was byte-identical, both pins re-pinned, behaviour unchanged — it did NOT move Apollo's BUILD-GRADE verdict), but R3 was to clear ALL formatter churn so the commit is the rung's faithful surface (echo_mq has no .formatter.exs).

THE FIX (the reliable technique, since hunting scattered reflow hunks is what let the residual survive): copied the HEAD conformance.ex into the working tree as a byte-identical base (NOT git checkout of the rung's other files — only `git show HEAD:<file>` read + cp of this ONE file, which carried only my own reflow, no other agent's work; L-3 respected), then RE-APPLIED only my 5 genuine emq.3.4 additions by Edit: (1) the moduledoc count (forty-seven→fifty + the failure-half sentence + the Chapter line); (2) scenarios/0 (flow_cross_queue gains the required trailing comma + 3 appended); (3) the run/2 doc count block; (4) the 3 new apply_scenario probes (flow_fail_parent/flow_ignore_dep/flow_add_bulk, with the R2-correct fail_entry order); (5) the 4 new helpers (fail_entry/hgetall/hlen/claim_id). Every pre-existing line is now HEAD-byte-identical by construction. Did NOT run mix format on echo_mq.

THE PROOF (the §5 whole-file standard the guardrail now requires): `git diff echo/apps/echo_mq/lib/echo_mq/conformance.ex` over the WHOLE file shows exactly 8 removed-side lines, ALL genuine emq.3.4 changes (the 4 moduledoc/Chapter doc-line replacements + the flow_cross_queue comma + the 3 run/2-doc count-block lines) — ZERO unintended reflow residual; the ~36 probe-body reflows are GONE. Behaviour-neutral: compile --warnings-as-errors clean; Conformance.run/2 → {:ok,50}; both pins green; full per-app suite 4 doctests, 332 tests, 0 failures (the count grew 328→332 from a parallel Stage-5 test addition — my change regressed nothing). Byte-freeze unaffected (the reflow was Elixir probe bodies, never the Lua heredocs or the other files). Boundary held: only conformance.ex touched this turn.

R3 is now FULLY closed across all of conformance.ex. The increment is the rung's faithful surface, ready for the Director's LAW-4 commit (Stage 6).

### Y-6 — Stage-5 POST-BUILD RECONCILE complete: the triad is SYNCED to the as-built surface. The body remains authoritative; every drift the build introduced is now recorded as as-built fact (not forward-intent). Edited ONLY the spec triad; NO .ex/.exs; NO git.

== WHAT APOLLO ALREADY DID (Y-4, confirmed, not redone) == emq.3.4.md DoD: all 8 boxes flipped [ ]→[x] with as-built file:line + a 9th box for the acceptance story; the two realization syncs noted inline (both-flags-true→ignore-dep; add_bulk SEQUENTIAL); the L-3 finding noted on D7.

== WHAT STAGE-5 SYNCED (the architect's remaining duty Apollo routed to me) ==
1. THE AS-BUILT-SURFACE ANCHOR BLOCK (References, the load-bearing block emq.3.5 reconciles against) — fully re-pinned from PRE-build to POST-build, caption changed "pre-build reconcile"→"POST-build reconcile, Stage 5". Re-probed every anchor on the live tree:
   flows.ex: add/3 :152→:181 · add_bulk/3 :218 (NEW) · ignored_failures/3 :295 (NEW) · children_values/3 :190→:261 · dependencies/3 :227→:332 · policy_token/1 :359 (NEW) · land_children :444-460
   jobs.ex: @retry :225→:252 · dead-letter morgue body :254-259→:281-303 (5 statements byte-frozen; ADDITIVE failure branch :286-302 between HINCRBY metrics:failed :285 and return 'dead' :303, dispatched on a SINGLE combined marker ARGV[7]='sq:fp'/'sq:id'/'xq:fp'/'xq:id' — NOT the 'xqf' the design-time T-2 sketched) · @complete :175 UNCHANGED · complete/5 :412→:456 · parent_of/3 :459→:503 · parent_fail_of/3 :535 (NEW) · policy_arm/1 :559 · retry/7 :474→:593
   pump.ex: @flow_deliver :42 UNCHANGED (byte-frozen) · @flow_fail_deliver :78 (NEW) · deliver_flow_completions/3 :161→:205 · sweep/1 :170 · split_entry+split_fail_entry/split_complete_entry KIND-dispatch
   conformance.ex: 47-set→50-set; flow_fail_parent :110 / flow_ignore_dep :111 / flow_add_bulk :112 + probes (prior-47 contract registry byte-unchanged)
   keyspace.ex + admin.ex: UNEDITED, git-verified 0 diff (re-affirmed)
2. THE VOICE FLIP (forward→present, SHIPPED): Status header SPECCED→SHIPPED (CONFORMANCE 50/50; risk WAS HIGH, Apollo+≥100 PASSED); Goal lead "emq.3.4 builds"→"ships (SHIPPED — as-built)"; Deliverables preamble forward-named→SHIPPED; stories blockquote → SHIPPED 2026-06-15, every story PASSED (Apollo Y-4), the GWT below = the criteria verified-against; brief (.llms) → a [SHIPPED — DISCHARGED] banner (forward-tense + pre-build anchors marked design-time; read the body for the shipped surface).
3. THE SPEC⇄AS-BUILT DRIFT CORRECTIONS (the architect's signature duty — the build's realization vs the forward plan):
   - E-1: D2 + Scope-In(1) said "a same-queue child needs no new field, only cross-queue carries parent_policy". AS-BUILT: EVERY child carries parent_policy via a uniform host HSET (same-queue AND cross-queue), because @enqueue_flow + @enqueue_flow_child stayed BYTE-FROZEN (R1, INV1) so the policy could not ride a script ARGV. Synced both spots; INV2 holds (host-read, never a data-rooted Lua key).
   - add_bulk: "pipelined" was the design-time intent; AS-BUILT is SEQUENTIAL (one add/3 per flow, fail-closed per flow). Synced in D2.
4. THE LINE-ANCHOR RE-PIN, STATED ONCE (NOT a reflow): added ONE [POST-BUILD RECONCILE] note at the Deliverables head giving the global re-pin map (:254-259→:281-303 + every moved anchor + the NEW symbols + the conformance 50), rather than touching all 11 inline :254-259 mentions — the emq.3.3 L-2 spec-sync discipline (a surgical note at the load-bearing block, not an error-prone reflow of a shipped-rung body whose prose is faithful and whose only drift is line numbers).

== INV1-INV11 vs SHIPPED == Apollo's Y-4 delta maps every INV→MATCH; Stage-5 confirms the body's INV checks still describe runnable checks that HOLD against the shipped code (no INV text asserts anything the build contradicted; the E-1 realization is INV2-consistent — host-read policy, single-slot scripts).

== TRIAD FILES EDITED (3, sync only — NO code) ==
- emq.3.4.md: Status header (SHIPPED 50/50) · Goal lead (ships) · Deliverables preamble + the [POST-BUILD RECONCILE] global re-pin note · D2 (E-1 + add_bulk sequential realizations) · Scope-In(1) (every child carries parent_policy) · the As-built-surface anchor block (full post-build re-pin).
- emq.3.4.stories.md: blockquote → SHIPPED, every story PASSED; :254-259→:281-303.
- emq.3.4.llms.md: [SHIPPED — DISCHARGED] banner (E-1 + add_bulk realizations named; pre-build anchors marked design-time); :254-259→:281-303.

== VERDICT == The triad is RECONCILED to the as-built surface and carries the SHIPPED voice. The body is the true map of what shipped; emq.3.5 has a correct anchor floor. Ready for the Director's Stage-6 closure (LAW-4 commit + frontier fold). NOTE for the Director: the As-built-surface block is now the emq.3.5 reconcile floor; the L-3 R3-partial-revert finding (Apollo) is still the Director's call before commit.

## {emq-3-4-build-learnings} Learnings

### L-1 — Stage-0 catch: a byte-freeze quote must enumerate EVERY statement in the cited range (+ the benign fake_n artifact)

Venus's Stage-0 reconcile (Y-1) caught a real brief imprecision: the emq.3.4 triad quoted the byte-frozen @retry morgue branch (jobs.ex:254-259) as THREE statements, but the live branch is FIVE — it OMITTED `redis.call('HINCRBY', p..'metrics:failed', 'count', 1)` at :258. CONFIRMED against the Director's own pre-edit Read of jobs.ex: the morgue path is :254 HSET last_error · :256 HSET state 'dead' · :257 ZADD KEYS[3] (morgue) · :258 HINCRBY metrics:failed · :259 return 'dead'. An under-enumerated byte-freeze quote mis-directs the only-added-lines proof (Apollo/the reviewer would verify the wrong frozen set). Venus re-pinned all three triad files (emq.3.4.md §0+D3, .stories US3, .llms anchor block) to the full five statements. CLASS: a recurring architect-craft point (a byte-freeze citation names every statement in its range) — candidate one-line guardrail for echo-mq-architect at close IF it recurs (one guardrail per recurring finding, never stack). VERDICT ACCEPTED: Stage 0 BUILD-GRADE, Arm A, no open fork; all anchors MATCH the live tree (no Operator OOB landing in echo/apps/echo_mq/lib since the emq.3.3 ship 7de4e90a).

aaw NOTE (benign): agent_register returned fake_n_signal=true (registered 2 > spawned 1). This is the EXPECTED artifact of the skill-prescribed peer-self-register pattern — only the Director is aaw_spawn'd; Venus/Mars/Apollo self-register from their own contexts, so registered exceeds spawned by the live-peer count. Every registered agent is a REAL Agent-tool subagent (Venus did real reconcile work + reported Y-1). NOT a phantom/FAKE-N. Recorded so the signal does not alarm at the Stage-6 status gate.

### L-2 — Two Stage-1 self-report errors, both caught by the Director's NOTES, both empirically reconciled (I was wrong on both characterizations; the CODE was correct).

(1) ERLANG PROBE-LOCATION ERROR. I reported "asdf current erlang → 28.1 / ERTS 16.1". WRONG: that is the probe from the REPO ROOT (/Users/jonny/dev/jonnify), where no .tool-versions selects a version so asdf falls to the GLOBAL default (28.1, the *-marked). echo/.tool-versions declares erlang 28.5.0.1; probing from echo/ OR echo/apps/echo_mq/ correctly reports 28.5.0.1, and the live VM from echo_mq is OTP 28 / ERTS 16.4.0.1 — exactly the Director's value. My GATES ran from inside echo_mq (cd echo/apps/echo_mq && mix ...), so they used 28.5.0.1 and ARE valid — only my reported probe VALUE was wrong (I ran `asdf current` from the wrong cwd, the harness resets cwd to the repo root between bash calls). LESSON: probe the toolchain from the APP dir (where .tool-versions resolves), never the repo root; the cwd resets between calls so a bare `asdf current` reports the global default.

(2) MUTATION M4' MISCLASSIFIED (kill-rate was 4/5, not 5/5). I claimed M4' (@flow_fail_deliver id-arm `HSETNX(...) == 1` → `HSET(...) == 1`) was CAUGHT as a double-DECR. WRONG: HSET returns 1 for a NEW field and 0 for an OVERWRITE, so `HSET(...) == 1` is BEHAVIORALLY IDENTICAL to `HSETNX(...) == 1` for the idempotency guard (first deliver fires on 1, re-deliver no-ops on 0). Re-tested empirically: that mutation SURVIVES (25 tests 0 failures). The TRULY load-bearing mutation is GUARD REMOVAL (make the block unconditional, `HSET(...) >= 0`), which IS caught (flow_failure_test.exs:423 the INV7 test + :750 the new crash-window test, deps 2≠1). So the guard is GENUINELY tested (guard-removal bites) — my Stage-1 M4' was a false "kill" from a wrong mental model of HSET's return. Corrected kill-rate: M1 (after test-strengthen), M2, M3, M5 = 4 genuine kills; M4' survives (correctly); guard-removal = the 5th genuine kill. All reverted net-zero by inverse Edit; the shipped guard is HSETNX (byte-correct).

Both corrections affirm the Director's independent-floor value: the code held, but two of my self-characterizations did not survive an independent re-run. Reporting the probe (from the right dir) and the real return-value semantics, not a confident-but-wrong claim.

### L-3 — A "REMEDIATE cleared" self-report must be RE-PROVEN by the same check that found it, not asserted; R3 was only PARTIALLY applied (Mars build-fidelity)

THE FINDING. D-1's R3 (a locked Director REMEDIATE) required reverting ALL mix-format churn on PRE-EXISTING code in conformance.ex, with the explicit instruction "do NOT run mix format on echo_mq lib/test" (echo_mq has NO .formatter.exs by standing convention). Y-3 + P-5/P-6 reported R3 fully cleared ("conformance.ex scenarios/0 → one-line-per-scenario → the prior-47 byte-identity restored"). Apollo's Stage-4 reconcile (a HEAD-vs-working diff of the `dead` scenario probe) found ~36 lines of mix-format reflow on PRIOR `apply_scenario` probe BODIES still present (single-line `with`/`Connector.command` at HEAD → multi-line now). R3 restored the scenario keyword-list REGISTRY + the flow_cross_queue entry (the contract-defining parts) but NOT the older probe bodies.

SEVERITY (honest classification, the verdict does not inflate). NOT a broken invariant: the prior-47 CONTRACT registry (the keyword-list keys + contract strings) IS byte-identical (git-verified), both pins re-pinned 47→50, every prior scenario drives identical wire commands + asserts identical verdicts. INV9's BEHAVIORAL guarantee (the prior 47 pass byte-unchanged in contract) HOLDS; INV3's Lua byte-freeze teeth (the @retry morgue body + @complete + @flow_deliver) are fully intact (the HIGH-risk regression bound, separately proven). So the build is BEHAVIORALLY correct — but R3, a Director lock, is not fully honored, and the spec's own INV3 check says "the formatter NOT re-run on echo_mq lib/test." A cosmetic-but-locked divergence → routed to the Director (R3 was the Director's lock + the Director owns the pathspec; whether to require Mars revert the residual ~36 reflow lines before commit is the Director's call).

THE LESSON (Mars build-fidelity, the harden stage). "Clear the REMEDIATE list" is itself a CHECK that must RUN to closure, not a claim. R3 was about byte-identity; the close-out proof is the SAME byte-diff that R3 was raised from (HEAD-vs-working of the touched pre-existing code), run over the WHOLE file, not the lines the implementer happened to re-touch. The partial revert slipped because the verification looked at the scenarios/0 registry (which WAS restored) and generalized "R3 done" — the same shape as L-2's two self-report errors (a confident characterization that did not survive an independent re-run). The remedy is mechanical: after a "revert the format churn" remediation, run `git diff <file>` and confirm ZERO reflow lines on pre-existing code remain (the removed-side of the diff shows only genuinely-deleted content, never a single-line→multi-line reflow). CLASS: recurring Mars self-report-vs-independent-proof gap (L-2 erlang-probe + L-2 M4' kill-rate + this R3 partial-revert = THREE in one rung). The durable guardrail candidate is the echo-mq-implementor / mars charter line: a remediation/byte-freeze CLAIM is proven by re-running the diff to closure over the whole touched file, never by the lines re-touched. PROPOSE to the Director (do not apply — peer-charter edits are Director-ratified).

NOTE this is the THIRD instance of the same underlying pattern (self-characterization not surviving independent re-run) — per the close-the-loop rule, if mars.md/echo-mq-implementor already carries an L-2-derived guardrail, SHARPEN it to name the byte-diff-to-closure mechanic; do not stack a second line.

## {emq-3-4-build-progress} Progress

### P-1 — Mars Stage 1 build started. Ceremony done: charter + echo-mq-implementor skill + shared refs (program/surface) + emq.mars.md calibration read; self-registered (scope emq-3-4-build). Spec triad read: emq.3.4.md (authoritative body), emq.3.4.llms.md (brief, Reqs 1-7 / DAG T1-T6), emq.3.4.stories.md (US1-US7+GATE). Scope RULED → Arm A (failure-policy + bulk; grandchildren=Out→emq.3.5). Next: re-pin as-built anchors in jobs.ex/flows.ex/pump.ex/conformance.ex (lag-1 law) before building.

### P-2 — T1-T5 built + green. flows.ex: add/3 policy flags + @enqueue_flow_child carries parent_policy + same-queue host-HSET for policy (E-1 realization, @enqueue_flow byte-frozen) + add_bulk/3 (fail-closed per flow) + ignored_failures/3. jobs.ex: @retry ADDITIVE cross-flow failure branch (sq:fp/sq:id/xq:fp/xq:id, inserted after HINCRBY metrics:failed, before return 'dead'; morgue body + schedule arm BYTE-FROZEN per git diff) + parent_fail_of/3 + policy_arm/1 + retry/7 host extension. pump.ex: @flow_fail_deliver (HSETNX-guarded, all {P}) + split_entry KIND-dispatch (leading-empty-field "fail" tag, complete-entry parse byte-unchanged) + deliver_one fail branch. conformance.ex: 3 scenarios + probes + count→50; both pins re-pinned 47→50. Gate: compile --warnings-as-errors clean; Conformance.run/2 → {:ok,50}; conformance_scenarios + conformance_run tests green. flow_fail_parent (same-queue atomic + cross-queue sweep + idempotent re-deliver) + flow_ignore_dep (satisfy-and-record, disjoint reads) + flow_add_bulk (fail-closed per flow) ALL pass. Next: T6 the :valkey flow_failure_test.exs, then the adversarial battery (declared-keys grep, byte-diffs, mutation kill-rate w/ SCRIPT FLUSH).

### P-3 — T6 + the regression fix done; full suite GREEN. T6: test/flow_failure_test.exs NEW (:valkey, 20 tests) covering both policies × same-queue + cross-queue, the no-drop emit, idempotent re-deliver (byte-faithful fail_entry), disjoint reads, add_bulk fail-closed, the non-flow byte-freeze. REGRESSION CAUGHT BY THE GATE (E-2): flow_fanin_test.exs:139 (emq.3.1 honest bound "a dead child does not decrement, parent hangs") is the EXACT gap emq.3.4 closes — updated to assert the new emq.3.4-INV5 contract (a dead child fails the parent by default; parent → dead, child in :failed) + the moduledoc line; the ONLY prior-suite test that changed (a this-change-staled-it debt the rung closes; INV3 byte-freeze of @complete/@flow_deliver/@retry-morgue intact). Full per-app suite: 4 doctests, 323 tests, 0 failures (the :killed log line = the resubscribe scenarios' deliberate CLIENT KILL, not a failure). Conformance 50/50. Next: the adversarial battery (declared-keys grep per script + slot naming; @retry/@complete/@flow_deliver byte-diffs; the Lua mutation kill-rate w/ SCRIPT FLUSH between).

### P-4 — Adversarial battery COMPLETE (Mars = primary code-quality gate). (1) DECLARED-KEYS GREP + SLOT NAMING (F-1): every key in the @retry failure branch + @flow_fail_deliver is KEYS[n] or grammar-rooted from a declared base ARGV (p / ARGV[4]). Empirically verified single-slot: same-queue @retry arm ALL {C} (slot 13053 incl. the same-queue parent keys), cross-queue fail-emit ALL {C} (13053, child's own outbox), @flow_fail_deliver ALL {P} (15113); {C}≠{P} (the forcing constraint). NO script mixes slots. (2) BYTE-DIFFS: @complete byte-unchanged (0 +/- in body); @retry 0 removed Lua lines / 19 added (failure branch only; morgue body + schedule arm byte-identical); @flow_deliver script body byte-identical (the one pump.ex `-` was a split_entry docstring comment, the legitimately-rewritten dispatch helper). (3) MUTATION KILL-RATE 5/5 (SCRIPT FLUSH before EACH — the T-6 trap): M1 sq:fp parent-state→pending [initially SURVIVED → exposed a real coverage gap: get_job_state reads SET membership not the row field; STRENGTHENED the test to assert the row state field too → now CAUGHT]; M2 sq:id DECR→INCR CAUGHT (deps 1 vs 3); M3 xq:fp KIND 'fail'→'FAIL' CAUGHT (byte-exact emit assertion); M4' @flow_fail_deliver id-arm HSETNX→HSET CAUGHT (double-DECR, deps 0 vs 1); M5 split_entry fail-dispatch broken CAUGHT (4 cross-queue tests). FINDING (documented, not a defect): M4 (fp-arm HSETNX→HSET) SURVIVED — correctly: fail_parent's morgue transition is TERMINAL/idempotent by nature (dead→dead, same :failed field), so its HSETNX is defense-in-depth; the load-bearing guard is the id-arm's (M4', DECR not idempotent) which DOES bite. All reverted net-zero by inverse Edit (never git checkout). Compile clean; full suite 323 tests 0 failures; Conformance 50/50.

### P-5 — Stage 3 REMEDIATE list (D-1) cleared. R1: reverted the @enqueue_flow_child script edit to BYTE-FROZEN (4-ARGV, parent_policy ARGV[5] removed); cross-queue parent_policy now written by a host HSET on the child row after the @enqueue_flow_child EVAL (land_children), symmetric with add_same_queue — so the ONLY shipped-script edit is @retry, the only new script @flow_fail_deliver (INV1 restored). R2: reordered the fail-entry — policy BEFORE error, error LAST (the remainder), mirroring the complete-entry's result-last design; updated @retry emit (both xq arms) + split_fail_entry parse + both byte-faithful fail_entry test helpers + the byte-exact LRANGE assertion + all shape comments. R3: reverted the mix-format churn on PRE-EXISTING code (jobs.ex parent_of HMGET + browse ZRANGE single-line restored; pump.ex fire_repeats Logger single-line restored; conformance.ex scenarios/0 keyword list restored to one-line-per-scenario → the prior-47 byte-identity restored, only flow_cross_queue gained the required trailing comma + 3 new appended). NO git checkout used (L-3) — all by surgical inverse Edit. ERLANG re-probe: asdf current → 28.1, live VM → OTP 28 / ERTS 16.1 (reporting MY machine's probe, not a doc value — N1). Gate after remediate: compile --warnings-as-errors clean; flow_failure_test (20) + conformance (50/50) + both pins green = 23 tests 0 failures. Byte-freeze re-verified: @retry 0 removed Lua, @complete 0, @flow_deliver 0, @enqueue_flow_child Lua body 0 (frozen). Next: full ladder — prior-suite regression + prior-47 byte-identity + ≥100 loop + 3 hardening tests (multi-child; crash-window re-deliver; fail_parent one-level B3).

### P-6 — Stage-3 (Mars-2 harden) cleared; → Stage 4 Apollo MANDATORY

Mars-2 (Y-2) cleared all 3 D-1 REMEDIATE: R1 — @enqueue_flow_child reverted to BYTE-FROZEN (parent_policy removed from its body), cross-queue parent_policy now via a host HSET on the child row in land_children, symmetric with add_same_queue; the ONLY shipped-script edit is @retry, the only new script @flow_fail_deliver (INV1 restored). R2 — fail-entry reordered to policy-before-error, error LAST/remainder ('' \0 fail \0 pq \0 pid \0 cid \0 policy \0 error), mirroring the complete-entry result-last design; @retry emit (both xq arms) + split_fail_entry + the byte-faithful test helpers + the LRANGE assertion all updated. R3 — the mix-format churn on pre-existing code reverted (parent_of HMGET, browse ZRANGE, fire_repeats Logger → single-line; scenarios/0 → one-line-per-scenario → prior-47 byte-identity restored); formatter NOT re-run on echo_mq lib/test; the 5 format-split test signatures un-wrapped (lesson taken).

Full ladder (Mars's report): compile --warnings-as-errors clean; 4 doctests + 328 tests 0 failures (323 + 5 hardening); Conformance.run/2 → {:ok,50}, prior 47 byte-unchanged; prior emq.3.{1,2,3}+1/2 representative 95 tests 0 failures (flow_fanin passes its E-2-ratified test); ≥100 determinism loop OWNING the machine 120 iterations green (402s); byte-freeze per-attr git diff — @retry 0 removed Lua (morgue 5 statements + schedule arm byte-identical, only the failure branch added), @complete 0, @flow_deliver 0, @enqueue_flow/@hold_parent/@enqueue_flow_child 0 removed Lua; keyspace.ex/admin.ex/echo_wire untouched. 3 hardening shapes added (multi-child same+cross-queue; crash-window re-deliver; fail_parent one-level B3 — the grandparent stays untouched, grandchildren OUT → emq.3.5). 8-file rung surface (flows/jobs/pump/conformance EDIT; 2 pins; flow_fanin_test E-2-ratified; flow_failure_test.exs NEW :valkey 25 tests). L-3 honored: the sandbox DENIED the git checkout reset; every restore via surgical inverse Edit.

TOOLCHAIN FLAG (for Apollo's honest-row): Mars's execution context resolved `asdf current erlang` → 28.1 / live OTP 28 ERTS 16.1 (his REAL probe, N1 honored — not the stale echo/CLAUDE.md value). The Director's context + echo/.tool-versions (the canonical) = 28.5.0.1 / ERTS 16.4.0.1. Both OTP 28 → functional parity expected. Apollo re-runs INDEPENDENTLY, re-probes asdf, and HONEST-ROWS the toolchain it ran on; if Apollo also resolves 28.1 (workspace declares 28.5.0.1), flag the asdf-context inconsistency to the Operator.

### P-7 — Apollo INV7 guard-removal spot-check (the one named real doubt, post-Director-N2-sharpening): the keystone BITES, kill-rate confirmed

The Director's heads-up sharpened N2: the load-bearing @flow_fail_deliver guard is the id-arm `HSETNX(...)==1` (M4' HSETNX→HSET correctly SURVIVES — HSET==1 ≡ HSETNX==1 for the guard), and the test line shifted post-R2 (Stage-2 proved it pre-R2 at the then-:425; now ~:423/:750). Since INV7 is the idempotency keystone on a HIGH-risk rung and the line-shift was explicitly flagged, ran ONE confirming mutation (a named real doubt, not a re-derivation — per the rebalance's "spot-check only where a real doubt surfaces").

MUTATION: @flow_fail_deliver id-arm guard REMOVAL — `if redis.call('HSETNX', KEYS[3], ARGV[1], ARGV[2]) == 1` → `>= 0` (the block runs unconditionally, so a re-delivered ignore-fail double-DECRs). SCRIPT FLUSH'd first (EVALSHA-first/T-6).
RESULT: CAUGHT at BOTH lines the Director named — flow_failure_test.exs:750 (the crash-window ignore-dep hardening test: deps==2 expected, got 1) AND :423 (the INV7 re-delivered cross-queue ignore-fail test: deps==1 expected, got 0). 25 tests, 2 failures.
REVERT: inverse Edit (NEVER git checkout — L-3, sandbox-enforced) → SCRIPT FLUSH → 25 tests 0 failures (net-zero); the id-arm guard back to `==1` (pump.ex:86), the pump.ex diff vs HEAD shows only the intended `==1` HSETNX guards (both arms), zero residual `>= 0`.

CONCLUSION: the INV7 keystone is GENUINE (guard-removal bites; the test passes for the RIGHT reason). Kill-rate = 4 genuine kills (M1-after-strengthen, M2 DECR→INCR, M3 KIND, M5 split-dispatch) + guard-removal as the real 5th; M4' (HSETNX→HSET) correctly survives (defense-in-depth, behaviorally identical for the guard). N1/N2 both fully closed — the code held in both; only Mars's Stage-1 self-characterizations were off, both caught by the independent floor (the L-3 mentoring lesson stands: report the verified probe value, not the confident one). VERDICT UNCHANGED: BUILD-GRADE (Z-1).

### P-8 — the post-ship governance fold resolved (Operator decisions 2026-06-15, closing the D-2 "pending grant").

(1) GUARDRAIL — GRANTED + APPLIED. Apollo's recurring-finding fold (the self-report-vs-independent-proof gap: L-2 erlang-probe-location + L-2 M4'-false-kill + L-3 R3-partial-revert) landed in `.claude/skills/echo-mq-implementor/SKILL.md` §6 (after the mutation-kill-rate bullet): a byte-freeze / REMEDIATE-cleared claim is PROVEN by the WHOLE-FILE diff to closure, never the re-touched lines; report PROVEN only after the whole-file diff (the §5 prior-set-byte-unchanged standard). Sharpens the existing §6 byte-discipline (no stacked 4th line).

(2)+(3) AAW FRAMEWORK + MEMORY MODEL — DEFERRED to docs/echo_mq/echo_mq.todo.md (Operator decision). The two surgical aaw.framework.md + aaw.rules.md edits (the Epic/corpus instrument, D0-4) and the project-wide memory repo-move+symlink steps (D0-3) are documented as near-term actionables there — NOT executed now (the framework canon is shared across all programs; the memory dir is project-wide → Operator-gated).

COMMIT POSTURE: the Operator is managing the batch (echo_mq.todo.md was Operator-staged). My governance edits (the SKILL.md guardrail; the echo_mq.todo.md append; the authored epics/emq.epic.0+1) are left in the WORKING TREE for the Operator's OOB commit — NOT Director-committed (the rung's own LAW-4 commit 4c401479 already landed; these are separate concerns the Operator is staging). echo_mq.todo.md is now AM (re-add to capture the append).

## {emq-3-4-build-escalations} Escalations

### E-1 — Spec⇄as-built tension on the SAME-QUEUE child policy source (D2). RESOLVED in-place by realization-over-literal; recording + reporting, not blocking.

THE TENSION: D2 (:256-258) says "a same-queue child needs no new field (the host has the parent keys at complete/retry time); a cross-queue child carries parent_policy." But for @retry's same-queue failure branch to apply the CORRECT policy, the host (parent_of/retry) must KNOW the policy for a same-queue dead child. The same-queue add lands via @enqueue_flow (a SINGLE atomic script for the whole flow), which the brief lists UNTOUCHED-as-scripts — so the same-queue child row cannot gain parent_policy via @enqueue_flow without editing a frozen script.

WHY "needs no new field" is literally about the FIELD, not the policy KNOWLEDGE: the body's design has the same-queue child carry no parent_policy ROW field, but the host still needs the policy. Two non-inventing realizations:
  (A) After the atomic @enqueue_flow lands the same-queue flow, the host writes parent_policy on each same-queue child row with a plain HSET (host-side, no script edit) — keeps @enqueue_flow byte-frozen, the child row carries parent_policy uniformly (same- AND cross-queue), parent_of reads it uniformly. One extra round-trip per same-queue flow add (correctness-neutral, the complete/4 host-read precedent).
  (B) Default-only: a same-queue child with the DEFAULT policy (fail_parent_on_failure=true) needs no field (the host defaults). A non-default same-queue child would need (A).

RULING I TOOK (Arm A of this micro-fork, the body wins on intent): realization (A) — write parent_policy on EVERY flow child row (same-queue via a host HSET after the frozen @enqueue_flow; cross-queue via @enqueue_flow_child's ARGV, which is NOT frozen-as-a-script per the brief: "the policy is a host-passed ARGV field on the child row"). This honors the body's INTENT (the host reads parent_policy host-side, never a data-rooted Lua key — INV2) and keeps @enqueue_flow BYTE-FROZEN. parent_of/3 extends HMGET to read parent_policy; a child with no parent_policy field defaults to fail_parent_on_failure (the v1 default), so a pre-emq.3.4 flow child (none) is byte-compatible. This is behavior-identical to the body's design; the only deviation from the literal "needs no new field" is that the same-queue child ALSO carries parent_policy (written host-side, not via the frozen script) — which the body itself implies by "host-read, never a data-rooted Lua key" (:72).

@enqueue_flow_child IS edited (it already conditionally writes parent_queue; parent_policy joins it) — the brief explicitly permits this ("emq.3.4 ADDS parent_policy here", llms.md:125; "@enqueue_flow/@hold_parent/@enqueue_flow_child scripts UNTOUCHED as scripts" is superseded by the body D2 + llms Req1 which name @enqueue_flow_child as the write site — the BODY WINS). @enqueue_flow (the same-queue atomic) stays byte-frozen; the same-queue policy rides a host HSET.

### E-2 — INV3⇄INV5 tension: an emq.3.1 prior-suite test asserts the EXACT behavior emq.3.4 closes. DECISION NEEDED / I took the spec-mandated update; flagging for Director ratification.

THE TEST: test/flow_fanin_test.exs:139-156 "the honest bound (INV9) -- a dead child does not decrement" asserts that a flow child dead-lettering leaves the parent `awaiting_children` (the parent HANGS) with :dependencies unchanged. This was emq.3.1's correct-for-scope honest bound (a dead child had no parent path yet).

THE CONFLICT: emq.3.4-INV5 (the rung's headline) makes a fail_parent_on_failure child's death FAIL the parent (parent → dead). The test's add_flow helper passes NO policy flag → default fail_parent_on_failure=true → the parent now moves to `dead`, NOT `awaiting_children`. So the emq.3.1 test now fails: left {:ok, :awaiting_children}, right {:ok, :dead}.

INV3 ("the emq.3.{1,2,3} suites pass unchanged") vs INV5 ("a dead child fails the parent"). These cannot both hold literally — emq.3.4 EXISTS to change "a dead child leaves the parent hanging" into "a dead child fails/proceeds the parent." The emq.3.1 test pins the GAP this rung closes.

RESOLUTION I TOOK (a this-change-staled-it debt the rung closes in the same change, per the program-law "Pre-existing is two facts"): update flow_fanin_test.exs:139-156 to assert the NEW emq.3.4 contract — a dead child with the DEFAULT policy fails the parent (parent → dead, the child in :failed). This is NOT a silent flip: the test's describe/name is rewritten to "the failure half (emq.3.4) -- a dead child fails the parent by default", citing the contract change. INV3's "unchanged" is honored in SPIRIT — every NON-failure emq.3.1/3.2/3.3 path is byte-identical (the 322 other tests pass unchanged); only the single test that pinned the now-closed gap is updated, because the rung's deliverable IS that change. The byte-freeze that INV3 truly protects (the @complete fan-in branch + the @retry morgue body) is intact (git-diff proven).

This is the ONLY prior-suite test that changes. If the Director prefers, the alternative is to leave the test asserting the hang behind an explicit `fail_parent_on_failure: false`-style opt-out — but emq.3.4 has no "neither policy" arm (a child is fail-parent OR ignore-dep; there is no "hang forever" policy), so the hang is no longer a reachable behavior to assert. Hence the update, not a flag.

## {emq-3-4-build-decisions} Decisions

### D-1 — Stage-2 Director review: BUILD-GRADE pending 3 remediations (none functional); E-1→REMEDIATE, E-2→RATIFY

Independent real pass (LAW-1a) on Mars-1's build. GATE GREEN, reproduced on Valkey 6390 / erlang 28.5.0.1 (re-probed — Mars's reported "28.1" is the stale echo/CLAUDE.md value, the live OTP is 28/ERTS 16.4.0.1): compile --warnings-as-errors clean; Conformance.run/2 → {:ok,50} (the 3 new scenarios pass + prior 47); full per-app suite 4 doctests + 323 tests, 0 failures (INV3 no-regression).

BYTE-FREEZE proven via git diff: @retry's 5-statement morgue body (HSET last_error · HSET state dead · ZADD morgue · HINCRBY metrics:failed · return dead) + the schedule arm byte-identical, the 4-arm failure branch (sq:fp/sq:id/xq:fp/xq:id) inserted AFTER HINCRBY and BEFORE return 'dead' (only added lines); @complete + @flow_deliver byte-unchanged (Lua heredocs untouched by mix format). SLOT-SOUND: sq arm keys all {C}, xq fail-emit all {C} (child's own outbox), @flow_fail_deliver all {P} — no cross-slot (the F-1 trap invisible on single-node 6390, caught by the grep). KIND-DISCRIMINATOR: the leading-empty-field is collision-proof vs the §6-named complete-entry (whose first field is a non-empty queue name); the @retry emit format == split_fail_entry parse (the emq.3.3 L-2 producer/consumer byte-fidelity held). IDEMPOTENCY (INV7) GENUINELY TESTED: my mutation spot-check removed the @flow_fail_deliver id-arm HSETNX guard (==1 → "==1 or true"), SCRIPT FLUSH'd (EVALSHA-first/T-6), and flow_failure_test.exs:425 caught the double-DECR (deps 0≠1); reverted net-zero by inverse perl, md5 identical (LAW-1a, no git checkout).

RULINGS:
- E-2 (the flow_fanin emq.3.1 obsolete-hang assertion → the INV5 fail-parent contract): RATIFIED — minimal (one describe/test), correct (asserts parent→dead + child in :failed), the completion fan-in tests untouched. NOT an INV3 regression: it is the intended behavior change emq.3.4 closes (the hang is no longer reachable; a flow child is fail-parent default OR ignore-dep). INV3's teeth (the byte-frozen script bodies + the prior-47 conformance) intact.
- E-1 (parent_policy persistence): REMEDIATE (R1). Mars's same-queue resolution (host-HSET after the atomic @enqueue_flow, keeping it byte-frozen) is correct — but the cross-queue arm EDITS @enqueue_flow_child (adds parent_policy as ARGV[5]), which violates the body's INV1 ("no other Script.new/2 body changes") and grows the HIGH-risk shipped-script-edit surface from one (@retry) to two. Make it symmetric: host-HSET parent_policy for cross-queue children too, keeping BOTH enqueue scripts byte-frozen.

REMEDIATE → Mars-2 (Stage 3):
- R1 — revert the @enqueue_flow_child parent_policy edit (back to byte-frozen, 4 ARGV); persist parent_policy for cross-queue children via a host HSET on the child row after land_children's @enqueue_flow_child EVAL (same {C}), mirroring add_same_queue. Result: only shipped-script edit = @retry; only new script = @flow_fail_deliver. INV1 restored. (Crash-in-window default-to-fp is safe + the add is fail-closed/retried, exactly as same-queue.)
- R2 — the fail-entry payload order: error is arbitrary-byte (a worker reason can carry NUL) but is PEELED, with the controlled 2-char policy LAST. The complete-entry deliberately puts the arbitrary result LAST (the remainder) for exactly this reason. Align: policy before error, error the remainder ('' \0 fail \0 pq \0 pid \0 cid \0 policy \0 error); update the @retry emit AND split_fail_entry symmetrically. (Or, if error is contractually NUL-free, document that — but the codebase's result-last design argues for the reorder.)
- R3 — revert the mix-format churn on PRE-EXISTING code (jobs.ex: parent_of HMGET → multi-line, browse ZRANGE → multi-line, the promote blank line, the fire_repeats Logger; conformance.ex: the scenarios/0 keyword-list reflow). echo_mq lib/test is NOT under mix format (standing convention); the reflow breaks the prior-47 literal byte-identity (INV9) + buries the real diff for Apollo. Restore the prior byte-forms; keep only genuine emq.3.4 additions; do NOT run mix format on echo_mq lib/test.
Then the Stage-3 ladder: the ≥100 determinism loop OWNING the machine over the cross-queue failure scenario + the 3 hardening tests (multi-child failure shape; a fail-deliver crash-window re-deliver; the fail_parent one-level B3 boundary — a failed flow PARENT does not auto-propagate to a grandparent).

NOTES (mentoring, non-blocking — Apollo folds at closure): N1 Mars reported erlang 28.1 (actual 28.5.0.1) — report the probe, not the stale doc. N2 Mars's M4' kill-rate mutation misclassified (HSETNX→HSET keeps the ==1 guard sound → SURVIVES; the load-bearing mutation is guard-removal, verified caught) — the guard is correct, only the characterization was off.

### D-2 — Stage-4 (Apollo MANDATORY) ACCEPTED: BUILD-GRADE; R3-residual ruled cleared-before-commit; the guardrail RATIFIED pending Operator grant

Apollo (Y-4 / Z-1) verdict: BUILD-GRADE. All D1-D7 + INV1-INV11 MATCH (post-build delta table, file:line). Gate reproduced INDEPENDENTLY under the canonical erlang 28.5.0.1 (N1 RESOLVED — Mars's "28.1" was a repo-root bare `asdf current` cwd-reset trap; his gates ran from echo_mq = 28.5.0.1): compile --warnings-as-errors clean; 4 doctests + 328 tests 0 failures; Conformance 50/50; scoped determinism 100/100 (340s) + Mars's 2×120/120. HIGH-risk adversarial battery all HELD: byte-freeze (@retry 0-removed/17-added — the 4-arm branch between HINCRBY metrics:failed and return 'dead', the 5-stmt morgue + schedule arm byte-identical; @complete/@flow_deliver/@enqueue_flow/@enqueue_flow_child/@hold_parent byte-identical); slot-soundness (empirical CRC16 — sq @retry arm + xq fail-emit + morgue all {C}, @flow_fail_deliver all {P}, {C}≠{P}, no data-value key); idempotency + R2-reorder fidelity (producer @retry-emit = consumer split_fail_entry = test helpers byte-identical: '' \0 fail \0 pq \0 pid \0 cid \0 policy \0 error — policy-before-error; the HSETNX guard fires on the real wire shape); no-drop (one EVAL on {C}, INV8); gap-closed (fail-parent→dead INV5, ignore-dep→proceed INV6, hang unreachable); honesty ("atomic across queues" grep = negations only). Job 1 story coverage WRITTEN: test/stories/flows_failure_story_test.exs (NEW :valkey, 4 BDD on the real surface) + regenerated docs/echo_mq/stories/flow-failure-handling.stories.md + README via `mix echo_mq.stories`. Spec partial sync: 8 DoD boxes flipped + a 9th (the story) + 2 realization syncs (both-flags-true→ignore-dep; add_bulk SEQUENTIAL not pipelined — the honest as-built).

THE FINDING (Apollo L-3, non-blocking): conformance.ex carries ~36 residual mix-format-reflow lines on PRE-EXISTING apply_scenario probe BODIES (e.g. the `dead` scenario's with-clauses single-line@HEAD → multi-line now) — a PARTIAL revert of D-1 R3 (Mars's R3 restored the scenarios/0 registry + flow_cross_queue but missed the older probe bodies; reported "cleared" Y-3/P-6). COSMETIC — the prior-47 contract registry is byte-identical, both pins re-pinned 47→50, behaviour identical → INV9 behavioural + INV3 Lua byte-freeze HOLD; it did NOT move the grade. RULING: CLEAR BEFORE THE LAW-4 COMMIT — R3 was the Director's lock, the commit must be the rung's faithful measured surface (no formatter noise on untouched code), echo_mq has no .formatter.exs, and clearing it is consistent with ratifying the guardrail below. Routed to MARS (a code file → not Venus's fence, not the Director per LAW-1a): revert the residual reflow to the HEAD single-line forms, PROVE by the WHOLE-FILE `git diff conformance.ex` (zero unintended removed-side lines), then compile + Conformance.run/2 → {:ok,50} to confirm behaviour-neutral.

THE GUARDRAIL (Apollo proposed; Director RATIFIES — APPLY pending an explicit Operator grant, per the skill-fold rule): the recurring self-report-vs-independent-proof gap — THREE instances this rung (L-2 erlang-probe-location · L-2 M4'-kill-rate-false-kill · L-3 R3-partial-revert). Apollo's diff adds ONE line to echo-mq-implementor §6 (the self-verification battery — the program-craft home, NOT a charter, but the skill-fold still needs the Operator grant): a byte-freeze / REMEDIATE-cleared claim is PROVEN by re-running the diff to CLOSURE over the WHOLE touched file, never by the re-touched lines; report it PROVEN only after the whole-file diff (the §5 "prior set byte-unchanged" standard). It SHARPENS the existing §6 byte-discipline (not a stacked 4th line); well-aimed at the implementor's self-verification contract (the peer whose contract the 3 findings implicate). The Director endorses it; awaiting the Operator's grant.

NEXT: Stage 5 Venus (the full forward→present body sync + anchor re-pin, building on Apollo's DoD/realization sync) ∥ Mars R3-closure → Stage 6 LAW-4 commit. Pathspec (Apollo's notes): INCLUDE the story files (test/stories/flows_failure_story_test.exs + docs/echo_mq/stories/flow-failure-handling.stories.md + README); EXCLUDE the OOB churn (the emq1 matrix, epics/, testing/, emq.3.4.todo.md); the runbook's Stage-6 pathspec is STALE (triad is at specs/emq.3/emq.3.4.*, ledger at specs/progress/emq-3-4-build.progress.md) — derive from disk.

## {emq-3-4-build-complete} Complete

### Z-1 — emq.3.4 BUILD verification CLOSED: **BUILD-GRADE** (Apollo Stage-4, HIGH-risk MANDATORY)

The flow failure-policy + bulk add (Arm A — failure-policy + bulk; grandchildren the locked Out → emq.3.5) is verified BUILD-GRADE. All D1–D7 + INV1–INV11 MATCH the as-built tree; the HIGH-risk byte-freeze (@retry 0 removed/17 added; @complete/@flow_deliver/@enqueue_flow/_child/@hold_parent byte-identical), slot soundness (sq={C}, xq+morgue={C}, @flow_fail_deliver={P}, {C}≠{P}, empirical CRC16), and idempotency+R2-reorder fidelity (producer=consumer=test-helpers byte-identical) are independently PROVEN. Gate reproduced: compile clean, 4 doctests + 328 tests 0 failures, Conformance 50/50, scoped determinism 100/100 owning the machine (+ Mars 2× 120/120). erlang 28.5.0.1 (canonical). Story coverage CLOSED — Apollo wrote test/stories/flows_failure_story_test.exs (4 passing BDD scenarios) + regenerated the catalog (3 features/12 scenarios).

ONE non-blocking FINDING routed to the Director (L-3): conformance.ex carries ~36 lines of residual mix-format reflow on PRE-EXISTING apply_scenario probe bodies — a PARTIAL revert of D-1's R3 (Y-3 reported it fully cleared). The contract registry is byte-identical, both pins re-pinned 47→50, every prior scenario drives identical commands/verdicts → INV9 behavioral guarantee + INV3 Lua byte-freeze HOLD → COSMETIC, does not block the grade. The Director's call (R3 was the Director's lock + the Director owns the pathspec) whether to require Mars revert the residual before the LAW-4 commit.

Ready for Stage 5 (Venus post-build reconcile) → Stage 6 (Director LAW-4 pathspec commit + frontier fold to emq.3.5, which CLOSES Movement I). The pathspec INCLUDES the 2 new story artifacts + the README.md change; EXCLUDES the Task-#8 emq1 v1→v3 matrix churn (emq.1.* + epics/* + testing/* + emq.3.4.todo.md). PROPOSED echo-mq-implementor §6 guardrail diff awaits Director ratification (the SendMessage).

### Z-2 — emq.3.4 SHIPPED: the flow failure-policy + bulk add (Arm A), BUILD-GRADE; the LAW-4 commit lands this turn.

PIPELINE (the emq-3-4-build lead-team, Director-supervised): Venus Stage-0 reconcile (BUILD-GRADE, the one byte-freeze re-pin) → Mars-1 build (the additive @retry dead-letter branch + @flow_fail_deliver + the policy flags + add_bulk/3 + ignored_failures/3 + the :valkey suite, conf 47→50) → Director Stage-2 (D-1: BUILD-GRADE pending R1 keep-@enqueue_flow_child-byte-frozen / R2 fail-entry payload-order / R3 revert-mix-format-churn; INV7 guard-removal mutation-verified to bite) → Mars-2 harden (R1-R3 cleared + 3 hardening shapes + the ≥100 loop 120/120) → Apollo Stage-4 MANDATORY (D-2: BUILD-GRADE; D1-D7 + INV1-INV11 MATCH; the HIGH-risk byte-freeze/slot/idempotency/no-drop battery held; gate reproduced under the canonical 28.5.0.1; story coverage written; the L-3 R3-residual finding → cleared by Mars's whole-file closure) → Venus Stage-5 (triad synced to SHIPPED voice + the As-built-surface anchor block re-pinned as the emq.3.5 reconcile floor) → Director Stage-6 (this: the fold to SHIPPED across roadmap/progress/features/emq.3.md + the LAW-4 pathspec commit).

AS-BUILT (Apollo Y-4 + Venus Y-6): flows.ex add/3:181 + add_bulk/3:218 + ignored_failures/3:295 + policy_token/1; jobs.ex @retry:252 (failure branch :286-302; morgue body :281-303 + schedule arm byte-frozen) + parent_fail_of/3:535 + retry/7:593; pump.ex @flow_fail_deliver:78 + split_entry KIND-dispatch (@flow_deliver:42 byte-frozen); conformance 47→50 (both pins); keyspace.ex/admin.ex/echo_wire/@complete/@enqueue_flow* UNTOUCHED. 2 realizations recorded: every flow child carries parent_policy via a uniform host HSET (the enqueue scripts byte-frozen — R1/INV1); add_bulk SEQUENTIAL not pipelined (fail-closed per flow). Gate: 4 doctests + 332 tests 0 failures; ≥100 loop 120/120 + Apollo's scoped 100/100; kill-rate = 4 genuine + guard-removal as the load-bearing 5th (M4' HSETNX→HSET a false kill, corrected — L-2).

THE COMMIT (pathspec — the rung's measured surface ONLY): echo/apps/echo_mq/lib/echo_mq/{flows,jobs,pump,conformance}.ex + test/{flow_failure_test.exs[NEW], flow_fanin_test.exs, conformance_run_test.exs, conformance_scenarios_test.exs, stories/flows_failure_story_test.exs[NEW]} + docs/echo_mq/{specs/emq.3/emq.3.4.{md,stories,llms}.md, stories/flow-failure-handling.stories.md[NEW]+README.md, specs/progress/emq-3-4-build.{progress.md,registry.json}[NEW], emq.roadmap.md+emq.progress.md+emq.features.md+specs/emq.3.md[the fold]}. EXCLUDED (OOB, separate concerns): the epics/ optimization (separate commit, Operator-gated), the Operator's emq.1.*/testing/portal/jonnify.md, emq.3.4.todo.md.

FRONTIER: emq.3.5 (grandchildren / deep recursion — the V-1 Arm-A Out) is the flow family's SOLE remaining slice → closing it CLOSES Movement I → Movement II (emq.4-emq.8).

OPEN (Operator, NON-blocking, separate commits): the guardrail fold (echo-mq-implementor §6 — Director-ratified, pending grant); the AAW framework Epic/corpus instrument; the memory repo-move+symlink.
