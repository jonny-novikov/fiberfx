# emq-3-2 — AAW scope ledger

## {emq-3-2-thinking} Thinking

### T-1 — UNDERSTAND: author emq.3.2 (child-result reads) — the design + the forks the as-built reveals

THE TASK (Operator-directed): author the emq.3.2 spec triad (emq.3.2.{md,stories.md,llms.md}) + the runbook emq.3.2.prompt.md + open this ledger. emq.3.2 = the SECOND sub-rung of the emq.3 flow family (the carve, emq.3.md): child-result reads — EchoMQ.Flows.children_values/3 + dependencies/3 over the shipped parent :processed/:dependencies subkeys (the v1 get_children_values/get_dependencies parity). A DESIGN-AUTHORING phase (Director + Venus, no Mars/Apollo — no code; the deliverable IS the spec).

THE AS-BUILT FLOOR (emq.3.1, shipped f9849efe — Venus re-pins at authoring):
- EchoMQ.Flows.add/3 + @enqueue_flow (flows.ex) — emq.3.2 ADDS children_values/3 + dependencies/3 to flows.ex.
- :processed = emq:{q}:job:<parent>:processed, a HASH. As built it holds child_id→child_id (a PRESENCE-MARKER, emq.3.1 O1/L-3 — NOT a real result; complete/4 has no result arg).
- :dependencies = emq:{q}:job:<parent>:dependencies, a STRING counter (the outstanding child count; the parent is released at zero).
- @complete's fan-in branch (jobs.ex) does `HSET KEYS[4] ARGV[1] ARGV[5]` — it writes WHATEVER complete/4 passes as ARGV[5] into :processed[child_id]. emq.3.1's complete/4 passes job_id (the presence-marker). THE HINGE: a real result needs only the HOST WRAPPER to pass a real result as ARGV[5] — the @complete SCRIPT is byte-unchanged. So real-result parity is reachable at NORMAL risk (no shipped-script edit).

THE TWO FORKS (the as-built reveals them; Venus SURFACES, the Operator rules):
- FORK R1 (the headline) — the result-payload mechanism. Arm A: children_values/3 returns the PRESENCE-SET (which children completed — reads :processed as-built; no completion change; pure-read). Arm B (RECOMMENDED): emq.3.2 extends the flow-child completion to carry a real result (complete gains a result arg, passed as the EXISTING ARGV[5] slot → :processed[child_id]=the result; the @complete Lua BYTE-UNCHANGED, only the host wrapper extends; non-flow completion byte-unchanged) + children_values/3 returns the real results = the v1 get_children_values parity, at NORMAL risk. RECOMMEND Arm B (the family's intent + v1 parity + cheap — the script is byte-unchanged). NOTE: Arm B keeps emq.3.2 NORMAL-risk (a host-API extension, NOT a shipped-script edit → no Apollo); Arm A is thinner (presence only, no result). The risk tier hinges on R1.
- FORK R2 — the dependencies-read shape. Arm A (RECOMMENDED): dependencies/3 returns the outstanding COUNT (reads :dependencies directly — minimal). Arm B: returns the pending SET (which children remain) — needs a NEW :children roster subkey written at @enqueue_flow (record all child ids at add) + the complement (:children − :processed); richer (the v1 get_dependencies set) but adds a subkey + an @enqueue_flow change. RECOMMEND Arm A (the count); Arm B deferrable.

THE SUBKEY LIFECYCLE (the new echo-mq-architect guardrail — emq.3.2 MUST name it): emq.3.2 READS :processed/:dependencies. Per Apollo L-5, these subkeys outlive the parent row (@complete DELs only the row) AND obliterate (del_job enumerates only :logs/:lock). emq.3.2's spec NAMES the cleanup disposition: when does :processed/:dependencies get retired? Options the spec must state — at the PARENT's @complete (the parent-row retire could DEL its own flow subkeys, since after the parent completes the results are consumed), or at obliterate (add :dependencies/:processed to del_job's enumerated list), or a later lifecycle rung. RECOMMEND: emq.3.2 names that the parent's own completion should retire its flow subkeys (the results are read by the parent handler BEFORE the parent completes, then the subkeys are dead) — and routes the obliterate-sweep + the parent-completion-cleanup to whichever rung owns it (emq.3.2 if cheap, else emq.3.x), stated as the honest bound. Do NOT leave it un-named (the L-5 lesson).

INHERITED HONEST BOUNDS (from emq.3.1, emq.3.2 addresses or carries): O1 :processed presence-marker (→ R1 resolves it); O2 the parent_of HGET per completion (emq.3.2 MAY fold the parent-read into the claim result — surface as an optional scope item); L-5 the subkey lifecycle (→ named above).

THE TRIAD SHAPE (the program's): emq.3.2.md (Goal · 5W · Scope · D-n · INV-n · the forks · DoD) · .stories.md (US-n Connextra + the EMQ.3.2-US-GATE Valkey gate + Coverage) · .llms.md (the Mars brief — References · Requirements · Topology · Agent stories) · emq.3.2.prompt.md (the runbook — Mode, the fork gate, the as-built floor, the stages, the commit pathspec). Authored to the recommended arms (R1·B, R2·A) so emq.3.2 is build-ready; the forks marked OPEN for the Operator. Forward-tense for the unbuilt surface. NO-INVENT (ground every ref in the as-built flows.ex/jobs.ex or the v1 reference or a design §). Risk tier: NORMAL (R1·B is a host-API extension, no shipped-script edit) — but if the Operator rules a @complete-touching variant, re-tier to HIGH-RISK.

NEXT: Venus authors the triad + runbook to these arms, surfacing R1/R2 + naming the subkey lifecycle; the Director reviews + folds the frontier (emq.progress.md emq.3.2 SPECCED + the emq.3.md carve emq.3.2 row → specced).

### T-2 — Stage-0 reconcile: the as-built emq.3.1 surface re-probed (NO-INVENT), the floor emq.3.2 reads

DESIGN-AUTHORING phase (the deliverable IS the spec triad; no code, no Mars/Apollo). emq.3.2 = the SECOND sub-rung of the emq.3 flow family: child-result READS over the parent's :processed/:dependencies subkeys (the v1 get_children_values/get_dependencies parity). Re-probed every anchor against the post-emq.3.1 tree (grep/Read, never trusting a pinned line):

THE AS-BUILT FLOOR (the :processed/:dependencies/complete shapes emq.3.2 reads):
1. EchoMQ.Flows.add/3 (flows.ex:75-104): add(conn, queue, %{parent: %{id, payload}, children: [spec]}) :: {:ok, {parent_id, [child_id]}} | {:error, term()}. emq.3.2 ADDS children_values/3 + dependencies/3 to THIS module.
2. @enqueue_flow (flows.ex:31-51): SET KEYS[2] (the parent :dependencies key) = N (a STRING counter — confirmed SET, not SADD/HSET). KEYS[2] = parent_key <> ":dependencies" (flows.ex:85). So :dependencies IS a STRING counter (Fork B Arm 1 as-built).
3. @complete fan-in branch (jobs.ex:181-188, the SHIPPED script — emq.3.2 must NOT edit it to stay NORMAL-risk): `if KEYS[3] and was_active == 1 then local left=DECR KEYS[3]; HSET KEYS[4] ARGV[1] ARGV[5]; if left<=0 then ZADD p..'pending' 0 ARGV[4]; HSET KEYS[5] 'state' 'pending' end end`. CONFIRMED: :processed is a HASH written `HSET KEYS[4] ARGV[1] ARGV[5]` = HSET <parent :processed> <child_id=ARGV[1]> <ARGV[5]>. :dependencies is `DECR KEYS[3]` = a STRING counter. The :processed VALUE (ARGV[5]) is the PRESENCE MARKER (O1).
4. complete/4 (jobs.ex:350-370): signature complete(conn, queue, job_id, token) — NO result arg. The fan-in passes ARGV[5]=job_id (jobs.ex:361: `argv ++ [parent_id, job_id]`), so :processed[child_id] = child_id (O1 = child_id→child_id presence marker). parent_of/3 (jobs.ex:382-389) does HGET <child> 'parent' on EVERY completion (O2 — the carried perf follow-up). THIS is the seam R1·B extends: ARGV[5] is ALREADY the HSET-into-:processed value; making it a real result only changes the HOST (complete gains a result arg) — the @complete Lua is BYTE-UNCHANGED.
5. obliterate/del_job (admin.ex:150-153): `local function del_job(id) local jk = base..'job:'..id; redis.call('DEL', jk, jk..':logs', jk..':lock') end` — a FIXED 3-key enumeration (jk, :logs, :lock) that does NOT include :dependencies/:processed (the L-5 leak surface — these survive obliterate too). admin.ex moduledoc (admin.ex:125-126 "every reachable job row") is accurate for the CURRENT row+logs+lock sweep.
6. CONFORMANCE = 45 (live, post-emq.3.1): conformance.ex:3,24 "forty-five"; run/2 doc n==45 (conformance.ex:102); both pin tests assert {:ok,45} (conformance_run_test.exs:41) + "forty-five names" @run_order (conformance_scenarios_test.exs:20-68). The additive-minor target for emq.3.2 is 45 → N (flow_children_values + optionally flow_dependencies).
7. Keyspace.job_key/2 (keyspace.ex:17-24) gates BrandedId.valid?/1 RAISES; subkeys compose via <> ":..." (no enum). add_log/5 (jobs.ex:458) the <> ":logs" precedent for <> ":dependencies"/<> ":processed". CLEAN SLATE: no children_values/dependencies symbol exists in flows.ex yet (genuinely new reads).

RESULT: every emq.3.2 dependency anchor is at as-built truth. The triad is build-grade to author. NO STALE/INVENTED/MISSING.

### T-3 — the two forks + the subkey-lifecycle disposition (the §2 guardrail), derived

FORK R1 (the headline) — the result-payload mechanism. The v1 get_children_values returns the REAL results children produced, keyed by child id (flow_producer.ex). As-built emq.3.1's :processed is a PRESENCE marker (child_id→child_id, O1) because complete/4 carries no result arg.
- Arm A (pure read of the as-built): children_values/3 returns the PRESENCE SET (which children completed) — reads :processed as-built, no completion change, a pure read. Steelman: zero risk, zero shipped-surface touch, trivially NORMAL-risk. Cost: NOT the v1 parity — it answers "which children completed", not "what they returned"; emq.3.2 would ship a read of a placeholder, deferring the real value indefinitely (re-opening O1, not closing it).
- Arm B (RECOMMENDED) — extend completion to carry a real result. complete gains a result argument (complete/5 or an opts result, Mars picks the A-1-clean form) passed as the EXISTING ARGV[5] slot; @complete ALREADY does `HSET KEYS[4] ARGV[1] ARGV[5]` (jobs.ex:183) so the @complete Lua is BYTE-UNCHANGED — only the host wrapper extends (and the non-flow completion stays byte-unchanged: a non-flow job has KEYS[3] nil, the HSET branch is unreached). children_values/3 then returns the REAL results = v1 get_children_values parity. Steelman: closes O1 (the real result, not a placeholder); the v1 capability; CHEAP — the script does not change, the seam is already built (emq.3.1 L-3 named exactly this as emq.3.2's). Cost: complete's public arity changes (a host-API extension) — but emq.3.1 deliberately left the slot.
- RECOMMENDATION: Arm B. The family intent (emq.3.md:98 "the child-result reads … the v1 get_children_values surface over the :processed subkey"), v1 parity, and cheapness all point to B; emq.3.1's L-3 explicitly named the real-result write as emq.3.2's. RISK-TIER CONSEQUENCE (load-bearing): Arm B keeps emq.3.2 NORMAL-risk (a host-API extension, NOT a shipped-script edit → no mandatory Apollo). A variant that EDITED @complete's Lua would re-tier to HIGH-RISK (the emq.3.1 precedent — its @complete edit forced mandatory Apollo). Because B touches only the host, emq.3.2 stays NORMAL-risk.

FORK R2 — the dependencies-read shape.
- Arm A (RECOMMENDED): dependencies/3 returns the outstanding COUNT (reads :dependencies directly via GET — it is a STRING counter as-built, flows.ex:49 SET / jobs.ex:182 DECR). Minimal — one GET, an integer. Cost: the count alone does not say WHICH children remain (only how many) — the v1 get_dependencies SET answer needs more.
- Arm B: dependencies/3 returns the pending SET (which children remain). Needs a NEW :children roster subkey written at @enqueue_flow (the full child-id set) + the complement (:children − :processed keys). Richer (the v1 get_dependencies set) but adds a §6 subkey AND an @enqueue_flow change (a flow-script edit — re-opens the write surface).
- RECOMMENDATION: Arm A (the count). It reads the as-built :dependencies with no write-side change; the SET answer (Arm B) adds a subkey + a flow-script edit, out of proportion to a read rung. The "which remain" set is a clean later carry if a consumer needs it.

THE SUBKEY-LIFECYCLE DISPOSITION (the NEW §2 guardrail — emq.3.2 MUST name it). emq.3.2 READS :processed/:dependencies; per emq.3.1 L-5 they OUTLIVE the parent row (@complete DELs only the row KEYS[2], jobs.ex:189; obliterate's del_job enumerates ONLY jk/:logs/:lock, admin.ex:152) AND obliterate. An un-named cleanup disposition is a SILENT at-rest leak the gate cannot catch (the suites purge per-test via on_exit). THE DISPOSITION emq.3.2 NAMES:
- The PARENT's own completion retires its flow subkeys. The parent handler reads its children's results (children_values/3) BEFORE the parent itself completes; once the parent completes, :processed/:dependencies are DEAD. So the parent's @complete COULD DEL its own :dependencies/:processed. BUT: the parent is itself a job completed via the SAME shipped @complete — adding a self-subkey-DEL to @complete would be a SHIPPED-SCRIPT EDIT (re-tiering emq.3.2 to HIGH-RISK) AND is unsound for a parent that is itself a child of a grandparent (deep recursion, Out). So the parent-completion cleanup is NOT cheap-in-scope for emq.3.2.
- THE NAMED DISPOSITION (the honest bound, routed): emq.3.2 names that (a) the flow subkeys are retired by a DEDICATED lifecycle mechanism — obliterate's del_job gains :dependencies/:processed in its enumerated DEL list (admin.ex:152, a one-line FIXED-list extension, slot-sound, A-1-clean) — and (b) per-flow completion cleanup (the parent DELing its own subkeys at completion) is the genuinely-owed design, deferred to the emq.3.x lifecycle rung (it needs the shipped-script-edit decision + the recursion shape, both HIGH-RISK / out-of-scope here). emq.3.2's DECISION: the cleanup is a NAMED CARRY, NOT in emq.3.2's read-only scope — because folding it in either (i) edits the shipped @complete (re-tiers to HIGH-RISK) or (ii) adds the obliterate-sweep, which is an Admin-surface change beyond a read rung's boundary. emq.3.2 NAMES the disposition (obliterate-sweep + per-flow cleanup) and routes it to the emq.3.x lifecycle rung; it does NOT leave it un-named (the L-5 lesson). The admin.ex obliterate moduledoc carry (emq.3.1 L-5: a one-line honest-bound note WHEN the lifecycle rung lands) is re-affirmed, NOT discharged here.

### T-4 — BUILD RUN opens (the design phase closed at Z-1/T-3): the forks ruled, the mode, the toolchain, the smallest-change derivation

THE TASK: /echo-mq-ship emq.3.2 — build the child-result reads end-to-end through the Flat-L2 lead-team to ONE LAW-4 commit. The triad (emq.3.2.{md,stories,llms,prompt}) + this ledger were authored in the design cycle (T-1..T-3, D-1..D-4, V-1/V-2, Y-1, Z-1) to the recommended arms; this run executes the build.

THE FORKS RULED (the Step-0 gate — Operator jonny, this turn): FORK R1 → Arm B (the real-result-carrying completion — complete gains a result arg threaded into the EXISTING ARGV[5] slot; :processed[child_id] holds the real result; children_values/3 returns it = v1 get_children_values parity; CLOSES O1; the @complete Lua BYTE-UNCHANGED → NORMAL-risk). FORK R2 → Arm A (dependencies/3 returns the outstanding COUNT — a pure GET of the :dependencies STRING counter). Both = the arms the triad is authored to → NO pre-build re-scope; emq.3.2 is build-ready as specified.

MODE: Flat-L2, NORMAL-RISK (Apollo OPTIONAL — the inverse of emq.3.1's HIGH-risk @complete-Lua edit). The single highest-leverage property + its own proof: the no-shipped-script-edit invariant (INV1/INV3) — an EMPTY git diff of every Script.new/2 attribute. The ≥100 determinism loop still applies to the mint/process-touching read scenario (a flow minting N+1 ids, fanned in, then read).

TOOLCHAIN (re-probed this turn): erlang 28.5.0.1 + elixir 1.18.4 (echo/.tool-versions); Valkey on 6390 → PONG; no rebase in progress; HEAD 75aebc3b; working tree dirty only with Operator out-of-band mercury(mla-1.progress.md)+art(2 files) — none touch echo_mq → EXCLUDE at commit.

THE SMALLEST CHANGE THAT PRESERVES CORRECTNESS (the boundary = echo/apps/echo_mq ONLY):
- do-nothing baseline: without emq.3.2 a flow's outcome is unconsumable (no parent read of children's results; O1 the :processed placeholder stays open).
- the change: (1) jobs.ex HOST-ONLY — complete gains a result arg passed as ARGV[5] instead of job_id; the @complete Lua byte-unchanged; the non-flow caller byte-unchanged (KEYS[3] nil → fan-in branch unreached). (2) flows.ex — children_values/3 (HGETALL→map) + dependencies/3 (GET→non-neg integer), both gating parent_id at Keyspace.job_key/2, both PURE reads (INV2). (3) conformance.ex — flow_children_values scenario + apply_scenario probe + re-pin 45→N in BOTH pin tests. (4) test/flow_children_values_test.exs NEW (:valkey).
- the boundary grep stays empty: no echo_wire, no keyspace.ex grammar edit, no admin.ex edit (the L-5/N1 lifecycle carry → emq.3.x), no Lua-attribute edit, apps/echomq untouched, mix.lock untouched.

INVARIANTS AS RUNNABLE CHECKS (the gate): INV1 empty git diff of every Script.new/2 attr · INV2 double-read leaves :dependencies count + :processed contents byte-identical · INV3 prior 45 conformance byte-unchanged + emq.1/2.{1..4}/3.1 suites pass · INV4 ill-formed parent_id raises at job_key/2 · INV5 two children with distinct results → children_values returns the RESULTS not the child ids (O1 closed) · INV6 additive-minor (probe in same change, both pins re-pinned) · INV7 lifecycle NAMED, admin.ex untouched · INV8 keys are exactly the parent's {q} slot.

THE PIPELINE (NORMAL-risk): Venus Stage-0 pre-build reconcile (re-pin the floor; confirm R1·B/R2·A; re-derive triad if a sibling moved an anchor) → Mars-1 build → Director solo review (REAL pass: gate re-run on 6390 + ≥1 adversarial probe incl. the empty-Lua-diff + a net-zero mutation spot-check) → Mars-2 harden + full ladder → [Apollo OPTIONAL — skip unless a finding elevates] → Venus Stage-5 post-build reconcile → Director Stage-6 closure + ONE pathspec commit + frontier fold.

### T-5 — Stage-0 BUILD-RUN reconcile (lag-1): every anchor re-probed against HEAD 75aebc3b (emq.3.1 shipped f9849efe, an earlier commit); one [RECONCILE] found + corrected

THE BUILD's Stage-0 (distinct from the design-cycle T-2 grounding) re-probed every anchor against the CURRENT post-emq.3.1 tree (HEAD 75aebc3b — NOT f9849efe; f9849efe is the emq.3.1 ship, an ancestor; the brief's "HEAD is 75aebc3b" is correct). Toolchain: erlang 28.5.0.1 + elixir 1.18.4 (echo/.tool-versions); Valkey 6390 → PONG. Working tree dirty ONLY with Operator out-of-band: docs/mercury/live_admin (mla-1.progress.md), docs/echo/art (2 appendix files), + the emq-3-2 ledger/registry — NONE touch echo_mq production → EXCLUDE at commit. No rebase in progress.

THE RECONCILE DELTA TABLE (the 7 brief anchors + the byte-unchanged-proof enumeration):
1. CONFORMANCE COUNT = 45 — MATCH. conformance.ex moduledoc "forty-five" (:3,24); run/2 doc "n == 45" (:102); pin tests at NEW paths test/conformance_run_test.exs:41 ({:ok, 45}) + test/conformance_scenarios_test.exs:68 (@run_order 45 names, "forty-five" :4) — NOTE the pin tests are test/conformance_*.exs (flat), NOT test/echo_mq/conformance_*.exs (the brief's :40 line hint said test/conformance_*; the dir is flat — confirmed). scenarios/0 LAST key = obliterate_grouped, the keyword list has 45 entries. Additive-minor target 45 → N.
2. @complete (BYTE-UNCHANGED Lua + ARGV[5] seam) — MATCH. jobs.ex @complete at :152-192; the fan-in branch at :181-188: `if KEYS[3] and was_active == 1 then local left = DECR KEYS[3]; HSET KEYS[4] ARGV[1] ARGV[5]; if left <= 0 then ZADD p..'pending' 0 ARGV[4]; HSET KEYS[5] 'state' 'pending' end end` — CONFIRMED ARGV[5] is ALREADY the value HSET into :processed (KEYS[4]). complete/4 at :350-370; argv ++ [parent_id, job_id] at :361 — job_id IS the current ARGV[5] (presence marker). parent_of/3 at :382-390 (HGET 'parent', O2). R1·B is host-only: the @complete Lua is byte-unchanged.
3. EchoMQ.Flows.add/3 + @enqueue_flow — MATCH. flows.ex: @enqueue_flow `SET KEYS[2] n` at :49 writes :dependencies as a STRING counter (NOT SADD/HSET — confirmed); KEYS[2] = parent_key <> ":dependencies" at :85. flows.ex carries ONLY add/3 + reject_cross_queue/2 — NO children_values/dependencies symbol (CLEAN SLATE confirmed; jobs.ex:380 comment explicitly names children_values as emq.3.2's).
4. add_log/5 (subkey-compose precedent) — MATCH. jobs.ex:526 `Keyspace.job_key(queue, job_id) <> ":logs"` (the def at :524, body :526). The read keys compose `<> ":processed"`/`<> ":dependencies"` identically.
5. Keyspace.job_key/2 — MATCH. keyspace.ex:17-24 gates BrandedId.valid?/1, RAISES ArgumentError on ill-formed (INV4). queue_key/2 at :13-15.
6. del_job (L-5/N1 lifecycle carry — DO NOT EDIT) — MATCH + a SECOND leak surface noted. admin.ex obliterate's del_job at :150-152 = FIXED `DEL jk, jk..':logs', jk..':lock'` (excludes flow subkeys — the L-5 leak). ALSO admin.ex:88-90 @drain's wipe() enumerates `DEL jk, jk..':logs'` (2 keys, no :lock, no flow subkeys) — a SECOND obliterate-class leak surface for the flow subkeys. The named D5/N1 lifecycle carry already covers "the obliterate-sweep gains the two subkeys"; the @drain path is the SAME class (a per-job destructive sweep with a FIXED subkey list). admin.ex stays UNTOUCHED this rung (the carry is routed to the emq.3.x lifecycle rung); the @drain second surface is folded into the named carry's scope (noted for Stage-5/the lifecycle rung, NOT a deliverable).
7. v1 capability reference (READ, never edit) — MATCH with a label-precision nuance. echomq/flow_producer.ex:64 get_children_values + :70 get_dependencies; echomq/job.ex:846 get_children_values/1 :: {:ok, map()} (HGETALL over :processed → a MAP — EXACT parity for R1·B children_values/3 HGETALL→map); :921 get_dependencies/1 :: {:ok, [String.t()]} (SMEMBERS — a SET); :949 get_dependencies_count/1 :: {:ok, non_neg_integer()} (SCARD — a COUNT). echomq/keys.ex:46 @job_subkeys ~w(lock logs dependencies processed failed unsuccessful); dependencies(ctx,id) at :282 = job <> ":dependencies". NUANCE: v1 :dependencies is a SET (SMEMBERS/SCARD); v2 emq.3.1 :dependencies is a STRING counter (SET/DECR/GET). So R2·A (the count via GET) is the v2 analog of v1 get_dependencies_COUNT (not get_dependencies the SET); R2·B (the pending SET) is the v1 get_dependencies analog. The triad body summary-prose (md:7-8,28; stories US3:57) labels R2·A "the v1 get_dependencies parity" — imprecise (it is the v1 _count variant's observable). But the load-bearing brief R3 (llms:65-69) + body D3 + stories US3 acceptance all state the MECHANISM precisely (a pure GET of the :dependencies STRING counter → a non-neg integer), so Mars is correctly directed. COSMETIC label imprecision, NOT a mis-directing spec⇄as-built contradiction → NOT a [RECONCILE]-blocker; left as-is (the mechanism governs; "get_dependencies" reasonably names the v1 family).

THE ONE [RECONCILE] (corrected): the byte-unchanged-proof Script.new/2 enumeration is STALE/INCOMPLETE. The triad's NORMAL-risk HEADLINE proof — "a git diff of every Script.new/2 attribute is empty" — parenthetically enumerates EIGHT attrs (@enqueue/@claim/@complete/@retry/@promote/@reap/@schedule/@enqueue_flow). But the LIVE tree carries FIFTEEN Script.new/2 attrs: the 8 named + SEVEN emq.2.x scripts (@update_data jobs.ex:448, @update_progress :468, @add_log :505, @remove_job :557, @reprocess :613, @extend_lock :637, @extend_locks :649) — these landed in the emq.2 parity cluster AFTER the 8-item list was first authored (the pre-2.x surface). A diff over only the 8 would PASS even if a Stage hand touched, e.g., @add_log → the NORMAL-risk proof has a 7-script blind spot. The proof's INTENT requires ALL FIFTEEN. Appears in 5 spots: md INV1 (:211), md INV3 (:224 by-reference), stories US4 (:88-89), prompt Stage-2 probe (:153) [all carry the incomplete 8-list]; llms gate-ladder (:141-143) + AS-7 (:171) ALREADY use the robust "every Script.new/2 attribute in jobs.ex + flows.ex" phrasing (no list — correct). FIX (the durable form, matching the brief's already-robust phrasing): replace the incomplete hardcoded 8-item list with "every `@... Script.new/2` attribute in jobs.ex + flows.ex (15 as-built — the 8 state-machine/flow scripts + the 7 emq.2.x mutation scripts)" so the gate enumerates the real set AND does not re-drift when emq.3.4 adds scripts. The body's prose already says "every"; this fix makes the parenthetical match the prose + as-built. SURGICAL sync to body INV1/INV3 + stories US4 + prompt Stage-2 — the brief stays as-is (already robust).

NEXT: apply the surgical [RECONCILE] edits (body INV1/INV3 + stories US4 + prompt Stage-2), record the decision (D-5) + the verdict (Y-2). Forks R1·B/R2·A re-confirmed ruled (T-4) → NO pre-build re-scope. Lifecycle disposition NAMED (D-2/D5/INV7) re-affirmed; admin.ex untouched. VERDICT: BUILD-GRADE after the one cosmetic-but-load-bearing enumeration correction.

### T-6 — Director Stage-0 GATE: Venus's reconcile VERIFIED against ground truth (the artifact, not the report) → PASS

The Director independently re-probed every load-bearing claim in Venus's Y-2 (LAW-1a — a real pass, not a glance):
- THE 15-SCRIPT COUNT (Venus's headline [RECONCILE]) is EXACT: grep `@[a-z_]+ +Script\.new` over jobs.ex + flows.ex = 14 (jobs.ex: @enqueue:14/@schedule:38/@claim:126/@complete:152/@retry:194/@promote:237/@reap:266/@update_data:448/@update_progress:468/@add_log:505/@remove_job:557/@reprocess:613/@extend_lock:637/@extend_locks:649) + 1 (flows.ex @enqueue_flow:31) = 15. The original proof enumerated only the 8 pre-emq.2.x scripts → a 7-script blind spot in the NORMAL-risk headline proof itself ("a gate must specify its own liveness"). Venus widened it to "every @… Script.new/2 in jobs.ex+flows.ex (all 15)" in 5 spots; the diff is surgical (md INV1+INV3, stories US4, llms gate-ladder, prompt Stage-2 probe) and smuggles NOTHING into the contract (D2-D6/arities/fork-arms untouched).
- THE @complete ARGV[5] SEAM is CONFIRMED at jobs.ex:181-188: `if KEYS[3] and was_active == 1 then local left = redis.call('DECR', KEYS[3]); redis.call('HSET', KEYS[4], ARGV[1], ARGV[5]); ...`. ARGV[5] IS the :processed value (KEYS[4]) keyed by child_id (ARGV[1]). R1·B host-only CONFIRMED — only the host-supplied ARGV[5] changes; the @complete Lua body byte-unchanged. The fan-in is gated on KEYS[3] → a non-flow job (KEYS[3] nil) never reaches it → the non-flow path is byte-unchanged by construction (INV3 structurally true).
- CONFORMANCE = 45 confirmed three ways (conformance.ex:3,24 "forty-five"; conformance_run_test.exs:41 {:ok,45}; conformance_scenarios_test.exs:68 "forty-five names"). flows.ex CLEAN SLATE confirmed (no children_values/dependencies def). Venus stayed in boundary — git status shows only the ledger + registry.json + the 4 triad files modified (+ the pre-existing Operator out-of-band mercury/art files); NO production code touched.
- BONUS FINDING accepted (NOT a new deliverable): admin.ex:88-90 @drain's wipe() is a SECOND obliterate-class leak surface for the flow subkeys (same destructive-sweep class as del_job) — folded into the N1 lifecycle carry's scope for the emq.3.x lifecycle rung; admin.ex stays untouched this rung.

VERDICT: Stage-0 BUILD-GRADE — the forks are ruled (R1·B + R2·A), the triad is build-ready with NO pre-build re-scope, the as-built floor is at truth (line numbers drifted per the lag-1 law but the surface is intact), the byte-unchanged proof is now hole-free. PROCEED to Stage 1 (Mars-1 build).

### T-7 — Mars-1 Stage-0 reconcile + build entry (the lag-1 re-probe, HEAD 75aebc3b)

Adopted the Mars charter + loaded echo-mq-implementor (read echo-mq-program.md + echo-mq-surface.md). Read the authoritative triad (emq.3.2.md body, .stories.md, .llms.md). Re-probed every anchor against the post-emq.3.1 tree — ALL confirmed at the brief's line numbers (no drift):
- @complete Lua = jobs.ex:152-192; fan-in branch :181-188; `HSET KEYS[4] ARGV[1] ARGV[5]` at :183 (ARGV[5] = the value HSET into :processed keyed by child id ARGV[1]).
- complete/4 = jobs.ex:350-370; the flow branch :359-361 passes `argv ++ [parent_id, job_id]` — the trailing job_id IS the current ARGV[5] (the emq.3.1 presence marker R1·B replaces with the result). The non-flow path (parent_of → nil) keeps keys/argv at length 2/3 → byte-unchanged when I add a defaulted result arg.
- parent_of/3 = jobs.ex:382-390 (HGET 'parent'); the O2/N2 fold is OPTIONAL — declining it (expands surface to the claim path; not a deliverable).
- flows.ex = CLEAN SLATE (only add/3 + reject_cross_queue/2; no children_values/dependencies symbol); aliases EchoMQ.{Connector, Keyspace, Script}; @enqueue_flow `SET KEYS[2] n` at :49 (:dependencies STRING counter); `parent_key <> ":dependencies"` at :85.
- job_key/2 = keyspace.ex:17-24, gates BrandedId.valid?/1, RAISES ArgumentError (INV4). Grammar needs no edit (:processed/:dependencies are job-key subkeys).
- Lua attrs = 15 total: 14 in jobs.ex (@enqueue/@schedule/@claim/@complete/@retry/@promote/@reap + @update_data/@update_progress/@add_log/@remove_job/@reprocess/@extend_lock/@extend_locks) + 1 in flows.ex (@enqueue_flow). Matches the brief's enumeration exactly.
- Conformance = 45 live; scenarios/0 tail = ...obliterate_grouped(:93), flow_add(:94), flow_fanin(:95); moduledoc "forty-five" at :3,:24; "n == 45" at :102. Pin tests: conformance_run_test.exs:41 `{:ok, 45}` (+ moduledoc "forty-five"); conformance_scenarios_test.exs @run_order ends :flow_add,:flow_fanin (:64-65) (+ test name + moduledoc "forty-five").
- del_job = admin.ex FIXED DEL list — DO NOT EDIT (untouched this rung; INV7/N1 carry).

V1 capability reference (READ-only, form NOT lifted): job.ex:846 get_children_values = HGETALL→map ({:ok,%{}} for [], else parse_hash_result) — the EXACT R1·B parity; :921 get_dependencies = SMEMBERS (a SET, NOT R2·A); :949 get_dependencies_count = SCARD (the COUNT, v1 analogue). v2 :dependencies is a STRING counter (emq.3.1 SET) → R2·A is GET parsed to int, never SCARD.

RESP shape (live-probed on 6390): the Connector is RESP3 (RESP.ex moduledoc: maps `%`→Elixir maps). HGETALL → %{"a"=>"ra","b"=>"rb"} directly; conformance pairs/1 (:1163-1167) defends BOTH map + flat-list shapes — children_values/3 will mirror that. GET on a missing key → nil.

Forks RULED: R1·B (real-result completion, ARGV[5] carries the result, @complete byte-unchanged — host-only) + R2·A (dependencies/3 = outstanding COUNT, GET on the STRING counter). The triad is authored to these → a no-op re-derive. dependencies/3 none-key sentinel: choosing {:ok, 0} (the honest "zero outstanding / not a flow" reading — matches D3's first option + the count's natural floor; no new error vocabulary).

Toolchain: Valkey PONG on 6390; erlang/elixir re-probed (below). Proceeding to build D4→D3/D2→D6 inside echo/apps/echo_mq only; agents run no git.

### T-8 — BUILD-TAIL RUN: continue emq.3.2 from post-Stage-2 (D-6 PASS) → Stage 3 harden ‖ Stage 5 reconcile + emq.3.3 prep → Stage 6 ship

THE TASK (Operator /echo-mq-ship, 2026-06-15): "Polish and harden emq.2.* - emq.3.2 reviewing comments, code style and test coverage against 'echomq' while it's not deleted. In parallel, reconcile emq.*testing.md documents for Mars-2, create emq.3.3.prompt to fan out Venus passing the clear instructions to author docs & specs + reconcile progress and roadmap."

THE STATE (re-probed this turn): emq.3.2 is BUILT + UNCOMMITTED in the working tree and has PASSED Stage 2 (the ledger T-4..D-6: forks ruled R1·B+R2·A by the Operator, Mars-1 built, Director Stage-2 solo review PASS, REMEDIATE list EMPTY, Apollo NOT required — NORMAL-risk, the empty-Lua-diff SHA-256-proven). Sanity re-gate this turn: compile clean + 22 flow tests 0 failures on 6390. The build is GREEN. What remains is the pipeline TAIL: Stage 3 (Mars-2 harden) → Stage 5 (Venus post-build reconcile) → Stage 6 (Director ship + fold). I APPEND from T-8 (the build-run history T-4..D-6 + the design Z-1 are FROZEN — records-freeze).

THE DIRECTIVE MAPPED:
- "Polish/harden … comments, code style, test coverage vs echomq" = STAGE 3 (Mars-2): benchmark the emq.3.x FLOW surface (flows.ex children_values/dependencies/add + jobs.ex complete/5 + the flow suites) against the v1 echo/apps/echomq flow surface (flow_producer.ex + job.ex get_children_values/get_dependencies/get_dependencies_count + flow_producer_test.exs) WHILE echomq is still in the tree (it dissolves at program end). Parity benchmark already shows a delta (v1 flow_producer_test 27 test/describe vs v2 ~7; doc density v1 44 vs v2 18) → benchmark COVERAGE parity (every v1 flow-read behavior has a v2 test), not line-count. PLUS the durable-harness GAP: D-6's ≥100 loop was tee'd to /tmp (EPHEMERAL — the emq.2.4 "proof≠harness" lesson); echo/rungs/bus/emq_3_2_check.sh does NOT exist → Mars-2 authors it (durable, LOOP_N-parameterized, keys off "0 failures"+exit-code).
- "reconcile testing.md for Mars-2 + progress/roadmap + create emq.3.3.prompt" = STAGE 5 (Venus): (a) post-build reconcile of the emq.3.2 triad body to as-built (children_values/3, dependencies/3, complete/5, conformance 46, O1 CLOSED, N1 lifecycle carry NAMED); (b) reconcile emq.testing.md + the testing/emq.N.testing.md ledgers from the STALE emq.2.4 board (43, 32 files, 251 cases) to the live 46-board (add flow rungs emq.3.1@45 + emq.3.2@46; §1 table, §3 matrix flow rows, §4 counts, §5 hot-place the flow-subkey lifecycle carry; add testing/emq.3.1+3.2.testing.md); (c) reconcile emq.progress.md + emq.roadmap.md (emq.3.2 SPECCED→shipped; surface emq.3.3); (d) AUTHOR emq.3.3.prompt.md — the runbook fanning out a FUTURE Venus to author the emq.3.3 cross-queue-flow quad (md/stories/llms), grounded in the emq.3.md carve (Fork A's cross-queue arm: the completion-signal hop / per-queue promote-style sweep / eventually-consistent fan-in; conformance flow_cross_queue; HIGH-risk — crosses a slot).

PARALLELISM: the two tracks are DISJOINT — Mars-2 owns echo/apps/echo_mq/** (code+tests) + echo/rungs/bus/ (harness); Venus-2 owns docs/echo_mq/** (triad + testing + progress + roadmap + features + emq.3.3.prompt). No write contention → genuine fan-out. The ONE shared fact (the final test count) is reconciled at Stage 6 from Mars-2's surface (one-owner-per-file: Venus records the count Mars-2's code establishes).

RISK TIER: NORMAL (D-3 re-confirmed — empty-Lua-diff SHA-proven at D-6; admin.ex untouched; the reads pure). Apollo OPTIONAL → NOT spawned unless a Stage-3 finding elevates the rung. The Director Stage-2 review + Mars-2's ladder + the durable harness are the NORMAL-risk floor.

TOOLCHAIN: erlang 28.5.0.1 + elixir 1.18.4; Valkey 6390 → PONG; HEAD 75aebc3b; no rebase. Tree dirty with the emq.3.2 build (echo_mq code+tests, uncommitted) + Operator out-of-band EXCLUDED at commit: docs/mercury/live_admin (mla.2.*), docs/echo/art (art8/9/91/92), docs/echo/mesh/ (new), [fsharp] HEAD — re-verify --cached is pure boundary before EACH commit (the concurrent-index race).

COMMIT PLAN (Stage 6, ≥2 scoped pathspec commits, NEVER git add -A): (1) the emq.3.2 RUNG — echo/apps/echo_mq/{lib/echo_mq/{flows,jobs,conformance}.ex, test/{flow_children_values_test,conformance_run_test,conformance_scenarios_test}.exs} + echo/rungs/bus/emq_3_2_check.sh+.out + the emq.3.2.{md,stories,llms,prompt}.md triad + emq-3-2.progress.md/.registry.json. (2) the PROGRAM-DOC reconcile + forward-prep — emq.testing.md + testing/*.md + emq.progress.md + emq.roadmap.md + emq.features.md + emq.3.md + emq.3.3.prompt.md.

NEXT: stand up the team (Mars-2 harden + Venus-2 reconcile/prep; fresh ephemeral team emq-3-2-tail; aaw scope stays emq-3-2); fan out Stage 3 ‖ Stage 5; Director Stage-6 ship.

### T-9 — Mars-2 Stage-3 harden (bounded per the Director's PASS/REMEDIATE-EMPTY verdict): the three focused deliverables, evidenced. Per the Director's instruction the ≥100 loop was NOT re-run (the Director ran it independently 100/100 — that gate is closed; my own Stage-1 run was also 120/120). NO code change.

(1) FORMAT POSTURE — RESOLVED, do-no-harm DECLINE confirmed honest:
- My OWN additions are format-clean: `mix format --check-formatted` PASSES on test/flow_children_values_test.exs (NEW) + test/conformance_run_test.exs + test/conformance_scenarios_test.exs (both pins). The flows.ex read region + jobs.ex complete/5 + the conformance flow_children_values probe each diff-clean against `mix format` output (region-by-region verified Stage-1 + re-verified here).
- The ~25 --check-formatted fails are PRE-EXISTING, PROVEN: `git show 75aebc3b:…/conformance.ex` + `:…/jobs.ex` ALREADY FAIL --check-formatted (the long-line style predates emq.3.2). The fails span the whole app (conformance.ex, jobs.ex, lanes.ex, meter.ex, pump.ex, consumer.ex + ~18 test files) — the app ships non-mix-format-conformant.
- DECLINE is correct: running `mix format` would reformat the byte-sensitive prior conformance scenario strings (breaching INV6) + emq.3.1's add/3 and ~25 files I didn't author — a regression. NOT run.

(2) COVERAGE TABLE (each D2–D6 + INV1–INV8 → exact covering test):
- D2 children_values/3 → flow_children_values_test.exs "two children completed with distinct results read back as the results keyed by id" + "a parent with no completed children yet returns {:ok, %{}}" + "an ill-formed parent_id raises…"; conformance.ex flow_children_values.
- D3 dependencies/3 → flow_children_values_test.exs "the count is N before any completion, N-k after k complete, 0 at full fan-in" + "a parent with no :dependencies key… returns the {:ok, 0} sentinel" + "an ill-formed parent_id raises…".
- D4 real-result completion (host-only) → the empty-Lua-diff SHA proof (@complete byte-unchanged) + flow_children_values_test.exs distinct-results assertion (the result reaches :processed) + the byte-unchanged non-flow @complete path (jobs_test.exs + conformance :complete/:retry/:dead).
- D5 lifecycle NAMED → spec body D5/N1 (prose, names obliterate-sweep + per-flow cleanup → emq.3.x) + INV7 static grep (zero cleanup) + admin.ex untouched (boundary grep).
- D6 proof → conformance_run_test.exs {:ok,46} + conformance_scenarios_test.exs @run_order 46 + the prior-45-byte-unchanged comm proof + the ≥100 loop (Director-run + Stage-1).
- INV1 wire law / no shipped-script edit → the 15-attr empty-Lua-diff SHA proof; the read fns issue only HGETALL/GET (grep).
- INV2 reads pure → flow_children_values_test.exs "a double-read leaves :dependencies and :processed byte-identical"; conformance flow_children_values purity arm; static grep (zero mutations).
- INV3 byte-unchanged surface → full per-app 272/0 (emq.1+2.{1-4}+3.1 unregressed) + the 15-attr SHA proof.
- INV4 branded identity at read boundary → flow_children_values_test.exs the two "ill-formed parent_id raises at Keyspace.job_key/2" tests (children_values + dependencies).
- INV5 O1 closed → flow_children_values_test.exs distinct-results "values == %{c1 => r-c1, c2 => r-c2}" + refute keys-map-to-self; conformance flow_children_values.
- INV6 additive-minor → conformance_scenarios_test.exs (names) + conformance_run_test.exs ({:ok,46}) + the comm -23/-13 prior-45-byte-unchanged git proof.
- INV7 lifecycle NAMED no cleanup → INV7 static grep (zero DEL/HDEL/UNLINK of a flow subkey) + admin.ex untouched.
- INV8 slot soundness single-queue → the read fns build keys of exactly the parent's {q} (children_values_test on per-test sub-queues); the touch-set is the read surface + the host result arg only.
NO GAP. Un-exercised BY DESIGN: R1·A presence-marker arm (not built, R1·B ruled); cross-queue/failure/bulk/deep-recursion = emq.3.3/3.4 (Out).

(3) FINAL CONFIRMATION:
- Empty-Lua-diff: 15 attrs (14 jobs.ex + 1 flows.ex) byte-identical HEAD 75aebc3b ↔ WORK by SHA-256.
- Boundary grep: echo_wire / keyspace.ex / admin.ex / mix.lock / apps/echomq — NONE touched. INV7: zero DEL/HDEL/UNLINK of a flow subkey.
- ONE clean full per-app run (echo/apps/echo_mq, TMPDIR=/tmp mix test --include valkey): 4 doctests, 272 tests, 0 failures; CONFORMANCE 46/46 (number confirmed).
- Toolchain: erlang 28.5.0.1 / elixir 1.18.4; Valkey 6390 PONG; {emq}:version echomq:2.0.0.

SURPRISE: none. REMEDIATE: 0 used (list empty). SHIP-GRADE; the 6 files (all in echo/apps/echo_mq) ready for Stage-5 (Venus reconcile) → Stage-6 (Director commit). No git.

### T-10

Mars-2 (emq-3-2-tail team) Stage-3 harden pass OPENS — the FULLER pass that closes the two gaps T-8 named (the v1 coverage-parity benchmark + the durable harness), additive to the bounded T-9 (which deferred the loop + authored no harness). NORMAL-risk; NO shipped-Lua edit; boundary = echo/apps/echo_mq/** + echo/rungs/bus/.

STATE RE-PROBED (HEAD 75aebc3b, build uncommitted): toolchain erlang 28.5.0.1 / elixir 1.18.4; Valkey 6390 -> PONG. The 5 modified files + 1 new test file present exactly as T-8 records. echo/rungs/bus/emq_3_2_check.sh CONFIRMED ABSENT (my deliverable). The emq_3_1_check.sh template (committed, 2026-06-15) is the structure I mirror.

THE V1 FLOW-READ DEPTH BAR (echomq, READ-ONLY, dissolves at program end) — re-probed:
- job.ex:846 get_children_values/1 = HGETALL over :processed -> {:ok, map} (parse_hash_result JSON-decodes values). EXACT R1·B parity for children_values/3.
- job.ex:921 get_dependencies/1 = SMEMBERS -> {:ok, [id]} (a SET) = Fork R2·B, DELIBERATELY NOT built.
- job.ex:949 get_dependencies_count/1 = SCARD -> {:ok, count} = the COUNT = the v1 analogue of v2 R2·A dependencies/3 (a GET of the STRING counter). THE NAME-COLLISION NUANCE to document: v2 dependencies/3 is the parity of v1 get_dependencies_COUNT, NOT v1 get_dependencies (the SMEMBERS set).
- flow_producer_test.exs = 27 test/describe markers, but only FIVE are flow-READ behaviors (the rest = add/2 construction = emq.3.1 flow_add; Worker integration fan-in = emq.3.1 flow_fanin; nested/multi-queue = emq.3.3+). The 5 read behaviors: (a) get_children_values empty -> {:ok, %{}}; (b) get_dependencies empty -> {:ok, []} [maps to v2 {:ok, 0} sentinel — R2·B form]; (c) get_dependencies for parent -> length==2 [R2·B form]; (d) get_dependencies_count -> count==3 (a 3-CHILD flow!); (e) Worker "parent can access children results after completion" -> map_size==2 (the RESULTS, post-completion).

THE COVERAGE-GAP CANDIDATES the v1 depth reveals (to assess vs the as-built ~7-test flow_children_values_test.exs): (1) a flow with >2 children — v1 (d) uses 3; v2's children_values_test uses 2 throughout, dependencies uses 3 for the count-down. ASSESS whether a >2-child children_values read is proven. (2) the obliterate/L-5 honest bound — v1 has no parity (its subkeys are swept differently); v2 should PIN the as-built leak so the named carry is asserted, not silent (the emq.3.1 harness gate-7 already forward-points to this as "Apollo T-9 / emq.3.2"). (3) a child completing with an empty/structured/large result. Add ONLY tests that prove a real behavior v1 proves and v2 does not yet — do NOT pad (INV2 anti-padding).

NEXT: read the as-built flow_add/flow_fanin tests to fix the exact current coverage; assess the gap candidates against it; add the justified ones; benchmark comments/style (the v1->v2 mapping docs incl. the dependencies/3 = get_dependencies_count nuance); author emq_3_2_check.sh + run it LOOP_N=100 owning the machine; re-prove the 15-attr empty-Lua-diff + the boundary grep; report the final echo_mq test count.

### T-11 — Venus-2 Stage-5 reconcile + emq.3.3.prompt authoring (Director-supervised, emq-3-2-tail, parallel to Mars-2's harden). Adopted the venus charter + loaded echo-mq-architect (re-read echo-mq-program.md + echo-mq-surface.md). aaw register Venus-2/architect (the registered>spawned FAKE-N flag is the known compact-ephemeral cross-turn registry artifact, not a real over-spawn — recorded, proceeding).

THE STATE re-probed (NO-INVENT, against the live tree, not a report): HEAD = 6772c39e "[emq] 3.2. quality gate NOT PASSED" (an Operator out-of-band checkpoint LABEL; the emq.3.2 CODE is green-built and present in HEAD — `git diff --name-only HEAD` for the 6 production files is EMPTY, so worktree==HEAD for them). The ONLY dirty paths are the docs I own: docs/echo_mq/specs/{emq-3-2.progress.md, emq-3-2.registry.json, emq.3.2.{md,stories,llms,prompt}.md}. So my Stage-5 reconciles the docs against an as-built surface that is already committed — the commit bookkeeping (and the "NOT PASSED" label vs the green build) is the Director's Stage-6 concern, NOT a spec⇄as-built contradiction I rule on.

AS-BUILT SURFACE RE-PROBED (the Stage-5 floor — every anchor at live truth):
- flows.ex children_values/3 (:130-141): @spec children_values(GenServer.server(), binary(), binary()) :: {:ok, map()} | {:error, term()}; gates parent_id (the when-guard is_binary), key = job_key(queue, parent_id) <> ":processed", Connector.command ["HGETALL", key]; {:ok, map} (RESP3 native) | {:ok, flat} when is_list -> {:ok, hash_pairs(flat)} (RESP2). hash_pairs/1 (:173) = chunk_every(2) |> Map.new. PURE read.
- flows.ex dependencies/3 (:159-168): @spec ... :: {:ok, non_neg_integer()} | {:error, term()}; key = job_key(queue, parent_id) <> ":dependencies"; ["GET", key]; {:ok, nil} -> {:ok, 0} (the none-key sentinel) | {:ok, n} binary -> String.to_integer | {:ok, n} integer>=0 -> {:ok, n} | other. PURE read.
- jobs.ex complete/5 (:365): def complete(conn, queue, job_id, token, result \\ nil); flow branch argv ++ [parent_id, result || job_id] (:376) — nil default -> job_id (the byte-unchanged non-flow/old-arity path). The @complete Lua attr byte-unchanged (Y-3/Y-4 SHA proof + Director D-6 hash).
- conformance.ex = 46: moduledoc "forty-six" (:3,:26), run/2 "n == 46" (:105), scenarios/0 tail flow_children_values (:67 in scenarios pin). Pins: conformance_run_test.exs:41 {:ok, 46} + "forty-six" (:4,5,28); conformance_scenarios_test.exs @run_order ends :flow_children_values (:67) + "forty-six names" (:4,70).

VERDICT-IN-PROGRESS: the as-built matches the recommended arms R1·B (complete/5 real-result via the byte-unchanged ARGV[5] seam — O1 CLOSED) + R2·A (dependencies/3 = the outstanding COUNT via GET, {:ok,0} sentinel). Conformance 46. O1 is closed (the :processed value is the real result, asserted by flow_children_values_test "results keyed by id != ids"). N1 lifecycle carry stands NAMED (admin.ex untouched — confirmed by the dirty-set excluding it). NEXT: reconcile the triad body + stories + llms to as-built; reconcile emq.testing.md + add testing/emq.3.{1,2}.testing.md; reconcile progress/roadmap/features/emq.3.md; AUTHOR emq.3.3.prompt.md (HIGH-risk cross-queue design-authoring runbook). The §4 testing case count is flagged for the Director's Stage-6 reconcile from Mars-2's final coverage (one-owner-per-file).

### T-12 — the contention discovery + the reconcile plan (records-freeze applied). KEY FINDING: the Operator is reconciling docs/echo_mq/specs/emq.3.2.md CONCURRENTLY (HEAD b26fc24f "[emq] 3.2. bump specs as: QUALITY GATE NOT PASSED" + an uncommitted body diff in the working tree). The Operator's uncommitted diff IS the Stage-5 body reconcile: INV5 collapsed to "R1·B shipped"; ALL DoD boxes ticked [x] with the real as-built anchors + test counts (272/0, conf 46/46, ≥100 loop 120/120, the 15-attr empty diff); N2 records the O2 fold DECLINED; the References footer flipped to "As-built surface (SHIPPED)" with children_values/3 @ flows.ex:130-140, dependencies/3 @ :157-168, complete/5 @ jobs.ex:365-380, parent_of/3 @ :397-405 (live-MATCH all). DECISION (one-owner-per-file + records-freeze): I do NOT contend the body — it is Operator-owned + correct this run. My three "modified since read" edit failures were the guard preventing a competing reconcile (the §7 multi-editor drift). VERIFIED against live code: every footer anchor MATCHes; the one RESIDUAL stale anchor the Operator's diff did NOT sweep = D2's inline add_log precedent "jobs.ex:458" (line ~163) — live add_log/5 is at :539, the <> ":logs" at :541 (emq.3.2's complete/5 docstring pushed it down). Flagged to the Director (a one-line Operator/Stage-6 sweep), NOT contended.

STATUS-VOICE RULING (NO-INVENT + honest-row): the build is GREEN (Y-4: 272/0, conf 46/46, loop 120/120) but the Operator's two latest commits are labelled "quality gate NOT PASSED" + the Operator is reworking the body — so I reconcile the dashboard/roadmap to "built + Stage-2-passed, shipping" and advance the frontier to emq.3.3-next, NOT "✅ SHIPPED" (the Director flips ✅ at the ratifying Stage-6 commit; my brief: "the Director flips to SHIPPED at the commit"). I do not assert a ship the Operator's labels contradict.

THE UNCONTENDED DELIVERABLES (my Stage-5 focus — the Operator is on the body, not these): (B) emq.testing.md (stale at the emq.2.4 board 43/32/251 — add emq.3.1@45 + emq.3.2@46 flow rows alongside the FROZEN §0 board) + NEW testing/emq.3.1.testing.md + testing/emq.3.2.testing.md; (C) emq.progress.md + emq.roadmap.md + emq.features.md + emq.3.md carve (all show emq.3.2 SPECCED/planned, 2 states stale → built/shipping; surface emq.3.3); (D) AUTHOR emq.3.3.prompt.md (the cross-queue design-authoring runbook — HIGH-risk).

AS-BUILT TEST SURFACE re-probed (the §3/§4 ground): flow_add_test 9 + flow_fanin_test 6 + flow_children_values_test 7; echo_mq now 36 test files / 273 cases (grep '  test '); conf 46 (flow_add/flow_fanin/flow_children_values registered, conformance.ex:96-98). The exact §4 final case count flagged for the Director's Stage-6 reconcile from Mars-2's final coverage (one-owner-per-file — Mars-2 owns the code+tests; I record the count its code establishes; 273 is the current live floor). emq.3.md carve (line 192-193): emq.3.2 row 📐 SPECCED → built/shipping (R1·B+R2·A ruled, conf 46); emq.3.3 row = cross-queue, Fork A completion-signal hop (per-queue promote-style sweep, eventually-consistent, conformance flow_cross_queue). Existing testing/ ledgers: emq.1, emq.2.1, emq.2.2, emq.2.3 (+ emq.testing.progress.md) — emq.3.1/3.2 to ADD.

### T-13 — Stage-5 post-build reconcile: the triad body synced to the as-built shipped surface; every anchor re-verified against live code (NO-INVENT); HEAVY concurrent-write environment navigated

THE STAGE: post-build reconcile (the build shipped via the parallel Director commits 6772c39e "quality gate NOT PASSED" → 0f14b1b2 "bump SHIPPED"; HEAD now 0f14b1b2, was 75aebc3b). jobs.ex + conformance.ex + the two pin tests are ALREADY COMMITTED (clean); flows.ex + the test file + the spec docs stay dirty. Re-probed the as-built shipped surface and synced the triad BODY (authoritative) → then stories/brief/runbook up to it.

THE AS-BUILT RESOLVED FORMS (the forks the spec left "the build picks" — now pinned, each verified against live code):
- complete/4 → complete/5 with `result \\ nil` (jobs.ex:365-385); the flow branch passes `argv ++ [parent_id, result || job_id]` (jobs.ex:376) — the result for a flow child, falling back to job_id (the emq.3.1 presence marker) on the nil default. The @complete Lua BYTE-UNCHANGED (SHA-verified, per Y-3). VERIFIED: jobs.ex:365 = `def complete(conn, queue, job_id, token, result \\ nil)`; :376 = `argv ++ [parent_id, result || job_id]`; :385 = closing end.
- dependencies/3 none-key sentinel = {:ok, 0} (flows.ex:176) — the build chose the count's natural floor (no new error vocabulary), NOT the {:error, :not_a_flow} alternative. VERIFIED flows.ex:176.
- children_values/3 = HGETALL→map of :processed, accepts RESP3 map + RESP2 flat via hash_pairs/1 (flows.ex:135-143). VERIFIED.
- conformance 45 → 46 (flow_children_values added; NO separate flow_dependencies scenario — the count read is exercised WITHIN flow_children_values). LIVE-VERIFIED: 46 scenarios, run/2 doc n==46, both pins {:ok,46}, "forty-six". The probe is conformance.ex:1096.
- N2 (the O2 parent_of HGET fold) = DECLINED — the build kept parent_of/3 a separate HGET (jobs.ex:397-405), never touched the claim path. O2 remains an open carry.
- O1 = CLOSED — flow_children_values asserts values == %{c1 => "r-"<>c1, c2 => "r-"<>c2} (the real results, provably != the ids), conformance.ex:1096-1130.

THE SYNC (the body authoritative, then derived files up to it):
- emq.3.2.md (BODY): Status banner → SHIPPED 2026-06-15 at CONFORMANCE 46/46; Goal (3) → complete/5 result\\nil + (4) 45 → 46; D2 → R1·B-shipped (dropped the R1·A hedge) + flows.ex:135-143; D3 → {:ok,0} resolved + flows.ex:172-181/:176; D4 → complete/5 jobs.ex:365-385/:376, the :361 stale anchor removed; INV5 → O1-closed past-fork (dropped R1·A branch) + the conformance.ex:1096 scenario; INV6 → 45 → 46; N2 → fold DECLINED + parent_of/3 jobs.ex:397-405; References "as-built" block → the real shipped anchors (children_values :135-143, dependencies :172-181/:176, complete/5 :365-385/:376, parent_of :397-405, conformance 46/probe :1096); DoD → all 8 boxes [x] with the as-built evidence (272 tests, 120/120 loop, 15-attr empty Lua diff, 46/46).
- emq.3.2.stories.md: US3 sentinel → {:ok,0} resolved; US6 → 45 → 46 / {:ok,46}.
- emq.3.2.llms.md: the as-built read-seams block → complete/5 :365-385/:376 + parent_of :397-405 (fold declined); R3 → {:ok,0} flows.ex:176; R4 → complete/5 result\\nil; R6 → 45 → 46 (no flow_dependencies); build-DAG step 4/5 + AS-6 → 45 → 46.
- emq.3.2.prompt.md: Status → SHIPPED 46/46; the rung-paragraph count → 45 → 46. (The as-built FLOOR section's "do NOT hardcode 45; re-pin 45 → N" left generic — it correctly teaches the discipline, a pre-build instruction.)

THE HEAVY CONCURRENT-WRITE ENVIRONMENT (the durable footgun, HEAVY this run as the memory flags): SIBLINGS wrote the same docs in parallel with my reconcile. (1) A commit (6772c39e "quality gate NOT PASSED") surgically CLOBBERED my body Status banner head — replaced "SHIPPED ... 46/46" with "QUALITY GATE NOT PASSED" + a stray blank line, corrupting the banner into a malformed 3-line head while leaving lines 5-20 of my edit intact. CAUGHT by the NO-INVENT verification sweep (re-`sed` the banner I'd already edited rather than trusting my prior write — the file disagreed with my context). REPAIRED to the proper SHIPPED banner. (2) flows.ex GREW ~13 lines (185 → 198) from a sibling moduledoc expansion BETWEEN my Stage-5 read and my citations — so my first-pass flows.ex anchors (:130-140/:157-168/:163) DRIFTED; re-probed and corrected ALL to the live truth (:135-143/:172-181/:176), then verified each points at real code. (3) A parallel features.md RECONCILER swept the whole file and SUPERSEDED my 3 parity folds with a more-careful "child-result reads BUILT at emq.3.2 (Stage-2 PASS); the Director flips ✅ at the ratifying commit" framing — content-equivalent or better (names the same surfaces + O1 closed + 45→46), so I STOOD DOWN on features.md (re-editing would fight an authoritative parallel sweep). The lesson: in a multi-writer tree, "I edited it" ≠ "it is edited" — the closing verification MUST re-read from disk; a surgical clobber elsewhere in the file passes the Edit tool's "modified since read" guard silently until an independent grep catches it.

THE STATUS TENSION (flagged for the Director, NOT resolved — closure is the Director's per LAW-4): my repaired body banner says "SHIPPED" (consistent with the committed 0f14b1b2 "bump SHIPPED"), but the parallel features.md reconciler uses "BUILT + Stage-2 PASS; ✅ at the ratifying commit" (pre-ratify framing). Whether emq.3.2 is "SHIPPED" or "BUILT-pending-the-ratifying-commit" in the body is the Director's call at Stage-6 — I surface the divergence rather than unilaterally pick.

VERDICT: the triad body is at as-built truth — every cited anchor VERIFIED against live code (NO-INVENT discharged); O1 CONFIRMED CLOSED in the body; the N1 lifecycle carry CONFIRMED NAMED (D5/INV7, admin.ex untouched); N2 recorded DECLINED. NEXT: T-9 + Y-5; the Director's Stage-6 closure owns the SHIPPED-vs-BUILT status + the ratifying commit + the features.md/roadmap/progress frontier fold (the parallel reconciler is handling features.md).

### T-14 — Stage-5 addendum: the @drain second-leak-surface folded into the N1 carry (the Director's re-issued directive item); body N1/D5/INV7 now name BOTH destructive sweeps

THE RE-ISSUED Stage-5 directive carried one NEW item beyond my completed Stage-5 sync (T-13/Y-6): fold the Stage-0 sub-finding (the @drain second leak surface I surfaced in T-5/Y-2 but had only NOTED, not folded into the body's N1 bound) into the N1 carry's scope — name BOTH destructive-sweep leak surfaces for the emq.3.x lifecycle rung. Re-verified on-disk first (the concurrent-write environment): body resolved forms PRESENT (7 hits), features.md row ADVANCED (3 folds by the parallel reconciler), body banner INTACT (no re-clobber) — only the @drain fold was outstanding.

THE TWO LEAK SURFACES (re-confirmed against current admin.ex, NO-INVENT — the file may have drifted since T-5):
- (a) obliterate's del_job — `DEL jk, jk..':logs', jk..':lock'` at admin.ex:152 (a 3-key FIXED list, no flow subkeys). VERIFIED admin.ex:152.
- (b) @drain's wipe() — `DEL jk, jk..':logs'` at admin.ex:90 (a 2-key FIXED list, no :lock, no flow subkeys). VERIFIED admin.ex:90 (@drain at :84, wipe() at :86, drain/3 at :109).
Both are obliterate-class destructive sweeps with FIXED subkey lists that EXCLUDE :dependencies/:processed → the flow subkeys survive BOTH (two at-rest leak surfaces, not one).

THE FOLD (body authoritative — 3 spots synced, internally consistent):
- N1 (the honest bound): now states "TWO Admin-surface destructive sweeps enumerate a FIXED subkey list that excludes the flow subkeys" — names (a) del_job admin.ex:152 + (b) @drain wipe() admin.ex:90; the cleanup disposition (1) the destructive-sweep extension (BOTH del_job AND wipe() gain the two subkeys) + (2) per-flow completion cleanup, both routed to the emq.3.x lifecycle rung.
- D5 (the deliverable): the disposition (a) now reads "BOTH FIXED-list Admin sweeps gain :dependencies/:processed: obliterate's del_job (admin.ex:152) AND @drain's wipe() (admin.ex:90)"; the Check requires the body name BOTH destructive sweeps + per-flow cleanup + the owning rung.
- INV7 (the check): now requires the body name "the TWO FIXED-list destructive sweeps (del_job admin.ex:152 AND @drain's wipe() admin.ex:90) gaining the subkeys, plus per-flow completion cleanup."

INVARIANT HELD: admin.ex UNTOUCHED (git status empty — re-verified clean); emq.3.2 adds ZERO cleanup; the carry is NAMED not discovered (the L-5 lesson). The directive's other items (the resolved forms complete/5 result\\nil + {:ok,0} + 45→46 + N2 declined; O1 CLOSED; the features.md row advanced) remain satisfied from the T-13 pass (re-verified: 9 resolved-form hits, features row 4 hits).

VERDICT: the @drain fold is complete; the body N1/D5/INV7 trio names both leak surfaces consistently; admin.ex untouched. The triad body is at as-built truth with the lifecycle carry fully scoped for the lifecycle rung. NEXT: Y-7 (the addendum report); the Director's Stage-6 closure. (NOTE: 4 new tasks #8-11 — a BDD EchoMQ.Story DSL + mix echo_mq.stories generator + story tests — appeared; these are PRODUCTION-CODE work for Mars/an implementor, NOT Venus's spec-triad scope — left unclaimed.)

### T-15 — Venus-2 post-ratification flip (the Director's D-7 resolution: emq.3.2 RATIFIED → SHIPPED; the durable harness emq_3_2_check.sh 8/8 Director-reproduced; the 3-way voice split resolves on the SHIPPED voice). Two focused edits on the views I own (docs/echo_mq/** only, NO code, NO git):

(1) THE FLIP — emq.3.2 BUILT/🔨 → SHIPPED/✅ at EVERY mention across the 4 views, exhaustive: emq.progress.md (the ladder row :73, the roll-up shipped 7→8 + emq.3.2 added to the shipped list + in-flight reworded :88-92, the mermaid node :115, the Movement-I summary :168, the roadmap-mirror emq.3 leg :204, the next-actions item #2 → done :299, the "Closed since" note :306-309) · emq.roadmap.md (the emq.3 row parenthetical :137, the emq.3.2 row → ✅ shipped + the harness link :139) · emq.features.md (the §172 header, the prose :181, the parity row :291 incl. the status cell ✅/🔨/📋 → ✅/📋, the summary :340) · emq.3.md (the family-status header :4, the carve row :195 🔨 BUILT → ✅ SHIPPED). PLUS the testing.md views (a view too): the §1 rung-table row :139 (🔨 built + close pending → ✅ shipped), the §1 flow prose :146 (built + gate-green → shipped). Ratification note used everywhere: "✅ shipped — ratified; durable harness echo/rungs/bus/emq_3_2_check.sh 8/8, Director-reproduced". CRITICAL HELD: emq.3 the FAMILY stays 🔨 OPEN (11 family-OPEN markers verified intact across the 4 views; 3.3/3.4 ahead) — ONLY the emq.3.2 sub-rung flipped; the family is NOT closed. The roll-up arithmetic kept honest: shipped 7→8 (emq.3.2 joins the shipped list) AND emq.3 stays in-flight (the sub-rung ships, the family doesn't).

(2) THE COUNT — emq.testing.md §4 "test population" echo_mq ≈273 placeholder → Mars-2's measured final 275 (4 doctests + 275 tests, 0 failures); the §1 prose roll-up (:150) updated to the firm 275 + "reconciled at Stage-6 under the one-owner-per-file law (the code+test owner establishes it, this view records it)". Conformance stays 46/46 ({:ok, 46} unchanged). The one shared fact reconciled from Mars-2's code surface exactly as the law prescribes — the doc-owner records the count the code establishes, never the reverse; my 273 was the honest raw grep floor, 275 is the authoritative measure.

BOUNDARY HELD: docs/echo_mq/** only. I did NOT touch the Operator-held triad body (emq.3.2.{md,llms,prompt,stories}.md — the Operator's concurrent SHIPPED-body reconcile, excluded from the Director's commit; the add_log :458→:539 sweep is the Operator's). I did NOT touch the emq.2.4 row's "close pending" (a separate, frozen Director ceremony — ratifying emq.3.2 says nothing of emq.2.4's formal close). The dirty echo/apps/echo_mq/{flows.ex, test/, mix.exs} + echo/rungs/bus/emq_3_2_check.{sh,out} are Mars-2/Operator's, NOT mine (I edit no code). NO git — the Director lands the two pathspec commits + dissolves the team. VERIFICATION: a tight grep confirms ZERO emq.3.2 non-SHIPPED status token across all 5 docs; the 11 family-OPEN markers intact; the count 275 + conf 46.

## {emq-3-2-decisions} Decisions

### D-1 — the conformance floor is 45; the additive-minor target is 45 → N (flow_children_values, + flow_dependencies if R2 ships its own scenario)

LOCKED at the Stage-0 reconcile (2026-06-15). Live count = 45, confirmed three ways: conformance.ex moduledoc "forty-five" (line 3,24) + run/2 doc n==45 (line 102); conformance_run_test.exs:41 {:ok, 45}; conformance_scenarios_test.exs @run_order "forty-five names" (line 20-68). emq.3.2 registers flow_children_values (and, if R2's set/count read warrants a distinct probe, flow_dependencies) WITH the probe in the SAME change, keeps the prior 45 BYTE-UNCHANGED (S-3/§5 additive-minor law), and re-pins 45 → N in BOTH pin tests + their "forty-five"→"forty-N" moduledocs. Authored target: 45 → 46 (flow_children_values asserts the real-result read + the dependencies count in one scenario) with flow_dependencies a SHOULD second scenario (45 → 47) if the count read is verified distinctly. Source: conformance.ex:3,24,102; conformance_run_test.exs:41; conformance_scenarios_test.exs:20-68.

### D-2 — the named subkey-lifecycle disposition: a NAMED CARRY to the emq.3.x lifecycle rung, NOT in emq.3.2's read-only scope (the §2 guardrail discharged)

LOCKED per the echo-mq-architect skill §2 guardrail (Apollo emq.3.1 L-5). emq.3.2 READS :processed/:dependencies; emq.3.1 L-5 established they OUTLIVE the parent row (@complete DELs only KEYS[2] the row, jobs.ex:189; obliterate's del_job enumerates ONLY jk/:logs/:lock, admin.ex:152) AND obliterate. The disposition emq.3.2 NAMES (do NOT leave un-named — the L-5 lesson): (1) the obliterate-sweep — del_job's FIXED enumeration (admin.ex:152) gains :dependencies/:processed (a one-line slot-sound, A-1-clean extension); (2) per-flow completion cleanup — the parent retiring its own :dependencies/:processed once it completes (it has read children_values BEFORE completing, so the subkeys are then dead). BOTH are ROUTED to the emq.3.x lifecycle rung, NOT folded into emq.3.2, because each would re-tier emq.3.2 OUT of its NORMAL-risk read-only boundary: (i) per-flow cleanup edits the SHIPPED @complete (→ HIGH-RISK, mandatory Apollo) and is unsound under deep recursion (a parent that is itself a grandchild — Out); (ii) the obliterate-sweep is an Admin-surface change beyond a read rung's stated boundary (echo/apps/echo_mq Flows reads only). emq.3.2's DECISION: the cleanup is a NAMED CARRY (the obliterate-sweep + per-flow cleanup, stated as the honest bound, routed to the emq.3.x lifecycle rung); emq.3.2 itself adds ZERO cleanup. The emq.3.1 L-5 admin.ex moduledoc carry (the one-line honest-bound note WHEN the lifecycle rung lands) is RE-AFFIRMED, NOT discharged here (admin.ex untouched). Source: admin.ex:125-126,152; jobs.ex:189; emq-3-1 L-5 / T-11.

### D-3 — the risk tier: emq.3.2 is NORMAL-risk (no shipped-script edit), so Apollo is NOT mandatory — CONDITIONAL on R1·B keeping the @complete Lua byte-unchanged

LOCKED. emq.3.1 was HIGH-RISK because it EDITED the shipped @complete Lua (the fan-in branch) → Apollo MANDATORY. emq.3.2 under the recommended arms (R1·B + R2·A) edits NO shipped Lua script: R1·B extends the HOST (complete gains a result arg passed as the EXISTING ARGV[5] slot — @complete's `HSET KEYS[4] ARGV[1] ARGV[5]` at jobs.ex:183 is BYTE-UNCHANGED); R2·A is a pure GET read of :dependencies; children_values/3 is a pure HGETALL read of :processed. So emq.3.2 = NORMAL-risk (a host-API extension + two pure reads + additive conformance). RISK-TIER CONSEQUENCE (the gate the prompt mandates Venus state): emq.3.2 does NOT require a dedicated Apollo evaluator (the Director's solo review + Mars's gate ladder suffice for a NORMAL-risk rung) — BUT the determinism discipline still applies (children_values reads a flow that minted N+1 ids; the read suite is process-touching → the ≥100 loop is RECOMMENDED for any mint/process-touching flow read scenario, per the master invariant). THE FORK-CONDITIONALITY: if the Operator rules R1·A (pure presence read, no completion change) emq.3.2 is EVEN lower-risk (zero host-signature change); if a future variant EDITED @complete's Lua (e.g. to store a richer result shape in-script) it would RE-TIER to HIGH-RISK + mandatory Apollo. The triad is authored to R1·B → NORMAL-risk → Apollo optional. Source: emq.3.1.md (HIGH-RISK rationale = the @complete edit); jobs.ex:183 (the ARGV[5] HSET seam already built).

### D-4 — Director ACCEPTS the emq.3.2 triad + runbook as build-ready; the 2 forks routed to the Operator; the frontier folded

The Director verified Venus's Y-1 against ground truth (the artifact, not the report):
- THE AS-BUILT GROUNDING is correct (re-confirmed independently at emq.3.1 ship): :dependencies a STRING counter (flows.ex SET / jobs.ex DECR); :processed a HASH written HSET KEYS[4] ARGV[1] ARGV[5] (jobs.ex:183); complete/4 passes job_id as ARGV[5] (the O1 presence marker); the @complete fan-in is the SHIPPED Lua; obliterate/del_job a FIXED jk/:logs/:lock (the L-5 leak); conformance = 45. Every anchor MATCH, NO-INVENT.
- THE TWO FORKS are correctly SURFACED-not-decided (build-grade): R1 (result-payload) marked OPEN — "built only after the Operator rules Fork R1" (emq.3.2.md:78) + the DoD D1 gate; R2 (deps-read) surfaced with its recommendation + the explicit "R2·B is NOT free" note. Authored to R1·B + R2·A (the recommended arms) so emq.3.2 is build-ready with no pre-build re-scope if ruled as recommended.
- THE KEY ARCHITECTURAL INSIGHT is sound + grounded: R1·B (the real-result-carrying completion) closes O1 via the EXISTING ARGV[5] seam — the @complete Lua is BYTE-UNCHANGED, only the host wrapper extends → emq.3.2 is NORMAL-risk (NOT a shipped-script edit; the inverse of emq.3.1's HIGH-risk @complete-Lua edit) → Apollo OPTIONAL. The determinism loop still applies (the read suite is mint/process-touching). This NORMAL-risk tier (D-3) is correct.
- THE SUBKEY-LIFECYCLE GUARDRAIL (the freshly-applied echo-mq-architect §2 rule) is DISCHARGED properly (D-2): Venus did not blindly fold cleanup in — it REASONED that per-flow cleanup would edit the shipped @complete (re-tier to HIGH-risk + unsound under recursion) and the obliterate-sweep is an Admin-surface change beyond a read rung's boundary, then NAMED the disposition (obliterate-sweep + per-flow cleanup) and ROUTED it as a carry to the emq.3.x lifecycle rung — "name it, do not discover it." The guardrail's intent is honored; the emq.3.1 admin.ex obliterate-moduledoc carry re-affirmed (admin.ex untouched).
THE DIRECTOR'S ROUTING: the 2 forks (R1 result-payload, R2 deps-read) are the OPERATOR's call (API-contract + representation forks — the architect surfaces, never rules). The triad ships authored to the recommended arms; nothing in emq.3.2's BUILD runs until the Operator rules Fork R1 (R2 is a cheap pre-build narrowing). The Director folded the frontier (emq.progress.md emq.3.2 SPECCED + the roll-up; emq.3.md the carve emq.3.2 row → 📐 SPECCED, the forks noted). VERDICT: the emq.3.2 triad + runbook are BUILD-GRADE (authored, build-ready, forks OPEN); accepted for the records commit. Source: emq.3.2.{md,stories,llms,prompt}.md; the emq-3-2 ledger T-1..T-3, D-1..D-3, V-1/V-2, Y-1.

### D-5 — the byte-unchanged-proof Script.new/2 enumeration corrected to the full 15 as-built (the one Stage-0 BUILD-RUN [RECONCILE]); the triad is build-grade to R1·B + R2·A

LOCKED at the build's Stage-0 reconcile (2026-06-15, Venus-1). The NORMAL-risk HEADLINE proof for emq.3.2 — "a git diff of every Script.new/2 attribute is empty" (INV1/INV3, the single highest-leverage property per the runbook §Risk-tier + D-3) — enumerated only the 8 PRE-emq.2.x scripts (@enqueue/@claim/@complete/@retry/@promote/@reap/@schedule/@enqueue_flow). The live jobs.ex + flows.ex carry 15 Script.new/2 attributes: those 8 + the 7 emq.2.x parity-cluster mutation scripts (@update_data jobs.ex:448, @update_progress :468, @add_log :505, @remove_job :557, @reprocess :613, @extend_lock :637, @extend_locks :649). A diff over only the 8 PASSES even if a build pass touched one of the 7 → the proof had a 7-script blind spot a green board never reveals. SAME CLASS as the venus charter "a gate must specify its OWN liveness" / "a check counts only if it RUNS [the right target]" — a proof whose check under-enumerates its target is underspecified.

THE FIX (the durable, self-maintaining form): replace the incomplete hardcoded 8-item list with "every `@… Script.new/2` attribute in jobs.ex + flows.ex (all 15 as-built — the 8 state-machine/flow scripts … plus the 7 emq.2.x mutation scripts …)". The "every … in jobs.ex + flows.ex" phrasing makes the gate enumerate the REAL set by file-scan AND not re-drift when emq.3.4 adds scripts; the explicit "15 as-built" parenthetical names the current set so Mars/Director can sanity-count. APPLIED to 5 spots (surgical, body authoritative): emq.3.2.md INV1 (the full check) + INV3 (references "all 15 — INV1's full enumeration"); emq.3.2.stories.md US4 (3rd acceptance criterion); emq.3.2.llms.md gate-ladder byte-unchanged-proof step; emq.3.2.prompt.md Stage-2 Director no-shipped-script-edit probe. NOT touched: emq.3.2.md:113 (the Scope "Out" line `@enqueue/@claim/@complete/@enqueue_flow — none` — an illustrative sample of shipped scripts R1·B does not edit, correctly representative not exhaustive); the llms AS-7 (already said "every Script.new/2 attribute", robust).

NO OTHER [RECONCILE]: anchors 1-7 (conformance 45, @complete ARGV[5] seam, @enqueue_flow :dependencies STRING counter, add_log <> ":logs", job_key/2 raises, del_job FIXED list, the v1 reference) all MATCH at as-built truth (T-5). One sub-finding folded into the existing named carry (NOT a new deliverable): admin.ex:88-90 @drain's wipe() is a SECOND obliterate-class leak surface for the flow subkeys (`DEL jk, jk..':logs'`, no :lock, no flow subkeys) — the D-2/D5/N1 lifecycle carry already names "the obliterate-sweep gains :dependencies/:processed"; the @drain path is the same destructive-sweep-with-FIXED-subkey-list class, noted for the emq.3.x lifecycle rung's scope (admin.ex stays UNTOUCHED this rung). One sub-finding folded as a label nuance (NOT a [RECONCILE], the mechanism governs): the body summary-prose label "R2·A = the v1 get_dependencies parity" is imprecise (v1 get_dependencies is the SMEMBERS SET; R2·A's count maps to v1 get_dependencies_COUNT/SCARD) — but the load-bearing brief R3 + body D3 + stories US3 all state the mechanism precisely (a pure GET of the :dependencies STRING counter → a non-neg integer), so Mars is correctly directed; left as-is (the v1 "get_dependencies" reasonably names the family).

CONSEQUENCE: the forks R1·B + R2·A are ruled (T-4 Operator gate) and the triad is authored to them → NO pre-build re-scope. The lifecycle disposition is NAMED (D-2/D5/INV7) and re-affirmed; admin.ex untouched. emq.3.2 is BUILD-GRADE after this one correction. Source: jobs.ex Script.new(:…) grep (15 attrs); emq.3.2.{md,stories,llms,prompt}.md (edited); the venus charter "gate liveness" clause.

### D-6 — Stage-2 Director solo review: PASS, REMEDIATE list EMPTY, Apollo NOT required (NORMAL-risk confirmed)

The Director ran a REAL Stage-2 pass against the artifact (LAW-1a), verifying all seven probes independently:
1. THE EMPTY-LUA-DIFF (the NORMAL-risk headline, INV1/INV3): SHA-256 of ALL 15 Script.new heredocs extracted from `git show HEAD:` vs the working tree — jobs.ex 496122519e…==496122519e…; flows.ex 3dac51b680…==3dac51b680…. IDENTICAL. No shipped Lua script edited — proven by the Director's own hash, not Mars's report.
2. HOST-ONLY complete (D4): the jobs.ex diff touches only the docstring + the `complete/4 → complete/5 (result \\ nil)` signature + ONE argv line (`argv ++ [parent_id, result || job_id]`). The @complete Lua attribute (:152-192) is nowhere in the hunk (:345-376). Default nil → `result || job_id` = job_id for a non-flow/shipped-arity caller → INV3 byte-unchanged by construction.
3. THE TWO PURE READS (D2/D3): HGETALL/GET only, gate parent_id at Keyspace.job_key/2 (INV4 raises), compose subkeys via the add_log `<> ":logs"` precedent, {:ok, 0} none-key sentinel. NO-INVENT confirmed: Connector.command/2 is real (connector.ex:47); Conformance.pairs/1 is the real RESP3/RESP2 precedent hash_pairs/1 mirrors.
4. ADDITIVE-MINOR conformance (INV6): the prior 45 are byte-unchanged (the ONLY change to a prior scenario is a trailing comma on flow_fanin — contract string byte-identical); flow_children_values + its apply_scenario probe + complete_with_result land in the SAME change; both pins re-pinned 45→46 + the moduledocs.
5. INDEPENDENT GATE RE-RUN on Valkey 6390: CONFORMANCE 46/46, 10 tests 0 failures. The new flow_children_values_test.exs (7 tests, async:false, per-test sub-queues) covers INV5 (results≠ids, provably), the none-yet {:ok,%{}}, INV4 raises (both reads), the R2·A count N→0, the {:ok,0} non-flow sentinel, INV2 purity (before/after raw-subkey snapshots).
6. MUTATION SPOT-CHECK (LAW-1a): Edit-in dropped `result || ` from the argv line → the O1-closed test FAILED (children_values returned %{id => id} presence markers, the emq.3.1 O1 state, not %{id => "r-"<>id}) → reverted by INVERSE Edit (NEVER git checkout in a dirty tree — the emq.3.1 L-3 footgun, and the tree carries Operator out-of-band art/mercury work) → jobs.ex SHA 24496d6d… byte-identical to pre-mutation, the diff Mars's 21-line change with zero residue. The suite has teeth.
7. THE INDEPENDENT ≥100 DETERMINISM LOOP: the Director ran 100 iterations over ALL five flow+conformance :valkey suites (flow_add, flow_children_values, flow_fanin, conformance_run, conformance_scenarios) owning the machine, tee'd to /tmp/emq32_determinism.log → 100/100 green, 0 failures. This is the Director's loop, independent of Mars (the umbrella's thrice-burned same-ms mint hazard did not fire).

BOUNDARY: only echo_mq production+test files in the diff (conformance/flows/jobs.ex + the 2 pin tests + the new suite); echo_wire/keyspace.ex/admin.ex/mix.lock/apps/echomq UNTOUCHED; INV7 zero DEL/HDEL/UNLINK of a flow subkey added.

APOLLO DECISION: NOT required. NORMAL-risk confirmed (the empty-Lua-diff is SHA-proven); the O2/N2 fold was DECLINED by Mars (jobs.ex docstring: "the carried perf follow-up the read API declines") so it did NOT reach the claim path; no finding elevates the rung. The Director's Stage-2 review + Mars-2's harden ladder + Venus's Stage-5 reconcile are the NORMAL-risk verification floor.

VERDICT: the build is BUILD-GRADE. REMEDIATE list EMPTY. Proceed to Stage 3 (Mars-2 focused harden — the format posture + the coverage table; the determinism loop is already Director-run).

### D-7 — Stage-6 ratification: the quality gate PASSES (Director independent harness 8/8) → converge the status-voice on SHIPPED; 2 scoped commits; Operator-held triad body + mesh EXCLUDED

THE GATE PASSES (the ratification basis). The Director re-ran the committed durable harness echo/rungs/bus/emq_3_2_check.sh INDEPENDENTLY at LOOP_N=100 (owning the machine) → PASS 8/8: toolchain (erlang 28.5.0.1 / PONG 6390) · compile (--warnings-as-errors clean) · suite (flow_add+flow_fanin+flow_children_values = 25 tests, 0 fail) · conformance ({:ok,46}) · determinism (100/100) · dbsize-flat (64==64) · empty-Lua (15 Script.new heredocs SHA-256-identical ec393a72↔worktree: jobs.ex 2bcd8b2e0436, flows.ex 7218a36342f4) · boundary (no echo_wire/echomq/mix.lock/keyspace/admin; zero flow-subkey DEL/HDEL/UNLINK). This independently reproduces Mars-2's committed .out + the build-run D-6 ≥100 loop → the proof is now DURABLE + Director-reproduced (the emq.2.4 ephemeral-proof anti-pattern is closed). NORMAL-risk (D-3) confirmed; Apollo NOT required.

THE 3-WAY STATUS-VOICE SPLIT (Venus-2 Y-7 escalation) RESOLVED → SHIPPED. (i) The Operator's triad BODY+prompt say "SHIPPED 46/46" (emq.3.2.md:3, Operator-held; Venus-2 verified it MATCHes live code on every anchor — children_values@flows.ex:130, dependencies@:157 + {:ok,0}@:163, complete/5@jobs.ex:365 + result||job_id@:376, @complete Lua @:152-192 byte-unchanged, conf 46, O1 CLOSED, N1 NAMED). (ii) Venus-2's reconciled views (progress/roadmap/features/emq.3.md) say "BUILT + Stage-2 PASS — the Director flips ✅ at the ratifying commit". (iii) The HEAD commit LABELS say "[emq] 3.2. quality gate NOT PASSED" (6772c39e — the RAW build, committed BEFORE this gate ran). RESOLUTION: ratify to SHIPPED. The "NOT PASSED" label was honest at the raw-build commit (the gate had not yet run); THIS run ran it (independent harness 8/8). The views' own text delegates the flip to the Director at the ratifying commit; the user invoked /echo-mq-ship to pass exactly this gate; the Operator's "bump SHIPPED" (0f14b1b2) + the SHIPPED body confirm the ship intent. So: flip the views BUILT → ✅ SHIPPED (ratified — harness 8/8, Director-reproducible); the Operator's SHIPPED body is now honest; convergence on SHIPPED across body + views. NOT an AskUserQuestion — the run state + the explicit ship directive resolve it; the commit is visible + reversible.

THE COMMIT BOUNDARY (the concurrent-index race is HOT — the Operator moved HEAD 75aebc3b→6772c39e→0f14b1b2 mid-run AND is ACTIVELY adding the mesh course + the mesh-writer skill). MY measured surface = 2 scoped pathspec commits:
- COMMIT 1 (the rung ratification — the gate-PASS made durable): echo/apps/echo_mq/lib/echo_mq/flows.ex (Mars-2 v1→v2 docstrings) + echo/apps/echo_mq/test/flow_children_values_test.exs (Mars-2 +3 v1-parity gap tests) + echo/rungs/bus/emq_3_2_check.sh + echo/rungs/bus/emq_3_2_check.out (the durable harness + the 8/8 board) + docs/echo_mq/specs/emq-3-2.progress.md (this ledger).
- COMMIT 2 (the program-doc reconcile + forward-prep): docs/echo_mq/emq.testing.md (+ §4 count → Mars-2's 275) + docs/echo_mq/testing/emq.3.1.testing.md + docs/echo_mq/testing/emq.3.2.testing.md (NEW) + docs/echo_mq/emq.progress.md + docs/echo_mq/emq.roadmap.md + docs/echo_mq/emq.features.md + docs/echo_mq/specs/emq.3.md (the views, flipped SHIPPED) + docs/echo_mq/specs/emq.3.3.prompt.md (the cross-queue design-authoring runbook).
EXCLUDED (Operator-held / concurrent — leave UNTOUCHED, NOT my directive): the triad BODY docs/echo_mq/specs/emq.3.2.{md,llms,prompt,stories}.md (the Operator's concurrent SHIPPED-body reconcile — Venus-2 verified correct + deferred ownership; ONE residual for the Operator's sweep: emq.3.2.md:163 add_log anchor ":458" STALE → live :539/:541); docs/echo/mesh/** ; .claude/skills/mesh-writer/. Re-verify git diff --cached is PURELY my surface immediately before EACH commit; abort on any foreign path (the L-3 concurrent-index discipline).

HONEST BOUNDS (carried, NOT remediated): (1) the format-decline — jobs.ex + conformance.ex fail mix format --check-formatted on a trailing-blank-after-end quirk PRESENT AT THE PARENT ec393a72 (pre-existing, NOT emq.3.2-introduced; emq.3.2's own additions — flows.ex + the test — are format-CLEAN; running mix format would churn the byte-frozen conformance scenarios → INV6 breach) → left untouched (do-no-harm), Director-verified pre-existing. (2) the N1 flow-subkey lifecycle carry (:dependencies/:processed survive @complete + obliterate + @drain) — NAMED + now EMPIRICALLY PINNED by Mars-2's L-5 honest-bound test (a future sweep flips it → prompts the spec update) → routed to the emq.3.x lifecycle rung; admin.ex untouched (INV7). Source: the harness .out (8/8); Mars-2 Y-5; Venus-2 Y-7; the live git status; jobs.ex/flows.ex as-built.

## {emq-3-2-alternatives} Alternatives

### V-1 — FORK R1 (the headline): the result-payload mechanism — pure presence read (Arm A) vs a real-result-carrying completion (Arm B, RECOMMENDED)

OPEN — the Operator's call (the API-contract fork: it changes complete's public arity). Venus surfaces, does not decide.
- Arm A — children_values/3 returns the PRESENCE SET (which children completed): reads :processed as-built (child_id→child_id), no completion change, a pure read. Risk: lowest (zero host-signature change). Cost: NOT the v1 get_children_values parity — answers "which completed", not "what they returned"; ships a read of a placeholder, re-opening O1 indefinitely rather than closing it.
- Arm B (RECOMMENDED) — extend completion to carry a REAL result: complete gains a result argument (complete/5 or an opts result — Mars picks the A-1-clean form) passed as the EXISTING ARGV[5] slot. @complete ALREADY does `HSET KEYS[4] ARGV[1] ARGV[5]` (jobs.ex:183) → the @complete Lua is BYTE-UNCHANGED; only the host wrapper extends (the non-flow completion stays byte-unchanged — KEYS[3] nil → the HSET branch unreached). children_values/3 then returns the REAL results = v1 get_children_values parity. Risk: NORMAL (a host-API extension, NOT a shipped-script edit). Cost: complete's public arity changes — but emq.3.1's L-3 deliberately left this slot for emq.3.2.
- RISK-TIER CONSEQUENCE: Arm B keeps emq.3.2 NORMAL-risk → no mandatory Apollo. A variant editing @complete's Lua → HIGH-RISK (the emq.3.1 precedent). RECOMMENDATION: Arm B (closes O1 + v1 parity + the script does not change). The triad is authored to Arm B with the fork marked OPEN; an Arm-A ruling is a cheap pre-build narrowing (drop the result arg; children_values reads the presence marker). Source: jobs.ex:183,350-361; flow_producer.ex:64 get_children_values; emq.3.1.md O1 / emq-3-1 L-3.

### V-2 — FORK R2: the dependencies-read shape — outstanding COUNT (Arm A, RECOMMENDED) vs the pending SET (Arm B)

OPEN — the Operator's call (a representation fork). Venus surfaces, does not decide.
- Arm A (RECOMMENDED) — dependencies/3 returns the outstanding COUNT: reads :dependencies directly (GET — it is a STRING counter as-built: flows.ex:49 SET, jobs.ex:182 DECR). Minimal: one GET → an integer. A pure read, zero write-side change. Cost: the count says how many remain, not WHICH — the v1 get_dependencies SET answer is not available.
- Arm B — dependencies/3 returns the pending SET (which children remain): needs a NEW :children roster subkey written at @enqueue_flow (the full child-id set) + the complement (:children members − :processed keys). Richer (the v1 get_dependencies set) but adds a §6 subkey AND an @enqueue_flow edit (a flow-SCRIPT edit — re-opens the write surface emq.3.2 otherwise leaves alone, and adds a third subkey to the lifecycle-cleanup carry D-2).
- RECOMMENDATION: Arm A (the count). It reads the as-built :dependencies with no write-side change, holding emq.3.2 to its read-only boundary; the SET answer (Arm B) adds a subkey + a flow-script edit out of proportion to a read rung, and is a clean later carry if a consumer needs "which remain". The triad authors dependencies/3 to the COUNT with the fork marked OPEN; an Arm-B ruling re-scopes to add the :children roster (a pre-build write-surface add — NOT a free re-scope, unlike R1·A→B). Source: flows.ex:49; jobs.ex:182; flow_producer.ex:70 get_dependencies.

## {emq-3-2-report} Report

### Y-1 — emq.3.2 triad + runbook AUTHORED, BUILD-GRADE (build-ready, forks OPEN)

THE RUNG: emq.3.2 = the SECOND sub-rung of the emq.3 flow family — the child-result READS (EchoMQ.Flows.children_values/3 over :processed + dependencies/3 over :dependencies, the v1 get_children_values/get_dependencies parity), which CLOSES emq.3.1's honest bound O1 (the :processed value becomes a real result, not a child_id→child_id presence marker).

THE AS-BUILT GROUNDING (re-probed 2026-06-15, NO-INVENT — every anchor at as-built truth):
- :dependencies is a STRING counter (flows.ex:49 SET KEYS[2] n; jobs.ex:182 DECR) — R2·A reads via GET. CONFIRMED.
- :processed is a HASH written HSET KEYS[4] ARGV[1] ARGV[5] (jobs.ex:183) — child_id→ARGV[5]. CONFIRMED.
- complete/4 (jobs.ex:350-370) has NO result arg; passes job_id as ARGV[5] (jobs.ex:361) = the O1 presence marker. The R1·B seam: ARGV[5] is ALREADY the HSET-into-:processed value → making it a real result changes the HOST ONLY; the @complete Lua (jobs.ex:152-192) is BYTE-UNCHANGED. CONFIRMED.
- obliterate/del_job (admin.ex:152) = a FIXED DEL jk/:logs/:lock — excludes the flow subkeys (the L-5 leak). CONFIRMED.
- LIVE CONFORMANCE = 45 (moduledoc "forty-five"; run/2 n==45; both pin tests {:ok,45} + "forty-five names"). Additive-minor target 45 → N (flow_children_values; +flow_dependencies a SHOULD second scenario).

THE FOUR FILES (one line each):
- emq.3.2.md — the contract: §0 (why second), Goal, 5W, Scope (In/Out + honest bounds N1 lifecycle-carry/N2 O2-optional/N3 single-queue-one-level), D1 fork gate · D2 children_values/3 · D3 dependencies/3 (R2·A count) · D4 the real-result completion (R1·B HOST-ONLY, @complete Lua byte-unchanged) · D5 the NAMED lifecycle disposition · D6 the proof; INV1-INV8 as runnable checks (the headline: the empty git diff of every Script.new/2 attribute = the NORMAL-risk proof); the forks R1/R2; DoD.
- emq.3.2.stories.md — US1-US6 (Connextra + Given/When/Then + INVEST + Implements) + the standing EMQ.3.2-US-GATE Valkey gate + the Coverage map (D1→US1 … D6→US6, US-GATE).
- emq.3.2.llms.md — the Mars brief: References (read-first) · R1-R7 (traced to stories+invariants) · Execution topology (the 6 EXACT files, the build-order DAG, the gate ladder) · AS-1..AS-7 (Directive + Acceptance gate).
- emq.3.2.prompt.md — the runbook: Mode Flat-L2 NORMAL-RISK (Apollo OPTIONAL — the inverse of emq.3.1's HIGH-risk); the fork gate R1 (gates the build) + R2 (R2·B noted NOT free); the as-built floor (re-probe); the pipeline (Venus→Mars-1→Director review→Mars-2→Director ship, Apollo conditional); the Stage-6 commit pathspec; acceptance.

THE TWO FORKS (V-1/V-2, OPEN — Operator's call):
- FORK R1 (headline, GATES the build): R1·A pure presence read vs R1·B (RECOMMENDED) the real-result-carrying completion (complete gains a result arg via the EXISTING ARGV[5] slot; @complete Lua BYTE-UNCHANGED; = v1 parity; closes O1). RISK-TIER CONSEQUENCE: R1·B is host-only → emq.3.2 stays NORMAL-risk → NO mandatory Apollo; a variant editing @complete's Lua → HIGH-RISK. R1·A→B is a FREE narrowing.
- FORK R2: R2·A (RECOMMENDED) dependencies/3 returns the COUNT (pure GET) vs R2·B the pending SET (adds a :children roster subkey + an @enqueue_flow edit — a pre-build write-surface add, NOT free).

THE NAMED SUBKEY-LIFECYCLE DISPOSITION (D-2/D5/INV7 — the §2 guardrail discharged): emq.3.2 READS :processed/:dependencies (they outlive the parent row + obliterate, emq.3.1 L-5). The disposition is NAMED, NOT discovered: (a) the obliterate-sweep (del_job gains the two subkeys) + (b) per-flow completion cleanup — BOTH a NAMED CARRY routed to the emq.3.x lifecycle rung (each re-tiers emq.3.2 out of NORMAL-risk: per-flow cleanup edits the shipped @complete → HIGH-RISK; the obliterate-sweep is an Admin-surface change beyond a read rung). emq.3.2 adds ZERO cleanup; admin.ex untouched; the emq.3.1 obliterate-moduledoc carry re-affirmed.

LIVE CONFORMANCE 45; additive-minor target 45 → N. VERDICT: BUILD-GRADE (authored, build-ready); the forks marked OPEN; emq.3.2 is build-ready to R1·B + R2·A with NO pre-build re-scope if ruled as recommended. NORMAL-risk (no shipped-script edit) → Apollo optional. NO production code touched; NO git; only the four emq.3.2.* files + this ledger.

### Y-2 — Stage-0 BUILD-RUN reconcile: BUILD-GRADE (1 [RECONCILE] corrected — the byte-unchanged-proof enumeration); forks R1·B/R2·A ruled, no pre-build re-scope; lifecycle disposition NAMED

THE RECONCILE DELTA TABLE (the 7 brief anchors, re-probed at HEAD 75aebc3b — emq.3.1 shipped f9849efe is an ancestor):
| # | Anchor | Verdict | Confirmed file:line |
|---|---|---|---|
| 1 | conformance count = 45 | MATCH | conformance.ex:3,24 "forty-five" · run/2 doc :102 "n == 45" · scenarios/0 :49 (45 keys, last = obliterate_grouped) · pins test/conformance_run_test.exs:41 ({:ok,45}) + test/conformance_scenarios_test.exs:68 (@run_order, :4 "forty-five") — NOTE pins are test/conformance_*.exs (FLAT), not test/echo_mq/* |
| 2 | @complete BYTE-UNCHANGED Lua + ARGV[5] seam | MATCH | jobs.ex @complete :152-192; fan-in :181-188 `HSET KEYS[4] ARGV[1] ARGV[5]` at :183 (ARGV[5] IS the :processed value) · complete/4 :350-370 · `argv ++ [parent_id, job_id]` :361 (job_id = current ARGV[5]) · parent_of/3 :382-390 (O2 HGET 'parent') |
| 3 | Flows.add/3 + @enqueue_flow :dependencies STRING counter | MATCH | flows.ex @enqueue_flow `SET KEYS[2] n` :49 (STRING, not SADD/HSET) · KEYS[2]=parent_key<>":dependencies" :85 · CLEAN SLATE: flows.ex has only add/3 + reject_cross_queue/2, no children_values/dependencies symbol |
| 4 | add_log/5 subkey-compose precedent | MATCH | jobs.ex:524-526 `Keyspace.job_key(queue, job_id) <> ":logs"` |
| 5 | Keyspace.job_key/2 gates + RAISES (INV4) | MATCH | keyspace.ex:17-24 (BrandedId.valid?/1, raise ArgumentError) · queue_key/2 :13-15 |
| 6 | del_job FIXED list (L-5/N1 carry, DO NOT EDIT) | MATCH | admin.ex obliterate del_job :150-152 `DEL jk, jk..':logs', jk..':lock'` (excludes flow subkeys) |
| 7 | v1 capability reference (READ only) | MATCH | echomq/flow_producer.ex:64,70 · job.ex:846 get_children_values/1 ::{:ok,map()} (HGETALL→map, EXACT R1·B parity) · :921 get_dependencies/1 ::{:ok,[String.t()]} (SMEMBERS=SET) · :949 get_dependencies_count/1 ::{:ok,non_neg_integer()} (SCARD=COUNT) · keys.ex:46 @job_subkeys, :282 dependencies(ctx,id)=job<>":dependencies" |
| — | byte-unchanged-proof Script.new/2 enumeration | **[RECONCILE]** → corrected | jobs.ex+flows.ex carry **15** Script.new/2 attrs (the spec named 8); see below |

THE ONE [RECONCILE] (corrected, D-5): the NORMAL-risk HEADLINE proof ("a git diff of every Script.new/2 attribute is empty", INV1/INV3) enumerated only the 8 pre-emq.2.x scripts; the live tree has 15 (the 8 + the 7 emq.2.x mutation scripts @update_data:448/@update_progress:468/@add_log:505/@remove_job:557/@reprocess:613/@extend_lock:637/@extend_locks:649). A diff over only the 8 passes even if a build pass touched one of the 7 → a 7-script blind spot in the very proof the rung's NORMAL-risk tier rests on (the "gate must specify its own liveness" class). FIXED to the durable "every `@… Script.new/2` attribute in jobs.ex + flows.ex (all 15 as-built — the 8 … plus the 7 emq.2.x …)" form (self-maintaining, won't re-drift at emq.3.4) in 5 spots: emq.3.2.md INV1 + INV3 · emq.3.2.stories.md US4 · emq.3.2.llms.md gate-ladder · emq.3.2.prompt.md Stage-2 probe.

THE CONFORMANCE COUNT confirmed LIVE = **45** (re-probed, not hardcoded); additive-minor target **45 → N** (flow_children_values; +flow_dependencies a SHOULD second scenario → 45 → 47).

THE FORKS ARE RULED (no re-open): R1 → Arm B (the real-result-carrying completion — complete gains a result arg threaded into the EXISTING ARGV[5] slot; @complete Lua BYTE-UNCHANGED → host-only → NORMAL-risk; closes O1; = v1 get_children_values parity) · R2 → Arm A (dependencies/3 returns the outstanding COUNT — a pure GET of the :dependencies STRING counter). The triad is AUTHORED to both arms → NO pre-build re-scope; the build is build-ready as specified. Verified R1·B is host-only (no @complete body change needed — the Lua already writes ARGV[5] at :183) and the Lua-attribute scripts in jobs.ex/flows.ex are all present.

THE LIFECYCLE DISPOSITION re-affirmed NAMED (D-2/D5/INV7, the §2 guardrail): emq.3.2 READS :processed/:dependencies (they outlive the parent row + obliterate, emq.3.1 L-5); the cleanup is a NAMED CARRY — (a) the obliterate-sweep (del_job gains the two subkeys) + (b) per-flow completion cleanup — both ROUTED to the emq.3.x lifecycle rung (each re-tiers out of NORMAL-risk). emq.3.2 adds ZERO cleanup; admin.ex UNTOUCHED; the emq.3.1 obliterate-moduledoc carry re-affirmed. SUB-FINDING folded into the existing carry's scope (NOT a new deliverable): admin.ex:88-90 @drain's wipe() is a SECOND obliterate-class leak surface for the flow subkeys (DEL jk, jk..':logs' — no :lock, no flow subkeys) — same destructive-sweep-with-FIXED-subkey-list class; noted for the lifecycle rung; admin.ex stays untouched this rung.

ESCALATIONS: none. No spec⇄as-built contradiction; the one [RECONCILE] was a clean correction (the spec prose already said "every"; the parenthetical now matches as-built).

VERDICT: **BUILD-GRADE.** Every anchor at as-built truth; the one enumeration [RECONCILE] corrected; the forks ruled (R1·B + R2·A) with no pre-build re-scope; the lifecycle disposition NAMED; admin.ex untouched. emq.3.2 is build-ready for Mars-1. NORMAL-risk (no shipped-script edit) → Apollo optional. Files edited (the spec triad ONLY, no production code, no git): emq.3.2.md (INV1 + INV3 enumeration → 15) · emq.3.2.stories.md (US4 enumeration → 15) · emq.3.2.llms.md (gate-ladder enumeration → 15) · emq.3.2.prompt.md (Stage-2 probe enumeration → 15). Ledger: T-5, D-5, Y-2.

### Y-3 — Mars-1 Stage-1 build COMPLETE: the child-result reads (D2/D3/D4) + flow_children_values conformance + the :valkey read suite. All gates green; the NORMAL-risk headline (empty Lua diff) PROVEN.

FILES (6, all inside echo/apps/echo_mq):
- lib/echo_mq/jobs.ex — EDIT (host-only): complete/4 → complete/5 (`result \\ nil`); the flow branch passes `argv ++ [parent_id, result || job_id]` (was `[parent_id, job_id]`). ONE functional line. @complete Lua attr (:152-192) BYTE-UNCHANGED.
- lib/echo_mq/flows.ex — EDIT: children_values/3 (HGETALL→map, {:ok,%{}} for none-yet; both RESP3-map + RESP2-flat shapes via hash_pairs/1) + dependencies/3 (GET→non_neg_integer, {:ok,0} none-key sentinel); both gate parent_id at Keyspace.job_key/2 BEFORE the wire; both READ-ONLY.
- lib/echo_mq/conformance.ex — EDIT: appended flow_children_values: to scenarios/0 + apply_scenario(:flow_children_values,…) probe + complete_with_result/2 helper, all contiguous; count re-pin 45→46 in moduledoc + run/2 doc.
- test/conformance_run_test.exs — EDIT: {:ok,45}→{:ok,46} + "forty-five"→"forty-six".
- test/conformance_scenarios_test.exs — EDIT: @run_order append :flow_children_values + "forty-five"→"forty-six".
- test/flow_children_values_test.exs — NEW (:valkey): 7 tests (results-keyed-by-id INV5; empty-parent {:ok,%{}}; ill-formed-id raise INV4; count N→N-k→0 D3; non-flow {:ok,0} sentinel; double-read byte-identity INV2).

FORM CHOSEN: complete/5 with `result \\ nil`. Default semantics: nil → ARGV[5] = job_id (the emq.3.1 presence marker), so the non-flow caller AND any old complete/4 call site take the byte-unchanged path. A flow child completed with a real result → ARGV[5] = result. dependencies/3 none-key sentinel = {:ok, 0} (the count's natural floor; no new error vocabulary; matches D3 first option).

CONFORMANCE: 46 (was 45). flow_children_values asserts INV5 (two children with DISTINCT results "r-"<>c1 / "r-"<>c2 → children_values returns the RESULTS keyed by id, provably != the ids) + the count (2→1→0). The prior 45 byte-unchanged: the ONLY deleted prior-scenario line is `flow_fanin:`, differing by a trailing COMMA only (contract string byte-identical).

EMPTY-LUA-DIFF PROOF (the NORMAL-risk headline): all 15 Script.new/2 heredocs (14 jobs.ex + 1 flows.ex) are byte-identical WORK vs HEAD 75aebc3b by per-attr SHA-256. @complete = 1932 bytes sha 8E64BF50B53B5D71 unchanged. The diff's only +/- lines near Lua are docstrings/comments + the new read-fn bodies — never inside a heredoc.

GATES (per-app, echo_mq, erlang 28.5.0.1 / elixir 1.18.4, Valkey 6390 PONG):
- mix compile --warnings-as-errors: CLEAN (after re-grouping the apply_scenario clause contiguously — a warning, fixed).
- Full echo_mq suite --include valkey: 4 doctests, 272 tests, 0 failures; CONFORMANCE 46/46.
- ≥100 determinism loop (flow_children_values + conformance_run + flow_add + flow_fanin, the mint/process-touching flow suites, owning the machine): 120/120 green, 0 failures. The same-ms mint-collision hazard did not fire.

BOUNDARY: echo_wire / keyspace.ex / admin.ex / mix.lock / apps/echomq ALL untouched (git-verified). INV7: ZERO DEL/HDEL/UNLINK of a flow subkey added.

DO-NO-HARM (flagged): `mix format --check-formatted` flags ~80 PRE-EXISTING long lines in conformance.ex + jobs.ex — verified pre-existing (HEAD's own conformance.ex fails the same check; the app ships non-mix-format-conformant, max line 120). Running mix format would reformat the byte-sensitive prior conformance scenario strings (breaching INV6) + emq.3.1's add/3. NOT run. My own additions ARE format-clean: the NEW test file + both pin-test edits pass --check-formatted; my flows.ex read-fn region is unchanged by the formatter; longest line I added = 95 (< the file's existing 120).

UMBRELLA-WIDE-TEST footgun (recorded): the first run from the umbrella root crashed in mercury_live_admin's test_helper (Postgres too_many_connections) — umbrella-wide mix test is BANNED; re-ran per-app from echo/apps/echo_mq. No echo_mq fault.

No git (Director commits at closure). Ready for Stage-2 Director review.

### Y-4 — Mars-2 Stage-3 HARDEN COMPLETE: full gate ladder re-run independently; REMEDIATE list was EMPTY (Director Stage-2 verdict); the format posture + coverage table delivered. SHIP-GRADE.

TREE INTEGRITY (the emq.3.1 L-3 guard — checked FIRST): HEAD still 75aebc3b; my 6 files all present + dirty (5 M + 1 ??); diffstat matches Stage-1 exactly (conformance 72, flows 69, jobs 21, 2 pin tests, the NEW 7076-byte file). The Director's Stage-2 mutation spot-check left a net-zero tree (LAW-1a) — no git-checkout clobber.

REMEDIATE: the Director's Stage-2 review (Z-/D-5 region) ruled "BUILD-GRADE, REMEDIATE list EMPTY; Apollo NOT required (NORMAL-risk SHA-proven; the O2/N2 fold was DECLINED so it never reached the claim path)". So Stage-3 = re-prove the ladder independently + the two named artifacts. NO code change this pass.

THE FULL LADDER (re-run independently — a check counts only if THIS pass runs it):
- Toolchain re-probe: erlang 28.5.0.1 / elixir 1.18.4 (asdf, no hardcode); Valkey 6390 PONG; {emq}:version = echomq:2.0.0 (fence intact, five-code union unextended).
- Force-clean recompile from echo/apps/echo_mq: Compiling 19 files, --warnings-as-errors, ZERO warnings.
- Full per-app suite --include valkey: 4 doctests, 272 tests, 0 failures; CONFORMANCE 46/46. Multi-seed complement (0/42/312540/999999): all 272/0.
- ≥100 determinism loop (the INDEPENDENT harden run, owning the machine, tee'd /tmp/emq32_harden_loop.log): flow_children_values + conformance_run + flow_add + flow_fanin (the mint/process-touching flow suites). START 04:40:20 → DONE 04:45:39; 120/120 green; 120 "0 failures" + 120 "CONFORMANCE 46/46" lines; ZERO FAILED. The same-ms mint-collision hazard did not fire.
- EMPTY-LUA-DIFF proof re-run: all 15 Script.new/2 heredocs (14 jobs.ex + 1 flows.ex) byte-identical WORK vs HEAD 75aebc3b by per-attr SHA-256.
- INV6 re-proven independently via git: comm -23 of the 45 prior name:"contract" lines (HEAD vs WORK) is EMPTY (no prior contract changed); comm -13 shows ONLY flow_children_values added. The flow_fanin line differs by a trailing comma only.
- INV2 re-proven statically: the read region issues exactly HGETALL + GET, zero mutating commands. INV7: zero DEL/HDEL/UNLINK of a flow subkey in the touch-set.

FORMAT POSTURE (the harden focus, region-by-region re-confirmed): `mix format` NOT run — deliberate do-no-harm. The ~80 --check-formatted hits are 100% PRE-EXISTING long lines in conformance.ex + jobs.ex (HEAD's own conformance.ex fails the same check; the app ships non-mix-format-conformant, max line 120). My OWN additions are ALL formatter-clean (mix format changes NOTHING in them): the NEW test file + both pin-test edits pass --check-formatted; the flows.ex read fns, the jobs.ex complete/5, and the conformance flow_children_values probe each diff-clean against `mix format` output. Running the formatter would rewrite the byte-sensitive prior conformance scenario strings (breaching INV6) + emq.3.1's add/3 — a regression over code I didn't author.

COVERAGE TABLE (every Deliverable + Invariant → its proving check; no gap):
- D1 forks (US1) → R1·B + R2·A ruled in the ledger BEFORE the build (D-5/Z-1); no pre-build re-scope. ✓
- D2 children_values/3 (US2, INV2/4/5) → flow_children_values_test "two children, distinct results → results keyed by id"; "empty parent → {:ok,%{}}"; "ill-formed id raises". ✓
- D3 dependencies/3 (US3, INV2/4) → flow_children_values_test "count N→N-k→0"; "non-flow → {:ok,0} sentinel"; "ill-formed id raises". ✓
- D4 real-result completion (US4, INV1/3/5) → the empty-Lua-diff SHA proof (@complete unchanged) + the distinct-results assertion + the byte-unchanged non-flow path (272/0 incl. all emq.2.x). ✓
- D5 lifecycle disposition NAMED (US5, INV7) → spec body D5/N1 names obliterate-sweep + per-flow cleanup → emq.3.x; INV7 static grep: zero cleanup added; admin.ex untouched. ✓
- D6 proof (US6, INV3/6) → CONFORMANCE 46/46; prior 45 byte-unchanged (comm proof); both pin tests re-pinned; the ≥100 loop. ✓
- INV1 wire law / INV3 byte-unchanged surface → the 15-attr SHA proof + 272/0 (emq.1+2.{1-4}+3.1 unregressed). ✓
- INV8 slot soundness / single-queue one-level → the reads build keys of exactly the parent's {q}; the touch-set is the read surface + the host result arg only. ✓
- US-GATE (Valkey honest-row) → all suites on 6390 the truth row; {emq}:version=echomq:2.0.0; grammar totality (the :processed/:dependencies subkeys classify under §6). ✓
NO GAP. The only un-exercised path by DESIGN: R1·A (the presence-marker arm) — not built (R1·B ruled); the cross-queue/failure/bulk/deep-recursion reads are emq.3.3/3.4 (Out).

BOUNDARY: echo_wire / keyspace.ex / admin.ex / mix.lock / apps/echomq ALL untouched. Files = the 6 inside echo/apps/echo_mq.

REMEDIATE loop: 0 of MAX 3 used (list was empty). No surprises, no escalations. SHIP-GRADE — ready for Stage-5 (Venus post-build reconcile) → Stage-6 (Director closure + the LAW-4 commit). No git (Director commits at closure).

### Y-5 — Mars-2 Stage-3 harden VERDICT: SHIP-GRADE. The fuller harden pass (the two gaps T-8 named) is DONE; emq.3.2 is harness-reproducible at NORMAL risk.

(1) V1-PARITY COVERAGE (echo/apps/echomq flow-read reference, READ-ONLY). The v1 depth bar is 5 flow-READ behaviors (flow_producer_test.exs 27 markers, but only 5 are reads — the rest = add/2 construction=emq.3.1 flow_add, Worker fan-in=emq.3.1 flow_fanin, nested/multi-queue=emq.3.3+):
  v1 get_children_values empty -> {:ok,%{}}          : v2 COVERED (children_values_test "no completed children yet")
  v1 get_children_values post-completion (map_size 2) : v2 COVERED (children_values_test "two children, distinct results")
  v1 get_dependencies_count -> count==3 (3-child)     : v2 COVERED (children_values_test count-down uses 3-child)
  v1 get_dependencies empty (not-a-flow)              : v2 COVERED ({:ok,0} sentinel test) [v1's SMEMBERS set form = Fork R2.B, deliberately NOT built]
  v1 get_children_values mid-handler (runs ON results): v2 GAP -> CLOSED below
  +3 tests ADDED to flow_children_values_test.exs (each proves a real behavior v1 proves and v2 did not yet — NOT padding, INV2):
   T1 "a three-child flow reads back all three distinct results keyed by id" — closes the 2-bounded asymmetry (v1's count test is 3-child; v2 results read was only 2-child).
   T2 "after a PARTIAL fan-in the map holds only the completed child's result" — the "runs ON the results" intermediate semantic (k-of-N: the map grows with fan-in; deps counts N-k). v1's worker reads this map mid-handler; v2 proved only empty+full.
   T3 "the flow-subkey lifecycle honest bound (D-2/N1)" — PINS the as-built L-5 leak (EMPIRICALLY verified on 6390: :processed/:dependencies survive @complete-of-the-parent AND obliterate; del_job's fixed jk/:logs/:lock misses them). Makes the named carry ASSERTED not silent — the gate the emq.3.1 harness forward-points to. admin.ex UNTOUCHED (INV7); a future sweep flips this test -> prompts the spec update.

(2) COMMENT/STYLE PARITY. flows.ex docstrings sharpened to the module-qualified v1 mapping: children_values/3 -> "the parity of v1 EchoMQ.Job.get_children_values/1" (HGETALL->%{child_id=>result}, the partial-fan-in shape mirrors v1). dependencies/3 -> "the parity of v1 EchoMQ.Job.get_dependencies_count/1" WITH an explicit NAME-COLLISION NOTE: v2 dependencies/3 = the COUNT (parity of v1 get_dependencies_COUNT/SCARD), NOT v1 get_dependencies/1 (the SMEMBERS SET = Fork R2.B); v1 :dependencies is a SET, v2 is a STRING counter — a porter is warned. complete/5 doc already carries the result-threading + O1-closure + byte-unchanged mapping (left as-is — it is the write side; cross-refs children_values/3). FORMAT: flows.ex + all touched test files mix-format CLEAN. jobs.ex + conformance.ex FAIL --check-formatted but PRE-EXISTING (both FAIL at parent ec393a72) and DISJOINT from emq.3.2's touch (format wants promote/browse :436/:449 + the byte-frozen scenarios/0 strings — running it would breach INV6) -> documented DECLINE, not run (jobs.ex/conformance.ex untouched by me).

(3) DURABLE HARNESS. echo/rungs/bus/emq_3_2_check.sh authored (mirrors emq_3_1_check.sh: LOOP_N-parameterized default 100; each gate keys off ExUnit ", 0 failures" + exit code, NEVER grep error; owns the machine, tees to file, reports from file) + echo/rungs/bus/emq_3_2_check.out committed (the LOOP_N=100 transcript, exit 0). PASS BOARD 8/8: toolchain(erlang 28.5.0.1/PONG 6390) · compile(warnings-as-errors clean) · suite(flow_add+flow_fanin+flow_children_values=25 tests,0 fail) · conformance({:ok,46}) · determinism(100/100 owning-the-machine) · dbsize-flat(64==64) · empty-Lua(15 attrs IDENTICAL) · boundary(clean).

(4) EMPTY-LUA RE-PROOF + BOUNDARY. The NORMAL-risk headline RE-PROVEN: every @... Script.new/2 heredoc body in jobs.ex+flows.ex (15 as-built = 14 jobs + 1 flows) byte-IDENTICAL ec393a72(pre-emq.3.2) <-> worktree by SHA-256 (jobs.ex 2bcd8b2e0436==, flows.ex 7218a36342f4==; 252+20 body lines). R1.B threads the result through the EXISTING ARGV[5] slot — no shipped Lua edited; the 7 emq.2.x mutation scripts (the once-blind-spot D-5 widened) untouched too. BOUNDARY GREP empty: NO echo_wire/keyspace.ex/admin.ex/mix.lock/apps-echomq in the diff; ZERO DEL/HDEL/UNLINK of a flow subkey added in lib (INV7).

(5) FINAL TEST COUNT (for Venus testing.md): echo/apps/echo_mq full per-app suite TMPDIR=/tmp mix test --include valkey = 4 doctests, 275 tests, 0 failures (272 -> 275, +3 the v1-parity gap tests); CONFORMANCE 46/46 (byte-unchanged 45 + flow_children_values). erlang 28.5.0.1 / elixir 1.18.4.

(6) SURPRISE/ESCALATION: none. HEAD moved 75aebc3b -> 6772c39e mid-flight (Operator committed the emq.3.2 build as "[emq] 3.2. quality gate NOT PASSED" + art/mercury out-of-band) — the concurrent-index footgun the brief warned of; handled (the empty-Lua/boundary proofs diff baseline ec393a72..worktree so they see the committed rung + my additions). My Stage-3 touch-set = 4 paths (flows.ex doc, flow_children_values_test.exs +3, emq_3_2_check.sh NEW, emq_3_2_check.out NEW) — disjoint from Venus-2's docs track (no contention). NO code-logic change (docstrings + tests + harness only); the empty-Lua headline holds. NO git (left for the Director's ratifying commit). REMEDIATE: 0.

### Y-6 — Stage-5 post-build reconcile COMPLETE: the triad body synced to the as-built shipped surface; O1 CLOSED + N1 lifecycle carry NAMED confirmed in the body; every anchor VERIFIED against live code

THE DELIVERABLE: the triad-body sync (the four emq.3.2.* files) brought to as-built truth after the build shipped. The body is authoritative; stories/brief/runbook synced up to it.

THE RESOLVED FORMS (the spec left these "the build picks"; now pinned + each VERIFIED against live code):
| Fork/choice | Spec said | As built | Verified |
|---|---|---|---|
| R1·B result-arg form | "complete/5 or a result: opt" | **complete/5 with `result \\ nil`** | jobs.ex:365-385; `argv ++ [parent_id, result || job_id]` :376 |
| R2·A none-key sentinel | "{:ok, 0} or {:error, :not_a_flow}" | **{:ok, 0}** (count's natural floor) | flows.ex:176 |
| conformance target | "45 → N (+flow_dependencies SHOULD)" | **45 → 46** (no separate flow_dependencies) | 46 scenarios, run/2 n==46, both pins {:ok,46} |
| N2 O2-fold | "optional; the build may decline" | **DECLINED** (claim path untouched) | parent_of/3 stays separate HGET, jobs.ex:397-405 |
| O1 | "closed under R1·B" | **CLOSED** | flow_children_values asserts values == the results != the ids, conformance.ex:1096 |

THE SYNC (one line each):
- emq.3.2.md (BODY): Status → SHIPPED 46/46; Goal/D2/D3/D4/INV5/INV6/N2/DoD/References all pinned to the as-built forms + the real line anchors; DoD's 8 boxes ticked [x] with evidence (272 tests, 120/120 loop, the 15-attr empty Lua diff, 46/46); the R1·A/R2·B fork-hedges dropped (past-fork).
- emq.3.2.stories.md: US3 sentinel → {:ok,0}; US6 → 45 → 46 / {:ok,46}.
- emq.3.2.llms.md: the as-built read-seams + R3/R4/R6 + build-DAG + AS-6 → the resolved forms + 45 → 46.
- emq.3.2.prompt.md: Status → SHIPPED 46/46; the rung count → 45 → 46.

O1 CLOSED — confirmed in the body (INV5 + the Status banner + D4): the :processed value is the real result under the shipped complete/5, proven by the flow_children_values scenario (values == %{c1 => "r-"<>c1, c2 => "r-"<>c2}, provably != the child ids).

N1 LIFECYCLE CARRY NAMED — confirmed in the body (D5/INV7/N1, untouched by the sync): the obliterate-sweep + per-flow cleanup routed to the emq.3.x lifecycle rung; emq.3.2 added ZERO cleanup; admin.ex untouched (re-verified clean).

NO-INVENT DISCHARGED: every as-built line anchor cited in the body was VERIFIED to point at real code (flows.ex:135-143/:172-181/:176, jobs.ex:365-385/:376/:397-405/:183, conformance.ex:1096) — 11/11 OK.

THE HEAVY CONCURRENT-WRITE ENVIRONMENT (HEAVY this run, per the durable footgun): siblings wrote the same docs in parallel. (1) Commit 6772c39e "quality gate NOT PASSED" CLOBBERED my body Status banner head (corrupted it to a malformed "QUALITY GATE NOT PASSED" + blank line) — CAUGHT by re-reading the banner I'd already edited, REPAIRED to SHIPPED. (2) flows.ex grew 185→198 lines from a sibling moduledoc edit between my read and my citations — my first-pass flows.ex anchors drifted; re-probed + corrected ALL to live truth + verified. (3) A parallel features.md reconciler swept the file and SUPERSEDED my 3 parity folds with an equivalent-or-better "child-result reads BUILT (Stage-2 PASS); the Director flips ✅ at the ratifying commit" framing — I STOOD DOWN on features.md (its reconciler is authoritative; re-editing would fight an active sweep). The features.md parity proof the Stage-5 directive named IS folded (by the parallel reconciler, more carefully than my version).

ONE STATUS TENSION FLAGGED (the Director's call, NOT resolved — LAW-4): my body banner says "SHIPPED" (matching the committed 0f14b1b2 "bump SHIPPED"), while the parallel features.md reconciler uses "BUILT + Stage-2 PASS; ✅ at the ratifying commit" (pre-ratify). Whether emq.3.2's body reads "SHIPPED" or "BUILT-pending-ratifying-commit" is the Director's Stage-6 closure decision — I surface the divergence rather than pick.

VERDICT: BUILD-GRADE / the triad body is at as-built truth. O1 CLOSED + N1 NAMED confirmed; N2 DECLINED recorded; every anchor verified (NO-INVENT). Files edited (spec triad only, no production code, no git): emq.3.2.md · emq.3.2.stories.md · emq.3.2.llms.md · emq.3.2.prompt.md. features.md folded by the parallel reconciler (I stood down). Ledger: T-13, Y-5. NEXT: the Director's Stage-6 closure (the SHIPPED-vs-BUILT status, the ratifying commit, the roadmap/progress frontier fold).

### Y-7 — Venus-2 Stage-5 reconcile + emq.3.3.prompt AUTHORED. The four deliverables COMPLETE (docs/echo_mq/** only, no code, no git). ONE load-bearing escalation: a 3-way status-VOICE contradiction across the doc-set (the Director's Stage-6 call).

(A) THE TRIAD BODY — Operator-owned this run, NOT contended. The Operator reconciled docs/echo_mq/specs/emq.3.2.{md,llms,stories,prompt}.md CONCURRENTLY (HEAD b26fc24f + an uncommitted body diff): INV5 → "R1·B shipped"; ALL DoD boxes ticked [x] with as-built anchors + real counts (272/0, conf 46/46, loop 120/120, the 15-attr empty diff); N2 → O2 fold DECLINED; the References footer → "As-built surface (SHIPPED)" with children_values/3 @ flows.ex:130-140, dependencies/3 @ :157-168, complete/5 @ jobs.ex:365-380, parent_of/3 @ :397-405. I VERIFIED every footer anchor against live code = MATCH (re-probed: children_values @ flows.ex:130/132, dependencies @ :157/159 + {:ok,0} sentinel @ :163, complete/5 @ jobs.ex:365 + `result || job_id` @ :376, @complete Lua @ :152-192 HSET @ :183, parent_of @ :397-405, conf 46 @ conformance.ex:3/26/105 + flow_children_values @ :98). O1 CLOSED confirmed (the :processed value is the real result — flow_children_values_test "results keyed by id != ids"). N1 lifecycle carry stands NAMED (admin.ex untouched — confirmed by the dirty-set). My 3 attempted body edits FAILED ("modified since read") — the guard prevented a competing reconcile (the echo CLAUDE.md §7 multi-editor drift). ONE RESIDUAL the Operator's diff did NOT sweep: D2's inline add_log precedent "jobs.ex:458" (emq.3.2.md:163) is STALE — live add_log/5 @ :539, the <> ":logs" @ :541 (complete/5's docstring pushed it down). A one-line Operator/Stage-6 sweep; NOT contended (the file is Operator-held).

(B) THE TESTING DOCS — reconciled (uncontended). emq.testing.md: §1 +2 flow rows (emq.3.1→45 ✅shipped, emq.3.2→46 🔨built+gate-green); §3 +2 sub-tables (the emq.3.1 + emq.3.2 US→test→lane→conf matrices, grounded in the live flow suites); §4 user-story axis 33→45, conformance 43→46, test-population 251→≈273 (FLAGGED "reconciled at Stage-6 from the harden track's coverage" — the one-owner-per-file law; 273 is the live floor: flow_add 9 + flow_fanin 6 + flow_children_values 10 over 251); §5 +2 hot-places (#9 the flow-subkey lifecycle carry incl. the @drain wipe() SECOND leak surface, #10 the O2 declined fold); §6 +2 ledger rows + a records-freeze clause. The §0 emq.2.4 board (43/32/251, 2026-06-14) left FROZEN. NEW: testing/emq.3.1.testing.md (106 lines — the single-queue proof table + the dead-child INV9 + the lifecycle carry) + testing/emq.3.2.testing.md (115 lines — the child-result reads proof table + the empty-Lua-diff proof + the lifecycle carry as a TESTED survival bound; the durable-harness task — Mars-2's emq_3_2_check.sh now EXISTS in the tree).

(C) THE PROGRAM DOCS — reconciled (uncontended). emq.progress.md: +emq.3.2 ladder row (🔨 built); the roll-up (emq.3.2 specced→in-flight built, planned 7→6, "next: emq.3.3"); the Movement-I summary 72%→76%; the roadmap-mirror emq.3 row (+emq.3.2 BUILT); the mermaid node; the next-actions table (#2 → emq.3.2 BUILT Stage-2 PASS, #3 → emq.3.3 the cross-queue design-authoring cycle); the "Closed since" note (+emq.3.1+3.2). emq.roadmap.md: the emq.3 row status + a NEW emq.3.2 BUILT row + a NEW emq.3.3 "NEXT design-authoring" row (Fork A's completion-signal hop, eventually-consistent, HIGH-risk) + emq.3.4 the remainder. emq.features.md: the emq.3.2 paragraph + the flow_producer parity row + the summary (the Operator pre-populated these accurately; I softened "SHIPPED"→"BUILT + Stage-2 PASS" for honest-voice). emq.3.md (the CARVE): the emq.3.2 row 📐 SPECCED → 🔨 BUILT (R1·B+R2·A RULED, conf 46, complete/5 host-only); the family-status header → "OPEN — 3.1 shipped, 3.2 built"; the emq.3.1 row +✅ SHIPPED.

(D) emq.3.3.prompt.md AUTHORED (258 lines) — the DESIGN-AUTHORING runbook (NOT a build runbook; emq.3.3 has NO triad). It fans out a future Venus (+ Director) to AUTHOR the cross-queue-flow quad (emq.3.3.{md,stories,llms} + a later build runbook). Structure mirrors emq.3.2.prompt.md (family-1¶ · rung-1¶ · Mode · the deliverable=quad+ADR · the surfaced forks · the as-built floor · the pipeline · the risk tier · the commit pathspec · acceptance · inputs) but for a design-authoring cycle: Mode = Design-Phase (Director+Venus, no Mars/Apollo; the venus V-SOLO-4 clause — the ADR comes FIRST, the triad derives); the pipeline opens with a DESIGN-MAKE stage (Stage 1 = the cross-queue ADR + the surfaced forks) before any triad lock. THE FOUR SURFACED FORKS (Venus surfaces, the Operator rules): the signal-key shape (a declared §6 subkey vs a per-queue waiting-set) · the sweep cadence/trigger (piggyback EchoMQ.Pump.sweep/1 vs a dedicated flow sweep) · the crash-recovery window (the at-least-once reconciliation — a child completes, the signal drops before the sweep) · the shipped-script touch (@complete must EMIT the signal → the HIGH-risk dimension). RISK TIER (of the build): HIGH — founds the cross-slot completion signal AND likely edits the shipped @complete → Apollo MANDATORY + the ≥100 loop (the INVERSE of emq.3.2's NORMAL-risk), stated so the authoring cycle carries it forward. NO-INVENT grounded: emq.3.md (the carve's emq.3.3 row + Fork A's cross-queue arm — the per-queue promote-style sweep, eventually-consistent, INV7 explicitly NOT atomic, conformance flow_cross_queue) + the as-built emq.3.1/3.2 flow surface + EchoMQ.Pump.sweep/1 (pump.ex:91) + the v1 cross-queue reference (flow_producer.ex add/2 :123, the per-node queue_name spanning :25-52, the data-value parent_key NOT lifted). The cross-queue MECHANISM is written forward-tense + surfaced-not-decided (it IS emq.3.3's design work).

THE ESCALATION (the Director's Stage-6 call — a spec⇄spec status-VOICE contradiction): the doc-set now splits 3 ways. (i) The Operator's triad body + prompt say "SHIPPED 2026-06-15 at CONFORMANCE 46/46" (emq.3.2.md:3, emq.3.2.prompt.md:3). (ii) The views I reconciled (progress/roadmap/features/emq.3.md) say "BUILT + Stage-2 PASS — the Director flips ✅ at the ratifying commit" (the honest-voice I chose: the build is green 272/0 but NOT yet Director-ratified). (iii) HEAD's two latest commit LABELS say "[emq] 3.2. quality gate NOT PASSED" / "bump specs as: QUALITY GATE NOT PASSED" (6772c39e, b26fc24f). I did NOT overwrite the Operator's "SHIPPED" body (Operator-owned this run) and did NOT flip my views to "SHIPPED" (no ratification exists to assert — NO-INVENT). The Director reconciles at Stage 6: either flip the body "SHIPPED"→"BUILT/shipping" to match the views + the commit labels, OR ratify (the green build justifies it) + flip the views to SHIPPED + re-label the commit. Whichever — the body and the views must converge on ONE voice; today they do not, and the "QUALITY GATE NOT PASSED" label is the load-bearing ambiguity. NO production-code contradiction (the as-built MATCHes the spec on every anchor — the split is purely the ship-status voice).

BOUNDARY: my set = docs/echo_mq/** only (8 modified docs incl. the Operator's 4 triad files + my emq.3.md/progress/roadmap/features/testing; 3 new — emq.3.3.prompt + the 2 testing ledgers). The dirty echo/apps/echo_mq/{flows.ex, test/flow_children_values_test.exs} + echo/rungs/bus/emq_3_2_check.{sh,out} are MARS-2's harden track + the durable harness (Task #1 complete), NOT mine — no write contention (disjoint owners, as the brief partitioned). NO git.

### Y-8 — Stage-5 addendum COMPLETE: the @drain second leak surface folded into the N1 lifecycle carry; the body names BOTH destructive sweeps

THE RE-ISSUED Stage-5 directive carried one NEW item beyond the completed Y-6 sync: fold the Stage-0 sub-finding (the @drain second leak surface) into the N1 carry's scope — name BOTH obliterate-class leak surfaces for the emq.3.x lifecycle rung. Re-verified on-disk first (concurrent-write environment): the resolved forms, O1-closed, features.md row, and banner all SURVIVED from Y-6; only the @drain fold was outstanding.

THE TWO LEAK SURFACES (re-confirmed against current admin.ex, NO-INVENT):
- (a) obliterate's del_job — DEL jk/:logs/:lock at admin.ex:152 (3-key FIXED list, excludes flow subkeys). VERIFIED.
- (b) @drain's wipe() — DEL jk/:logs at admin.ex:90 (2-key FIXED list, no :lock, excludes flow subkeys). VERIFIED (@drain :84, wipe() :86, drain/3 :109).
Both exclude :dependencies/:processed → the flow subkeys survive BOTH (two at-rest leak surfaces).

THE FOLD (body authoritative — 3 spots, internally consistent):
- N1 (honest bound): "TWO Admin-surface destructive sweeps enumerate a FIXED subkey list that excludes the flow subkeys" — names (a) del_job admin.ex:152 + (b) @drain wipe() admin.ex:90; disposition = (1) the sweep extension (BOTH gain the subkeys) + (2) per-flow cleanup, both routed to the emq.3.x lifecycle rung.
- D5 (deliverable): the disposition (a) now reads "BOTH FIXED-list Admin sweeps gain :dependencies/:processed: del_job (admin.ex:152) AND @drain's wipe() (admin.ex:90)"; Check requires the body name BOTH sweeps + per-flow cleanup + the owning rung.
- INV7 (check): requires the body name "the TWO FIXED-list destructive sweeps (del_job admin.ex:152 AND @drain's wipe() admin.ex:90) gaining the subkeys, plus per-flow completion cleanup."

INVARIANT HELD: admin.ex UNTOUCHED (git status empty — re-verified clean); emq.3.2 adds ZERO cleanup; the carry is NAMED not discovered.

THE RECONCILE DELTA (this addendum):
| Claim | Status |
|---|---|
| N1 names BOTH leak surfaces (del_job + @drain wipe()) | [RECONCILE] → folded (N1/D5/INV7) |
| admin.ex:90 @drain wipe() = DEL jk/:logs (2-key) | MATCH (verified) |
| admin.ex:152 del_job = DEL jk/:logs/:lock (3-key) | MATCH (verified) |
| admin.ex untouched | MATCH (git clean) |
| O1 closed (Y-6) | MATCH (held) |
| resolved forms complete/5 result\\nil + {:ok,0} + 45→46 + N2 declined (Y-6) | MATCH (held) |
| features.md row advanced (Y-6, parallel reconciler) | MATCH (held) |

VERDICT: BUILD-GRADE / ship-grade. The @drain fold is complete; the body N1/D5/INV7 trio names both leak surfaces consistently; admin.ex untouched; O1 CLOSED + N1 NAMED (both surfaces). The triad body is at as-built truth with the lifecycle carry FULLY scoped for the lifecycle rung. Files edited this addendum (spec body only, no production code, no git): docs/echo_mq/specs/emq.3.2.md (N1 + D5 + INV7). Ledger: T-14, Y-7. The Director's Stage-6 closure owns the ratifying commit + the SHIPPED-vs-BUILT status. (The 4 new tasks #8-11 — a BDD EchoMQ.Story DSL + generator + story tests — are production-code work for Mars/an implementor, outside Venus's spec-triad scope; left unclaimed.)

### Y-9 — Director Stage-6 ratifying report: emq.3.2 SHIPPED (quality gate PASSED — durable + Director-reproduced)

THE VERDICT: emq.3.2 (the flow family's child-result reads — EchoMQ.Flows.children_values/3 + dependencies/3 + the R1·B real-result completion) is RATIFIED → SHIPPED. This /echo-mq-ship tail run converts the Operator's raw build (6772c39e, labeled "quality gate NOT PASSED") into a PASSED, harness-reproducible ship via Stage 3 (harden) + Stage 5 (reconcile) + the Director's Stage-6 ratification.

THE GATE (independent, 8/8): the Director re-ran echo/rungs/bus/emq_3_2_check.sh at LOOP_N=100 owning the machine → PASS 8/8 (toolchain · compile clean · suite 25/0 · conformance {:ok,46} · determinism 100/100 · dbsize-flat 64==64 · empty-Lua 15-heredoc SHA-identical ec393a72↔worktree · boundary clean). It reproduces Mars-2's committed .out + the build-run D-6 ≥100 loop → the emq.2.4 ephemeral-proof anti-pattern is structurally closed (the proof is a committed, re-runnable artifact, not a /tmp tee that a crash evaporates).

THE HARDEN (Stage 3, Mars-2 Y-5): the emq.3.x flow surface benchmarked against the v1 echo/apps/echomq flow reference (the FROZEN line, "while it's not deleted") for comments/style/COVERAGE parity. +3 v1-parity gap tests, each proving a real v1 behavior v2 lacked: the 3-child width read; the partial-fan-in "runs ON results" map (k-of-N); the L-5/N1 lifecycle leak EMPIRICALLY PINNED (:dependencies/:processed survive @complete-of-parent + obliterate — the named carry is now ASSERTED not silent; a future sweep flips the test → prompts the spec). flows.ex docstrings carry the v1→v2 mapping incl. the dependencies/3 = v1 get_dependencies_COUNT (SCARD) name-collision warning (NOT v1 get_dependencies the SMEMBERS set = the unbuilt Fork R2·B). echo_mq full per-app suite: 4 doctests, 275 tests, 0 failures; conformance 46/46. The empty-Lua headline re-proven (15 Script.new heredocs SHA-identical) → NORMAL-risk; no shipped Lua edited (R1·B threads the result through the existing ARGV[5] slot).

THE RECONCILE (Stage 5, Venus-2 Y-7): emq.testing.md brought from the stale emq.2.4 board (43) to the live 46-board (§1 flow rows, §3 flow matrix, §4 counts → 275, §5 the flow-subkey lifecycle hot-place) + new testing/emq.3.1.testing.md + emq.3.2.testing.md; the program views (progress/roadmap/features/emq.3.md) reconciled + the frontier advanced to emq.3.3; the status-voice converged on SHIPPED (D-7). The triad BODY reconcile is Operator-held (concurrent, Venus-2-verified-correct on every anchor) → excluded from the Director's commit.

THE FORWARD-PREP: docs/echo_mq/specs/emq.3.3.prompt.md authored — the DESIGN-AUTHORING runbook fanning out a future Venus+Director to author the emq.3.3 cross-queue-flow quad. Opens with a design-make stage (the cross-queue ADR + 4 surfaced forks: the signal-key shape · the sweep cadence — piggyback Pump.sweep/1 vs dedicated · the crash-recovery at-least-once window · the @complete shipped-script touch). RISK: HIGH (cross-slot completion signal + a shipped-script edit → Apollo MANDATORY + the ≥100 loop — the inverse of emq.3.2's NORMAL-risk). Grounded NO-INVENT in emq.3.md (Fork A's cross-queue arm: eventually-consistent, INV7 not-atomic, conformance flow_cross_queue) + the as-built emq.3.1/3.2 surface + Pump.sweep/1 + the v1 cross-queue reference.

THE BOUNDS (carried, NOT remediated): (1) the format-decline — jobs.ex+conformance.ex fail mix format --check-formatted on a trailing-blank-after-end quirk PRESENT AT THE PARENT ec393a72 (pre-existing; emq.3.2's own additions flows.ex+the test are CLEAN; mix format would churn the byte-frozen conformance scenarios → INV6 breach) → do-no-harm, left, Director-verified pre-existing; (2) the N1 flow-subkey lifecycle carry → NAMED + now PINNED → the emq.3.x lifecycle rung; admin.ex untouched (INV7).

THE COMMITS (Stage 6, 2 scoped pathspec): (1) the rung ratification (Mars-2's flows.ex docstrings + the +3 tests + the durable harness emq_3_2_check.{sh,out} + this ledger); (2) the program-doc reconcile + the SHIPPED view-flip + emq.3.3.prompt.md. EXCLUDED (Operator-held/concurrent): the triad body emq.3.2.{md,llms,prompt,stories}.md + docs/echo/mesh/** + .claude/skills/mesh-writer/. NORMAL-risk; Apollo not required; the --cached boundary re-verified before each commit.

## {emq-3-2-complete} Complete

### Z-1 — emq.3.2 SPECCED: the child-result reads triad + runbook authored (the flow family's read side); forks R1/R2 OPEN

THE DESIGN-AUTHORING PHASE (scope emq-3-2, Director + Venus — no Mars/Apollo, the deliverable IS the spec) is complete. emq.3.2 = the SECOND sub-rung of the emq.3 flow family: child-result reads — EchoMQ.Flows.children_values/3 over the parent's :processed HASH + dependencies/3 over the :dependencies STRING counter (the v1 get_children_values/get_dependencies parity), CLOSING emq.3.1's honest bound O1 (the :processed value becomes a real result, not a child_id→child_id presence marker, via the existing ARGV[5] seam — the @complete Lua byte-unchanged).

AUTHORED (Venus, 4 files): emq.3.2.md (the contract — §0/Goal/5W/Scope, D1-D6, INV1-INV8 as runnable checks, the forks R1/R2, the honest bounds, DoD), emq.3.2.stories.md (US1-US6 + the EMQ.3.2-US-GATE + Coverage), emq.3.2.llms.md (the Mars brief — R1-R7 + topology + AS-1..AS-7), emq.3.2.prompt.md (the runbook — Mode Flat-L2 NORMAL-RISK, Apollo OPTIONAL). Grounded NO-INVENT in the as-built emq.3.1 surface; forward-tense; the forks surfaced-not-decided.

THE TWO FORKS (the Operator's call before the build): R1 (the headline, GATES the build) — the result-payload mechanism: R1·A pure presence read vs R1·B (RECOMMENDED) the real-result-carrying completion (complete gains a result arg via the EXISTING ARGV[5] slot; @complete Lua BYTE-UNCHANGED; = v1 parity; closes O1; NORMAL-risk). R2 — the deps-read shape: R2·A (RECOMMENDED) the outstanding COUNT (pure GET) vs R2·B the pending SET (adds a :children roster + an @enqueue_flow edit — NOT free).

THE RISK TIER: NORMAL (R1·B is host-only, no shipped-script edit) → Apollo OPTIONAL — the inverse of emq.3.1's HIGH-risk (which edited the @complete Lua). The ≥100 determinism loop still applies to the mint/process-touching read suite.

THE SUBKEY-LIFECYCLE GUARDRAIL (the echo-mq-architect §2 rule, Operator-granted + applied this cycle from Apollo's emq.3.1 L-5): DISCHARGED — emq.3.2 NAMES the disposition (the obliterate-sweep + per-flow completion cleanup) and ROUTES it as a carry to the emq.3.x lifecycle rung (each would re-tier emq.3.2 out of NORMAL-risk); emq.3.2 adds ZERO cleanup. The guardrail proven in practice on the very next rung.

CONFORMANCE 45 → N (flow_children_values; +flow_dependencies a SHOULD). Ledger: T-1..T-3, D-1..D-4, V-1/V-2, Y-1, Z-1. NEXT: the Operator rules Fork R1 (then R2), then /echo-mq-ship emq.3.2 builds it (NORMAL-risk, Apollo optional). The Director commits the authoring (records) + the L-5 guardrail (Operator-granted) by pathspec; the team dissolves.

### Z-2 — emq.3.2 SHIPPED: the child-result reads ratified (quality gate PASSED, durable harness 8/8) — the build-tail run complete

emq.3.2 (the flow family's child-result reads — EchoMQ.Flows.children_values/3 + dependencies/3 + the R1·B real-result completion) is RATIFIED + SHIPPED. This /echo-mq-ship tail run (scope emq-3-2) converted the Operator's raw build (6772c39e "quality gate NOT PASSED" + 0f14b1b2 "bump SHIPPED") into a PASSED, harness-reproducible ship: Stage 3 (Mars-2 harden — v1-parity coverage + the durable harness) ‖ Stage 5 (Venus-2 reconcile — testing/progress/roadmap to the 46-board + emq.3.3.prompt) → Stage 6 (Director ratification).

THE GATE: the Director's independent re-run of echo/rungs/bus/emq_3_2_check.sh at LOOP_N=100 owning the machine → PASS 8/8 (toolchain · compile · suite 25/0 · conformance {:ok,46} · determinism 100/100 · dbsize-flat 64==64 · empty-Lua 15-heredoc SHA-identical ec393a72↔worktree · boundary). Reproduces Mars-2's committed .out + the build-run D-6 loop. NORMAL-risk confirmed (the empty-Lua headline — no shipped Lua edited; R1·B threads the result through the existing ARGV[5] slot); Apollo not required. The emq.2.4 ephemeral-proof anti-pattern is structurally closed — the proof is a committed, re-runnable artifact.

THE LAW-4 COMMITS (2 scoped pathspec; the concurrent-index race handled by a per-commit git diff --cached re-verify): (1) the rung ratification — flows.ex docstrings + flow_children_values_test.exs (+3 v1-parity gap tests) + emq_3_2_check.{sh,out} + this ledger; (2) the program-doc reconcile + the SHIPPED view-flip + emq.3.3.prompt.md. EXCLUDED (Operator-held / concurrent — left untouched): the triad body emq.3.2.{md,llms,prompt,stories}.md + docs/echo/mesh/** + .claude/skills/mesh-writer/ + the Operator's new echo_mq scaffolding (mix.exs, lib/mix/, test/{stories,support}/).

THE STATUS-VOICE CONVERGED on SHIPPED (D-7): the views flipped 🔨 BUILT → ✅ SHIPPED (ratified, harness 8/8 Director-reproduced), the emq.3.2 determinism figure normalized to the harness's verifiable 100/100 (NOT the emq.1 loop-#1 120/120 — real history, left), the §4 count to Mars-2's measured 275; emq.3 the FAMILY stays OPEN (3.3/3.4 ahead). The Operator's SHIPPED body is now honest (the gate it claimed has run + passed).

THE FRONTIER: emq.3.3 (the cross-queue flow) — the design-authoring runbook emq.3.3.prompt.md is authored (HIGH-risk; opens with a design-make stage + 4 surfaced cross-queue forks: the signal-key shape · the sweep cadence — Pump.sweep/1 vs dedicated · the crash-recovery at-least-once window · the @complete shipped-script touch). Movement I closes after emq.3.3 (cross-queue) + emq.3.4 (failure-policy + bulk).

CARRIED BOUNDS: (1) the format-decline (jobs.ex/conformance.ex pre-existing trailing-blank quirk at ec393a72; emq.3.2's own additions clean; mix format would breach INV6 on the byte-frozen scenarios) — do-no-harm; (2) the N1 flow-subkey lifecycle carry — NAMED + EMPIRICALLY PINNED (Mars-2's L-5 honest-bound test) → the emq.3.x lifecycle rung; admin.ex untouched (INV7).

The team emq-3-2-tail (Mars-2 + Venus-2) is dissolved after the commits. Ledger this turn: T-8/T-10/T-11/T-12 · D-7 · Y-5/Y-7/Y-9 · Z-2.
