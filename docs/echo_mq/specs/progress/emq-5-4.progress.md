# emq-5-4 — AAW scope ledger

## {emq-5-4-thinking} Thinking

### T-1 — emq.5.4 §0 derivation (the partitioned finish + dynamic delay) · the batches-family closer

5W. WHO: Flat-L2 (Venus author+reconcile · Mars build · Director verify+ship; Apollo optional — NORMAL rung). WHAT: the last emq.5 rung — (1) the **partitioned finish**: a claimed batch resolves as a partition over the shipped per-member transitions (complete / retry-poison-alone / dead), host orchestration, no edit to a shipped script; (2) **dynamic delay**: a new `Jobs.delay/N` re-scores an ACTIVE member onto the schedule set from the handler, attempts preserved (a "not now", not a failure-retry). WHEN: 5.1/5.2/5.3 shipped, conf 70, HEAD ecb48cd9. WHERE: echo/apps/echo_mq ONLY (host verbs in jobs.ex + a pure partition helper + conformance scenarios). WHY: close the batches family — give the batch handler a structured whole-batch resolution + a handler-priced delay without burning an attempt.

The as-built floor (probed). @schedule jobs.ex:55 · @complete :257 · @retry :334 · @promote :394; host enqueue_at :84 · claim_batch :520 · complete/5 :589 · retry/7 :759 · promote/2 :845. NO `delay`/`reschedule` verb exists — delay is genuinely new. Conf list = 70 (counted lib/echo_mq/conformance.ex:89-158); pins test/conformance_run_test.exs:56 `{:ok, 70}` + test/conformance_scenarios_test.exs:33/107 `@run_order`. (Moduledoc OPENING prose lags — "fifty-five"/"sixty-four" — narration only, not a count-law breach; Venus trues it up when she extends the narrative.)

THE LOAD-BEARING FINDING (re-frames FORK 5.4-A). The carve's lean — "a thin delay/N over the byte-frozen @schedule" — is mechanically BLOCKED by the as-built. @schedule's first two acts (jobs.ex:59-70): `if EXISTS(KEYS[1])==1 then return 0` (idempotency guard — refuses any id whose row already exists) and `HSET … 'attempts','0'` (resets attempts, writes a FRESH row, requires payload re-supplied). An active member's row ALREADY exists with state=active, attempts=N — so @schedule on it no-ops (:duplicate) or, absent the guard, would wipe attempt history. A true active→schedule re-score must `ZREM active` + `HSET state=scheduled` (PRESERVE attempts) + `ZADD schedule now+delay` on the server clock. That is NOT what @schedule does → the lean is wrong.

Solution space (incl. do-nothing). (0) DO-NOTHING: 5.2's settle/3 already routes per-member complete/retry from a verdict map; MISSING is (a) a first-class partition value over the three outcome classes and (b) a handler-driven delay (active→schedule) that preserves attempts. Handler hand-rolls both — rejected. (A) reuse byte-frozen @schedule — BLOCKED (above). (B) a NEW minimal `@delay` script: ZREM active / HSET state=scheduled (attempts preserved) / ZADD schedule (server TIME for relative, caller ms for absolute, matching @schedule's two modes) — ONE additive script, atomic, declared-keys [job_row, active, schedule] all emq:{q}:-rooted. Contradicts the carve's "zero new Lua in 5.4" but is the HONEST minimum. (C) fold into promote/schedule — @promote moves due-scheduled→pending (wrong direction); overloading it is worse. Likely rejected.

The honest fork (Venus to frame as Arms). The real choice is **B (a new minimal attempts-preserving @delay re-score, atomic in ONE script) vs a host-only two-step (ZREM active + enqueue_at) that is NON-ATOMIC** — a crash between the two strands the member in neither set (lost job). Atomicity ⇒ one script ⇒ B. So the reconcile should converge on a new minimal @delay; FORK 5.4-A's "reuse @schedule / zero new Lua" lean is the thing Venus corrects against as-built. SECONDARY fork to surface: does delay/N require the lease TOKEN (EMQSTALE-guarded like complete/retry, so a stale lease-holder cannot delay a member another worker now owns)? Lean: YES — delay is a lease-holder action.

Invariants as runnable checks. (1) Byte-freeze: @schedule/@complete/@retry/@promote bodies byte-identical to HEAD — `git diff` jobs.ex shows 0 changed lines inside those Script.new bodies; the only NEW redis.call lines are @delay's. (2) Declared-keys on @delay: every KEYS[n] a real key, the {q} slot pinned by braced keys, no ARGV-base-derived key (the F-1/A-1 rule). (3) Server clock: @delay's relative-delay score reads TIME in-script, never the caller's clock; an absolute due-time is the caller's ms (the documented score-only client-clock surface, like enqueue_at). (4) Attempts preserved: a delayed member's attempts field unchanged across delay (the partition's "delay" ≠ retry) — asserted. (5) Partition exhaustive+disjoint: every claimed member lands in exactly one of {completed,retried,dead,delayed}; dead EMERGES from retry at max_attempts (not a caller verdict) — a pure, doctested function. (6) Conformance additive-minor: prior 70 byte-unchanged + git-verified, new scenario(s) probe-registered, re-pin BOTH (run_test {:ok,70+N}, scenarios_test @run_order). (7) Determinism: MULTI-SEED sweep, NO ≥100 loop — delay RELEASES a lease (active→schedule), it does not mint an id or acquire a lease, so no same-ms branded-mint hazard (carve §3: 5.2/5.4 introduce no new mint/lease).

Risk & planes. NORMAL — reuses shipped byte-frozen transitions; the one new @delay is the INVERSE of a claim (releases, never acquires/mints). No destructive at-rest op, no wire break. Wire @wire_version FROZEN echomq:2.4.2 (additive new script, like @bclaim/@gbclaim); rung label → 2.5.2 (within-family patch); conf 70 → 70+N. Apollo OPTIONAL (out-of-pipeline; the user authorized a calibration fold this session).

Smallest change that preserves correctness. ONE new minimal @delay (active→schedule, attempts-preserving, server-clock) + `Jobs.delay/5` (conn, queue, job_id, token, delay_ms) EMQSTALE-guarded; the batch verdict vocabulary gains a `{:delay, ms}` variant routed to delay; a pure partition helper maps verdict→per-member call; N conformance scenarios (the partition over a batch + the dynamic re-score + attempts-preserved + the stale-delay refusal). STOP-gate before Mars: FORK 5.4-A ruled via AskUserQuestion (A/B/C + the token sub-fork).

### T-2

V-1 — emq.5.4 lag-1 reconcile + FORK 5.4-A framing (the partitioned finish + dynamic delay)

RECONCILE (every surface grounded in echo_mq as-built):
- @schedule jobs.ex:55-73, enqueue_at/6 :84, enqueue_in/6 :95, private schedule/7 :106 — MATCH.
- @complete jobs.ex:257, complete/5 :589 — MATCH (byte-frozen reuse target). @retry jobs.ex:334, retry/7 :759 — MATCH (the `{:ok,:dead}` emerges at the attempts cap, NOT a caller verdict). @promote jobs.ex:394, promote/2 :845 — MATCH.
- BatchConsumer.settle/3 batch_consumer.ex:257-269 — MATCH (the emq.5.2 verdict-map router: `:ok`→complete/5, `{:error,reason}`→retry/7; absent-member fail-safe retry). BatchShaper.Core decide/4 — MATCH. Events.publish/5 events.ex:117 — MATCH (the per-member event seam).
- Conformance: 70 scenarios; pins conformance_run_test.exs:56 `{:ok,70}` + conformance_scenarios_test.exs:33-104 `@run_order` (70 names) + :107 — MATCH. Moduledoc OPENING prose LAGS ("fifty-five" :3, "sixty-four" :55) vs live 70 — narration, not a count-law breach (count-law lives in the two pins, which read 70). Mars trues it up when extending the narrative.
- Two version planes: rung label echo_mq/mix.exs:7 = "2.5.1" → 5.4 climbs to "2.5.2"; wire @wire_version echo_wire/.../connector.ex:35 = "echomq:2.4.2" FROZEN (no new wire class). MATCH.

THE CRITICAL RECONCILE TARGET — Director T-1, CONFIRMED against source (did NOT take on faith):
@schedule (jobs.ex:59-70) opens `if redis.call('EXISTS', KEYS[1]) == 1 then return 0 end` (the idempotency guard) and `HSET KEYS[1] 'state' 'scheduled' 'attempts' '0' 'payload' ARGV[2]` (RESETS attempts to 0, writes a FRESH row, DEMANDS the payload). An already-ACTIVE batch member's row EXISTS with state=active, attempts=N → @schedule on it returns 0 (`:duplicate` host-side) and NEVER re-scores it; if the guard were bypassed it would WIPE the attempt history + demand a re-supplied payload. => @schedule CANNOT re-score an active member. The carve's "reuse @schedule / zero new Lua in 5.4" lean (emq.5.md:60,108) is MECHANICALLY WRONG for an active→schedule re-score. A true re-score must ZREM active + HSET state=scheduled (PRESERVING attempts) + ZADD schedule now+delay on the server clock — the inverse of @claim (releases a lease, mints nothing).

FORK 5.4-A FRAMING (honest arms per as-built):
- Arm B ◄ RECOMMEND — a NEW minimal @delay script (additive, atomic in ONE inline Script.new: ZREM active / HSET state=scheduled attempts-PRESERVED / ZADD schedule; server TIME relative-delay + caller-ms absolute due — the two modes @schedule documents). Costs ONE new Lua (corrects "zero new Lua") but is the atomic minimum.
- Arm A′ — host-only two-step (ZREM active then enqueue_at) — NON-ATOMIC: a crash between strands leaves the member in NEITHER set (lost). Reject on the atomicity invariant.
- Arm C — fold into promote/schedule — @promote moves due-scheduled→pending (WRONG direction). Reject.
- Token sub-fork: delay/N SHOULD be token-fenced (EMQSTALE, the complete/retry pattern) — a stale lease-holder must not re-delay a member another worker now owns. LEAN: token-required.

POSTURE: conformance 70→70+N (partition-over-a-batch · dynamic-delay re-score · attempts-preserved-across-delay · stale-delay EMQSTALE-refusal); additive-minor (prior 70 byte-unchanged + git-verified, new probe-registered, BOTH pins re-pinned). Determinism: MULTI-SEED sweep, NO ≥100 loop (delay RELEASES a lease, no new mint/lease per carve §3). Risk NORMAL (one new additive script — the @delay — NOT a frozen-script edit; @complete/@retry/@schedule stay byte-frozen).

The partition: a claimed batch resolves as {completed, retried, dead, delayed} where the per-member verdict is `:ok | {:error,reason} | {:delay,ms}`; `dead` EMERGES from retry/7 returning `{:ok,:dead}` at max_attempts (NOT a caller verdict); pure, exhaustive+disjoint over the verdict map. Extends emq.5.2 settle/3 — `{:delay,ms}` is the new vocabulary (the THIRD settle branch).

### T-3

V-1 — emq.5.4 triad AUTHORED + verified (BUILD-GRADE pending the FORK 5.4-A ruling)

FOUR files authored under docs/echo_mq/specs/emq2/emq.5/emq.5.rungs/:
- emq.5.4.md (authoritative body: §0 slice, Goal, 5W, Scope, 8 INVs, FORK 5.4-A four-part Arms + token sub-fork, DoD, map)
- emq.5.4.stories.md (6 US: PARTITION/DELAY/STALE/CADENCE/ADDITIVE/GATE + Coverage)
- emq.5.4.llms.md (Mars brief: References paths-first, R0-R7 traced, topology + file set + DAG, AS-1..AS-5)
- emq.5.4.prompt.md (runbook: Mode Flat-L2, OPEN-fork routing, gate ladder, boundary, NORMAL risk, DoD)

VERIFICATION (the L-2 discipline — report exactly the scope verified):
- msh specs echo_mq @severity=error over the WHOLE docs/echo_mq tree = NO FINDINGS (the actual tree-wide run, not "my files resolve" generalized). Cross-links hand-verified against the real FS: 4-up canon (../../../../emq.{design,roadmap}.md), ../emq.5.md carve, same-dir emq.5.2.md, 5-up specs.approach.md all resolve.
- msh hygiene: bracket-then-paren KEYS[n](word) dead-link pattern = 0 (used KEYS[n]=word throughout).
- Framing: 0 violations (the only 2 grep hits are the propagation-clause STATEMENT quoting the banned words as the rule, per the emq.5.3.llms.md template). Zero migration framing = 0.

KEY DESIGN DECISIONS recorded in the body:
- The CARVE LEAN CORRECTED: "reuse @schedule / zero new Lua in 5.4" is mechanically wrong (T-2). The body states this in the header + §0 + FORK 5.4-A, and the DoD's Stage-5 item directs updating the carve's emq.5.4 row + FORK 5.4-A line to record the ruled mechanism.
- FORK 5.4-A framed Arm B (recommend) / A′ (reject-atomicity) / C (reject-direction+byte-freeze) + token sub-fork (recommend token-required). Ready for the Director's AskUserQuestion.
- The partition = pure EchoMQ.BatchFinish.partition/N → %{completed,retried,dead,delayed}; dead EMERGES from @retry {:ok,:dead}; extends emq.5.2 settle/3 with the {:delay,ms} THIRD branch.
- Posture: conf 70→70+N additive-minor; MULTI-SEED sweep NOT ≥100 loop (no new mint/lease); wire FROZEN echomq:2.4.2, label →2.5.2.

STATUS: BUILD-GRADE conditional on R0 (the FORK 5.4-A ruling). Every surface MATCH-grounded or forward-tense for the unbuilt; no STALE/INVENTED/MISSING. Director rules the fork, Venus re-derives to the ruling, THEN Mars builds.

### T-4

V-Consolidator — consolidation: B · T · N established + KB decision-record written

CONSOLIDATION (genuine synthesis, each arm's Steelman weighed against its Steward — not a rubber-stamp): converged on the Director's expected B · T · N, every reuse loser kept on record with its best case.
- Decision 1 (FORK 5.4-A, the delay re-score) = Arm B, a NEW minimal atomic @delay (ZREM active → HSET state=scheduled attempts-PRESERVED → ZADD schedule; server-clock relative / caller-ms absolute). Carrying reason: the ONLY arm both atomic AND attempts-preserving — the inverse of @claim. A′ rejected on atomicity+attempts (host two-step strands the member in NEITHER set on a crash, lease already released, reaper can't recover; routes @schedule's attempts'0'); C rejected on direction (@promote moves due-scheduled→pending) + byte-freeze (NORMAL→HIGH).
- Decision 2 (token sub-fork) = Arm T, token-required delay/5 (EMQSTALE), symmetric with complete/5:589 / retry/7:759 / extend_lock/5:1142. F (token-free) rejected for the handler's verb — a control-plane "push out" is a separate operator verb later (the emq.4.1 reassign/drain precedent).
- Decision 3 (partition surface) = Arm N, a NEW pure EchoMQ.BatchFinish.partition/N → %{completed,retried,dead,delayed} (exhaustive+disjoint; dead EMERGES from @retry {:ok,:dead}), mirroring emq.5.2's pure-core/process split (BatchShaper.Core.decide/4 split from BatchConsumer). X (fold into settle) rejected.

CROSS-FORK PATTERN recorded: all three are reuse-shipped-surface vs add-one-minimal-new-surface, and each reuse path breaks an earlier-rung invariant — atomicity (A′), lease-fence uniformity (F), pure-core/process split (X). B·T·N reinforces into ONE coherent design: a new pure BatchFinish routes {:delay,ms} → token-fenced atomic @delay.

POSTURE: conf 70→70+N additive-minor (re-pin BOTH pins — conformance_run_test.exs:56 {:ok,70} + conformance_scenarios_test.exs:33 @run_order/:107) · label 2.5.1→2.5.2 · wire @wire_version FROZEN echomq:2.4.2 · NORMAL · MULTI-SEED sweep not the ≥100 loop (delay releases a lease, partition is pure → no new mint/lease, carve §3).

GROUNDING (re-verified at source, NO-INVENT): @schedule jobs.ex:55 EXISTS-guard :59-61 + HSET attempts'0' :70 (the load-bearing finding, confirmed not on faith) · @complete:257/complete/5:589 (EMQSTALE→:stale :616) · @retry:334/retry/7:759 ({:ok,"dead"} arm :807→{:ok,:dead} :834) · @promote:394 (ZRANGEBYSCORE due→ZREM→re-enqueue, due-scheduled→pending direction confirmed :398-405) · enqueue_at:84/enqueue_in:95 · extend_lock/5:1142/@extend_lock:1065 · BatchShaper.Core.decide/4 batch_shaper/core.ex:76 (pure, doctested :63-72) · conformance scenarios/0 :87 run/2 :179, pins read 70 (moduledoc opening prose lags "fifty-five" :3 — narration, not a count-law breach) · mix.exs:7=2.5.1, connector.ex:35=echomq:2.4.2.

ONE GROUNDING CORRECTION: the brief/ledger cite "BatchConsumer.settle/3" as a public router; at source it is `defp settle(s, members, verdicts)` batch_consumer.ex:257-269 — a PRIVATE process method (routes :ok→Jobs.complete :261, {:error,reason}→Jobs.retry :265, missing-verdict fail-safe :259, publish/3 :274 over Events.publish/5 events.ex:117). The correction STRENGTHENS Decision 3 (Arm N): a private method that does IO is unmistakably a process method → the pure partition must NOT be folded into it.

KB at docs/echo_mq/kb/emq-5-4-batches-finish-decisions.md (the *-fork-decision.md genre; { id="emq-5-4-batches-finish-decisions" } anchor; corpus voice). msh specs echo_mq @error over the whole tree = my file produces ZERO findings (the lone error is the PRE-EXISTING ledger artifact emq-5-4.progress.md:59 where the prose KEYS[n](word) hygiene note is mis-parsed as a link — not mine). bracket-then-paren=0, framing=0.

### T-5

V-1 — emq.5.4 triad + carve TRUED-UP to the ruling B · T · N (spec files only; Mars-1 owns code in the disjoint tree)

SCOPE (5 files, SPEC ONLY — no production code touched):
1. emq.5.4.md (body) — FORK 5.4-A collapsed OPEN→RULED: header status (RULED B·T·N + D-1/D-2/D-3 + KB link), §0 "Why NORMAL", Goal (a)/(b)/(c), 5W What/Who/When/Where, Scope In/Out (chosen-against arms A′/C/F/X recorded as road-not-taken), INV header + INV-Delay-Atomic (Arm-C hedge removed) + INV-Delay-Token (att-token fence + the settle-threads-att grounding), the §"rung's forks" REWRITTEN (3 ruled sub-sections: FORK 5.4-A→Arm B, token sub-fork→Arm T, partition→Arm N, + the fourth-move cadence branch, + the cross-fork pattern), DoD (item 1 ✓ RULED), the reuse-targets footer (settle/3→defp settle + the att fence).
2. emq.5.4.stories.md — header (OPEN→RULED B·T·N), US-DELAY/US-STALE/US-CADENCE (Jobs.delay/N→/5, settle/3→defp settle, att-token), US-GATE + Coverage + traceability closer.
3. emq.5.4.llms.md (Mars's archival brief) — header (RULED), §1 References (settle/3→defp settle + THE FENCE-ARG RESOLVED: att, the attempts-token, threaded to Jobs.complete:261/Jobs.retry:265, each member {id,_payload,att}@:258 — recorded per D-2's grounding note), R0 gate (OPEN→RULED-proceed), R1-R4 + topology + DAG + AS-1..AS-5 + short prompt.
4. emq.5.4.prompt.md (runbook) — intro fork-note, rung paragraph, Mode (AskUserQuestion→RULED), the "fork is OPEN" section REWRITTEN to RULED B·T·N (3 decisions + cross-fork pattern), gate-ladder notes, boundary, Risk tier, DoD.
5. emq.5.md (family carve) — item (5): the emq.5.4 row trued up (the partition = NEW pure BatchFinish D-3=N; delay = NEW atomic @delay D-1=B; the "reuse @schedule/zero new Lua" lean CORRECTED), the family-total note (":63" — "one additive script per CLAIM rung PLUS one for the RESOLVE half @delay; zero new Lua in 5.2" — the "zero new Lua in 5.4" read corrected), the §4 FORK 5.4-A line (RULED B·T·N + the correction).

THE GROUNDING CORRECTION (Director's #3) — PROPAGATED: every "BatchConsumer.settle/3" cite → the PRIVATE defp settle(s,members,verdicts) batch_consumer.ex:257-269 (a process method that does IO). Re-verified at source. STRENGTHENS D-3=Arm N (a private IO method is unmistakably a process method → the pure partition stays a separate module).
THE FENCE-ARG (Director's #4) — RESOLVED at source + RECORDED: defp settle destructures {id,_payload,att}@:258 and passes att to BOTH Jobs.complete@:261 (at /4) and Jobs.retry@:265 — the batch path fences by the ATTEMPTS-TOKEN (att), NOT a lock-token. So the {:delay,ms} branch passes att identically; @delay's in-script fence mirrors @complete's/@retry's. Recorded in the brief §1 + R3/R4/AS-2/AS-3 + the body INV-Delay-Token. (Mars re-grounds at source; Director-verify re-grounds the thread.)

HYGIENE (the L-2 discipline — exact scope): msh specs echo_mq @error tree-wide → the ONLY 2 errors are the PRE-EXISTING ledger false-positives at progress.md:59/:87 (the KEYS[n](word) hygiene-note prose mis-parsed; Director-flagged, records-frozen, NOT touched). MY 5 FILES = ZERO findings. Bracket-then-paren KEYS[n](word)=0. Framing=0 (2 grep hits = the propagation-clause statement). Residual OPEN-fork/Venus-recommends/assumes-Arm-B in the 4 triad files=0. KB links depth-verified (rungs→kb = ../../../../ 4-up; carve emq.5.md→kb = ../../../ 3-up — counted against the real FS, the L-2 trap avoided). Version planes consistent: label 2.5.2 ×4, wire echomq:2.4.2 FROZEN ×4, 70→70+N ×4, B·T·N ×5.

STATUS: triad + carve trued-up to B·T·N, msh-clean. Mars-1 builds the ruled mechanism from the synced brief.

### T-6 — Director pre-grounding (verify-prep, ahead of Mars-1's report; the shipped scripts are byte-frozen so this reads a stable tree). INDEPENDENTLY CONFIRMED the att-token fence at source — the crux of D-2=T. @complete (jobs.ex:372-377) opens: `local att = redis.call('HGET', KEYS[2], 'attempts'); if not att then return 0 end; if att ~= ARGV[2] then return redis.error_reply('EMQSTALE complete token mismatch')`. The row's attempts IS the fencing token (ARGV[2]='token' per the :336 header), minted by @claim (:285) and @bclaim (:328) via `HINCRBY <row> 'attempts' 1` — @bclaim's comment names it "the per-member fencing token, INV6" (:303). So @delay's fence (HGET the row attempts -> compare to the passed att-token -> EMQSTALE on mismatch) is a FAITHFUL MIRROR of @complete's existing fence, NOT a new fence style. This independently CONFIRMS Venus-1's T-5 (the batch path fences by att; defp settle passes att to complete/4 + retry at batch_consumer.ex:261/265). Triple-grounded with Mars-1's independent grounding pending.

Attempts-PRESERVATION (Arm B's ratified semantic, KB §6.1): @delay reads attempts ONLY to fence and leaves the field UNTOUCHED at N — the contrast is with @schedule's `attempts '0'` reset (:70), NOT with @claim's increment. The conformance assertion is `attempts == N after delay` (preserved, not 0). SEMANTIC NOTE flagged to the Operator (NOT a re-open — the ratified "untouched" stands): a delayed member re-enters via the NORMAL schedule->pending->@claim path, whose HINCRBY re-increments N->N+1, so repeated delays advance a job toward max_attempts. Coherent with "preserve, don't reset"; a future "delay exempt from the attempt budget" would be a distinct, un-ruled design.

NOT independently re-read this turn (deferred to the Stage-3 verify against Mars's actual diff): @retry's {:ok, :dead} emergence (the partition's `dead` class, D-3=N) — the KB line numbers have drifted (KB ':834' is now parent_fail_link's @spec; @retry's Script.new is past @complete's long flow-fan-in body, not :334). Mars grounds it; the verify re-confirms. Holding for Mars-1's report, then the Stage-3 Director verify (independent gate re-run on Valkey 6390 + byte-freeze grep + declared-keys on @delay + the att-token thread + a net-zero attempts-preservation mutation).

## {emq-5-4-decisions} Decisions

### D-1 — FORK 5.4-A (delay re-score mechanism) -> Arm B: a NEW minimal atomic @delay script. Operator-ratified under the architect's-approach four-part method (KB docs/echo_mq/kb/emq-5-4-decisions.md §6.1). @delay = one EVAL: fence the in-flight member -> ZREM active (release the lease) -> HSET state=scheduled with attempts PRESERVED (the defining delta from @schedule's HSET attempts '0') -> ZADD schedule at server-TIME-relative now+ms (the run-in mode, @schedule math jobs.ex:63-66) or caller-absolute ms (the run-at mode). The inverse of @claim (active->scheduled, mints nothing). CHOSEN-AGAINST: Arm A' (host two-step ZREM active then enqueue_at) -> NON-ATOMIC: a crash between the ZREM and the schedule write leaves the member in NEITHER set (lost; lease already released so the reaper cannot recover it) + routes through @schedule whose HSET attempts '0' (:70) wipes attempt history -> breaks INV-Delay-Atomic + INV-Delay-Rescore. Arm C (fold @promote/@schedule) -> @promote moves due-scheduled->pending (the WRONG direction, jobs.ex:398-405) + any fold EDITS a byte-frozen script -> re-grades NORMAL->HIGH (Apollo mandatory). Load-bearing finding (T-1, source-confirmed): @schedule's EXISTS==1 -> return 0 guard (jobs.ex:59-61) no-ops on a live row and HSET attempts '0' (:70) resets attempts -> the carve's "reuse @schedule / zero new Lua" lean is mechanically blocked for an active->scheduled re-score. The four shipped scripts @schedule/@complete/@retry/@promote stay BYTE-FROZEN; @delay is the ONLY new Lua.

### D-2 — the delay fence (the token sub-fork) -> Arm T: delay/N is FENCED — it refuses (EMQSTALE) to re-score an in-flight member without the holder's authorization, symmetric with the bus's other in-flight transitions (@complete / @retry / @extend_lock). Closes the stale-holder race: worker A stalls -> reaper recovers the member -> worker B re-claims it; a stale A must NOT be able to re-delay B's member. CHOSEN-AGAINST: Arm F (token-free delay) -> breaks lease-fence uniformity (any caller could re-delay any active member by id, yanking it from its owner); a token-free operator "push-out" is a SEPARATE control-plane verb to add later (the emq.4.1 reassign/drain precedent — operator verbs are their own surface). GROUNDING NOTE binding on the build: the exact fence ARGUMENT the settle {:delay, ms} branch passes MUST match how its siblings Jobs.complete / Jobs.retry fence in defp settle (batch_consumer.ex:261 / :265). The KB §1 records complete is invoked there at /4 (the batch path) — so Mars grounds att-token-vs-lock-token AT SOURCE (complete/5 :589, retry/7 :759, the settle call sites) and threads @delay's fence IDENTICALLY to its siblings; the @delay script's in-script fence mirrors @complete's / @retry's. The Director-verify re-grounds this thread. The PRINCIPLE (D-2=T) holds regardless of the arg: delay does not yank an in-flight member without the same authorization complete/retry demand.

### D-3 — the partition surface -> Arm N: a NEW pure module EchoMQ.BatchFinish.partition/N -> %{completed, retried, dead, delayed} (exhaustive + disjoint over the claimed members; dead EMERGES from @retry's {:ok, :dead} outcome at the attempts cap (jobs.ex:807/:834), NOT a caller verdict). Mirrors emq.5.2's DELIBERATE pure-core/process split — BatchShaper.Core.decide/4 (batch_shaper/core.ex:76, doctested) was split OUT of the BatchConsumer process so the central decision stays pure + doctested. CHOSEN-AGAINST: Arm X (fold into the private settle/3 + BatchShaper.Core in place) -> settle is a PRIVATE PROCESS method that does IO (calls Jobs.complete/retry + publishes — batch_consumer.ex:257-269); folding the pure partition RE-BURIES the logic 5.2 paid to split out + grows a shipped 5.2 surface rather than leaving it byte-stable. GROUNDING CORRECTION folded (source-confirmed): the brief/ledger "BatchConsumer.settle/3" is the PRIVATE defp settle(s, members, verdicts), not a public router — which STRENGTHENS Arm N (a private IO method is unmistakably a process method, exactly why the pure partition must not fold into it). The fourth move (NOT a fork — it follows from D-1..D-3): defp settle gains ONLY the {:delay, ms} routing branch (the THIRD beside :ok / {:error, reason}) routing through delay/N + a delayed per-member event over the byte-frozen Events.publish/5 (events.ex:117); settle stays a thin router, the partition stays pure.



## {emq-5-4-report} Report

### Y-1 — Stage-3 Director verify: BUILD-GRADE, zero defects. An independent pass (not a glance) against the disk on Valkey 6390.

STATIC AXIS (clean):
- @delay (jobs.ex:155-176) satisfies B·T·N: atomic one-EVAL (kind-guard -> fence -> ZREM active -> HSET state scheduled -> ZADD schedule); attempts-PRESERVED by OMISSION (HSET writes only 'state', no 'attempts' field); token-fence `if att ~= ARGV[2] then EMQSTALE` is a faithful mirror of @complete:372-377 (confirmed T-6); declared keys are the STRONGEST A-1 form (KEYS[1]=row, KEYS[2]=active, KEYS[3]=schedule -- all real KEYS[], none ARGV-derived, the row key a first-class declared root).
- Jobs.delay/6: token-fenced, :in/:at modes (enqueue_in/at pair), returns :ok | {:error,:stale} | {:error,:gone} | {:error,:kind}.
- defp settle (batch_consumer.ex:266-301): behavior-preserving refactor (Enum.each -> Enum.map_reduce), the :ok/{:error} paths byte-behavior-identical, the {:delay,ms} THIRD branch routes Jobs.delay with the SAME att token + a 'delayed' event; returns BatchFinish.partition.
- EchoMQ.BatchFinish (new, pure): partition/2 total + exhaustive + disjoint; `dead` EMERGES from the {:ok,:dead} OUTCOME not the verdict (un-spoofable); doctested + a thorough unit suite (exhaustive/disjoint/order/fail-safe/empty).

BYTE-FREEZE: PROVEN -- jobs.ex is +115/-0 (zero deletions, so no shipped script line could change); the redis.call +/- grep shows 5 added lines (all @delay), 0 removed. @schedule/@complete/@retry/@promote/@claim/@bclaim/@gbclaim/@enqueue byte-identical. @wire_version frozen echomq:2.4.2 (untouched, out of boundary).

CONFORMANCE (additive-minor): the prior 70 byte-unchanged (only the unavoidable continuation comma after grouped_batch_fairness); 3 new scenarios (batch_partition, batch_delay, batch_delay_stale) probe-registered in the same change; BOTH pins re-pinned 70->73 (conformance_run_test.exs:58, conformance_scenarios_test.exs @run_order). The new scenarios carry explicit no-op-defeaters + at-rest cross-checks; batch_delay_stale does a SCRIPT FLUSH to exercise the EVALSHA cold-cache path.

DYNAMIC AXIS (green + deterministic): independent gate re-run = 11 doctests, 462 tests, 0 failures, CONFORMANCE 73/73 (seed 0). Multi-seed sweep seeds 1-5 all 462/0 -> 6 deterministic green runs. NORMAL-rung posture: multi-seed, NOT the >=100 loop (delay releases a lease + mints nothing -> the same-ms branded-id mint hazard is absent by construction). 7th deliverable (story-gen) shipped: batch_finish_story_test.exs (3 BDD scenarios) + docs/echo_mq/stories/batch-finish.stories.md + README.

NET-ZERO MUTATION (LAW-1a, test teeth): injected the @schedule attempts-reset bug into @delay's HSET -> conformance batch_delay FAILED {:fail,{:ok,"0"}} AND the story 'attempts preserved' scenario FAILED (story_test.exs:104), an ISOLATED 72/73 kill (no collateral); reverted by inverse Edit -> jobs.ex back to +115/-0, the formerly-failing suites green (73/73, 0 failures), residual grep clean. Two independent suites have teeth on attempts-preservation.

DEFECTS: ZERO. Mars-2 COLLAPSED (emq.5.1 precedent -- no remediate items).

OBSERVATIONS (non-blocking):
- O-1 (forward-looking): the partition is computed in settle + returned from flush + directly tested, but NOT surfaced as a NEW external batch-level event -- consistent with Arm N's minimal-surface ruling; US-PARTITION is satisfied by the pure module + its direct test. A future telemetry rung (emq.8) could surface it.
- O-2 (out-of-boundary, pre-existing): an echo_data compile warning (champ_view.ex:82, EchoData.ChampView referencing EchoStore.Graft.read_at) -- a dependency app, NOT introduced by this rung, does not fail echo_mq's --warnings-as-errors gate. An echo_data concern, separate.

Risk-tripwire (emq.4.1 lesson): no new mint/lease surfaced mid-build -> NORMAL held, no re-grade. Apollo OPTIONAL (no process/lease/destructive/frozen-line touch) -- closure already met (stories shipped). Ready for the LAW-4 ship pending the Operator's go-ahead.
