# emq-4-3 — AAW scope ledger

## {emq-4-3-thinking} Thinking

### T-1 — emq.4.3 §0 derivation (park-don't-poll metronome; Fork A → Arm B)

[5W] Harden/found the wake-notify beat so a parked consumer is served within the beat with NO lost wakeup + fair wakes across parked consumers. Movement II, groups family, 3rd sub-rung (after 4.1 control plane ✅, 4.2 recovery ✅). Mode Flat-L2, HIGH-risk (Apollo MANDATORY, ≥100 determinism loop, deepened Director verify).

[As-built, VERIFIED] The park-don't-poll CORE is SHIPPED & robust: consumer.ex spawn_link loop (:40), beat_ms 1000 (:58), loop reap→promote→drain→park (:91-98), park = BLPOP emq:{q}:wake <beat> via Connector.command/3 (:144-149). Wake-push (LPUSH 'wake' + LTRIM 0 63) baked into every serviceable transition: jobs.ex ×4, stalled.ex ×1, lanes.ex grouped path ×1. Single per-queue wake LIST capped 64, shared across all lanes/consumers. The token-LIST design AVOIDS the classic lost-wakeup by construction (the wake persists; BLPOP consumes it; drain is exhaustive so one token triggers full service).

[Solution space incl. do-nothing] The shipped loop is robust on the happy path — do-nothing is a real baseline. Risk concentrates in (a) multi-consumer/multi-lane fairness on the single shared wake, (b) cross-run same-ms branded-id mint contention.

[Reconcile deltas the build MUST carry] (1) seed repeats "prior 52 byte-unchanged" — live pin is {:ok, 55} (conformance_run_test.exs:48) → re-pin 55→N; (2) invariants pin {emq}:version = echomq:2.0.0 — live climbing fence is echomq:2.4.2 → emq.4.3 advances it to echomq:2.4.3.

[Invariants as runnable checks] lost-wakeup (:valkey admit-while-parked + park-boundary race → served within the beat); fairness (N parked, none starved); server clock on any touched lease (grep host-ts = 0); additive-minor (prior 55 byte-unchanged, re-pin both pinning tests); byte-freeze unchanged script logic (grep redis.call on diff = 0 where the rung doesn't name it); no wire break.

[Smallest change] Under Arm B: the minimal NEW blocking-claim primitive that subsumes the wake-token two-step; the mechanism + the frozen-wire question are the next fork (D-1).

[Toolchain] Erlang 28.5.0.1 / Elixir 1.18.4 (echo/.tool-versions); Valkey 6390 → PONG.

### T-2 — V-1 emq.4.3 reconcile begins (Arm B re-derivation)

Director ruling D-1: Fork A → Arm B (found a NEW blocking-claim primitive that subsumes the shipped wake-token two-step). The seed emq.4.3.md was authored to Arm A (deepen) — must re-derive to Arm B.
First reconcile reads confirm two deltas already:
- consumer.ex:147 — park/1 = `Connector.command(s.conn, ["BLPOP", wake, secs], s.beat_ms + 2_000)`. The connector ALREADY carries an arbitrary blocking command with a custom (beat+2000ms) timeout. The seed's "no new connector verb needed for BLPOP" is MATCH.
- consumer.ex:91-97 — loop = check_control → reap → promote → drain(rotating claim) → park. drain (114-142) is exhaustive recursion until Lanes.claim → :empty. So one wake token triggers full lane service (the lost-wakeup-by-construction argument the seed makes).
- consumer.ex:40 spawn_link (NOT GenServer) MATCH; beat_ms default 1000 :58 MATCH.
Next: the FROZEN WIRE verdict (echo_wire Connector public API + the committed freeze records), the wake-push sites (lanes.ex/jobs.ex/stalled.ex), the live conformance count + version fence.

### T-3 — emq.4.3 reconcile COMPLETE + FROZEN-WIRE VERDICT

RECONCILE TABLE (all MATCH except 2 STALE deltas, both carried):
- consumer.ex: park loop spawn_link:40 (NOT GenServer) / beat_ms 1000:58 / loop reap→promote→drain→park :91-97 / park/1 = Connector.command(conn,["BLPOP",wake,secs],beat_ms+2000):144-149 — all MATCH the seed.
- lanes.ex: @gclaim ring rotation `LMOVE ring ring LEFT RIGHT`:38, server-clock TIME lease :50-51, ZPOPMIN head; wake-push (LPUSH p..'wake' '1' + LTRIM 0 63) in @genqueue:30-31 @gresume:77-78 @glimit:94-95 @greassign:132-133 @greap_group:375-376. Single per-queue wake LIST capped 64. MATCH.
- jobs.ex wake-push: @complete:200-201 @retry:277-278 @promote:336-337 @reap:366-367. stalled.ex @sweep_stalled:83-84. All server-clock TIME. MATCH. (7 wake-push scripts total across the 3 files.)
- DELTA 1 (count): live conformance = {:ok, 55}, NOT seed's 52. conformance_run_test.exs:48 `== {:ok, 55}`; conformance_scenarios_test.exs @run_order = 55 names (last: dedup_release region). emq.4.2 prompt confirms 54→55 at 4.2 ship. RE-PIN 55→N.
- DELTA 2 (fence): @wire_version "echomq:2.4.2" (connector.ex:35), NOT seed's "echomq:2.0.0". emq.4.2 climbed 2.4.1→2.4.2 in lockstep (mix.exs label + @wire_version + {emq}:version, D-3). emq.4.3 climbs 2.4.2→2.4.3. NOT 3.0.0 (that MAJOR is ratified at emq.8).

FROZEN-WIRE VERDICT: Connector/RESP/Script frozen by committed records (echo_wire.ex:12-14 states it; EchoWire facade = the forward name). Public connector surface: command/3, pipeline/3, eval/5, push_command/3, subscribe/2, unsubscribe/2, noreply_pipeline/3, transaction_pipeline/3, stats, wire_version. command/3 (connector.ex:49) carries ARBITRARY parts with a custom timeout — park's BLPOP rides it (consumer.ex:147). A blocking BLMOVE/BLMPOP would ride the SAME command/3 verb → NO new connector verb, NO echo_wire edit, NO facade change for either Arm-B mechanism. The connector is a serialized FIFO GenServer — a blocking command/3 holds the WHOLE connector for the block; the consumer already holds a DEDICATED connector lane (moduledoc "blocking verbs get their own lane"), so this is sound.

### T-4 — emq.4.3 triad authored to Arm B + link-rebase defect caught

TRIAD AUTHORED (Arm B, D-1): docs/echo_mq/specs/emq2/emq.4/emq.4.rungs/emq.4.3.{md,stories.md,prompt.md}. The .md RE-DERIVED from Arm A→Arm B (grep-confirmed no stale "Arm A recommended" language survives); .stories.md + .prompt.md authored fresh.
MECHANISM SUB-FORK framed (FORK A-MECH, withheld per D's instruction): three arms, each four-part (Rationale/5W/Steelman/Steward+Cost) with FROZEN-WIRE verdict + touch-set + §6 question + floor-raise + risk:
- MECH-(i) RECOMMENDED: server-side BLMOVE block-move on the shipped emq:{q}:wake → atomic @gclaim. Smallest founding that genuinely subsumes the two-step; block-target IS the readiness structure (lost-wakeup closed by construction); reuses emq:{q}:wake (NO §6); @gclaim + 7 wake-push scripts byte-frozen; §12.2 honored. Touch: consumer.ex park/1 + conformance + mix.exs + tests. Floor: BLMOVE 6.2 (LMOVE's blocking sibling, §12.5) = minor.
- MECH-(ii): dedicated metronome process. Largest touch-set; a process re-org that doesn't by itself found a new blocking primitive (§12.2 still forces block-then-@gclaim). Dis-recommended alone.
- MECH-(iii): per-lane emq:{q}:wake:<group> LIST = a NEW §6 type member (CLOSED registry, keyspace.ex:14) → grammar edit (FORK A-MECH-§6) + touches all 7 wake-push scripts (re-addressing, INV4). Answers the thundering-herd but widest; can layer on MECH-(i) later.
FROZEN-WIRE VERDICT (headline): NO echo_wire edit for ANY mechanism — Connector.command/3 (connector.ex:49) carries arbitrary blocking commands (park's BLPOP proof, consumer.ex:147); Connector/RESP/Script frozen by committed records (echo_wire.ex:12-14). Only the @wire_version CONSTANT moves (2.4.2→2.4.3, lockstep D-3) — a constant move, not a logic edit (the records freeze the CONTRACT, not the version string).
TWO RECONCILE DELTAS carried: (1) 52→55 (live count, both pins + 3 module-doc "fifty-five"; emq.4.1 +reassign/+lane_drain, emq.4.2 +reap_group); (2) echomq:2.0.0→2.4.2, emq.4.3 climbs to 2.4.3 (NOT 3.0.0 — MAJOR at emq.8).
LINK DEFECT CAUGHT (the reorg-link-rebase lesson): the seed predates the emq2/ nesting → its ../../../emq.design.md (3-level) was stale; corrected to ../../../../emq.design.md (4-level, matching the proven 4.2 precedent) + display-text aligned to target. Reproducible [ -f ] resolution sweep over all 3 files = all targets RESOLVE.
NO-INVENT posture: did NOT assert a free-standing "BLMOVE since X.Y" engine-version number (would violate cite-valkey.io); grounded the floor-raise in §12.5's own LMOVE-6.2 citation + routed the precise version assertion to the build's truth-row first-run.

### T-5 — Strategic check on MECH-(iii) per-lane wake (Operator conditional: "opt in IF strategically beneficial in roadmap and emq4.roadmap.md; any caveats → AskUserQuestion")

VERIFIED Venus's two load-bearing claims first: §12.2 (emq.design.md:457-463) — client-side pops bypass the atomic @gclaim, REJECTED by design → every mechanism is block-on-signal-then-@gclaim (Venus's framing holds). Frozen-wire (echo_wire.ex:12-14) — Connector/RESP/Script frozen by committed records, but command/3 carries arbitrary blocking cmds (park's BLPOP rides it) → NO echo_wire edit for any mechanism → dual-architect escalation NOT triggered (single Venus right-sized).

Strategic check of MECH-(iii):
- emq.roadmap.md emq.4 row (:121): generic 4 axes; lane fairness → emq.4.4 (weighted/deficit rotation). No per-lane-wake specificity.
- emq4.roadmap.md: it is the "EchoMQ → Oban Pro parity" roadmap (Phases 1-6: durable history/recording · transactional enqueue · workflows/batches/chains · smart-engine parity · Pro extension · polyglot). Lane/fairness strategy = Phase 4 (rotating ring + Lanes.limit/4 + cluster-wide atomic counters). PER-LANE WAKE / metronome / lost-wakeup: ABSENT entirely.
- Family carve emq.4.md: INV7 splits fairness — 4.3 = wake soundness (no lost wakeup, fair across parked CONSUMERS); 4.4 = lane fairness ("which lane served, in what share"). INV8 = no pre-emption of a later family rung. EMQ.4-INV1 = "no new lane key family; §6 grammar unedited." Fork B Arm 3 (4.4) IS "a per-lane budget refreshed by the metronome" — per-lane machinery contemplated AT 4.4.

FINDING: per-lane wake is NOT strategically mandated by either roadmap, and carries 3 caveats: (1) not roadmap-justified; (2) pre-empts emq.4.4 (INV8); (3) violates EMQ.4-INV1 (needs a §6 wake:<group> member). Condition NOT met → per the Operator's "any caveats → AskUserQuestion", surfacing.

RECOMMENDATION: MECH-(i) BLMOVE block-move on the shipped wake → @gclaim (smallest; closes the lost-wakeup window by construction; @gclaim + all 7 wake scripts byte-frozen; no §6 edit; honors INV1/INV7/INV8), with per-lane fairness DEFERRED to emq.4.4 (its Fork B Arm 3) where the carve already puts it. Topology for MECH-(i): standard Flat-L2 (build bounded) + Apollo MANDATORY + ≥100 loop; stories = Venus's .stories.md. NOTE two-roadmaps situation (emq4.roadmap.md = Oban-Pro-parity framing, parallel to emq.roadmap.md's rung-ladder) flagged for the Operator.

### T-6

Director note — FORK A-MECH consultation brief authored (aaw.architect-approach technique → KB)

Per Operator directive, applied the architect's-approach technique to FORK A-MECH (the emq.4.3 blocking-primitive mechanism) and wrote the consultation brief to docs/echo_mq/kb/emq-4-3-metronome-design.md, following the emq-durability-design.md pattern (the four-part lens Rationale/5W/Steelman/Steward as interrogation; the as-built floor verified at source; the design-team rec = MECH-(i) stated-to-be-tested; a per-fork "hard question"; cross-cutting; references).

Director contributions beyond the three-option list:
(1) The §12.2 COLLAPSE flag — §12.2 forecloses a block-and-claim, so every arm is block-on-signal-then-@gclaim; MECH-(i) (BLMOVE on the wake token) may therefore be Arm A relabelled. The well-posed decision is: accept-(i) as the canon-legal best / take the genuinely-distinct (ii) / re-open §12.2 (a larger design-canon rung).
(2) MECH-(iv) SURFACED — block on the RING itself (BLMOVE ring→sink LEFT RIGHT, then @gclaim that lane): blocks on the true work-readiness structure, not a proxy token — the most genuine "new primitive", eliminates the thundering herd; pending the @gclaim-rotation-interaction question (does it force an @gclaim edit?).
(3) MECH-(iii) strategic check carried forward (prior trace): NOT roadmap-justified (emq4.roadmap.md = Oban-Pro-parity, per-lane wake absent; lane fairness = ring+limits+counters, Phase 4), pre-empts emq.4.4 (INV8), needs a §6 grammar edit (EMQ.4-INV1).

Next per the technique: the answer wave — an independent divergent-lens architect answers the brief → Director synthesis → Operator rules. FORK A-MECH NOT yet ruled; no build spawned.

### T-7 — emq.4.3 SHIP run opens (Flat-L2, HIGH-risk; MECH-(ii) the metronome-as-system)

No open Operator fork remains: FORK A = Arm B (D-1) · FORK A-MECH = MECH-(ii) (D-2) · FORK A-MECH-§6 closed by construction (the fan-out is BEAM messages, not a keyspace registry → no §6 grammar edit). Pre-build records straight: D-2 on the ledger, emq.roadmap.md reconciled, the decision doc docs/echo_mq/kb/metronome-design/metronome-fork-decision.md written + link-clean.

STAGE PLAN (Director holds the gate between each): Venus re-derive the triad emq.4.3.{md,prompt.md,stories.md} from the provisional MECH-(i) to MECH-(ii) → Director gate → Mars-1 build the core primitive → Mars-1 the multi-consumer proof harness (divided across the two passes per the no-overload directive) → Director DEEPENED verify (≥100 loop + multi-consumer fairness + §12.2 no-bypass + FROZEN-WIRE + byte-frozen @gclaim + a net-zero mutation spot-check) → Mars-2 harden + full ladder + ≥100 loop owning the machine → Apollo MANDATORY (independent ladder + ≥100 loop re-run + BUILD-GRADE) → Venus post-build reconcile → Director ship (one LAW-4 pathspec commit, on Operator confirmation per the standing commit constraint).

BUILD TARGET (forward-tense; Mars re-probes the floor at Stage 0): a NEW EchoMQ.Metronome — a supervised process per queue owning the single BLPOP emq:{q}:wake block + an idle-consumer registry; on a wake it pokes the k registered-idle consumers over BEAM messages, each running the byte-frozen @gclaim ONCE (one claim per idle consumer per wake = consumer-fair; re-poke promptly when work remains) + EchoMQ.Consumer rewired (loses park/1, gains a registration: register-idle → :claim_once → @gclaim once → re-register) + a host-wiring helper (library law: owner-started, no mod: auto-start) + the conformance scenario(s) (additive minor, prior 55 byte-unchanged, re-pin 55→N) + the version climb 2.4.2→2.4.3 (mix.exs label + @wire_version constant in lockstep). NO @gclaim edit, NO §6 edit, NO echo_wire logic edit.

GATE LADDER (per-app, NEVER umbrella-wide): asdf current erlang re-probe (expect Erlang 28.5.0.1 / Elixir 1.18.4) · valkey-cli -p 6390 ping → PONG · TMPDIR=/tmp mix compile --warnings-as-errors · TMPDIR=/tmp mix test --include valkey · Conformance.run/2 → {:ok,N} additive-minor (both pins re-pinned) · the ≥100 determinism loop owning the machine (the lost-wakeup race + same-ms branded-JOB mint are cross-run). Boundary = echo/apps/echo_mq + the one @wire_version constant in echo/apps/echo_wire/lib/echo_mq/connector.ex. apps/echomq + codemojex + echo_bot UNTOUCHED.

### T-8 — Venus re-derive opens (MECH-(i) → MECH-(ii)), charter+craft loaded, D-1/D-2 confirmed

Charter (.claude/agents/venus.md) + echo-mq-architect skill + shared refs (echo-mq-program.md, echo-mq-surface.md) loaded. Self-registered (parent ccl-emq-4-3-1). Decision records read: metronome-fork-decision.md (the ruling MECH-(ii)) + metronome-fork-venus-2.md (the BEAM-process-axis basis).

LEDGER CONFIRMS the build target is clean of open forks: D-1 (Fork A = Arm B), D-2 (FORK A-MECH = MECH-(ii) metronome-as-system), FORK A-MECH-§6 CLOSED by construction (BEAM-message fan-out, NOT a keyspace registry → no §6 grammar edit). T-7 stage plan + T-1..T-6 reconcile carried.

TWO SEED DELTAS already pinned in T-1..T-4 (confirm at re-probe): (1) conformance count 55 (NOT seed's 52) — re-pin 55→N; (2) @wire_version "echomq:2.4.2" (NOT 2.0.0) — emq.4.3 climbs 2.4.2→2.4.3 (additive minor, NOT a floor-raise: BLPOP shipped; NOT 3.0.0: MAJOR at emq.8).

TASK: re-derive the triad (emq.4.3.{md,prompt.md,stories.md}) from the PROVISIONAL MECH-(i) (a BLMOVE-on-wake park swap) to the RULED MECH-(ii) (the metronome-as-system), lag-1 reconciled against the as-built tree. NEXT: read the current triad + re-probe consumer.ex/lanes.ex/mix.exs/connector.ex/conformance + the two pin tests.

### T-9 — emq.4.3 lag-1 reconcile COMPLETE (MECH-(ii) re-derivation); 3 corrections carried

RECONCILE (all MATCH except 3 carried deltas):
- Toolchain: Elixir 1.18.4 / Erlang 28.5.0.1 (echo/.tool-versions; asdf current confirms /echo/.tool-versions source). MATCH.
- consumer.ex: spawn_link :39-40 (NOT GenServer) + trap_exit :41; dedicated lane :43-51 (self-starts Connector if no :conn); loop check_control→reap→promote→drain→park :91-98; park/1 = Connector.command(conn,["BLPOP",wake,secs],beat_ms+2000) :144-149; drain exhaustive :114-142; stop/2 monitor+:emq_stop DOWN-bounded :78-89; beat_ms 1000 :58. ALL MATCH.
- lanes.ex @gclaim :37-61 BYTE-FROZEN target: LMOVE ring ring :38, ZPOPMIN :41, server-clock TIME :50-51, gactive HINCRBY :53. MATCH.
- mix.exs: version "2.4.2" :7; library law extra_applications:[:logger] ONLY, NO mod: :21. MATCH (library, host-started).
- FROZEN-WIRE: Connector.command/3 carries blocking BLPOP (consumer.ex:147 proof); @wire_version "echomq:2.4.2" connector.ex:35. records freeze Connector/RESP/Script.
- consumer_test.exs: @moduletag :valkey, Connector.start_link(port:6390), per-test queue + on_exit purge, wait_until/2 (400 tries), Snowflake.start(4). MATCH (the harness model the proof extends).

THREE DELTAS CARRIED:
(1) COUNT 55 (seed said 52): conformance_run_test.exs:48 `== {:ok, 55}`; conformance_scenarios_test.exs:28-86 @run_order = exactly 55 names (last 9 = flow family); :87 asserts Keyword.keys(scenarios())==@run_order; prose "fifty-five". emq.4.1 +reassign/+lane_drain (52→54), emq.4.2 +reap_group (54→55). RE-PIN 55→N in BOTH pins.
(2) FENCE echomq:2.4.2 (seed said 2.0.0): emq.4.3 climbs 2.4.2→2.4.3 (mix.exs:7 label + connector.ex:35 @wire_version constant in lockstep, D-3). ADDITIVE MINOR (count climbs), NOT a floor-raise (BLPOP shipped), NOT 3.0.0 (MAJOR at emq.8).
(3) WAKE-PUSH COUNT = 10 distinct scripts, NOT "seven" (the decision-doc/prior-trace figure is stale arithmetic). lanes.ex ×5 (@genqueue:30, @gresume:77, @glimit:94, @greassign:132, @greap_group:375), jobs.ex ×4 (@complete:200, @retry:277, @promote:336, @reap:366), stalled.ex ×1 (@sweep_stalled:83). One LPUSH emq:{q}:wake '1' + LTRIM 0 63 each, the SINGLE per-queue wake LIST capped 64. Under MECH-(ii): ALL 10 BYTE-FROZEN — the metronome blocks on the SAME shipped emq:{q}:wake token they push to. INV4 states "the wake-pushing scripts (10) byte-unchanged" — exact, not "seven".

MECH-(ii) BUILD SHAPE confirmed clean of forks: NO @gclaim edit (byte-frozen), NO §6 grammar edit (keyspace.ex:14 queue_key/2 builds emq:{q}:<type> directly; BEAM-message fan-out, no keyspace registry), NO echo_wire LOGIC edit (block rides command/3; only @wire_version CONSTANT moves). VERDICT: BUILD-GRADE achievable — re-derive the triad to MECH-(ii) now.

### T-10 — emq.4.3 triad RE-DERIVED to MECH-(ii) (the metronome-as-system); BUILD-GRADE; link sweep 0 broken

THREE FILES re-derived from the provisional MECH-(i) (BLMOVE-on-wake park swap, mechanism WITHHELD) to the RULED MECH-(ii) (metronome-as-system, D-1+D-2):
- emq.4.3.md (body, authoritative): status banner → MECH-(ii) RULED (drop "withheld/authored to MECH-(i)"); Goal/Rationale(5W)/What/Where/Scope re-scoped to the NEW EchoMQ.Metronome (supervised process per queue, single BLPOP emq:{q}:wake block + idle-consumer registry + BEAM-message fan-out, one @gclaim per idle consumer per wake, re-poke when work remains, pure decision core) + Consumer rewire (drops park/1, gains registration) + host-wiring helper (library law, no mod:). FORK A-MECH section REPLACED with "all Operator forks RULED" + the THREE BUILD-TIME SEAMS (SEAM-1 reap/promote migration, SEAM-2 host-wiring API shape, SEAM-3 registration message protocol — Mars pins at Stage-0, NOT Operator forks). 5 invariants re-cut: INV1 (sound/no-lost-wakeup/fair-across-REGISTERED/bounded-starvation), INV2 (server-clock/@gclaim-frozen/poke-triggers-claim/metronome-owns-no-lease), INV3 (wire-law/NO-FLOOR-RAISE/FROZEN-WIRE/§6-UNEDITED), INV4 (shipped-surface byte-unchanged: @gclaim + 10 wake-push scripts + keyspace.ex; metronome NEW/additive), INV5 (additive-minor 55→N). DoD re-cut.
- emq.4.3.stories.md: 4 metronome stories each defeating a no-op — US1 admit-while-registered → served WELL BEFORE the beat (defeats poll-on-beat); US2 lost-wakeup race at the REGISTRATION boundary (the metronome holds the block continuously, not a transitioning consumer — window closed by construction) + crash-mid-claim (monitor-removed registration, reaped job); US3 multi-consumer fairness (N registered, one @gclaim per idle consumer per wake, none starves — defeats poke-one-to-exhaustive-drain) THE LOAD-BEARING PROOF; US4 registration/deregistration + drain-on-stop (monitor-detect dead consumer, clean stop/:shutdown, no orphaned registration/leaked claim). + US5 FROZEN-WIRE/§12.2/NO-floor-raise + US6 conformance additive + US-GATE. Coverage map re-cut.
- emq.4.3.prompt.md: build-choice gate REMOVED (mechanism ruled); touch-set = NEW metronome.ex + consumer.ex rewire + host-wiring helper + conformance.ex + mix.exs + @wire_version constant + tests; NO lanes.ex/jobs.ex/stalled.ex/keyspace.ex (byte-frozen under (ii)). Mars DIVIDED across 2 passes (PASS 1 core primitive, PASS 2 multi-consumer proof — the no-overload directive). Director deepened probes (≥100 loop + fairness + §12.2 no-bypass + FROZEN-WIRE + NO-floor-raise + byte-frozen @gclaim + net-zero mutation). Apollo MANDATORY. Commit pathspec adjusted to the (ii) touch-set (metronome.ex NEW, lanes/jobs/stalled/keyspace EXCLUDED).

KEY CORRECTION carried (NO-INVENT): MECH-(ii) raises NO computed-floor (uses the SHIPPED BLPOP — the MECH-(i)/(iv-b) BLMOVE floor-raise does NOT apply); the version climb 2.4.2→2.4.3 is an additive minor purely because the conformance count grows. The 3 BLMOVE/BLMPOP mentions remaining are the Director/Apollo NO-floor-raise PROBES ("no BLMOVE enters") — correct.

LINK SWEEP (reproducible, the reorg-link-rebase lesson): 43 relative targets across the 3 files, 0 BROKEN. Fixed the kb decision-record link display-text (was 3-level ../../../kb, target was 4-level ../../../../kb that resolves — aligned display to the resolving target). FRAMING clean (no first-person/interior-state verbs).

VERDICT: BUILD-GRADE — the triad is ready for Mars. No Operator fork open (D-1 Arm B, D-2 MECH-(ii), FORK A-MECH-§6 CLOSED). Three build-time seams surfaced for Mars's Stage-0 (not forks). Stage-5 reconcile note: emq.4.md family still carries stale 52 + 2.0.0 (re-pin if the Director includes emq.4.md in the commit).

### T-11 — Stage-1 gate PASS: the triad re-derived to MECH-(ii), ready for Mars

Director read both load-bearing artifacts (emq.4.3.md + emq.4.3.prompt.md) — the files, not the peer report. VERIFIED: status → MECH-(ii) the metronome-as-system; Goal/What/Where/Scope/INV1-5/DoD all re-scoped to the new supervised EchoMQ.Metronome + consumer rewire + host-wiring helper; NO floor-raise stated correctly (BLPOP is shipped — the decisive difference from the chosen-against (iv-b)); §6 UNEDITED (BEAM-message fan-out, not a keyspace registry); @gclaim byte-frozen (the poke triggers it, never edits it); the byte-freeze set named exactly = @gclaim + 10 wake-push scripts (5 lanes.ex + 4 jobs.ex + 1 stalled.ex — Venus corrected the stale "seven" arithmetic) + keyspace.ex; the forks section = all Operator forks RULED (FORK A=Arm B/D-1; FORK A-MECH=MECH-(ii)/D-2; FORK A-MECH-§6 CLOSED by construction) + 3 build-time seams (SEAM-1 reap/promote migration, SEAM-2 host-wiring API, SEAM-3 registration protocol) correctly classified NOT Operator forks (Mars pins at Stage-0); the runbook divides Mars across PASS 1 (core primitive) + PASS 2 (multi-consumer proof) per the no-overload directive; the commit pathspec adjusted to the (ii) touch-set (NEW metronome.ex + consumer.ex rewire + host-wiring + conformance + mix.exs + @wire_version constant + tests; EXCLUDES lanes.ex/jobs.ex/stalled.ex/keyspace.ex). No open Operator fork → no AskUserQuestion needed. Venus's reproducible link sweep: 43 targets, 0 broken. GATE: PASS — ready for Mars. One Director design ruling added before the build: D-3 (the additive/opt-in rewire).

### T-12 — Mars Stage-0 re-probe: floor MATCHES except a CARRIED in-flight-tree delta (the lag-1 law, live)

TOOLCHAIN: Elixir 1.18.4 / Erlang 28.5.0.1 (echo/.tool-versions, asdf current confirms). Valkey 6390 → PONG. MATCH.

BUILD TARGETS BYTE-CLEAN vs HEAD (git diff HEAD --stat = empty): consumer.ex, lanes.ex, keyspace.ex, stalled.ex, pump.ex, pump/core.ex. The rewire target + the byte-frozen scripts are untouched in the working tree — PASS 1 builds on a clean base.

ANCHORS RE-PROBED, all MATCH the prompt's floor:
- consumer.ex: spawn_link :39-40 (NOT GenServer) + trap_exit :41; dedicated lane self-start :43-51; loop check_control→reap→promote→drain→park :91-98; park/1 = Connector.command(conn,["BLPOP",wake,secs],beat_ms+2000) :144-149 (wake = Keyspace.queue_key(queue,"wake") :146); drain exhaustive :114-142; check_control settle-point :104-112 (:emq_stop→exit(:normal); {:EXIT,_,:shutdown}→exit(:shutdown)); stop/2 monitor+:emq_stop :78-89; child_spec/1 :18-25 (:permanent); beat_ms 1000 :58.
- lanes.ex @gclaim :37-61 BYTE-FROZEN: LMOVE ring ring LEFT RIGHT :38, ZPOPMIN lane :41, server-clock TIME :50-51, gactive HINCRBY :53. Lanes.claim/3 :171-184 — Connector.eval(@gclaim, keys=[ring,active], argv=[base,lease_ms]) → :empty | {:ok,{id,payload,att,group}}.
- Jobs.reap/2 (jobs.ex:758), Jobs.promote/3 (jobs.ex:751), Jobs.complete/4, Jobs.retry/7 — the settle/cadence surface. MATCH.
- pump.ex = GenServer (timer Process.send_after); pump/core.ex = pure decision core (tick_ms/1, batch/1). The metronome follows the CONSUMER spawn_link shape (it holds the blocking BLPOP — a GenServer blocked in handle_info cannot drain its mailbox), borrowing Pump's pure-core split.

CARRIED DELTA (in-flight uncommitted tree, NOT emq.4.3, NOT mine): the working tree has uncommitted mods bumping echo_mq+echo_wire mix.exs 2.4.2→2.5.0, @wire_version echomq:2.4.2→echomq:2.5.0 (connector.ex:35), conformance 55→57 (+:pool_enqueue +:pool_order scenarios), + jobs.ex/jobs_test.exs mods. This is the "wire valkey pipes"/ewr pool effort (commits adc5ec10/7551f118/bbe407b1/cfe67220 post-emq.4.1). HEAD is clean at 2.4.2/55. CONSEQUENCE: the spec pins emq.4.3 at 2.4.2→2.4.3 / 55→N, but the live tree is already at 2.5.0 / 57. This is a spec⇄as-built version/count contradiction the Director must reconcile at PASS 2 / commit (NOT a PASS-1 blocker — PASS 1 touches none of those files). PASS 1 builds metronome.ex (NEW) + consumer.ex (clean rewire target) + host-wiring (NEW) only; the version/count reconcile is deferred to PASS 2.

### T-13 — Mars Stage-0 SEAM pins (against the real code; none an Operator fork — build choices)

SEAM-1 (reap/promote cadence migration) PINNED: metronome-OWNED for the pool path. The metronome's beat loop runs Jobs.reap/2 + Jobs.promote/3 (pump_batch) ONCE per beat, then blocks on Connector.command(conn,["BLPOP",wake,secs],beat_ms+2000) — the IDENTICAL park verb relocated from consumer.ex:147. A :metronome consumer DROPS reap→promote from its loop (one beat per queue, the metronome owns it). A STANDALONE consumer (no :metronome) KEEPS its shipped reap→promote→drain→park loop BYTE-UNCHANGED (D-3 — codemojex unbroken). So the cadence is metronome-owned for the pool, residual-on-consumer for the standalone — both correct, the design intent (one beat per queue) honored for the pool.

SEAM-2 (host-wiring API) PINNED: a NEW module EchoMQ.Queue (use Supervisor — the EchoMQ.Pool precedent, pool.ex). EchoMQ.Queue.child_spec/1 + start_link/1 start ONE Metronome + N Consumers (each Consumer wired :metronome → the metronome's pid/name) under the host's supervision tree, :one_for_one. Host-started, no mod: auto-start (the library law — mix.exs has extra_applications:[:logger] only). The metronome is registered by name so the consumers can resolve it at start (a queue-derived via: name). Modeled on Pool (use Supervisor + child_spec) + Consumer.child_spec/1 + Pump child_spec.

SEAM-3 (registration message protocol) PINNED against consumer.ex:104-112 settle-point discipline:
- consumer→metronome: {:register_idle, consumer_pid} (the consumer is idle, awaiting a poke).
- metronome on register: Process.monitor(consumer_pid) + add {pid → ref} to the registry.
- metronome→consumer: {:claim_once, metronome_pid} (authorize ONE @gclaim via Lanes.claim/3).
- consumer on :claim_once: run Lanes.claim/3 ONCE → handle → settle (Jobs.complete/4 | Jobs.retry/7) → re-send {:register_idle, self()}. On :empty, re-register immediately (no work to do; await the next poke).
- consumer at settle point on :emq_stop/{:EXIT,_,:shutdown}: send {:deregister, self()} to the metronome BEFORE exit (clean deregister, no orphaned registration).
- metronome on {:DOWN, ref, :process, pid, _}: remove pid from the registry (monitor-detected death — no orphaned registration, the metronome survives).
- metronome on {:deregister, pid}: Process.demonitor(ref,[:flush]) + remove from the registry.

PURE CORE (the program's thin-but-robust law, modeled on pump/core.ex): EchoMQ.Metronome.Core — beat_ms/1 (validate :beat_ms, default 1000) + the dispatch decision: given the registry (list of idle pids) decide which to poke + how many :claim_once to authorize per wake = one per idle consumer per wake (D-2 dispatch contract; a poke-one-to-exhaustive-drain would hog the beat). Testable WITHOUT Valkey.

HOST-WIRING HOME: no lib/echo_mq.ex top-level module exists (probed); the helper is the NEW EchoMQ.Queue module (not an extension of consumer.ex/metronome.ex), keeping each process owning its own child_spec — orthogonal, the BCS idiom.

### T-14 — Mars PASS 1 BUILT (core primitive); compile + pure + byte-freeze GREEN; :valkey BLOCKED by a pre-existing in-flight version-fence collision (NOT mine)

FILES (PASS 1 touch-set, all inside echo/apps/echo_mq/lib + test):
- NEW lib/echo_mq/metronome/core.ex — the pure decision core: beat_ms/1 (default 1000, refuses non-positive — the Pump.Core law), dispatch/1 (one :claim_once per registered-idle consumer per wake = the D-2 contract; order-preserving, head=idle-longest), repoke?/1 (re-poke promptly iff poked>0 AND idle>0). Testable without Valkey. 3 doctests.
- NEW lib/echo_mq/metronome.ex — EchoMQ.Metronome: a supervised spawn_link-loop process per queue (the consumer.ex:39-51 shape — trap_exit, dedicated connector lane self-started or :conn), child_spec/1 (:permanent), stop/2 (monitor + :emq_stop, DOWN-bounded). Beat loop: drain_mailbox (settle-point) → Jobs.reap → Jobs.promote (SEAM-1, one beat per queue) → block on Connector.command(["BLPOP",wake,secs],beat_ms+2000) [the IDENTICAL park verb relocated, consumer.ex:147] → poke_round (one :claim_once per idle consumer via Core.dispatch, then drain + re-poke while Core.repoke?). Registry = ordered idle-pid list + monitor-ref map; register monitors once; {:DOWN} forgets (survives, no orphan); {:deregister} demonitors+drops. Owns NO Valkey lease (the BLPOP timeout is host-side — INV2). NO new Lua (grep redis.call on metronome.ex = 0).
- EDIT lib/echo_mq/consumer.ex — ADDITIVE :metronome option (D-3). start_link branches: nil metronome → loop(Map.delete(state,:metronome)) [the shipped standalone loop, the state map BYTE-IDENTICAL to HEAD's 8-key literal]; a metronome → metronome_loop (register_idle → await_poke → on :claim_once run Lanes.claim/3 ONCE + handle + settle [Jobs.complete | Jobs.retry, the identical drain/1 settle, once not exhaustively] → re-register; on :emq_stop/:shutdown deregister-then-exit at the settle point). The shipped loop/1, drain/1, park/1, check_control/0, stop/2, child_spec/1 ALL byte-identical to HEAD (closure diff: the ONLY removed lines are the 3-line old loop(%{...}) invocation). §12.2 honored — the consumer NEVER pops the lane/ring; @gclaim does, via Lanes.claim/3.
- NEW lib/echo_mq/queue.ex — EchoMQ.Queue: the host-wiring helper (SEAM-2; use Supervisor, the Pool precedent). start_link/1 + child_spec/1 start ONE Metronome (registered metronome_name(name), started FIRST) + N Consumers (each :metronome-wired), strategy :rest_for_one (a metronome restart cascades a clean consumer re-register; a consumer crash restarts only it). Host-started, no mod: (the library law).
- NEW test/metronome_core_test.exs — mirrors pump_core_test.exs: doctest EchoMQ.Metronome.Core + 4 explicit pins (beat_ms default/refusal, dispatch=one-per-idle, repoke? truth table). async:true (pure, no process-global state — race-free per the async law). The doctest invocation WIRED (the "a check counts only if it RUNS" law — the moduledoc doctests were inert until this).

GATE (PASS 1):
- TMPDIR=/tmp mix compile --warnings-as-errors → GREEN (4 files compiled, echo_mq app generated).
- Pure suite (TMPDIR=/tmp mix test, :valkey excluded) → 4 doctests, 392 tests, 0 failures, 323 excluded. The standalone consumer path does NOT regress any non-valkey test.
- metronome_core_test alone → 3 doctests, 4 tests, 0 failures.
- BYTE-FREEZE (my diff vs HEAD): lanes.ex redis.call delta = 0 (@gclaim + 5 wake-push frozen); stalled.ex redis.call delta = 0 (1 wake-push frozen); keyspace.ex no delta (no §6 edit). consumer.ex closure diff = purely additive (no shipped-loop bytes touched). PASS.
- BOUNDARY: my touch-set = ONLY consumer.ex (EDIT) + 3 NEW lib files + 1 NEW test. No apps/echomq, no codemojex/echo_bot. PASS.
- FROZEN-WIRE: echo_wire/lib/ — my PASS-1 diff touches it ZERO. (The @wire_version 2.4.2→2.5.0 change in connector.ex is PRE-EXISTING in-flight, NOT mine; PASS 1 makes no version step.)

BLOCKER (pre-existing, NOT emq.4.3, NOT mine — the brief gap for the Director): the echo_mq :valkey suite (incl. consumer_test.exs, my D-3 end-to-end proof) is BLOCKED by {:version_fence, "echomq:2.4.2"}. ROOT CAUSE: the in-flight uncommitted working tree bumped connector @wire_version 2.4.2→2.5.0 (the ewr.* wire program — version_reflection_test.exs:3 "EWR.1.4 the version-reflection rule"; +:pool_enqueue/+:pool_order scenarios; commits adc5ec10/7551f118/bbe407b1/cfe67220 post-emq.4.1), but the live {emq}:version key on 6390's default DB is STILL echomq:2.4.2 (HEAD-consistent), so connector.fence/2 refuses every connect (the `got` branch — connector.ex:486). The echo_mq :valkey tests use the default DB and rely on the key matching; the documented re-seed (DEL {emq}:version, then reconnect SET NX claims the new version) is a SHARED-STATE mutation correctly DENIED to me (beyond PASS-1 code-only scope, belongs to the in-flight version bump). RESOLUTION owed at the Director's Stage-2 (which re-runs the ladder on 6390 + can authorize the re-seed) AND a SPEC⇄AS-BUILT VERSION CONTRADICTION the Director must reconcile: the spec pins emq.4.3 at 2.4.2→2.4.3 / conformance 55→N, but the live tree is ALREADY at 2.5.0 / 57 (the in-flight effort jumped the minor + added 2 scenarios). PASS 2 (the :valkey metronome harness + the conformance scenario + the version step) cannot proceed until the version/count baseline is reconciled (is emq.4.3's version step now 2.5.0→2.5.1? does it append after :pool_order at 57→N?). I do NOT touch the in-flight files; the reconcile is the Director's.

### T-15 — Version-sync flux during the PASS-1 gate; re-seed done, Director PAUSED version edits pending the Operator's settle

The (a) re-seed was performed: DEL {emq}:version → reclaimed echomq:2.5.0; consumer_test.exs (:valkey) ran GREEN (3/0), confirming the fence unblocked AND Mars's D-3 standalone proof end-to-end. The :valkey gate worked at that instant.

BUT the version files are in ACTIVE real-time flux (Operator editing out-of-band): echo_wire/mix.exs 2.4.2→2.4.4→2.5.0 (and STAGED with connector.ex); @wire_version 2.4.2→2.5.0→REVERTED to 2.4.2. So the live key (2.5.0, from my re-seed) now re-mismatches @wire_version (2.4.2) → the gate is re-blocked. Every re-seed is invalidated by the next @wire_version edit — a moving target.

The version arc for emq.4.3 (stay in the 2.4.x line → 2.4.3 per the original spec, with the ewr.* 2.5.0 reverted/separate; vs adopt 2.5.x building on the ewr.4.1 pool work) is the OPERATOR's live decision. Director reverted its one speculative echo_mq/mix.exs edit (2.5.0 → 2.4.2, the HEAD baseline) to withdraw from the actively-edited version files, and PAUSED all version edits/re-seeds. Awaiting the Operator's settled @wire_version + mix.exs target, then: re-seed the live key to match → align emq.4.3's PASS-2 version step → resume the build on a stable baseline.

Mars PASS 1 (NEW metronome.ex + metronome/core.ex + queue.ex + the additive :metronome consumer rewire) is BUILT, compiles --warnings-as-errors clean, the pure suite (392/0) + the standalone D-3 :valkey proof green; the byte-freeze (@gclaim + 10 wake-push + keyspace.ex) + FROZEN-WIRE + boundary all clean. PASS 2 (the multi-consumer fairness harness + the metronome conformance scenario + emq.4.3's version step + the count re-pin) is ready to build on a stable version baseline.

### T-16 — Version blocker RESOLVED (Operator directive); base gate re-confirmed GREEN; emq.4.3 decoupled; Mars PASS 2 next

Actions (Director, per the Operator's "DROP THE TEST / INDEPENDENT versions / finish the rung" directive): (1) dropped echo_mq/test/version_reflection_test.exs; (2) re-seeded {emq}:version → echomq:2.4.2 via SET (== @wire_version, connector.ex:35); (3) bumped echo_mq/mix.exs 2.4.2→2.4.3 (the rung label; Operator's "# rung number in echo_mq roadmap (INDEPENDENT from echo_wire version)" comment carried). The Operator independently set echo_wire/mix.exs → 2.5.0 (its own roadmap) and reverted @wire_version back to echomq:2.4.2.

BASE GATE GREEN on the settled tree (asdf: Erlang 28.5.0.1 / Elixir 1.18.4; Valkey 6390 PONG):
- TMPDIR=/tmp mix compile --warnings-as-errors → clean (the whole tree: in-flight ewr.4.1 + my PASS 1 + the version edits compile together).
- metronome_core_test.exs → 3 doctests, 4 tests, 0 failures (pure).
- consumer_test.exs --include valkey → 3 tests, 0 failures — the connector CONNECTED (boot fence unblocked: GET {emq}:version == @wire_version == echomq:2.4.2) AND the D-3 standalone consumer path works end-to-end.

RULED: D-4 (version reframe — un-fuse the lockstep; test dropped; mix.exs=label; @wire_version frozen at 2.4.2; echo_wire untouched; connector.ex dropped from the pathspec) + D-5 (no conformance scenario — conformance.ex + both pins byte-unchanged; INV5 reinterpreted; the metronome proof is the :valkey process suite + ≥100 loop; emq.4.3 fully decoupled from ewr.4.1 — zero shared files).

NEXT: Mars-1 PASS 2 — build the :valkey metronome PROCESS proof as a NEW test/metronome_test.exs: US1 serve-WELL-BEFORE-the-beat (defeats poll-on-beat); US2 lost-wakeup at the registration boundary (the metronome holds the block continuously) + crash-mid-claim (monitor-removed registration, reaped job); US3 multi-consumer fairness (N registered, one @gclaim per idle consumer per wake, none starves — THE LOAD-BEARING PROOF, defeats poke-one-to-exhaustive-drain); US4 registration/drain (consumer death → registration removed + metronome survives; stop/:shutdown → deregistered, no leaked claim). Model on consumer_test.exs (@moduletag :valkey, Connector port 6390, per-test queue + on_exit purge, wait_until/2, Snowflake.start(4)). DO NOT touch conformance.ex / connector.ex / mix.exs (settled/frozen/done). Then Director DEEPENED verify (≥100 loop owning the machine + fairness + §12.2 no-bypass + FROZEN-WIRE + byte-frozen @gclaim + net-zero mutation).

### T-17 — Mars PASS 2 Stage-0 re-probe (the :valkey metronome PROCESS proof)

TOOLCHAIN: Erlang 28.5.0.1 / Elixir 1.18.4 (echo/.tool-versions, asdf current confirms). Valkey 6390 → PONG. {emq}:version = echomq:2.4.2 (== @wire_version connector.ex:35 — boot fence PASSES; do NOT DEL/re-seed per D-4/D-5).

AS-BUILT API RE-PROBED (line-cited, all MATCH the brief's floor):
- EchoMQ.Metronome (metronome.ex): start_link/1 :53 (opts :queue, :conn|:connector, :beat_ms, :pump_batch, :name) — spawn_link loop, registers self() as :name if given; child_spec/1 :35 (:permanent, shutdown 5_000); stop/2 :95 (monitor + {:emq_stop,from,ref}, DOWN-bounded). Loop :113 = drain_mailbox → Jobs.reap → Jobs.promote(pump_batch) → block(BLPOP wake :127-132, the IDENTICAL park verb relocated from consumer.ex:147) → poke_round :142 (Core.dispatch → send {:claim_once, self()} per idle, drain, repoke? loop). Registry = ordered idle list (head=idle-longest) + monitor map; register :189 monitors once; {:DOWN} forget :213 (survives, no orphan); {:deregister} demonitor+drop :201.
- EchoMQ.Metronome.Core (metronome/core.ex): beat_ms/1 :26 (default 1000, refuses non-positive), dispatch/1 :53 (== registered list, one :claim_once per idle per wake = D-2), repoke?/1 :76 (poked>0 AND idle>0).
- EchoMQ.Queue (queue.ex): start_link/1 :37 (opts :queue,:handler,:size(4),:name,:connector,:beat_ms,:pump_batch,:lease_ms,:retry_delay_ms,:max_attempts); child_spec/1 :48 (:supervisor,:permanent); metronome_name/1 :61 = :"#{name}_metronome"; init :64 starts metronome FIRST (registered) then N consumers (:metronome-wired), strategy :rest_for_one.
- EchoMQ.Consumer :metronome branch (consumer.ex): start_link :52 branches nil→loop (standalone, byte-frozen) / metronome→metronome_loop :185 (register_idle :190 → await_poke :197 → on {:claim_once} run claim_once :225 [Lanes.claim/3 ONCE → handle → Jobs.complete/4 | Jobs.retry/7] → re-register; on :emq_stop/:shutdown deregister :251 THEN exit at the settle point). §12.2 honored — consumer never pops lane/ring; Lanes.claim → @gclaim does (lanes.ex:178 Connector.eval @gclaim).
- Lanes.enqueue/5 (lanes.ex:143) → {:ok,:enqueued} (pushes emq:{q}:wake :152). Lanes.claim/3 (lanes.ex:171) → :empty | {:ok,{id,payload,att,group}}. Jobs.reap/2 (jobs.ex:758) returns expired leases→pending (the crash-mid-claim redelivery path: a killed consumer's lease expires, the metronome's per-beat Jobs.reap redelivers). Jobs.promote/3 (jobs.ex:751).

WORKING TREE: my PASS-2 file test/metronome_test.exs does NOT exist (??-untracked: metronome.ex/metronome/core.ex/queue.ex/metronome_core_test.exs = my PASS 1; conformance.ex/jobs.ex/jobs_test.exs/conformance_*_test.exs/mix.exs = in-flight ewr.4.1 + rung-label, LEAVE ALONE per D-5 decoupling). consumer.ex EDIT = my PASS 1 (110 insertions, additive :metronome). PASS 2 adds ONE new file only.

NEXT: write test/metronome_test.exs — US1 serve-well-before-the-beat (latency bound), US2 lost-wakeup at registration boundary + crash-mid-claim reap, US3 multi-consumer fairness (THE LOAD-BEARING PROOF), US4 registration/drain contract. Then the gate ladder + flake-shake.

### T-18 — Mars PASS 2 BUILT (the :valkey metronome PROCESS proof) + a LOST-WAKEUP DEFECT surfaced in PASS-1 metronome.ex (F-1, verified fix, NOT applied)

DELIVERABLE: NEW test/metronome_test.exs (554 lines, 27 assert/refute, @moduletag :valkey). Modeled on consumer_test.exs (Connector port 6390, per-test unique queue + on_exit purge over KEYS emq:{q}:*, wait_until/2 5ms poll, Snowflake.start(4), branded JOB/PRT ids). Four stories, each no-op-defeating: US1 serve-WELL-BEFORE-the-beat (line 80; beat_ms 2000, asserts elapsed < 600ms after a warm-up establishes the registered-idle precondition), US2 lost-wakeup boundary stream (line 148) + crash-mid-claim reap-redelivery (line 199; lone crasher kills self holding a 200ms lease, healthy consumer serves the reaped job, active ZCARD==0), US3 multi-consumer fairness (line 285; N=4, M=40, asserts all 4 serve >=1 + max <= 3x fair-share, defeats poke-one-to-exhaustive-drain), US4 registration/drain (line 360 kill-survives, line 423 stop-clean-no-leaked-claim active ZCARD==0). Harness craft: unlinked_consumer/1 (sever the test↔consumer spawn_link so Process.exit(_,:kill) does not abort the test; the production Process.monitor path is untouched — a monitor is not a link), stop_pool/1 + stop_all/1 deterministic teardown (orphaned-blocker contention turns a prompt serve into a tight-budget timeout).

GATE (per-app, echo/apps/echo_mq, TMPDIR=/tmp): asdf Erlang 28.5.0.1 / Elixir 1.18.4 MATCH; valkey-cli -p 6390 PONG; {emq}:version == echomq:2.4.2 == @wire_version (boot fence passes, NOT re-seeded per D-4/D-5). compile --warnings-as-errors exit=0 (echo_mq + my test clean; the echo_data champ_view.ex EchoStore.Graft.* warnings are in-flight ??-untracked, not mine, not fatal). PASS-1 regression: metronome_core_test + consumer_test --include valkey = 3 doctests + 7 tests, 0 failures (unchanged). MUTATION KILL-RATE 2/2 (net-zero inverse-Edit reverts, no Lua/EVALSHA so no SCRIPT FLUSH needed): (a) Core.dispatch → Enum.take(_,1) [poke-one drain] → US3 fairness CAUGHT ("only 1 of 4 served"); (b) block → Process.sleep(beat) ignoring wake [poll-on-beat] → US1 latency CAUGHT (only US1 red). INV greps: §12.2 no-bypass — test has 0 ZPOPMIN/LMOVE/ZMPOP/LMPOP commands (the one LMOVE is a comment); claims ride Lanes.enqueue/5 + the pool path Lanes.claim/3→@gclaim. Byte-freeze — redis.call=0 in metronome.ex/queue.ex/core.ex; lanes.ex/stalled.ex/keyspace.ex 0 diff lines vs HEAD (@gclaim + 10 wake-push + §6 grammar frozen). FROZEN-WIRE — echo_wire/lib unchanged by me. Boundary — my ONLY change is NEW test/metronome_test.exs; metronome.ex net-zero (the F-1 probe fix reverted, poke_round dispatch-first).

F-1 DEFECT (PASS-1 metronome.ex lost-wakeup; CRITICAL; NOT fixed — charter STOP-and-report). The :valkey proof surfaced a genuine lost-wakeup bug: the metronome consumes a wake token in block/1's BLPOP, then poke_round runs Core.dispatch(s.idle) against a STALE s.idle snapshot — if a consumer's {:register_idle} is still in the mailbox (in flight after an empty/served claim), s.idle==[] so NO ONE is poked; the wake's effect is dropped and the job waits a FULL beat. REPRODUCED isolated: US1 timed admit served at ~2060ms (== beat_ms 2000) every run, wake LIST depth 0 right after admit (BLPOP consumed it). This is exactly the US2 "lost-wakeup closed by construction" property — currently VIOLATED. US2 PASSES only because beat_ms 100 + a 5000ms within-beat budget MASKS it; US1 (beat 2000 + <600ms bound) is what BITES it. VERIFIED FIX (one line, probe-tested then reverted net-zero): in poke_round/1 add `s = drain_mailbox(s)` BEFORE Core.dispatch (drain the registry the consumed wake should act on) → US1 served in 2ms, seed 17 6/6 (was 0/8). This belongs to Mars-2 / the Director (PASS-1 lib, out of PASS-2 scope). CONSEQUENCE: current full suite = 6 tests, 1 failure (US1 RED on F-1); the other 5 are solid in isolation across seeds (US2a 4/4, US2b 3/3, US3 4/4, US4-kill 3/3, US4-stop 3/3). I did NOT weaken US1 to green-against-the-bug (that would be a false-green — the exact failure mode the program guards). NEXT: Director rules whether Mars-2 applies the drain-before-dispatch fix to metronome.ex; then the suite goes 6/0 and the ≥100 loop (the Director/Mars-2 Stage-3 job) can own the machine.

### T-19 — Remediation+harden pipeline stood up (Operator-ordered, /echo-mq-ship emq.4.3)

State at kickoff: the F-1 lost-wakeup fix is ALREADY in the tree — metronome.ex poke_round/1:147 now drains the mailbox (`s = drain_mailbox(s)`) BEFORE Core.dispatch(s.idle):148, folding in any {:register_idle} that arrived during block/1's BLPOP (applied by the killed MarsPolish pass, mtime 01:00; file still correctly untracked). conformance.ex/jobs.ex/lanes.ex clean → D-5 (no conformance delta) holds. mix.exs=2.4.3 (rung LABEL, D-4); @wire_version stays echomq:2.4.2 (zero wire delta this rung).

Toolchain change: `.claude/agents/mars.md` modified — the mars agent-type now carries the Skill tool, so a `mars`-type spawn is protocol-compliant (it can Skill(echo-mq-implementor) directly); resolves last turn's general-purpose-only deviation.

Custom pipeline order (Operator): (1) Apollo PRE-calibrate agents+skills grounded in the F-1 finding + the Skill-tool add (PROPOSE→Director-ratified; edits live under .claude/, a SEPARATE concern from the code commit) → (2) Mars VERIFY the applied fix + harden (full per-app gate + ≥100 determinism loop — HIGH-risk process/lease suite) → (3) Director solo review + consolidate → decide Mars-2 fan-out → (4) Venus reconcile triad to as-built (soften 'drops park/1' per D-3 additive/opt-in) → (5) Apollo mentorship: findings + learnings. LAW-4 ship commit is the tail, Operator-confirmed; pathspec = the emq.4.3 code surface + the emq.4.3 triad + the emq-4-3 ledger ONLY (the entangled docs-reconcile tree stays a separate commit).

Mode: Flat-L2. Risk: HIGH (new supervised process + the metronome's single BLPOP block-then-act loop).

## {emq-4-3-decisions} Decisions

### D-1 — Fork A ruled: Arm B (found a new blocking-claim primitive)

Operator ruled Fork A = Arm B (NOT the recommended Arm A). emq.4.3 re-scopes from "deepen the shipped BLPOP-wake loop" to "found a NEW blocking-claim primitive" that subsumes the wake-token two-step.

Opens a mechanism sub-fork: (i) server-side blocking grouped-claim — block directly on the ring/lanes (BLMOVE/BLMPOP). NOTE Connector.command/3 ALREADY carries blocking commands (park uses BLPOP today with a custom timeout), so (i) may ride the EXISTING connector with NO frozen-wire edit. vs (ii) a dedicated metronome process distinct from EchoMQ.Consumer that owns the beat/notify. The frozen-wire-touch question is the pivotal reconcile delta (the wire broke once → a touch is the risk surface).

NEXT: Venus authors the triad to Arm B + frames the mechanism fork as four-part Arms; Director rules it via AskUserQuestion BEFORE Mars. Dual-architect escalation GATED on the reconcile putting a frozen-wire touch genuinely in play. HIGH-risk stands either way (Apollo MANDATORY, ≥100 loop, deepened verify).

### D-2 — FORK A-MECH ruled: MECH-(ii), the metronome-as-system (the opt-in solution; synthesis of venus-1 ⊕ venus-2)

Operator ruled FORK A-MECH = **MECH-(ii)** — a dedicated, supervised **metronome system** per queue — after the two-architect consultation (the brief `docs/echo_mq/kb/emq-4-3-metronome-design.md` → venus-1's spec-steward answer → venus-2's consumer-steward answer, consolidated in `docs/echo_mq/kb/metronome-design/`). Decision record: `docs/echo_mq/kb/metronome-design/metronome-fork-decision.md`.

THE MECHANISM (forward-tense — built at the ship). A NEW supervised process per queue owns the beat and the SINGLE Valkey block — `BLPOP emq:{q}:wake <beat>`, the SHIPPED verb on the SHIPPED token (no new blocking command, **NO floor-raise**) — plus a registry of idle consumers. On a wake it pokes the *k* registered-idle consumers over BEAM messages; **each runs the byte-frozen `@gclaim` exactly once** (the dispatch contract — one claim per idle consumer per wake — is what makes it consumer-fair; a poke-one-to-exhaustive-drain would hog the beat). Consumers lose their private `BLPOP` park and gain a registration (register idle → receive `:claim_once` → run `@gclaim` once → re-register). `@gclaim` is BYTE-FROZEN; §12.2 is not approached (the block stays a block-on-signal, the claim stays the atomic script); the §6 grammar is NOT edited (the fan-out is BEAM messages, not a keyspace registry); NO `echo_wire` edit (the block rides `Connector.command/3`). The only wire move is the version climb 2.4.2 → 2.4.3 (an additive minor — the conformance count climbs; NOT a floor-raise, `BLPOP` is already in the inventory).

WHY (ii) OVER (iv-b) — the (ii)-vs-(iv-b) resolution. Both architects reject MECH-(iii) and agree the proof (a multi-consumer harness + the ≥100 loop) is the load-bearing work. They DIVERGE on the axis: venus-1 (wire axis) founds the new primitive by relocating SELECTION (§12.2 binds the claim, not the selection) → MECH-(iv-b), block on the ring / rotate-in-place, which RE-GRADES `@gclaim`. venus-2 (BEAM-process axis) founds it as a SYSTEM (the stack's own law — a system is an OTP process that owns its data) → MECH-(ii), which buys (iv-b)'s entire multi-consumer benefit (herd-free, consumer-fair) WITHOUT re-grading the safety-critical claim and REVERSIBLY (delete a supervisor child vs. re-found a frozen script). On a HIGH-risk frozen-surface rung, BEAM-reversibility outranks wire-elegance; (ii) also refuses the 4.3↔4.4 coupling (iv-b) forces (it leaves the rotation-fairness seam inside `@gclaim` for 4.4's Fork B). The found-new charter (D-1) is honored on the PROCESS axis, not the wire axis.

THE PIVOT, RESOLVED. venus-2's gating fact — "will the pooled consumer run as a pool of parked consumers on a shared queue?" — resolves YES: echo_bot is the roadmap's headline-planned consumer (`emq.roadmap.md:31-32`, "Telegram notifications at scale"), and a notification fan-out is a pool (many sends, few queues). The pool makes the herd real and selects a multi-consumer founding; (ii) is the reversible one. codemojex (one-lane-per-player today) is the degenerate one-consumer case and is not harmed.

RISK / GATE. HIGH — but the risk concentrates on the BEAM process/lease surface + the consumer-fairness proof (NOT a wire floor-raise). Apollo MANDATORY; the ≥100 determinism loop owns the proof; the multi-consumer fairness harness (which the 55-scenario suite lacks) is the load-bearing new test surface.

NEXT (the ship). Venus re-derives the triad (`emq.4.3.{md,prompt.md,stories.md}`) from the provisional MECH-(i) to MECH-(ii) at the ship's Stage-0/1 (the body was authored to MECH-(i) pending this ruling — the prompt's Stage-0/1 directive). Then Mars builds the metronome system + the consumer rewire + the multi-consumer proof; Director deepened-verify (≥100 loop + fairness + §12.2 no-bypass + FROZEN-WIRE + byte-frozen `@gclaim`); Mars-2 harden; Apollo MANDATORY; Director ship (one LAW-4 pathspec commit). Topology: Flat-L2, with Mars's build divided across its two passes (the core primitive, then the proof harness) to avoid single-agent overload (the Operator's divide-and-conquer directive) — both architects confirm (ii) is a **bounded** touch-set, NOT a divide-and-conquer fan-out (only MECH-(iii) would have forced the wide formation).

### D-3 — The consumer rewire is ADDITIVE/opt-in (the program's opt-in + reversibility law); codemojex is NOT broken

Verifying the triad surfaced that "drops park/1" / "loses the private park" is stated too strongly. RULED: the metronome is OPT-IN. EchoMQ.Consumer GAINS a `:metronome` registration path — given a metronome it registers idle and is poked (no self-park); WITHOUT a metronome it RETAINS the shipped self-park (`park/1`, the BLPOP wake loop), so existing STANDALONE consumers (codemojex — one-lane-per-player, LIVE) keep working byte-compatibly and need NO out-of-band migration. The metronome eliminates the herd FOR A POOL (echo_bot's Telegram dispatch); a lone consumer self-parks (one blocker is no herd).

GROUNDING: the program's law — emq.roadmap.md:142 ("thin but robust: opt-in families") + emq4.roadmap.md:263 ("Reversibility and opt-in (the library law) ... adopts ... incrementally and can back out"). The library law (no mod: auto-start) already makes every plane opt-in; the metronome is one more opt-in plane.

WHY RULE, NOT ASK: the asymmetry settles it — additive's downside if wrong is a retained fallback (minor, removable later); breaking's downside if wrong is a LIVE consumer down (major). Additive also keeps the shipped consumer_test.exs GREEN unchanged (the standalone path is byte-compatible; the NEW multi-consumer suite exercises the metronome path) and is fully consistent with MECH-(ii) + D-1 (the metronome is the POOL's coordinator; a lone consumer needs none — exactly venus-2's "codemojex doesn't need it, echo_bot does"). The program's opt-in law settles the design — not an Operator fork; the Director rules within the ruled mechanism.

SPEC DELTA (Venus syncs at Stage-5): soften the triad's "drops park/1"/"loses the private park" → "park/1 is the no-metronome FALLBACK; with a metronome the consumer registers instead of self-parking (the metronome is opt-in)." Mars builds to THIS (additive) from PASS 1.

### D-4 — Version model REFRAMED (supersedes D-3's lockstep): echo_mq rung-label ⫫ wire-protocol version

Operator directive (verbatim): "DROP THE TEST OF VERSION DRIFT. LIBRARIES COMPATIBLE BY DESIGN. NO MATTER. mix.exs comment: # rung number in echo_mq roadmap (INDEPENDENT from echo_wire version). Finish current echo_mq rung." This UN-FUSES D-3's lockstep (the mix.exs label ⊗ @wire_version ⊗ {emq}:version all carrying ONE number, climbing together). New model:

(1) version_reflection_test.exs (the EWR.1.4 reflection rule) DROPPED — grep-confirmed the ONLY cross-library-coupling reader (Application.spec(:echo_mq/:echo_wire,:vsn)). The two surviving readers are the legit single-source fence, untouched: connector_test.exs + conformance.ex:189 compare the live {emq}:version to Connector.wire_version() (the connector's OWN @wire_version constant), not cross-library app vsns.
(2) echo_mq/mix.exs version = the echo_mq ROADMAP RUNG LABEL = 2.4.3 (emq.4.3), read by NOBODY at runtime; INDEPENDENT from echo_wire (Operator set echo_wire to its own rung 2.5.0). Operator's comment carried.
(3) @wire_version does NOT climb — STAYS "echomq:2.4.2". emq.4.3 has ZERO wire-protocol delta (pure-BEAM: no new command/script, no §6 edit, no floor-raise). connector.ex BYTE-FROZEN → echo_wire touched NOT AT ALL → connector.ex DROPPED from the Stage-6 commit pathspec. This is MORE faithful to FROZEN-WIRE than D-3's "move the one @wire_version constant" carve-out (which is now moot).
(4) {emq}:version fence re-seeded to echomq:2.4.2 (SET == @wire_version) — the boot fence passes; consumer_test --include valkey 3/0 confirms the connect.

WHY RULE NOT ASK: the directive is explicit + unambiguous ("INDEPENDENT", "no matter"); the Director reconciles the spec to the directive within the ruled mechanism (MECH-(ii)) — not an Operator fork. Stage-5 syncs the triad (INV3 "the only wire touch is @wire_version" → "echo_wire untouched"; the D-3 lockstep clause → the INDEPENDENT-versions model).

### D-5 — emq.4.3 adds NO conformance scenario (the D-4 cascade); conformance.ex + both pin tests BYTE-UNCHANGED; the rung is fully decoupled from in-flight ewr.4.1

The conformance battery probes WIRE behavior. emq.4.3's metronome introduces ZERO new wire behavior — it blocks on the SHIPPED emq:{q}:wake and triggers the SHIPPED @gclaim; the wire trace of a metronome-coordinated claim is byte-identical to a standalone consumer's (the NUMBER of blockers is a deployment topology, not a protocol behavior conformance tests). The spec's metronome conformance scenario served TWO purposes, BOTH now moot: (a) probe a new wire behavior — none exists; (b) justify the protocol minor 2.4.2→2.4.3 — D-4 froze @wire_version (no minor to justify). So conformance.ex + conformance_run_test.exs + conformance_scenarios_test.exs are BYTE-UNCHANGED by emq.4.3; NO re-pin.

INV5 REINTERPRETED: "conformance byte-unchanged (no wire delta); the metronome proof is the :valkey PROCESS suite (US1-US4) + the ≥100 determinism loop" — NOT an additive-minor count climb. Stage-5 syncs the triad.

DECOUPLING (the bonus): this removes emq.4.3's last shared-file entanglement with the in-flight ewr.4.1 pool effort (which owns conformance.ex 55→57 +:pool_enqueue/:pool_order, jobs.ex opts \\ [], conformance_run_test {:ok,57}, conformance_scenarios_test +2, jobs_test +65). emq.4.3's touch-set now shares ZERO files with ewr.4.1 and touches echo_wire NOT AT ALL:
  NEW  metronome.ex, metronome/core.ex, queue.ex, metronome_core_test.exs, metronome_test.exs (the :valkey proof — a NEW file, not an edit to consumer_test/jobs_test)
  EDIT consumer.ex (additive :metronome path, D-3), mix.exs (the rung label 2.4.3)
  DOCS the triad (3), the ledger, emq.roadmap.md, emq.progress.md
This makes the Stage-6 LAW-4 commit a faithful, self-contained record of exactly emq.4.3 — the ewr.4.1 concern commits independently (Operator out-of-band), with NO ordering dependency.

ASYMMETRY (why rule): if a conformance scenario WAS warranted, it is a trivial later additive-minor; forcing one NOW re-entangles emq.4.3 with ewr.4.1 in 3 shared files AND commits a category error (a BEAM-process property dressed as a wire-conformance scenario). Director ruling within MECH-(ii), downstream of the explicit D-4 directive.

### D-6 — F-1 lost-wakeup defect in metronome.ex CONFIRMED by the Director's independent verify; Mars-2 applies the drain_mailbox-before-dispatch fix; COMMIT BLOCKED until green

The Director's Stage-2 verify (independent gate re-run, NOT the self-report) reproduced the failure the proof suite surfaced: metronome_test.exs US1 (line 80→132) RED — a job admitted to a registered-idle pool is NOT served within 1500ms (served a full ~2000ms beat late). This corroborates Mars's F-1.

ROOT CAUSE (Mars-diagnosed, Director-confirmed): metronome.ex poke_round/1 runs Core.dispatch against a STALE s.idle snapshot. A consumer's {:register_idle} that arrived DURING block/1's BLPOP is still in the mailbox (not yet drained into s.idle) when the consumed wake is dispatched → no idle pid is poked → the wake's EFFECT is dropped → the job waits a full beat. This is the EXACT lost-wakeup hazard the rung exists to defeat (INV1 "no lost wakeup"). It is MASKED on short-beat US2 (100ms beat + 5000ms budget) and BITTEN on long-beat US1 (2000ms beat + tight <600ms bound). The proof did its job — and Mars did NOT weaken US1 to pass against buggy code (no false-green, the failure mode the program guards).

FIX (Mars-verified, probe-tested then reverted net-zero; NOT applied — PASS-1 lib, out of PASS-2 scope): in poke_round/1, add `s = drain_mailbox(s)` BEFORE Core.dispatch, so the consumed wake dispatches against a FRESH registry. Result: US1 served in 2ms; seed 17 6/6 (was 0/8). Strictly better — also processes {:DOWN}/{:deregister} before dispatch, so a dead pid is never poked. The pure Core.dispatch (one-claim-per-idle, fair) is UNCHANGED; only the loop's ordering is corrected — this is the metronome's correctness contract (the consumed wake acts on the live registry), not a workaround.

RULING: Mars-2 (the harden pass, same Mars identity) applies the one-line fix to metronome.ex, then runs the FULL per-app gate + the ≥100 determinism loop OWNING THE MACHINE (the lost-wakeup + same-ms branded-JOB mint are cross-run — this loop is non-negotiable on the HIGH-risk rung; the failure just proved a single green run is not enough). The Director then re-verifies (independent loop spot-check + §12.2/byte-freeze/FROZEN-WIRE/boundary probes + a net-zero mutation) before the commit. COMMIT BLOCKED while the gate is RED — the Director does not commit broken code. Apollo MANDATORY remains a ship precondition.

### D-7 — Fan out a targeted Mars-2 to HARDEN (the F-2 register-race + the rung's stale doc)

The rung is GREEN and shippable as-is (Y-3); this is an Operator-mandated HARDEN, not a ship-blocker. Rationale: the Operator's mandate is "remediate AND harden"; this is a HIGH-risk permanent process surface; F-2 is the gate-invisible class the program's discipline exists to close (the F-1 sibling); the fix is 2 lines, idiomatic, low-regression. So close the latent race now rather than ratify it into the permanent surface.

Mars-2 scope (TIGHT): (1) metronome.ex — move the name registration OUT of the spawn_link'd fn to the parent (Process.register(pid, name) after spawn_link, before {:ok, pid}; remove the in-fn register), so the name exists synchronously before start_link returns and the rest_for_one supervisor cannot start a consumer against an unregistered name; KEEP the F-1 drain at poke_round/1:147 intact. (2) metronome_test.exs:8 — drop/relax the brittle "55-scenario" count (prefer no number). NOT conformance_run_test.exs (pre-existing, out of boundary). Then the full per-app gate + a FRESH ≥100 determinism loop (the changed startup path warrants it) + byte-freeze/boundary confirmations; conformance.ex byte-unchanged (D-5). No commit (Director-only). REMEDIATE loop MAX 3.

## {emq-4-3-learnings} Learnings

### L-1 — Arm B's mechanism (i) collides with design §12.2, which is the spine of the whole fork

The design canon §12.2 (emq.design.md:457-463) already RULED against client-side multi-key pops (LMPOP/ZMPOP): "Availability is not the blocker (Since 7.0.0); necessity is. Every v2 state transition is one Lua script — atomic on the engine — so a client-side pop would BYPASS the script layer's event and bookkeeping path: a second, weaker transition path is the opposite of a wire contract... claim IS ZPOPMIN inside the claim script, so the client-side variant has no role at all." And §12.5 (emq.design.md:483-490): the 2.0 core wire is bounded by the 7.0-level CEILING; floor ≈ 6.2-level (LMOVE the newest); "a computed-floor raise is a protocol minor at minimum."
IMPLICATION for Arm B mechanism (i) — server-side blocking grouped-claim (BLMOVE/BLMPOP on the ring/lane): a Lua EVAL CANNOT block (no blocking command is allowed inside a script — Valkey rejects it). So the block MUST be a connector-level blocking command FOLLOWED by the atomic @gclaim EVAL. But the ring is `LMOVE ring ring LEFT RIGHT` (rotate) and the lane is a ZSET (ZPOPMIN). A BLMOVE blocks on a LIST; the lane is a ZSET, so BLMOVE cannot pop the lane. BLMPOP (7.0, at the ceiling) blocks on a ZSET but pops the member OUTSIDE the @gclaim script — bypassing the TIME lease + attempts mint + gactive bookkeeping + the ring-rotation fairness — EXACTLY the §12.2-forbidden "second weaker transition path." So a blocking pop that REPLACES the wake-token two-step would either (a) need the block on a NEW per-lane signal LIST (a wake-shaped key, BLMOVE-able) feeding the SAME @gclaim EVAL (a block-then-claim, NOT a block-AND-claim) — which is the wake token RENAMED, not subsumed; or (b) bypass @gclaim (forbidden). This is the decisive cost the steward analysis must surface: Arm B's "subsume the two-step" is in TENSION with the design's one-transition-one-script law + the §12.2 ruling. A genuine subsumption keeps @gclaim as the claim and only changes how the consumer BLOCKS — i.e. it blocks on the readiness signal directly (a server-side block) instead of BLPOP-on-wake-then-drain. The signal it blocks on is still a §6-grammar key.

### L-2 — Two process learnings for Apollo's Stage-6 mentorship

(1) Report-before-idle (the persistence law). Mars-2 applied the harden correctly but emitted THREE idle notifications with NO final report; only a late interim landed. Root cause: its determinism loop kept dying (so it had no final number) and it idled rather than reporting the done parts + the blocker. Lesson for the implementor charter/skill: SendMessage a report (even an interim — "done X/Y, blocked on Z, ETA") BEFORE going idle; silence reads as a stall and forces the Director to recover by hand. The Director's countermeasure worked and should stay default: verify the TREE (LAW-1a), never wait on narration — the code artifacts are on disk regardless of whether the agent reports.

(2) The ≥100 determinism loop must NOT run as a BACKGROUND task — both Mars's (sub-agent) AND the Director's (main-session) background loops were reaped mid-run (exit 144 / signal 16; Mars ~seed 15; the Director's empty-output = killed-not-failed). Reliable pattern: run the loop in FOREGROUND chunks sized under the 600s Bash cap — at ~6.5s/run for the metronome suite, ~55 iterations per ~6-min chunk, two chunks → 110/110. Candidate fold into the gate-ladder guidance (echo-mq-program / echo-mq-implementor): "the ≥100 loop runs FOREGROUND in timeout-bounded chunks driven to an accumulated count; background runs get reaped."

### L-3 — Stage-6 mentorship consolidation (the rung's craft + process lessons, folded forward)

The emq.4.3 metronome surfaced TWO gate-invisible defects of ONE family + two process slips. Recorded here (the echo_mq retrospective home is this learnings channel; the rung .md carries no `### Went not well` section). Mentoring is by peer + channel; the durable `.claude/` edits are PROPOSED to the Director (the harness fences self-modification of agent/skill config from a peer-relayed grant — apollo.md's named brake; not worked around).

FINDING FAMILY (→ Mars, durable craft guardrail) — BEAM process-lifecycle ordering, gate-invisible:
- F-1 (lost-wakeup): poke_round/1 dispatched against a STALE s.idle captured before block/1's BLPOP; a {:register_idle} arriving DURING the block sat un-drained → the consumed wake poked nobody → the admit waited a full beat. MASKED by the short-beat US2 (100ms beat / 5000ms budget); BITTEN by the long-beat US1 (2000ms beat / <600ms bound). Fix metronome.ex:126 — drain at the TOP of poke_round/1 before reading the registry.
- F-2 (register race, the F-1 sibling): Metronome.start_link ran Process.register INSIDE the spawn_link'd fn → the name could be absent when start_link returned → a rest_for_one Queue could send/2 to an unregistered atom (RAISES). Effectively unhittable + self-healing → gate + 100/100 loop never see it. Fix metronome.ex:94 — register from the PARENT on the returned pid before {:ok, pid}.
- THE CLASS: a new supervised-process surface has a startup/notify ORDERING contract no functional test asserts. The discipline: reason about "what is in flight when this line runs" and assert the ordering directly — drain-before-read (F-1); register-before-return (F-2); plus the long-beat no-op-defeater timed test (a fallback wider than the assertion bound forges a false-green). This BROADENS the Stage-1 DIFF-1 implementor guardrail (block-then-act drain) to the full process-startup ordering class — F-2 is its second citation. SHARPEN the one line, do NOT stack a second (apollo.md close-the-loop rule).

PROCESS (→ Mars charter/skill + gate-ladder, durable), both already flagged at L-2:
- L-2(1) report-before-idle: Mars-2 emitted 3 idle notifications with NO final report (its determinism loop kept dying so it had no number, and idled rather than reporting done-parts+blocker). Lesson: SendMessage an interim report ("done X/Y, blocked on Z") BEFORE going idle; silence reads as a stall. The Director's countermeasure (verify the TREE per LAW-1a, never wait on narration) worked and stays default.
- L-2(2) the ≥100 loop must run FOREGROUND in timeout-bounded chunks driven to an accumulated count: BOTH Mars's and the Director's BACKGROUND loops were reaped mid-run (exit 144 / signal 16). At ~6.5s/run for the metronome suite, ~55 iters per ~6-min chunk, two chunks → 110/110 (the proven pattern this rung used).

GUARDRAILS AUDITED (close-the-loop): the Stage-1 DIFF-1 guardrail (planted this rung) cannot yet be reconciled — emq.4.4 is the next rung to test whether the predicted process-ordering failure stays away. The F-1/F-2 pair is the evidence the guardrail is correctly aimed at a REAL repeatable class (two occurrences, one rung).

VERDICT (independent of both duties): BUILD-GRADE — corroborated by the Director's Y-1/Y-2 (FULL suite 402/0, conformance 59/59 byte-unchanged per D-5, 110/110 loop, mutation kill-rates F-1 1/1 + F-2 1/1 + US3 1/1, @wire_version echomq:2.4.2 zero wire delta, boundary clean). The grade bends to neither the documentation nor the mentoring duty.

## {emq-4-3-report} Report

### Y-1 — Director Stage-3 independent verify: BUILD-GRADE confirmed; one latent finding (F-2)

Independent reproduction on Valkey 6390 (not Mars's word):
- compile --warnings-as-errors: echo_mq boundary CLEAN (only the pre-existing echo_data→EchoStore.Graft.* WIP warnings — outside the boundary, not introduced here).
- FULL per-app suite --include valkey: 7 doctests, 401 tests, 0 failures. CONFORMANCE 59/59 (UNCHANGED; D-5 holds — conformance.ex + both pin tests byte-unchanged; the 4 scenarios beyond 55 are the recent ewr.2.5/2.6 pool+native-expiry rungs).
- Boundary/byte-freeze: only consumer.ex (+111/-4, additive :metronome path; 0 redis.call added) + mix.exs (2.4.3 label, D-4) modified; metronome.ex/core.ex/queue.ex + 2 tests untracked; conformance.ex/jobs.ex/lanes.ex/connector.ex byte-unchanged; @wire_version echomq:2.4.2; siblings + mix.lock clean.
- F-1 drain fix present at metronome.ex:147.
- Mutation kill-rate, two distinct load-bearing tests proven to BITE: Mars's drain-removal → US1 RED (1/1); Director's dispatch→Enum.take(_,1) head-only → US3 RED ("only 1 of 4 served, counts=[40]") → reverted net-zero, US3 green (1/1).

Code read (core/queue/consumer/test): SOUND — additive opt-in (D-3, standalone path byte-frozen), byte-frozen @gclaim via Lanes.claim (§12.2, consumer never pops the lane), rest_for_one supervision, consumer-fair dispatch (one claim per idle per wake), monitor-based registry safety.

FINDING F-2 (latent, gate-invisible, LOW severity) — metronome-name registration race: Metronome.start_link registers the name INSIDE the spawn_link'd fn, so the name may be unregistered when start_link returns; the Queue (rest_for_one) then starts consumers whose first act sends to that atom name (send/2 to an unregistered name RAISES). Effectively unhittable (consumer's first send is gated behind Connector.start_link's TCP+RESP handshake, ~1000x the metronome's 2-BIF register) and self-healing (the supervisor restarts the consumer; the name exists by then) — which is why the gate + 100/100 loop never see it. Idiomatic close: register from the parent on the returned pid before {:ok, pid}.
Cosmetic: metronome_test.exs:8 moduledoc "the 55-scenario conformance suite" is stale (now 59) — the rung's own file. (conformance_run_test.exs:5 moduledoc also stale at 55, but PRE-EXISTING / out-of-scope — not the rung's debt.)

### Y-2 — Director Stage-4-post re-verify: HARDEN RATIFIED (F-2 closed), all green

Mars-2 applied the harden correctly (verified in the tree — Mars never sent a final report; recovered by direct verification + a late interim that confirmed it):
- F-2 fix: metronome.ex:94 `if name, do: Process.register(pid, name)` from the PARENT on the returned pid, before {:ok, pid}; the in-fn register removed; exactly one Process.register; the F-1 drain intact at poke_round/1 top. Closes the registration race by construction (the name exists before start_link returns, so a rest_for_one consumer cannot send to an unregistered atom).
- New F-2 regression test (metronome_test.exs): 50 fresh start_links, asserts Process.whereis(name)==pid SYNCHRONOUSLY (no sleep). Doc reworded (metronome_test.exs:8 brittle "55-scenario" count dropped).

Independent gate reproduction (mine, not Mars's word):
- compile --warnings-as-errors: echo_mq boundary clean (only pre-existing echo_data Graft-WIP warnings, outside boundary).
- FULL suite --include valkey: 7 doctests, 402 tests, 0 failures (401 + the new F-2 test). CONFORMANCE 59/59 unchanged (D-5; conformance.ex + both pin tests byte-unchanged).
- Mutation kill-rates: F-2 1/1 (Director: comment-out the parent register → the 50-start test RED at the whereis assert → reverted net-zero, green) + Mars's DISTINCT in-fn-restore mutation → RED = double-proven; US3 1/1 (Stage-3 fairness, dispatch→head-only → RED).
- ≥100 determinism loop: 110/110 clean (seeds 1..110, two uncontended foreground chunks @ ~6.5s/run).
- Boundary/byte-freeze: only consumer.ex (+113/-4) + mix.exs (2.4.3 label, D-4) tracked-modified; metronome.ex/core.ex/queue.ex + 2 tests untracked; 0 redis.call in new files; conformance.ex/jobs.ex/lanes.ex byte-unchanged; @wire_version echomq:2.4.2; siblings + mix.lock + apps/echomq untouched.

VERDICT: BUILD-GRADE. Ship-ready pending Venus's triad reconcile (Stage 5) + Apollo mentorship (Stage 6) + the Operator-confirmed LAW-4 commit.

## {emq-4-3-complete} Complete

### Z-1 — emq.4.3 SHIPPED (park-don't-poll metronome, MECH-(ii) the metronome-as-system)

The /echo-mq-ship remediation+harden pipeline closed BUILD-GRADE across all six stages: Apollo pre-calibrate (proposals, .claude/ fenced) → Mars verify (F-1 drain confirmed) → Director Stage-3 review (Y-1; US3 mutation 1/1; D-7) → Mars-2 harden (F-2 register race closed) → Director Stage-4-post re-verify (Y-2; 110/110 loop; F-2 mutation 1/1) → Venus Stage-5 triad reconcile (D-3/D-4/D-5 corrected) → Apollo Stage-6 mentorship (L-3 + Proposals A–D, fenced).

As shipped: EchoMQ.Metronome (one BLPOP emq:{q}:wake blocker + idle registry per queue) + EchoMQ.Metronome.Core (pure dispatch/repoke) + EchoMQ.Queue (rest_for_one host wiring: one metronome + N consumers) + EchoMQ.Consumer's opt-in :metronome pool path (D-3 additive — standalone park/1 retained byte-for-byte, protecting codemojex). Two gate-invisible process-ordering defects closed and each mutation-proven: F-1 (drain the mailbox at poke_round/1 top before dispatch) + F-2 (register the metronome name from the parent before start_link returns).

No wire delta: @wire_version stays echomq:2.4.2; mix.exs 2.4.3 = the roadmap-rung label, independent of echo_wire (D-4). No conformance scenario — a BEAM process/lease property, not a wire trace; Conformance.run/2 → {:ok, 59} byte-unchanged (D-5). Proof: the :valkey process suite (metronome_test US1–US4 + the F-2 synchronous-registration test + metronome_core_test) + a 110/110 determinism loop. Gate: 402 tests / 0 failures, conformance 59/59, THREE distinct mutation kills (US1 drain, F-2 register, US3 fairness).

LAW-4 commit: ONE Director pathspec commit of the rung's measured surface — 7 code files (metronome.ex, metronome/core.ex, queue.ex, consumer.ex, mix.exs, metronome_test.exs, metronome_core_test.exs) + the emq.4.3 triad (.md/.stories.md/.prompt.md) + the emq-4-3 ledger (progress.md + registry.json) = 12 files. EXCLUDED: conformance.ex/jobs.ex/lanes.ex/connector.ex (byte-unchanged), the entangled docs-reconcile tree, mix.lock, the .claude/ calibration diffs (harness-fenced; queued for first-party application). No push. Operator-confirmed direct.

Next frontier: emq.4.4.
