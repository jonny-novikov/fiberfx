# emq-2-4 — AAW scope ledger

## {emq-2-4-thinking} Thinking

### T-1 — emq.2.4 design cycle: §0 derivation + the ground truth passed to Venus

MODE: Flat-L2 DESIGN PHASE (Director + Venus; NO Mars build, NO Apollo — Apollo is out of design phases per the aaw D-7 lock). Scope emq-2-4. Operator jonny. Ledger docs/echo_mq/specs/emq-2-4.progress.md. The stale emq-2-3 team is stood down (Mars-1 shut down; TeamDelete) and a fresh team orchestrated.

WHY (Operator directive). The emq.2 parity cluster (read→ops→watch) is nearly closed: emq.2.1 (read) shipped 7d98ef86, emq.2.2 (ops) shipped 76fc947c, emq.2.3 (watch) BUILT on disk this run (harden landed, gate not yet fully run, UNCOMMITTED). The Operator redirected from finishing 2.3 inline to a design/reconcile cycle that (a) catalogs the whole EchoMQ 3.0 vision, (b) PROVES v2 full-parity, (c) finds the gaps 2.1/2.2/2.3 left vs the emq.2 design, and (d) specs emq.2.4 as the final parity-closing stage — seeded by the Operator's observation that v1 `echomq` has dramatically more test files than v2 `echo_mq`.

GROUND TRUTH (passed to Venus):

1) emq.2.3 AS-BUILT (uncommitted). 6 lib modules — EchoMQ.{Events, Telemetry(Meter surface), Stalled(Checker), Cancel(Token)} + EchoMQ.Locks (lock_manager.ex + lock_manager/core.ex) + Jobs.extend_lock/5 & extend_locks/4. 5 test suites (cancel 19, meter 11, events 8, jobs_extend 9, locks_stalled 13). Conformance 32→37 (+5: lock_extend, stalled, events, telemetry, cancel; the 32 prior byte-unchanged, both pins re-pinned). Both determinism flakes fixed at root — the A1 cast/call race (a Locks.is_tracked?/2 FIFO barrier after the track_job cast) and the version-fence linked-exit propagation (trap_exit hoisted to the fence describe's setup + fence_db_flush rewritten to raw redis-cli). Compile clean; the two fixed suites green; the full ≥100 gate was interrupted by the Operator redirect (honest posture: NOT yet ratified by a completed solo loop).

2) THE TEST-COVERAGE GAP (the Operator's question — QUANTIFIED). v1 echomq: 41 files · 534 tests+properties · 195 describes · 15,322 test LoC over 13,189 lib LoC (1.16:1). v2 echo_mq: 28 files · 201 tests · 36 describes · 3,529 test LoC over 4,083 lib LoC (0.86:1). The gap concentrates in v1 categories: a 7-file concurrency/stress/scaling cluster (concurrency_diagnostic/scaling/verification, high_concurrency, multi_worker, stress, redis_baseline); 4 job_scheduler depth files; 5 worker files; a 3-file telemetry/OTel tree; a 7-file conformance/ harness (bundle, containment_probe, fence, interop, migration, supervised_boot, truth_row_probes). Some maps to DEFERRED v2 rungs (worker abstraction→emq.6, OTel→emq.8) or a different discipline (the ≥100 determinism loop replaces dedicated stress files); a real residue is GENUINE under-port of scenario depth for the SHIPPED read/ops/watch surface. emq.2.4's test mandate = close that residue; do NOT pad coverage for unshipped rungs (dishonest).

3) THE v1→v2 PARITY SKELETON (Venus fleshes out + grounds). v1 backoff→v2 Backoff (emq.1); cancellation_token→Cancel (emq.2.3); champ→echo_data CHAMP (M0); fence/version→echo_wire Connector fence (emq.0); flow_producer→emq.3; id→echo_data Snowflake/BrandedId (M0); job & queue→Jobs/Admin/Metrics (emq.2.1/2.2); job_scheduler→Repeat/Pump (emq.1); keys→Keyspace; lock_manager→Locks (emq.2.3); opentelemetry→emq.8; queue_events→Events (emq.2.3); redis_connection→echo_wire Connector (emq.0); scripts→inline Script.new (per-module law); stalled_checker→Stalled (emq.2.3); telemetry→Telemetry surface (emq.2.3) / contract (emq.8); worker & behaviour→Consumer (partial; full worker abstraction emq.6); migration/echomq.migrate→RULED OUT (drain-precondition; emq.2.design ADR-0).

DELIVERABLES (Venus, sequenced): (D-V1) NEW docs/echo_mq/emq.features.md — the EchoMQ 3.0 feature catalog (emq.2.x building + emq.3–emq.8 upcoming + the 3.x stream tier as proposed) with use cases for consumers/systems and ESPECIALLY the exemplar Exchange Platform (echo/apps/exchange, the TRD.* traces) + a full md table PROVING v2 full-parity (each v1 capability → which emq.x rewrites it: shipped/building/planned). (D-V2) the emq.2.md ⇄ emq.2.3* reconcile → a side-by-side gap table (what the emq.2 design promised vs what 2.1/2.2/2.3 as-built delivered). (D-V3) the emq.2.4 triad (.md/.stories.md/.llms.md) — the FINAL parity-closing stage: (1) implement the missing features + improvement opportunities the gap table surfaces, (2) the COMPLETE test suite that closes the v1↔v2 coverage gap for the shipped parity surface. THEN the Director consolidates docs/echo_mq/specs/emq.2.prompt.md (currently the RETIRED migration runbook) into the live cluster orchestration runbook that drives the emq.2.4 build + closes the cluster.

SMALLEST CHANGE THAT PRESERVES CORRECTNESS: this cycle authors DOCS only (the catalog + the reconcile + the emq.2.4 triad + the runbook); zero echo_mq code change; emq.2.4's BUILD is a later cycle the consolidated runbook drives. NO-INVENT: every feature cites a real module/file or a design §; forward-tense ("emq.N builds…") for unshipped surface; records-freeze on the emq-2-1/2-2/2-3 ledgers.

### T-2 — D-V1 ground: the as-built reconcile (verified against the tree 2026-06-14)

GROUNDED (every figure re-probed, file:line where load-bearing):

CONFORMANCE = 37 (T-1's 32→37 CONFIRMED). conformance.ex:25 scenarios/0 lists 37; the two pin tests (conformance_run_test.exs run/2→{:ok,37}, conformance_scenarios_test.exs @run_order 37 names) both re-pinned. The 18 founding+emq.1 byte-unchanged + 6 read (counts/state/metrics/dedup/rate/lane_depth) + 8 ops (queue_pause/drain/obliterate/update_data/update_progress/job_logs/remove_job/reprocess_job) + 5 watch (lock_extend/stalled/events/telemetry/cancel).

emq.2.3 AS-BUILT SURFACE (uncommitted on disk, the watch plane). EchoMQ.Events (events.ex): subscribe/2, unsubscribe/2, close/2, channel/1, publish/5, event_name/1 + the GenServer + handle_event/3 behaviour. EchoMQ.Telemetry (telemetry.ex): attach/4, attach_many/4, emit/3, span/3 + job_added/job_started/job_completed/job_failed/job_retried/worker_started/worker_stopped/rate_limit_hit. EchoMQ.StalledChecker (stalled_checker.ex): check/3, job_stalled?/4, child_spec/1, start_link/1, stop/2 + the :sweep timer. EchoMQ.CancellationToken (cancellation_token.ex): new/0, cancel/3, check/1, check!/1 + the typed exception. EchoMQ.LockManager (lock_manager.ex + lock_manager/core.ex): track_job/3, untrack_job/2, get_active_job_count/1, get_tracked_job_ids/1, is_tracked?/2, child_spec/1, start_link/1, stop/2 + Core.lease_ms/marker_ttl_ms/extend_ms. EchoMQ.Jobs.extend_lock/5 (jobs.ex:646) + extend_locks/4 (jobs.ex:671) via @extend_lock (jobs.ex:569) + @extend_locks (jobs.ex:581) inline scripts. 5 test suites confirmed: cancel_test, meter_test (telemetry), events_integration_test, jobs_extend_test, locks_stalled_test.

THE TEST GAP (Operator's question — QUANTIFIED, re-probed). v1 echomq: 41 files (39 _test + helper + support) · 534 tests+properties · 195 describes · 15,322 test LoC over 13,189 lib LoC (1.16:1). v2 echo_mq: 28 files · 201 tests · 36 describes · 3,529 test LoC over 4,083 lib LoC (0.86:1). THE CONCENTRATION (corrected from T-1's framing — the BIGGEST cluster is job_scheduler, not worker): job_scheduler depth 95 tests/4 files (unit 12, integration 52, processing 10, stress 21); worker 76/3 (worker 19, integration 48, cancellation 9); telemetry/OTel 44/3 (telemetry 10, behaviour 9, opentelemetry 25); conformance/ harness 27/7 (bundle 6, containment_probe 1, fence 11, interop 1, migration 5, supervised_boot 2, truth_row_probes 1); the stress/concurrency cluster 20/7 (concurrency_diagnostic 2, scaling 1, verification 4, high_concurrency 1, multi_worker 1, stress 8, redis_baseline 3) — its value is SCENARIO DIVERSITY, not count, so the ≥100 determinism loop replaces it honestly.

THE v1 REFERENCE INVENTORY (probed). 21 lib modules: backoff, cancellation_token, champ, fence, fence_error, flow_producer, id, job, job_scheduler, keys, lock_manager, migration, queue (2144 LoC hub), queue_events, redis_connection, scripts, stalled_checker, telemetry, types, version, worker (1908 LoC). 26 Lua scripts. flow_producer: add/2, add_bulk/2. job_scheduler: upsert/7, get/4, list/3, count/3, remove/4, remove_by_key/4, calculate_next_millis/2. champ→echo_data BCS CHAMP; id→echo_data Snowflake/BrandedId; fence→echo_wire Connector fence; version→echo_wire @wire_version; migration→RULED OUT (drain-precondition, ADR-0).

EXCHANGE TRACES (exchange.specs.md §Jobs:87, exchange.roadmap.md:50-58): Jobs surface = settlement/notifications/EOD/reconciliation on EchoMQ.Jobs drained by EchoMQ.Consumer shaped by EchoMQ.Lanes one-group-per-venue (TRD.3 settlement-on-a-venue-lane). Scheduled/repeatable → emq.1 (2.1 row). TRD.4 market-data-as-claims → emq3.2 stream; TRD.6 risk-as-consumer-group → emq3.3. emq.1 SHIPPED so the Jobs trace is closed.

NOTE (skill-ref staleness, not my triad): .claude/skills/echo-mq-program.md:77 says "as-built today the set is 18" — stale; live is 37. A surface-map drift, flagged to the Director, not a triad edit.

### T-3 — emq.2.4 BUILD cycle: the §0 derivation + the lag-1 deltas handed to Venus-2

MODE: Flat-L2 BUILD PHASE, HIGH-RISK (process/mint-touching watch-depth suites + the ≥100 determinism loop) → Apollo MANDATORY. Scope emq-2-4. Operator jonny. This appends AFTER the design cycle's Z-1 — records-freeze on the design half (T-1/T-2, A-1/A-2, D-1/D-2/D-3, Y-1/Y-2, L-1, Z-1). Build-cycle entries: T-3, D-4+, Y-3+, L-2+, Z-2. Team: a fresh emq-2-4 TeamCreate; build peers Venus-2 (architect) / Mars-1 (implementor) / Apollo-1 (evaluator) — Venus-2 distinct from the design-cycle Venus-1; Mars/Apollo first-use (the D-7 lock kept both out of the design phase).

ALL FORKS SETTLED — the Stage-1 gate is reachable, no launch-time fork stop:
- G1 (rate-ceiling-into-claim) = ARM 2 (D-3, Operator 2026-06-14): hold Metrics.is_maxed/2 as the consult-before-claim pure-read; document the contract; NO @claim/@gclaim edit. The @claim-edit risk dimension is OFF; lanes.ex drops from the pathspec.
- G2 (metrics :data rolling series) = HOLD to emq.8 (D-4, Operator THIS turn): D3 records the documented hold; get_metrics/3 stays honest-0; emq.2.4 adds NO @complete/@retry :data write — a clean, test-focused closer whose whole risk surface stays in the test suite (consistent with Arm 2's spirit + emq.2.1's L-2(1) routing of the rolling series to emq.8's presentation contract).
- G3 (de:* orphan) = DOCUMENTED, not fixed (declared-keys honest limit; no SCAN, no backref — a backref breaks the three-field row INV1, a SCAN crosses slots/breaks A-1).

5W (build): WHY — close the emq.2 parity cluster (read 2.1 ✅ · ops 2.2 ✅ · watch 2.3 ✅ · close 2.4) so apps/echomq can dissolve on a proven-at-depth echo_mq. WHAT — inside echo/apps/echo_mq under the v2 laws: D2 (Arm-2 contract doc + a :valkey consult-then-skip test, no @claim edit), D3 (record the G2 HOLD), D4 (de: documented + a bounded-complete release test), D5 metrics_depth, D6 admin_depth, D7 watch_depth (process-touching → ≥100 loop), D8 (attribution + the conformance 37→N re-pin), PLUS the C1 carry (rename the files to match the as-built modules) + the C2 carry (the two probe-verified-but-untested branches). WHO — the program, the consumers (every queue-health/operator/watch read exercised at v1 depth), the conformance harness (grows by genuine new scenarios), Apollo (MANDATORY independent re-run). WHEN — Movement I, the 4th & last emq.2 rung, on the NOW-COMMITTED emq.2.3 foundation. WHERE — echo/apps/echo_mq ONLY (Arm 2 touches no echo_wire, no lanes.ex, no @claim); apps/echomq + mix.lock UNTOUCHED.

INVARIANTS AS RUNNABLE CHECKS (INV1-8): INV1 the 37 prior conformance byte-unchanged (git-verified) + additive-minor + re-pin 37→N in BOTH pin tests (conformance_run_test.exs:37 run/2→{:ok,37}; conformance_scenarios_test.exs:16 @run_order). INV2 the honesty gate — no depth test drives an unshipped surface (no worker-abstraction / OTel-contract / distributed-cancel / flow / durable-stream test); a false-green is forbidden. INV3 no regression (emq.1 + 2.1/2.2/2.3 + Conformance.run/2 unchanged). INV4 declared keys + inline Script.new/2 (no priv/) — N/A for new wire under the G1/G2 rulings (Arm 2 + HOLD add no Lua); the depth tests construct no key outside the §6 grammar. INV5 branded identity at every job boundary; distinct ids per job; the ≥100 loop surfaces a same-ms mint collision. INV6 the family boundary (worker→emq.6, telemetry contract→emq.8, durable stream→emq3.2, flows→emq.3). INV7 ≥100 loop for D7 (process-touching); multi-seed sweep for D5/D6 (synchronous deterministic round-trips — running the loop there forges load the rung did not introduce). INV8 the fork+risk gate — all forks ruled; HIGH-RISK → Apollo MANDATORY.

THE LAG-1 DELTAS Venus-2's Stage-0 reconcile must re-pin (the design docs predate the emq.2.3 commit):
1) emq.2.3 is NOW COMMITTED (3c6461ff the rung + 5a3fdd73 the docs fold) — the design docs say "on disk, uncommitted". Re-pin to committed.
2) MODULE-NAME DRIFT (doc⇄code): the design triad/runbook/features.md cite the v1-style names EchoMQ.{Telemetry, LockManager(+Core), StalledChecker, CancellationToken}; the AS-BUILT committed code uses the collision-free names EchoMQ.{Meter, Locks(+Core), Stalled, Cancel} (verified: stalled_checker.ex → defmodule EchoMQ.Stalled). Venus-2 reconciles every anchor MATCH-or-[RECONCILE]. This is the same drift the C1 carry resolves at the FILE level (telemetry.ex→meter.ex, lock_manager.ex→locks.ex+core, stalled_checker.ex→stalled.ex, cancellation_token.ex→cancel.ex so basename matches module).
3) THE C2 CARRY (emq.2.3 deferred): extend_locks/4 partial-batch [live,stale,gone] + the @sweep_stalled group-aware recover branch were probe-verified by Apollo but lack standing tests → they fold into D7 watch_depth.
4) Conformance floor = 37 (re-probe at B0; do NOT hardcode if a sibling commit moved it).
5) Toolchain: echo/.tool-versions pins erlang 28.5.0.1 (the global is 28.1 — asdf resolves 28.5.0.1 INSIDE echo/apps/echo_mq, the build dir; re-probe from the app dir, never the repo root). Valkey 6390 PONG.

SMALLEST CHANGE: emq.2.4 is primarily a TEST rung. The feature residue is tiny under the rulings (Arm 2 = a doc + one consult-then-skip test, zero code edit; G2 = a recorded hold, zero code; G3 = a doc + a bounded-complete test). The bulk = D5/D6/D7 depth suites + the C1 file renames + D8 attribution + the additive conformance scenarios. NO new wire class, NO new key type, NO transition rewrite, NO frozen-line touch.

### T-4 — emq.2.4 ship-close recycle: ground truth + the harness mandate

MODE: Flat-L2 BUILD-CLOSE RECYCLE, HIGH-RISK (process/mint depth + a shipped-script-edit floor) -> Apollo MANDATORY. The prior build cycle (T-3..P-3) stalled MID-STAGE-4: Apollo-1 ran its LAW-1a mutation spot-check (L-3 — the git-checkout-on-dirty footgun that briefly clobbered the uncommitted obliterate fix, traced+restored) but a SYSTEM CRASH interrupted before Apollo filed Y-3 or the rung closed (no Y-3, no Z-2; registry last_seen all <=15:28Z, dead). The Operator then committed the DOCS as 7c2f2405 "2.4 incomplete. Not ready to test. Harness required." and flagged (QA) emq.testing.md inconsistent + the code commits never made.

GROUND TRUTH AT RESUME (Director-verified independently — verify the artifact, not the prior board):
- THREE emq.2.4-arc commits now on master: 92a8f042 (the obliterate fix, admin.ex — committed last session) · 7c2f2405 (Operator docs: features/testing/triad/ledger/registry/prompt) · 3298e4bc (THE CLOSER CODE, committed THIS resume: the 5 C1 renames byte-identical so files match modules, conformance 37->43 additive-minor both pins re-pinned, the 5 depth suites). The closer code was UNCOMMITTED before this resume — the crash left it on disk only, so the committed green-board described a tree state in no commit.
- INDEPENDENT GATE RE-RUN (Director, live working tree, Valkey 6390): TMPDIR=/tmp mix compile --warnings-as-errors clean; mix test --include valkey = 4 doctests / 250 tests / 0 failures; EchoMQ.Conformance.run/2 = {:ok, 43} (all 6 new scenarios green incl. obliterate_grouped). The C1 renames diff byte-identical vs HEAD. CONCLUSION: the CODE was never broken — the failure was PROCESS (verified code left uncommitted by the crash + the determinism proof was EPHEMERAL, a hand-run loop tee'd to /tmp, evaporated on the crash).

THE QA ISSUES DECODED (the recycle's targets): (1) emq.testing.md INCONSISTENT = its 0 green-board (committed in 7c2f2405) asserts all-green for code that was then uncommitted, beside the Operator's "not ready" title; ALSO emq.progress.md dashboard carries a stale one-line "24/24". (2) COMMITS NOT MADE = the closer code (now landed 3298e4bc). (3) BUGS UNFIXED DUE TO CRASH = the obliterate INV3 leak WAS fixed (92a8f042) but its proving closer never landed, so from git history the fix looked unsubstantiated; AND a crash mid-Stage-4 means any OTHER defect Apollo would have surfaced was never surfaced.

THE RECYCLE MANDATE (to ship a TESTED, REPRODUCIBLE emq.2.4):
(a) THE HARNESS (the Operator's "Harness required"): build a COMMITTED, re-runnable proof script (echo/rungs/-style) that runs the emq.2.4 gate ladder + the >=100 determinism loop over the process-touching depth suites + the multi-seed sweep + the dbsize-flat leak check, and emits a PASS/FAIL board — so the green-board is reproducible by anyone and a crash cannot evaporate it. This is the structural fix for "ephemeral proof != a harness".
(b) THE emq.testing.md HONEST-IN-FACT RECONCILE: re-true 0 to the as-built committed state (3 commits, conf 43, 250/0), frame every figure as reproducible-via-the-harness (cite the committed script, not a /tmp tee), and resolve the contradiction with the Operator's "not ready" title (the rung is now code-landed+gate-green; the board states that honestly, not a premature "shipped").
(c) APOLLO-2 STAGE-4 (MANDATORY, the interrupted gate): independent re-run of the full ladder + the NEW harness; an ADVERSARIAL bug-hunt for any OTHER detected-but-unfixed defect (declared-keys/F-1 on any new Lua — expected none under Arm 2/G2-HOLD; the order theorem; at-most-once; the obliterate fix's A-1/INV1 re-verify); the green-board honest-by-construction verify; a mutation kill-rate; BUILD-GRADE/BLOCKED + Y-3.
(d) THE DIRECTOR STAGE-6 CLOSE: the LAW-4 commit(s) for the harness + the emq.testing.md reconcile + the ledger (Y-3/Z-2/D-8); then the cluster-close fold (emq.progress.md + emq.roadmap.md: emq.2.4 -> shipped, the emq.2 cluster read->ops->watch->close CLOSED, apps/echomq cleared to dissolve) + Z-2.

PIPELINE: Mars-3 (harness + emq.testing.md reconcile + re-gate, tee to the committed harness output) -> Director Stage-review (run the harness myself, verify reproducible + honest) -> Apollo-2 (Stage 4 + Y-3) -> Director Stage-6 close (LAW-4 commits + fold + Z-2; a light Venus/Director triad reconcile folded into the close since A-3 already reconciled the triad).

BOUNDARY: echo/apps/echo_mq + echo/rungs (the harness home) + docs/echo_mq ONLY. apps/echomq (frozen v1) + mix.lock UNTOUCHED. EXCLUDE all Operator out-of-band (the staged apps/echomq/guides deletion, investex/exchange/mercury/db/aob/fsharp paths, the html tree) from every commit — guarded pathspec commits only, re-verify --cached is pure boundary immediately before each commit (the concurrent-index race). Records-freeze: the design+build half (T-1..3, A-1..3, D-1..7, L-1..3, P-1..3, Y-1..2, Z-1) is byte-intact; this cycle appends T-4+, D-8+, Y-3, P-4+, Z-2.

## {emq-2-4-analysis} Analysis

### A-1 — D-V2: the emq.2 ⇄ emq.2.{1,2,3} reconcile gap table (the rationale opening of emq.2.4)

WHAT emq.2 PROMISED (emq.2.design.md): a full-parity rewrite of the v1 capability floor echo_mq lacks, carved read(2.1)→ops(2.2)→watch(2.3) over the as-built floor, under the v2 laws. ADR-2 fixed the parity/family boundary (what the cluster ships vs what emq.3–8 keep). WHAT 2.1/2.2/2.3 DELIVERED: verified as-built — 37 conformance, EchoMQ.Metrics (10 verbs)/Admin (4)+Jobs-mutations (6)/Events+Telemetry+LockManager+StalledChecker+CancellationToken+Jobs.extend_lock(s). The FEATURE parity is essentially COMPLETE for the carved floor; the residue is concentrated in (i) a few RECORDED build deferrals and (ii) the TEST DEPTH.

THE GAP TABLE — three honest categories:

== REAL RESIDUE (parity the design named, left missing/partial for the SHIPPED surface — emq.2.4 closes) ==
G1. is_maxed/rate gate NOT wired into a claim transition — shipped as a pure-read primitive only (emq.2.1 L-2(2): "wiring the gate INTO a claim transition is emq.2.2's"). emq.2.2 shipped queue-wide pause but did NOT wire the rate ceiling into @claim/@gclaim. RESIDUE: an over-ceiling claim does not auto-refuse; a claimer must consult is_maxed/2 by hand. PARTIAL — design ADR (emq.2.1 D6) named the auto-refuse; not delivered. [emq.2.4 candidate: D-feature OR explicit defer-with-reason.]
G2. metrics :data rolling series NOT written — emq.2.1 D4 [REALIZED]: only the scalar count is written; the §6 grammar registers metrics:completed[:data] but get_metrics reports data_points=0 (honest-no-phantom). RESIDUE: the rolling time-series is unbuilt (emq.2.1 routed it to emq.8 presentation). IMPROVEMENT-OPPORTUNITY/PARTIAL.
G3. de:* orphan release is bounded-complete, not swept — emq.2.2 D4/D8 [RECONCILE]: obliterate does NOT sweep de:*; remove_job/4 takes a caller-supplied dedup_id (the row stores no deid backref). RESIDUE: an orphaned de:<did> with no live referrer is not discoverable under declared-keys (honest limit, NOT a fixable gap without a backref that breaks INV1). DEFERRED-BY-DESIGN (declared-keys honest limit) — document, do not "fix".
G4. get_job_counts / get_job_count_by_types (the v1 aggregate-counts-in-one-call) — emq.2.1 ships get_counts/3 (per requested state); the v1 get_job_counts returns ALL states in one shot. RESIDUE: minor — a convenience aggregate not individually shipped. IMPROVEMENT-OPPORTUNITY (cheap additive read).

== DELIBERATE DEFERRAL (named to a later rung — NOT emq.2.4; honestly attributed) ==
G5. the worker abstraction (worker.ex pause/resume/paused?/running?/active_count, get_next_job, the processing/concurrency model) → emq.6 (design ADR-2 — Consumer is partial parity; the abstraction depth is lifecycle-controls). NOT emq.2.4.
G6. get_workers / get_workers_count / the worker REGISTRY read → emq.6 (the worker roster; emq.2.3 ships the lock plane not the registry). NOT emq.2.4.
G7. distributed cancel → emq.6 (emq.2.3 ships the local cooperative token; ADR-2). NOT emq.2.4.
G8. the telemetry CONTRACT (payload-shape assertions + the engine matrix) → emq.8 (ADR-2 two-layer split; emq.2.3 ships the surface that fires). NOT emq.2.4.
G9. export_prometheus_metrics (the Prometheus format wrapper) → emq.8 (emq.2.1 Scope Out — presentation). NOT emq.2.4.
G10. the durable replayable event stream (ids + range reads) → emq3.2 (emq.2.3 ships pub/sub subscription; ADR-4). NOT emq.2.4.
G11. flow_producer (parent/child) → emq.3. NOT emq.2.4.

== THE TEST-DEPTH RESIDUE (the Operator's question — the REAL emq.2.4 mandate) ==
The SHIPPED read/ops/watch surface is conformance-asserted (37 scenarios, +20 over emq.0) + per-verb suites (metrics 20, admin, jobs_ops, jobs_extend 9, cancel 19, meter 11, events, locks_stalled 13) — but at FAR less scenario DEPTH than v1's matching surface. v1's read/ops/watch-relevant depth: queue_getters/queue_integration/rate_limiter_integration (reads), obliterate_test + queue_integration (ops), queue_events_integration + lock_manager-in-worker + worker_cancellation 9 + the stalled paths (watch). The GENUINE under-port = v1's MULTI-JOB, CONCURRENT, EDGE-CASE depth for exactly the shipped verbs (e.g. counts under concurrent enqueue/claim/complete; drain/obliterate under a populated multi-set queue with active jobs; lock-extend racing the reaper; the stalled sweep under partial-lapse; events across reconnect under load). THIS is what emq.2.4's test suite ports — NOT the deferred-rung depth (job_scheduler 95/worker 76/OTel 44 belong to emq.1-already-shipped / emq.6 / emq.8 respectively; the 7-file stress cluster's VALUE is scenario diversity → the ≥100 determinism loop replaces it honestly, per the master-invariant gate ladder).

emq.2.4 SCOPE (two-part, from this table): (1) the REAL RESIDUE that is a genuine feature gap — G1 (rate-into-claim, IF the Operator wants it — possible fork: it edits @claim, the emq.2.2-avoided elevated risk) + G4 (get_job_counts aggregate) + optionally G2 (:data series, or hold to emq.8); G3 documented-not-fixed. (2) THE COMPLETE TEST SUITE closing the depth residue for the shipped surface, with the deferred-rung depth EXPLICITLY attributed (no padding for unshipped rungs).

ONE FORK CANDIDATE for the Operator (surface in D-V3, do not decide): G1 — does emq.2.4 WIRE the rate ceiling into the claim transition (closing the emq.2.1-named auto-refuse, but editing the shipped @claim/@gclaim — the exact elevated risk emq.2.2 chose FORM-b to avoid), or hold it as the pure-read primitive + document the consult-before-claim contract? Architecture/risk call.

### A-2 — D-V2 CORRECTION (two ledger-grounded refinements to A-1's gap table)

CORRECTION 1 — G4 is NOT a gap (RESOLVED). v2 EchoMQ.Metrics.get_counts/3 takes a LIST of states (metrics.ex: `when is_binary(queue) and is_list(states)`) and returns a count map over all requested states in one call. That IS the v1 get_job_counts(queue, types) / get_job_count_by_types capability (the batch per-type count). DROP G4 from the residue — it is COVERED. (The parity-proof table B.1 already maps queue.ex reads → get_counts/3; this confirms the batch-counts form is included.) The only nuance: v1 `count/2` (the grand-total-across-all-states single number) is get_counts/3 with the full state list summed client-side — trivial, not a gap.

CORRECTION 2 — G1 (rate-ceiling-into-claim) is a GENUINE, TWICE-RECORDED FORK, the one real feature residual on emq.2.4. The trail: emq.2.1 ledger D-5 shipped is_maxed/2 + EMQRATE as a PURE-READ primitive and EXPLICITLY flagged "whether a transition (claim) CALLS the gate is emq.2.2's operator-plane wiring (the gate is a pure-read primitive here)... Logged for Venus." emq.2.2 ledger named editing the shipped @claim as the rung's elevated risk (D1 option (a) gate-INSIDE-@claim "edits emq.1's shipped @claim → the claim conformance scenario MUST re-run byte-for-byte identical") and chose FORM-b for the queue-wide PAUSE to AVOID that edit — but emq.2.2 wired the rate gate into claim NOWHERE (grep: emq.2.2 ledger mentions rate only in passing; no rate-into-claim deliverable). NET: the over-ceiling-claim auto-refuse was NAMED by emq.2.1 (D6/US5: "refuses an over-ceiling CLAIM"), REALIZED-DOWN to a pure-read primitive (L-2(2)), routed to emq.2.2, and emq.2.2 did NOT build it. It now lands on emq.2.4 AS A FORK because closing it edits the shipped @claim/@gclaim (the exact elevated risk emq.2.2 sidestepped).

THE FORK (Operator's call — surface in D-V3, do not decide):
G1-FORK — does emq.2.4 WIRE the rate ceiling into the claim transition?
  Arm 1 (wire it): @claim/@gclaim read meta.concurrency vs ZCARD active FIRST and refuse EMQRATE (or short-circuit empty) before popping — closing the emq.2.1-named auto-refuse, the operationally complete behavior (an over-ceiling claim cannot succeed even if a caller forgets to consult is_maxed/2). COST: edits emq.1's shipped @claim + emq.2.2's claim path; the `claim`/`rate`/`limit`/`rotate` conformance scenarios MUST be re-verified byte-identical-or-updated; the named HIGH-RISK edit.
  Arm 2 (hold the primitive + document): keep is_maxed/2 as the pure-read gate a claimer consults; emq.2.4 ships NO @claim edit; the triad documents the consult-before-claim contract as the parity posture (the v1 isMaxed-2 was itself a separate read the worker called, NOT inside the claim script — so Arm 2 is arguably the FAITHFUL v1 parity, and the v1 worker.ex called isMaxed before moveToActive). COST: a forgetful caller can over-claim; the auto-refuse emq.2.1 prose named is not delivered (documented as a defer).
RECOMMENDATION TO SURFACE: Arm 2 is the cheaper AND more faithful parity (v1 isMaxed-2 is a pre-claim read, not in moveToActive-11), and it keeps emq.2.4's risk to the test suite; Arm 1 is the operationally-stronger but @claim-editing path. Venus surfaces both; the Operator rules. emq.2.4 is authored to Arm 2 (the recommended), with Arm 1 a cheap pre-build re-scope if ruled.

NET RESIDUE FOR emq.2.4 (corrected): FEATURES = G1 (the fork, authored Arm 2) + G2 (the metrics :data rolling series — IMPROVEMENT, optionally built or held to emq.8) ; G3 (de:* orphan) DOCUMENTED-not-fixed (declared-keys honest limit). The PRIMARY mandate = THE TEST SUITE closing the depth residue for the shipped read/ops/watch surface, deferred-rung depth explicitly attributed (G5–G11 → emq.3/emq.6/emq.8/emq3.2, NOT padded here).

### A-3 — Venus-2 Stage-0 lag-1 reconcile (post-emq.2.3-commit) — VERDICT: BUILD-GRADE

Re-probed every anchor against the live tree (grep/Read; line numbers were hints). Every claim is MATCH or an explicit [RECONCILE]; zero STALE/INVENTED/MISSING block. The triad is build-grade for Stage 1.

## Delta table (anchor | as-built | verdict)
1. emq.2.3 commit status — triad/runbook said "on disk, uncommitted"; git: shipped `3c6461ff` (rung) + `5a3fdd73` (docs fold). [RECONCILE] — re-pinned to committed across emq.2.4.{md,llms} + emq.2.prompt.md (lines 21-22/114/242).
2. WATCH-PLANE MODULE-NAME DRIFT (load-bearing) — triad/runbook/features.md cited v1-style `EchoMQ.{Telemetry,LockManager(+Core),StalledChecker,CancellationToken}`. As-built MODULES (grep `defmodule`): `EchoMQ.{Meter,Locks(+Locks.Core),Stalled,Cancel}` (telemetry.ex→Meter, lock_manager.ex→Locks, lock_manager/core.ex→Locks.Core, stalled_checker.ex→Stalled, cancellation_token.ex→Cancel; events.ex→Events MATCH). FILES still basename the v1 way. [RECONCILE] — every doc module citation re-pinned to the as-built names (emq.2.4.md L21/206/213/355, emq.2.4.llms.md L34/100/157/214, emq.2.prompt.md L114-118, emq.features.md L135/142/145/148). The roadmap (emq.progress.md L201) ALREADY uses `EchoMQ.Meter` — the drift was the spec layer, not the roadmap.
3. C1 carry (FILE renames) — recorded as Mars's Stage-1 carry in every touched doc: telemetry.ex→meter.ex, lock_manager.ex→locks.ex(+locks/core.ex), stalled_checker.ex→stalled.ex, cancellation_token.ex→cancel.ex. Modules ALREADY renamed in code; only filenames lag. A pure rename, no module-body change.
4. Conformance floor = 37 — VERIFIED two ways: `@run_order` pin = 37 entries; `scenarios/0` keyword list = 37 entries; both pins read `{:ok, 37}` (conformance_run_test.exs:37) + `Keyword.keys == @run_order` (conformance_scenarios_test.exs:57). MATCH — not hardcoded; the live structural count is 37.
5. C2 carry targets — CONFIRMED shipped + untested-at-depth: `Jobs.extend_locks/4` (jobs.ex:671) returns `{:ok, failed}` = the un-extendable [live,stale,gone] partial-batch (the Lua decides which re-score, returns failures). `@sweep_stalled` (stalled_checker.ex:50) has a real GROUP-AWARE branch — `if g then ... lane = p..'g:'..g..':pending' ... RPUSH ring/LPUSH wake` — distinct from flat `ZADD KEYS[2]` recovery; the D7 sweep depth must drive BOTH branches. Both declare their KEYS[] + read server TIME (S-6/S-7 clean).
6. Shipped read/ops/watch surface — all verbs re-pinned MATCH: Metrics 10 verbs (metrics.ex:54-260, is_maxed arity-2), Admin pause/resume/drain/obliterate (admin.ex), Jobs mutations update_data/progress/add_log/get_job_logs/remove_job/reprocess_job + extend_lock/5 + extend_locks/4 (jobs.ex), watch surface Events/Meter/Stalled/Cancel/Locks verbs all present.
7. v1 reference-depth files — all 7 EXIST (echo/apps/echomq/test/echomq/{queue_getters,queue_integration,rate_limiter_integration,obliterate,queue_events_integration,worker_cancellation,worker_integration}_test.exs). The EXCLUDED files (worker/opentelemetry/flow_producer/stress/high_concurrency/redis_baseline) EXIST → D8 attribution is real and grounded. NOTE: `job_scheduler_test.exs` is ABSENT (scheduler depth lives under a different v1 filename) — D8's "scheduler → emq.1" attribution stays honest (capability is emq.1's, shipped+tested there) but must not imply a file to skip.
8. is_maxed/2 return shape [RECONCILE] — stories.md:42-43 said "answers `false` below". As-built returns `:ok` below / `{:error,:rate}` at-or-above (NOT boolean). Re-pinned US2 acceptance to the real shape so the depth-suite author asserts `:ok`/`{:error,:rate}`.
9. get_metrics/3 :data read — CONFIRMED already `LLEN`s `metrics:<which>:data` and reports `data_points` honestly (0 today). So a (hypothetical) D3 build would be WRITE-side only; read side needs no change. Reinforces the §6 key is a LIST → LPUSH+LTRIM the right primitive (not HINCRBY) — recorded in the D3 emq.8-alternative note.

## Forks confirmed (both RULED — recorded, not re-litigated)
- G1 (rate-into-claim) = Arm 2 (Operator, ledger D-3, 2026-06-14): hold `is_maxed/2` as the consult-before-claim pure-read; NO @claim/@gclaim edit; document the contract. The "RESOLVED: Arm 2" banner is in emq.2.4.md §The surfaced fork; D2 builds Arm 2 directly; the original surfacing kept below as the decision record.
- G2 (metrics :data series) = HOLD to emq.8 (Operator, ledger D-4, 2026-06-14, at build launch). The triad had D3 as an OPEN "build OR hold" choice — [RECONCILE] re-pinned across the body (gap-table G2, D3, Scope-In, What, DoD), stories US3, and the brief (R3, AS6, the Stage-4 prompt, the touched-files set) to the RULED documented hold: emq.2.4 writes NO :data series, @complete/@retry byte-unchanged on the series side, data_points honest-0, the build-path kept as the recorded emq.8 alternative.
- G3 (de: orphan) = documented, not fixed (declared-keys honest limit) — D4 unchanged, MATCH.

## Consequence of both rulings (the carve sharpened)
With Arm 2 + the D-4 hold, there is NO @claim/@gclaim edit and NO :data series write. The only lib/ MUTATIONS are the C1 file renames + the conformance scenarios; everything else is NEW test files. The touched-files set was re-pinned to reflect this (jobs.ex/metrics.ex moved to NOT-EDITED, the C1 renames + a rate_consult_test.exs NEW added). This is the clean, low-risk boundary the Director reviews against.

## The as-built floor Stage-1 (Mars-1) builds on
- Shipped read plane: EchoMQ.Metrics 10 verbs (metrics.ex). Ops plane: EchoMQ.Admin + 6 Jobs mutations. Watch plane: EchoMQ.{Events,Meter,Stalled,Cancel,Locks(+Core)} + Jobs.extend_lock/5 + extend_locks/4 — committed `3c6461ff`.
- Conformance floor = 37 (both pins); the depth suites register additive-minor → 37→N, prior 37 byte-unchanged.
- INV2/INV7/INV8 read correctly for the ruled arms (INV8 fork-gate satisfied at spec-time by the Arm-2 ruling; INV2 false-green forbid intact; INV7 ≥100 loop binds the D7 process-touching suites).

## Escalations
NONE. No spec⇄as-built contradiction beyond the known deltas; no depth scenario drives an unshipped verb; conformance count = 37 confirmed. Build-grade.

## Triad files edited (one line each)
- emq.2.4.md — re-pin emq.2.3→committed; watch modules→Meter/Locks/Stalled/Cancel; is_maxed shape; D3 ruled-hold (gap-table/D3/Scope/What/DoD); C1-carry [RECONCILE] note on the as-built-floor line.
- emq.2.4.stories.md — US2 is_maxed `:ok`/`{:error,:rate}` shape; US3 re-titled + re-derived to the ruled D-4 hold.
- emq.2.4.llms.md — watch modules→as-built names in References/topology/AS5/Stage-0 prompt; D3 ruled-hold in R3/AS6/Stage-4; touched-files set re-pinned (C1 renames in, jobs.ex/metrics.ex out).
- emq.2.prompt.md (runbook) — emq.2.3→shipped 3c6461ff; watch surface block→as-built module names; the commit-pathspec note re-pinned (emq.2.3 already committed).
- emq.features.md (parity proof) — the four watch-plane module citations→Meter/Locks/Stalled/Cancel.

## {emq-2-4-report} Report

### Y-1 — Venus-1 final report: the emq.2.4 design cycle (D-V1 + D-V2 + D-V3 all delivered)

THREE DELIVERABLES ON DISK, all ground-probed against the as-built tree (T-2), the gap derived + corrected (A-1/A-2):

D-V1 — docs/echo_mq/emq.features.md (343 lines, NEW canon-level peer to emq.design.md/emq.roadmap.md). (a) the complete EchoMQ 3.0 feature set by Movement (M0 substrate ✅ · M-I core ✅+🔨 · M-II emq.4–8 📋 · 3.x stream tier 🔭 · unslotted proposals) with use cases for consumers/systems + the Exchange exemplar (real TRD traces). (b) the v1→v2 full-parity-proof table: B.1 = 26 lib rows, B.2 = 25 script rows, B.3 = the verdict. Every v1 capability ✅/🔨/📋/—. Voice tracks status. Framing clean (0 legacy/old).

D-V2 — the emq.2 ⇄ emq.2.{1,2,3} reconcile gap table (ledger A-1/A-2; opens emq.2.4.md §0). 11 gap rows in 3 honest categories: REAL RESIDUE (G1 rate-into-claim PARTIAL=the fork, G2 metrics :data IMPROVEMENT, G3 de: orphan DOCUMENTED-not-fixed), DELIBERATE DEFERRAL (G5–G11 → emq.6/emq.8/emq.3/emq3.2, NOT padded), THE TEST-DEPTH RESIDUE (the primary mandate). G4 corrected to NOT-a-gap (get_counts/3 takes a state list = the v1 batch). The PRIMARY finding: the FEATURE parity for the carved floor is essentially complete; the residue is the TEST DEPTH for the shipped read/ops/watch surface.

D-V3 — the emq.2.4 triad: emq.2.4.md (351L, spec body) + emq.2.4.stories.md (195L, 9 stories incl. US-GATE) + emq.2.4.llms.md (276L, the Mars brief). Two-part scope: (1) the feature residue (rate-gate to the ruled arm, optional :data series, de: documented) + (2) THE COMPLETE TEST SUITE closing the depth gap for the SHIPPED surface (D5 metrics_depth, D6 admin_depth, D7 watch_depth under the ≥100 loop) with the un-ported v1 depth EXPLICITLY ATTRIBUTED (D8: worker→emq.6, OTel→emq.8, distributed-cancel→emq.6, flow→emq.3, scheduler→emq.1-shipped, stress-files→the ≥100 loop). INV2 = the honesty gate: no test drives an unshipped surface (a false-green forbidden). 37→N conformance re-pin, 37 prior byte-unchanged. HIGH-RISK → Apollo MANDATORY.

QUALITY GATES (all 6 PASS): Voice 0-hype; Structure (6 sections + 5W + DoD + Coverage + INVEST-encodes); Traceability (D1-8→Coverage, R1-10→[US:], AS1-8→[implements], INV1-8→encodes — all complete); Fences even; Links all resolve (4 files); Format Writerside-clean.

THE ONE FORK SURFACED (Operator's call, NOT decided — emq.2.4.md "The surfaced fork"): G1 rate-ceiling-into-claim. Arm 1 = wire @claim/@gclaim (operationally complete, edits the SHIPPED script = the risk emq.2.2 avoided). Arm 2 (RECOMMENDED) = hold the pure-read primitive + document consult-before-claim (cheaper, lower-risk, AND more faithful — v1 isMaxed-2 is a pre-claim read, not inside moveToActive-11). Triad authored to Arm 2; an Arm-1 ruling is a cheap pre-build re-scope of D2. Nothing in D2 builds until ruled.

ONE FLAG (surface-map drift, not a triad edit): .claude/skills/echo-mq-program.md:77 says "the set is 18" — stale; live is 37.

VERDICT: BUILD-GRADE. The three docs are internally consistent + ground-true; the emq.2.4 triad is ready for the cluster runbook the Director consolidates. The emq.2 cluster's closing rung is specced.

### Y-2 — Director final report: the emq.2.4 design cycle COMPLETE

DELIVERED (all build-grade, ground-true, boundary-clean — verified by Director ground-truth inspection, not the peer's self-verdict):
1. docs/echo_mq/emq.features.md (NEW, 343L, canon-level) — Part A: the complete EchoMQ 3.0 feature set by Movement (0 substrate · I core incl. the emq.2 cluster · II extension · the 3.x stream tier · the unslotted proposals) with consumer/system use cases + the Exchange exemplar (real TRD traces). Part B: the v1→v2 full-parity-proof table — B.1 (26 lib rows) + B.2 (26 Lua-script rows) + B.3 (the verdict); every v1 capability → its v2 home → which emq.x → status; migration.ex the one ruled-out row (ADR-0). The Operator's "which emq.x?" answered.
2. the emq.2 ⇄ emq.2.{1,2,3} reconcile (ledger A-1/A-2; opens emq.2.4.md §0) — an 11-row gap table in 3 honest categories. REAL RESIDUE = G1 (the rate-gate fork) + G2 (metrics :data) + G3 (de: orphan documented); G4 self-corrected to NOT-a-gap (get_counts/3 takes a state list); G5-G11 DELIBERATE DEFERRAL attributed to emq.3/6/8/3.2 (NOT padded). PRIMARY finding: feature parity for the carved floor is essentially complete; the residue is the TEST DEPTH (the Operator's flag).
3. the emq.2.4 triad (emq.2.4.md 351L + .stories.md 9 stories + .llms.md the Mars brief) — the final parity-closing stage: the feature residue + the complete test suite porting v1's scenario DEPTH for the SHIPPED read/ops/watch surface. INV2 = the honesty gate (no test drives an unshipped surface — a false-green forbidden); the un-ported depth explicitly attributed (worker→emq.6, OTel→emq.8, flow→emq.3, scheduler→emq.1, stress→the ≥100 loop). HIGH-RISK → Apollo MANDATORY at build.
4. docs/echo_mq/specs/emq.2.prompt.md CONSOLIDATED — the retired migration runbook replaced by the LIVE cluster-closing orchestration runbook: the cluster context, the high-risk pipeline (Venus→Mars-1→Director→Mars-2→Apollo→Director), the as-built floor anchors, the G1 fork as the ONE open seam (default Arm 2), the Stage-6 build pathspec (excludes Operator out-of-band; emq.2.3 commits under its own pathspec), the cluster-close acceptance.
5. SOLO PROGRESS UPDATE — emq.progress.md + emq.roadmap.md folded: emq.2.3 → 🔨 IN FLIGHT (built, uncommitted); emq.2.4 → 📐 SPECCED; the cluster reframed read→ops→watch→close (3/4 built); features.md referenced. The test gap quantified (v1 echomq 534 tests/41 files/195 describes vs v2 echo_mq 201/28/36) in T-1/T-2 + the dashboard.

THE ONE OPEN OPERATOR SEAM (surfaced, never decided — D-2): G1 the rate-gate fork. Arm 2 (RECOMMENDED — hold the pure-read primitive + document consult-before-claim; the faithful v1 parity; lower risk) vs Arm 1 (wire @claim/@gclaim; operationally complete; edits emq.1's shipped script). Carried into the runbook (default Arm 2); ruled at the emq.2.4 build launch or by the Operator now.

PROCESS: a fresh emq-2-4 team (Director + Venus-1; design phase — no Mars build, no Apollo, per the aaw D-7 lock); the stale emq-2-3 team stood down (Mars-1 shut down; no active TeamCreate context remained). Venus-1's three deliverables verified by reading every artifact + re-probing the boundary (D-1 ACCEPT). One out-of-charter touch (a stub on the empty course file) reverted (L-1). The frozen emq-2-1/2-2/2-3 ledgers uncontaminated (records-freeze). ZERO echo_mq code change (design phase). NO commit (design docs — the touch-set surfaced for the Operator).

FLAG (a small follow-up, under an Operator grant): .claude/skills/echo-mq-program.md:77 says "as-built today the set is 18" — stale; live conformance is 37.

VERDICT: the emq.2 parity cluster's closing rung (emq.2.4) is SPECCED, the v1→v2 parity is PROVEN (emq.features.md Part B), and the cluster runbook is LIVE. emq.2.4 is ready to build (pending the G1 ruling).

### Y-3 — Apollo-2 Stage-4 (MANDATORY, the crash-interrupted gate, re-run to completion): VERDICT = BUILD-GRADE

The Stage-4 gate the prior cycle's crash cut short BEFORE Apollo-1 filed Y-3 is now run to completion, adversarially, owning the machine. Every mandate item discharged; no blocking delta; no new defect. emq.2.4 (the EchoMQ parity-closer) is BUILD-GRADE.

== 1 · THE HARNESS FULL RE-RUN (independent, LOOP_N=100, owning the machine) ==
`cd echo && bash rungs/bus/emq_2_4_check.sh` → PASS 7/7, board byte-matches the committed emq_2_4_check.out AND emq.testing.md §0 (honest-by-construction confirmed — the doc asserts only what the harness observes):
- toolchain ok (erlang 28.5.0.1 re-probed from apps/echo_mq; Valkey :6390 PONG)
- compile ok (--warnings-as-errors clean)
- suite ok ([4 doctests, 250 tests, 0 failures]; pure 182-excl / wire 72-excl all 0 failures)
- conformance ok (Conformance.run/2 → {:ok, 43})
- determinism ok (THE ≥100 LOOP = 100/100 GREEN over watch_depth+admin_depth+conformance_run — the full uncontended loop, the determinism proof the crash had evaporated, now a third independent full-100 behind the board)
- leak ok (dbsize FLAT across the loop; absolute floats, only the delta asserted)
- seed-sweep ok (5/5 over the synchronous read/ops suites, seeds 0 1 42 312540 999999)

== 2 · THE ADVERSARIAL FINDINGS TABLE (try-to-break, not bless) ==
| Probe | Verdict | Evidence |
|---|---|---|
| @obliterate fix (the shipped-script edit, highest risk) | CLEAN | declared-keys-clean (lane key + row key both off the declared base root KEYS[2]; NO SCAN, NO backref); budget-sound (the `if budget<=0 return 1` guard stops the next group's ZRANGE-0-budget-1 degenerating to ZRANGE 0 -1; the `ZCARD>0 → return 1` :more leaves a partly-drained lane intact); slot-sound under {q}; the stricter lane-loop bound vs the closed flat sets is correct-by-design (lanes are an OPEN family) |
| Declared-keys / F-1 on the arc's Lua | CLEAN | the ONLY Lua wire change in the whole emq.2.4 arc is @obliterate (92a8f042). The closer 3298e4bc lib touch = conformance.ex (M, additive) + 5 renames (R100). NO @claim/@gclaim edit (G1=Arm-2 held), NO :data write (G2=HOLD held), NO new Script.new in any lib or test file |
| INV1 — prior 37 conformance byte-unchanged | CLEAN | git show 3298e4bc -- conformance.ex: removed lines are ONLY moduledoc prose (37→43 narrative) + the cancel: contract string gaining a trailing comma (body byte-identical); ZERO prior apply_scenario probe body changed; the 6 new (unknown_state/rate_consult/dedup_release/extend_locks_batch/stalled_group/obliterate_grouped) are pure additions; both pins re-pinned to 43 (run_test:40 {:ok,43}; scenarios_test @run_order = 43 entries) |
| INV2 — the honesty gate (no false-green) | PASS | read ALL 5 depth suites (43 tests) + ALL 6 new conformance probes: every assertion drives a SHIPPED read/ops/watch verb. NO worker-abstraction, NO OTel payload-CONTRACT matrix (Meter test asserts only "the surface fires", guards on :telemetry being optional), NO distributed-cancel (Cancel asserts only the LOCAL token), NO flow, NO durable-stream range-read (Events asserts pub/sub re-delivery via the resubscribe MapSet, not a replayable stream). Deferred depth attributed in every moduledoc (worker→emq.6, OTel→emq.8, dist-cancel→emq.6, stream→emq3.2). The dedup orphan is asserted to SURVIVE obliterate (the honest limit made a passing regression-protected fact, not hidden) |
| C1 renames byte-identical | CLEAN | all 5 old-HEAD-body vs new-body diffs EMPTY (telemetry→meter, stalled_checker→stalled, cancellation_token→cancel, lock_manager→locks, lock_manager/core→locks/core); R100 in the rename-aware name-status — a pure inert move |
| Order theorem (byte=mint) / at-most-once | CLEAN | metrics_depth concurrent-enqueue mints 12 ids on 12 conns → length(uniq)==12 (no same-ms collision survives, corroborated by the 100/100 loop); the @extend_locks `att==token` fence is the at-most-once guard (proven by mutation #2); mint-order discipline (enqueue-oldest-first, claim ZPOPMIN) threads every multi-job test |

== 3 · THE MUTATION KILL-RATE = 3/3 (8 tests killed) — see L-4 ==
(1) @obliterate del_job loop removed → 3 RED (obliterate_grouped conf + 2 admin_depth); (2) @extend_locks fence att==token→att → 3 RED (extend_locks_batch conf + 2 watch_depth, the partial-batch test naming the exact wrongly-succeeding id); (3) @sweep_stalled grouped recover→flat KEYS[2] → 2 RED (stalled_group conf + watch_depth, the discriminating ZSCORE-lane assertion). Each clean single-job/flat counterpart stayed GREEN — the GROUPED/MULTI-token DEPTH is what discriminates. All reverted via inverse-Edit; git diff CLEAN after each; final whole-tree-clean verified.

== 4 · NEW DEFECT / SHIPPABILITY FLAG ==
NONE that blocks — BUILD-GRADE. One non-blocking OBSERVATION about the INHERITED surface (NOT an emq.2.4 finding, NOT a decision for me): the shipped @extend_locks (jobs.ex, emq.2.3 C2 surface) derives its per-job row key `jk` from ARGV[1] (base), not from a KEYS[n] root. Read strictly against S-6 that is an ARGV-rooted key; in practice it is slot-sound (ARGV[1]=queue_key carries the same {q} as KEYS[1]) and is the established pre-existing Jobs.* convention for dynamic job-id sets, NOT something emq.2.4 introduced (the closer added zero Lua here; the only NEW Lua, @obliterate, IS rooted in the declared KEYS[2]). I FLAG it for the Director/Operator as a possible program-wide A-1-wording reconcile target for a LATER rung — I do NOT block emq.2.4 on it and I do NOT decide it.

== 5 · THE VERDICT ==
BUILD-GRADE. The QA "detected bugs not fixed" concern is closed three ways: the one real defect the depth mandate found (L-2 obliterate leak) is FIXED + committed (92a8f042) + proven by the obliterate_grouped scenario + the flipped admin_depth test (mutation #1 confirms the proof has teeth); the determinism proof is now a COMMITTED re-runnable harness (re-run to 100/100 here, the structural fix for "ephemeral proof != a harness"); and the adversarial bug-hunt the crash cut short found NO other defect across the order-theorem/declared-keys/at-most-once/honesty surfaces. The green-board is honest-by-construction and reproduced. Records-freeze held (T-1..3/A-1..3/D-1..7/L-1..3/P-1..4/Y-1..2/Z-1 byte-intact; appended Y-3 + L-4). Apollo-2 ran NO git commits.

SPEC-SYNC: none owed — A-3 already reconciled the triad to the as-built committed surface; the harness + emq.testing.md §0 are the rung's testing record (the Director's Stage-2 P-4 verified them). Released to the Director for Stage-6 close (the LAW-4 commits per the P-3 carve: COMMIT 1 = the harness + emq.testing.md §0 close-time fix; the lib/test/conformance closer is ALREADY committed as 3298e4bc + 92a8f042 — no re-commit of code).

## {emq-2-4-decisions} Decisions

### D-1 — Director review of Venus-1's three deliverables: ACCEPTED, build-grade

Reviewed against ground truth (T-1/T-2), the as-built tree, and the design canon. VERDICT: build-grade, ground-true, boundary-clean.

(a) docs/echo_mq/emq.features.md (343L) — the 3.0 feature catalog + the v1→v2 parity-proof table. Part A: the complete surface by Movement with consumer/system use cases + the Exchange exemplar (real TRD traces). Part B: B.1 (26 lib rows) + B.2 (26 Lua-script rows) + B.3 (the verdict) — every v1 capability accounted for (✅ rewritten / 🔨 building / 📋 scheduled-at-a-named-rung / — ruled-out), migration.ex correctly the one ruled-out row (drain-precondition, ADR-0). Module citations spot-verified real. Voice tracks status. This is the Operator's "which emq.x?" answer.

(b) the emq.2 ⇄ emq.2.{1,2,3} reconcile gap table (ledger A-1/A-2; opens emq.2.4.md §0) — 11 rows, 3 honest categories (REAL RESIDUE G1-G3; G4 self-corrected to not-a-gap; DELIBERATE DEFERRAL G5-G11 attributed to emq.3/6/8/3.2, NOT padded; the TEST-DEPTH residue the primary mandate). The two A-2 self-corrections are grounded in the prior rungs' ledgers — high-rigor architect work.

(c) the emq.2.4 triad (emq.2.4.md 351L + .stories.md 195L + .llms.md 276L) — the final parity-closing stage. §0 gap table → Goal/5W → Scope (In = the residue + the depth suite; Out = the deferred-rung depth explicitly excluded) → D1-D8 → INV1-8 → the surfaced fork → DoD. The load-bearing invariant is INV2 (the honesty gate: no test drives an unshipped surface — a false-green forbidden), which makes "complete test suite" mean "port v1 DEPTH for the SHIPPED read/ops/watch surface + attribute the rest," not pad. Matches the emq.2.1/2.2/2.3 triad shape; specs.approach.md 6 gates met.

BOUNDARY: zero echo_mq code change (design phase, docs only). Venus's one out-of-charter touch — a stub on the empty docs/echo_mq/course/echo_mq.course.md — REVERTED by the Director (out of the spec-only charter; the course is the echo-mq-expert's domain). The frozen emq-2-1/2-2/2-3 ledgers UNCONTAMINATED (records-freeze held). emq.progress.md carries only the Director's two solo-update edits. apps/echomq + mix.lock + the Exchange tree untouched.

### D-2 — the G1 rate-gate fork: carried into the consolidated emq.2.prompt.md runbook as the ONE open Operator seam (default Arm 2, recommended)

Venus surfaced (never decided) the one genuine open feature decision the cluster left: emq.2.1 named "the rate ceiling auto-refuses an over-ceiling claim" (D6/US5) but shipped is_maxed/2 as a PURE-READ primitive and routed the transition-side wiring to emq.2.2; emq.2.2 chose FORM-b for pause PRECISELY to avoid editing the shipped @claim and did not wire the gate — so the auto-refuse is unbuilt and closing it lands on emq.2.4 (editing the shipped @claim/@gclaim).

THE FORK: Arm 1 (wire @claim/@gclaim ceiling check — operationally complete but edits emq.1's shipped script; the named HIGH-RISK; conformance claim/rate/limit/rotate re-verified byte-identical-or-updated) vs Arm 2 (hold is_maxed/2 as the consult-before-claim pure-read; NO @claim edit; ALSO the more faithful v1 parity — v1 isMaxed-2 is a pre-claim read the worker calls, not a step inside moveToActive-11). Venus RECOMMENDS Arm 2 (cheaper + lower-risk + more faithful); the triad is authored to Arm 2; an Arm-1 ruling is a cheap pre-build re-scope of D2.

DIRECTOR DECISION: carry the fork into the consolidated emq.2.prompt.md as the ONE open Operator seam (the emq.2.prompt.md "settled forks + the ONE open seam" pattern), default Arm 2 — ruled at the emq.2.4 BUILD launch (or now, if the Operator rules it). The cluster runbook drives a FUTURE build cycle; the fork is a build-launch gate, not a design-cycle blocker. Surfaced to the Operator at this cycle's close (AskUserQuestion). Concurs with Venus's recommendation: Arm 2 is both lower-risk and the faithful v1 parity.

### D-3 — Operator ruling: the G1 rate-gate fork = Arm 2 (2026-06-14)

The Operator ruled the one open seam (the G1 rate-ceiling-into-claim wiring, surfaced D-2) = ARM 2 (the recommended). emq.2.4 holds Metrics.is_maxed/2 as the consult-before-claim pure-read primitive (unchanged), documents the consult-before-claim contract, and ships NO @claim/@gclaim edit — the faithful v1 parity (v1 isMaxed-2 is a pre-claim read the worker calls, not a step inside moveToActive-11) and the lower-risk close (the whole emq.2.4 risk surface stays in the test suite).

CONSEQUENCE: the triad is already authored to Arm 2, so NO pre-build re-scope is needed; EMQ.2.4-D1's fork-settlement gate is satisfied at SPEC time (not deferred to the build launch), and the emq.2.4 build proceeds to Arm 2 directly without a launch-time fork stop. The @claim-edit risk dimension is OFF (Arm 2 touches no claim path); lanes.ex/@gclaim drops from the Stage-6 commit pathspec. emq.2.4 remains HIGH-RISK on the process/mint dimension alone (the watch depth suites + the ≥100 loop), so Apollo stays MANDATORY.

RECORDED into: emq.2.prompt.md (the "Settled forks" heading + the OPEN→RULED banner + the resolution + the risk tier (b) N/A + the pathspec lanes.ex excluded) and emq.2.4.md (the fork section → RESOLVED Arm 2 banner). Venus recommended Arm 2 (ledger A-2); the Director concurred (D-2); the Operator ruled (D-3).

### D-4 — Operator ruling: G2 (metrics :data rolling series) = HOLD to emq.8 (2026-06-14)

The one open build-shaping decision the triad reserved for the Operator (EMQ.2.4-D3: "the build does not silently choose… the pre-build reconcile fixes it from the Operator's call") is RULED at the build launch (AskUserQuestion, recommended option taken): emq.2.4 HOLDS the metrics :data rolling time-series to emq.8.

CONSEQUENCE: EMQ.2.4-D3 records the documented hold — get_metrics/3 keeps reporting data_points = 0 (honest-no-phantom, unchanged from emq.2.1 D4 [REALIZED]); emq.2.4 ships NO @complete/@retry :data write and NO new declared-keys Lua surface. The rolling series + its presentation contract land at emq.8 (where the Prometheus/OTel presentation lives — matching emq.2.1's own L-2(1) routing). This keeps the parity closer's WHOLE risk surface in the test suite, consistent with the Arm-2 (D-3) spirit: emq.2.4 = the Arm-2 consult-before-claim contract doc + the D5/D6/D7 depth suites + the C1/C2 carries + the de: documentation, and nothing that adds a new wire-write to the closing rung.

WHY (reasoning surfaced to the Operator): (a) lower-risk — no new Lua, no new INV1/A-1 surface for Apollo to re-verify on a rung whose mandate is test depth; (b) faithful to the established routing — emq.2.1 already sent the rolling series to emq.8's presentation contract; (c) a clean close — the closer ships no half-built presentation feature ahead of its contract rung. The alternative (build it now: a declared-keys LPUSH+LTRIM on @complete/@retry + real data_points + a grow/trim :valkey test + 1 additive conformance scenario) was the steelman; the Operator chose the hold.

RECORDED into: emq.2.4.md EMQ.2.4-D3 (→ HOLD banner) + emq.2.prompt.md ("the recorded hold to emq.8 — D3 fixes which" → fixed: HOLD) at Venus-2's Stage-0/Stage-5 reconcile. Forks now ALL settled: G1=Arm 2 (D-3), G2=HOLD (D-4), G3=documented.

### D-5 — Operator ruling: the obliterate grouped-row leak (L-2 finding) = FIX NOW, separate scoped commit (2026-06-14)

The Director verified the INV3 finding against ground truth (the @obliterate script + emq.2.2.md EMQ.2.2-D4 + the probe) and escalated the fix-vs-defer architecture/risk decision (it edits a SHIPPED emq.2.2 script — the risk dimension the Arm-2 ruling D-3 steered emq.2.4 away from). The Operator ruled (AskUserQuestion): FIX NOW, in this ship, as a SEPARATE scoped commit.

CONSEQUENCE: the @obliterate fix lands this ship — inside the existing groups loop, before DELing `g:<g>:pending`, ZRANGE its members and del_job each (the row key derives from the declared base root `base..'job:'..id` — slot-sound, A-1-clean), budget-accounted (obliterate is :more/:ok bounded). The `obliterate` conformance scenario is re-verified byte-identical-or-updated (INV1). The D6 admin_depth `@tag :finding` test FLIPS from asserting the as-built leak to asserting the FIXED behavior (grouped rows cleared). Committed as its OWN `[emq] emq.2.2 fix — obliterate clears grouped job rows` pathspec (admin.ex + conformance + the flipped test), SEPARATE from the emq.2.4 closer commit (LAW-4 faithfulness — the fix is emq.2.2's surface, not the closer's). Apollo (already MANDATORY) re-verifies the shipped-script edit (the order theorem + INV1 byte-unchanged on the 37 + the declared-keys A-1 lint on the changed @obliterate). The emq.2 cluster closes with the floor CORRECT, not documented-broken — the strongest "proven-at-depth, apps/echomq can dissolve" close. The fix is routed to Mars-2 (the refine/polish pass), NOT Mars-1.

### D-6 — Operator directive: the Mars-2 restructure (fresh Opus effort-max polish pass) + the emq.testing.md read/write GRANT (2026-06-14)

The Operator restructured the Stage-1→3 flow and granted a new artifact:

(1) FRESH MARS-2 (not a resume). Let Mars-1 FINISH its current build pass, then SHUT IT DOWN (SendMessage shutdown_request); spawn a FRESH Mars-2 — model Opus, effort max (the Operator notes this is the default for team agents) — with INDEPENDENT context but the .prompt.md runbook (emq.2.prompt.md) + the spec triad EMBEDDED in its spawn prompt, to "refine and polish." This departs from the usual resume-Mars-1-as-Mars-2 (one identity, two passes): a fresh independent builder re-derives correctness from the spec, not from its own Stage-1 reasoning — a stronger verification posture (the adversarial-verify logic applied to the harden pass). Mars-2's charter: apply the D-5 obliterate fix; remediate the Director's Stage-2 review REMEDIATE list; harden; run the FULL gate ladder (per-app compile --warnings-as-errors; per-app pure + :valkey + process suites; Conformance.run/2 with the 37 prior byte-unchanged + the new ones probe-registered; the ≥100 determinism loop over the process-touching depth suites D7; the multi-seed sweep for D5/D6); author the testing green-board (below).

(2) THE emq.testing.md GRANT (Operator-approved, explicit read/write). A new program-doc-family file docs/echo_mq/emq.testing.md — the gate-ladder GREEN-BOARD that verifies, for this rung: every suite green (pure + :valkey + process, per-app counts), the conformance count 37→N with the 37 prior byte-unchanged, the ≥100 determinism-loop result (PASS/FAIL count, dbsize-flat leak check), the honest-row (Valkey 6390) posture, AND the roadmap-clearance for emq.2.4 (its dependencies all shipped — read 2.1 / ops 2.2 / watch 2.3 — and the cluster-close has no blocker). HONEST-BY-CONSTRUCTION: Mars-2 writes ONLY the gate output it actually observed (tee'd from the real run); Apollo INDEPENDENTLY re-runs and confirms the board matches reality before it is committed (verify the artifact, not the report); the Director spot-checks. Committed with the emq.2.4 closer (the rung's testing record). The Operator grant sanctions read/write by Mars-2 + Apollo + the Director without further asking.

PIPELINE (revised): Mars-1 finishes → Director Stage-2 solo review (the REMEDIATE list, incl. the D-5 obliterate fix as item 1) → shut down Mars-1 → fresh Mars-2 (Opus effort-max, independent+embedded) refines/polishes/hardens + authors emq.testing.md → Apollo Stage-4 (MANDATORY, independent re-run + green-board verify) → Venus-2 Stage-5 reconcile → Director Stage-6 close: TWO scoped commits (the emq.2.2 obliterate fix + the emq.2.4 closer) + the cluster-close fold.

### D-7 — Mars-2: NOT running `mix format` on the touched files (the long-line conformance-registry style is the COMMITTED convention)

Mars-2's `mix format --check-formatted` over the touched files reports drift in `conformance.ex` + `admin_depth_test.exs`, but the drift is overwhelmingly in PRE-EXISTING code (the existing scenario-contract strings + `apply_scenario` command bodies), not Mars-2's additions. Verified two ways: (1) the COMMITTED HEAD `conformance.ex` (the emq.2.3 tree) is itself not `mix format`-clean — the long-line, one-scenario-per-line registry style is the deliberate committed convention; (2) the emq gate ladder (the program skill + echo/CLAUDE.md §3) does NOT include `mix format --check-formatted` — the gates are compile-warnings-as-errors + per-app test + Conformance.run/2 + the ≥100 loop; `mix format` is listed as AVAILABLE, not mandatory.

DECISION: do NOT run `mix format`. Running it would reflow ~100 lines of pre-existing code outside Mars-2's scope AND visually churn the prior conformance scenarios INV1 requires byte-stable — the opposite of keeping the blast radius reviewable. Mars-2's own additions MATCH the surrounding convention: the `obliterate_grouped:` contract line is 130 chars, sitting between its neighbors `dedup_release` (135) and `stalled_group` (109) — one-line-per-scenario, consistent with the registry. INV1 re-verified independent of formatter cosmetics: the git diff vs HEAD shows all 6 added `apply_scenario` clauses as pure additions (+), ZERO prior clauses removed/changed; the only Mars-2 clause is `obliterate_grouped`. The compile gate is `--warnings-as-errors` clean; the suite is green. If the Director wants the file `mix format`-normalized, that is a SEPARATE program-wide style pass (it would touch the prior committed scenarios), not part of this rung's obliterate-fix scope.

### D-8 — emq.2.4 ship-close: BUILD-GRADE ratified; the harness + honest green-board committed; the emq.2 cluster CLOSED

Apollo-2 rendered BUILD-GRADE (Y-3): the full-100 harness re-run PASS 7/7 (board byte-matches the committed .out + emq.testing.md §0 — honest-by-construction), the adversarial table all-CLEAN (@obliterate declared-keys + budget sound; INV1 the 37 prior byte-unchanged; INV2 no false-green; C1 byte-identical; order-theorem/at-most-once), the mutation kill-rate 3/3 (8 tests killed), all reverted clean (inverse-Edit, the L-3 protocol). The Director independently corroborated: the resume gate re-run (250/0 --include valkey, Conformance {:ok,43}) + P-4 (ran the harness myself, PASS 7/7, LOOP_N=10 smoke). The echo_mq tree is clean (no leftover mutation).

DECISION (the LAW-4 ratify): emq.2.4 SHIPS. The arc is THREE commits — the obliterate fix 92a8f042 (prior session), the closer code 3298e4bc (this resume), and THIS close commit (the committed harness echo/rungs/bus/emq_2_4_check.{sh,out} + the honest-in-fact emq.testing.md §0 + the cluster-close fold emq.progress.md/emq.roadmap.md + the ledger). The Operator's 7c2f2405 carried the triad/features/ledger DOCS; the close commit carries the PROOF (the harness) the Operator's "Harness required" demanded + the status fold. With emq.2.4 the emq.2 parity cluster CLOSES (read 2.1 7d98ef86 / ops 2.2 76fc947c / watch 2.3 3c6461ff / close 2.4 — all shipped, proven at depth), so apps/echomq (frozen v1 1.3.0) is clear to dissolve on Operator-owned timing.

THE QA TRIAD CLOSED: (1) commits-not-made → the closer code 3298e4bc + this close commit landed (guarded pathspec, excluding all Operator out-of-band); (2) emq.testing.md inconsistent → reconciled honest-in-fact + reproducible-via-the-committed-harness (Mars-3, Director-verified P-4); (3) detected-bugs-not-fixed-due-to-crash → the obliterate leak fixed+committed+proven (mutation #1 teeth), and Apollo-2's adversarial bug-hunt found NO other defect across order-theorem/declared-keys/at-most-once/honesty. The structural fix for the crash class: ephemeral /tmp proof → a COMMITTED re-runnable harness (a crash can no longer evaporate the green-board).

CARRIED (non-blocking, Apollo Y-3 §4): the inherited @extend_locks (jobs.ex, the emq.2.3 C2 surface) derives its per-job row key `jk` from ARGV[1], not a KEYS[n] root — slot-sound in practice (ARGV[1]=queue_key carries the same {q} as KEYS[1]), the established pre-existing Jobs.* convention, NOT introduced by emq.2.4 (the closer added zero Lua there; the only NEW Lua, @obliterate, IS rooted in the declared KEYS[2]). Flagged for the Operator as a possible program-wide A-1-wording reconcile target for a LATER rung; NOT decided here, NOT a blocker.

COMMIT-CARVE: the close commit's pathspec = echo/rungs/bus/emq_2_4_check.{sh,out} + docs/echo_mq/emq.testing.md + docs/echo_mq/emq.progress.md + docs/echo_mq/emq.roadmap.md + docs/echo_mq/specs/emq-2-4.progress.md (the ledger). EXCLUDED: every Operator out-of-band path (the staged apps/echomq/guides deletion, investex/exchange/mercury/db/aob/fsharp, the trd_9 exchange checks, html) — re-verify git diff --cached is pure boundary immediately before commit (the concurrent-index race).

## {emq-2-4-learnings} Learnings

### L-1 — design-phase peer hygiene: real-work-but-skipped-checkpoints, and one out-of-charter touch

Two process notes from Venus-1's run (both minor — the WORK was build-grade, unlike the prior session's phantom-completion implementor):

(1) CHECKPOINT DISCIPLINE: Venus-1 jumped straight to "all 3 deliverables complete, BUILD-GRADE" via a single idle-notification summary, skipping the required per-deliverable checkpoints (SendMessage after EACH of D-V1/D-V2/D-V3). UNLIKE the prior implementor's phantom claims, the artifacts WERE real and ground-true on disk — so the Director's ground-truth verification (read every deliverable, re-probe the boundary) confirmed rather than caught-out. CARRY-FORWARD: the Director's independent artifact verification is the gate regardless of checkpoint cadence; a confident "BUILD-GRADE" self-verdict is corroborated by reading the artifact, never accepted on the claim. (Same law as the implementor lesson — verify the artifact, not the report — here it confirmed.)

(2) CHARTER BOUNDARY: Venus-1 stubbed the empty docs/echo_mq/course/echo_mq.course.md (a 7-line stub + a [RECONCILE] marker) — outside its spec-only charter (the course is the echo-mq-expert's domain). The Director reverted it (the file was empty; no Operator content lost). CARRY-FORWARD: an architect on a design phase edits ONLY the spec triad + the design docs in scope; a stray course/content touch is reverted at review to keep the cycle's commit boundary faithful.

### L-2 — INV3 FINDING (Director-verified): the emq.2.2 obliterate leaks grouped job rows — the depth mandate's first real catch

Mars-1's D6 admin-depth suite surfaced a REAL spec⇄as-built defect in the SHIPPED emq.2.2 obliterate (NOT a spec defect, NOT introduced by emq.2.4). The Director verified it against ground truth (read the script + the spec line + confirmed the probe) — not accepted on the peer's claim:

THE DEFECT (echo/apps/echo_mq/lib/echo_mq/admin.ex @obliterate). The `del_job(id)` helper (DEL job:<id> + :logs + :lock) is called ONLY for ids read from the four FLAT sets (active/pending/schedule/dead). The OPEN-lane loop `for g,_ in pairs(groups) do redis.call('DEL', base..'g:'..g..':pending') end` deletes each lane's `g:<g>:pending` ZSET but does NOT iterate its members to del_job their `emq:{q}:job:<id>` rows first. So a grouped job enqueued via Lanes.enqueue/5 and never claimed has its row SURVIVE obliterate.

THE SPEC SAYS OTHERWISE (emq.2.2.md EMQ.2.2-D4): obliterate destroys "...the lane structures gactive/glimit/ring/wake/paused-SET + each g:<g>:pending... and EVERY REACHABLE JOB ROW + its :logs/:lock subkeys." The grouped rows are in scope per the spec; the as-built leaks them.

WHY INVISIBLE: every existing emq.2.2 admin_test.exs obliterate case clears flat sets only — none populates a lane. The depth mandate (populated multi-set queues with grouped jobs) is exactly what exposed it. This is the empirical answer to the Operator's founding question (does v1's test depth matter?): YES — it caught a real resource leak in the one op whose contract is "leave nothing behind."

MARS-1 HANDLED IT PER INV3 (not papered over): test/admin_depth_test.exs carries an explicit `@tag :finding` test asserting the AS-BUILT honest behavior (the lane SET clears, the grouped ROW survives) — visible, green, traceable; the clean "clears every flat set" test uses no lane job, so it stays a true green.

THE FIX (proposed, sound-in-principle, declared-keys-clean): inside the existing groups loop, before DELing `g:<g>:pending`, ZRANGE its members and del_job each (the row key derives from the declared base root base..'job:'..id — slot-sound, A-1-clean), budget-accounted (obliterate is budget-bounded, :more/:ok). This EDITS A SHIPPED emq.2.2 script → re-opens the shipped-script-edit risk dimension the Arm-2 ruling (D-3) steered emq.2.4 away from → an Operator architecture/risk decision (fix-now-in-this-ship vs defer-to-a-dedicated-rung). Escalated to the Operator (AskUserQuestion); the ruling lands as D-5. CARRY-FORWARD for the program: the depth-port mandate is load-bearing — it finds real shipped-surface defects the conformance set + unit suites miss; an INV3 finding is documented honestly in-suite AND escalated, never weakened.

### L-3 — the mutation-revert footgun: `git checkout` on an UNCOMMITTED-fix base clobbers the fix (Apollo verification-discipline self-finding)

During the Stage-4 evaluation, Apollo-1 ran the LAW-1a mutation spot-check on the highest-risk surface (the @obliterate lane-drain): edit-in a fault (remove the `del_job(id)` loop) → confirm a test catches it → revert. The mutation was CAUGHT cleanly (3/3 — the obliterate_grouped conformance scenario + 2 admin_depth tests went RED, kill-rate 100%). The revert, however, was `git checkout -- echo/apps/echo_mq/lib/echo_mq/admin.ex` — and admin.ex was **modified-uncommitted** (the emq.2.2 obliterate FIX lives only in the working tree; HEAD `d91aa5e7` still carries the pre-fix emq.2.2 `admin.ex` from `76fc947c`). So `git checkout` restored the file to **HEAD (the pre-FIX leaking version)**, NOT to "the state before the mutation." Every obliterate-grouped probe leaked for the rest of the run until the root cause was traced.

THE TRACE (recorded because the symptom MIMICKED a real defect for ~10 probes): the leak survived a `SCRIPT FLUSH` (not a Valkey script-cache poison — Script.new SHAs the body), survived an `rm -rf _build/{dev,test}/lib/echo_mq` + clean recompile (not a stale BEAM), and reproduced via a hand-driven redis-cli EVAL of the on-disk body AND the compiled `Admin.obliterate` (so not an Elixir-path artifact). A debug-instrumented EVAL of a CORRECTLY-typed lane loop WORKED — the discriminator that proved the on-disk script itself was the pre-fix version. `git show HEAD:admin.ex` then confirmed HEAD's group loop is the bare `redis.call('DEL', base..'g:'..g..':pending')` with NO `del_job` — the leak. `git log -- admin.ex` = last touched at `76fc947c` (the fix never committed). CONCLUSION: Apollo's own revert destroyed the uncommitted fix; the build was correct at session start.

THE RESTORATION (hygiene, not authoring): the fix bytes were recovered from Apollo's session-start Read of the working-tree admin.ex (the correct fixed version) and Written back. Re-verified: `git diff HEAD admin.ex` = +23/-2 (the lane-drain fix, matching P-3/D-5/L-2); the attack probe GREEN again ({:done,6} leaked_rows=0 footprint_empty=true, identical to session-start); full suite 250/0; conformance 43/43.

CARRY-FORWARD (durable, for the evaluator charter / the program skill): **the LAW-1a mutation spot-check's revert step is `git checkout -- <path>` ONLY when `<path>` is git-CLEAN; when the file is modified-uncommitted (the normal case for a rung's in-flight surface), `git checkout` reverts to HEAD and DESTROYS the uncommitted work.** The safe mutation protocol on a dirty file: (a) `cp <path> <path>.apollo-bak` BEFORE the edit, mutate, run, then restore from the backup (`mv <path>.apollo-bak <path>`); OR (b) capture the exact pre-mutation bytes (a Read) and Write them back; OR (c) apply the mutation as a precise Edit and revert it with the inverse Edit (never `git checkout`). NEVER `git checkout` a dirty working-tree file to undo a mutation. This is the evaluator analog of the "verify the artifact, not the report" law: verify the REVERT (`git diff` shows the EXPECTED rung diff, not an empty/HEAD diff) before trusting it landed.

### L-4 — Apollo-2 Stage-4 mutation kill-rate: the depth-suite teeth PROVEN (3/3, 8 tests), closing the QA "detected bugs not fixed" concern

The crash-interrupted Stage-4 left a QA worry that a defect Apollo-1 would have surfaced went un-surfaced. Apollo-2 ran the adversarial bug-hunt the crash cut short and PROVED the emq.2.4 depth suites discriminate real defects (not just confirm green). THREE high-risk mutations, each KILLED by a SPECIFIC test, each reverted clean:

(1) @obliterate lane-drain — removed `for _, id in ipairs(ids) do del_job(id) end` (reintroduce the L-2 leak) → 3 RED: the `obliterate_grouped` conformance scenario ({:fail,{:ok,1}} the row still EXISTS) + the admin_depth "del_job's the grouped JOB ROWS" test (EXISTS≠0) + the admin_depth bounded-budget grouped test. The clean flat-set obliterate scenario stayed GREEN (it never populates a lane) — the depth mandate's lane-populating tests are exactly what catch it.

(2) @extend_locks token fence (jobs.ex) — `att and att == token` → `att` (any row with an attempts field extends, defeating the at-most-once token guard) → 3 RED: the `extend_locks_batch` conformance scenario + the watch_depth Locks-plane "drops a stale token" test (extended:1 vs expected dropped:[id]) + the watch_depth [live,stale,gone] partial-batch test (failed=[gone] vs expected [stale,gone], naming the exact wrongly-succeeding id). The single-token `stale`/`lock_extend` scenarios stayed GREEN — only the multi-token batch depth localizes it.

(3) @sweep_stalled grouped recover (stalled.ex) — `ZADD lane` → `ZADD KEYS[2]` (a grouped job wrongly recovers into the FLAT pending, bypassing fair-lane ring/gactive accounting) → 2 RED: the `stalled_group` conformance scenario ({:fail,{:ok,nil}} not in the lane) + the watch_depth group-aware test (ZSCORE lane nil vs expected 0.0). The subtlest mutation — the job IS recovered (claimable in pending), so a naive `recovered:[id]` assertion would stay GREEN; only the discriminating destination-set assertion (lane present AND flat nil) catches it.

THE PATTERN (durable, for the program): the L-2 obliterate leak and the #3 misroute are the SAME defect class — state landing in the WRONG set, invisible until a test populates the GROUPED structure and asserts the DESTINATION. The happy-path conformance set + the per-verb unit suites cannot see them; v1's multi-job/grouped scenario DEPTH (the emq.2.4 mandate) can. This is the empirical answer to the Operator's founding question: the depth port is load-bearing — it both FOUND a real shipped-surface defect (L-2) and PROVES, by kill-rate, that the closer's suites have teeth.

REVERT DISCIPLINE (L-3 closed): every mutation reverted via the inverse Edit (NEVER `git checkout`), `git diff -- <path>` verified CLEAN before trusting it; the final tree-clean verify confirmed all 3 files + the whole echo_mq lib/test tree match the committed state, zero residue. The L-3 footgun is doubly neutralized: the fix is committed (a checkout would have been safe) AND the disciplined inverse-Edit was used regardless. NOTE: an Operator out-of-band commit (479febf5 [exchange] investex client) landed mid-run; verified boundary-clean (touches no echo_mq, no rungs/bus) — the concurrent-tree event the T-4 boundary predicted, confirmed harmless.

## {emq-2-4-complete} Complete

### Z-1 — emq-2-4 design cycle COMPLETE

The features catalog (docs/echo_mq/emq.features.md) + the v1→v2 full-parity proof + the emq.2 ⇄ emq.2.{1,2,3} reconcile gap table + the emq.2.4 triad + the consolidated cluster runbook (emq.2.prompt.md) all delivered — build-grade, ground-true, boundary-clean (Director ground-truth verification, D-1 ACCEPT). The solo progress update folded emq.2.3 → IN FLIGHT + emq.2.4 → SPECCED into the dashboard + roadmap. The ONE open Operator seam (G1 the rate-gate fork) is surfaced (default Arm 2; D-2). Decisions locked: D-1 (Director review ACCEPT), D-2 (the carried fork). Reports: Y-2 (Director). Learnings: L-1 (peer hygiene). 

NO commit this cycle — design docs (the catalog + the triad + the runbook + the ledger); the touch-set is surfaced for the Operator (echo_mq code untouched; the emq.2.3 watch-plane code remains its own uncommitted [emq] surface). The emq.2 parity cluster's closing rung is SPECCED and the runbook is LIVE; emq.2.4 is ready to build once the G1 fork is ruled. Frontier after emq.2.4: emq.3 (parent/flow — closes Movement I) or an emq.7/emq3.1 pull-forward.

### Z-2 — emq-2-4 build cycle COMPLETE: the emq.2.4 parity-closer SHIPPED; the emq.2 cluster CLOSED

The emq.2.4 ship-close recycle (after the prior cycle's mid-Stage-4 system crash) is complete. PIPELINE: Director T-4 (resume ground truth + the harness mandate) → Mars-3 (the committed harness echo/rungs/bus/emq_2_4_check.sh + the emq.testing.md §0 honest reconcile) → Director Stage-2 (P-4 — ran the harness myself, PASS 7/7) → Apollo-2 Stage-4 MANDATORY (Y-3 BUILD-GRADE; the full-100 harness PASS 7/7; the adversarial table all-CLEAN; mutation kill 3/3, 8 tests) → Director Stage-6 close (this).

SHIPPED — emq.2.4 = the obliterate fix `92a8f042` + the closer code `3298e4bc` (5 C1 renames byte-identical, conformance 37→43, the 5 depth suites metrics_depth/admin_depth/rate_consult/dedup_bound/watch_depth) + this close commit (the committed harness + the honest green-board + the cluster-close fold). GATE: compile --warnings-as-errors clean · 250/0 mix test --include valkey · Conformance.run/2 {:ok,43} · the ≥100 determinism loop 100/100 — now a COMMITTED re-runnable harness (the Operator's "Harness required", the structural fix for "ephemeral proof != a harness") · dbsize-flat leak check · multi-seed sweep 5/5. DECISIONS: D-8 (the LAW-4 ratify). REPORTS: Y-3 (Apollo-2 BUILD-GRADE). LEARNINGS: L-4 (Apollo-2 — the mutation kill-rate teeth, closing the QA "detected bugs not fixed" concern). PROGRESS: P-4 (Director Stage-2).

THE emq.2 PARITY CLUSTER IS CLOSED: read (2.1 `7d98ef86`) → ops (2.2 `76fc947c`) → watch (2.3 `3c6461ff`) → close (2.4). apps/echomq (frozen v1 1.3.0) is clear to dissolve on a proven-at-depth echo_mq (Operator-owned timing). FRONTIER: emq.3 (parent/flow — the A-1-compatible flow design §11.10 first, then the build; closes Movement I) or an emq.7 / emq3.1-3.2 pull-forward (Operator slot). ONE CARRIED non-blocking flag: the inherited @extend_locks ARGV-rooted-key A-1-wording reconcile (Y-3 §4), for a later rung.

PROCESS: a fresh emq-2-4 recycle team (Director + Mars-3 + Apollo-2; the crash-dead Apollo-1/Mars-1/Mars-2/Venus-2 roster stood down). Records-freeze HELD — the design+build half (T-1..3, A-1..3, D-1..7, L-1..3, P-1..3, Y-1..2, Z-1) is byte-intact; this cycle appended T-4, P-4, Y-3, L-4, D-8, Z-2. The peers ran NO git; the Director commits by guarded pathspec (the concurrent-index race + the L-3 footgun both respected). The three QA-reported delivery issues (commits-not-made, emq.testing.md-inconsistent, detected-bugs-not-fixed) are ALL closed.

## {emq-2-4-progress} Progress

### P-1 — Stage-0 gate PASSED (Director independent verification of Venus-2's A-3 reconcile)

Venus-2 reported BUILD-GRADE; the Director verified the artifact (not the report) against ground truth before releasing Stage 1:
- AS-BUILT module names CONFIRMED via `grep defmodule`: events.ex→Events, telemetry.ex→Meter, lock_manager.ex→Locks (+lock_manager/core.ex→Locks.Core), stalled_checker.ex→Stalled, cancellation_token.ex→Cancel. The triad re-pin landed BOTH ways: 0 stale v1-style CamelCase names left in the 5 docs AND the as-built names positively present (features 6 / prompt 6 / md 3 / llms 5).
- is_maxed/2 SHAPE CONFIRMED (metrics.ex:222): @spec :: :ok | {:error,:rate} | {:error,term()}; body {:ok,0}→:ok, EMQRATE→{:error,:rate}. Venus-2's US2 correction (from the stories' wrong "answers false below") is right — a real catch that would have mis-shaped Mars's Arm-2 consult-then-skip test.
- C2 target extend_locks/4 (jobs.ex) return = {:ok, failed} (the un-extendable [live,stale,gone] partial-batch); INV5-gated (Keyspace.job_key per id) + declared KEYS=[active] (A-1 clean). @sweep_stalled group-aware recover branch confirmed at stalled_checker.ex (the `if g then … g:<g>:pending … RPUSH ring` lane path) — both C2 branches real and untested-at-depth.
- Conformance floor = 37 pinned two ways (conformance_run_test.exs:37 {:ok,37}; conformance_scenarios_test.exs @run_order + Keyword.keys==@run_order). emq.2.3→committed re-pin landed (no "uncommitted/on disk" live claim remains).
- RECORDS-FREEZE HELD: A-3 appended; the design half (T-1/T-2, A-1/A-2, D-1/D-2/D-3, Y-1/Y-2, L-1, Z-1) byte-intact.

VERDICT: Stage 0 build-grade. The as-built floor is true; the rulings (G1 Arm 2, G2 HOLD, G3 documented) carry no @claim edit and no :data write — the only lib/ mutations Stage 1 makes are the C1 file renames + the additive conformance scenarios; everything else is NEW test files. Stage 1 (Mars-1) released to build against the A-3 floor.

### P-2 — Stage-2 Director solo review: PASS (a REAL pass — independent gate + adversarial probes + mutation spot-check) + the REMEDIATE list for Mars-2

The Director verified Mars-1's Stage-1 build against ground truth (not the self-report):
- TOUCH-SET FAITHFUL: git status/diff --stat scoped to echo/apps/echo_mq — the ruled arms HELD (jobs.ex/metrics.ex/lanes.ex ABSENT from the diff = no @claim edit, no :data write). Only lib edit = conformance.ex (+5 scenarios). 5 files DELETED + 5 new (the C1 renames). 5 new depth suites. echo_wire/echomq/mix.lock untouched.
- C1 BYTE-IDENTICAL: diff HEAD:old vs working:new for all 5 renames → IDENTICAL (stalled_checker→stalled, lock_manager→locks, core, telemetry→meter, cancellation_token→cancel). 4 old basenames gone; 0 lingering basename references. A pure inert move.
- CONFORMANCE 37→42 ADDITIVE-MINOR: git diff conformance.ex — the 11 removed lines are 10 moduledoc-prose (the 37→42 narrative) + 1 the cancel: entry gaining ONLY a trailing comma (contract string byte-identical). ZERO prior scenario name/contract/verdict-body changed; no prior apply_scenario probe touched. +5 (unknown_state, rate_consult, dedup_release, extend_locks_batch, stalled_group) each with its probe. Both pins re-pinned == 42.
- INDEPENDENT GATE RE-RUN (Director's own, Valkey 6390): erlang 28.5.0.1 (app dir); compile --warnings-as-errors clean; FULL --include valkey = 4 doctests, 249 tests, 0 failures (the GenServer-killed line = the expected reconnect-kill-drill captured-log); direct Conformance.run/2 shows all 42 in live order. The clean --warnings-as-errors compile is itself an INV2 floor (no undefined same-app verb).
- INV2 PROBE: every Module.verb the 5 new suites call cross-checked real (incl. the two non-obvious — Lanes.depth/3 lanes.ex:193, Locks.extend/1 locks.ex:201). No verb drives an unshipped surface (Meter = the surface that fires not the contract→emq.8; Cancel = the local token not distributed→emq.6). No false-green.
- MUTATION SPOT-CHECK (LAW-1a): broke the @extend_locks token fence (att==token → att); watch_depth went RED with 3 failures (the C2 extend_locks_batch test: failed=[gone] vs expected [stale,gone]; the Locks.extend depth test: extended:1 vs dropped:[id]) — the depth coverage discriminates. Reverted via git checkout -- jobs.ex; git diff --stat jobs.ex EMPTY (net-zero, HEAD-clean); fence restored; watch_depth 5x green-after-revert (13 tests 0 failures each — a determinism spot-check, the full ≥100 loop is Mars-2/Apollo's).

VERDICT: Stage-1 build is BUILD-GRADE — clean, faithful, teeth-proven. NO defects found in Mars-1's surface.

THE REMEDIATE LIST for the fresh Mars-2 (the Opus effort-max refine/polish pass, D-6):
1. THE D-5 OBLITERATE FIX (the one real remediation, a SEPARATE [emq] emq.2.2-fix commit): inside @obliterate's `for g,_ in pairs(groups)` loop, BEFORE `DEL base..'g:'..g..':pending'`, ZRANGE the lane members and del_job each (the row key derives from the declared base root base..'job:'..id — slot-sound, A-1-clean), budget-accounted (the :more/:ok bound). Re-verify the `obliterate` conformance scenario byte-identical-or-updated (INV1). FLIP the D6 admin_depth @tag:finding test from asserting the as-built LEAK to asserting the FIXED behavior (grouped rows cleared). Keep it deterministic (no sleep-as-sync).
2. THE emq.testing.md GREEN-BOARD (D-6 Operator grant): author docs/echo_mq/emq.testing.md — the HONEST gate-ladder record (per-app suite counts, conformance 42 with the 37+5 split, the ≥100 loop result PASS/FAIL + dbsize-flat leak check, the honest-row Valkey-6390 posture, the roadmap-clearance for emq.2.4: deps read 2.1/ops 2.2/watch 2.3 all shipped, the cluster-close unblocked). Write ONLY observed output (tee'd from the real run); Apollo independently re-verifies the board.
3. THE FULL GATE LADDER + ≥100 LOOP, RE-RUN ON THE POST-FIX TREE: since the obliterate fix changes admin.ex + the obliterate scenario, re-run compile + per-app pure+:valkey+process suites + Conformance.run/2 ({:ok,42}, the 37 prior byte-unchanged) + the ≥100 determinism loop over the process-touching depth suites (watch_depth + the stalled_group/timer-touching conformance + admin_depth with the flipped obliterate test) owning the machine, tee to a file; the multi-seed sweep for the synchronous read/ops suites (D5/D6/D2/D4).
Mars-1 found NO other polish items — the build is clean; Mars-2's scope is the fix + the green-board + the post-fix re-gate.

### P-3 — Stage-3 gate PASSED (Director verification of Mars-2's polish pass) + the commit-carve constraint

The Director verified Mars-2's three P-2 deliverables against ground truth (re-ran, did not read):
1. THE OBLITERATE FIX — read the ACTUAL on-disk @obliterate (admin.ex): inside the groups loop, the lane is drained as a state set (ZRANGE 0 budget-1 → del_job each → ZREM → budget-=#ids), the lane ZSET DELed only once fully drained (else `return 1` :more, re-read next call), `if budget<=0 return 1` per iteration prevents the next group's ZRANGE-with-budget-0 degenerating to ZRANGE 0 -1. CORRECT, declared-keys-clean (every key from the base root KEYS[2], slot-sound — no new KEYS[], no SCAN, no backref — L-2 respected), budget-sound (mirrors the flat-set loops). The aux-key DEL stays after the loop (fires only on :ok). 
2. CONFORMANCE 42→43 — obliterate_grouped registered (conformance.ex:75 contract, :968 probe, scenarios_test.exs:62 @run_order); both pins == 43. INV1 git-verified: ZERO prior apply_scenario clauses removed/changed (only the 6 additive — Mars-1's 5 + obliterate_grouped). The flipped D6 admin_depth test now asserts the FIXED behavior (grouped rows cleared) + a bounded-grouped test; @tag:finding dropped.
3. THE emq.testing.md GREEN-BOARD — read it: honest-in-form (every figure framed as observed/tee'd output + the exact command + the explicit "Apollo re-runs independently" clause); §0 = toolchain/compile/suites/conformance-43-split/≥100-loop/multi-seed/roadmap-clearance/boundary; §1 = the conformance chain-of-record. Mars-2 prepended it to a pre-existing testing-strategy doc + reconciled stale figures — KEEPING it as the combined program-doc-family testing view (the Operator's "emq.*testing*.md" grant). Accepted as the combined doc (not split).

INDEPENDENT RE-RUN (Director's own, Valkey 6390): compile clean; FULL --include valkey = 4 doctests, 250 tests, 0 failures; Conformance {:ok,43} via the pin (re-run green 16x incl. the determinism loop); determinism spot-check 15/15 PASSED (corroborates Mars-2's tee'd 100/100); dbsize 12 flat. The board is honest-in-FACT — two independent runs now back it (Apollo provides the third + the full 100).

D-7 (Mars-2's no-mix-format transparency note): CONCURRED — running it would reflow ~100 pre-existing committed lines + visually churn the prior conformance scenarios INV1 needs byte-stable; the gate ladder doesn't mandate it; Mars-2's additions match the committed long-line registry convention. Sound judgment, well-recorded.

THE COMMIT-CARVE CONSTRAINT (for Stage 6, flagged by Mars-2 + Director-confirmed): the D-5 "separate [emq] emq.2.2-fix commit" is FORCED by the pin invariant to be admin.ex ALONE. Reason: the conformance count→43 + both pins + obliterate_grouped + the 5 depth scenarios all live in conformance.ex + the two pin tests; splitting them by hunk would leave an intermediate commit whose pin asserts 43 while <43 scenarios exist → the pin test fails at that commit. So ALL conformance changes ship in ONE commit. Carve: COMMIT 1 (emq.2.2-fix) = admin.ex ALONE (the shipped-script defect fix); COMMIT 2 (emq.2.4-closer) = everything else (the renames, the depth suites incl. the flipped obliterate test, conformance.ex with all 6 new scenarios incl. obliterate_grouped, both pins→43, the triad, the ledger, emq.testing.md, the roadmap/progress/features folds). ORDER: commit 1 (the fix) FIRST, then commit 2 (the closer, whose obliterate_grouped scenario passes only because admin.ex is already fixed). The fix's test evidence riding in commit 2 is acceptable (the closer is literally the depth suite that found+proves the floor; the two commits land together).

VERDICT: Stage-3 build-grade. Apollo (Stage 4, MANDATORY) released for the independent re-run + the green-board honest-by-construction verify + the @obliterate shipped-script-edit adversarial re-verification.

### P-4 — Stage-2 Director review of Mars-3's harness + emq.testing.md reconcile: PASS (ran the harness myself)

Mars-3 delivered; verified the ARTIFACT, not the report:
1. THE COMMITTED HARNESS echo/rungs/bus/emq_2_4_check.sh (+ the committed transcript emq_2_4_check.out, PASS 7/7, LOOP_N=100). Read line-by-line: 7 real gates — toolchain / compile / suite (+ the pure 182-excl & wire 72-excl column split) / conformance (a DIRECT EchoMQ.Conformance.run/2 one-liner over a live 6390, its own gate beyond the pin test) / the >=100 determinism loop over the process-touching depth suites (watch_depth+admin_depth+conformance_run) / the dbsize leak check / the multi-seed sweep. Each gate is a real subprocess; the board keys off the ExUnit "0 failures" summary + the exit code, NEVER a grep for "error" (the watch-plane reconnect-kill drill is correctly non-fatal). LOOP_N parameterized (committed default 100; LOOP_N=10 smoke). Portable root resolution (no hardcoded path in the logic). This is the structural fix for "ephemeral proof != a harness" — the determinism proof is now committed + re-runnable, so a crash cannot evaporate it.
2. DIRECTOR INDEPENDENT RUN (LOOP_N=10 smoke — the P-3 precedent; the full 100 is Apollo's): PASS 7/7 — toolchain ok (erlang 28.5.0.1 re-probed from apps/echo_mq, Valkey 6390 PONG), compile --warnings-as-errors clean, suite [4 doctests, 250 tests, 0 failures] + pure 182-excl + wire 72-excl, conformance {:ok,43}, determinism 10/10, leak dbsize 1==1 flat, seed-sweep 5/5. The harness REPRODUCES the board from a different operator at a different LOOP_N — the reproducibility the Operator's "Harness required" demanded.
3. emq.testing.md §0 RECONCILE — honest-in-fact: re-framed to "code-landed on master + gate-green + harness-reproducible — NOT yet shipped" (no overclaim; the formal CLUSTER CLOSE is the Director's fold, a later step), resolving the contradiction with the Operator's 7c2f2405 "not ready" title; cites the 3 commits (92a8f042 fix / 7c2f2405 docs / 3298e4bc closer) + the committed harness + .out transcript; the dbsize-floats-but-delta-flat honesty kept (12 earlier, 1 now; only the before==after asserted).

BOUNDARY: echo/rungs/bus/emq_2_4_check.{sh,out} + docs/echo_mq/emq.testing.md ONLY — git status confirms echo_mq lib/test untouched, apps/echomq + mix.lock untouched. Mars-3 ran NO git.

ONE MINOR REMEDIATE (close-time, non-blocking): emq.testing.md §0's blockquote cites a 17:52->17:59Z harness run while the committed .out transcript is the 18:04->18:11Z run — two runs, IDENTICAL figures (which DEMONSTRATES reproducibility, not a false green). The record should cite the committed transcript's run; fix at the Stage-6 close.

VERDICT: Stage-2 PASS. The harness is sound + reproduces; the testing reconcile is honest. Apollo-2 (Stage 4, MANDATORY) released for the independent full-100 re-run + the adversarial bug-hunt (the QA "detected bugs not fixed" concern) + the green-board honest-by-construction verify + a mutation kill-rate + Y-3. Mars-3 kept idle-available in case Apollo surfaces a REMEDIATE fix.
