# F6.6 · user stories

> Who the interactive catalog serves, what they need, and how we will know it works. Connextra stories with
> Given/When/Then acceptance criteria; together they cover every deliverable in [`f6.6.md`](f6.6.md).

## F6.6-US1 — Search as I type

As a **learner**, I want the course list to filter as I type, so that I find a course without submitting or reloading.

Acceptance criteria
- Given the search box, when I type, then `phx-change="search"` fires `handle_event("search", ...)`.
- Given the event, when it runs, then it filters through the `Catalog` context and re-assigns `@courses`.
- Given each keystroke, when handled, then the rendered list updates without a reload.

INVEST — independent; testable with `render_change`; encodes F6.6-INV1, INV3.
Priority: must · Size: 3 · Implements deliverables: F6.6-D3.

## F6.6-US2 — Create without a reload

As a **learner** (an author), I want to add a course inline, so that I see it appear without leaving the page.

Acceptance criteria
- Given the form, when I submit, then `phx-submit="create"` calls `Portal.create_course/1`.
- Given `{:ok, _}`, when it returns, then the view flashes and resets the form.
- Given `{:error, %Ecto.Changeset{}}`, when it returns, then the view assigns `to_form(changeset)` and errors show
  inline.

INVEST — independent; testable with `render_submit`; encodes F6.6-INV5.
Priority: must · Size: 3 · Implements deliverables: F6.6-D4.

## F6.6-US3 — Fast first paint

As a **visitor**, I want the page to render before the socket connects, so that first paint is fast and indexable.

Acceptance criteria
- Given a first request, when it renders, then a disconnected HTTP render paints the page.
- Given the socket, when it connects, then a connected mount takes over.
- Given side effects, when present, then `connected?/1` guards them so they run once on the live socket.

INVEST — independent; testable by the disconnected and connected mounts; encodes F6.6-INV2.
Priority: must · Size: 2 · Implements deliverables: F6.6-D2.

## F6.6-US4 — Large lists stay light

As a **developer**, I want the list rendered as a stream, so that a large, append-only catalog does not sit in socket
memory.

Acceptance criteria
- Given the list, when initialized, then it uses `stream/3` and `phx-update="stream"`.
- Given a new course, when added, then `stream_insert/3` inserts the row.
- Given the socket, when inspected, then the full list is not held as an assign.

INVEST — independent; testable by the stream usage; encodes F6.6-INV4.
Priority: should · Size: 2 · Implements deliverables: F6.6-D5.

## F6.6-US5 — One interactive surface over the facade

As a **developer**, I want the LiveView to call only the facade and reuse the F6.5 component, so that interactivity adds
no boundary leaks and no duplicated markup.

Acceptance criteria
- Given `CatalogLive`, when inspected, then it calls only `Portal`/the `Catalog` context — no engine, `Repo`, or
  `GenServer.call`.
- Given `render/1`, when it draws a row, then it reuses the F6.5 `course_card` component.
- Given the view, when it changes state, then it does so through assigns and events.

INVEST — independent; testable by inspection and rendering; encodes F6.6-INV1, INV6.
Priority: must · Size: 1 · Implements deliverables: F6.6-D1.

## F6.6-US6 — Reach the page

As a **visitor**, I want a route to the live catalog, so that I can open it.

Acceptance criteria
- Given the router, when inspected, then `live "/catalog", CatalogLive` is declared in a `:browser` scope.
- Given the path, when I navigate, then `CatalogLive` mounts and renders.

INVEST — independent; small; testable by the route; encodes F6.6-D6.
Priority: must · Size: 1 · Implements deliverables: F6.6-D6.

---

Coverage: D1→US5 · D2→US3 · D3→US1 · D4→US2 · D5→US4 · D6→US6 · D7→US1,US2,US3,US4. Spec: [`f6.6.md`](f6.6.md) · Agent
brief: [`f6.6.llms.md`](f6.6.llms.md).
