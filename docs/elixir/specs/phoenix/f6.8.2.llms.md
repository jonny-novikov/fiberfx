# F6.8.2 · agent brief (llms)

> Implementation brief for an agent building **deployment**: a scoped umbrella release (a `releases/0` in the
> root `mix.exs` naming only the Portal apps, excluding `echo_bot`), a completed multi-stage Dockerfile (built
> FROM the Operator's `docs/deploy/Dockerfile.template`, version-corrected to Elixir 1.18.4 / OTP 28.5.0.1 and
> de-Codemoji'd), the completed runtime-vs-compile config split + the prod deploy-bind, a `Portal.Release`
> migrate task, a libcluster topology, the per-machine `worker_id` fix, and a distilled `fly.toml`. References
> (cite as-built `file:line`; the templates are reference inputs, not edited), real arities (cite, never invent),
> the execution topology + task DAG + the EXACT files touched, agent stories (Directive + Acceptance gate), and a
> paste-ready prompt. Pairs with the spec [`f6.8.2.md`](f6.8.2.md) and the stories [`f6.8.2.stories.md`](f6.8.2.stories.md).
> **The live deploy is DEFERRED to the Operator** — the Operator creates the Fly app + machines and runs `fly
> deploy`; this rung ships the ARTIFACTS, gated BUILD-LOCAL. The F5 engine is unchanged; the web still calls only
> the `Portal` facade. **Framing for everything emitted (and for any agent briefed — propagate this clause): no
> gendered pronouns for agents; no perceptual or interior-state verbs for tools or agents ("sees"/"wants"/"detects");
> no first-person narration ("we"/"our"/"I").**

> **OPERATOR-PINNED (settled — APPLY these; ZERO open intent forks remain for F6.8.2 — fully build-ready):**
> - fly `app = "echo-portal"`; `PHX_HOST = "echo-portal.fly.dev"` (the Portal's OWN host — distinct from
>   `:deep_link_base_url`, which STAYS `jonnify.fly.dev`); VM `memory = "512mb"`; `primary_region = "fra"`.
> - **Release name = `portal`** — `releases/0` defines `portal: [...]`; the binary is `bin/portal`; migration is
>   `bin/portal eval "Portal.Release.migrate()"`; the Dockerfile `ENTRYPOINT` is `["bin/portal", "start"]`. The
>   release name `portal` is INDEPENDENT of the fly app `echo-portal` — do NOT conflate them (the OTP release
>   artifact vs. the Fly application).
> - **OTP Docker tag = `1.18.4-erlang-28.5.0.1-debian-bookworm-20260518`** (`OTP_VERSION="28.5.0.1"`,
>   `DEBIAN_DATE="20260518"`) — VERIFIED on Docker Hub: there is NO `1.18.4-erlang-28.1` image; the published 28.x
>   tags jump `28.0.4` → `28.5.0.1`. The build ALSO bumps `echo/.tool-versions` `erlang 28.1` → `28.5.0.1` so dev
>   aligns to prod (a BUILD deliverable — D11/AS9 — NOT a pre-build spec edit; bumping it now would break the
>   Operator's current local `28.1` env before the rung builds).
> - **`worker_id` source = `FLY_MACHINE_ID` hash** — `:erlang.phash2(System.get_env("FLY_MACHINE_ID"), 1024)`;
>   topology-agnostic (no region+ordinal). The `unique_constraint(:id)` catalog-write question stays a build note.

## References

- **The Operator's templates (reference INPUTS — cite by `file:line`; do NOT edit them):**
  - `docs/deploy/Dockerfile.template` (56 lines) — the multi-stage image build, Codemoji-shaped:
    - `:3` — the tag-format comment `hexpm/elixir:1.18.4-erlang-27.3.4.1-debian-bookworm-20251117` — OTP **27**,
      WRONG for this umbrella (RECONCILE 1). `:5` — `FROM hexpm/elixir:${ELIXIR_VERSION}-erlang-${OTP_VERSION}-debian-bookworm-${DEBIAN_DATE} AS elixir-base`.
    - `:8` `ENV MIX_ENV=prod`; `:12-16` apt build deps; `:19-20` `mix local.hex/rebar`.
    - `:25` comment names `live_svelte` (absent here); `:30-33` `COPY mix.exs mix.lock`/`config`/`apps/echo/mix.exs`/`apps/echo/config`;
      `:36-37` `mix deps.get --only prod` + `mix deps.compile`; `:40-41` `COPY apps/echo/lib`/`apps/echo/priv`;
      `:44-45` `WORKDIR /app/apps/echo` + `mix compile` (the Codemoji single-app build, RECONCILE 2).
    - `:53` `RUN mix phx.digest`; `:56` `RUN mix release` — **ENDS HERE**, no runtime stage / `ENTRYPOINT`
      (RECONCILE 2 completes it).
  - `docs/deploy/fly.toml` (179 lines) — the full Codemoji v8 production config:
    - `:21` `app = "codemoji-phoenix"`, `:22` `primary_region = "fra"`, `:23-24` `kill_signal = "SIGTERM"`/`kill_timeout = "30s"`.
    - `:29-37` `[build] dockerfile` + `[build.args]` (`NODE_VERSION=22` DROP, `ELIXIR_VERSION=1.18.4` MATCH,
      `OTP_VERSION="27.3.4.1"` → **`28.5.0.1`** (PINNED — no `28.1` image exists), `DEBIAN_DATE="20251117"` → **`20260518`**).
    - `:42-43` `[deploy] strategy = "rolling"`; `:124-125` `[experimental] auto_rollback = true`; `:130-133`
      `[compute]` (shared/2cpu/2048 — distill to the Portal's needs).
    - `:48-120` `[env]` — KEEP `PHX_HOST` `:50` / `PHX_SERVER` `:51` / `MIX_ENV` `:52` / `PORT` `:53` / `POOL_SIZE`
      `:54`, the IPv6/cluster trio `ERL_INETRC` `:70` / `ECTO_IPV6=true` `:74` / `ERL_AFLAGS="-proto_dist inet6_tcp"`
      `:78`, `TZ` `:104` / `LANG` `:105`. DROP `DEV_LIVE` `:56`, all Fastify `:80-90`, SQLitestream/Volume `:92-101`,
      `LOG_LEVEL` `:106`, `GAME_DB_NAME` `:109`, all `DD_*` `:111-120`.
    - `:138-178` `[[services]]` — KEEP `internal_port = 4000` `:140`, `auto_stop_machines=false`/`auto_start_machines=true`/`min_machines_running=1`
      `:141-143` (note the Presence/cluster trade), the `[[services.ports]]` 80→443 `force_https` `:151-161`, and the
      `[[services.http_checks]]` `GET /health` `:170-176` (MATCHES the F6.1 route). Distill the `concurrency`
      `:145-148` to Portal scale; the `[[services.tcp_checks]]` `:164-167` is optional.
- **The as-built config (cite; this is what the rung COMPLETES):**
  - `echo/mix.exs:1-21` — `Echo.MixProject`, `apps_path: "apps"`, `:18-20` `defp deps, do: []` — **NO `releases/0`**
    (D1 adds it).
  - `echo/config/runtime.exs` — `:8` `if config_env() != :test`; `:15-29` the `SECRET_KEY_BASE` resolve (raises under
    `:prod` if missing, dev fallback otherwise); `:31` `port = String.to_integer(System.get_env("PORT", "4000"))`;
    **`:33-36`** `config :portal_web, PortalWeb.Endpoint, http: [ip: {127, 0, 0, 1}, port: port], secret_key_base:
    …, server: true` — **the loopback bind (RECONCILE 3, D3)**; `:43-58` the `DATABASE_URL` resolve (raises under
    `:prod`, sets `Portal.Repo` url/pool); `:65-67` the `:deep_link_base_url` env override (already wired).
    **`PHX_HOST` is NOT read here** — D3 adds it.
  - `echo/config/prod.exs` — today ONLY `config :portal, :event_store, Portal.EventStore.Postgres` (the F5.8 adapter
    pick). NO endpoint `url`/`cache_static_manifest`/`server` settings — D4 adds them.
  - `echo/apps/portal_web/lib/portal_web/endpoint.ex:10` — `use Phoenix.Endpoint, otp_app: :portal_web` (the `url`/`host`
    come from config — D4's prod additions resolve against it).
  - `echo/apps/portal/lib/portal/id.ex:13` — `@node 1` (its comment: "F6.8 derives it per machine"); `:19-21`
    `def new(<<_::binary-size(3)>> = namespace), do: namespace <> Base62.encode(Snowflake.generate(worker_id: @node))`.
    **D7 derives `@node` per machine.** `echo/CLAUDE.md §4` records the same-ms collision rationale.
  - `echo/apps/portal_web/lib/portal_web/router.ex:113` — `get("/health", PortalWeb.CourseController, :health)` — the
    F6.1 liveness route the `fly.toml` `http_checks path = "/health"` targets (the alignment, D8/INV3). **NOTE: the
    combined `f6.8.md` cited `:93`; F6.5/F6.6/F6.8.1 added routes above it, so the as-built line is `113` — path +
    controller unchanged, the `CourseController.health/2` action is `course_controller.ex:66`.**
  - `echo/apps/portal_web/lib/portal_web/application.ex` — the web app supervisor; the AS-BUILT child list is
    `[PortalWeb.Telemetry, PortalWeb.Endpoint, PortalWeb.Presence]` (F6.7 added `Presence`; `:36`
    `max_restarts: 1000, max_seconds: 5` — the F6.7 storm-ceiling fix) — **D6 adds a `Cluster.Supervisor` child**
    here (a 4th child). Keep the existing three + `max_restarts: 1000`; do not regress them.
  - `echo/.tool-versions` — AS-BUILT `elixir 1.18.4` / `erlang 28.1`. **D11/AS9 bumps `erlang` → `28.5.0.1`** at
    build so dev matches the PINNED prod image tag `1.18.4-erlang-28.5.0.1-debian-bookworm-20260518` (RECONCILE 1 —
    no `28.1` image exists; the published 28.x tags jump `28.0.4` → `28.5.0.1`). NOT a pre-build edit (would break
    the Operator's local `28.1` env before the rung builds).
- **Deps, as-built (cite — the net-new edges):**
  - `echo/apps/portal_web/mix.exs deps/0` — `{:portal, in_umbrella: true}`, `{:phoenix, "~> 1.7"}`, `{:phoenix_ecto,
    "~> 4.6"}`, `{:phoenix_live_view, "~> 1.0"}`, `{:phoenix_html, "~> 4.1"}`, `{:telemetry_metrics, …}`,
    `{:telemetry_poller, …}`, `{:jason, …}`, `{:bandit, "~> 1.5"}`, `{:lazy_html, …, only: :test}`. **`libcluster`/`dns_cluster`
    are NOT here** — D6 adds `{:libcluster, "~> 3.3"}` (the per-app DEP-GRAPH-VISIBILITY edge; `mix.lock` confirms
    neither is locked).
  - `echo/apps/portal/mix.exs deps/0` — `{:ecto_sql, "~> 3.12"}`, `{:postgrex, ">= 0.0.0"}`, `{:phoenix_pubsub, "~>
    2.1"}` (+ `echo_data`/`jason`/`stream_data`). `Portal.Repo` (F6.3) is the repo `Portal.Release.migrate/0` runs.
- **Real arities (cite, never invent):**
  - `Portal.ID.new/1` (`id.ex:19`), `Portal.ID.valid?/1` (`:55`), the `worker_id` is the `Snowflake.generate/1` opt.
  - `Portal.subscribe/1`, `Portal.broadcast/2` (`portal.ex:62-63`, the F6.7 PubSub facade) — the web's ONLY PubSub
    path; clustering does not add a new one.
  - **NET-NEW this rung:** `Portal.Release.migrate/0` + `Portal.Release.rollback/2` (D5 — define them); the
    `Cluster.Supervisor` child + a `Cluster.Strategy.*` topology (libcluster, D6). `Ecto.Migrator` (the migrate
    primitive `Portal.Release.migrate/0` calls — `Ecto.Migrator.run/4` / `with_repo/2`).

## The fly.toml distill — keep / drop (the rule, pinned per key)

**Rule:** KEEP a key IFF the Portal (a single Phoenix release + Postgres) needs it; DROP everything that serves
Codemoji's Fastify workers / Redis / SQLitestream / Datadog / game DB. The `docs/deploy/fly.toml` is the source;
the authored `fly.toml` is a new umbrella-root file.

| Section / key | Template `file:line` | Verdict |
|---|---|---|
| `app` | `:21` `codemoji-phoenix` | **SET → `"echo-portal"`** (OPERATOR-PINNED — distinct app, NOT the template's name) |
| `primary_region` | `:22` `fra` | **KEEP `"fra"`** (OPERATOR-PINNED, MATCHES) |
| `kill_signal`, `kill_timeout` | `:23-24` | **KEEP** |
| `[build] dockerfile` + `ELIXIR_VERSION`, `DEBIAN_DATE` | `:30`, `:34`, `:37` | **KEEP** |
| `OTP_VERSION` | `:35` `27.3.4.1` | **SET → `28.5.0.1`** (PINNED, Docker-Hub-verified; no `28.1` image) |
| `DEBIAN_DATE` | `:37` `20251117` | **SET → `20260518`** (the date in the pinned 28.5.0.1 tag) |
| `NODE_VERSION` | `:33` | **DROP** — no Node build |
| `[deploy] strategy = "rolling"` | `:42-43` | **KEEP** |
| `[experimental] auto_rollback` | `:124-125` | **KEEP** |
| `[env]` `PHX_HOST` | `:50` `jonnify.fly.dev` | **SET → `"echo-portal.fly.dev"`** (OPERATOR-PINNED — the Portal's OWN host; NOT the template's value, NOT `:deep_link_base_url`) |
| `[env]` `PHX_SERVER`/`MIX_ENV`/`PORT`/`POOL_SIZE` | `:51-54` | **KEEP** |
| `[env]` `ERL_INETRC`/`ECTO_IPV6`/`ERL_AFLAGS` (the IPv6/cluster trio) | `:70`, `:74`, `:78` | **KEEP** — libcluster substrate |
| `[env]` `TZ`/`LANG` | `:104-105` | **KEEP** |
| `[env]` `DEV_LIVE`, `LOG_LEVEL`, all Fastify (`FASTIFY_PORT`/`WORKER_*`/`QUEUE_*`/`NODE_*`/`WORKER_PATH`/`ENTRY_POINT`), SQLitestream (`DISTR_WATCH_*`/`PACKAGES_DB*`), Volume (`VOLUME_WATCH_*`), `GAME_DB_NAME`, all `DD_*` | `:56`, `:80-101`, `:106`, `:109`, `:111-120` | **DROP** |
| `[compute]`/`[[vm]]` memory | `:130-133` `memory_mb = 2048` | **SET → `512mb`** (OPERATOR-PINNED — single Phoenix release; distill cpu too) |
| `[[services]]` `internal_port = 4000` + auto_start/stop + `min_machines_running` | `:138-143` | **KEEP** (note the Presence/cluster trade on `min_machines_running`) |
| `[[services.ports]]` 80→443 `force_https` + TLS alpn | `:151-161` | **KEEP** |
| `[[services.http_checks]]` `GET /health` | `:170-176` | **KEEP** — MATCHES as-built `router.ex:113` (the alignment, INV3) |
| `[[services.tcp_checks]]`, `[services.concurrency]` | `:164-167`, `:145-148` | **KEEP (distill)** — optional tcp check; concurrency to Portal scale |
| the Codemoji header comments | `:1-19`, section banners | **DROP** |

## Requirements

- **F6.8.2-R1 (the scoped release).** Add a `releases/0` to `echo/mix.exs` (today `:18-20` `deps: []`, no
  `releases/0`) defining the Portal release **named `portal`** (OPERATOR-PINNED; independent of the fly app
  `echo-portal`) with `applications: [portal_web: :permanent, portal: :permanent, echo_data: :permanent]` —
  EXCLUDING `echo_bot`. `MIX_ENV=prod mix release portal` builds a self-contained release. [US: F6.8.2-US1]
- **F6.8.2-R2 (the Dockerfile, FROM the template).** Author `echo/Dockerfile` (a new file; the template is NOT
  edited) FROM `docs/deploy/Dockerfile.template`, corrected: build args `ELIXIR_VERSION=1.18.4` +
  `OTP_VERSION=28.5.0.1` + `DEBIAN_DATE=20260518` (the PINNED base tag `1.18.4-erlang-28.5.0.1-debian-bookworm-20260518`
  — no `28.1` image exists; drop `NODE_VERSION`); replace the `apps/echo` copy/build (`template:30-45`) with `COPY mix.exs mix.lock ./` +
  `COPY config config` + `COPY apps/portal apps/portal` + `COPY apps/portal_web apps/portal_web` + `COPY
  apps/echo_data apps/echo_data`, then `mix deps.get --only prod` → `mix deps.compile` → `mix compile` → `mix
  phx.digest` → `mix release portal` from the umbrella ROOT (NOT `cd apps/echo`); add a slim runtime stage
  (`FROM` a Debian-slim/the hexpm runtime base, install `openssl`/`libstdc++`/`ncurses` as needed, `COPY
  --from=build /app/_build/prod/rel/portal ./`, `EXPOSE 4000`, `ENV MIX_ENV=prod`, `ENTRYPOINT ["bin/portal",
  "start"]`). Exclude `apps/echo_bot` from every `COPY`. [US: F6.8.2-US2]
- **F6.8.2-R3 (the runtime config split + deploy-bind).** In `echo/config/runtime.exs`: change the prod endpoint
  bind from `http: [ip: {127, 0, 0, 1}, port: port]` (`:34`) to bind `{0, 0, 0, 0}` under prod/release (keep
  `127.0.0.1` for dev — branch on `config_env()`/an env flag); read `PHX_HOST` from env into the endpoint `url`
  host at boot (alongside the existing `SECRET_KEY_BASE` `:15-29` + `DATABASE_URL` `:43-58` raises). `PHX_HOST` is
  the Portal's OWN host (OPERATOR-PINNED `echo-portal.fly.dev`, set in `fly.toml`). LEAVE the `:deep_link_base_url`
  override mechanism (`:65-67`) AS-IS — it is the strangler-fig FALLBACK origin and STAYS `https://jonnify.fly.dev`
  (the `config.exs:46` default); the deploy does NOT set `DEEP_LINK_BASE_URL` (the Fiber site is unchanged). Two
  hosts, two keys — do not point deep_link at the Portal host. A missing required prod env var fails the boot
  loudly. [US: F6.8.2-US3]
- **F6.8.2-R4 (the prod compile-time config).** In `echo/config/prod.exs` (today only the event-store adapter):
  add `config :portal_web, PortalWeb.Endpoint, url: [host: …, port: 443, scheme: "https"], cache_static_manifest:
  "priv/static/cache_manifest.json"` and a `server`-from-`PHX_SERVER` toggle. No secret at compile time (INV2).
  [US: F6.8.2-US3]
- **F6.8.2-R5 (`Portal.Release`).** Create `echo/apps/portal/lib/portal/release.ex` (`Portal.Release`):
  `migrate/0` — `load_app()` (`Application.load(:portal)`), then for `Portal.Repo` run
  `Ecto.Migrator.with_repo(Portal.Repo, &Ecto.Migrator.run(&1, :up, all: true))`; `rollback/2` — `with_repo`
  running `:down, to: version`. Invoked via `bin/portal eval "Portal.Release.migrate()"`. It names `Portal.Repo`
  (the F6.3 repo), an ops task outside the web boundary. [US: F6.8.2-US4]
- **F6.8.2-R6 (libcluster).** Add `{:libcluster, "~> 3.3"}` to `echo/apps/portal_web/mix.exs` `deps/0` (the
  per-app edge) + `mix deps.get`; add a `{Cluster.Supervisor, [topology, [name: PortalWeb.ClusterSupervisor]]}`
  child to `PortalWeb.Application`'s supervision tree, with a Fly-suited topology (e.g.
  `Cluster.Strategy.DNSPoll` over `<app>.internal`, or `Cluster.Strategy.Gossip`) read from config. The web
  reaches PubSub/Presence ONLY through `Portal.subscribe/1`/`Portal.broadcast/2` — clustering adds no web→engine
  path. [US: F6.8.2-US5]
- **F6.8.2-R7 (per-machine `worker_id`).** In `echo/apps/portal/lib/portal/id.ex`, derive `@node` (today `1`,
  `:13`) per machine via `:erlang.phash2(System.get_env("FLY_MACHINE_ID"), 1024)` (OPERATOR-PINNED: the
  `FLY_MACHINE_ID` hash, topology-agnostic — a `|| node()` fallback for a non-Fly boot is acceptable but the
  pinned source is `FLY_MACHINE_ID`) — so two machines minting in the same ms do not collide. Single-node behavior
  is unchanged. Note `unique_constraint(:id)` on the catalog write path as a complementary guard. [US: F6.8.2-US6]
- **F6.8.2-R8 (the distilled fly.toml).** Author `echo/fly.toml` (a new umbrella-root file; the template is NOT
  edited) per the keep/drop table above — the Portal essentials, the `/health` check (matching as-built
  `router.ex:113`), all Codemoji config dropped. Apply the OPERATOR PINS: `app = "echo-portal"`,
  `primary_region = "fra"`, `[[vm]] memory = "512mb"`, `PHX_HOST = "echo-portal.fly.dev"` (the Portal's own host —
  NOT `jonnify.fly.dev`, and NOT the deep-link base). Validate with `fly config validate` (if available). [US:
  F6.8.2-US7]
- **F6.8.2-R9 (the strangler-fig boundary + BUILD-LOCAL verify).** State the routes the Portal owns vs. the Fiber
  fallback (D9); KEEP `:deep_link_base_url` at the `config.exs:46` default `jonnify.fly.dev` (the deploy does NOT
  override it). Verify BUILD-LOCAL: `MIX_ENV=prod mix release portal` builds; `bin/portal eval
  "Portal.Release.migrate()"` runs against a prod-config DB; `fly config validate` passes; a local prod boot
  answers `/health` 200. The live `fly deploy` is [DEFERRED to the Operator]. [US: F6.8.2-US8]
- **F6.8.2-R10 (dev toolchain bump).** Bump `echo/.tool-versions` `erlang 28.1` → `28.5.0.1` (Elixir `1.18.4`
  unchanged) so the dev toolchain matches the PINNED prod image tag. This is a BUILD-TIME edit (NOT pre-build) —
  bumping it before the rung builds would break the Operator's current local `28.1` env. [US: F6.8.2-US2; D11]

## Execution topology

```text
Build / deploy pipeline (the Operator runs the final fly deploy; F6.8.2 ships the artifacts):

  Dockerfile (FROM template, 1.18.4/28.5.0.1, de-Codemoji'd)   # tag 1.18.4-erlang-28.5.0.1-debian-bookworm-20260518
    build stage  : COPY root mix.exs/mix.lock/config + apps/{portal,portal_web,echo_data}   # NOT apps/echo_bot
                   mix deps.get --only prod -> deps.compile -> compile -> phx.digest -> mix release portal   # umbrella ROOT
    runtime stage: COPY --from=build _build/prod/rel/portal -> EXPOSE 4000 -> ENTRYPOINT bin/portal start
  .tool-versions: erlang 28.1 -> 28.5.0.1   # dev aligns to prod image (BUILD-time edit)

  releases/0 (root mix.exs): portal = [portal_web, portal, echo_data]   # echo_bot EXCLUDED

  boot sequence (Operator, on Fly):
    bin/portal eval "Portal.Release.migrate()"   # Ecto.Migrator on Portal.Repo, BEFORE serving
      -> bin/portal start
           -> runtime.exs: SECRET_KEY_BASE/DATABASE_URL/PHX_HOST from env; bind {0,0,0,0} (prod); deep_link_base set
           -> prod.exs: endpoint url(https:443) + cache_static_manifest + server<-PHX_SERVER
           -> PortalWeb.Application: [Telemetry, Endpoint, Cluster.Supervisor(topology)]   # libcluster
                -> Phoenix.PubSub + PortalWeb.Presence span nodes (F6.7 single-node -> cluster-correct)
           -> Portal.ID worker_id = phash2(FLY_MACHINE_ID)   # multi-node mint-safe

  strangler-fig (prod): Portal owns / /elixir /course/agile-agent-workflow /login /auth/* live-/courses /my/courses /health
                        deep /elixir/<…> /course/<…> -> Fiber via :deep_link_base_url
```

```text
Task DAG:
  T1  root mix.exs releases/0 (portal = portal_web/portal/echo_data; echo_bot excluded)
  T2  config split: runtime.exs (bind {0,0,0,0} prod / 127.0.0.1 dev; PHX_HOST=echo-portal.fly.dev; deep_link_base STAYS jonnify.fly.dev) + prod.exs (url/manifest/server)
  T3  Portal.Release (migrate/0 + rollback/2 via Ecto.Migrator)        [needs Portal.Repo — F6.3, present]
  T4  libcluster: apps/portal_web/mix.exs {:libcluster,…} + mix deps.get + Cluster.Supervisor child in PortalWeb.Application
  T5  Portal.ID @node -> per-machine (phash2 FLY_MACHINE_ID); single-node unchanged
       └─▶ T6  Dockerfile (FROM template; 1.18.4/28.5.0.1; umbrella-root build of the 3 apps; runtime stage + ENTRYPOINT)
                + .tool-versions erlang 28.1 -> 28.5.0.1 (dev aligns to prod image)
            └─▶ T7  fly.toml (distilled per the keep/drop table; app=echo-portal; PHX_HOST=echo-portal.fly.dev; 512mb; /health check; Codemoji dropped)
                 └─▶ T8  verify BUILD-LOCAL (mix release portal; bin/portal eval migrate; fly config validate; /health 200)
```

Verify (T8, BUILD-LOCAL — the live `fly deploy` DEFERRED to the Operator): `MIX_ENV=prod mix release portal`
builds clean and lists `portal_web`/`portal`/`echo_data` (NOT `echo_bot`); `bin/portal eval
"Portal.Release.migrate()"` runs against a prod-config DB (the migration applies); `fly config validate` passes
(or a schema review); a local prod-config boot answers `GET /health` 200; the `docs/deploy/` templates are
unedited; a boundary grep shows the web still reaches PubSub/Presence only via the facade; the F5
engine/store/contexts/branded-id format are byte-unchanged (the per-machine `worker_id` is a runtime-derived
value, the only id change).

## Touched files (EXACT)

- **edit** — `echo/mix.exs` (add `releases/0` — the Portal release named `portal`, `echo_bot` excluded).
- **edit** — `echo/.tool-versions` (`erlang 28.1` → `28.5.0.1`; dev aligns to the prod image — D11/R10/AS9; a
  BUILD-time edit, NOT pre-build).
- **new** — `echo/Dockerfile` (FROM `docs/deploy/Dockerfile.template`; 1.18.4 / OTP `28.5.0.1` — tag
  `1.18.4-erlang-28.5.0.1-debian-bookworm-20260518`; umbrella-root build of `portal`/`portal_web`/`echo_data`;
  runtime stage + `EXPOSE 4000` + `ENTRYPOINT bin/portal start`).
- **new** — `echo/fly.toml` (distilled from `docs/deploy/fly.toml` per the keep/drop table; `app = "echo-portal"`,
  `PHX_HOST = "echo-portal.fly.dev"`, `memory = "512mb"`; the `/health` check; Codemoji config dropped).
- **edit** — `echo/config/runtime.exs` (the prod deploy-bind `{0,0,0,0}` / dev loopback; `PHX_HOST` into the url;
  `:deep_link_base_url` LEFT at the `config.exs:46` default `jonnify.fly.dev` — not overridden).
- **edit** — `echo/config/prod.exs` (the endpoint `url`/`cache_static_manifest`/`server`-toggle; the event-store
  adapter line stays).
- **new** — `echo/apps/portal/lib/portal/release.ex` (`Portal.Release`: `migrate/0` + `rollback/2`).
- **edit** — `echo/apps/portal_web/mix.exs` (`{:libcluster, "~> 3.3"}` in `deps/0`) + **edit** `echo/mix.lock`
  (the resolved libcluster dep — `mix deps.get`).
- **edit** — `echo/apps/portal_web/lib/portal_web/application.ex` (the `Cluster.Supervisor` child).
- **edit** — `echo/apps/portal/lib/portal/id.ex` (the per-machine `worker_id` derivation; `@node`).
- **new (optional)** — a release/migrate test (`apps/portal/test/portal/release_test.exs`) or a config smoke; the
  primary gate is the BUILD-LOCAL recipe.
- **unchanged / do NOT touch** — `docs/deploy/Dockerfile.template` + `docs/deploy/fly.toml` (reference INPUTS);
  `apps/echo_bot` (the out-of-band F10 bot, excluded from the release); EVERYTHING below the facade except the
  per-machine `worker_id` (the engine, `EventStore` port, catalog/learning/accounts contexts, the store, the
  branded-id format — byte-unchanged); the auth surface (F6.8.1).

## Agent stories

- **F6.8.2-AS1** [implements R1; US1] — Directive: add `releases/0` to `echo/mix.exs` defining the Portal release
  **named `portal`** (OPERATOR-PINNED; independent of the fly app `echo-portal`) listing `[portal_web: :permanent,
  portal: :permanent, echo_data: :permanent]`, EXCLUDING `echo_bot`. Acceptance gate: `MIX_ENV=prod mix release
  portal` builds clean; the assembled release's app list includes `portal_web`/`portal`/`echo_data` and NOT
  `echo_bot`. Invariant: the release is Portal-scoped (INV6).
- **F6.8.2-AS2** [implements R3, R4; US3] — Directive: complete the config split. In `runtime.exs`: bind
  `{0,0,0,0}` under prod/release (`127.0.0.1` for dev), read `PHX_HOST` (the Portal's own host, `echo-portal.fly.dev`)
  into the endpoint `url`. LEAVE `:deep_link_base_url` (`:65-67`) AS-IS — it STAYS `jonnify.fly.dev` (the Fiber
  fallback origin, `config.exs:46` default); do NOT set `DEEP_LINK_BASE_URL` for the deploy. In `prod.exs`: add the
  endpoint `url`(https:443)/`cache_static_manifest`/`server`-from-`PHX_SERVER`. Acceptance gate: a prod-config boot
  with the env set binds `{0,0,0,0}` and reads the host; a boot with a missing `SECRET_KEY_BASE`/`DATABASE_URL`
  raises loudly; no secret literal lives in `config.exs`/`prod.exs`; `:deep_link_base_url` resolves to
  `jonnify.fly.dev` (unchanged). Invariant: config by phase, the deploy bind reachable, dev loopback (INV2, INV3).
- **F6.8.2-AS3** [implements R5; US4] — Directive: create `echo/apps/portal/lib/portal/release.ex`
  (`Portal.Release`) with `migrate/0` (`Ecto.Migrator.with_repo(Portal.Repo, …run :up, all: true)`) + `rollback/2`.
  Acceptance gate: `bin/portal eval "Portal.Release.migrate()"` runs against a prod-config DB and applies the
  migration without `mix`; `migrate/0` names `Portal.Repo`. Invariant: release-native migration, before serving
  (INV1).
- **F6.8.2-AS4** [implements R6; US5] — Directive: add `{:libcluster, "~> 3.3"}` to `apps/portal_web/mix.exs` +
  `mix deps.get`; add a `Cluster.Supervisor` child (a Fly-suited topology from config) to `PortalWeb.Application`.
  Acceptance gate: `apps/portal_web/mix.exs deps/0` declares `libcluster`, present in `mix.lock`;
  `PortalWeb.Application` starts the `Cluster.Supervisor` child; a boundary grep confirms the web reaches
  PubSub/Presence only via `Portal.subscribe/1`/`Portal.broadcast/2`. Invariant: cluster-correct real-time, the
  facade unbreached (INV4).
- **F6.8.2-AS5** [implements R7; US6] — Directive: derive `Portal.ID`'s `@node` per machine (from
  `FLY_MACHINE_ID`, hashed into 1024). Acceptance gate: two simulated machine identities yield different
  `worker_id`s; single-node mint behavior is unchanged; the branded-id format/namespaces/`Portal.ID` surface are
  otherwise unchanged. Invariant: the id mint is multi-node-safe (INV5).
- **F6.8.2-AS6** [implements R2; US2; depends on AS1] — Directive: author `echo/Dockerfile` FROM the template,
  corrected (the PINNED tag `1.18.4-erlang-28.5.0.1-debian-bookworm-20260518` — `OTP_VERSION=28.5.0.1`,
  `DEBIAN_DATE=20260518`, drop `NODE_VERSION`), the `apps/echo` build replaced by the umbrella-root build of
  `portal`/`portal_web`/`echo_data` (`mix deps.get --only prod` → `deps.compile` → `compile` → `phx.digest` →
  `mix release portal`), a runtime stage + `EXPOSE 4000` + `ENTRYPOINT bin/portal start`. Acceptance gate: the
  Dockerfile builds the image (or, if a local Docker build is unavailable, a line-by-line review confirms the
  version args = `28.5.0.1`/`20260518`, the umbrella-root build of the 3 apps excluding `echo_bot`, and the
  runtime stage); `docs/deploy/Dockerfile.template` is unedited. Invariant: the release is Portal-scoped, the
  template is an input (INV6).
- **F6.8.2-AS9** [implements R10; US2; D11] — Directive: bump `echo/.tool-versions` `erlang 28.1` → `28.5.0.1`
  (Elixir `1.18.4` unchanged) so dev matches the PINNED prod image tag. A BUILD-time edit (NOT pre-build).
  Acceptance gate: `echo/.tool-versions` reads `erlang 28.5.0.1`; the umbrella compiles + the suite is green under
  it; the dev erlang now equals the Dockerfile `OTP_VERSION`. Invariant: dev/prod toolchain parity (RECONCILE 1).
- **F6.8.2-AS7** [implements R8; US7; depends on AS6] — Directive: author `echo/fly.toml` distilled per the
  keep/drop table — the Portal essentials with the OPERATOR PINS (`app = "echo-portal"`, `primary_region = "fra"`,
  `[[vm]] memory = "512mb"`, `PHX_HOST = "echo-portal.fly.dev"`), the `GET /health` check (matching as-built
  `router.ex:113`), all Fastify/Redis/SQLitestream/Datadog/`GAME_DB`/Codemoji config dropped. Acceptance gate:
  `fly config validate` passes (or a schema review); `app == "echo-portal"`, `PHX_HOST == "echo-portal.fly.dev"`
  (and NOT confused with `:deep_link_base_url`, which stays `jonnify.fly.dev`), `memory == "512mb"`; the `/health`
  path matches `router.ex:113`; a grep for `WORKER_`/`DD_`/`REDIS`/`FASTIFY`/`SQLITE` finds nothing;
  `docs/deploy/fly.toml` is unedited. Invariant: the template is an input (INV6).
- **F6.8.2-AS8** [implements R9; verification] — Directive: state the strangler-fig boundary (two hosts:
  `PHX_HOST = echo-portal.fly.dev` vs. `:deep_link_base_url` STAYS `jonnify.fly.dev`, NOT overridden); verify
  BUILD-LOCAL. Acceptance gate: `MIX_ENV=prod mix release portal` builds; `bin/portal eval
  "Portal.Release.migrate()"` runs against a prod-config DB; `fly config validate` passes; a local prod boot
  answers `/health` 200; the live `fly deploy` is [DEFERRED to the Operator]; the F5 engine/domain are unchanged;
  a boundary grep confirms the web calls only the facade. Invariant: every Deliverable → a story; every
  Requirement cites a real arity; `Portal.Release`/`libcluster`/`Cluster.Supervisor` are NET-NEW (defined, not
  assumed); the engine is unchanged (INV7).

## Paste-ready prompt

> Build **F6.8.2 · Deployment** in the Portal umbrella (`/Users/jonny/dev/jonnify/echo`), per [`f6.8.2.md`](f6.8.2.md)
> (authoritative), [`f6.8.2.stories.md`](f6.8.2.stories.md) (acceptance), and this brief. **Framing (propagate to
> any sub-agent): no gendered pronouns for agents; no perceptual or interior-state verbs for tools or agents; no
> first-person narration.**
>
> **The live deploy is DEFERRED to the Operator** — the Operator creates the Fly app + machines and runs `fly
> deploy`. This rung ships the ARTIFACTS, gated BUILD-LOCAL. **Do NOT edit the Operator's templates** —
> `docs/deploy/Dockerfile.template` and `docs/deploy/fly.toml` are reference INPUTS you build FROM. **Do NOT touch
> `apps/echo_bot`** (the out-of-band F10 bot, excluded from the Portal release). **All forks are OPERATOR-PINNED
> (no open decisions):** release name `portal` (≠ the fly app `echo-portal`); OTP Docker tag
> `1.18.4-erlang-28.5.0.1-debian-bookworm-20260518` (`OTP_VERSION=28.5.0.1`, `DEBIAN_DATE=20260518` — no `28.1`
> image exists); `worker_id` = `:erlang.phash2(System.get_env("FLY_MACHINE_ID"), 1024)`; fly `app = "echo-portal"`,
> `PHX_HOST = "echo-portal.fly.dev"` (≠ `:deep_link_base_url`, which STAYS `jonnify.fly.dev`), `memory = "512mb"`,
> `primary_region = "fra"`.
>
> Do, in order: **(1)** Add a scoped `releases/0` to the root `echo/mix.exs` — `portal = [portal_web, portal,
> echo_data]` `:permanent`, EXCLUDING `echo_bot`. **(2)** Complete the config split: in `runtime.exs` bind
> `{0,0,0,0}` under prod (keep `127.0.0.1` for dev), read `PHX_HOST` (`echo-portal.fly.dev`) into the endpoint
> `url`; LEAVE `:deep_link_base_url` at the `config.exs:46` default `jonnify.fly.dev` (do NOT override it); in
> `prod.exs` add the endpoint `url`(https:443)/`cache_static_manifest`/`server`-from-`PHX_SERVER`.
> **(3)** Create `apps/portal/lib/portal/release.ex` (`Portal.Release.migrate/0` via `Ecto.Migrator` on
> `Portal.Repo` + `rollback/2`). **(4)** Add `{:libcluster, "~> 3.3"}` to `apps/portal_web/mix.exs` + `mix
> deps.get` + a `Cluster.Supervisor` child (a Fly topology from config) in `PortalWeb.Application`. **(5)** Derive
> `Portal.ID`'s `@node` per machine (`phash2(FLY_MACHINE_ID, 1024)`); single-node behavior unchanged. **(6)**
> Author `echo/Dockerfile` FROM the template (the pinned tag `1.18.4-erlang-28.5.0.1-debian-bookworm-20260518`,
> drop `NODE_VERSION`, the `apps/echo` build → the umbrella-root build of the 3 apps, a runtime stage + `EXPOSE
> 4000` + `ENTRYPOINT bin/portal start`) AND bump `echo/.tool-versions` `erlang 28.1` → `28.5.0.1` so dev matches
> prod. **(7)** Author `echo/fly.toml` distilled per the keep/drop table (`app = "echo-portal"`,
> `PHX_HOST = "echo-portal.fly.dev"`, `memory = "512mb"`, the `/health` check, Codemoji config dropped). **(8)**
> State the strangler-fig boundary + verify BUILD-LOCAL (`MIX_ENV=prod mix release portal` builds; `bin/portal
> eval "Portal.Release.migrate()"` runs; `fly config validate`; a local prod boot answers `/health` 200).
>
> **Discipline:** `Portal.Release`/`libcluster`/`Cluster.Supervisor` are NET-NEW — define them; `Portal.Repo`
> (F6.3), `Portal.ID` (`id.ex`), `Portal.subscribe/1`/`Portal.broadcast/2` (the F6.7 facade) ALREADY EXIST — cite,
> don't reinvent. The web reaches PubSub/Presence ONLY through the facade — clustering adds no web→engine path
> (the master invariant). Cite real arities; invent no arity/flag/topology. Touch NOTHING below the facade except
> the per-machine `worker_id`. The live `fly` actions are the Operator's; ship the artifacts.

---

Spec: [`f6.8.2.md`](f6.8.2.md) · Stories: [`f6.8.2.stories.md`](f6.8.2.stories.md) · Parent:
[`f6.8.md`](f6.8.md) · Auth sibling: [`f6.8.1.md`](f6.8.1.md) · Capstone (runs under this deploy):
[`f6.9.md`](f6.9.md) · Approach: [`../specs.approach.md`](../specs.approach.md).
