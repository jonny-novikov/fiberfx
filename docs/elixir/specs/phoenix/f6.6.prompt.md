Director's F6.6 ship plan (lead-team over the specialized agents)

Stage 1 — venus (reconcile + author the brief):
subagent_type: venus
Rung F6.6 (LiveView) — make F6.5's catalog interactive. F6.5 shipped at 9e49dab;
the triad is docs/elixir/specs/phoenix/f6.6.{md,stories.md,llms.md} (groomed: a
[RECONCILE] callout; the .stories.md is already Connextra+GWT+Coverage).

Reconcile F6.6 against the as-built F6.5 surface (CatalogComponents, CourseController,
the Portal facade, the router) and author the .llms.md brief. SURFACE these two forks
to the Director — do not decide them:
1. Does GET /courses become `live "/courses", CatalogLive` (replacing
CourseController.index), or does the live catalog sit elsewhere?
2. Live search: add a Portal facade search/filter fn (the master invariant forbids
the LiveView naming Catalog), or ratify a LiveView→context relaxation?

Stage 2 — mars (Mars-1, build):
subagent_type: mars
Build F6.6 to Venus's brief [paste Venus's ground-facts contract + the two resolved
forks]. Get it compiling clean.
Surface: CatalogLive (mount/3 two-stage + connected?, handle_event/3); live search
(phx-change → re-assign @courses); live create (phx-submit → Portal.create_course/1,
flash on {:ok}, to_form(cs) inline errors on {:error}); streams (stream/3 +
stream_insert/3 + phx-update="stream"); the live route; reuse the F6.5 course_card.

Stage 3 — mars (Mars-2, harden):
subagent_type: mars
Harden Mars-1's F6.6 tree. Add LiveViewTest coverage to the Given/When/Then in
f6.6.stories.md (render_change for search, render_submit for create ok+error, the
stream, the two-stage mount). Idiom audit. mix test green + the determinism loop.

Stage 4 — apollo (verify):
subagent_type: apollo
Reconcile-post F6.6: as-built vs the f6.6 deliverables/invariants + the GWT acceptance
in f6.6.stories.md. Re-run the gate. Spec-sync f6.6.md to as-built. Verdict.

Stage 5 — Director:
Review Apollo's verdict → one LAW-4 commit (echo/apps/portal_web/ + f6.6.md), explicit pathspec, excluding html/ work.

Stage 6 - Director:
Reconcile f6.7 - f6.9 on completion having findings and learnings (Agile Agents Feedback Loop) 