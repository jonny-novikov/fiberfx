# F6 · Progress — the Phoenix web ladder, rung by rung

> The append-only retrospective for the F6 (Phoenix) value ladder — the sibling of
> [`../pragmatic/f5.progress.md`](../pragmatic/f5.progress.md) for the F5 engine arc. One entry per shipped
> rung, written at its close from the Apollo verification record: what went exceptionally, what went well,
> what did not, the process opportunities, the gaps carried forward, and the spec / agent-definition
> refinements the rung earned. The per-rung specs are the source of truth (`f6.N.{md,stories.md,llms.md}`);
> this file records how the build *ran* and the lessons folded forward, so the next stateless spawn inherits
> them.
>
> F6.1–F6.5 shipped before this ledger opened; the record begins at **F6.6** — the first rung run by the
> `venus` / `mars` / `apollo` lead-team under the reconcile + mentoring loop.

## F6.7 — Real-time (PubSub & Presence) · BUILD-GRADE on deliverables; one PRE-EXISTING suite-determinism flake surfaced (routed to the Director)

**Slice.** Push F6.6's per-socket catalog state across every connected client. The `Portal` facade gains `subscribe/1`
+ `broadcast/2` over a supervised `{Phoenix.PubSub, name: Portal.PubSub}` (built as `def`, not `defdelegate` — no
`Portal.*` target); `Portal.Catalog` gains `update_course/2` (the ratified US0 write, the `create_course/1` mirror with
`Repo.update/1`) + a private ok-only `broadcast/2` helper wired into BOTH write paths; `CatalogLive` subscribes on its
connected mount, applies `stream_insert/3` in three `handle_info/2` clauses (create `at: 0`, update replace, both gated
to the active `@query` via `matches?/2`; `"presence_diff"` recomputes `@viewers`); `PortalWeb.Presence` (web-tier child)
gives a CRDT-backed viewer count. D6 (channel) DEFERRED (the `could`-priority US5, skipped).

**Team & gate.** Venus (reconcile + brief) → Mars-1 (build) → Mars-2 (harden: the 8 F6.7 GWT tests + the
committed-DB `delete_all` isolation) → Apollo (verify). **Verdict: BUILD-GRADE on every F6.7 DELIVERABLE** — every
D1–D7 / INV1–INV6 / GWT promise is MATCH or explicit DEFERRED (D6); zero STALE/INVENTED/MISSING in F6.7's own surface.
Gate reproduced independently: `mix compile --warnings-as-errors` exit 0 (forced full, all four apps, no warnings);
single suite run echo_data 2 · **portal 11 doctests / 4 properties / 87** · portal_web 47 · echo_bot 28 (the Operator's
F10 app, in-umbrella) — **0 failures**. Liveness GREEN against the Director's durable `:4000`: `/health` 200,
`/courses` 200 with `<p data-viewers>0 viewing</p>` (0 is correct for a socketless `curl` — the disconnected first
paint; a live socket recomputes to 1).

**The determinism finding — a PRE-EXISTING harness race F6.7 exposed, not a deliverable defect.** The ≥100 loop on the
FULL `portal_web` suite did NOT pass clean: round 1 **42/100** (fail @ 43), round 2 **14/100** (fail @ 15), each the
SAME failure — `PortalWeb.CourseControllerTest` `POST /courses` raising `(ArgumentError) the table identifier does not
refer to an existing ETS table` on `:ets.lookup(PortalWeb.Endpoint, :secret_key_base)`. Root-caused: the F6.1
`EndpointSupervisionTest` brutally kills the shared named endpoint (`Process.exit(ep, :kill)`, the only `Process.exit`
on it) and polls `/health` until 200 — but a healthy `/health` proves the endpoint PROCESS is back, NOT that its ETS
config table is repopulated, and the ~200-restart storm is async to the test process, so a following controller test can
read `:secret_key_base` during the teardown window. **Discriminator (decisive): the same suite with ONLY
`endpoint_supervision_test.exs` excluded ran 60/60 GREEN** — every F6.7 test (two-LiveView cross-client, broadcast
honesty, Presence count, the live-search × broadcast gate) is deterministic. So the flake is 100% the endpoint-kill
test racing other endpoint readers (the documented `portal-web-endpoint-kill-restart-storm` hazard class), which F6.7
WIDENED by adding `live/2` socket tests (16 call sites) to the shared-endpoint suite but did not author or cause.
**The gate is reported AS REPRODUCED, not as Mars reported it** (Mars-2 reported 100/100 twice; the independent loop is
the gate — echo/CLAUDE.md §4 — and it flaked). Verdict held BUILD-GRADE on deliverables with the flake surfaced as a
carried gap routed to the Director (the fix is a test-harness change, not an F6.7 code change).

### Went exceptionally well
- **The lag-1 reconcile (Venus, pre-build) caught two would-be STALEs before Mars touched code.** US0's
  `update_course/2` did not exist (F6.4/F6.5 ship only an arity-0 `change_course/0`) — pinned as the one ratified new
  write, the exact `create_course/1` mirror, so Mars built it without inventing a surface. And the Presence-child
  TREE-PLACEMENT (`PortalWeb.Application`, the web tier — NOT `Portal.Application`) was pinned with the boot-order
  rationale (`:portal_web` → `:portal` so `Portal.PubSub` is up first), so the as-built child landed in the right tree
  first try. INV5 verified: exactly two new children, correct trees, no third.
- **The G3 standing-liveness guardrail FIRED CLEAN — the first PROCESS-rung where it did.** Mars handed the Director the
  boot command and respected the durable `:4000`; Apollo verified against THAT node (never booted an ephemeral one),
  and the node outlived the verification turn. The AAW-parity micro-sharpen's premise held in the field: the agent's
  sole path is the handoff. (Loop-closure: the predicted failure — a dead `:4000` at Operator-probe time — stayed away.)
- **The dep-edge was caught and correctly categorized, not waved through.** The brief said "no new dependency"; the
  as-built `apps/portal/mix.exs` GAINED `{:phoenix_pubsub, "~> 2.1"}`. Apollo verified `mix.lock` is unchanged BY F6.7
  (the +2 lines in the working-tree `mix.lock` are the Operator's `yaml_elixir`/`yamerl` for echo_bot — OUT-OF-BAND),
  that `phoenix_pubsub` is framework-free (empty dep list), and that the `:portal`-scoped `mix deps.tree` carries it but
  NO web framework — so F6.1-INV1's INTENT holds. The "no new dependency" claim was a STALE *mechanism* fact:
  transitive-lock presence ≠ per-app dep-graph visibility in an umbrella.

### Went well
- **Facade-only held (INV2), proven by a DOC-STRIPPED grep.** The naive grep matched only `@moduledoc`/comment prose
  (lines that legitimately say "never `Phoenix.PubSub`"); the doc-stripped code lines name only `Portal.subscribe`,
  `PortalWeb.Presence.track`, `PortalWeb.Presence.list` — zero `Phoenix.PubSub`/`Portal.broadcast`/`Repo`/`Engine`/
  `GenServer.call`. The test file's own `strip_docs_and_comments/1` guard asserts the same independently.
- **INV1 (broadcast only on success) held by READING the helper, not trusting the suite.** `catalog.ex`'s `broadcast/2`
  has exactly two clauses: `{:ok, %Course{}}` fires, `{:error, %Ecto.Changeset{}}` passes through silently. No
  unconditional or `{:error}` broadcast path. The ok-only helper is the single place both writes broadcast from.
- **course_card reuse (INV6).** The only `<.course_card>` call site is in `render/1`; `handle_info/2` feeds the SAME
  `:courses` stream the render iterates — no divergent row markup in the handlers. `stream_insert` keys on the course
  id (create `at: 0`, update no-`at:`), so a re-delivery replaces rather than duplicates.
- **No inert doctest.** `update_course/2`'s moduledoc `iex>` example is WIRED — `catalog_test.exs:14` invokes `doctest
  Portal.Catalog`, and the doctest count rose 3→4 when run alone, so it actually executes (the F6.6 inert-doctest
  lesson held).

### Went not well
- **The full-suite ≥100 gate is non-deterministic — and the implementer's report said otherwise.** Mars-2 reported
  "100/100 twice, isolated"; the independent loop flaked at 42/100 and 14/100 on the same machine. Whatever the cause
  (load-sensitivity, a narrower scope, or luck), this re-confirms the arc's standing rule: the implementer's single (or
  even repeated) run is NOT the gate — the verifier's independent loop is, and it must be reported as found. The flake
  is a pre-existing harness race, but a green-claim that the independent loop contradicts is itself the finding.
- **A coarse failure-grep on the loop logs false-flagged passing iterations.** Apollo's first `grep -l "Error"` over the
  loop logs matched the word "Error" in passing iterations' deliberately-exercised changeset-error stacktraces
  (the title-only-submit test) and LiveView debug logs — three "failed" iterations that all ended `0 failures`. Caught
  by reading the hit + the loop's own `|| break` (the authoritative signal). The "a grep verdict is only as trustworthy
  as its pattern" class, turned inward on the verifier again — disambiguated, not vouched.

### Opportunities to improve (process)
- **The endpoint-kill test should not share a suite run with endpoint-readers under the determinism loop.** The race is
  inherent to brutally killing a shared named endpoint mid-suite; `/health`-polling proves process-liveness but not
  ETS-config readiness. Candidate fixes (the Director/Operator owns the pick — a test-harness change, not F6.7 code):
  run `endpoint_supervision_test.exs` in its OWN `mix test` invocation (a tag + a separate loop step), OR have its
  `eventually/2` also poll a `:secret_key_base`-reading endpoint call (a `dispatch`), not only `/health`, before the
  test returns, so the storm has drained before the next test runs. This is the determinism-gate analog of the F6.6
  PK-collision `delete_all` fix: make the loop deterministic with NO production change.

### Gaps carried forward
- **The full `portal_web` determinism loop is RED until the endpoint-kill harness coupling is addressed** (above) —
  ~3–7%/run, the `EndpointSupervisionTest` ETS-teardown race; F6.7 deliverables are 60/60 clean without it. Routed to
  the Director as a test-harness item (NOT an F6.7 code defect). The next process-rung's loop will keep flaking until
  fixed.
- **The dynamic-UI styling reckoning is STILL unbuilt** (unchanged since F6.5.5) — `CatalogLive` stays UNSTYLED; the
  broadcast row renders through the same bare `course_card` the mount uses (F6.7 owns consistency, not styling). The
  root layout + shared token stylesheet + `CoreComponents` reckoning come due together at F6.8.
- **The CRS PK-collision in PRODUCTION stays unguarded** (carried from F6.6) — `create_course/1` (and now
  `update_course/2`'s create-mirror sibling) mints outside the single-writer engine; routed to F6.8 (`@node = 1` → per
  machine).

### Spec & agent-definition refinements
- **`f6.7.md` synced to as-built** (Apollo, keeper role): a POST-BUILD `[RECONCILE]` record added (the `def`-not-
  `defdelegate` realization; the `mix.exs` dep-edge with `mix.lock` unchanged; D6 DEFERRED; the explicit note that
  config/mix.lock/echo_bot working-tree entries are the Operator's OUT-OF-BAND F10 work); D1's "two bare defdelegate-
  style one-liners" → `def`; the two "no new dependency" claims → the mechanism correction; D6 → DEFERRED.
- **`f6.7.llms.md` synced** — the References dep claim + the touched-files "no mix.exs change" corrected to the as-built
  one-line edge; D6 DEFERRED noted. (The historical implementation-prompt block left verbatim — it is the input Mars
  consumed; the authoritative body carries the corrected record.)
- **`venus.md` — a dep-graph-visibility guardrail PROPOSED (propose-only; Operator-ratified, Director-applied).** "No
  new dependency" is a per-app DEP-GRAPH-VISIBILITY claim (does THIS umbrella app DECLARE the edge?), not merely a
  `mix.lock`-presence claim — a module locked transitively is not compile-visible to an app that does not declare it.
  This is the umbrella-specific sibling of the existing "mechanism words are claims too" / "probe-not-config-read"
  reconcile lines; it SHARPENS the reconcile discipline for the dep-graph case rather than stacking a style rule. Cited
  to F6.7. Exact diff in the Apollo report to the Director.
- **`mars.md` — none owed.** The G3 standing-liveness line (the AAW-parity sharpen, if applied) fired clean; the
  cite-don't-invent line held (Mars flagged the `def`-not-`defdelegate` realization rather than inventing a delegate
  target — exactly the "realization over literal" discharge). No recurring Mars failure this rung. The full-suite flake
  is a harness race, not a Mars build defect — no builder guardrail is the right instrument for it.
- **`apollo.md` — none owed.** The doc-stripped-grep and read-the-hit disciplines fired (caught my own coarse-grep
  false flag); the independent-loop-is-the-gate rule fired (reported the flake against Mars's green claim). The verdict
  firewall held: the determinism flake is surfaced as a carried gap, the grade held BUILD-GRADE on deliverables, and
  neither was traded for the other.

## F6.6 — LiveView (the interactive catalog) · shipped `3cf2480`

**Slice.** `PortalWeb.CatalogLive` makes F6.5's static `/courses` interactive without a reload — live search
(`phx-change` → `Portal.search_courses/1`) and live create (`phx-submit` → `Portal.create_course/1`) over a
`stream`, reusing the F6.5 `course_card`; `live "/courses"` supersedes the static index. One ratified facade
addition: `Portal.search_courses/1` → `Catalog.search_courses/1 :: [Course.t()]` (read-only).

**Team & gate.** Venus (reconcile + brief) → Mars-1 (build) → Mars-2 (harden) → Apollo (verify:
BUILD-GRADE, every promise MATCH). Gate: compile `--warnings-as-errors` clean; echo_data 2 · portal 10
doctests / 4 properties / 86 · portal_web 37; **determinism 100/100** (Apollo) + 30/30 (Director re-run) —
this rung is id-touching (`CRS` mint + `Repo.insert`), so the ≥100 loop is the real gate.

### Went exceptionally well
- **Shift-left caught the `@enforce_keys` trap pre-build.** Venus's lag-1 reconcile corrected the brief's
  `change_course/1` → arity-0 `change_course/0` (a bare `%Course{}` literal under `@enforce_keys` does not
  compile) BEFORE Mars touched code — the F6.5 mid-build correction, retired one rung earlier.
- **The ≥100 determinism loop caught a real §4 PK-collision** the multi-seed run missed: `create_course/1`
  mints outside the single-writer engine and `courses` has no `unique_constraint(:id)`, so two same-ms seq-0
  mints RAISE `Ecto.ConstraintError`. The 100-loop failed at iteration 4 while all five seeds passed — the
  canonical §4 signature. Held for the suite with a per-test `delete_all(Course)`.
- **Independent convergence.** Apollo's spawn and the Director's ratification surfaced the SAME findings (the
  inert doctest, the `@courses`-as-assign drift) from separate contexts — agreement that is evidence, not an
  echo chamber.

### Went well
- **Facade-only held (INV1).** A grep of the CatalogLive code region is clean — no `Portal.Engine` / `Repo`
  / `GenServer.call` / direct `Catalog`. The naive grep matched only moduledoc prose; the code names only
  the `Portal` facade.
- **`course_card` reuse (INV6)** — the stream `dom_id` rides a wrapper `<div>`; the F6.5 component is
  byte-unchanged.
- **The `[RECONCILE]` discipline worked as designed** — the route / component / search direction was settled
  before the build, so no fork re-opened mid-rung.

### Went not well
- **An inert doctest shipped a false coverage promise.** `search_courses/1`'s moduledoc carried an `iex>`
  doctest, but no test file invoked `doctest Portal.Catalog`, so AS0's "a doctest shows the filter" never
  executed. Apollo wired the invocation (+1 unit test). → guardrail folded into `mars.md`.
- **The brief fought its own invariant.** The body / brief said the search "re-assigns `@courses`" while INV4
  says the list is a `stream`, never an assign. Mars overrode it to streams correctly, but the brief
  mis-directed — a Venus brief-fidelity miss, not a Mars craft one. → guardrail folded into `venus.md`.
- **A stale guardrail in the durable assets.** `mars.md` and `f6.6.prompt.md` said `seq 1 20` while §4 and
  `phoenix.operator.md` say `≥100`; the rung proved 20 too few. The cross-asset check (now in `apollo.md`)
  caught it. → all four assets reconciled to `≥100`.

### Opportunities to improve (process)
- **The mentoring feedback loop is now formalized.** `apollo.md` gains the post-build mentoring duty (fold
  each rung's recurring finding into `{mars,venus}.md` + this retrospective) and the `.operator.md`-keeper
  duty — both gated downstream of the verdict (the firewall: grade first, mentor second, never soften the
  grade for a teachable fix).
- **A standing liveness criterion + an Operator's guide.** `phoenix.md` / `phoenix.roadmap.md` gain "keep the
  Portal live & hot every rung" as a checkable gate; `phoenix.operator.md` is the new L0 field manual for the
  roadmap → specs → stories → llms → teams → agents → ship workload.

### Gaps carried forward
- **The CRS PK-collision is unguarded in PRODUCTION, not just tests** — `create_course/1` mints outside the
  single-writer engine and `courses` has no `unique_constraint(:id)`; rate ~0 today but genuinely unguarded
  (sibling enroll / controller tests share the latent flake). → routed to the **F6.8** id rung
  (`echo/CLAUDE.md` §4 already flags `@node = 1` → "F6.8 derives it per machine").
- **The success flash is invisible** — the create path `put_flash`es, but no root-layout `<.flash_group>`
  renders `@flash`. → the layout rung; marked `[DEFERRED]` in `f6.6.stories.md` US2 (Mars-2 asserted the
  inserted row, not the flash, to avoid a false-green).

### Spec & agent-definition refinements
- **`f6.6` triad** synced to the as-built stream mechanism (the "re-assigns `@courses`" prose contradicted
  INV4); US2 `[DEFERRED]` flash note; the `Portal.Catalog` doctests made live.
- **`apollo.md`** — the four-role calibration (verifier + docs-keeper + mentor) with the verdict firewall,
  per-peer differential mentoring, and guardrail loop-closure.
- **`mars.md`** — the inert-doctest guardrail + `seq 1 20` → `≥100`.
- **`venus.md`** — the "mechanism words are claims too" reconcile guardrail.
- **Stage 6 (the feedback loop) — DONE.** The F6.7–F6.9 `[RECONCILE]` callouts were folded forward: F6.7's
  route is RESOLVED (`live "/courses"` shipped), the `connected?/1` seam is recorded built-and-waiting for
  F6.7-D3's `subscribe`, and the live-search × broadcast-insert interaction is flagged for the F6.7 build;
  F6.8 gains the CRS id-collision (the id/deploy rung) + the public-`/courses` classification + the
  `127.0.0.1` bind note; F6.9 gains the stream + `course_card` live-list pattern. `f6.7.prompt.md` authored
  (the F6.6 template + the liveness gate + the mentor step).

### Post-ship finding — the liveness gate must be RUN, not just documented
The standing liveness criterion (Portal live on `:4000`) was added in `6e4eb79`, AFTER F6.6 shipped
(`3cf2480`), so F6.6's own gate never ran it — and `mix test` runs the endpoint with `server: false`
(`config/test.exs`), so a 100%-green suite never bound `:4000`. With no dev daemon running, `:4000` read as
inaccessible. **Diagnosis: not a code defect** — the node boots clean (Postgres up, `portal_dev` migrated,
`PortalWeb.Endpoint` binds `127.0.0.1:4000`), `/health` → 200, `/courses` renders `CatalogLive`. Root cause:
the check was documented but never executed — the same **"a check counts only if it RUNS"** class as the
inert doctest. Mentoring folded forward (Operator-ratified): `mars.md` runs the liveness check before
reporting done; `apollo.md` re-runs the liveness boot + curl as part of the gate (not just `mix test`);
`f6.7.prompt.md` bakes the liveness gate into Stage 4/5. *(Deploy note for F6.8: the endpoint binds
`127.0.0.1`, not `0.0.0.0` — fine for local dev, but a Fly/clustered deploy needs the broader bind.)*

> **SHARPENED at F6.5.5 (the same finding RECURRED despite this guardrail).** The polish pass's Mars-3 AND
> Apollo BOTH *did* run the liveness boot+curl this guardrail prescribes — and it STILL failed the Operator's
> on-demand probe, because they ran it TRANSIENTLY (boot→curl→**kill**) from ephemeral agent processes reaped at
> turn-end, leaving `:4000` dead when the Operator looked (*"claimed DONE by Implementors is falsy"*). A finding
> that recurs despite its guardrail proves the guardrail is mis-worded: "run the liveness check" is satisfied by
> a transient boot. The fix is to SHARPEN the existing line (not stack a second): **liveness is a STANDING
> property — leave the node RUNNING from a non-ephemeral context (the Director's main session / the deploy) or
> hand the Director the one-line boot command, so the Operator can verify ON DEMAND.** Propose-only diff for
> `mars.md` + `apollo.md` in [`f6.5.5.progress.md`](f6.5.5.progress.md) §5, cited to F6.5.5; the build itself was
> correct (the Director re-booted durably + re-probed both styled routes — a GATE defect, not a code defect).

## F6.5.5 — Apply the design system (two STATIC parity pages) · BUILD-GRADE

**Slice.** The styling rung inserted between F6.5 (views, shipped) and F6.6 (LiveView, shipped) but built
AFTER both. The Portal rendered UNSTYLED (`layouts: []`, `priv/static/assets/` held only `.keep`, `/`
returned a bare `<h1>Portal</h1>`). This rung applies the F0 dark-editorial system by PUBLISHING two real
course-index pages at parity with their static Fiber originals: `:4000/` reproduces `html/courses.html` (the
two-course index, Fiber `:8765/courses`) and `:4000/elixir` reproduces `elixir/index.html` (the Elixir index,
Fiber `:8765/elixir`). Mechanism: two full-document HEEx templates, NO root layout; per-page extracted assets
(`courses.css`/`courses.js`, `elixir-index.css`/`elixir-index.js`) over `Plug.Static`; NO bundler, NO shared
`app.css`, NO `CoreComponents`. Internal deep links remapped to `https://jonnify.fly.dev` (strangler-fig);
Portal serves only `/` + `/elixir`. The lone new web surface: `PageController.elixir/2` + `get("/elixir", …)`.

**The reframe (single-page → two-page parity).** The rung was reframed twice. ORIGINAL: style the synthetic
`/courses` LiveView catalog. First reframe: ONE published page at `/` reproducing `elixir/index.html`, catalog
demoted. Operator's final correction: BOTH course-index pages, each at the route a visitor expects, each at
parity with its live static original — `/` = `html/courses.html`, `/elixir` = `elixir/index.html`. The
two-page contract SUPERSEDED the prior single-page root-layout mechanism: a single shared root layout cannot
serve both at byte-parity because the two `<head>`s are irreconcilable (different titles, font subsets, and
entirely different `<style>` token sets — `courses.html` uses `--surface`/`--body`, `elixir/index.html` uses
`--elixir`/`--measure`). Hence per-page full-document templates and four extracted assets, not one `app.css`.

**FIX A — the F6.1 latent the rung surfaced.** `endpoint.ex`'s `Plug.Static` was set by F6.1 to `at: "/assets"`
while `static_paths/0` is the `phx.new` default written for `at: "/"`. With `at: "/assets"` the prefix is
STRIPPED, so `GET /assets/courses.css` resolves to the inner path `"courses.css"` (∉ `only:`) → 404. F6.5.5 is
the mount's FIRST consumer, so the latent surfaced here. The ratified 1-line fix (`at: "/assets"` → `at: "/"`)
is the precondition for serving the extracted assets — the lone `endpoint.ex` edit.

**Team & gate.** Venus (reconcile + brief + the triad FIX-A fold) → Mars-1 (the two pages) → Mars-2 (harden +
e2e) → Apollo (verify: BUILD-GRADE, every promise MATCH). Gate: compile `--warnings-as-errors` clean (forced
full recompile, exit 0, no warnings); `TMPDIR=/tmp mix test` **38 tests, 0 failures**. The **≥100 determinism
loop was correctly NOT run** (INV8) — styling + static publishing touches no id-mint and no process state, so
the same-millisecond mint hazard the loop guards cannot fire; the liveness criterion is the two styled-index
curls + the parity gate. Visual-proxy gate: clamp-spacing parse (0 unspaced `[+-]` in both CSS files),
token-fidelity diffs CLEAN, the two-origin Playwright parity gate **11/11 across both origins**.

### Went exceptionally well
- **The clamp-spacing proxy held by construction, proven both ways.** Every `clamp()` in both CSS files is
  spaced — but more than that, a full-body diff proved each CSS file is its master `<style>` body relocated
  BYTE-FOR-BYTE (not just the `:root` head): `elixir-index.css` == `elixir/index.html:12-308` clean,
  `courses.css` == the two `courses.html` `<style>` bodies concatenated clean. The highest-leverage proxy (the
  204-page UA-fallback bug) is satisfied by construction, not by spot-check — the no-reformat discipline held.
- **The liveness-with-visual-dimension gate ran and the FIX-A DOUBLE proved the mount move.** The prior rung's
  post-ship finding (the liveness gate must be RUN, not just documented) was honored: the node booted on
  `:4000` (InMemory EventStore, no Postgres), `/health` → 200, both indexes returned the STYLED envelope
  (real `<head>` + the per-page asset `<link>`, zero bare `<h1>Portal>`), all four assets 200 `text/css`/
  `text/javascript`. The load-bearing FIX-A proof was the DOUBLE: `/assets/assets/courses.css` now 404s while
  `/assets/courses.css` 200s — confirming the mount genuinely moved from `at: "/assets"` to `at: "/"`, not an
  accidental both-paths serve a single `/assets/courses.css → 200` curl could not distinguish.
- **The two-origin parity gate is real render-parity, not a claim.** 11/11 — the type-scale + geometry
  assertions pass IDENTICALLY on Fiber `:8765` AND Portal `:4000` (the `/elixir` h1 > 70px clamp guard on
  both), and the Portal-only remap assertions hold (0 `href="/elixir/"` deep links in the rendered DOM, 79
  production deep links, the arc selector's "Open Fn" → production). The untouched `elixir-hero.spec.ts`
  lesson-hub baseline still passed 8/8 against `:8765` (no regression to the deep tree).

### Went well
- **Remap completeness exhaustive.** Rendered `/elixir` carries 0 un-remapped deep links, exactly 3 bare
  `/elixir` self-links, 79 production deep links; the agile card remapped on `/`; all 6 arc `CH` `route` fields
  remapped in `elixir-index.js`. The only non-`:root` JS delta vs the master is those 6 route fields (+ one
  benign concat newline between the two relocated `<script>` bodies) — zero content drift.
- **INV6 below-the-facade byte-unchanged.** Nothing under `apps/portal` / `apps/echo_data` changed;
  `portal_web.ex`, `page_html.ex`, config untouched; the `endpoint.ex` diff is exactly the `at:` line + its
  comment; the `router.ex` diff is exactly one new `get("/elixir", …)` line.
- **Facade-only held (INV1), proven across all 9 touched surfaces.** A per-file grep (no
  `Portal.Engine`/`Repo`/`GenServer.call`/`CoreComponents`/`PortalWeb.Layouts`) is clean; neither page makes a
  facade call; the lone `Portal.courses_of/1` in `router.ex` is a PRE-EXISTING moduledoc comment outside this
  rung's diff (confirmed: the router diff adds no `Portal.`/`courses_of` reference).
- **Token fidelity, the per-page nuance respected.** `elixir-index.css`'s `:root` diffs CLEAN against
  `elixir/index.html:12-26` (the F0-D1 byte-for-byte fidelity check); `courses.css`'s `:root` diffs CLEAN
  against `html/courses.html:13-28` (its OWN separate token set — verified but NOT failed for differing from
  F0-D1, per the two-page nuance Venus pinned).

### Went not well
- **A first-pass facade grep false-greened.** Apollo's initial boundary grep passed a multi-file string as one
  `ugrep` argument → "No such file or directory" warning, and the `|| echo CLEAN` branch fired a green that had
  NOT actually run. Caught and re-run per-file (genuinely clean). The lesson is self-directed: a verifier's own
  check that emits CLEAN on a tool error is the "a check counts only if it RUNS" class turned inward.

### Opportunities to improve (process)
- **Mechanism surface-facts are claims, verifiable only by probe.** The original brief asserted the
  `Plug.Static` mount "already serves `/assets/*` with NO endpoint change" — a config-read that the as-built
  404 disproved. A single `curl :4000/assets/courses.css` pre-build would have caught it. This is a recurring
  shape (the F6.6 retro recorded "mechanism words are claims too") — it earns a durable Venus guardrail
  (below), not just a one-off correction.

### Gaps carried forward
- **The dynamic-UI styling reckoning is still unbuilt** — F6.5.5 styled STATIC pages only; the LiveView
  `/courses` catalog stays UNSTYLED (the two-page contract has no root layout to carry CSS into it). The root
  layout + single shared token stylesheet (F0-INV2 realized once, not per-page) + the `CatalogComponents`
  restyle + the `CoreComponents` reckoning ALL come due together at **F6.8** (the first dynamic styled pages —
  the auth LiveViews). The downstream `[RECONCILE]` callouts in f6.7/f6.8/f6.9 + `phoenix.roadmap.md` were
  corrected to this reality (confirmed consistent with the as-built this verification).
- **The F6.6 flash-paint deferral STAYS deferred (moved, not closed).** The prior single-page framing had this
  rung's root layout close F6.6's `[DEFERRED]` flash dependency. The two-page contract introduces no shared
  layout and no `<.flash_group>`, so the flash-paint deferral moves to F6.8's app layout — not closed here.
- **`f0.roadmap.md` M4/M5 are a marked foundation `[RECONCILE]`, owned by the Operator.** Lines 92–93 still
  read "root layout + committed `app.css`" and mark M4/M5 "planned — executed by F6.5.5." The triad
  deliberately DEFERRED this upstream foundation correction (f6.5.5.md §Why) rather than silently apply it —
  f0.roadmap is the Operator-owned design foundation. The correction owed (surfaced to the Director, NOT
  applied by Apollo): M4 "root layout" → "two full-document HEEx templates"; "`app.css`" → "per-page extracted
  CSS files"; M4/M5 status → shipped/gated by F6.5.5.

### Spec & agent-definition refinements
- **`f6.5.5` triad** synced to the as-built by Venus: FIX A folded through (§How.2, the resolved
  `[RECONCILE]`, the DoD checks, the touched-files list); the single-page → two-page reframe recorded as an
  auditable supersession; the body's golden-master line ranges corrected (the prior `:311-547` body range was
  stale → `:311-731`). Verified this verification; no residual stale claim found.
- **`mars.md`** — a standing **clamp-spacing lint** guardrail PROPOSED (Operator-ratified, applied by the
  Director): before reporting done on any rung that touches CSS, grep every `clamp()` in the touched/created
  CSS for an unspaced `[+-]` inside the args; an unspaced operator is invalid CSS that silently drops to the
  UA default (the 204-page bug) and is gate-invisible to `mix test`. Cited to F6.5.5.
- **`venus.md`** — a **"verify serving/mount facts by PROBE, not config-read"** guardrail PROPOSED: a
  surface-fact about what a mount/route SERVES (vs. what its config declares) is a claim provable only by a
  curl/HTTP probe. The F6.5.5 brief's "the mount already serves `/assets/*`" was config-read drift the
  as-built 404 disproved; a pre-build `curl :4000/assets/<file>` would have caught it. This SHARPENS — does
  not stack onto — the F6.6 "mechanism words are claims too" line by naming the probe as the discharge.

### Polish pass — the configurable deep-link base (D9 / INV9) · BUILD-GRADE (one minor reconciliation)

**Slice.** A post-build polish (Venus-2 reconcile → Mars-3 build → Apollo-2 verify) the Operator requested:
make the remap host CONFIGURABLE rather than the bare literal `https://jonnify.fly.dev` written 22× across
the templates + JS, AND keep Portal serving its OWN static assets. Venus-2 embedded the contract as a new
**D9 + a two-sided INV9** with a five-category link taxonomy (the base applies to CATEGORY-4 nav deep-links
ONLY — never the category-2 `~p"/assets/…"` assets, the category-1 `/`+`/elixir` self-routes, the
category-3 external CDN, or the category-5 `#main` anchor). As-built: `config :portal_web,
:deep_link_base_url` (default in `config.exs`, env override `DEEP_LINK_BASE_URL` in `runtime.exs`) read by a
new one-line `PortalWeb.deep_link_base/0` resolver; the HEEx markup interpolates it (`deep_link_base() <>
"/elixir/…"`, 20 hrefs in `elixir.html.heex` + 1 agile card in `home.html.heex`); the static
`elixir-index.js` reverts its 6 arc `CH` routes to relative and the `<head>` injects the value ONCE as
`<meta name="deep-link-base" content={PortalWeb.deep_link_base()}>`, which the JS reads (`meta` first,
`window.__deepLinkBase` fallback) to build `BASE + route`.

**Gate (Apollo-2 reproduced).** Compile `--warnings-as-errors` clean (forced full, exit 0); `mix test` **38,
0 failures**. The ≥100 loop correctly NOT run (still no id/process surface). Clamp-spacing + token-fidelity
re-confirmed clean (the polish touched NO CSS — only `elixir-index.js` among the assets, so INV3/INV2 hold by
construction). `elixir-hero.spec.ts` baseline 8/8 (no deep-tree regression).

### Went exceptionally well
- **Configurability is a CHECKED claim on BOTH surfaces — proven by an actual config swap, not just the
  default.** The parity spec parameterizes `NAV_BASE` (`PORTAL_DEEP_LINK_BASE`, default the literal). Apollo-2
  booted the node a SECOND time with `DEEP_LINK_BASE_URL=https://example.test` and re-ran: the rendered markup
  emitted **79 `example.test/elixir/`** deep links + the agile card, the injected `<meta>` carried
  `example.test`, the arc-selector test's JS-built "Open Fn" link carried `example.test`, and **ZERO
  `jonnify.fly.dev` survived in either rendered page** — proving the host comes from config, not a baked
  literal, on the server-rendered AND the static-JS surface. **13/13 under BOTH the default and the swap.**
- **INV9(b) asset-locality is its own gate, held under the swap.** Two NEW e2e tests (one per page) assert the
  `~p"/assets/…"` tags stay root-relative and flag ANY absolute `/assets/` URL as a regression. Probes
  confirmed all four assets serve **200 `text/css`/`text/javascript` from Portal with NO `Location:` redirect**
  to fly.dev, and `:4000/elixir` carried **zero `example.test/assets`** even when the nav base was the
  override — the injection reaches category-4 nav only, never an asset fetch. (The load-bearing distinction:
  a page pulling its own CSS/JS from the base visually matches yet is a hollow shell + a liveness regression.)
- **The boundary-crossing is exactly once.** Config → the injected `<meta>` value → read by the markup at
  render time and the static JS at run time. No `jonnify.fly.dev` literal in any CSS, JS, or `.heex` body.

### Went not well
- **One INV9(a) literal sits in live code the contract's grep excludes — a MINOR reconciliation, not a
  block.** INV9(a) (and its DoD grep) states the literal lives **ONLY** in `config/config.exs` (+ the
  `runtime.exs` override), and `grep -rn jonnify.fly.dev apps/portal_web/lib apps/portal_web/priv` should
  return "only the injection line." The as-built grep instead returns `portal_web.ex:27` — the resolver's
  `Application.get_env(:portal_web, :deep_link_base_url, "https://jonnify.fly.dev")` **3rd-arg fallback** (+
  a self-referential mention in its docstring, `:21`). The injection line itself (`elixir.html.heex:15`) is
  literal-FREE (it carries `PortalWeb.deep_link_base()`). The SPIRIT of INV9(a) — one source of truth, a host
  change touches one file — **holds**: `config.exs` always sets the key, so the `get_env` 3rd arg is
  unreachable defense-in-depth, and the host is genuinely changed in one place. But the LETTER (the literal
  lives ONLY in config) is missed by that one belt-and-suspenders literal. It does not affect rendered output,
  parity, liveness, or the swap (all green). **Verdict held at BUILD-GRADE; the reconciliation is a one-token
  choice routed to the Director (below) — the grade does not move for a teachable one-liner.**

### Spec refinements (the D9/INV9 reconciliation — routed to the Director, Apollo does not self-apply intent)
> **SUPERSEDED below by "Reconciliation CLOSED" (:291).** This block's option (a) "drop the resolver's 3rd arg"
> was one verifier's pick; the RATIFIED resolution (Director's direction + the applied spec-sync) is **option (c):
> KEEP the 3-arg resolver — it is the form D9/§How.5/AS8 explicitly prescribe — and sync the spec's grep-WORDING
> instead.** Read this block as the audit trail of the options weighed; the closed outcome is at :291.
- **Resolve the INV9(a) literal-count gap by EITHER (a or b), Director's pick — both zero-behavior:**
  - **(a) drop the resolver's 3rd arg** — `Application.get_env(:portal_web, :deep_link_base_url)` (no default).
    `config.exs` always sets the key, so the value is unchanged; this makes the grep return truly "only config,"
    matching INV9(a) verbatim. *(Risk: a future env that forgets the key yields `nil` — but `config.exs` is
    compiled in for every env, so the key is always present; acceptably safe.)* **Apollo's recommendation** —
    it makes the as-built match the invariant exactly rather than amending the invariant to the code.
  - **(b) amend INV9(a)** to permit the resolver's idiomatic `get_env` default — "the literal lives only in
    `config.exs` **and the resolver's `Application.get_env/3` default beside it**" — and adjust the DoD grep's
    stated expectation. This blesses the defense-in-depth as the Elixir idiom it is.
- **`f6.5.5.md` §How.5 / D9 name the JS injection as `window.__deepLinkBase`; the as-built uses
  `<meta name="deep-link-base">` (the JS reads the meta first, `window.__deepLinkBase` as fallback).** A
  benign mechanism variation (both cross the boundary once, both grep-clean of the literal), but a spec-vs-code
  wording drift — sync the body to the as-built `<meta>` surface (or note both are admissible) so the next
  reconcile does not flag it. *(Surfaced to Venus-2/the Director; the body is authoritative and should record
  what shipped.)*
- **No `mars.md`/`venus.md` guardrail earned by the polish.** The INV9(a) gap is a one-off literal-placement
  slip on a brand-new invariant, not a recurring failure mode — a `SendMessage`-class note, not a durable
  line. The prior F6.5.5 guardrails (clamp-lint for `mars.md`, probe-not-config-read for `venus.md`) stand;
  the polish gave no occasion to re-fire either (it touched no CSS; the config-swap WAS probed, not
  config-read — arguably the venus probe-guardrail's spirit already applied).

### Reconciliation CLOSED (Apollo independent re-verify · the ratified resolution)
> An independent Apollo re-verification of the polish pass reproduced every gate (compile clean · 38 tests ·
> **13/13 parity on BOTH the default AND the `example.test` override base** · 8/8 baseline · liveness styled
> on both routes · INV9 two-sided by grep + probe + the swap · INV1 facade-clean · determinism correctly N/A)
> and **confirms BUILD-GRADE, zero blocking deltas.** It resolves the two open items above to a single
> ratified outcome:
>
> - **The INV9(a) "literal at the resolver default" is NOT a gap to remove — it is the PRESCRIBED form.**
>   D9 / §How.5 / AS8 explicitly specify the resolver as
>   `Application.get_env(:portal_web, :deep_link_base_url, "https://jonnify.fly.dev")` — the 3-arg form WITH the
>   default. The drift was the spec's *grep-wording* ("the literal lives ONLY in `config.exs`"; "only the
>   injection line"), which predates both the resolver-default form and the `<meta>` injection choice. So the
>   correct resolution is **(c) sync the spec wording to the as-built**, NOT the earlier-proposed (a)
>   drop-the-3rd-arg (which would remove an idiomatic, prescribed default and add a `nil`-risk). Apollo's
>   independent recommendation supersedes the (a)-leaning note above: keep the 3-arg resolver; fix the words.
> - **Applied this pass (Apollo's spec-sync, the keeper role — record what shipped, do not redesign):** the
>   `f6.5.5` triad's INV9(a) grep-target wording (body §How.5 + INV9(a) + the DoD line; the stories check; the
>   brief R8) + the JS-injection mechanism (`<meta name="deep-link-base">` as as-built, `<script>window.__deepLinkBase>`
>   noted as the sanctioned alternative) were synced so body / stories / brief agree with each other AND the
>   code. The load-bearing INTENT of INV9 (no host literal on any remap surface; the base read once; assets
>   stay Portal-local) held throughout — this was a cosmetic STALE-vs-as-built, never a behavior defect.
> - **Agent-defs: none owed (confirmed).** `mars.md`'s "Realization over literal" line is what produced the
>   `<meta>` choice (it fired and held); `venus.md`'s probe-not-config-read line held (the swap was probed).
>   One PREVENTIVE Venus candidate — the "a config value crossing the server/static boundary crosses it
>   EXACTLY ONCE" architecture line — is propose-only (in `f6.5.5.progress.md` §5), NOT owed now (first clean
>   occurrence; the rule is already in the triad body for the F6.8 fold's Venus to read at reconcile).
> - **Inward (Apollo craft):** two coarse boundary greps tripped false-positives on as-built CONTENT — a
>   pre-existing `CoreComponents` comment (`portal_web.ex:76`) and `GenServer`/`Snowflake` CURRICULUM TITLES in
>   the rendered `.heex` chapter cards. Both disambiguated by reading the matched line (comment-excluded /
>   `import`-`alias`-only re-grep). No leak. Recorded so the next verifier expects content-bearing pages to trip
>   code-pattern greps and reads the hit before calling it a leak — the false-green class the verifier exists to
>   catch, applied to itself.

#### Chapter-level lessons (distilled from the F6.5.5 close-out Mentoring Q&A — full Q&A in [`f6.5.5.progress.md`](f6.5.5.progress.md))
These generalize past F6.5.5 to every later rung:
- **Liveness is a STANDING property for an Operator-facing rung, not a transient gate.** boot→curl→kill from
  an ephemeral agent process proves CAPABILITY but leaves no artifact the Operator can re-probe — leave the
  node running (or hand the Director a durable boot command). The F6.6 "green suite, dead route" hazard, lifted
  one level. (Sharpens the F6.6 liveness guardrail in `mars.md`/`apollo.md`.)
- **When a sanctioned realization drifts the spec, sync PROSE to a deliverable-pinned realization; sync CODE
  only when the realization itself breaks an invariant.** The stale party is the one NOT pinned by an authority
  the other already cites. (F6.5.5: the prescribed 3-arg resolver outranked the incidental "ONLY in config"
  sentence — keep the code, fix the words; do not amend code to match prose and break the deliverable that
  prose served.)
- **A config value shared by a server-rendered surface and a byte-served static asset crosses the boundary
  EXACTLY once** — read once on the server, inject once for the asset to read; the runtime analog of F0-INV2
  ("declared once"), and the grep that returns "only one source" IS the proof, made an exercised claim by a
  config swap.
- **A grep/check verdict is only as trustworthy as its pattern** — a CLEAN/leak emitted on a coarse match or a
  tool error is the "a check counts only if it RUNS" failure turned inward on the verifier; read the hit and
  tighten the pattern before vouching the verdict.
- **The grade-first firewall + "a guardrail that re-fires correctly earns its place (no edit)":** mentoring
  runs on the verdict's evidence AFTER the verdict is rendered; a finding that would move the grade is a
  BLOCKING delta routed to Mars, never a softening note — so nothing grade-moving survives to the mentoring
  stage. A durable peer-def line is earned by a recurring FAILURE, never by restating a guardrail that worked.

## AAW-parity — a THIRD static parity page (`/course/agile-agent-workflow`) · BUILD-GRADE · the mentoring MEASUREMENT

**Slice.** A repeatable WORKLOAD (not a chapter rung), the third instance of the F6.5.5 parity-page pattern:
Phoenix serves `:4000/course/agile-agent-workflow` at byte-faithful, design-system parity with the static
Fiber master `html/agile-agent-workflow/index.html` (`:8765`), and the home "agents" card flips from a
production REMAP to a LOCAL route (strangler-fig). Contract: [`aaw-parity.md`](aaw-parity.md). Seven files
(MarsAAW): NEW `page_html/agile.html.heex` (full-document template, inline `<style>`/`<script>` →
`~p"/assets/agile-index.{css,js}"`, **no** `<meta name="deep-link-base">` — the agile JS builds no deep
links) + NEW `agile-index.css` (master `<style>` 12-308 + 327-338 verbatim) + `agile-index.js` (master
`<script>` 709-733 + 737-752 verbatim); EDIT `page_controller.ex` (+`agile/2`), `router.ex` (+route in the
public scope), `home.html.heex:41` (agile card remap → LOCAL), `index-parity.spec.ts` (a 3rd AGILE describe
+ a navigation test + the COURSES remap-test update), `page_controller_test.exs` (+1 agile ConnTest). Team:
VenusAAW (reconcile + contract) → MarsAAW (build) → ApolloAAW (verify + measure + document).

**Why this rung is the mentoring MEASUREMENT.** Same Venus→Mars→Apollo loop as the two prior parity pages,
run a THIRD time — so the FIVE guardrails the prior pages earned become explicit acceptance gates (the
contract's §5). The question the rung answers: did the mentoring TAKE — does each guardrail fire
pre-emptively, nothing recurring.

**Verdict: BUILD-GRADE.** Every promise (R1-R6, the §7 href taxonomy, the two-sided INV9, all five
guardrails) is **MATCH** — zero STALE/INVENTED/MISSING. Gate reproduced independently against the Director's
durable `:4000`: `mix compile --warnings-as-errors` exit 0 (forced full recompile, no warnings); `mix test`
**39 tests** (3 ConnTests incl. the new agile, 10 doctests, 4 properties) **0 failures**; the portal_web
determinism loop **100/100 GREEN** (run as an INDEPENDENT adversarial check though the rung is presentation-
only and correctly N/A under INV8 — it touches no id-mint or process state); the e2e parity suite **20/20**
on BOTH the default base AND the `example.test` override base. The two relocated assets are **byte-identical**
to the master line ranges (proven by `diff <(sed -n …)` — the relocation cannot have drifted a clamp). The
§7 href counts are **exact** in template source AND rendered DOM (23 + 5 category-4 remaps carry the base; 1
bare index + 4 bare `/elixir` relative; 0 un-remapped deep links). Boundary grep clean — the template names
only `deep_link_base/0`; no `Portal.Engine`/`Repo`/`Store`/`GenServer.call`; no `jonnify.fly.dev` outside the
`portal_web.ex` resolver default + moduledoc.

### The five-guardrail measurement — did the mentoring take

| # | Guardrail (origin) | Verdict | Evidence |
|---|---|---|---|
| G1 | clamp-spacing (`mars.md`, F6.5.5) | **FIRED-CLEAN** | `grep -nE 'clamp\([^)]*[+-][^ 0-9.]' agile-index.css` → 0; 9 clamps, all spaced; the relocated CSS is byte-identical to master 12-308 + 327-338, so clean BY CONSTRUCTION. e2e `h1` > 70px on both origins. |
| G2 | probe-not-config-read (`venus.md`, F6.5.5) | **FIRED-CLEAN** | VenusAAW's §1.1 serving facts are curls (`:4000` route 404 + asset absence pre-build), not `endpoint.ex` reads; AND VenusAAW READ both `<script>` blocks to kill the `<meta>` cargo-cult (the decisive §1.3 finding: neither block builds a `/course/...` URL → no injection needed). Apollo re-probed post-build: route 200, assets 200 local. |
| G3 | standing-liveness (`mars.md`/`apollo.md`, F6.6 → sharpened F6.5.5) | **FIRED-CLEAN (with a micro-sharpen owed — below)** | MarsAAW did the load-bearing thing: NOT boot→curl→kill, but **handed the Director the boot command** and respected the Director's durable `:4000`. The guardrail FIRED. |
| G4 | asset-locality, two-sided (`apollo.md` gate, F6.5.5) | **FIRED-CLEAN** | (a) `curl :4000/assets/agile-index.css` → 200 `text/css`, `.js` → 200 `text/javascript`, 0 redirects; (b) rendered refs are `~p"/assets/…"`, ZERO `/assets` ref carries the base or is absolute — held even UNDER the config swap. |
| G5 | configurable-base (`apollo.md`/e2e, F6.5.5 polish) | **FIRED-CLEAN** | Boot with `DEEP_LINK_BASE_URL=https://example.test`: 23 + 5 category-4 links re-render with the override, ZERO `jonnify.fly.dev` literal survives (proves config not baked), assets stay root-relative. e2e under `PORTAL_DEEP_LINK_BASE` 20/20. |

**"Did the mentoring take" — the assessment.** Yes, decisively. All five guardrails fired pre-emptively on
the FIRST pass of a fresh stateless team — no STALE reached the build, no clamp drifted, no asset was swept,
no `<meta>` was cargo-culted, the base proved configurable under a real swap. This is the third consecutive
parity page and the FIRST to need zero post-build code correction: the F6.5.5 pages each surfaced a finding
(FIX-A mount 404; the D9/INV9 polish), the AAW page surfaced NONE. The durable-channel mentoring (lines
planted in `mars.md`/`venus.md`/`apollo.md`) is auditing clean: the clamp-spacing line (G1) and probe-not-
config line (G2) re-fired correctly and have earned their place — no edit, per "a guardrail that re-fires
correctly earns its place." The one finding is on G3, and it is a wording sharpen, not a recurrence of the
underlying failure (the node WAS durably served — the build was correct).

### The G3 nuance — adjudicated (a micro-sharpen owed to `mars.md` ONLY)

MarsAAW's standing-liveness behavior split: the **load-bearing** action was correct (handoff to the Director
+ respect for `:4000`), but MarsAAW **also reported "left `:4010`/`:4011` UP"** as if those were standing
artifacts — and both DIED at MarsAAW's turn-end (the Director's probe hit connection-refused). This confirms
the guardrail's own premise ("an agent-booted node is reaped at turn-end") IN THE FIELD, and reveals that the
current `mars.md` wording still invites the wrong read: it offers "leave the node RUNNING (a durable boot
from a non-ephemeral context, **or** hand the Director the one-line boot command)" — the "leave running"
branch, even qualified by "non-ephemeral context," reads to a fresh spawn as something the AGENT itself can
do, with the handoff a mere alternative. An **agent cannot leave anything durably running**; its ONLY
reliable path is the handoff. The asymmetry is the tell: `apollo.md` (the verifier's line) already scopes the
durable boot correctly ("a durable boot from the Director's main session or the deploy, **not the agent's
ephemeral process**") and needs NO change; `mars.md` (the builder's line) does not yet make the agent/Director
split exclusive. Per "a finding that recurs despite its guardrail proves the guardrail is mis-worded — SHARPEN
the existing line, never stack a second," this earns a **micro-sharpen to the existing `mars.md` line**
(propose-only, Operator-ratified, Director-applied — Apollo does not self-apply a peer-def edit). The proposed
diff is in "Spec & agent-definition refinements" below. This is a craft (contract-fidelity) lesson, not an
intent one — it is owed.

### Went exceptionally well
- **First zero-correction parity page — the mentoring compounded.** Three parity pages in, the team needed no
  post-build code fix for the first time. The two F6.5.5 pages each taught a lesson (the FIX-A mount 404, the
  D9 polish); those lessons, planted as durable guardrails, fired pre-emptively here. The verification found
  ONLY a wording sharpen, never a defect.
- **Byte-identical relocation proven by line-range diff — G1 clean by construction.** `diff <(sed -n
  '12,308p;327,338p' master) agile-index.css` and `diff <(sed -n '709,733p;737,752p' master) agile-index.js`
  both report BYTE-IDENTICAL. The clamp-spacing proxy (the 204-page UA-fallback bug) is satisfied because no
  transform exists that COULD have drifted a clamp — the strongest form of the gate, not a spot-check.
- **The `<meta>` cargo-cult was killed at the SOURCE (Venus), not patched at verify.** The elixir parity page
  carries `<meta name="deep-link-base">` solely for its `elixir-index.js` arc link-builder. VenusAAW READ
  both agile `<script>` blocks, confirmed neither builds a `/course/...` URL, and pinned "NO `<meta>`" in the
  contract (§1.3, §7 note). MarsAAW honored it; Apollo confirmed the master itself has zero `deep-link-base`
  occurrences. A per-page READ of the JS replaced a copy-the-prior-page reflex — exactly the discharge G2
  prescribes, applied to a structural decision rather than a serving fact.
- **The href taxonomy is exact in BOTH source and rendered DOM.** 23 `deep_link_base() <> "/course/.../"`sub
  + 5 `/elixir/course` in the template source; the live `:4000` render shows the identical 23 + 5 carrying
  the base, 1 bare index relative, 4 bare `/elixir` relative, 0 un-remapped deep links. The strong form of
  D4, not the `> 0` minimum.

### Went well
- **The config-swap gate ran on an ephemeral node WITHIN the turn, then was torn down — never presented as
  standing.** G5's `example.test` boot (on `:4010`/`:4011`) proved the re-render, then was killed; the report
  cites the Director's `:4000` as the sole standing artifact and confirms `:4010`/`:4011` DOWN afterward. The
  G3 discipline applied to Apollo's OWN ephemeral checks.
- **The home-card flip closes the page toward its golden master.** `home.html.heex:41` is now
  `href="/course/agile-agent-workflow"` (relative), matching `html/courses.html:163`; the e2e navigation test
  proves clicking it lands on the Portal's own agile page (200), never production — the strangler-fig flip
  made observable end to end.
- **Below-the-facade byte-unchanged; sibling assets byte-unchanged.** Nothing under `apps/portal` /
  `apps/echo_data` changed; the only new assets are `agile-index.{css,js}` (the prior `courses.*` /
  `elixir-index.*` show no diff vs HEAD); the `router.ex` diff is exactly one `get(…)` line in the public
  scope; the `page_controller.ex` diff is exactly the `agile/2` clause.

### Went not well
- **A fresh Mars spawn still read "leave the node running" as an agent-doable action.** MarsAAW's secondary
  report ("left `:4010`/`:4011` up") shows the G3 wording's "or hand off" framing did not make the
  agent/Director split EXCLUSIVE. The underlying behavior was correct (the handoff worked), so this is a
  near-miss, not a recurrence of the dead-port failure — but it is the seam the micro-sharpen closes.

### Opportunities to improve (process)
- **A repeatable workload deserves a runbook, not re-derivation each time.** Three parity pages have run the
  same six-step spine (identify master → reconcile-by-probe → contract → build → gate → ratify+commit). This
  verification ADDED that spine to `phoenix.operator.md` as the L0 sub-runbook §5.1 ("add a static parity page
  through Phoenix"), generalized to "the Nth parity page" with the L0 decision split (the Operator picks the
  page + ratifies the route; each agent's stage is fixed). The next parity page reads the runbook.

### Gaps carried forward
- **The dynamic-UI styling reckoning is STILL unbuilt (unchanged from F6.5.5).** This is a third STATIC parity
  page; the LiveView `/courses` catalog stays UNSTYLED. The root layout + shared token stylesheet +
  `CoreComponents` reckoning still come due together at F6.8.
- **The agile master is actively authored (the drift caveat, §10).** Parity is against the CURRENT 755-line
  snapshot. A STRUCTURAL change to the master (new `<style>`/`<script>` blocks, new deep-link categories, a
  changed e2e-landmark selector) requires a Portal re-sync — the same six-file edit re-run. The e2e checks
  computed style + geometry (not bytes), so it TOLERATES copy edits; only a structural shift breaks it.

### Spec & agent-definition refinements
- **`aaw-parity.md`** synced to the as-built by Apollo (the keeper role — record what shipped): the status
  line updated from "BUILD-GRADE — pending ratification" to "BUILD-GRADE — SHIPPED + verified," recording the
  byte-identical asset proof, the exact href counts, the reproduced gate (39 tests, 100/100 loop, 20/20 e2e on
  both bases), and the clean boundary. No intent redesign — record only.
- **`phoenix.operator.md`** extended with **§5.1 "Workload — add a static parity page through Phoenix (the
  Nth parity page)"**: the L0 decision-split table (Operator picks the page + ratifies the new route; Venus
  reconciles by PROBE + reads both `<script>` blocks for the `<meta>` decision; Mars builds; Apollo gates) +
  the standing-liveness nuance (the Director holds the durable node; an agent's sole path is the handoff) +
  the parity-specific gate vocabulary (the master line-range byte-diff, the href-taxonomy counts, the
  per-page `<meta>`-injection decision). Process facts recorded from shipped reality, not aspiration.
- **`mars.md`** — a **micro-SHARPEN of the existing G3 standing-liveness line** PROPOSED (propose-only,
  Operator-ratified, Director-applied; Apollo does not self-apply a peer-def edit). The current line offers
  "leave the node RUNNING (a durable boot from a non-ephemeral context, **or** hand the Director the one-line
  boot command)"; a fresh MarsAAW read the "leave running" branch as agent-doable and reported its ephemeral
  `:4010`/`:4011` as "left up" (both reaped at turn-end). SHARPEN (not stack) the existing line to make the
  split EXCLUSIVE for an agent — the handoff is the agent's SOLE path; "leave running" is the Director/deploy
  path alone. Cited to AAW-parity. Proposed diff:

  > **Current** (`mars.md`, the liveness bullet):
  > "And liveness is a STANDING property, not a one-shot gate: for an Operator-facing rung, leave the node
  > RUNNING (a durable boot from a non-ephemeral context, or hand the Director the one-line boot command) —
  > never boot→curl→kill, because an agent-booted node is reaped at turn-end…"
  >
  > **Proposed** (one clause sharpened; the rest verbatim):
  > "And liveness is a STANDING property, not a one-shot gate: for an Operator-facing rung, the node must be
  > left RUNNING from a non-ephemeral context — the Director's main session or the deploy. **A spawned agent
  > CANNOT do this** (its process, and any node it boots, is reaped at turn-end), so the agent's SOLE reliable
  > path is to **hand the Director the one-line boot command** and report the route as served-pending-boot,
  > never to claim it "left up." Never boot→curl→kill; a server torn down when the turn ends makes
  > "DONE / the Portal lives at :4000" unfalsifiable when the Operator probes on demand (F6.5.5: the polish
  > curled :4000 green then killed it; AAW-parity: a fresh spawn reported ephemeral :4010/:4011 'left up,'
  > both dead to the next probe)."
- **`apollo.md`** — **none owed (confirmed).** My own standing-liveness line (lines 74-77) already scopes the
  durable boot correctly ("a durable boot from the Director's main session or the deploy, **not the agent's
  ephemeral process**"); it fired clean this rung (the config-swap node was torn down, `:4000` cited as the
  sole standing artifact). `venus.md`'s probe-not-config-read line (G2) fired clean and is unchanged. The
  asymmetry — `apollo.md` exclusive, `mars.md` not-yet-exclusive — is precisely why the sharpen lands on
  `mars.md` alone.

#### Chapter-level lesson (distilled, generalizes past AAW-parity)
- **A guardrail can fire CLEAN on its load-bearing action yet still be mis-worded on a secondary read — the
  sharpen is owed when a fresh spawn predictably repeats the secondary slip, even with the primary failure
  absent.** G3's underlying failure (a dead `:4000` the Operator probes) did NOT recur — MarsAAW's handoff
  worked. But the SAME spawn reported ephemeral nodes as "left up," the exact mis-read the F6.5.5 sharpen aimed
  to prevent, surviving because the wording's "or hand off" branch was non-exclusive. The signal to sharpen is
  not only "the failure recurred" but "a fresh stateless spawn read the line the way that PRODUCES the
  failure" — caught here before it cost a dead port, because the Director held the durable node. Keep the
  sharpen surgical (one clause, the rest verbatim) and on the ONE def whose contract the slip implicates (the
  builder's `mars.md`, not the verifier's already-exclusive `apollo.md`).
