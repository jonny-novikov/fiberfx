# F6.7 · Real-time (PubSub & Presence)

> Push F6.6's per-socket state across every connected client. A facade `subscribe`/`broadcast` wrapper over
> `Portal.PubSub` (supervised); context broadcasts that fire only after a successful write; `CatalogLive` subscribing on
> its connected mount and applying `stream_insert/3` in `handle_info/2`; and `Presence` for a viewer count correct
> across the cluster. The only new supervision children are `Phoenix.PubSub` and `PortalWeb.Presence`; everything below
> the facade is unchanged.

## [RECONCILE] — folded forward from F6.5, then F6.6 (`3cf2480`)

- **POST-BUILD (Apollo, F6.7 verify) — as-built record for F6.8's reconcile (MATCH unless noted).** The rung shipped
  BUILD-GRADE; three as-built facts the next rung reconciles against: **(1)** `Portal.subscribe/1` + `Portal.broadcast/2`
  are `def`, NOT `defdelegate` (no `Portal.*` target — they wrap `Phoenix.PubSub` directly; INV2 still holds, the web
  names only `Portal.subscribe`), `portal.ex:62-63`. **(2)** The dependency claim is a *mechanism* correction:
  `mix.lock` is UNCHANGED by F6.7 (`phoenix_pubsub` was already locked as a non-optional dep of `phoenix`), but
  `apps/portal/mix.exs:59` GAINED the one-line declared edge `{:phoenix_pubsub, "~> 2.1"}` — an umbrella app cannot
  compile-reference a module it does not declare, so "no new dependency" was true for the LOCK and false for the per-app
  DEP-GRAPH visibility. The master invariant's INTENT holds: `apps/portal/mix.exs` declares `phoenix_pubsub` (framework-
  free BEAM infra, empty dep list in `mix.lock`) but NO web framework (`phoenix`/`phoenix_live_view`/`phoenix_html`/
  `plug`/`bandit`) — the `:portal`-scoped `mix deps.tree` carries only `phoenix_pubsub`. **(3)** D6 (channel + `UserSocket`)
  is DEFERRED — not built (the `could`-priority US5 was skipped). *(Why: record what shipped so F6.8 reconciles against
  truth, not the pre-build claim. The `config.exs`/`mix.lock`/`apps/echo_bot/` entries in the working tree are the
  Operator's concurrent F10-bot work, OUT-OF-BAND to F6.7 — F6.7 changed NO config.)*
- **Surface — `update_course/2` does not exist (reconciled `3cf2480`; MISSING, ratification-gated).** D2/Scope broadcast from `create_course/1` AND `update_course/2`, but F6.4/F6.5 ship no edit path: `Catalog` has list/get/fetch/create + an **arity-0** `change_course/0` (a *blank-form* builder, `catalog.ex:107`), and F6.6 added only the read-only `search_courses/1` — there is **no** `update_course/2`, no `Portal.update_course/2` delegate, and no `change_course/2`. Define `update_course/2` (+ its `Portal` facade delegate) when this rung builds, or drop the `:course_updated` broadcast until an edit flow exists. **The changeset surface it rides already exists** — `Course.changeset/2` (`course.ex:47`, `course \\ struct(__MODULE__, %{}), attrs` → `%Ecto.Changeset{}`) — so `update_course/2` is the exact mirror of `create_course/1` (`catalog.ex:118-125`) with `Repo.update/1` substituted for `Repo.insert/1`: `course |> Course.changeset(attrs) |> Repo.update()`. It returns `{:ok, %Course{}} | {:error, %Ecto.Changeset{}}`, identical to `create_course/1`'s reconciled shape (`catalog.ex:117`), so the ok-only `broadcast/2` helper is consistent across both write paths. This is the one new **write** surface the rung adds below the facade — Venus pins it, the **Director ratifies before build**. *(Why: no update path is built yet; the changeset primitive is.)*
- **Route inheritance — RESOLVED by F6.6.** `CatalogLive` is live at `live "/courses"` (F6.6 shipped `3cf2480`, superseding the static index); this rung references it by module and adds no route. *(Was the F6.6 route reconcile; now shipped — no longer open.)*
- **The `connected?/1` seam is built and waiting (F6.6).** F6.6's two-stage mount left `connected?/1` guarding a no-op `:connected?` marker *explicitly as the F6.7 seam*; D3's `Portal.subscribe("courses")` lands in exactly that branch — extend it, do not restructure the mount. *(Why: F6.6 built the seam for this rung.)*
- **Live search × broadcast insert — DECIDED: gate the insert on a `@query` match (ratification-gated build directive).** F6.6's search keeps `@query` and re-streams with `reset: true` (`catalog_live.ex:136-145`) to show only matches; F6.7's `handle_info` `stream_insert` ignores `@query`, so an unconditional insert prepends a non-matching row into a filtered view (learner types "elixir", someone creates "Rust", "Rust" appears) — until the next keystroke re-streams and drops it. **Resolution:** `handle_info/2` inserts only when the course matches the active query — `@query == "" or String.contains?(String.downcase(course.title), String.downcase(@query))` (empty query = match-all; the case-insensitive substring is the in-Elixir equivalent of `search_courses/1`'s `ilike "%term%"`, `catalog.ex:60-69`). **Reasoning:** INV3 has each client patch its own DOM and US1 wants the view to stay *fresh* — a fresh view that silently breaks the learner's active filter is worse freshness than one that respects it; option 2 ("accept + let the next keystroke fix it") is a visible glitch on a row the learner explicitly filtered out. **Cost, accepted and bounded:** a 1-line predicate duplicates the filter's *substring* semantics (not the full query) in the view — a small INV2/DRY tension; the canonical filter stays `Portal.search_courses/1`, and the broadcast path needs only the title-substring test, so the duplication is the title check alone. The Director ratifies this build-behavior pick. *(Why: F6.6 shipped a live filter the broadcast path must reckon with; the gate keeps the filter honest under real-time.)*
- **Styling — F6.5.5 did NOT style the live catalog; F6.7 owns CONSISTENCY, not inheritance.** F6.7's `stream_insert` prepends (create) / replaces (update) rows rendered by `PortalWeb.CatalogComponents.course_card/1`. [F6.5.5](f6.5.5.md) was reframed to ship **two STATIC parity pages** (`/` = `html/courses.html`, `/elixir` = `elixir/index.html`) via per-page **extracted CSS** — it added NO root layout, NO shared `app.css`, and did NOT restyle `CatalogComponents`, so `course_card` and the F6.6 `live "/courses"` `CatalogLive` it renders in are **still unstyled** after F6.5.5. The invariant F6.7 owns is therefore *consistency*: a broadcast-inserted row must render through the SAME `course_card/1` the mount uses, so it matches the mounted list — styled if a prior pass has styled the component, bare if not. Do not emit a divergent row in `handle_info/2`. The "style the live catalog" work (restyle `CatalogComponents` + a shared token stylesheet / root layout for the **dynamic** UI) is unbuilt after F6.5.5 and lands at the first dynamic styled page (F6.8, or an earlier dedicated pass) — F6.7 does not depend on it, only on rendering the broadcast row identically to the mount. *(Why: F6.5.5 styled static documents, not the LiveView component F6.7 streams through; the broadcast path must match the mount, whatever its styled state.)*

## Goal

After F6.7, the catalog updates live across every connected client. The `Portal` facade gains a PubSub wrapper —
`subscribe/1` and `broadcast/2` over `Portal.PubSub`, started in the supervision tree. The `Catalog` context broadcasts
`{:course_created, course}` and `{:course_updated, course}` after a successful write, via a `broadcast/2` helper that
fires only on `{:ok, _}`. `CatalogLive` subscribes to `"courses"` on its connected mount and handles those events in
`handle_info/2`, calling `stream_insert/3` (prepend on create, replace on update), so every connected client's list
updates without a reload. `Presence` (`track/3` on the connected mount plus a `"presence_diff"` handler) recomputes a
live viewer count correct across the cluster. Optionally, a minimal channel (`join/3` + `handle_in/3`) serves a
non-LiveView client. Everything still calls only the facade; the only new tree children are `Phoenix.PubSub` and
`PortalWeb.Presence`.

## Rationale (5W)

- **Why** — PubSub is how the BEAM turns one user's write into every user's update: a context broadcasts a fact after
  it commits, and each subscribed LiveView patches its own DOM — no polling, correct across a cluster. Presence adds an
  accurate, distributed count of who is here. Broadcasting only on `{:ok, _}` keeps the stream of facts honest —
  clients learn of a change only after it happened. Without PubSub, multi-client freshness needs polling or a bespoke
  socket protocol.
- **What** — the facade `subscribe`/`broadcast` wrapper (supervised), context broadcasts on successful writes, a
  subscribed LiveView applying stream updates, `Presence` for a live viewer count, and an optional channel.
- **Who** — Learners (a catalog that updates as others change it) and Instructors (a live viewer count and immediate
  propagation of edits).
- **When** — the seventh Phoenix rung; it pushes F6.6's per-socket state across all clients over the same facade, and
  it is the real-time substrate the F6.9 dashboard folds.
- **Where** — the `Portal` facade and `Portal.Catalog` (broadcast helper + `update_course/2`), `Portal.Application`
  (the `Phoenix.PubSub` child), `PortalWeb.Application` (the `PortalWeb.Presence` child — a web-tier process),
  `lib/portal_web/live/catalog_live.ex`, the new `lib/portal_web/presence.ex`, and optionally a channel module plus a
  `UserSocket`.

## Scope

- **In** — `Portal.subscribe/1` and `Portal.broadcast/2` over `Portal.PubSub` (supervised); the new `update_course/2`
  write (+ its facade delegate, US0) so `:course_updated` has a real path to ride; broadcasts from `create_course/1` and
  `update_course/2` firing only on `{:ok, _}`; `CatalogLive` subscribing to `"courses"` on the connected mount and
  handling events in `handle_info/2` into `stream_insert/3` (prepend on create, replace on update), gated to the active
  `@query`; `Presence` `track/3` + `presence_diff` recomputing a live viewer count across the cluster; and optionally a
  minimal channel (`join/3` + `handle_in/3`) for a non-LiveView client.
- **Out** — authentication and registration (→ F6.8); the dashboard (→ F6.9); cluster setup and the deployed release
  (→ F6.8); any new domain feature; and changes below the facade.

## Deliverables

- **F6.7-D1** — the facade PubSub wrapper: `Portal.subscribe(topic)` calling `Phoenix.PubSub.subscribe(Portal.PubSub,
  topic)` and `Portal.broadcast(topic, message)` calling `Phoenix.PubSub.broadcast(Portal.PubSub, topic, message)`
  (**as-built `def`, not `defdelegate`** — there is no `Portal.*` target to delegate to; the wrappers reach
  `Phoenix.PubSub` directly. They own no logic — one delegating call each, the `defdelegate`-style idiom — so INV2 holds:
  `portal.ex:62-63`); `{Phoenix.PubSub, name: Portal.PubSub}` added to `Portal.Application`'s children
  (`application.ex:42-48`, was `Repo, Store, EventStore.adapter(), Engine`). The Endpoint already names this server —
  `config.exs:38` declares `pubsub_server: Portal.PubSub` (written at F6.1 "for a later rung but NOT started", L28-30):
  the config key is already correct, so D1 **starts the process the key already points at**; no config change.
  **As-built dependency edge:** `phoenix_pubsub` is already in `mix.lock` (a non-optional dep of `phoenix`), so
  `mix.lock` is UNCHANGED by this rung — but `apps/portal/mix.exs` had no DECLARED edge to it, and an umbrella app
  cannot compile-reference a module it does not declare, so D1 adds the one-line visibility edge
  `{:phoenix_pubsub, "~> 2.1"}` (`apps/portal/mix.exs:59`). The added artifact in the build is zero (lock unchanged);
  the change is the per-app dep-graph edge alone. (Brief deviation, recorded: `f6.7.llms.md` L33's "no new dependency"
  conflated lock-presence with per-app dep-graph visibility — true for the lock, but the wrappers/child could not
  compile without the declared edge.)
- **F6.7-D2** — broadcasts from the context: `create_course/1` and `update_course/2` emit `{:course_created, course}` /
  `{:course_updated, course}` after a successful write, via a `broadcast/2` helper that fires only on `{:ok, _}` (a
  failed write broadcasts nothing). `update_course/2` is the F6.7-US0 new write surface (RECONCILE, above):
  `course |> Course.changeset(attrs) |> Repo.update()`, the `create_course/1` mirror (`catalog.ex:118-125`) with
  `Repo.update/1` for `Repo.insert/1`, returning `{:ok, %Course{}} | {:error, %Ecto.Changeset{}}` — so the one helper
  closes over both `{:ok, _}` results identically.
- **F6.7-D3** — a subscribed LiveView: `CatalogLive` subscribes to `"courses"` on its connected mount by EXTENDING the
  existing `connected?/1` true-branch (`catalog_live.ex:69-73`, today `assign(socket, :connected?, true)` — the F6.6
  seam built for this rung) with `Portal.subscribe("courses")`; it does NOT restructure the mount. It handles
  `{:course_created, _}` / `{:course_updated, _}` in `handle_info/2`.
- **F6.7-D4** — stream updates from broadcasts, gated to the active filter: `handle_info/2` calls
  `stream_insert(socket, :courses, course, at: 0)` on create and `stream_insert(socket, :courses, course)` on update —
  **but only when the course matches `@query`** (the RECONCILE live-search × broadcast decision):
  `@query == "" or String.contains?(String.downcase(course.title), String.downcase(@query))`. The inserted row renders
  through the SAME `course_card/1` the mount uses (`catalog_components.ex:32`, `attr :course` required), mirroring the
  create path's `stream_insert(:courses, course, at: 0)` (`catalog_live.ex:163`), so a broadcast row matches the
  mounted list (INV6/consistency). Every connected client's list updates without a reload.
- **F6.7-D5** — `Presence`: `PortalWeb.Presence` (a NEW `lib/portal_web/presence.ex`, `use Phoenix.Presence, otp_app:
  :portal_web, pubsub_server: Portal.PubSub`), `track/3` on the connected mount and a `%{event: "presence_diff"}`
  handler recomputing a `viewers` count correct across the cluster; the `Presence` child is added to **`PortalWeb.Application`**
  (`application.ex:16-20`, was `[PortalWeb.Telemetry, PortalWeb.Endpoint]`) — Presence is a web-tier process, and it
  depends on `Portal.PubSub` (started by D1 in the `:portal` app, which boots first via the `:portal_web` → `:portal`
  app dependency). `Phoenix.Presence` ships inside `phoenix` (already a `:portal_web` dep), so no new dependency on the
  `:portal_web` side — the only declared dep-graph change this rung is the D1 `phoenix_pubsub` edge on the `:portal`
  side (above). As-built: `apps/portal_web/lib/portal_web/presence.ex` + the child at `application.ex:19`.
- **F6.7-D6** — (optional, `could`) a minimal channel: a `join/3` + `handle_in/3` module (and a `UserSocket` entry) for
  a non-LiveView client. **[DEFERRED — not built F6.7.]** The `could`-priority US5 was skipped; the LiveView path (D3/D4)
  is the F6.7 client and no non-LiveView consumer exists yet. No channel module, no `UserSocket`, and no `socket` entry
  in `endpoint.ex` were added. A later rung with a non-LiveView client builds it against this same `Portal.PubSub`.
- **F6.7-D7** — verification: a create in one session appears in another without reload; an update replaces the row in
  every client; the viewer count reflects connect and disconnect across nodes; a failed write broadcasts nothing; the
  only new tree children are PubSub and Presence; and the LiveView still calls only the facade.

## Invariants

- **F6.7-INV1 (broadcast only on success)** — a broadcast fires only after a successful write (`{:ok, _}`); a failed
  write broadcasts nothing, so clients only ever learn of facts.
- **F6.7-INV2 (real-time over the facade — the master invariant)** — `subscribe`/`broadcast` go through `Portal` over
  `Portal.PubSub`; the LiveView and context name no transport beyond the facade wrapper, and nothing names the engine,
  a `Repo`, or `GenServer.call`.
- **F6.7-INV3 (per-client patching)** — each subscribed client patches its own DOM from the broadcast via
  `stream_insert`; no client holds the full list, and no reload occurs.
- **F6.7-INV4 (cluster-correct presence)** — `Presence` `track` + `presence_diff` yield a viewer count correct across
  the cluster (CRDT-backed), not a single-node counter.
- **F6.7-INV5 (minimal new surface)** — the only new supervision children are `Phoenix.PubSub` and `PortalWeb.Presence`;
  everything below the facade is unchanged (the F5→F6 handoff invariant holds).
- **F6.7-INV6 (idempotent stream identity)** — `stream_insert` keys on the course id, so a create prepends once and an
  update replaces in place; a re-delivered broadcast does not duplicate a row.

## Definition of Done

- [ ] `Portal.subscribe/1` and `Portal.broadcast/2` wrap `Portal.PubSub`, which is supervised (F6.7-D1).
- [ ] `create_course/1` and `update_course/2` broadcast only on `{:ok, _}` (F6.7-INV1).
- [ ] `CatalogLive` subscribes on the connected mount and applies `stream_insert/3` in `handle_info/2` (F6.7-D3, D4).
- [ ] A create in one session appears in another without reload; an update replaces the row everywhere (F6.7-INV3).
- [ ] `Presence` yields a viewer count correct across the cluster (F6.7-INV4).
- [ ] The only new supervision children are `Phoenix.PubSub` and `PortalWeb.Presence` (F6.7-INV5).
- [ ] The LiveView still calls only the facade (F6.7-INV2); every deliverable maps to a story (see
      `./f6.7.stories.md`).

---

Stories: [`./f6.7.stories.md`](f6.7.stories.md) · Agent brief: [`./f6.7.llms.md`](f6.7.llms.md) · Index:
[`./phoenix.md`](phoenix.md) · LiveView: [`./f6.6.md`](f6.6.md) · Events & the log:
[`../pragmatic/decider-pattern.md`](../pragmatic/decider-pattern.md) · Approach: [`../specs.approach.md`](../specs.approach.md)
