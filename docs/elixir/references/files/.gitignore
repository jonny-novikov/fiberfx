# The offline mirror is large (HTML + PDFs) and re-creatable from fetch_refs.py.
# Track the tool, manifest.json, and INDEX.md — not the mirrored bytes.
# Remove this file to commit the offline copies into the repo.
*
!.gitignore
