# mx-7-3-1 — AAW scope ledger

## Retrospective — mx.7.3.1 (Apollo, standing-mentor pass, 2026-06-30)

Post-ship, propose-only. Rung shipped BUILD-GRADE (Director-verified): `@mercury/ui` barrel 50→51
component folders (0 removed); packages typecheck+build + sb:typecheck + sb:build + apps build all
exit 0; INV-6 holds (no `@internationalized/date` import in `mercury-ui/src` — the date layer arrives
via `@mercury/core`); mutation spot-check → `TS2339`, net-zero. Reconcile-confirm (spot-check):
CONFIRM BUILD-GRADE — barrel = 51 `export * from "./components/…"` lines (DateField at `index.ts:50`);
the precise import grep over `mercury-ui/src` is empty (the two textual hits are a `.tsx` comment and
the `.prompt.md` contract, not imports); `useDateField` is exported from the `@mercury/core` barrel and
its internal machinery imports the real `@internationalized/date` functions (`CalendarDate`,
`DateFormatter`, `parseDate`).

### Went well
- **The write-ready dispatch survived the connection-death pattern.** After three Mars spawns died to
  `ECONNRESET` mid-read having written nothing, the build landed once the dispatch was pre-ground into a
  write-first map and split into short waves — the recovery encoded as `x.md §5 LAW-1b` and folded into
  the four durable assets (`mars.md`, `venus.md`, `apollo.md`, the mercury-ship skill).
- **The wave-split held.** Wave 1 = the `@mercury/core` `useDateField` composable; wave 2 = the
  `@mercury/ui` DateField home. Each wave finished inside a spawn's lifetime, and the on-disk tree was the
  recovery surface — no wave restarted from zero.
- **The foundation/consumer split is clean.** INV-6 is structural, not incidental: `@mercury/ui` consumes
  dates only through `@mercury/core`, so the date dependency stays in one package.

### Watch
- **Subagent infra fragility.** Three deaths to `ECONNRESET` on a long read-heavy run is an environment
  cost the loop now routes around (pre-ground + wave-split + recover-from-tree), not a code defect — but
  it remains a standing tax on any rung that composes an intricate existing surface. Re-audit `LAW-1b`
  next rung: did the predicted death stay away under a pre-ground dispatch?
- **Registry artifact (advisory, not a defect).** The wave-split recovery left `registered (7) > spawned
  (3)` → a `FAKE-N` advisory in the scope. Expected for a connection-death recovery where waves were
  registered without a paired `aaw_spawn`; recorded here so the next reader does not mistake it for a
  delegation violation.

### Carry-forward
- **mx.7.3.2 `Calendar` reuses the foundation via a sibling `useCalendar`.** The date machinery now lives
  in `@mercury/core` (`internal/date-time/*`); the next rung composes `Calendar` from a sibling composable
  rather than re-importing `@internationalized/date` into `@mercury/ui` — INV-6 extends unchanged. Dispatch
  it write-ready (front-load the `useDateField`/`useCalendar` signatures + the DateField call-shape as the
  ≤2–3 named files) and wave-split it (core composable, then ui home) per `LAW-1b`.

