# mx.6 — Apps-side Pages — DROPPED

> **DROPPED 2026-06-29 — Operator-ruled "skip apps at all."** No page-level stories for any app.
> Superseded by the new ladder **mx.7 · mx.8 · mx.9** (import · stories · showcase).

The apps-side Page deliverable this rung specified is cancelled. By the time of the drop, three of the
five apps it targeted (`showcase`, `catalogue`, `docs`) had already been retired from the workspace;
only `echomq` + `mobile` (product demos) and `storybook` (the host) remain as workspace members. The
component-documentation surface those apps carried is re-founded fresh as the **mx.9** showcase
application — so the value mx.6 chased is not lost, it moves.

The new Movement-III tail:

- **mx.7** — import the Claude-Design bundle's net-new components into `@mercury/ui`, translated to the
  `.mx-*` + token idiom (additive — the barrel master invariant holds).
- **mx.8** — enrich the Storybook stories: palette switching · roundings · variant switching · actions ·
  real-world usage scenes.
- **mx.9** — ONE comprehensive showcase application (library browser · documentation · API reference ·
  do/don't · recipes), replacing the retired apps.

The cancelled triad's other files (`mx.6.stories.md`, `mx.6.llms.md`, `mx-6.progress.md`,
`mx-6.registry.json`) were deleted on drop; this tombstone is the directory's only remaining file. See
the roadmap [`../../mercury.roadmap.md`](../../mercury.roadmap.md) and the dashboard
[`../../mercury.progress.md`](../../mercury.progress.md).
