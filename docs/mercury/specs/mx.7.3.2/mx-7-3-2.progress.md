# mx-7-3-2 — AAW scope ledger

## Retrospective — mx.7.3.2 (Apollo, standing-mentor pass, 2026-06-30)

Post-ship, propose-only. Rung shipped **BUILD-GRADE** (Director-verified; independently re-verified here).
`@mercury/ui` barrel grew +1 component folder (`Calendar` at `index.ts:51`, 0 removed/renamed); the resolved
`dist/index.d.ts:33` carries the Calendar `export *` (master invariant additive). Independent gate
(scoped `@mercury/*`, Node 22.18.0 / pnpm 10.17.1): the three package typechecks + `@mercury/ui build` +
`sb:typecheck` + `sb:build` all EXIT 0; `sb:build` registers `Inputs/Calendar` (prior + 1, 275 modules).
INV-2/3/4/6/8/11 greps clean; INV-6 holds (no `@internationalized/date` *import* in `mercury-ui/src` — the
two textual hits are a doc-comment and the contract; the date layer arrives through `@mercury/core`'s
`useCalendar`). A2 ruled **arm (a)**, consistent with mx.7.3.1: the sibling `useCalendar` composes the same
owned `internal/date-time` layer (`createFormatter`, `getLastFirstDayOfWeek`, `toDate`) the `useDateField`
hook established. The grid machine holds under adversarial probe (controlled/uncontrolled split, functional
month paging, today/selected/outside states, 42-cell 6-week alignment, the `chevron-right`+CSS-flip nav).

### Went well
- **The mx.7.3.1 carry-forward landed verbatim — close-the-loop confirmed.** Last rung predicted
  "`Calendar` reuses the foundation via a sibling `useCalendar`" rather than re-importing
  `@internationalized/date` into `@mercury/ui`. Shipped exactly: `Calendar.tsx` imports only from
  `@mercury/core`; the new logic in `use-calendar.ts` is the grid assembly + month paging, everything else
  delegated to the established date layer. The guardrail held; INV-6 extends unchanged.
- **The 6-week grid is correct at the edges a green render hides.** `getLastFirstDayOfWeek` pins the 1st into
  row 1 and the fixed 42 cells always cover the month (max 31 + ≤6 leading = ≤37 ≤ 42); the
  `isOutsideMonth` check on `.month` alone is safe because a 42-day window cannot span a same-month-number
  collision across years. The weekday headers derive from `gridStart` so columns and labels align.
- **Controlled/uncontrolled is the canonical split.** `isControlled = value !== undefined`; `select` mutates
  internal state only when uncontrolled and always fires `onChange`. No state desync.

### Watch
- **INV-5's type-system backstop is illusory (pre-existing, out of scope, routed to the Operator).** The
  glyph is real (`chevron-right` ∈ `ICONS`), so INV-5 *holds as-built* — but it is enforced by **manual
  verification, not the type system**. `Icon.tsx:4` annotates `const ICONS: Record<string, ReactNode>`, so
  `IconName = keyof typeof ICONS` widens to `string`; `<Icon name="chevron-left" />` (a non-existent glyph)
  type-checks clean. The "a typed name makes typecheck the backstop" framing proves nothing. This is the
  `Icon` foundation (shipped before this rung), affects every `Icon` consumer, and is **out of mx.7.3.2's
  edit surface** — recorded, not fixed. See the Operator fork + the Venus guardrail below.
- **`visibleMonth` does not follow a later controlled `value` change (observation, not a defect).**
  `visibleMonth` is independent nav state seeded once from `value ?? defaultValue ?? today`
  (`use-calendar.ts:84–86`); changing a controlled `value` to a different month updates the selection but
  not the displayed month. Spec-consistent (paging is scoped separate from selection; arrow-key roving is
  out of batch). Moot for the mx.7.4 `Popover` date-picker (mount-on-open). Flagged for the Operator only if
  value→month follow is wanted.

### Carry-forward
- **Tokens/CSS added (reuse, do not re-add):** the `.mx-calendar` block (`additions.css:565–610`) + its
  `--mx-cal-accent` ramp (reuses the mx.7.1 `--<id>-9` set + `--fg-on-brand`); default `--mx-cal-accent:
  var(--iris-9)`. No new tokens minted.
- **The owned date layer is established** — `@mercury/core` `internal/date-time/*` now backs both
  `useDateField` and `useCalendar`. A further date component composes a sibling composable; never
  re-import `@internationalized/date` into `@mercury/ui` (INV-6).
- **Re-audit next rung (LAW-1b / guardrail close-the-loop):** did the dispatch run write-ready without the
  mx.7.3.1 `ECONNRESET` deaths? If a future date/enum/named-token rung lands, did the Venus type-narrows
  guardrail (below) keep an INV from claiming a false type-level backstop?

---

## Apollo — mentor proposals (Director ratifies)

> Propose-only. These are NOT applied. The harness fences a peer's self-edit; an agent-def change is a
> process change the Director owns and commits. Each guardrail is aimed at the charter whose contract the
> finding implicates.

### P-1 — Venus (architect): SHARPEN the existing "a gate must specify its OWN liveness" bullet to cover the type-level case

**Finding (this rung).** INV-5 was authored as a runnable gate whose proof was the type system ("the nav
chevron resolves to a live `IconName`; an invented glyph would not type-check"). It does not: `IconName =
keyof (ICONS: Record<string, ReactNode>)` widens to `string`, so every name type-checks and the gate
no-ops to false-green. This is the SAME class as `venus.md:37–46` (a no-op satisfying a gate's letter),
in **type-level** form — so sharpen that bullet rather than stack a new one (one guardrail per recurring
finding). It is also the next level of the mx.7.2 L5 glyph lesson ("bundle glyph names ≠ live set"): even
the *live* `IconName` is not type-enforced.

**Implicates Venus's contract** (author invariants AS runnable gates; verify the gate actually proves the
outcome). Not Mars — Mars used a real glyph correctly; the build is right as-built.

**Proposed exact diff to `.claude/agents/venus.md`** — append to the bullet ending at line 46 (`…written
into the acceptance.`), one new sentence inside the SAME bullet:

```diff
   tightened INV-8/G5/G6 to require the live tier to prove its own liveness). Same "a check counts
-  only if it RUNS" class, written into the acceptance.
+  only if it RUNS" class, written into the acceptance. **The type system is a gate too — verify it
+  actually NARROWS.** When an invariant claims a typed value is its backstop ("a typed name makes
+  typecheck reject a bad value"), check the type literally narrows: a `: Record<string, _>`
+  annotation widens `keyof` to `string`, so every value type-checks and the gate no-ops to
+  false-green. Where the type does not narrow, specify a REAL gate (a test/grep that the value ∈ the
+  set) or state the enforcement is MANUAL (mx.7.3.2: INV-5 claimed the live `IconName` made typecheck
+  reject an invented glyph, but `IconName = keyof (ICONS: Record<string,ReactNode>)` = `string`, so
+  `<Icon name="chevron-left">` type-checks — the glyph was right by manual verification, not the type).
```

### P-2 — Operator fork (surfaced, NOT made): tighten the `Icon` glyph type so `IconName` narrows

**Pre-existing, out of this rung's edit surface; an Operator/Director decision, not an Apollo fix.** To make
INV-5 a real type-level gate across all `Icon` consumers, change `Icon.tsx:4` from the annotation to a
`satisfies` (or drop the annotation), so `keyof typeof ICONS` becomes the literal glyph union:

```diff
-const ICONS: Record<string, ReactNode> = {
+const ICONS = {
   ...
-};
+} satisfies Record<string, ReactNode>;
```

Then `IconName = keyof typeof ICONS` is `"arrow" | "check" | … | "chevron-right" | …` and
`<Icon name="chevron-left" />` becomes a compile error. **Trade-off:** this BREAKS any dynamic-name
consumer (`<Icon name={someString} />`) — that is exactly why it is an Operator fork, not a silent fix. If
the Operator declines the tightening, the alternative real gate is a unit test asserting each `Icon`
call-site's literal name ∈ `Object.keys(ICONS)`. Scope: a `foundations/Icon` change, a future rung — not
mx.7.3.2.

### P-3 — Close-the-loop (no new guardrail): the mx.7.3.1 carry-forward held

The mx.7.3.1 carry-forward (reuse the foundation via a sibling `useCalendar`; do not re-import
`@internationalized/date` into `@mercury/ui`) was predicted and landed verbatim — INV-6 extended unchanged.
The guardrail earned its place; nothing to sharpen. Recorded so the next reader sees the audit closed.

### No D-7 nits
The `Calendar.prompt.md` contract is accurate and complete: role · grounded `## Props` (match the `.tsx`) ·
`## The enum language` (the "iris default" claim is backed by `additions.css:576`) · `## Composition`
(DateField + Popover forward-tense + Icon, real relative paths) · cited `## Examples` · `## Notes`. No
proposal.
