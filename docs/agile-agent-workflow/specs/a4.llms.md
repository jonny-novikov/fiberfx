# A4 · The spec layer — agent brief (`.llms.md`)

> The machine-readable brief for a Claude **Author** building any module of chapter **A4 "The spec layer:
> specifications & acceptance"** (to be served at `/course/agile-agent-workflow/spec`). Read it with the chapter spec
> [`a4.md`](a4.md) and the user stories [`a4.stories.md`](a4.stories.md). The craft is the `agile-course-writer` skill;
> this brief is A4's specifics. **Parent:** [`aaw.roadmap.md`](aaw.roadmap.md), Part IV.

## Status & first step

A4 is **planned**: its landing `/spec` and modules A4.1–A4.7 are not yet built. **Author the chapter landing first**
(the keystone — model it on the A3 landing `html/agile-agent-workflow/roadmap/index.html`: a hero, a framing
interactive, the recap/overview, a `.mods` grid of A4.1–A4.7, References, pager), then fan out its modules. Add
`a4=spec` to the cms `--chapter-alias` when gating.

## What A4 teaches

How to write a specification that is precise, traceable, and accepted only when proven — *correct by definition*. A4
enters with the A3 roadmap (rungs that point at specs) and leaves the reader able to write a rung's spec and its
`.stories.md`, with a traceability chain that makes completion verifiable from the text alone.

## Structure (to build)

| Module | Title | Route |
|---|---|---|
| A4.1 | Specification by Example | `/spec/by-example` |
| A4.2 | The triad: spec, stories, agent brief | `/spec/the-triad` |
| A4.3 | Anatomy of a spec | `/spec/spec-anatomy` |
| A4.4 | From stories to a .stories.md | `/spec/to-stories` |
| A4.5 | Invariants: properties that must always hold | `/spec/invariants` |
| A4.6 | Traceability — correct by definition | `/spec/traceability` |
| A4.7 | Workshop — specifying Portal's engine | `/spec/workshop` |

(Slugs suggested; an Author refines its dive slugs from the stories.)

## Grounding (no-invent — the real exemplar)

The worked examples are the Portal's real rung triads under `docs/elixir/specs/`: a spec `phoenix/f6.N.md` (Goal,
Rationale 5W, Scope, Deliverables, Invariants, Definition of Done), a stories file `phoenix/f6.N.stories.md` (Connextra
+ Given/When/Then + INVEST + Coverage), and the contract `specs.approach.md`. Quote them **verbatim**; invent no rung,
invariant, or surface. The only Portal API to name freely is `Portal.ID.generate/1`/`decode/1`; the real F6 surfaces
(`Portal` facade, `%Portal.Error{}`, the f6.N rungs) may be cited as they appear in source. This course's OWN specs
(`a3.md`, `a4.md`, `a4.stories.md`) are a second, self-referential exemplar — A4 can show its own spec as a worked spec.

## Conventions every A4 page holds

Same as the course standard (see `agile-course-writer` skill + `a3.llms.md`): copy the design system verbatim (hub ←
`why/two-layers/index.html`, lesson ← `why/two-layers/spec.html`), reuse stamp `TSK0Ng9hnHJgW0`, accent elixir-purple;
segmented route-tag (`spec` links the chapter once built); canonical `.foot-cols` footer; ≥3 real vetted Sources; two
interactives per lesson (real computation, live `.geo-readout`, pure functions, degrade, prefers-reduced-motion, no
storage); a `.bridge` (principle → Portal practice) + `.take`; the model pager (hub `prev`=landing, `next`=first dive;
dives chain). md-first under `docs/agile-agent-workflow/content/spec/<module>/`.

## Sources (the vetted set for A4)

Specification by Example → `https://gojko.net/books/specification-by-example/`; User Stories Applied →
`https://www.mountaingoatsoftware.com/books/user-stories-applied`; Gherkin reference →
`https://cucumber.io/docs/gherkin/reference/`; INVEST in Good Stories →
`https://xp123.com/articles/invest-in-good-stories-and-smart-tasks/`; Continuous Delivery →
`https://continuousdelivery.com/`. Never fabricate a URL.

## Acceptance (correct by definition)

Ship only at `STATUS: PASS` — all ten gates (`--chapter-alias …,a4=spec --require-refs`), the gate-invisible checks,
and the relevant Given/When/Then from [`a4.stories.md`](a4.stories.md). Never run git; edit only the module's own files
+ md sources; do not touch the chapter landing or living views (the Operator syncs them).

## Related

- Chapter spec: [`a4.md`](a4.md) · user stories: [`a4.stories.md`](a4.stories.md) · the plan: [`aaw.roadmap.md`](aaw.roadmap.md).
- Cross-link (once resolving): A0.2.2 four-artifacts `/what/four-artifacts`, A2.04 acceptance `/decomposition/acceptance`,
  A2.03 INVEST `/decomposition/invest`, A3.3 roadmap-anatomy `/roadmap/roadmap-anatomy`, and `/elixir/phoenix`.
