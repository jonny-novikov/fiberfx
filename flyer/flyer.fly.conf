# flyer.fly.conf - Fly.io PostgreSQL configuration
# Use with: ./flyer --config flyer.fly.conf pg <command>

postgres {
    host       localhost;
    port       25432;
    database   codemoji_game;
    user       fireheadz_studio;
    password   9El4M7mB5Bkxqv5;
    export_dir /tmp/codemoji-migration;
    sql_dir    /Users/jonny/dev/fireheadz/phoenix/sql;
}
