---
name: bcs-family-tooling
description: The bcs-* cross-course calibration family (bcs-writer skill + /bcs-reconcile + /bcs-author + guide) that re-grounds /redis-patterns and /echomq in the new docs/echo/bcs/bcs.N.md manuscript
metadata: 
  node_type: memory
  type: project
  originSessionId: 889bdb0d-b446-4014-8ea3-137fa9024fac
---

Built 2026-06-25. The new BCS manuscript `docs/echo/bcs/bcs.N.md` (B0–B8, ALL built; the old `docs/echo/bcs/content/bcsN.*` figure source is RETIRED/absent) changed the direction of the two consumer courses, so a **cross-course `bcs-*` family** now calibrates them from one source of truth:

- **`bcs-writer` skill** (`.claude/skills/bcs-writer/` — SKILL.md + `references/bcs-canon.md`): a calibration **OVERLAY**, NOT a per-course craft skill. Loaded ON TOP of `redis-course-writer` (R-chapters) / `echo-mq-writer` (E-chapters); owns only the cross-cutting direction. Disagreement rule: bcs-writer wins on cross-cutting facts (surface names, figure source, doors); the per-course skill wins on identity/craft (re-skin vs dark-editorial). It does NOT re-skin.
- **`/bcs-reconcile R<N>… E<N>…`**: brings EXISTING pages of either course to the new direction in one cross-course run (R=redis, E=echomq). For an unbuilt echomq pillar it builds-to-target (echo-mq-writer model).
- **`/bcs-author <course> <chapter> <module…>`**: greenfield sibling — authors NEW pages born in the new direction.
- **Guide**: `docs/echo/bcs/bcs.course-tooling.guide.md` (human-facing how-to).

**The five calibration deltas** (verbatim in the skill): (1) figure source `content/bcsN.*` → `bcs.N.md`; (2) EchoCache → EchoStore (`echo/apps/echo_store`); (3) Exchange (deleted) → codemojex (`echo/apps/codemojex`, manuscript B7); (4) the NEW persistence floor tier (`EchoStore.Graft.*`/CubDB · Rust `echo_graft`/Fjall · Tigris `If-None-Match` fence · the durability dial · Oban the comparison) + a NEW door `/echo-persistence` (real, `html/echo-persistence`); (5) refined branded-id canon (3-char ns + 11 Base62, epoch `1704067200000`, boot vectors `placement("USR0KHTOWnGLuC")=234878118` / `parse("USR0NgWEfAEJfs")={:ok,"USR",320636799581945856}` / `decode("USRzzzzzzzzzzz")=:error`).

**Numbering** (in canon-digest §5): R0–R8 redis (r3=queues, r4=time-delay-priority); E0–E5 echomq NEW three-pillar spine (E0 overview · E1 protocol · E2 queue · **E3 bus** · E4 cache · E5 proof) — the retired E0–E8 numbering does NOT apply. **Collision gotcha:** unlike `/redis-reconcile` (aliases B<N>→R<N>), in the bcs-* family `B<N>` = the MANUSCRIPT chapter (the source, never a target); redis is always R<N>. `html/echomq/bus` is NOT yet built → E3 is build-to-target. cms path = `go/jonnify-cms` (NOT the stale `apps/jonnify-cms`); gate adds `--routes-from /echo-persistence=html/echo-persistence`.

**Calibration banners** added (2026-06-25) to the stale tooling: echomq side (EchoCache/Exchange actively wrong) — `echo-mq-writer` SKILL + course-map + `echo-mq-reconcile` command; redis side (figure-source only, else current) — `redis-course-writer` SKILL + `redis-expert` agent. Each defers to bcs-writer.

**First run DONE (2026-06-25, `/bcs-reconcile R3 R4 + echomq protocol + queue`):** 90 pages reconciled, 90/90 gate PASS, all scrubs 0. Redis R3 queues + R4 time-delay-priority = DEEP reconcile (re-skin dark→contract-sheet + re-ground off the DELETED Go port `apps/echomq-go` → real `EchoMQ.{Jobs,Consumer,Stalled,Lanes,Repeat,Backoff,Metronome}` Elixir + Portal→codemojex + the 5 deltas; 6 redis-expert agents/chapter + 1 chapter-root agent for the landing+3 chapter-dives — the chapter-root pages were a FAN-OUT GAP I caught in adversarial verify). echomq protocol+queue = LIGHT additive (already calibrated, corrective deltas 0): delta-5 id-canon vector `234878118` added to protocol/immutability; delta-4 `/echo-persistence` door + `EchoStore.StreamArchive`→Graft on the queue's 5 durability-frontier pages (2 general-purpose agents, no echo-mq-expert registered → fallback). Living views synced (queues.md/time-delay-priority.md specs + toc + echomq-doors.md + echomq content-map/course-map; both chapter llms.txt rewritten). Fixed the echomq home's dangling `/bcs/trading`→`/bcs/codemojex` (Exchange→codemojex, B7). **Gotchas:** the R3/R4 chapter SPECS grounded in the deleted Go port (the v1-vs-v2 trap); R4 priority is RETIRED-by-design (`lanes.ex` — no `getPriorityScore`; fair-lane weight `Lanes.wclaim/@gwclaim` + mint-order); `EchoMQ.Repeat` is interval-only (`every_ms`, EXISTS-upsert, no cron); real schedule key is `emq:{q}:schedule` not `:delayed`; the echomq md-mirror tree is `docs/echo/echo_mq/markdown/` NOT `docs/echo_mq/course/markdown/`; `EchoMQ.Keys\b` scrub needs the word boundary (else matches `EchoMQ.Keyspace`).

**Second run DONE (2026-06-25, `/bcs-reconcile` gap-fix + calibration):** (1) **RETIRED the orphaned echomq legacy** — `html/echomq/{core,substrate}` (21 pages, retired E0–E8 numbering, citing the DELETED Go port `apps/echomq-go` + frozen `echo/apps/echomq`) were unreachable from the live six-pillar course (home/overview/protocol/queue link in = 0; they linked OUT to dead routes) → `rm -rf` html + md mirrors, dropped 21 sitemap `<url>` blocks; Operator chose Retire (AskUserQuestion) over reconcile-in-place. Live spine now exactly **overview·protocol·queue** (built) + bus·cache·proof (soon). (2) **Dragonfly→Valkey 9** on the 9 live pages it survived on (1 general-purpose agent): reframed "thread-per-shard engine" → **Valkey Cluster hash-slot** (CRC16 of the `{q}` `{hashtag}` → 1 of 16384 slots; CROSSSLOT-safety is WHY declared-keys+hashtag exist); doc-link `dragonflydb.io/.../flags` → `valkey.io/topics/cluster-spec/` (1 used `commands/cluster-keyslot/` to dodge a dup); dropped `--lock_on_hashtags`. (3) **Removed "EchoMQ 2.0"**: `echomq:2.0.0`→**`echomq:2.4.2`** (the REAL `@wire_version`, `echo/apps/echo_mq/lib/echo_mq/stream_consumer.ex:91` — NOT 2.0.0, NOT 3.0.0; the 3.0 cutover is PENDING, "3.x"/Stream-Tier-3.0 = delivery name not wire stamp). (4) **`redis-under-portal`→`redis-under-game`**: dir+md-mirror-dir+md-file renames + 35-file content sed + 4 sitemap locs. (5) **bare prose `R[N].M`→links** (~12, redis-expert agent via each chapter's llms.txt as the R-code→slug map; link first prose mention only, skip route-tags/pagers/kickers/hub-num-spans/ASCII-diagram-comments/JS-strings/self-refs). (6) **R2 coordination got its missing `/bcs/bus` door** (was bare `/bcs`); redis agent do-no-harm-fixed a pre-existing dangling `/bcs/bus/fence-and-keyspace`→`/bcs/bus/the-keyspace` on 2 pages. (7) **Door-map remapped** (`redis-patterns.echomq-doors.md`) E0–E8→six-pillar + **Portal→codemojex** + added the /bcs-door table; echomq home md mirror + protocol mirror rewritten to as-shipped six-pillar. (8) **CALIBRATED** SKILL §1a (3 standing disciplines: Valkey-only/version-2.4.2/reachability-retire-orphans) + §3 scrubs (0 dragonfly, 0 echomq:2.0.0) + canon §5 + command Steps 0(reachability)/3. **Gotchas:** the cms gate CANNOT resolve a route-root+fragment link (`/echomq#course-map`) even when the `id` exists → use a same-page `#anchor` (do-no-harm-fixed an Operator's UNCOMMITTED home edit that introduced it); reachability-before-reconcile is the load-bearing new discipline (a blind sed "succeeds" on orphans while preserving stale citations). All 20 touched pages + both homes gate PASS; scrubs all 0.

**Third run DONE (2026-06-25, `/bcs-writer` greenfield via the bcs-author flow): authored R5 opening + UNLOCKED the EchoMQ Bus pillar.** 10 new pages, all gate PASS (R5 module pages 10/10 once the landing landed): **/redis-patterns/streams-events** — the R5 chapter landing + **R5.01 `streams-event-sourcing`** (hub + 3 dives: the-append-only-log/replay-and-rebuild/the-cursor) + **R5.02 `streams-consumer-patterns`** (hub + 3 dives: the-blocking-read/consumer-groups/maxlen-trimming); and **/echomq/bus** — the Bus pillar landing (dark-editorial, the broadcast + retained-log thesis, 6 soon module cards). Formation: 3 agents one wave (2 redis-expert for R5.01/R5.02 + 1 general-purpose echo-mq-writer for the Bus landing) → 1 redis-expert for the R5 landing (after modules existed) → orchestrator did both home relinks + living views. **Grounding = the real SHIPPED Stream Tier** (`EchoMQ.Stream.{append,read,read_window,read_since,trim}` → `emq:{q}:stream`; `EchoMQ.StreamConsumer` consumer-groups/ack/resume; `EchoMQ.StreamRetention.sweep`; `EchoMQ.Events`/`EchoMQ.Cancel` the pub/sub contrast; `EchoStore.StreamArchive.fold` → Graft floor → `/echo-persistence`; brand `EVT`; manuscript `bcs.3.md` §B3.3). **KEY CALIBRATION:** the stream tier (emq3.1–3.6) **shipped 2026-06-23 = REAL CODE**, so the Bus pillar grounds in real surfaces NOT `[RECONCILE]`-canon — the `echo-mq-writer/references/course-map.md` Bus/Cache grounding rows (55/56) were STALE (said "stream-tier canon → `[RECONCILE]`-shadowed" + `EchoCache.*`/`.Shadow`); fixed to real `EchoMQ.{Events,Stream,StreamConsumer,StreamRetention}`+`EchoStore.StreamArchive` / `EchoStore.{Table,…}`. **Gotchas:** the R5 chapter spec (`specs/streams-events/streams-events.md`) is STALE (grounds in the deleted Go port `apps/echomq-go/.../events.go` + "QueueEvents" + `echomq:cancel` + "Portal") → wrote `streams-events.prompt.md` with the re-homed grounding as the authoritative brief + added a supersession banner to the spec; the agents' honesty win — `EchoMQ.Cancel` is worker-side COOPERATIVE (distributed cancel = emq.6, UNBUILT) so the Bus landing kept it off the module grid + I fixed the door-map/applied-block "distributed cancel over the bus" overclaim → "pub/sub events + the retained log"; **zsh does NOT word-split an unquoted `$D` var** (a multi-dir scrub silently errored to 0 — a false-clean; pass paths explicitly). Living views: redis home (R5 soon→built, footer link, booknote) + echomq home (Bus chap-link + dropped "on the build front" + fixed stale "cache's shadow"→"durable floor") + toc R5 block (real grounding + ✓built + fixed R5.04 slug collision + codemojex) + door-map (Bus built + R5 built) + echomq content-map (Bus here-now) + chapter `streams-events/llms.txt` (redis convention = chapter-level only; echomq pillars have NO llms.txt). Bus landing stamp `EMQ0Nb1VTbfnu4`. **Still owed:** the deeper `echo-mq-writer/course-map.md` lines 78/80/103-108 still describe the stream tier as `[RECONCILE]`-canon (only the table rows fixed) → a full echo-mq-writer reconcile; the echomq home Bus module grid (5 cards) vs the Bus landing grid (6 cards) differ slightly (both soon, harmless).

**Fourth run DONE (2026-06-25, `/bcs-reconcile` WIRE-VERSION reconcile — Operator directive "echomq:3.0.0 in every single place; omit v1/2.0; courses about current version 3.x, just 'EchoMQ' in prose").** **GROUND-TRUTH CORRECTION:** the as-built `@wire_version` is **`echomq:3.0.0`** (`echo/apps/echo_wire/lib/echo_mq/connector.ex:35` — the wire stamp HAS cut over since the Stream Tier went whole 2026-06-23; the prior §1a.B `2.4.2` pin was read from a `stream_consumer.ex` COMMENT, not the constant → stale). Reconciled: (a) **wire constant** `echomq:2.0.0` (redis ×13) + `echomq:2.4.2` (echomq the-version-fence + llms) → **`echomq:3.0.0`** across served HTML + md mirrors (28 subs / 22 files, guarded script `/tmp/reconcile_wireversion.py`); (b) **version-history FRAMING stripped to present-tense** — redis `reserved-tier.html` v1-fork interactive figure STRUCTURALLY REBUILT (4 nodes→3: dropped the "frozen v1 line / push source / absorbed / three movements / version 2.0.0" node; `viewBox="190 0 920 330"` pan re-centres the kept 3 with no coordinate surgery; SVG+segbar+JS `PARTS`+readouts all consistent at 3, gates PASS) + the-immutable-core ("the break / v1 line / 1.3.0") + bcs `bus/index`+`the-keyspace`+`the-stream-tier` ("EchoMQ 3.0" / "could not become 2.0" / "three movements") + the MANUSCRIPT **`docs/echo/bcs/bcs.3.md`** (the SOURCE — reconciled so it can't re-seed drift on the next authored page); (c) **calibration moved 2.4.2→3.0.0**: bcs-writer §1a.B + §3 scrub, bcs-canon §5, bcs-reconcile Steps 0/3, the `reframe-echomq.md` contract (naming-law + figure rows incl. the `@wire_version "echomq:3.0.0"` as-built row) + `coordination.md` + `echomq-doors.md`. Scrub patterns WIDENED to flag the OLD versions as drift: `echomq:2\.[0-9]\.[0-9]|EchoMQ [0-9]\.[0-9]|v1 line`. 16/16 touched HTML gate PASS; served narrative + manuscript + live authority = **0 stale version refs**. **`reserved-tier.html` got a structural figure surgery** (the single hardest page — its whole subject was the v1→v2 fork). **DELIBERATELY LEFT (records-freeze):** the spent per-rung prompts (`specs/time-delay-priority/r4.04-06.prompt.md`'s "mandatory EchoMQ 2.0 focus" + `specs/reframe-echomq/re0.*`) + the `redis-patterns.progress.md` ledger — faithful records of past rungs (regenerate on their chapter's own reconcile; rewriting falsifies history). **`/echo-persistence` DONE (2026-06-25, Operator confirmed via `/bcs-reconcile echo-persistence`):** the separate persistence course (already on `echomq:3.0.0`) carried deliberate "EchoMQ 3.0" / "EchoMQ 4+" product-label prose (16 html + 9 `docs/echo-persistence` md-mirror edits across 8 files) → stripped to plain "EchoMQ" / "the platform"; the `the-stream-tier-ladder` page's FACTUALLY-STALE claim ("only the eventual `echomq:3.0.0` is a major … deferred") fixed to "the bus now speaks `echomq:3.0.0`" (the cutover landed). KEPT: the `emq3.1–emq3.6` spec-rung citations (real grounding sourced to `emq.streams.md`, NOT product labels — 54 across the course) + the surviving `echomq:3.0.0` constant. **Do-no-harm bar, NOT PASS:** echo-persistence is its OWN design system (served raw by echo-static, `docs/echo-persistence` = its md mirror) — its `platform/*` pages FAIL the BCS cms gate at BASELINE on a pre-existing `/echo-persistence#map` route-root+fragment dangling link (the known cms limitation; the `id` exists), nothing my text edits touched; `index.html` stayed PASS→PASS. LEFT (not served narrative): `echo_mq.course.md`'s retired-framing list (NAMES "EchoMQ 2.0" to document its removal, like a scrub pattern) + `docs/echo_mq/kb/echo-bus-v3/` (a version-organized internal KB). **Closure run (2026-06-25, "reconcile remaining found gaps"):** the redis-side COURSE calibration (`redis-course-writer` SKILL+course-map · `redis-reconcile` · `redis-write`) reconciled to `echomq:3.0.0` + no-version-label — it MIRRORED the old bcs-writer §1a.B (still pinned `echomq:2.0.0` + "tell the v1 history"), so a new redis page would have re-seeded the drift; and the spent per-rung prompts (`r4.04-06.prompt.md` + `re0.{md,prompt,stories,llms}`) swept (constant `echomq:2.0.0`→`3.0.0` via `/tmp/reconcile_spent_prompts.py` + label "EchoMQ 2.0"→"EchoMQ" + the "Verified 2.0 facts"/v1-lineage-note framing → present-tense). The only version strings now in ANY course/calibration file are the SCRUB-RULE texts that NAME the forbidden labels (`SKILL.md:87`, `reframe-echomq.md:43-44` — correct, like a grep pattern). **LEFT BY DESIGN (a domain boundary, not laziness):** (1) the `redis-patterns.progress.md` LEDGER (records-freeze — an audit trail; its entries also carry other point-in-time-stale facts e.g. "Dragonfly-native"); (2) the entire `docs/echo_mq/` PROGRAM canon (~445 `echomq:2.x` — a DIFFERENT domain governed by the echo_mq CLAUDE.md + lead-team; it records the version ARC as engineering history — the rungs genuinely shipped over `echomq:2.4.2`, rewriting it would falsify history). **FLAG for the echo_mq program team:** `.claude/skills/echo-mq-surface.md:26` still maps the as-built `@wire_version "echomq:2.0.0"` — factually stale vs the verified `echomq:3.0.0` (`echo_wire/connector.ex:35`); a one-line program-doc fix, left for that domain.

**Fifth run DONE (2026-06-25, `/bcs-writer R5.05 + echomq Bus 03 with 3 dives`): closed R5 + grew the Bus pillar.** 5 new pages, all gate PASS (independently re-gated), all BCS scrubs 0. **/redis-patterns/streams-events/workshop** — the **R5.05 single-page workshop** (a live **codemojex** activity feed on the real Stream Tier: a guess/score/settle event appended via `EchoMQ.Stream.append/4`→`EVT`, the feed a fold via `read/6`, consumed by `EchoMQ.StreamConsumer`, bounded by `EchoMQ.StreamRetention`, archived by `EchoStore.StreamArchive`→`/echo-persistence`; surfaces `Codemojex.{Guesses.submit/3,ScoreWorker,Settle.close/1,Scoring.score/2,Rooms.close_game/1,Notifier.notify/3}` — the agent SELF-CAUGHT + fixed an invented `Notifier.post`, the no-invent discipline working). **/echomq/bus/the-consumer-group** — **Bus module 03** (hub + 3 dives `the-group-door`/`recover-self-then-peers`/`at-least-once-and-the-handler`), grounded ENTIRELY in `EchoMQ.StreamConsumer` (the reader law: lazy `XGROUP CREATE…MKSTREAM` swallow-only-`BUSYGROUP` + DECLARED `:group_start` no-default; drain-own-PEL recovers SELF then `XAUTOCLAIM` recovers dead PEERS; the at-least-once handler `%{id,payload,attempts,group}` `:ok`→`XACK`/error→leave-unacked; the **order-theorem PEL exception** = a re-claimed entry returns OUT of mint order → handler MUST be idempotent, the branded id the dedup key; **NO Lua** — `XGROUP`/`XREADGROUP`/`XACK`/`XAUTOCLAIM`/`XPENDING` issued DIRECT). Formation: 2 agents one wave (1 redis-expert R5.05 + 1 general-purpose echo-mq-writer Bus 03) → orchestrator gated + relinked + synced (no echo-mq-expert agent registered → general-purpose + echo-mq-writer skill, the standing fallback). Briefs EXTENDED IN PLACE: `docs/echo_mq/course/bus.prompt.md` (added MODULE 03 + the `EchoMQ.StreamConsumer` floor bullet — now the template for modules 04/05) + `streams-events.prompt.md` (MODULE R5.05, replacing the "do NOT build" deferral). Living views: Bus landing (module-03 card+footer `soon→built`) + R5 landing (workshop card `soon→built` + a `#workshop` CTA) + `echo-mq-writer/course-map.md` §2/§9 (Bus 01/02/03 built, next 04 time-travel) + redis TOC (R5 `✓complete`, R5.05 `✓built`, the course-status summary) + `streams-events.md` mirror status line. EMQ stamp `EMQ0OGUWI87UdF`. **Gotcha — systemically-stale secondary view:** the redis ROADMAP (`redis-patterns.roadmap.md`) status column marks R3/R4/R5 ALL `○ planned` despite all three being built, and its `→ E6,E7` door cells name nonexistent pillars → LEFT the table (the TOC is the authoritative live view; a one-cell R5 fix would imply vouching for a stale row) — flag for a dedicated roadmap reconcile (R3/R4/R5 status + the E0–E8→six-pillar door re-home).

**Sixth run DONE (2026-06-25, `/bcs-author echomq module 04 + 05 + workshop`): the EchoMQ Bus pillar is COMPLETE.** 9 new pages, all gate PASS (independently re-gated), all scrubs 0. **/echomq/bus/time-travel** (module 04, hub + 3 dives `time-is-the-address`/`the-two-reads`/`backtest-audit-debug`) — `EchoMQ.Stream.{read_window/6,read_since/5,minid_floor/1,maxid_ceil/1}`: **time IS an id position** (`"<ms>-0"` floor / `"<ms>-<0x3FFFFF>"` ceil, exact edges proven in the moduledoc; `ms = DateTime.to_unix(dt,:millisecond)`), **zero new Lua** (delegates to the byte-frozen `read/6`/`XRANGE`). **/echomq/bus/retention-and-archive** (module 05, hub + 3 dives `retention-is-a-policy`/`nothing-is-lost`/`the-door-to-persistence`) — `Stream.trim/4` (`XTRIM MAXLEN ~`/`MINID`; `~` SAFE default, **INV4** = never deletes inside the window) + `EchoMQ.StreamRetention` (opt-in `:transient` driver, declared BEAM-side `:policy`, DECOUPLED from consumer liveness) + `EchoStore.StreamArchive.fold/3` (CubDB pages at `@archive_base = 2^49` **disjoint by construction**, the `W` watermark = a branded `EVT` id **NEVER `head_lsn`**, **fold-before-trim**, `merge_read/5` split on W) → the **`/echo-persistence`** door (`<a>`-linked). **/echomq/bus/workshop** (single `workshop.html`, folds all 5 modules — a **codemojex** retained activity feed; doors to the redis R5.05 same-feed workshop). Formation: **WAVE 1** = modules 04 + 05 concurrent (general-purpose + echo-mq-writer + bcs-writer); **WAVE 2** = the workshop (after 04/05, so it links all 5) → orchestrator gated + relinked + synced. `bus.prompt.md` gained MODULE 04/05 + WORKSHOP sections + the StreamRetention/StreamArchive floor bullets (the templates). Living views: **Bus landing** (cards 04/05/06 + footer `soon→built` — all 6 built) + **echomq home Bus grid RESTRUCTURED 5→6 cards** all built (resolves the Fifth-run 5-vs-6 discrepancy) + course-map §2/§9 (**Bus COMPLETE**, next Cache) + door-map (Bus COMPLETE + the `/echo-persistence` edge). **Gotchas:** (1) a wave-1 agent (module 05) was **REJECTED on its first tool calls** (it ran `find`/`cat` over `~/.claude` → a permission gate, died at 0 tool_uses) → **re-ran as a lone agent with the EXACT skill path** (`/Users/jonny/dev/jonnify/.claude/skills/…`) + "use Read, never `find`/`cat` over `~/.claude`" — passed; ADD that tooling-note to every fan-out brief. (2) the **zsh unquoted-var false-clean** trap bit AGAIN (a multi-path `$NEW` scrub errored to all-0 — a false-clean) → pass scrub paths **EXPLICITLY**. (3) **NON-DEFECT (canon already fixed):** an agent flagged using brand `PLR` — which IS real (`generate!("PLR")` on disk); bcs-canon §4 + SKILL §2.3 were **already corrected** (Operator) from the stale `USR/RMM/RND` to the real **`PLR·ROM·GAM·SES·GES`** — the session-start skill context was stale. 3 agents grounding on `generate!` literals re-verified the canon. EMQ stamp `EMQ0OGUWI87UdF`.

**Seventh run DONE (2026-06-25, `/bcs-writer` → finish Cache (coherence 03 + workshop) + build Proof (E5)): 🏁 the EchoMQ course is COMPLETE — all SIX pillars.** 15 new pages, all gate PASS (independently re-gated 15/15), all scrubs 0. **CACHE pillar COMPLETE:** `/echomq/cache/coherence` (module 03, hub + 3 dives `a-message-about-a-name`/`the-two-lanes`/`newer-wins-the-conditional-drop`) — `EchoStore.Coherence` "a message about a name" (29-byte id+version payload, `newer?/2` the 11-byte snowflake-payload compare ACROSS kinds, the two lanes broadcast/`Ring`-applier + job/`Journal`-outbox, the **`:coherence_drop` Lua** = the pillar's ONE Lua, taught two-beat byte-faithful → `/echo-persistence`) + `/echomq/cache/workshop` (a codemojex near-cache end to end). **PROOF pillar BUILT (E5):** landing + `the-conformance-suite` (01 — `EchoMQ.Conformance.scenarios/0`/`run/2`, **NO hard count** — the only "79" is a CSS color) + `telemetry-and-the-read-plane` (02 — `EchoMQ.Meter` the zero-cost `:telemetry` surface + `EchoMQ.Metrics` the read plane) + the `workshop`; the **Benchmark is a `soon` frontier card — NO surface on disk** (no-invent: named, never built). Formation: **3 waves of ≤2** (W1 Proof-landing + Cache-03 · W2 Proof-01 + Proof-02 · W3 Cache-ws + Proof-ws). Authored `proof.prompt.md` (new pillar brief) + extended `cache.prompt.md` (MODULE 03 + WORKSHOP + the Coherence/Ring/Journal floor). Living views: Cache landing (04 ws `soon→built`) + Proof landing (01/02/04 `soon→built`, 03 stays `soon`) + echomq home (Cache grid 03/04→built + the **Proof grid RESTRUCTURED** — flipped 01/02/04, **fixed a stale OTel/W3C invention on card 02** → the real Meter/read-plane, dropped "on the build front") + course-map §2/§9 (Cache+Proof COMPLETE → COURSE COMPLETE) + door-map (Cache+Proof COMPLETE + the summary). **Gotchas:** (1) the `:telemetry` module is **`EchoMQ.Meter` NOT `EchoMQ.Telemetry`** (the frozen-v1 name — the §3a scrub forbids it; agents taught Meter, 0 Telemetry). (2) TWO agents **stalled on the FIRST tool call** (a `find`/`cat` over `~/.claude` permission gate, or cut off mid-Skill-load) → re-ran lone with the EXACT skill path + "work steadily to completion" — both passed; the standing fix for fan-out briefs. (3) the **zsh unquoted-var false-clean** bit AGAIN (a `$M` mounts var → cms NO-STATUS on 15 pages; a `$NEW` multi-path scrub → all-0) → pass cms FLAGS **and** scrub PATHS EXPLICITLY, never via a var. (4) the home Proof grid pre-existed with PLANNED labels module 02 does NOT teach → corrected (a no-invent manifest sync). EMQ stamp `EMQ0OGUWI87UdF`. The Benchmark module is the one deferred frontier (build if/when a bench surface lands).

**Still owed:** the redis **roadmap status/door reconcile** (R3/R4/R5 `planned→built` + E0–E8→six-pillar doors) + the TOC conventions' stale `content/bcs3.*` figure-source (a delta-1 slip at `redis-patterns.toc.md:45`); the broad Portal residue in redis R1/R6/R7 toc rows + R7.07; the EchoCache→EchoStore corpus cascade for `/bcs`+`/mesh`+`/art`; restoring the absent `bcs-course-writer` skill the `bcs-expert` agent references; the deferred echomq **Benchmark** module (no bench surface on disk). Related: [[echo-store-rename]] [[redis-reframe-echomq]] [[redis-patterns-course]] [[echo_mq/echo-mq-three-movements]].
