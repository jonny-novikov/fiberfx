---
name: bcs-family-tooling
description: The bcs-* cross-course calibration family (bcs-writer skill + /bcs-reconcile + /bcs-author + guide) that re-grounds /redis-patterns and /echomq in the new docs/echo/bcs/bcs.N.md manuscript
metadata: 
  node_type: memory
  type: project
  originSessionId: 889bdb0d-b446-4014-8ea3-137fa9024fac
---

Built 2026-06-25. The new BCS manuscript `docs/echo/bcs/bcs.N.md` (B0–B8, ALL built; the old `docs/echo/bcs/content/bcsN.*` figure source is RETIRED/absent) changed the direction of the two consumer courses, so a **cross-course `bcs-*` family** now calibrates them from one source of truth:

- **`bcs-writer` skill** (`.claude/skills/bcs-writer/` — SKILL.md + `references/bcs-canon.md`): a calibration **OVERLAY**, NOT a per-course craft skill. Loaded ON TOP of `redis-course-writer` (R-chapters) / `echo-mq-writer` (E-chapters); owns only the cross-cutting direction. Disagreement rule: bcs-writer wins on cross-cutting facts (surface names, figure source, doors); the per-course skill wins on identity/craft (re-skin vs dark-editorial). It does NOT re-skin.
- **`/bcs-reconcile R<N>… E<N>…`**: brings EXISTING pages of either course to the new direction in one cross-course run (R=redis, E=echomq). For an unbuilt echomq pillar it builds-to-target (echo-mq-writer model).
- **`/bcs-author <course> <chapter> <module…>`**: greenfield sibling — authors NEW pages born in the new direction.
- **Guide**: `docs/echo/bcs/bcs.course-tooling.guide.md` (human-facing how-to).

**The five calibration deltas** (verbatim in the skill): (1) figure source `content/bcsN.*` → `bcs.N.md`; (2) EchoCache → EchoStore (`echo/apps/echo_store`); (3) Exchange (deleted) → codemojex (`echo/apps/codemojex`, manuscript B7); (4) the NEW persistence floor tier (`EchoStore.Graft.*`/CubDB · Rust `echo_graft`/Fjall · Tigris `If-None-Match` fence · the durability dial · Oban the comparison) + a NEW door `/echo-persistence` (real, `html/echo-persistence`); (5) refined branded-id canon (3-char ns + 11 Base62, epoch `1704067200000`, boot vectors `placement("USR0KHTOWnGLuC")=234878118` / `parse("USR0NgWEfAEJfs")={:ok,"USR",320636799581945856}` / `decode("USRzzzzzzzzzzz")=:error`).

**Numbering** (in canon-digest §5): R0–R8 redis (r3=queues, r4=time-delay-priority); E0–E5 echomq NEW three-pillar spine (E0 overview · E1 protocol · E2 queue · **E3 bus** · E4 cache · E5 proof) — the retired E0–E8 numbering does NOT apply. **Collision gotcha:** unlike `/redis-reconcile` (aliases B<N>→R<N>), in the bcs-* family `B<N>` = the MANUSCRIPT chapter (the source, never a target); redis is always R<N>. `html/echomq/bus` is NOT yet built → E3 is build-to-target. cms path = `go/jonnify-cms` (NOT the stale `apps/jonnify-cms`); gate adds `--routes-from /echo-persistence=html/echo-persistence`.

**Calibration banners** added (2026-06-25) to the stale tooling: echomq side (EchoCache/Exchange actively wrong) — `echo-mq-writer` SKILL + course-map + `echo-mq-reconcile` command; redis side (figure-source only, else current) — `redis-course-writer` SKILL + `redis-expert` agent. Each defers to bcs-writer.

**First run DONE (2026-06-25, `/bcs-reconcile R3 R4 + echomq protocol + queue`):** 90 pages reconciled, 90/90 gate PASS, all scrubs 0. Redis R3 queues + R4 time-delay-priority = DEEP reconcile (re-skin dark→contract-sheet + re-ground off the DELETED Go port `apps/echomq-go` → real `EchoMQ.{Jobs,Consumer,Stalled,Lanes,Repeat,Backoff,Metronome}` Elixir + Portal→codemojex + the 5 deltas; 6 redis-expert agents/chapter + 1 chapter-root agent for the landing+3 chapter-dives — the chapter-root pages were a FAN-OUT GAP I caught in adversarial verify). echomq protocol+queue = LIGHT additive (already calibrated, corrective deltas 0): delta-5 id-canon vector `234878118` added to protocol/immutability; delta-4 `/echo-persistence` door + `EchoStore.StreamArchive`→Graft on the queue's 5 durability-frontier pages (2 general-purpose agents, no echo-mq-expert registered → fallback). Living views synced (queues.md/time-delay-priority.md specs + toc + echomq-doors.md + echomq content-map/course-map; both chapter llms.txt rewritten). Fixed the echomq home's dangling `/bcs/trading`→`/bcs/codemojex` (Exchange→codemojex, B7). **Gotchas:** the R3/R4 chapter SPECS grounded in the deleted Go port (the v1-vs-v2 trap); R4 priority is RETIRED-by-design (`lanes.ex` — no `getPriorityScore`; fair-lane weight `Lanes.wclaim/@gwclaim` + mint-order); `EchoMQ.Repeat` is interval-only (`every_ms`, EXISTS-upsert, no cron); real schedule key is `emq:{q}:schedule` not `:delayed`; the echomq md-mirror tree is `docs/echo/echo_mq/markdown/` NOT `docs/echo_mq/course/markdown/`; `EchoMQ.Keys\b` scrub needs the word boundary (else matches `EchoMQ.Keyspace`).

**Second run DONE (2026-06-25, `/bcs-reconcile` gap-fix + calibration):** (1) **RETIRED the orphaned echomq legacy** — `html/echomq/{core,substrate}` (21 pages, retired E0–E8 numbering, citing the DELETED Go port `apps/echomq-go` + frozen `echo/apps/echomq`) were unreachable from the live six-pillar course (home/overview/protocol/queue link in = 0; they linked OUT to dead routes) → `rm -rf` html + md mirrors, dropped 21 sitemap `<url>` blocks; Operator chose Retire (AskUserQuestion) over reconcile-in-place. Live spine now exactly **overview·protocol·queue** (built) + bus·cache·proof (soon). (2) **Dragonfly→Valkey 9** on the 9 live pages it survived on (1 general-purpose agent): reframed "thread-per-shard engine" → **Valkey Cluster hash-slot** (CRC16 of the `{q}` `{hashtag}` → 1 of 16384 slots; CROSSSLOT-safety is WHY declared-keys+hashtag exist); doc-link `dragonflydb.io/.../flags` → `valkey.io/topics/cluster-spec/` (1 used `commands/cluster-keyslot/` to dodge a dup); dropped `--lock_on_hashtags`. (3) **Removed "EchoMQ 2.0"**: `echomq:2.0.0`→**`echomq:2.4.2`** (the REAL `@wire_version`, `echo/apps/echo_mq/lib/echo_mq/stream_consumer.ex:91` — NOT 2.0.0, NOT 3.0.0; the 3.0 cutover is PENDING, "3.x"/Stream-Tier-3.0 = delivery name not wire stamp). (4) **`redis-under-portal`→`redis-under-game`**: dir+md-mirror-dir+md-file renames + 35-file content sed + 4 sitemap locs. (5) **bare prose `R[N].M`→links** (~12, redis-expert agent via each chapter's llms.txt as the R-code→slug map; link first prose mention only, skip route-tags/pagers/kickers/hub-num-spans/ASCII-diagram-comments/JS-strings/self-refs). (6) **R2 coordination got its missing `/bcs/bus` door** (was bare `/bcs`); redis agent do-no-harm-fixed a pre-existing dangling `/bcs/bus/fence-and-keyspace`→`/bcs/bus/the-keyspace` on 2 pages. (7) **Door-map remapped** (`redis-patterns.echomq-doors.md`) E0–E8→six-pillar + **Portal→codemojex** + added the /bcs-door table; echomq home md mirror + protocol mirror rewritten to as-shipped six-pillar. (8) **CALIBRATED** SKILL §1a (3 standing disciplines: Valkey-only/version-2.4.2/reachability-retire-orphans) + §3 scrubs (0 dragonfly, 0 echomq:2.0.0) + canon §5 + command Steps 0(reachability)/3. **Gotchas:** the cms gate CANNOT resolve a route-root+fragment link (`/echomq#course-map`) even when the `id` exists → use a same-page `#anchor` (do-no-harm-fixed an Operator's UNCOMMITTED home edit that introduced it); reachability-before-reconcile is the load-bearing new discipline (a blind sed "succeeds" on orphans while preserving stale citations). All 20 touched pages + both homes gate PASS; scrubs all 0.

**Still owed:** echomq **bus (E3)** + **cache (E4)** + **proof (E5)** pillars (bus/cache/proof unbuilt or transitional → `/bcs-author`); the broad Portal residue in redis R1/R5/R6/R7 toc rows + R7.07; `the-version-fence.html` wire constant `echomq:2.0.0` is stale vs as-built `echomq:2.4.2` (connector.ex:35); the EchoCache→EchoStore corpus cascade for `/bcs`+`/mesh`+`/art`; restoring the absent `bcs-course-writer` skill the `bcs-expert` agent references. Related: [[echo-store-rename]] [[redis-reframe-echomq]] [[redis-patterns-course]] [[echo_mq/echo-mq-three-movements]].
