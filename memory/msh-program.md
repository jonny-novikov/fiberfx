---
name: msh-program
description: "docs/msh = the FORWARD msh program (hot-context engine + prompt builder for Claude agents over memory/ + docs/ + transcripts): genesis FOUNDED + msh2.1 SHIPPED 2026-07-02 (commits 18c34942 genesis · 7cd4af54 supersede banners · ff504aee msh2.1); rulings D-1..D-12 on the msh-genesis ledger; ladder msh2.2..msh2.10 (M1 substrate · M2 engine · M3 docs joins · M4 aaw authority); NEXT = msh2.2 frontmatter v2 + fenced backfill"
project: msh
metadata:
  node_type: memory
  type: project
  originSessionId: 80e48dfe-e5b7-4d5f-824a-9c8be119f878
---

**The program.** `docs/msh/` is the forward msh program — the hot-context engine: `memory_search` (ranked cited recall, msh2.3) → `memory_context` + the MCP prompt (the one-call budgeted rung-open pack, msh2.5) over the `memory/` corpus and (after msh2.7 ingestion) the `docs/<prog>` trees. Canon: `msh.design.md` (binding) + `msh.roadmap.md` (msh2.N ladder + Seams + the deferred-standing trigger set) + `program/msh.program.md` (gate ladders per rung type; LAW-4 commit law; the dogfood law — msh gates its own docs). Genesis record: `kb/genesis/` (steward/steelman lens pair + Director synthesis with the CONVERGED/DIVERGED fork ledger); run ledger `specs/progress/msh-genesis.progress.md`. The OLD `docs/go/msh/` tree is the frozen reverse-mode as-built record (rung ids msh.0–msh.6), superseded by banner — forward rungs are **msh2.N** (D-4), never reuse msh.N.

**The key rulings** (full arms in the synthesis §3/§5): read-only toolchain verbatim — msh writes NOTHING, corpus edits only via staged byte-diffed rungs (D-5); docs trees JOIN the engine by direct multi-root ingestion, NO generated index artifact and nothing new named `*.registry.json` (D-6); the in-process `{size,mtime}+SHA` snapshot cache ships BEFORE ingestion, disk index never (D-7, 0.36s full-audit baseline recorded); frontmatter v2 = `{project, status, review_after}` with a day-one review-due stale rule, `tags` deferred to miss-log evidence (D-8); packs are section-capable, truncation only at heading boundaries, `path#heading` citations (D-9); `role` is a caller param until the M4 served routing table exists (D-10); history rides as `originSessionId` pointers + a prepared `history_search` invocation, the ≤10% snippet tier evidence-gated with a RAN/CAPPED/DEGRADED contract (D-11); M4 = aaw server-data-first — routing/formation query tools (18→22, selftest re-pinned), THEN docs/aaw rewritten to pointers, restatement census → 1 the exit gate (D-12).

**Ship process.** A rung = Venus triad (`specs/msh2.N.{md,stories.md,llms.md}` from the ruled design, never the reverse) → Mars build (island `go/msh`; hermetic gates `GOWORK=off go build -o BIN ./cmd` + vet + test + gofmt) → Director independent gate re-run + adversarial probe → `make mcp` hot-swap (NEVER pkill; the MCP client reconnected transparently after swap in the genesis run) → Z on the ledger → ONE exact-pathspec commit.

**Gotchas.** `go/msh/cmd/history.go` + `history_test.go` are UNTRACKED pre-existing files — a blanket `git add go/msh` would sweep the Operator's uncommitted history_search work into a rung commit; always pathspec exact files. Pre-existing gofmt debt (untouched, escalated L-1): `memory/internal/linkx/extractor_test.go`, `memory/internal/stale/context.go`, `memory/internal/stale/rules.go`. `msh specs` area resolution: an EXISTING PATH wins over `docs/<area>` (area "go" lints the `go/` source tree — use "docs/go" for the docs). The live anchor lacks `docs_root` (renders `docs: -`) and still points at echo_mq — re-pointing to msh is Operator-timed. Related: [[msh-mcp-server]] [[aaw-memory]] [[go-workspace-active-dev]].
