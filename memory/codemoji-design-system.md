---
name: codemoji-design-system
description: "node/codemoji-design is now a Storybook design system (accent orange/blue/green + gold theming) emitting Tailwind v4 tokens; app import documented, not yet wired"
metadata: 
  node_type: memory
  type: project
  originSessionId: 466fdd7e-18b5-4685-aa04-c820181e763a
---

`node/codemoji-design` (`@codemoji/design`) is now BOTH the figma-local extraction CLI AND a **Storybook design system** — shipped `301cecbb` on `echo_mq` via /x-mode (Mars build + Director review + ship). Run: `pnpm -C node/codemoji-design storybook` (dev :6006) · `build-storybook` · `pnpm theme` (= `node bin/codemoji-design.mjs theme`, regenerates `dist/theme.css` offline, byte-reproducible).

**Token pipeline:** `tokens/tokens.mjs` (base oklch palette seeded verbatim from `codemoji-app/src/styles.css :root`) → `src/theme.mjs` generator → `dist/theme.css` (Tailwind v4 `@theme inline`, names MATCH the app = drop-in; a single themeable `--accent` that collapses the app's accent double-definition; `:root` default orange; `[data-theme=orange|blue|green]` overrides; `--gradient-gold`). Name parity 54/54 (+3 gold tokens).

**Theming (Operator-ruled):** accent themes = orange `#FF8400` (current) / blue `#0050FF` / green `#00D95F`, swapped via `[data-theme]` (Storybook toolbar); **gold** formalized as the `--gradient-gold` token (supersedes the app's raster `/images/rooms/gold.png` + the copied inline banner gradient). Stories: Foundations · Components (Button `buy`→`bg-accent`, `golden`→`--gradient-gold`; Badge) · Golden · Screens — the 34-screen `Catalog` grid from `gameplay/manifest.json` PLUS dedicated per-screen stories **Rooms (Lobby)** · **Game (Free)** · **Golden Game** (commit `364a626a`) on a shared `ScreenView` phone-frame; screens are manifest-derived via `canonicalOf` in `screens.data.mjs` (no hardcoded ids beyond `GOLDEN_SCREEN_IDS`); Golden Game = the two boost-class states (in-progress `1089:19410` / finished `1108:27589`) with a gold bezel + `--gradient-gold`; each screen story embeds one live themed CTA (`bg-accent`) bridging the static PNG to the live tokens. Gotcha: the `.mjs` import needs NO `@ts-expect-error` (allowJs infers it; the shipped `Screens.stories.tsx` still carries a now-unused one — tsc never runs here, `@types/node` absent ⇒ build-storybook is the only wired gate).

**Catalog exclusion filter (commit `1c70588c`):** `gameplay/manifest.json` carries a top-level `excluded` block (id + reason); `screens.data.mjs` builds `EXCLUDED_IDS` and applies it ONCE so the catalog grid, the manifest-derived groups, and the dedicated stories all drop the same screens — onboarding (`21:958/780/948`), 3 board design-exploration variants (`707:19464/702:17102/897:16801`), the leaderboard (`21:759`); 34→27 screens, empty categories dropped (onboarding goes, board set 7→4). Soft filter — PNGs + entries kept, reversible.

**Board components (commit `48ec8364`):** a component + story for EACH interactive element of the gameplay board `94:2974`, under `stories/board/` (titles `Board/*`). Shared primitives `lib/{EmojiTile,BoardCard}`; `EmojiSlots` (slot states) is the exemplar; then `EmojiKeyboard · GuessActions (Check/Clear) · PreviousAttempt · StatusBar · RoundInfo (timer/prize) · KeysBalance (Buy) · BoardTabs (History/Leaderboard) · Leaderboard · ShareKeys · GameRules`; + a `Board/Overview` capstone assembling them into the board layout (13 Board/* groups). Self-contained re-expressions of the real app widgets (`widgets/{emoji-slots,emotion-picker,status-bar,game-rules}`, `entities/{leaderboard,balance}`, `shared/ui/{button,tabs}`) — Unicode emojis NOT SpriteEmoji, tokens only, NO jotai/i18n/router/Radix/dnd-kit. Built via a **4-builder Agent fan-out** over the proven exemplar+primitives, then central integrate (grep sweep = no app-imports/raw-hex/`/images` raster/default-palette/forbidden-deps; whole-board tsc; build green). Pattern note: fan-out paid off here (10 independent components, each grounded in a different app widget) where the screen-stories rung went direct — [[right-size-formation-and-write-only-artifacts]].

**Lobby components (commit `08f21b0b`):** a component + story for each element of the Rooms→Lobby screen `121:2056`, under `stories/lobby/` (titles `Lobby/*`). `RoomCard` (+ golden variant) the exemplar; then `RoomList · SubscriptionBanner · PromoBanner · ArchiveRoomItem · ArchiveList · BuyKeysBanner · CharacterFooter`; + a `Lobby/Overview` capstone. **KEY — cross-screen reuse:** `Lobby/Overview` imports `../board/{StatusBar,GameRules,ShareKeys}` and the `../board/lib/{BoardCard,EmojiTile}` primitives (the archive's revealed code = a row of `EmojiTile`), NOT rebuilt — the design-system payoff. Grounded in `entities/rooms/{room-item,rooms-list}` + `widgets/{subscription-banner,promo-banner,archive-rooms,buy-keys-banner,character-footer}`; self-contained (Unicode not raster/SpriteEmoji, tokens only, no jotai/i18n/router/Radix/lucide). Built via a 3-builder fan-out over the RoomCard exemplar. 9 Lobby/* groups; **32 story groups total** across the system (Foundations · Components · Golden · Screens · Board · Lobby). Debt: `BoardCard`/`EmojiTile`/`StatusBar`/`GameRules`/`ShareKeys` are de-facto SHARED primitives that live under `board/` — a future rung could re-house them to `stories/shared/` (touches the board files, so deferred).

**Golden Game components (commit `b463f29b`):** the boost-class Golden Room screens `1089:19410` (in progress) / `1108:27589` (finished), under `stories/golden-game/` (titles `Golden Game/*`). Only **3 NEW** surfaces — `GoldenHero` (gold-gradient round timer + boosted pool + "Golden Room" banner via `bg-gradient-gold` + a `var(--gradient-gold)` clip-text fill), `GoldenAnswerReveal` (the revealed code, reuses `EmojiTile`), `GoldenLeaderboard` (rank · avatar · revealed code as inline glyphs · prize — golden standings reveal codes + payouts) — because the Golden Game IS the board with a gold treatment: the `Golden Game/Overview` (In progress + Finished states) REUSES board `StatusBar/EmojiSlots/GuessActions/EmojiKeyboard/BoardTabs/GameRules/ShareKeys`. Built DIRECTLY (small + reuse-heavy → right-sized vs the board/lobby fan-outs; the declining new-component count per screen — board 10, lobby 8, golden 3 — is the library paying off). 4 Golden Game/* groups; **36 story groups total** across the system.

**Button ROLE colors + a latent bg-gradient FIX (commit `f6b4be9a`):** action buttons are now colored by INTENT, not the global theme. New role tokens/variants: `purchase` = `--gradient-purchase` (orange buy gradient `#FF8800→#FF4800` = the app's "Приобрести ключи" / `variant="gradient"`), `enter` = `--color-enter` (blue `#0050FF`, Open safe / Enter room — free or paid, non-gold). Golden stays gold; `buy` (bg-accent, themeable) stays for generic accent CTAs (share, subscription). Applied: purchase → PromoBanner · BuyKeysBanner · KeysBalance · Game-screen "Buy keys"; enter → RoomCard (non-gold) · lobby "Enter room". `Components/Button` gained a "Color overrides (black → role)" showcase. **GOTCHA (latent bug, now fixed):** Tailwind v4 has NO `bg-gradient-<name>` utility for a `--gradient-*` theme var, so `bg-gradient-gold` was a silent NO-OP from the FIRST rung — the golden Button/GoldenHero/GoldenAnswerReveal rendered with NO gradient background (the `Golden/Treatment` story masked it by using inline `var(--gradient-gold)`). Fixed by emitting `@utility bg-gradient-gold` + `bg-gradient-purchase` from `theme.mjs` into `dist/theme.css` (works through the preview.css `@import` chain). **LESSON: verify a gradient/utility class by grepping the BUILT css for the `.bg-…{…}` RULE, not just the token var — build-success hides a no-op class** (the visual-render gap). Also: a broad `tsc` surfaced ~5 pre-existing unused-`@ts-expect-error` warnings in foundations/golden/screens stories (same latent class as Screens.stories; tsc still not a wired gate) — left as-is.

**Screen DRIFT VIEW + extract-to-component (commit `66eeb81f`):** the dedicated per-screen heroes (`Screens/Rooms (Lobby)`·`Game (Free)`·`Golden Game`) no longer show ONLY the static Figma PNG — each renders the screen rebuilt LIVE from the design system on the LEFT beside the Figma export on the RIGHT (a two-pane drift comparison). This was the fix for "the lobby screen still has black buttons": a raster can never reflect a component change, so the live build shows the blue `enter` room-entry buttons while the PNG keeps the app's black ones — the drift is the point. **Refactor:** the live compositions were trapped as PRIVATE functions inside the section `*/Overview.stories.tsx` files (story files export stories, not reusable React) — extracted into shared plain components `lobby/LobbyScreen.tsx`·`board/BoardScreen.tsx`·`golden-game/GoldenScreen.tsx` (with co-located sample data, e.g. `LOBBY_ROOMS`), so BOTH the `*/Overview` story AND the `Screens/*` drift view render ONE source of truth (DRY). `ScreenView.tsx` gained `LiveFrame` (device bezel wrapping live DOM on the `--color-bg-app-*` gradient) + `DriftView` (the live|Figma two-pane: header, role-color caption, screen metadata); `PhoneFrame`+`LiveFrame` share one `Bezel`; the now-unused `ScreenView`+`ThemeNote` (the old single-pane static hero) were DROPPED. Stories-only rung — NO token change, `dist/theme.css` untouched. Pattern: LEFT pane = `LiveFrame` grows to content height so it sits full-length beside the full-height reference PNG. The variant GRIDS (`DesignVariants`/`DesignExplorations`/`Progression`) keep `PhoneFrame` (all-PNG, unchanged). Story-group count unchanged (no new `.stories.tsx`, just shared component modules).

**Lobby FIGMA-MATCH via figma-local (commit `ba6c5992`):** the `Screens/Rooms (Lobby)` drift view's LIVE pane now reproduces the Figma master (node `121:2056`) — text + sizes extracted from the live **figma-local MCP bridge** — so the only residual difference vs the Figma reference is the deliberate one (role colors). Operator-ruled fidelity = **"Faithful, tokens-first" (NOT pixel-exact)** + scope = **lobby first**. Changes: frame width 300→**375** (real iPhone frame); real **Russian** copy verbatim (StatusBar @vitalysacred·3584/58/34; PromoBanner/BuyKeysBanner "Игроки нашли $25693 в сейфах…" + black "Проверить 🔑3" placeholder button; SubscriptionBanner "Тысяча"/"Это что такое?"; RoomList "Выбери сейф…" + cards Простой сейф/$52, Золотая комната/$10 golden, Стальной ящик/$1352 ⭐⭐/140/24.32%; ArchiveList ×2 title-prop "Ваши золотые комнаты"+"Архив комнат"; GameRules "🎲 Правила игры"+Russian scoring+outline "Как играть?"; ShareKeys "Получи ключ бесплатно"+black "Поделиться"+BLUE "Пригласить друга"+Mr.Freeman tip; CharacterFooter mascot+🔑 no caption); **LobbyScreen reordered** to Figma's vertical sequence. **COLOR deliberately NOT matched** — room-entry stays blue `enter`/golden stays gild (the role layer the drift view exists to show); with text+size matched the button-color delta is now the clean signal. Prices render WITHOUT thousands separators (Figma "$1352" not "1,352" — dropped `toLocaleString`). GameRules/ShareKeys/StatusBar are SHARED with the board → updated to Russian (app is fully Russian = globally correct; board's board-specific widgets still English until a board pass). **figma-local extraction GOTCHAS (this bridge, older plugin, handshake "unknown"):** `depth`/`maxNodes` on `get-node-properties` are IGNORED (returns node + LITE child stubs only) ⇒ manual BFS node-by-node; **`get-batch-nodes` is advertised but UNBACKED** ("Unknown action") ⇒ truly one node per call; a TEXT node's `name` MIRRORS its `characters` (+ real `fontSize`/`fontName.family`/`fills{r,g,b}`); filter `visible:false` (alternate/hidden states like the 50px "Enhanced Dot Digital-7" "$1000"/"Давай играть" — the VISIBLE lobby is ALL Noto Sans Mono, which the design system already has, so NO @font-face needed); a delegated crawler agent got STRUCTURE right but mis-reported specifics (S2 headline, Card C) ⇒ **direct-read the prominent strings, trust the crawl only for long body paragraphs**. `get-current-page` is 187k chars (NEVER pull whole pages). canonical ids: lobby 121:2056, board 94:2974, golden 1089:19410/1108:27589. **Lobby follow-up (commit `5040bbac`):** added **NavPhonePanel** (`stories/lobby/NavPhonePanel.tsx` — the Figma "Header old (iOS)" chrome) at the top of LobbyScreen — REBUILT from real exported raster assets (commit `22c3d1f5`, supersedes the first emoji/text version): `public/assets/status-bar/{iphone-topbar.png (full-width iOS bar),tg-back.png (‹Back pill),tg-menu.png (chevron+⋮ menu),cm-logo-sm.png (the CODEMOJI CAT mascot = center logo, h-10/40px, w-auto)}`; bg-transparent (assets carry their own surfaces). **Required a new staticDir** `{from:'../public', to:'/'}` in `.storybook/main.ts` (was only `gameplay/assets→/gameplay`) so assets resolve at `/assets/status-bar/*` (verified copied into storybook-static). NOTE the Operator commits the binary status-bar PNGs out-of-band (iphone-topbar/tg-back/tg-menu/cm-logo.png were already tracked; I committed only the new cm-logo-sm.png + code). `cm-logo-sm.png`=cat mascot (tuxedo cat, yellow crown, magenta blob, white scarf), NOT a wordmark. applied the board's **"All Screen Fill"** gradient to LobbyScreen full-bleed (= board node 94:2974 fill = vertical #E8F3F7→#AFC7D6 = `--color-bg-app-from`→`--color-bg-app-to` tokens; LiveFrame already paints the same so seamless); StatusBar resources row → **smaller (text-2xs) + non-bold handle + plain numbers** (3,584→3584, dropped `toLocaleString`); PromoBanner/BuyKeysBanner CTA → **"Приобрести ключи ⭐" on the orange `purchase` gradient** (NOT the black "Проверить"). **LESSON (the "Проверить" bug):** a button's label = the TEXT child's `characters`, NEVER the instance/frame NODE NAME — the name is a stale placeholder ("Проверить") while characters="Приобрести ключи"; this also explains why `find-nodes "Приобрести"` returned [] (find-nodes matches NAMES). GOTCHA: `npx tsc` flaked ("not the tsc command") — use `pnpm exec tsc` (no local node_modules/.bin/tsc; typescript hoisted to the node/ workspace root). The Operator pre-staged `node/codemoji-design/public/assets/cm-logo.png` (the CODEMOJI logo) — left staged, EXCLUDED from the rung commit via pathspec. OWED next: same Figma-match pass for **board (94:2974)** + **golden** screens (still English/300px); optionally swap the NavPhonePanel 🎭 for the real cm-logo.png + wire the logo asset.

**VISUAL TEST TOOLS + nav Figma-align (commit `0c58ffcd`) — the "quality is bad" fix:** the Operator pushed back that I was verifying builds/strings but NOT the rendered pixels. Added real visual tooling in `node/codemoji-design/visual/`: **`shoot.mjs`** (serves storybook-static/ on an ephemeral http port so `/assets/*` resolve, opens the story `iframe.html?id=<storyId>&viewMode=story` in headless Playwright Chromium, screenshots an element; default selector `figure`=first device frame=the LIVE drift pane; `CLIP_H=<px>` env caps tall screens) + **`compare.mjs`** (pixelmatch two PNGs, crops to common top-left so different sizes still diff, writes a highlighted diff, exits>0 at >2% mismatch). Scripts `pnpm shoot <id> [selector] [out]` / `pnpm compare <a> <b> [diff]`; devDeps `playwright`+`pixelmatch`+`pngjs` (Chromium shared w/ mcp/e2e cache). **WORKFLOW NOW: after a UI change, `pnpm build-storybook` → `pnpm shoot` → Read the PNG → compare to Figma; iterate BEFORE reporting.** Used it to fix the nav: status bar FULL-BLEED (new `flush` prop on LiveFrame+DriftView = no inner padding, lobby passes it → time/battery near phone corners, nav moved up), header inset 16px, content inset 8px; the cat logo enlarged on a circle = `bg-control` (#A8ACB0, sampled OPAQUE from tg-back.png via pngjs) — a NEW `--color-control` token (tokens.mjs→theme.mjs→dist/theme.css; `.bg-control` compiles). LESSON: sample raster colors with pngjs (`PNG.sync.read`), run from INSIDE the package (node resolves deps from there, not from a scratchpad path). [[mcp-e2e-figure-validator]]

**Gotchas:** root `.gitignore` ignores `bin/`, `dist`, AND nested `.gitignore` — the package CLI (`bin/codemoji-design.mjs`) + `dist/theme.css` were **force-added** (future new bin/dist files need `git add -f`); the package `.gitignore` is uncommitted-but-effective. "golden" is OVERLOADED — the *boost class* (`gold_multiplier` on a classic game; the export's golden *screens* `1089:19410`/`1108:27589`) vs the blind commit-reveal game *type* (the app's `entities/golden-games` + the mocked `pages/game/golden-game-tabs.tsx`). `figma/` + `screens/` under the package remain UNTRACKED (pre-existing prior-session work).

**OWED (NOT done this rung):** wire `codemoji-app/src/styles.css` to `@import '@codemoji/design/theme.css'` + a `data-theme` toggle (documented in `THEMING.md` only); capture real Figma variable *modes* as themes via `resolve-variables` — blocked on the Windows plugin reload ([[figma-local-program]]); rebuild the golden blind-gameplay surface (the app's `golden-game-tabs.tsx` is mocked). [[codemojex-program]]
