---
name: codemoji-design-system
description: "node/codemoji-design is now a Storybook design system (accent orange/blue/green + gold theming) emitting Tailwind v4 tokens; app import documented, not yet wired; design-system SPECS live in docs/codemojex-tma/design/ (cmd.* ladder)"
project: codemojex
metadata: 
  node_type: memory
  type: project
  originSessionId: 466fdd7e-18b5-4685-aa04-c820181e763a
---

`node/codemoji-design` (`@codemoji/design`) is now BOTH the figma-local extraction CLI AND a **Storybook design system** — shipped `301cecbb` on `echo_mq` via /x-mode (Mars build + Director review + ship). Run: `pnpm -C node/codemoji-design storybook` (dev :6006) · `build-storybook` · `pnpm theme` (= `node bin/codemoji-design.mjs theme`, regenerates `dist/theme.css` offline, byte-reproducible).

**Token pipeline:** `tokens/tokens.mjs` (base oklch palette seeded verbatim from `codemoji-app/src/styles.css :root`) → `src/theme.mjs` generator → `dist/theme.css` (Tailwind v4 `@theme inline`, names MATCH the app = drop-in; a single themeable `--accent` that collapses the app's accent double-definition; `:root` default orange; `[data-theme=orange|blue|green]` overrides; `--gradient-gold`). Name parity 54/54 (+3 gold tokens).

**Theming (Operator-ruled):** accent themes = orange `#FF8400` (current) / blue `#0050FF` / green `#00D95F`, swapped via `[data-theme]` (Storybook toolbar); **gold** formalized as the `--gradient-gold` token (supersedes the app's raster `/images/rooms/gold.png` + the copied inline banner gradient). Stories: Foundations · Components (Button `buy`→`bg-accent`, `golden`→`--gradient-gold`; Badge) · Golden · Screens — the 34-screen `Catalog` grid from `gameplay/manifest.json` PLUS dedicated per-screen stories **Rooms (Lobby)** · **Game (Free)** · **Golden Game** (commit `364a626a`) on a shared `ScreenView` phone-frame; screens are manifest-derived via `canonicalOf` in `screens.data.mjs` (no hardcoded ids beyond `GOLDEN_SCREEN_IDS`); Golden Game = the two boost-class states (in-progress `1089:19410` / finished `1108:27589`) with a gold bezel + `--gradient-gold`; each screen story embeds one live themed CTA (`bg-accent`) bridging the static PNG to the live tokens. Gotcha: the `.mjs` import needs NO `@ts-expect-error` (allowJs infers it; the shipped `Screens.stories.tsx` still carries a now-unused one — tsc never runs here, `@types/node` absent ⇒ build-storybook is the only wired gate).

**Catalog exclusion filter (commit `1c70588c`):** `gameplay/manifest.json` carries a top-level `excluded` block (id + reason); `screens.data.mjs` builds `EXCLUDED_IDS` and applies it ONCE so the catalog grid, the manifest-derived groups, and the dedicated stories all drop the same screens — onboarding (`21:958/780/948`), 3 board design-exploration variants (`707:19464/702:17102/897:16801`), the leaderboard (`21:759`); 34→27 screens, empty categories dropped (onboarding goes, board set 7→4). Soft filter — PNGs + entries kept, reversible.

**Board components (commit `48ec8364`):** a component + story for EACH interactive element of the gameplay board `94:2974`, under `stories/board/` (titles `Board/*`). Shared primitives `lib/{EmojiTile,BoardCard}`; `EmojiSlots` (slot states) is the exemplar; then `EmojiKeyboard · GuessActions (Check/Clear) · PreviousAttempt · StatusBar · RoundInfo (timer/prize) · KeysBalance (Buy) · BoardTabs (History/Leaderboard) · Leaderboard · ShareKeys · GameRules`; + a `Board/Overview` capstone assembling them into the board layout (13 Board/* groups). Self-contained re-expressions of the real app widgets (`widgets/{emoji-slots,emotion-picker,status-bar,game-rules}`, `entities/{leaderboard,balance}`, `shared/ui/{button,tabs}`) — Unicode emojis NOT SpriteEmoji, tokens only, NO jotai/i18n/router/Radix/dnd-kit. Built via a **4-builder Agent fan-out** over the proven exemplar+primitives, then central integrate (grep sweep = no app-imports/raw-hex/`/images` raster/default-palette/forbidden-deps; whole-board tsc; build green). Pattern note: fan-out paid off here (10 independent components, each grounded in a different app widget) where the screen-stories rung went direct — [[right-size-formation-and-write-only-artifacts]].

**Lobby components (commit `08f21b0b`):** a component + story for each element of the Rooms→Lobby screen `121:2056`, under `stories/lobby/` (titles `Lobby/*`). `RoomCard` (+ golden variant) the exemplar; then `RoomList · SubscriptionBanner · PromoBanner · ArchiveRoomItem · ArchiveList · BuyKeysBanner · CharacterFooter`; + a `Lobby/Overview` capstone. **KEY — cross-screen reuse:** `Lobby/Overview` imports `../board/{StatusBar,GameRules,ShareKeys}` and the `../board/lib/{BoardCard,EmojiTile}` primitives (the archive's revealed code = a row of `EmojiTile`), NOT rebuilt — the design-system payoff. Grounded in `entities/rooms/{room-item,rooms-list}` + `widgets/{subscription-banner,promo-banner,archive-rooms,buy-keys-banner,character-footer}`; self-contained (Unicode not raster/SpriteEmoji, tokens only, no jotai/i18n/router/Radix/lucide). Built via a 3-builder fan-out over the RoomCard exemplar. 9 Lobby/* groups; **32 story groups total** across the system (Foundations · Components · Golden · Screens · Board · Lobby). Debt: `BoardCard`/`EmojiTile`/`StatusBar`/`GameRules`/`ShareKeys` are de-facto SHARED primitives that live under `board/` — a future rung could re-house them to `stories/shared/` (touches the board files, so deferred).

**Golden Game components (commit `b463f29b`):** the boost-class Golden Room screens `1089:19410` (in progress) / `1108:27589` (finished), under `stories/golden-game/` (titles `Golden Game/*`). Only **3 NEW** surfaces — `GoldenHero` (gold-gradient round timer + boosted pool + "Golden Room" banner via `bg-gradient-gold` + a `var(--gradient-gold)` clip-text fill), `GoldenAnswerReveal` (the revealed code, reuses `EmojiTile`), `GoldenLeaderboard` (rank · avatar · revealed code as inline glyphs · prize — golden standings reveal codes + payouts) — because the Golden Game IS the board with a gold treatment: the `Golden Game/Overview` (In progress + Finished states) REUSES board `StatusBar/EmojiSlots/GuessActions/EmojiKeyboard/BoardTabs/GameRules/ShareKeys`. Built DIRECTLY (small + reuse-heavy → right-sized vs the board/lobby fan-outs; the declining new-component count per screen — board 10, lobby 8, golden 3 — is the library paying off). 4 Golden Game/* groups; **36 story groups total** across the system.

**Button ROLE colors + a latent bg-gradient FIX (commit `f6b4be9a`):** action buttons are now colored by INTENT, not the global theme. New role tokens/variants: `purchase` = `--gradient-purchase` (orange buy gradient `#FF8800→#FF4800` = the app's "Приобрести ключи" / `variant="gradient"`), `enter` = `--color-enter` (blue `#0050FF`, Open safe / Enter room — free or paid, non-gold). Golden stays gold; `buy` (bg-accent, themeable) stays for generic accent CTAs (share, subscription). Applied: purchase → PromoBanner · BuyKeysBanner · KeysBalance · Game-screen "Buy keys"; enter → RoomCard (non-gold) · lobby "Enter room". `Components/Button` gained a "Color overrides (black → role)" showcase. **GOTCHA (latent bug, now fixed):** Tailwind v4 has NO `bg-gradient-<name>` utility for a `--gradient-*` theme var, so `bg-gradient-gold` was a silent NO-OP from the FIRST rung — the golden Button/GoldenHero/GoldenAnswerReveal rendered with NO gradient background (the `Golden/Treatment` story masked it by using inline `var(--gradient-gold)`). Fixed by emitting `@utility bg-gradient-gold` + `bg-gradient-purchase` from `theme.mjs` into `dist/theme.css` (works through the preview.css `@import` chain). **LESSON: verify a gradient/utility class by grepping the BUILT css for the `.bg-…{…}` RULE, not just the token var — build-success hides a no-op class** (the visual-render gap). Also: a broad `tsc` surfaced ~5 pre-existing unused-`@ts-expect-error` warnings in foundations/golden/screens stories (same latent class as Screens.stories; tsc still not a wired gate) — left as-is.

**Screen DRIFT VIEW + extract-to-component (commit `66eeb81f`):** the dedicated per-screen heroes (`Screens/Rooms (Lobby)`·`Game (Free)`·`Golden Game`) no longer show ONLY the static Figma PNG — each renders the screen rebuilt LIVE from the design system on the LEFT beside the Figma export on the RIGHT (a two-pane drift comparison). This was the fix for "the lobby screen still has black buttons": a raster can never reflect a component change, so the live build shows the blue `enter` room-entry buttons while the PNG keeps the app's black ones — the drift is the point. **Refactor:** the live compositions were trapped as PRIVATE functions inside the section `*/Overview.stories.tsx` files (story files export stories, not reusable React) — extracted into shared plain components `lobby/LobbyScreen.tsx`·`board/BoardScreen.tsx`·`golden-game/GoldenScreen.tsx` (with co-located sample data, e.g. `LOBBY_ROOMS`), so BOTH the `*/Overview` story AND the `Screens/*` drift view render ONE source of truth (DRY). `ScreenView.tsx` gained `LiveFrame` (device bezel wrapping live DOM on the `--color-bg-app-*` gradient) + `DriftView` (the live|Figma two-pane: header, role-color caption, screen metadata); `PhoneFrame`+`LiveFrame` share one `Bezel`; the now-unused `ScreenView`+`ThemeNote` (the old single-pane static hero) were DROPPED. Stories-only rung — NO token change, `dist/theme.css` untouched. Pattern: LEFT pane = `LiveFrame` grows to content height so it sits full-length beside the full-height reference PNG. The variant GRIDS (`DesignVariants`/`DesignExplorations`/`Progression`) keep `PhoneFrame` (all-PNG, unchanged). Story-group count unchanged (no new `.stories.tsx`, just shared component modules).

**Lobby FIGMA-MATCH via figma-local (commit `ba6c5992`):** the `Screens/Rooms (Lobby)` drift view's LIVE pane now reproduces the Figma master (node `121:2056`) — text + sizes extracted from the live **figma-local MCP bridge** — so the only residual difference vs the Figma reference is the deliberate one (role colors). Operator-ruled fidelity = **"Faithful, tokens-first" (NOT pixel-exact)** + scope = **lobby first**. Changes: frame width 300→**375** (real iPhone frame); real **Russian** copy verbatim (StatusBar @vitalysacred·3584/58/34; PromoBanner/BuyKeysBanner "Игроки нашли $25693 в сейфах…" + black "Проверить 🔑3" placeholder button; SubscriptionBanner "Тысяча"/"Это что такое?"; RoomList "Выбери сейф…" + cards Бокс для разминки/$52, Золотая комната/$10 golden, Стальной ящик/$1352 ⭐⭐/140/24.32%; ArchiveList ×2 title-prop "Ваши золотые комнаты"+"Архив комнат"; GameRules "🎲 Правила игры"+Russian scoring+outline "Как играть?"; ShareKeys "Получи ключ бесплатно"+black "Поделиться"+BLUE "Пригласить друга"+Mr.Freeman tip; CharacterFooter mascot+🔑 no caption); **LobbyScreen reordered** to Figma's vertical sequence. **COLOR deliberately NOT matched** — room-entry stays blue `enter`/golden stays gild (the role layer the drift view exists to show); with text+size matched the button-color delta is now the clean signal. Prices render WITHOUT thousands separators (Figma "$1352" not "1,352" — dropped `toLocaleString`). GameRules/ShareKeys/StatusBar are SHARED with the board → updated to Russian (app is fully Russian = globally correct; board's board-specific widgets still English until a board pass). **figma-local extraction GOTCHAS (this bridge, older plugin, handshake "unknown"):** `depth`/`maxNodes` on `get-node-properties` are IGNORED (returns node + LITE child stubs only) ⇒ manual BFS node-by-node; **`get-batch-nodes` is advertised but UNBACKED** ("Unknown action") ⇒ truly one node per call; a TEXT node's `name` MIRRORS its `characters` (+ real `fontSize`/`fontName.family`/`fills{r,g,b}`); filter `visible:false` (alternate/hidden states like the 50px "Enhanced Dot Digital-7" "$1000"/"Давай играть" — the VISIBLE lobby is ALL Noto Sans Mono, which the design system already has, so NO @font-face needed); a delegated crawler agent got STRUCTURE right but mis-reported specifics (S2 headline, Card C) ⇒ **direct-read the prominent strings, trust the crawl only for long body paragraphs**. `get-current-page` is 187k chars (NEVER pull whole pages). canonical ids: lobby 121:2056, board 94:2974, golden 1089:19410/1108:27589. **Lobby follow-up (commit `5040bbac`):** added **NavPhonePanel** (`stories/lobby/NavPhonePanel.tsx` — the Figma "Header old (iOS)" chrome) at the top of LobbyScreen — REBUILT from real exported raster assets (commit `22c3d1f5`, supersedes the first emoji/text version): `public/assets/status-bar/{iphone-topbar.png (full-width iOS bar),tg-back.png (‹Back pill),tg-menu.png (chevron+⋮ menu),cm-logo-sm.png (the CODEMOJI CAT mascot = center logo, h-10/40px, w-auto)}`; bg-transparent (assets carry their own surfaces). **Required a new staticDir** `{from:'../public', to:'/'}` in `.storybook/main.ts` (was only `gameplay/assets→/gameplay`) so assets resolve at `/assets/status-bar/*` (verified copied into storybook-static). NOTE the Operator commits the binary status-bar PNGs out-of-band (iphone-topbar/tg-back/tg-menu/cm-logo.png were already tracked; I committed only the new cm-logo-sm.png + code). `cm-logo-sm.png`=cat mascot (tuxedo cat, yellow crown, magenta blob, white scarf), NOT a wordmark. applied the board's **"All Screen Fill"** gradient to LobbyScreen full-bleed (= board node 94:2974 fill = vertical #E8F3F7→#AFC7D6 = `--color-bg-app-from`→`--color-bg-app-to` tokens; LiveFrame already paints the same so seamless); StatusBar resources row → **smaller (text-2xs) + non-bold handle + plain numbers** (3,584→3584, dropped `toLocaleString`); PromoBanner/BuyKeysBanner CTA → **"Приобрести ключи ⭐" on the orange `purchase` gradient** (NOT the black "Проверить"). **LESSON (the "Проверить" bug):** a button's label = the TEXT child's `characters`, NEVER the instance/frame NODE NAME — the name is a stale placeholder ("Проверить") while characters="Приобрести ключи"; this also explains why `find-nodes "Приобрести"` returned [] (find-nodes matches NAMES). GOTCHA: `npx tsc` flaked ("not the tsc command") — use `pnpm exec tsc` (no local node_modules/.bin/tsc; typescript hoisted to the node/ workspace root). The Operator pre-staged `node/codemoji-design/public/assets/cm-logo.png` (the CODEMOJI logo) — left staged, EXCLUDED from the rung commit via pathspec. OWED next: same Figma-match pass for the **golden** screens (still English/300px — **board DONE** `c7b6df74`, lobby DONE); optionally swap the NavPhonePanel 🎭 for the real cm-logo.png + wire the logo asset.

**VISUAL TEST TOOLS + nav Figma-align (commit `0c58ffcd`) — the "quality is bad" fix:** the Operator pushed back that I was verifying builds/strings but NOT the rendered pixels. Added real visual tooling in `node/codemoji-design/visual/`: **`shoot.mjs`** (serves storybook-static/ on an ephemeral http port so `/assets/*` resolve, opens the story `iframe.html?id=<storyId>&viewMode=story` in headless Playwright Chromium, screenshots an element; default selector `figure`=first device frame=the LIVE drift pane; `CLIP_H=<px>` env caps tall screens) + **`compare.mjs`** (pixelmatch two PNGs, crops to common top-left so different sizes still diff, writes a highlighted diff, exits>0 at >2% mismatch). Scripts `pnpm shoot <id> [selector] [out]` / `pnpm compare <a> <b> [diff]`; devDeps `playwright`+`pixelmatch`+`pngjs` (Chromium shared w/ mcp/e2e cache). **WORKFLOW NOW: after a UI change, `pnpm build-storybook` → `pnpm shoot` → Read the PNG → compare to Figma; iterate BEFORE reporting.** Used it to fix the nav: status bar FULL-BLEED (new `flush` prop on LiveFrame+DriftView = no inner padding, lobby passes it → time/battery near phone corners, nav moved up), header inset 16px, content inset 8px; the cat logo enlarged on a circle = `bg-control` (#A8ACB0, sampled OPAQUE from tg-back.png via pngjs) — a NEW `--color-control` token (tokens.mjs→theme.mjs→dist/theme.css; `.bg-control` compiles). LESSON: sample raster colors with pngjs (`PNG.sync.read`), run from INSIDE the package (node resolves deps from there, not from a scratchpad path). [[mcp-e2e-figure-validator]]

**GOLD treatment = TEXTURE, not gradient (commit `e94a3c41`):** the Operator rejected the custom CSS gradient — the golden "Открыть сейф" (and EVERY golden surface) must use the Figma gold TEXTURE. Restored the app's raster `public/images/rooms/gold.png` (816×1456 metallic liquid-gold) → copied to `public/assets/gold.png` (served via the public/ staticDir). **Replaced `--gradient-gold` (the 7-stop linear-gradient) with `--gold-texture: url('/assets/gold.png')` + `@utility bg-gold-texture { background-image: var(--gold-texture); background-size: cover; background-position: center; }`** (tokens.mjs `gold.gradientGold`→`gold.texture`; theme.mjs emits it in :root + the utility; regenerated dist/theme.css). **GOTCHA: name it `bg-gold-texture` NOT `bg-gold`** — `--color-gold` (#CC7500) already auto-generates a `bg-gold` color utility, so `bg-gold` would collide. Migrated ALL usages ("each place"): golden Button variant (lobby + golden screens), gold Badge, GoldenHero (card `bg-gold-texture` + the "Golden Room" clip-text via `backgroundImage:var(--gold-texture)`+cover, NOT `background` shorthand), GoldenAnswerReveal, the phone Bezel gild + BoostBadge (ScreenView inline), Golden/Treatment doc swatch+card, Screens GOLDEN-ROOM tag. **LESSON: tokenizing a raster TEXTURE as a CSS gradient loses fidelity — a flat gradient can't be liquid-gold; now that rasters are first-class (served via public/ staticDir), use the real texture.**

**Lobby ROUNDED SHEET + Board background (commit `e4da7349`):** the Operator's two Figma-fidelity fixes for the Lobby. (1) The rooms now sit in a **rounded box** = Figma node **"Frame 139" (`128:2732`)**: a full-bleed panel, fill **#D8E4EB = `--color-background`** (the token already equals it), **top corners r≈26** (measured off the export via pngjs corner-trace), holding the room list + golden rooms + archive + rules + share + mascot + promo. (2) Background = the **Board gradient** (`--color-bg-app-from #E8F3F7 → --color-bg-app-to #AFC7D6`, **verified == Board `94:2974` gradient stops**). **KEY non-obvious figma fact:** the Figma lobby's OWN backdrop is **solid BLACK** (`121:2056` child "Rectangle 5" `106:3536` = #000000, full-bleed to y≈560 where the sheet starts) — the Operator's ruling OVERRIDES that black with the Board gradient. Implemented in `LobbyScreen.tsx` ONLY by splitting into two bands: UPPER (NavPhonePanel + StatusBar + PromoBanner + SubscriptionBanner) floats on the gradient (px-2 inset); LOWER = `rounded-t-[26px] bg-background` full-bleed sheet (px-2 pt-6 pb-10) for everything from RoomList down. **STANDING RULE (Operator-confirmed): the lobby — and screens generally — use the BOARD gradient as background, OVERRIDING the designer's per-screen backdrop** (the lobby's is black). **Elevation follow-up (commit `1d133d9a`):** the sheet first rendered INVISIBLE — the board gradient (#E8F3F7→#AFC7D6) passes through the sheet's own fill #D8E4EB at ~20-28% down, exactly where the sheet starts, so the box was ZERO-CONTRAST (proven: a pixel probe found "#D8E4EB" spanning the full width at the sheet's height = it's the gradient, not a box). In Figma it only reads because the backdrop is black. Fix = ELEVATION: an upward soft drop-shadow + 1px top highlight (`relative z-10` + inline boxShadow `0 -1px 0 rgba(255,255,255,.7), 0 -5px 22px rgba(31,45,61,.20), 0 -16px 40px rgba(31,45,61,.10)`); fill + r≈26 unchanged. **LESSON: when a panel's fill is a color the page gradient passes through, the box is invisible no matter the radius — add elevation (shadow), don't just round corners; verify with a screenshot, a pixel probe that matches the fill color will false-positive on the gradient.** tsc clean, build green. **Pathspec discipline:** `NavPhonePanel.tsx` had an Operator staged-only tweak (circle h-14→h-12) — committed ONLY `LobbyScreen.tsx` (both `e4da7349` + `1d133d9a`), left NavPhonePanel staged & untouched.

**Board FIGMA-MATCH via figma-local (commit `c7b6df74`):** the `Screens/Game (Free)` board (node `94:2974`) brought to fidelity — the same pass as the lobby, Operator-ruled **FULL FIDELITY** (copy EN→RU + size align + rebuild the structurally-divergent blocks). **NEW components** `stories/board/{BalancePill,StatCards,InfoDashboard}.tsx`: `InfoDashboard` = the master's drop-shadowed 3-row "Info" block (balance pill "Добавить ключи на баланс"/"Баланс 🔑 34" · the timer+prize PAIR · 3 stat cards "147 👥 Всего игроков / 0 🎯 Всего попыток / 0 ⭐ Лучшая попытка"), REPLACING the old StatusBar+RoundInfo+KeysBalance trio. **RoundInfo restructured** board-only into the two-card pair (white timer "Конец раунда" + green "$2352"/"Призовой пул 💎 468"); new API `{timeLeft, prizeUsd, diamonds}`. **Leaderboard redesigned** board-only: @handles + a time-or-% metric + score bar + the "🔔 Уведомления о смене лидеров" toggle; new API `{handle, score, metric, avatar, isCurrentPlayer}` (verbatim master data @phantomblade/@prokiller88/@lolkekcheburek420… top-3 tied 540 show the TIME they hit it). **Copy:** heading "Отгадай код из 6 эмодзи"; PreviousAttempt "Предыдущая попытка" (dropped /600 + tap-to-reuse, label-left/emoji-center/points-right); GuessActions "Стереть"/"Проверить 🔑 5" (the BLUE `enter` submit = master, not black); BoardTabs "📋 История"/"🏆 Лидерборд" (ids unchanged). **BoardScreen self-contained** (paints the board gradient = the override rule + own px-2 + `NavPhonePanel` chrome); order flipped to **Rules→Share**; + the "Бесплатный ключ будет доступен через 15 часов" footer. **CONSUMER-MAP FIRST (the load-bearing move):** grep every importer before touching a shared component — `Leaderboard/RoundInfo/PreviousAttempt` are board-ONLY (golden uses `GoldenHero`+`GoldenLeaderboard`, separate) so safe to redesign; `GuessActions/BoardTabs` are SHARED with `golden-game/GoldenScreen` → the RU/blue change flows there too (acceptable — golden IS the same board; its English HEADINGS stay until the golden pass); `StatusBar/ShareKeys` shared with the LOBBY → NOT edited (StatusBar just dropped from the board; the free-key line is a SCREEN-level footer after the logo in the master, so it went into BoardScreen NOT the shared ShareKeys, keeping the lobby untouched). `KeysBalance`+`StatusBar` files KEPT (own stories). **Deliberate drift kept** (the DriftView's whole point): emoji avatars stand in for the master's photo/flag rasters. (Drift NOT kept: the leaderboard metric/bar — see the Main Blue refinement below.) **Leaderboard Main Blue refinement (commit `dfab7c76`):** Operator pinned the leaderboard time/% metric + score bar to the app's FIXED **"Main Blue" #54C0EC** (NOT the themeable `accent` orange the first pass left them on, NOT the dark `enter` #0050FF — I guessed `enter` first, Operator corrected to #54C0EC, which is the app's ORIGINAL leaderboard tint). Added a real token: `'main-blue': '#54C0EC'` in tokens.mjs `literalColors` → theme.mjs emits `--color-main-blue` in `@theme inline` → `bg-main-blue`/`text-main-blue` auto-compile (verified `.text/.bg-main-blue{…#54c0ec}` in built css + the rendered story). Leaderboard.tsx uses `text-main-blue`/`bg-main-blue`. **LESSON: never hardcode a hex in a component (the design-system grep-sweep forbids raw-hex) — add a NAMED token via the tokens.mjs→theme.mjs→dist/theme.css pipeline (same pattern as `--color-enter`/`--color-control`); literalColors entries emit ONLY in themeBlock (one line), base entries also need rootBlock.** dist/theme.css is tracked (force-added prior) so a normal pathspec commit works (no `-f` needed for edits). **Active-tab follow-up (commit `960c5faa`):** the first main-blue pass fixed only the leaderboard ROWS — the Operator then flagged (drift view) that the active TAB "🏆 Лидерборд" was STILL orange (BoardTabs active = `border-accent text-accent`). Pinned BoardTabs active state → `border-main-blue text-main-blue` (#54C0EC, == reference). **LESSON: `accent` (themeable orange) leaks into MULTIPLE board surfaces — when pinning a section to a fixed color, grep ALL `accent` utility classes in the section's components (rows AND the tab strip), don't fix just the one you screenshotted.** BoardTabs is shared → the Golden standings tab goes blue too (consistent). After this, NO `accent` utility classes remain in `stories/board/*.tsx` (only a stale comment in KeysBalance.stories). **figma-local craft:** the older-plugin **TEXT-node `name` == `characters`** trick gave EXACT Russian copy with zero OCR (`get-node-properties` on a text frame); `export-node` on SUB-frames renders at native size = READABLE, the full 2461px frame downscales too small; `Group 5` (`251:2366`) was `visible:false` = a hidden modal, skipped. Verified: build-storybook green; the live|master drift view (`screens-game-free--board`, set to `width=375 flush`) aligns section-by-section; tsc clean over the changed graph (only the pre-existing TS2578). **Pathspec:** committed 12 board files; left Operator working-tree `lobby/{BuyKeysBanner,PromoBanner}.tsx` + staged `echo/.env.example` UNTOUCHED.

**Board CONTROLS rewritten from the app — SPRITE EMOJI (commit `31c438a3`):** the Operator called the guess block + emoji keyboard "broken" and said "rewrite controls from node/codemoji-app". Root cause: my controls were a loose Unicode-glyph approximation; the app draws EVERY gameplay emoji from a **sprite sheet**. **This REVERSES the design system's earlier "Unicode NOT SpriteEmoji, no binary" principle — but ONLY for the board gameplay controls** (Operator-ruled; chose "App sprite sheet (exact)" over keeping Unicode when asked). New `stories/board/lib/SpriteEmoji.tsx` = faithful port of the app's renderer (`<div>` with `background-image` + negative `background-position`; `backgroundSize` = whole sheet scaled so one cell renders at the display size). Sheet COPIED from the app: `public/assets/emoji/01-emoji-set.png` (720×1080 = **10 cols × 15 rows @ 72px**; served at `/assets/emoji/` via the public/ staticDir). **Codes are "XXYY" = COLUMN-first (XX=col, YY=row)** — easy to swap (I shipped two swaps — `0605`=anchor vs `0506`=🦋, `0406`=roach vs `0604`=🍓 — caught ONLY by reading the screenshot; LESSON: always render-verify a hand-mapped sprite grid, col-first inverts the natural "row,col" reading). Rewrote 3 board components to the app's EXACT styling: **EmojiSlots** (filled `bg-primary/10`; the ACTIVE/next slot is DARK `bg-active-slot` #1F1F1F with a pulsing white `?` — NOT the light-blue the Figma master showed, the app's real token wins; empty = TRANSPARENT not grey; sprite size 40; `size-13` tiles; dnd-kit omitted), **EmojiKeyboard** (7-col grid of WHITE keys `bg-white ring-1 ring-black/10`; SELECTED=green `bg-green-100 ring-green-400`, USED=faded `bg-white/40`; sprite 34; curated 28-code SAMPLE_CODES default), **PreviousAttempt** (app's CENTERED `"Предыдущая попытка:"` + sprite row + bare score; sprite 14 — fixes the label-wrapping that read as broken). `app vs Figma` conflicts resolved toward the APP for these controls (dark active slot; transparent empties). **EmojiTile (Unicode) is RETAINED** for the lobby archive revealed-codes + GoldenAnswerReveal (out of scope) → the system now MIXES sprite (board controls) + Unicode (lobby/golden reveals). `EmojiSlots` is shared with `golden-game/GoldenScreen` → updated its call to codes so it keeps working (kept heading English = golden pass still owed). The sprite PNG committed with the rung (essential to the component; gold.png precedent). **Guess-panel RECONCILE (commit `413b378d`):** Operator then said "reconcile the guess panel" → aligned the card fully to the app's `widgets/emotion-picker` + `features/emoji-actions` (the source of truth, verified against `public/lang/ru/translation.json`): heading "Отгадай"→**"Угадай код из 6 эмодзи"** (`t('game.guessTheCode')`); clear "Стереть"→**"Очистить"** (`t('game.actions.clear')`); the app's explicit panel margins (heading mb-3, PreviousAttempt mb-3, EmojiSlots mb-4) replacing the flex-col gap wrapper; PreviousAttempt made tappable (onClick) like the app. So the earlier "kept Figma copy for heading/clear" decision was REVERSED — for the controls the APP wins over Figma ("Угадай"/"Очистить" not "Отгадай"/"Стереть"). The card surface + the blue "Проверить 🔑 5" already matched.

**i18n + Storybook LANGUAGE TOGGLE (board `be5e3c18`; lobby+golden `d1f3b561`):** Operator: "Add toggler in the storybook to switch between languages and implement i18 support for Components." Via AskUserQuestion the Operator chose **full react-i18next** (over a zero-dep lightweight context) + scope **board screen ONLY**. Added `i18next`+`react-i18next` to **devDeps** (mirroring `react` as a devDep — the package's published surface is theme.css/tokens, not the React components) — both ALREADY in the shared `node/` pnpm store (the app uses them) so `pnpm install` did `downloaded 0`. **Engine** `stories/i18n/i18n.ts` = the app's `src/i18n/i18n.ts` TRIMMED for a catalog: `resources` BUNDLED (imported JSON, NOT HttpBackend) ⇒ sync init ⇒ `react:{useSuspense:false}` (no Suspense boundary needed); NO LanguageDetector (the toolbar IS the detector); `fallbackLng:'ru'`; `isInitialized` guard for HMR. **Toolbar** `.storybook/preview.tsx`: a 2nd globalType `locale` (🌐 ru/en) beside `theme`; decorator `withI18n` wraps every story in `<I18nextProvider>` + a `useEffect([locale])` that calls `i18n.changeLanguage` (in an effect, NOT during render) — `useTranslation`'s subscription re-renders consumers on the `languageChanged` event. **Dictionaries** = the app's `public/lang/{en,ru}/translation.json` COPIED VERBATIM to `stories/i18n/locales/{en,ru}/translation.json` (the "same files" the Operator chose) + a NEW top-level `board.*` namespace. **Two-tier key strategy (reusable for future component-i18n passes):** shared game vocab → EXISTING app keys (`game.guessTheCode`, `game.actions.clear/check`, `game.tabs.history/leaderboard`, `lobbyInfo.totalPlayers/totalAttempts/bestAttempt/balance/prizePool`, `share.toStories`, `gameRules.inviteFriend`, `leaderboard.notifications`, `common.lock` as the lock aria-label); board-specific OR divergent-wording copy → `board.*` (freeKeyIn `{{hours}}`, addKeys, roundEnds, previousAttempt, shareKeys.{title,description,tip}, gameRules.{title,description,howToPlay,rules.*,scoring.*}) PRESERVING the current RU verbatim + adding EN. **Net RU change = ONE word** (notifications "лидеров"→the app's "лидера"); all else byte-identical in RU, fully EN. 11 components → `useTranslation()`+`t()` (module-level string consts SCORING/RULES/DEFAULT_TABS/TIP MOVED into the body — a hook can't run at module scope): BalancePill·BoardScreen·BoardTabs·EmojiSlots·GameRules·GuessActions·Leaderboard·PreviousAttempt·RoundInfo·ShareKeys·StatCards (StatusBar/InfoDashboard untouched = no translatable text). **GOTCHA — shoot globals:** `visual/shoot.mjs` interpolates the story `id` straight into the iframe URL, so drive a Storybook global by appending it to the id arg: `shoot "screens-game-free--board&globals=locale:en" figure out.png` — NO shoot.mjs change. **Verified** RU+EN full-board renders (both shot+Read; the toggle flips every region; layout holds); tsc clean (only pre-existing TS2578); build-storybook green. react-i18next falls back to the global init even without a provider, so other stories rendering board components don't break. **Pathspec** (EXCL. Operator out-of-band `lobby/{BuyKeysBanner,PromoBanner}.tsx` + `public/assets/status-bar/cm-logo-md.png` + `echo/.env.example`): `.storybook/preview.tsx` · `package.json` · the 11 board `*.tsx` · `stories/i18n/**` · `node/pnpm-lock.yaml`.

**Lobby + golden i18n extension (`d1f3b561`):** the same two-tier pass over the lobby + golden-game components. New `lobby.*` (subscription pitch, archive section titles) + `golden.*` (roundTime, prizePoolBoost `{{boost}}`, roomTitle, readRules, correctAnswer, viewWinners) namespaces; reused app keys `rooms.{emoji,cells,chooseRoom,getKeysDescription,joinGame}` · `archive.showMore` · `common.close` · `gameOverDialog.playAgain` · `game.guessTheCode`. Edited RoomCard·RoomList·SubscriptionBanner·ArchiveList·LobbyScreen + GoldenHero·GoldenAnswerReveal·GoldenScreen (the golden screens' REUSED board components localize for free — same reuse-graph payoff as the original build). **Demo DATA stays Russian** (Figma-curated room names/CTAs/winners — like the board's @handles); **PromoBanner + BuyKeysBanner NOT localized** (Operator hand-edits them out-of-band — still hardcoded RU = the one visible gap in the EN lobby). Verified lobby+golden RU+EN (5 shots read). **GIT LESSONS (this turn):** (1) `git commit -- <paths>` (the --only form) UNSTAGES a staged *new* file (one with no HEAD blob) as a side-effect — re-`git add` it afterwards to restore the Operator's index (a *modified tracked* file's staging survives fine); use the --only form precisely SO a bare `git commit` can't sweep the Operator's pre-staged files. (2) Coexisted with a concurrent Operator out-of-band commit (`61bf8b9d` "polish" renamed a room + touched LobbyScreen/RoomList, the two files I was also editing): because the polish was already on disk when I Read those files, my Edits layered on top and `git commit -- <paths>` recorded ONLY my i18n diff (no clobber) — always verify overlap with `git show <theirs>` vs `git show <mine>` + a grep for their change in HEAD after committing.

**Board emoji grid → SCROLLABLE 5-row window (`eb9741a1`):** Operator: the board Emoji Grid must show 5 visible rows (Figma) + extra 28 emoji ⇒ a scrollable, touch-capable component. EmojiKeyboard expanded 28→**56** codes (8 rows × 7 cols; the +28 drawn from the **FULLY-FILLED 10×15 sprite sheet** — every one of the 150 cells is a real emoji, so ANY `XXYY` code col 0-9 / row 0-14 is valid; Read the PNG to map cells) + wrapped in a scroll viewport. **5-row window WITHOUT magic px:** the keys are square, so the window height tracks its width via `style={{ aspectRatio: `${columns} / ${visibleRows + 0.4}` }}` (the +0.4 = a peek of row 6 = the scroll affordance; new `visibleRows` prop, default 5). WHY not match the app exactly: the app bounds its keyboard by **viewport height** (`h-[calc(100dvh-Npx)]` parent → keyboard `flex-1 overflow-hidden`, inner `overflow-y-auto h-full`, `cellSize="auto"`=w-full aspect-square, `columns={7}`) NOT a row count — "5 rows" is a CATALOG-only spec, hence the aspect-ratio trick (a catalog has no viewport-height parent to fill). **Mobile touch = 3 CSS props, no JS:** `overflow-y-auto` + `-webkit-overflow-scrolling: touch` (iOS/Telegram-webview momentum) + `touch-action: pan-y` (vertical drag scrolls, taps still fire) + `overscroll-contain` (no page scroll-chaining). Slim scrollbar via Tailwind arbitrary `[&::-webkit-scrollbar]:…` utilities (no global CSS needed). SHARED with golden-game/GoldenScreen → it scrolls there too. Verified: 5 full rows + a 6th-row peek render (isolated `board-emoji-keyboard--default` + board context); tsc clean; build green.

**Board tabs SWITCHABLE (History ⇄ Leaderboard) + Figma-vs-live drift tools (`31499e64`):** Operator: "reconcile BoardCard > BoardTabs with the Figma master — GuessHistory (missing) beside Leaderboard, aligned paddings, avatars, switchable tab" + "use figma-local + implement screenshot comparison tools (live vs exported Figma) + WebSearch github for figma-transform packages". **The gap:** BoardTabs rendered `children` REGARDLESS of the active tab (Leaderboard always showed, no History at all — only the underline color changed). **Fix:** (1) NEW `stories/board/GuessHistory.tsx` = the FIRST/default tab, faithful to the app's `entities/history/history-item` (🔖 attempt no. · the guessed emoji row with the per-peg **green/yellow/red annotation** cells — fixed literals `#4CAF50`/`#FFC107`/`#F44336` like the app, a player taps to cycle · points over a Main-Blue bar); its right column width = the Leaderboard row's `w-[88px]` so the BARS ALIGN when switching; empty state = `history.empty`+`makeFirstAttempt`. (2) BoardTabs made genuinely switchable: new **`panels?: Record<id, ReactNode>`** prop (content keyed by tab id, paired with the default LOCALIZED labels — more ergonomic than per-tab `content` since labels live inside BoardTabs with `t()`) + `defaultActive` (defaults to the FIRST tab = History, matching the app's `defaultValue="history"`); panel resolution `panels?.[id] ?? tab.content ?? children` keeps the single-panel `children` fallback so **Golden keeps working untouched** (GoldenScreen just passes `defaultActive="leaderboard"`). (3) **Padding aligned:** removed the strip's own `px-4`+`gap-8` so the tab strip + panel are FLUSH to the host BoardCard's `p-4` — the active-tab underline now lines up over the row content (rows carry their own `px-2`, shared History+Leaderboard). **Figma ships only the Leaderboard-active state** (node `709:38903`); there's NO history-active board frame ⇒ the app's history-item is the authoritative History design ("app wins for controls"). **TOOLING (the repo already had `shoot.mjs`+`compare.mjs`+`extract`→manifest):** added `visual/figma-export.mjs` (export ONE node→PNG via `src/bridge.mjs`, the token-free bridge — the "capture exported Figma" half) + `visual/drift.mjs` (figma-export → shoot → compare in ONE command) + enhanced `compare.mjs` to also emit a **side-by-side composite** (`<diff>.sxs.png`); scripts `pnpm figma-export`/`pnpm drift`. **LESSON: pixel-diff is the WRONG gate for Figma-vs-live** — the drift run showed 13.6% mismatch that's just font anti-aliasing + photo-vs-emoji avatars, NOT a regression; the side-by-side composite is the artifact to READ, the % gate is reserved for same-engine baselines (so drift.mjs doesn't fail on compare's exit). **figma-transform packages surveyed** (WebSearch, all need a FIGMA_TOKEN so none adopted — the bridge `extract` is token-free): didoo/figma-api, mariohamann/figma-export-assets, WeAreDevelopers/figma-exporter (recorded in visual/README.md). Verified: both tabs render+switch via the drift loop (History annotations; Leaderboard matches the master); tsc clean (pre-existing TS2578 only); build green. 11-file pathspec commit, clean tree (no Operator files staged this turn).

**Retina @2x export + advanced drift tools + drift-removal pass (`5fed2c02` l10n · `a0e86df3` figma-mcp · `515d0cbc` tooling · `68576180` drift):** Operator: "figma-export must replace figma references with Retina @2x; 1) Localize first 2) Fan out frontend Agents to implement tooling + remove design drift with advanced new tools." Ran as **localize → 2-agent Wave-1 (tooling) → 3-agent Wave-2 (per-screen drift)**, Director-verified + committed (agents don't commit). **L10N (`5fed2c02`):** PromoBanner + BuyKeysBanner (the last hardcoded-RU rendered components) → `lobby.promo.*` (Figma RU verbatim + EN) + reuse `keys.purchase.button`; component i18n now COMPLETE — residual RU is Figma-curated DATA only (room names/CTAs; RoomCard falls back to `t('rooms.joinGame')`). **@2x (`a0e86df3` figma-mcp + part of `515d0cbc`):** the figma-local plugin's `exportNode` did `exportAsync({format})` 1× only; added an optional `scale` → Figma `constraint:{type:'SCALE',value:s}` for PNG/JPG (SVG + s===1 unchanged = byte-safe for every existing caller), dispatch forwards `params.scale`, result echoes honored `scale`; chain = plugin `code.js` → `src/bridge.mjs exportNode(id,fmt,scale)` → `figma-export.mjs` (DEFAULTS to @2x, pass trailing `1` for 1×) → `bin extract` (@2x refs + `manifest.renderScale`) → new `visual/reexport-references.mjs <screen>` (re-exports every manifest figure @2x + replaces the reference PNGs in place, idempotent, zero-dep PNG-header sizing, warns "all unchanged" = plugin not reloaded). **CRITICAL @2x BLOCKER: the LIVE deployed plugin ignores scale (tested — every export still 1×); @2x is inert until the Figma plugin is RELOADED on the WINDOWS machine (Operator deploy, [[operator-runs-deploys]]).** After reload: `cd node/codemoji-design && pnpm reexport-references codemojies` → dims should double (375×2461 → 750×4922). Mac-side chain reports the gap end-to-end (`[asked @2x, plugin returned @1x — reload the Figma plugin]`). **Advanced drift tools (`515d0cbc`, in `visual/`):** `overlay.mjs <figma> <live> [prefix]` = onion-skin + difference-blend (resize to common width via a dep-free Playwright canvas — NO sharp; `OffscreenCanvas.convertToBlob` fails headless, use `toDataURL`); `structure-diff.mjs <screen> <storyId> [sel]` = element-level Δpos/Δsize (manifest figure bboxes ↔ live DOM bboxes, greedy match). README has a "which tool" table. **Wave-2 drift fixes (`68576180`):** BOARD = already well-matched, NO edits; LOBBY = RoomCard progress bar onto one row (bar flex-1 + percent inline) + LobbyScreen subscription-hero close ✕ wired; GOLDEN = both in-room CTAs de-gilded → `variant="default"` (the masters show a dark `bg-primary`=`oklch(0 0 0)`=#000 button; gold is for hero/answer/leaderboard only) + GoldenHero label `golden.roundTime`→`board.roundEnds`. **KEY DRIFT-ANALYSIS LESSONS:** (1) the live render is only **~42-46% of the Figma frame height** because the DS deliberately STUBS Figma's image-heavy regions (webp hero/mascot → flat placeholders) → whole-screen overlay is MEANINGLESS, compare section-by-section; the "vertical compactness" is NOT drift. (2) `structure-diff`'s greedy matcher MIS-PAIRS background/full-frame figures (Home Indicator→footer text, Rectangle 26 bg→balance pill) — it's a diagnostic AID, confirm visually. (3) IGNORE artifacts: the `figure` device-bezel ~+7px x-offset (use `#storybook-root > div`), component-story 1000px viewport stretch, off-canvas extract figures (board Rectangle 13). (4) only the BOARD has a `figma/<screen>/manifest.json` (codemojies) → structure-diff is board-only; lobby/golden have whole-screen PNGs in `gameplay/assets/` (overlay-only). **OPEN FLAGS for the Operator (real, not fixed):** golden TOP-CHROME gap (masters use NavPhonePanel+BalancePill+StatCards; live has only thin StatusBar — the biggest golden drift, a restructure); golden finished CTA copy ("Играть ещё (в открытую комнату)" vs `gameOverDialog.playAgain`); golden hero prize unit ($ vs 💎); lobby room/archive COUNT (Figma 4/5 vs data 3/3 — curated); Mr.Freeman tip POSITION (in shared board/ShareKeys, lands after share block). **PRE-EXISTING tsconfig bug:** bare `pnpm exec tsc` aborts `TS2688 Cannot find type definition file for 'node'` (tsconfig pins `types:["node"]` but `@types/node` absent) — use the explicit-flag invocation (`--lib ES2022,DOM,DOM.Iterable …`) which bypasses tsconfig.

**Gotchas:** root `.gitignore` ignores `bin/`, `dist`, AND nested `.gitignore` — the package CLI (`bin/codemoji-design.mjs`) + `dist/theme.css` were **force-added** (future new bin/dist files need `git add -f`); the package `.gitignore` is uncommitted-but-effective. "golden" is OVERLOADED — the *boost class* (`gold_multiplier` on a classic game; the export's golden *screens* `1089:19410`/`1108:27589`) vs the blind commit-reveal game *type* (the app's `entities/golden-games` + the mocked `pages/game/golden-game-tabs.tsx`). `figma/` + `screens/` under the package remain UNTRACKED (pre-existing prior-session work).

**DESIGN-SYSTEM SPEC SET (`docs/codemojex-tma/design/`, commit `32a9dcaa` on top of the Operator's mid-turn `37505c7d`):** authored the design-system spec layer as the COMPANION to the three-tier RENDERING roadmap `docs/codemojex-tma/codemojex-tma.roadmap.md` (Static welcome → LiveView lobby → LiveReact board; the React board island = the components catalogued in node/codemoji-design, mounted via `mount(el,props,bridge)` against `BoardProps`). Files: **`codemojex.design.roadmap.md`** (the `cmd.1`..`cmd.6` rung ladder + the reconcile contract [Figma master → live re-expression → drift removal → deliberate overrides; the app/canon WINS on controls] + the screen↔master table + the toolchain + standing laws + open seams) and **`specs/cmd.1.{md,stories.md,llms.md}`** = "Golden Room ↔ Figma Reconcile" (the f10.1-style triad: Goal·5W·Scope·Deliverables·Invariants·As-built·DoD + Given/When/Then stories + an agent brief). cmd.1 captures: the two golden masters (`1089:19410` in-progress / `1108:27589` finished); the **deliberate-overrides table** (dark `variant="default"` CTA · gold-texture gild · Main-Blue standings bar · board-gradient bg — PRESERVE, never "fix"); the blind/sealed contract (GoldenAnswerReveal finished-only); the golden-manifest extraction gap (only `figma/codemojies/`=board is extracted → structure-diff is board-only; golden has whole-screen PNGs → overlay-only); and **the canon-drift flag (cmd.1 D6/INV6):** the golden components STILL encode the REMOVED `gold_multiplier` — `GoldenHero` `boost?: number /** gold_multiplier */` + `golden.prizePoolBoost`="Призовой пул · {{boost}}× буст" + `screens.data.mjs` "boost-class" labels — vs the approved $1-buy-in TOURNAMENT canon (codemojex.design.md, calibration D-16); the recorded direction = **canon supersedes the pre-recalibration Figma master**, flagged for Operator confirmation ([[codemojex-program]] L-15). **GROUND-FACT (corrects a stale claim in this very note's early paragraphs + my prior mental model):** the gold token is **`--gold-texture`** (`url('/assets/gold.png')` via the `bg-gold-texture` @utility) + the `--gold-*` family (`--gold-surface`/`--gold-border`/`--gold-foreground`, `--color-gold` #CC7500) — **NOT `--gradient-gold`**, which was REPLACED in `e94a3c41` (see the gold-texture paragraph above). I nearly cited the non-existent `--gradient-gold` in the spec (carried from the stale early-paragraph framing) and caught it ONLY by grepping `dist/theme.css` for the real tokens before committing. **LESSON: verify a token/flag name against `dist/theme.css` (or the source) before citing it in a spec — a chronological build-log's early entries age into half-truths.** Author flow used: read a reference triad (`docs/bot/f10.1.{md,stories.md,llms.md}`) for the house format; grounded every node id / i18n key / token against the live tree; ran a relative-link resolution sweep (29 links, 0 broken) + an i18n-key existence check before committing. Authored DIRECTLY (Director-as-architect), NOT via an x-mode team — right-sized for a 4-file docs deliverable ([[right-size-formation-and-write-only-artifacts]]).

**OWED (NOT done this rung):** wire `codemoji-app/src/styles.css` to `@import '@codemoji/design/theme.css'` + a `data-theme` toggle (documented in `THEMING.md` only); capture real Figma variable *modes* as themes via `resolve-variables` — blocked on the Windows plugin reload ([[figma-local-program]]); rebuild the golden blind-gameplay surface (the app's `golden-game-tabs.tsx` is mocked — its REAL tabs are a custom "Ваши варианты" + golden-leaderboard-with-prizes layout, NOT History/Leaderboard; GoldenScreen currently borrows the shared BoardTabs single-panel via `defaultActive="leaderboard"` until that pass); **RELOAD the Windows Figma plugin** then `pnpm reexport-references codemojies` to actually land the @2x references (the @2x tooling is shipped but inert until the reload — [[operator-runs-deploys]]); the golden **top-chrome restructure** (add NavPhonePanel+BalancePill+StatCards to match the masters — the biggest remaining golden drift, flagged not fixed); optionally key the demo room DATA (names/CTAs) for a fully-English lobby (the banners themselves are now localized, `5fed2c02`); add `@types/node` to devDeps (or drop `types:["node"]` from tsconfig) so bare `tsc` stops aborting. [[codemojex-program]]
