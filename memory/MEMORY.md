# Memory index

- [msh mcp server](msh-mcp-server.md) — go/msh = the SELF-CONTAINED "msh" Go module (msh-memory folded in at go/msh/memory/; old top-level module deleted): CLI `msh memory|mint|specs` + MCP server serving `mcp__msh__{memory_audit,memory_stale,memory_graph,memory_scan,memory_project,mint,specs}` over streamable-HTTP `:8899` (registered in .mcp.json); **specs (2026-06-17) = stale-md-link checker over a docs tree (`msh specs echo_mq`): resolves relative links to the real filesystem + checks #anchors, skips http/site-absolute `/route` links; memory/internal/speclint + command.SpecsLinks**; `mint`=brd14 branded snowflake (codec vendored from dev/echo_data/runtimes/go/brandedid, minter ports EchoData.Snowflake); **`.msh-memory.json`=project anchor (root + project{name,code,roadmap,state.current_rung}); root AUTO-resolved (command.ResolveRoot walk-up) so `msh memory scan` is a one-liner; project surfaced via `msh memory project`/`memory_project`**; adding a tool/starting server needs `/mcp` reconnect; mcp-go=official go-sdk at go/mcp-go; GOWORK=off per-module; go.work has pre-existing broken members (warn-not-block)
- [mcpd controller](mcpd-controller.md) — go/mcpd Go cobra+bubbletea controller for the aaw(:8905)+msh(:8899) MCP servers: `bin/mcpd`, bare=TUI, `start/restart/stop [-d]` (default fg-supervise, -d detach), `make mcp`=build+safe-hot-swap-restart-detached; gotchas: build→rename→stop→start (a failed build never kills a live server), aaw flock-release wait (else INSTANCE_LOCKED), literal `localhost:8905` for aaw's wire-check, GOWORK=off builds, manages both via foreground `serve` not msh's own daemon; [[msh-mcp-server]]
- [go workspace active-dev](go-workspace-active-dev.md) — go/ = the local agent OS in active dev: aaw (task-mgmt :8905, aligned to docs/aaw/aaw.framework.md) + msh (memory :8899, 7 tools) → fully-fledged; go/go.work over the agent-infra 4 (builds clean; GOWORK=off for mcpd/CI + standalone tools); apps/→go/ reorg done; specs docs/go/; root CLAUDE.md redirects Go work → go/CLAUDE.md; [[go-mcp-go-research-preview]]
- [go mcp-go research preview](go-mcp-go-research-preview.md) — go/mcp-go = Research Preview of the OFFICIAL MCP Go SDK (NOT mark3labs); first-party fork FREE TO MODIFY (D-5); aaw+msh via replace ../mcp-go; [[go-workspace-active-dev]]
- [echo-courses program](echo-courses-program.md) — go/echo-courses = new Echo v5 server rebuilding /courses from a catalog (preserves the 5 published URLs); Echo v5.2.0 vendored at go/echo, consumed via `replace => ../echo`, build GOWORK=off (not a go.work member); specs docs/echo_courses (ec.1–ec.6); **ec.1–ec.6 COMPLETE** (ec.1–ec.5 SHIPPED 2026-06-21, ec.6 SHIPPED 2026-06-22) via `/echo-courses-ship` (x-mode; ec.2–ec.5 = Director + 1 mars builder; ec.6 = Venus + Director + Apollo, no mars); ec.4 = the live site (routes + URL-parity + catalog-driven index/detail + facet_order + deploy-ready Dockerfile/fly.toml; verified via the LOCAL DEV SERVER, operator runs `fly deploy`); ec.5 = polish (`4e566adf`/`0f8461b7`: the design-system CSS + the two interactive scripts externalized to embedded, content-hash-served `web/static/app.<hash>.{css,js}` byte-equivalent to the former inline blocks via `internal/asset` + per-page SEO/OG/canonical from env CANONICAL_BASE via a `render.Head` struct + `internal/handler/seo.go` /sitemap.xml + /robots.txt; **D-1 Arm A1 embed+content-hash ruled up front**; Stage-4 collapsed; gotchas: the reconcile dropped "fonts" — they stay on the Google Fonts CDN; the hash routes are GET-only HEAD→405 non-blocking); **ladder RECONCILED to deploy-forward vertical slices** (every rung from ec.4 ships a complete live site; roadmap §7 decisions ruled + F-C landing); ec.6 = the production cutover DOCS-ONLY (`752b673d`; HIGH, Apollo-mandatory → BUILD-GRADE; recon found jonnify.fly.dev is a large Operator-owned multi-section site — server source out-of-VCS — serving the courses index at `/` (not /courses) + the 5 course paths AS deep-course roots, so reconciled to an **index-only `/` flip**; an Operator-facing cutover runbook + flyctl-v0.4.6-pinned two-step rollback + a 15-assertion smoke; the team ran no fly + touched no live app, the **Operator executes the cutover**); **the echo-courses build program ec.1–ec.6 is COMPLETE — only the Operator's manual `fly deploy` cutover remains**; gotcha: a build can land out-of-band RED on its own gate (gofmt + a test asserting a JS comment html/template strips from inline `<script>`) — re-run the gate on disk; v5 gotchas: no e.Shutdown (StartConfig+signal.NotifyContext), `*echo.Context` struct, Renderer `Render(c,w,name,data)`; [[go-workspace-active-dev]] [[jonnify-cms-toolchain]] [[echo-static-server]]
- [echo-static server](echo-static-server.md) — go/echo-static = a SECOND Echo v5 server serving the repo html/ tree RAW byte-for-byte (parity), port :1330, vs echo-courses' catalog rebuild (:1323); built 2026-06-22 via a fanned-out Workflow (10 agents, 153/154 green) to URGENTLY serve the new /echo-persistence course (25pp persistence course, content pre-authored on disk); **try-files resolver** (P→P.html→P/index.html, dir→index.html) for clean extensionless URLs; v5 gotchas: catch-all must be `RouteNotFound("/*")` not `GET("/*")`, and `http.ServeFile` + manual 404 bytes (c.File/e.Static reject abs/.. fs.FS paths); `make start` (:1330, HTML_DIR=../../html, mirrors echo-courses start/stop/restart/status); known divergence: /elixir 404s (the elixir course is at repo-root elixir/, OUTSIDE html/); echo-persistence now has a home-page tile in html/index.html (6th series-card, persistence track, accent #d68f2f, storage-tier SVG, counters 5→6, added 2026-06-22) but NOT yet in html/sitemap.xml; uncommitted, no Dockerfile yet; [[echo-courses-program]]
- [mesh course toolkit](mesh-course.md) — /mesh "EchoMesh, In Depth" + the mesh-* toolkit (mesh-expert agent + mesh-course-writer skill + /mesh-write) + the `.htabs` hover-tab component; **M0–M8 built (37 pages A+, live, MSH stamp) — COURSE COMPLETE**; M8 "The Future Is Now" (2026-06-15) = the journey signature (/redis-patterns→/echomq→/bcs→/art→/mesh, each step no new external dep) + a 3-pillar reveal (BEAM-as-Fowler-LMAX, self-healing, Valkey/Postgres/SQLite-journal→Tigris), pieces REAL composition PROPOSED; gotcha: the no-future cms gate's blunt `/future` substring check collides with the m8=future slug — fixed in apollo.go (exempts `/mesh/future`); violet+CAP-duality on the /bcs contract-sheet basis; senior successor to /art's EchoMesh chapters
- [right-size formation + write-only artifacts](right-size-formation-and-write-only-artifacts.md) — Operator cost-discipline (after a 2-line fix cost ~300k): Director triages formation at bootstrap (trivial → 1 builder, NOT the full team; rigor constant, only ceremony scales) + generated bundles are WRITE-ONLY (never Read/grep-lines/git-diff; verify via source + grep -c + the running-server curl); **canonical charter → docs/AAW_DEVELOPMENT.md** (code-first, slim high-level specs, don't-fuse-horizon-decisions-into-rungs)
- [art course](art-course.md) — /art "The Art of BCS" = senior continuation of /bcs; infra trio (art-course-writer skill + art-expert agent + /art-write) + A0 landing BUILT (PASS A+) + server-wired; architect's-blueprint identity (--a-* indigo/avail/mesh/edge), borrowed-availability calculator; resume /art-write a0 credo runtime map
- [bcs course](bcs-course.md) — /bcs course (docs/echo/bcs), own contract-sheet identity; B0+B1+B2 BUILT; B3 (B3.1–B3.3), B4 (B4.1–B4.4; B4.5 BLOCKED), B8 capstone (landing+B8.1–B8.2, 9pp) all in progress; B8 two-layer grounding = substrate AS-BUILT+hardened (echo/apps/echo_store+echo_mq, docs/echo_mq/emq.roadmap.md) vs Exchange.* consumer PROPOSED; resume /bcs-write trading strategies scale-out OR bus fair-lanes
- [exchange platform](exchange-platform.md) — REAL-code build of the BCS B8 trading capstone in echo/apps/exchange (Exchange.*); docs RENAMED docs/trading→docs/exchange (rung quads keep `trd` codename); trd.1.1 Gateway MVP SHIPPED 39cc2baa via x-mode Flat-L2; next trd.1.2
- [jonnify-cms toolchain](jonnify-cms-toolchain.md) — Go Cobra tool auditing/gating the /elixir course (manifest×filesystem×gates); not in CLAUDE.md
- [agile course canonical route](agile-course-canonical-route.md) — /course/agile-agent-workflow 3-level course; A0–A5 BUILT, frontier A6 (needs a6.* triad); aaw.progress.md = live dashboard; fan-out + md-first patterns in file
- [elixir course update pipeline](elixir/elixir-course-update-pipeline.md) — elixir-progress.md triggers the update: watcher + elixir-refs-rollout workflow; the run sequence
- [elixir hero svg fan-out](elixir/elixir-hero-svg-fanout.md) — fan-out adding interactive hero SVGs per chapter hub (F2–F5 done, F6 as built); build→adversarial-verify
- [user commits elixir batches](user-commits-elixir-batches.md) — the user commits course work out-of-band; the tree goes clean between turns
- [elixir references bibliography](elixir/elixir-references-bibliography.md) — REFS dict → kb md → page insert; stale-source generator bug the gates can't catch; do-no-harm repair pattern
- [elixir content fan-out drift](elixir/elixir-content-fanout-drift.md) — author-agents invent/redefine the Portal API; gates pass it; ground-facts + no-invent + adversarial-verify guard
- [course nav prose: no redundant status](course-nav-prose-no-redundant-status.md) — no "chapter is complete"-style prose in /elixir nav; pills already show status — describe structure + arc
- [elixir batch presentational drift](elixir/elixir-batch-presentational-drift.md) — new module batches diverge from the template gate-invisibly; audit one-liners + do-no-harm fixes
- [elixir toolkit portable kit](elixir/elixir-toolkit-portable-kit.md) — docs/elixir/toolkit = portable re-upload authoring kit (own build_page.py); content re-synced FROM live; _head aligned
- [cms content-store parity](cms-content-store-parity.md) — jonnify-cms SQLite content-store + Go byte-parity test (204pp round-trip identical)
- [workflow heavy agent → no schema](workflow-heavy-agent-no-schema.md) — edit-heavy Workflow agents return prose (big required schemas abort the run); small schemas on read-only passes; recover from tree
- [apollo mentoring loop](apollo-mentoring-loop.md) — Apollo = .operator.md writer + Venus/Mars mentor, PROPOSE-ONLY; aim a guardrail at the peer whose CONTRACT the finding implicates
- [elixir content md tree](elixir/elixir-content-md-tree.md) — docs/elixir/content/<route>/*.md = per-route source-of-record for all 204 /elixir pages; backfill workflow + bijection-verify
- [echo_bot multibot engine](echo-bot-multibot-engine.md) — echo_bot = standalone YAML-driven multibot engine in apps/echo_bot; specs by Venus
- [redis-patterns course](redis-patterns-course.md) — /redis-patterns 30 patterns APPLIED; R0–R4 COMPLETE (28/53 modules), next R5 strategic entry; senior-narrative + prompt-pack fan-out; the gotcha bank is archived in the file
- [redis reframe-echomq](redis-reframe-echomq.md) — /redis-patterns reframe → contract-sheet identity grounded in REAL echo/apps code, EXCHANGE PLATFORM (echo/apps/exchange) the consumer, ZERO BullMQ, no .out, Valkey-only; B0/R0 (10pp) + B1/R1 caching (29pp) + B2/R2 coordination (22pp) reconciled + 4 meta-files + contract reground + one-shot /redis-reconcile command (B<N>→R<N>); B2 = the v1-vs-v2 trap (cite echo/apps/echo_mq v2, NOT frozen echo/apps/echomq or the deleted Go port); emq path→docs/echo_mq/emq.design.md; resume /redis-reconcile B3 (R3 queues)

## AAW - Agile Agent Workflow discipline and mcp server 

- [aaw memory index](aaw/aaw-memory.md) - slim doc-pointer index of aaw related memory notes.
- [aaw mcp design phase](aaw/aaw-mcp-design-phase.md) — v2 design phase complete; locks D-7 (Apollo OUT of design phases) + D-8 (synthesis = real venus type); build entry MCP-1
- [aaw mcp server ladder](aaw/aaw-mcp-server-ladder.md) — MCP1 shipped 7972859f; tiered formation D-10; live server runs the pre-rung binary until Operator restart; next MCP2
- [aaw mcp server v2](aaw/aaw-mcp-server-v2.md) — [RECONCILE] mcp1–3 SHIPPED, mcp4 BUILD-GRADE, mcp5 specced (transport→mcp8), mcp6 = measurement rung; ledger real=SLUG/dotted=symlink; dashboard specs/mcp.progress.md

## EchoMQ program (`echo_mq/`)

The echo_mq 3.0 library development driven by the `emq.*` specs — canon `docs/echo_mq/`, operating manual + agent calibrations `docs/echo_mq/program/`, source `echo/apps/echo_mq` (above `echo_wire`, beside `echo_data`/`echo_store`) in the BCS umbrella.

- [echo_mq three movements](echo_mq/echo-mq-three-movements.md) — the master program memory: echo/apps/echo_mq = THE v2/v3 lib (apps/echomq REMOVED — rewritten fresh + deleted). DEPTH on-disk at docs/echo_mq/program/emq.program.md (operating manual + agent calibrations emq.{venus,mars,apollo}.md); canon docs/echo_mq/emq.design.md + emq.roadmap.md. **emq.0/1/2.1-2.4/3.1-3.5 SHIPPED — MOVEMENT I CLOSED** (emq.3.5 grandchildren SHIPPED, Apollo BUILD-GRADE; the same-queue re-emit FALSE-GREEN gotcha — depth-4 multi-tick the load-bearing proof); flow family parity-complete → **Movement II (emq.4–8) opens**; Epics layer at docs/echo_mq/epics/. Recalibration 2026-06-17 (D-1): Mars=code-quality gate+stories, Director verifies, Apollo=Mentor ONLY (out of pipeline, calibrates agents from consolidated findings); consumer pivot codemojex(live)+echo_bot(planned Telegram), Exchange dropped. **emq.4 (groups deepened) chapter SPECCED 2026-06-18** — 4.1-4.4 carved spec-only via /echo-mq-ship (Venus author + Director verify, no Mars/code); spine 4.1 control plane (re-aims RETIRED changePriority/getCountsPerPriority) · 4.2 group-aware recovery · 4.3 park-don't-poll metronome HIGH · 4.4 weighted/deficit+drill; forks A(4.3 deepen-vs-found)/B(4.4 mechanism) OPEN, C(4.1 intra-group priority) RULED PARK; build order 4.1✅→4.2→4.3(Apollo-mandatory)→4.4. **emq.4.1 (fair-lanes control plane) SHIPPED 2026-06-18** — `reassign/4`+`@greassign` (multi-key atomic lane move; the `HSET <row> group=dst` load-bearing rewrite jobs.ex reads at complete/retry/promote/reap) + `drain/3`+`@gdrain` (lane-scoped DESTRUCTIVE drain, blast-radius bounded by construction); conf 52→54; **HIGH-risk** (R3 destructive drain surfaced MID-BUILD → Operator ruled BUILD D-5 → NORMAL→HIGH; gate = blast-radius mutation battery, NOT the ≥100 loop); commits `6bca0d6d` rung/`7dc828ff` 3.0-note/`6d79e17e` program-cal; **echomq:3.0.0 era** (additive minors over frozen 2.0.0 → 3.0.0 ratified at emq.8); Apollo Stage-7 Operator-grantable (D-4 process-doc reconcile + D-5 destructive-op eval). emq.4.2/4.3/4.4 SHIPPED → emq.4 groups family PARITY-COMPLETE 2026-06-19 (4.4 `361fd663` = Fork B Arm 2 additive weighted multi-pop `@gwclaim`/`wclaim/3` + `emq:{q}:gweight`, `@gclaim` byte-frozen, conf 61, NORMAL+). **RECONCILED 2026-06-19** — the emq.4 family fold folded into the canon docs (roadmap/progress/changelog/emq.4.md were LAGGING at conf 55 / "4.3 RULED, 4.4 next"; now show conf 61, emq.4 CLOSED, the two version planes, the pinned lineage incl. the ewr.2.5/2.6 +4 interleave and 4.3-metronome +0); msh link sweep 0. **emq.5 (batches) family OPEN — emq.5.1 (batch-claim spine) SHIPPED 2026-06-20** via /echo-mq-ship Flat-L2 (`1c36b70a` foundation triad+carve / `bca36d0c` rung): the NEW inline `@bclaim` = a count-variant ZPOPMIN emq:{q}:pending loop ×N INSIDE the script (the non-grouped @gwclaim generalization; one TIME, one batch lease, per-member attempts; design §6.2, NOT client LMPOP/ZMPOP) + `Jobs.claim_batch/4` (manual-pull host API, pause-first, `{:ok,[{id,payload,att}]}`|`:empty`). Forks ruled D-1 the LOOP / D-2 return-the-short-batch (non-blocking; M=0/paused→:empty; oversized→depth) / D-3 +3 conf→64 (`batch_claim`·`batch_claim_short`·`batch_partial_failure`) / D-4 label 2.5.0; `@claim`+all shipped scripts BYTE-FROZEN (jobs.ex 94 ins/0 del); declared-keys A-1 = KEYS[1]=pending/KEYS[2]=active PIN the {q} slot, row jk=ARGV[1]..id rides it; wire @wire_version FROZEN echomq:2.4.2 (label-plane only climbs). Director verify = byte-freeze + F-1-by-hand + order-theorem + net-zero ZPOPMIN→ZPOPMAX caught 6× + TWO independent ≥100 loops 100/0; Mars-2 collapsed (zero findings); L-1 = INV3 "an ARGV base is NOT a declared root; the braced KEYS[] pin the {q} slot" folded into spec (gate-invisible on single-node Valkey → the manual declared-keys review is the only defense). **emq.5.2 (shaping) + emq.5.3 (group-affinity) SHIPPED 2026-06-21** — 5.2 a NEW `EchoMQ.BatchConsumer` module (a SIBLING of Consumer, the mode-lean reversed) + the pure `BatchShaper.Core`, watch-depth over the byte-frozen @bclaim, per-member verdict map + fail-safe, conf 64→67, label 2.5.0 held (`60de5dc8`/`47562bbb`); 5.3 the NEW additive `@gbclaim` = the `@gwclaim` ISOMORPH minus the gweight read (ring-rotated, K=min(depth,glimit headroom)) + `Lanes.bclaim/3`, reuse gactive, conf 67→70, label 2.5.1, the INDEPENDENT ≥100 loop 100/100 (`a299aa73`/`242375df`); **5.3-C** (ring-rotated vs caller-named) a NEW fork Venus surfaced at the reconcile — caller-named affinity is a FUTURE additive `bclaim/4`. **NEXT: emq.5.4** the partitioned finish (over byte-frozen @complete/@retry/@schedule, NORMAL). **Calibration COMPOUNDING** (`9b84245b` 5.1/5.2 · `ecb48cd9` 5.3): the echo-mq skills now carry declared-keys A-1/L-1 + conformance-mirror + link-depth + verify-scope + the position-encoded label convention + the msh bracket-then-paren Lua-prose hygiene + the additive-isomorph build-craft; L-1+L-2 RE-FIRED GREEN on 5.3. Slim pointer
- [ewr wire program](echo_mq/ewr-wire-program.md) — NEW EchoWire client-core spec program docs/echo_mq/wire/ (prefix ewr., sibling to emq.*): ports valkey-go/rueidis CONSTRUCTION ergonomics onto the owned wire echo_wire, ADDITIVE over Connector.pipeline/3 (the connector ALREADY auto-pipelines — adds construction sugar, not pipelining); design fork ewr.design.md RULED Arm A = EchoWire.Pipe (%Pipe{conn,cmds}→exec=pipeline/3, curated-verbs + Pipe.command/2 escape; arms B Cmd/C Query chosen-against but layerable later); Movement I core (ewr.1.1 Pipe · 1.2 cmd-value+cf-flags ADVISORY · 1.3 two-tier errors) + Movement II CLIENT TRACKING caching SHIPPED 2026-06-18 as echo_store `:tracking` coherence mode (EchoStore.Table, ZERO wire edit — connector already sufficed; wire-MAJOR demoted to deferred warmth opt; docs/echo_mq/store/store.tracking.md); ABOVE conformance boundary (facade stays 11 verbs, run/2→{:ok,52} byte-stable, Connector/RESP/Script/Pool frozen, EchoMQ.Pool has pipeline NOT txn/noreply); founding rung ewr.1.1 SPECCED — canon+triad+ledger authored 2026-06-18, NOT built, next /echo-mq-ship Flat-L2 slug ewr-1-1 risk-LOW; store.design.md does NOT consume the surface (rides frozen Connector PUB/SUB → wire NOT store-forced); companion method docs/aaw/aaw.architect-approach.md authored same chain
- [ewr bench roadmap](echo_mq/ewr-bench-roadmap.md) — the bench-derived EchoMQ wire-version ladder (docs/echo_mq/wire/ewr4.roadmap.md, Rungs 1-6 → echomq:2.5.0..3.0.0), DISTINCT from the ewr.1.x client-core; "ewr.2.6"=Rung 2 native expiry=spec ewr.4.2, "2.5"=Rung 1 client floor=ewr.4.1; client floor (5eca83fa/d52bf08a) + native expiry lock-field-on-job-hash HEXPIRE (626e6674) SHIPPED Solo 2026-06-19; THE PATTERN = ship-substance-additively-in-boundary-green + DEFER-the-fence-cutover (shared :6390 fence-climb bricks others + contested version number → client-contract/additive rung defaults to NO climb), the antidote to the ~2h/~370k fence saga
- [echo_store rename](echo_mq/echo-store-rename.md) — echo_cache→echo_store (2026-06-18, code-complete): app :echo_store v2.0.0, EchoStore.*; keyspace UNCHANGED ecc: (no ecs:); EchoCache.Shadow RETIRED; new EchoStore.Graft.* CubDB/Tigris engine; decision doc docs/echo_mq/store/design/store.design.md; docs/echo_mq reconciled link-clean (msh specs→0, was 177) + rename-complete; CLAUDE.md manuals + codemojex code + Claude memory DONE 2026-06-18; course docs (bcs/redis/mesh/art) + repo memory corpus pending own reconciles
- [echo_graft fork](echo_graft/echo-graft-fork.md) — **from-scratch** Rust durability tier seeded from Graft, **NO upstream compat** (not API/wire/cherry-pick; upstream=read-only idea source; standing brief `echo/apps/echo_graft/README.md` SUPERSEDES the former `FORK.md`); echo_store consumes it; WORK TREE `echo/apps/echo_graft` (cargo workspace inside the Elixir umbrella's apps/, Mix-invisible), REFERENCE `github.local/graft` @ b07d9312 (read-only, never edit/sync-back); 6-rung epic eg.1–eg.6 (specs `docs/graft/`); **eg.1 carve + eg.2 remote-fence(live-Tigris) + eg.3 branded-id/change-feed SHIPPED** (98 tests, determinism 100/100, NOT committed — untracked on jonnify `echo_mq` branch); ground truths: remote=**opendal** NOT object_store, native id=base58 `Gid` (branded `{NS}{base62}` is caller-supplied, no Rust minter), commit OCC version-level NOT page-level, macOS `tcp_user_timeout` cfg-gate, **fault tests need `--test-threads=1`** (global precept state); **eg.4 DESIGN-RECONCILED 2026-06-21** (Venus design rung): the umbrella has TWO functional-twin Graft engines — Rust `echo_graft` + a complete native-BEAM `EchoStore.Graft.*` (CubDB; VolumeServer single-writer, Streamer→Tigris, Sync feed `graft:{vol}:commits`) in apps/echo_store ("no foreign engine"); duplicated role = the page/Volume engine; the only hot-path need (LOW-VOLUME `EchoStore.Durability.*` outbox) already met natively. **Operator ruled D-1=A COEXIST** (both kept, native canonical+UNTOUCHED) + **D-2 name=`echo_graft_backend`** (Rust EchoMQ-participant crate/binary; wire `echo_graft_proto`; Elixir client `EchoStore.GraftBackend` = coexisting peer NOT replacement; Rust lane `egraft:feed:{vol}` distinct). Design doc `docs/graft/graft.engine-split.design.md`; `graft.4.md` reconciled+unblocked (claim narrowed to "drive the Rust page-engine"). eg.4 builds backend sidecar + byte-frozen versioned RESP3 proto (reuse `EchoMQ.RESP`) + Elixir client + dual-side conformance; transport in-process/abstract, live bus leg env-gated; eg.6 shootout per-workload (D-5). [[echo-store-rename]]
- [echo_mq stories generator](echo_mq/echo-mq-stories-generator.md) — the BDD self-documenting-tests → generated-stories pipeline (built 2026-06-15, NOT in CLAUDE.md): the `EchoMQ.Story` DSL (test/support/echo_mq/story.ex) + `mix echo_mq.stories` (offline, no Valkey) → docs/echo_mq/stories/<feature>.stories.md + a README catalogue; macro gotchas (emit fully-qualified ExUnit.Case.test for hygiene; step macros expand inline so a given_ var reaches then_); regenerate after editing any scenario
- [echomq umbrella app — REMOVED](echo_mq/echomq-umbrella-app.md) — apps/echomq is REMOVED: its surface was rewritten fresh into echo_mq under the v2 laws, then deleted (single source of truth); tombstone — residual fact: echo_mq's EchoMq.MixProject name dodged the old EchoMQ.MixProject collision (now moot)
- [reorg link-rebase verification](echo_mq/reorg-link-rebase-verification.md) — craft lesson from the echo_mq specs/ reorg (cbba4517): GENERALIZE the relative-link re-base rule after a docs folder move (don't enumerate the links you happened to notice) + run a REPRODUCIBLE full sweep on a STABLE tree; a "0 broken" claim is only as good as the checker's coverage; anchors the spec-home convention (specs/ = chapter triads · decomposition → specs/emq.N/ · ledgers → specs/progress/)
