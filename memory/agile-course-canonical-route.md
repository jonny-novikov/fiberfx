---
name: agile-course-canonical-route
description: /course/agile-agent-workflow route + the 3-level A<chapter>.<module>.<subpage> structure (chapter→module→subpage, ≥3 subpages, "dives into" like elixir); home page is the route manifest
metadata:
  node_type: memory
  originSessionId: 73b81fc7-ffef-4d6d-ac38-349cafb4dda9
  type: project
---

The `/Users/jonny/dev/jonnify` "Agile Agent Workflow" course (`html/agile-agent-workflow/`) is served at **`/course/agile-agent-workflow`** — re-mounted in `main.go` from the old `/agile-agent-workflow` (which now 404s). Chapter segment = the **on-disk directory name** (semantic, e.g. `why`); the `/course` prefix is KEPT. Tell jonnify-cms the mount explicitly: `cms check --routes-from /course/agile-agent-workflow=html/agile-agent-workflow --chapter-alias a0=what,a1=why,a2=decomposition --require-refs <files>` (alias one `a<N>=<dir>` per built chapter).

**Three-level structure `A<chapter>.<module>.<subpage>`, like the elixir course:**
- **Chapter** `A[N]` (A0…A7) — top level, has a **landing**. e.g. A1 = Why → `/why` (`why/index.html`).
- **Module** `A[N].[M]` — a chapter *has* modules, each with a **hub** nested under the chapter dir. e.g. A2.01 "Value, not tasks" → `/decomposition/value` (`decomposition/value/index.html`).
- **Subpage** `A[N].[M].[S]` — each module has **≥3** subpages (the "Dives into" list on the hub). e.g. A2.01.1/.2/.3 → `/decomposition/value/{outcome-not-chore,who-benefits,vertical-slice}`.
- **Module-hub pager convention (the model, gate-invisible):** `prev` = the **chapter landing**, `next` = the module's **own first dive** (NOT sibling-module chaining). Match `why/two-layers/index.html`.
- **A0 was consolidated:** the old `/intro` landing is **retired** — `/what` now doubles as the A0 chapter landing AND the A0.2 module hub (so `a0=what`). A1+ nest modules normally.

**The course home page (`index.html`) IS the route manifest** — its chapter tiles *define* the routes via deliberate forward-links. It historically **failed the `links` gate by design** while chapters were unbuilt; as of the A5/A6/A7 landings batch **ALL EIGHT chapter landings (A0–A7: `/what`,`/why`,`/decomposition`,`/roadmap`,`/spec`,`/brief`,`/reliability`,`/portal`) exist, so the home now gates `links` PASS** (the manifest's promise is true — the chapter skeleton is complete end to end). Do **not** add an "Open chapter" label to the tiles — the whole card is already an `<a>`, so it's redundant (user feedback). A **References section (`.refs`) is mandatory on every page** — enforced by the opt-in tenth gate `cms check --require-refs` (off by default so it never fails elixir pages).

**Authoring infrastructure (as of 2026-06-04):** the course has a dedicated **`agile-expert` subagent** (`.claude/agents/agile-expert.md`) — a THIN def that loads the `agile-course-writer` skill as its single source of truth and hard-codes only the spawn defaults + gate-invisible rules + guardrails (clickable segmented route-tag, canonical `.foot-cols` footer, real vetted Sources links, no-invent `Portal.ID`-only API, never run git). The author guide is **`docs/agile-agent-workflow/CLAUDE.md`** (points at the skill + the four living views + the gate + the registry). **GOTCHA:** a freshly-written `.claude/agents/*.md` is NOT spawnable mid-session (the agent registry loads at session start) — `Agent(subagent_type:"agile-expert")` errors "agent type not found"; fall back to `subagent_type:"general-purpose"` with a self-contained brief (it works because the prompt already says "load the agile-course-writer skill first"). There is also a **`/agile-write` command** (`.claude/commands/agile-write.md`) that fans out one agile-expert per module then does the orchestrator-only relink+gate+sync, and an Operator runbook **`docs/agile-agent-workflow/agile-agent-workflow.operator.md`**. **Registry-reload nuance:** the mid-session "not spawnable" gotcha resolves once the session advances/reloads — agile-expert DID resolve in a later turn, so TRY `subagent_type:"agile-expert"` first and fall back to general-purpose only on an actual "agent type not found" error (don't pre-empt the fallback). **TOC-race GOTCHA:** fan-out agents tend to over-sync the shared TOC (scope creep past their "edit only your module" contract) → concurrent writes to the TOC race and clobber (the A2.04–07 batch lost the §2.6/§2.7 built-markers this way); keep **all four-view sync an orchestrator-only step AFTER the fan-out** and reconcile the TOC deterministically. Build state (2026-06-05): A0 (`/what`) built; **A1 COMPLETE (A1.01–A1.06)**; **A2 COMPLETE (A2.01–A2.07)**; **A3 COMPLETE — landing + 3 orientation dives + A3.1–A3.9** (`/roadmap/{agile-distilled,xp-small-batches,roadmap-anatomy,thin-but-robust,milestones,program-roadmap,tracer-bullets,workshop,glossary}`); **A4 COMPLETE — landing + all seven A4.1–A4.7 built** (`/spec/{by-example,the-triad,spec-anatomy,to-stories,invariants,traceability,workshop}`, NO orientation dives — landing goes straight to the 7 module cards; grounded VERBATIM on the real F6.1/F5.1 rung triads under `docs/elixir/specs/` read-only); **A5/A6/A7 chapter LANDINGS + a why/what/how orientation triptych each are now BUILT** (cross-chapter landings batch, 12 pages A+ — `/brief`, `/reliability`, `/portal`, each + `{why,what,how}`; A5 grounded on its seeded triad, A6/A7 grounded ONLY on `aaw.roadmap.md` with their module/step sets honestly DEFERRED via a `.note`, NOT invented). **A5 COMPLETE (2026-06-05) — landing + 3 orientation dives + ALL EIGHT modules A5.1–A5.8 built** (each hub + 3 dives = **32 pages**, 32/32 gate A+): `/brief/{llms-txt,references-requirements,execution-topology,agent-stories,implementation-prompt,running-agents,the-thesis,workshop}`, grounded VERBATIM on real `f6.1.llms.md` (References/`R1…R8`, topology/`T1→T7`, `AS1…AS4`, the prompt), the `f6.6`/`f6.7` ship prompts, and `f5.2.llms.md` (workshop's engine rung; `Portal.Accounts`/`Catalog`/`Learning` are REAL F5 contexts — verify a surprising surface against THAT page's grounding file, not the as-built F6 facade). **Frontier = A6 "Reliability" — landing+orientation built but NO triad; seed `a6.*` then build (same loop). A7 follows.** **NEW course-level instrument: `docs/agile-agent-workflow/specs/aaw.progress.md`** — a live completion DASHBOARD (distinct from the per-chapter `a<N>.progress.md` build narratives): per-rung status glyph (✓/◐/○) + a 24-cell ANSI block bar `█░` + % + (built/total), at page granularity (module = hub+3 dives = 4 pages); recounted at every rung-close alongside the four views; course total counts 47 modules (A6 excluded until its triad is seeded). Currently 38/47 = 81%. The **four living views must agree**: served pages, `docs/.../agile-agent-workflow.toc.md`, `course-map.md`, `docs/.../llms.md`.

**Spec-first build (as of 2026-06-05, the course is its own exemplar):** the course now has its OWN spec system at `docs/agile-agent-workflow/specs/`, mirroring the Portal's `docs/elixir/specs/` one altitude up. Structure (user-directed): a **single** course roadmap `aaw.roadmap.md` (NOT per-chapter — `a3.roadmap.md` was created then deleted as stale), the build runbook `aaw.operator.md`, and per-chapter `a<N>.llms.md` (agent brief) + `a<N>.stories.md` (user stories for learner/developer/Claude-Agent, each Connextra + Given/When/Then + acceptance). **Each spec file opens with a `>` directive** (the user seeds empty files with `[WRITE … HERE]` placeholders to fill). The loop: write the chapter's stories + brief, THEN fan out `agile-expert` Authors that read `a<N>.llms.md` + `a<N>.stories.md` as their spec and build hub+dives from it. **A3 is COMPLETE (all nine A3.1–A3.9 built spec-first, each grounded VERBATIM on the real F6 `docs/elixir/specs/phoenix/phoenix.roadmap.md`).** **Reusable pattern (the A3.4–A3.8 batch, user-directed): supervised Senior-Writer-then-fanout.** Spawn ONE lead agent FIRST (sequential) to (a) review/edit the already-built sibling modules do-no-harm and (b) author **`a<N>.progress.md`** in `specs/` — the agile analogue of the Portal's `f6.progress.md`: a `>`-directive narrative carrying the chapter arc + the locked grounding facts to reuse verbatim + the per-module plan (slug/route/dives/bridge) + any Director mandates. Then EMBED that narrative verbatim into each parallel module Author's prompt so all modules stay mutually consistent with the already-built ones. **Director mandate honored for A3/A2.07: spec/component-level only — NO Elixir code on the page; `pre.code` carries `roadmap.md`/table markdown, never source; cite `/elixir/phoenix` for where the framework implements an idea.** The model-pager convention (hub `next` = own first dive, never a sibling) means concurrent module agents share NO pager links → no cross-agent `links`-gate races; the only shared file is the landing, which the orchestrator relinks after the fan-out. **A4 `/spec` shipped this way (Senior-Writer `a4.progress.md` → 7-way `agile-expert` fan-out → orchestrator relink+gate+sync): 29 pages 29/29 PASS, all agents resolved `agile-expert` (no fallback), no-code mandate held (every `pre.code` = spec/stories markdown, never Elixir; `Portal.Engine`/`%Portal.Error{}` only inside the verbatim master-invariant quote).** **REFINEMENT-PASS pattern (2026-06-05, user-directed; the supervised-Senior-Writer loop applied to a CORRECTIVE pass, page-granular):** when a built chapter needs a cross-cutting content fix, spawn ONE Senior Writer to author a self-contained `## Refinement Pass` section INSIDE `a<N>.progress.md` (the embedded brief — defects, corrected ground truth, a per-PAGE scope table, the verbatim component code, the gate rule, do-no-harm notes), then fan out **one agile-expert per PAGE that needs editing** (NOT per module), each reading only that section + its one page; orchestrator verifies + syncs. A4's pass fixed two defects: (1) a retired-ROUTE citation — `GET /courses/:user_id` retired into the protected `/my/courses` at F6.5, BUT the facade FUNCTION `Portal.courses_of/1` is REAL/as-built (it is what `/my/courses` calls) → re-ground the route, KEEP the function (route≠function; an earlier summary wrongly called the function stale); a surviving `/courses/:user_id` is allowed ONLY inside an explicit draft-frame/reconcile note; (2) bare-filename spec citations (`f6.1.stories.md`) → an inline **`.specref` citation chip** = the build-stamp affordance applied to a citation (branded id `sr-<section>-<rung>`, click-to-expand tooltip = short desc + link) → a new **spec-ladder viewer `/spec/specimens`** (git-iteration slider over the shipped F6.1→F6.6 ladder). **CRITICAL chip gate rule:** the `links` gate exact-matches `href` and does NOT strip a `#fragment`, so a chip MUST use a **bare-route href + `data-sr-hash="f6-N"`** (JS appends `#f6-N` on click) — `href=".../specimens#f6-1"` FAILS `links`. **Do-no-harm judgment that made the pass correct (not mechanical):** chip ONLY true dangling PROSE citations; LEAVE `f6.N.*` that are SVG/figure labels, `.geo-readout` output, `pre.code` lines, or the page's illustrated subject (several pages correctly got 0 chips; chip tally varied 0–12/module by real need). `workshop` cites F5.1 (the engine rung, BELOW the F6 viewer ladder) → named reference + `/elixir` cross-link, NO viewer chip (no `#f5-1` stop). A5 `/brief` is the frontier (triad seeded; needs its landing first). Live completion is tracked in `aaw.progress.md` (the new dashboard). **PER-PAGE FULL-MODULE-BUILD pattern (2026-06-05, user-directed: "senior writer → brief,narrative → prompts,directives → spawn agents one per page of complete set of remaining modules"):** to build ALL of a chapter's remaining modules at once, ONE Senior Writer authors a single `a<N>.progress.md` embedded brief with **32 per-page specs** (Section C: per page — route, file path, md path, title/meta, route-tag, eyebrow, exact pager, the worked F6 artifact + the exact ids to quote, both interactives' pure-fn signatures + readout strings, the bridge/take, the `/elixir` cross-link, the 3 Sources) + Section D shared directives + Section E gate cmd + a REVERSE verification (A5.8→A5.1, each module's predecessor-must-leave-true sentence — caught + closed one real gap: the first-two-stories plan belongs in A5.4 not A5.3). Then fan out **ONE agent per PAGE** (hub + dives independent — the model-pager loop is self-contained per module, so no cross-agent races; each agent reads ONLY its Section-C block + Sections C/D/E). Verify ADVERSARIALLY before trusting reports: authoritative re-gate, the invented-API grep (`Portal.X` minus `Portal.ID` — but a token is OK if it's verbatim in THAT page's grounding file, e.g. `Portal.Learning` from f5.2), clamp-spacing, live 200s, `/elixir`-link presence. **WAVE SIZING: 8 concurrent ~140k-token agents tripped a server-side RATE LIMIT; 4-agent waves (one module each) stayed under it.** **MD-FIRST CHECKPOINT = crash resilience (the big lesson):** transient `socket closed`/`rate limited` errors hit agents at the REPORT boundary (`subagent_tokens:0`, `tool_uses:11–18`) — but because every agent writes its `.md` source-of-record BEFORE the HTML, the interrupted agents left COMPLETE md drafts (lead, worked example, both interactives specced, bridge, sources) and only the HTML was missing. Recover by spawning build-from-md agents (read the existing `content/.../<page>.md` → assemble HTML faithfully, no re-derivation) — faster + zero content loss. Two A5 waves (A5.6 dives, all of A5.7) recovered this way. **The `/elixir` CROSS-LINK MANDATE (user's standing ask): every page that mentions a Portal spec/feature/story/roadmap rung cross-links the matching REAL `/elixir/phoenix/<module>` route — pre-verify the route table resolves (200) BEFORE the fan-out (the scheme is semantic dir names `lifecycle`/`routing`/`ecto`/`contexts`/`heex`/`liveview`/`pubsub`/`deployment`, NOT `f6-NN` slugs); a 404 cross-link silently fails the `links` gate.** **CROSS-CHAPTER LANDINGS batch (2026-06-05, user-directed via `/agile-write`): build several chapter LANDINGS at once, each with a why/what/how orientation triptych (the A3 "landing + 3 orientation dives" shape — orientation dives are loose `<chapter>/{why,what,how}.html`, NOT modules; counted separately in the dashboard like A3's "3 dives").** The runbook the user specified: ONE Senior Writer prepares the WHOLE-course-end narrative into a shared embedded brief (`landings.progress.md`) → runs a **REVERSE verification (A7→A6→A5→A4): confirm each chapter's `why` echoes what its predecessor leaves unfinished** (the "path behind") → then fan out **per-CHAPTER** writers (one agent builds its landing + 3 dives = 4 coupled pages; NOT per-page — coupling: shared narrative + pager chain landing→why→what→how, so per-page would race). Cross-chapter landing.prev (`A6←/brief`, `A7←/reliability`) is a sibling-in-batch → one expected transient `links` FAIL until the sibling lands; orchestrator runs the authoritative full-batch gate at the end. **No-triad chapters (A6/A7 had NO `a<N>.*` spec): ground ONLY on `aaw.roadmap.md` at chapter altitude, write a deferral `.note` (NOT invented module cards) — the no-invent guard's highest-risk case, and it held (grep: 0 fabricated module cards).** **API-FAILURE RECOVERY lesson:** the 3 chapter-agents all died with `403/socket` errors (`subagent_tokens:0` but `tool_uses:24–28`, ~25min each) — they had done the work but never reported. The right move was NOT a blind retry: inspect ON-DISK state (11/12 pages built + gating; the 12th had its `content/.../how.md` source already drafted), then recover ONLY the one missing page (`portal/how.html`) from its md + a gating sibling as the model. Don't trust agent reports OR their absence — verify the filesystem.

**Why:** Unlike `/elixir` (URL==filesystem via serveDirTree), this course is authored against an *intended* `/course/...` scheme the server lagged; deriving routes from the filesystem (`/agile-agent-workflow/...`) was a corrected mistake. Relates to [[course-nav-prose-no-redundant-status]], [[elixir-content-fanout-drift]], [[elixir-batch-presentational-drift]], [[jonnify-cms-toolchain]].

## Archived index line (2026-06-12, index compaction)

/course/agile-agent-workflow route + 3-level A<chapter>.<module>.<subpage> structure (chapter landing → module hub → ≥3 subpages, "dives into" like elixir; A0 consolidated /intro→/what; hub pager=chapter+own-first-dive); home page = route manifest (intentional unbuilt forward-links fail links gate by design); refs mandate (--require-refs, opt-in 10th gate); cms needs explicit mount=dir + a0=what,a1=why,a2=decomposition aliases; agile-expert subagent + /agile-write command + docs/.../CLAUDE.md author guide + operator.md runbook (new defs not spawnable until session reload → try agile-expert first; fan-out agents over-sync the shared TOC → keep view-sync orchestrator-only); Parts I–V COMPLETE (A0–A5 all built; A3+A4 spec-first via supervised Senior-Writer-then-fanout + embedded a<N>.progress.md narrative; A4=29pp 29/29 PASS, no-code mandate=pre.code is spec/stories md never Elixir); A5 /brief = all 8 modules A5.1–A5.8 built (32pp) via PER-PAGE fan-out from a5.progress.md (32 per-page specs + reverse-verify), grounded on real f6.1/f6.6/f6.7/f5.2 briefs w/ /elixir cross-link per page; md-first checkpoint = crash-recovery (socket/rate-limit drops at report boundary → rebuild HTML from surviving md; 4-agent waves stay under rate limit). Frontier = A6 (landing+orientation built, needs a6.* triad). NEW course-level instrument specs/aaw.progress.md = live completion DASHBOARD (38/47=81%) (per-rung ✓/◐/○ + 24-cell ANSI bar █░ + %, recounted per rung-close; ≠ the per-chapter a<N>.progress.md build narratives)
