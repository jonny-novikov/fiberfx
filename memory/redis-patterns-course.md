---
name: redis-patterns-course
description: "/redis-patterns course — spec system + staged build; R0+R1+R2 COMPLETE; EchoMQ grounding = iconic Elixir echo/apps/echomq + door to the real /echomq course (never Go-port-as-EchoMQ / 'built next')"
metadata: 
  node_type: memory
  type: project
  originSessionId: 73b81fc7-ffef-4d6d-ac38-349cafb4dda9
---

A new jonnify course **redis-patterns** (folder-routed `/redis-patterns`, course letter **R**). Teaches the 30 Redis design patterns **applied** — each grounded in how the real **EchoMQ-in-Portal** uses it ("the Lua scripts ARE the protocol"; EchoMQ = the candidate for Portal's reserved F7–F9 multi-runtime layer per `docs/elixir/specs/portal.roadmap.md`). Opens the door to a separate, dedicated **EchoMQ course** built next with the same toolkit.

**Build is STAGED (each separately approved). Plan file: `~/.claude/plans/curried-foraging-scott.md`.**

**Stage 1 — the SPEC SYSTEM — DONE + verified (design-only, NO pages).** Extends the AAW patterns, shaped like `docs/elixir/specs/`. The 11 authored docs:
- `docs/redis-patterns/redis-patterns.toc.md` — the chapter→module→dive TOC (~46 modules, ~140 dives).
- `docs/redis-patterns/redis-patterns.roadmap.md` — program roadmap + **the canonical pattern→EchoMQ grounding map** (the 5-agent research; don't re-derive) + the chapter-dependency DAG + milestones.
- `docs/redis-patterns/specs/redis-patterns.md` — the **"specs → chapter authoring" contract** (adapted from `specs.approach.md`) + the `<chapter>.md` template + the chapter map.
- `docs/redis-patterns/specs/<chapter>/<chapter>.md` ×8 — per-chapter index/spec (module ladder + grounding + dives + the →door).
- **R0 is DEEPENED to the full per-module triad** (the exemplar; resolves the "deepen?" open decision): `specs/overview/` holds `overview.md` (chapter index) + `r0.md` (chapter roadmap, = the `<chapter>.roadmap.md` equivalent, user named it `r0.md`) + the **R0.1 triad** `r0.1.{md,stories,llms}` — a PAGE-AUTHORING adaptation of the elixir `fN.M` triad (spec=what pages/why w/ D#/INV#; stories=who reads, Connextra+G/W/T+encodes+Coverage; llms=the brief an authoring agent runs w/ the cms gate as proof, R#[US:]/AS#[implements]). The deep-spec layout STARTS at R0; R1–R8 hold the chapter index today and deepen to triads as authored. **R0.1 = the course's two DIRECTORY pages** (amended): D1 = HOME `html/redis-patterns/index.html` (route `/redis-patterns`), model = **`elixir/index.html`** (hero + "how to read this" + **THE MAP** = the full chapter→module directory: a `.chap` section per chapter over a `.mods` grid of its MODULE cards — same grid as `/elixir/course`, so the home carries the whole contents; **NO separate `/overview/course`**); D2 = the OVERVIEW LANDING `html/redis-patterns/overview/index.html` (route `/redis-patterns/overview`) = the R0 chapter landing: R0 module cards (R0.2/R0.3) + an **"Up next" grid** (`.upnext`, reusing the home's chapter cards, the chapters AFTER the current one). Shared card classes `.chap/.chap-head/.cid/.mods/.mod/.num/.pill/.t/.o/.chap-link/.c-one` (copy verbatim from `elixir/index.html` + `elixir/course/index.html`). HOME + every CHAPTER LANDING are ROUTE MANIFESTS (forward-link unbuilt → `soon` pill; `links` FAIL EXPECTED, like the /elixir & AAW homes). **CHAPTER-LANDING ANATOMY** (now in the contract): overview → why & when (use cases) → what (module cards) → how to apply → the **EchoMQ exemplar workshop module** + the "Up next" grid; applies to R1–R8 (R0's landing is the orientation variant). **Per-module spec = a QUAD**: `<mod>.md` (spec) + `.stories.md` + `.llms.md` (brief) + `.prompt.md` (runnable prompt, extracted into its own file). **R0.1 + R0.2 quads authored** (R0.2 = "Redis under Portal" = hub + 3 dives the-facade-seam/two-roles/reserved-tier, grounded on the REAL Portal facade + the reserved F7–F9 tier; a TEACHING module so links MUST resolve, NOT a manifest). R0.3 (patterns become protocol) follows the same shape. Spec-doc voice = impersonal, NO first person (stricter than elixir specs.approach which allows "we'll know").

**Chapter slugs are LOCKED to the 8 pre-created `specs/` folders** (the user created them empty): `caching` (R1) · `coordination` (R2) · `queues` (R3) · `time-delay-priority` (R4) · `streams-events` (R5) · `flow-control` (R6) · `data-modeling` (R7) · `production-operations` (R8). **R0 = Overview** = the course landing (no `specs/` folder; lives in toc/roadmap). Arc = pattern families sequenced along the EchoMQ build (coordination → queues → time → streams → flow → ops), so the course doubles as a guided build of Portal's Redis tier.

**The grounding boundary (the master rule, in the contract + roadmap):** a redis-patterns module teaches a transferable Redis technique proven by ONE tight REAL excerpt — a real EchoMQ key (`bull:{queue}:wait`), command (`RPOPLPUSH`), Lua script (`moveToActive`/`ExtendLock`/`moveToFinished`), or Go fn (`apps/echomq-go/pkg/echomq/scripts/scripts.go`) — OR a Portal surface where the pattern is not an EchoMQ one. EchoMQ implementation depth → the dedicated EchoMQ course. NOT EchoMQ-grounded (ground on Portal/standalone): the cache family + the modeling family (vectors/geo/bitmap/session). `redlock` + `probabilistic-data-structures` are taught as **contrasts**. **No-invent**: never fabricate an EchoMQ module, Lua script, Redis command, or Portal API. Each of the 30 patterns has ONE primary module; 5 recur as labelled *applied* examples (`atomic-updates`, `delayed-queue`, `lexicographic-sorted-sets`, `redis-as-primary-database`, `streams-event-sourcing`).

**Stage 2 (later) — SCAFFOLD:** toolkit at `docs/redis-patterns/toolkit/` (fork the elixir kit, INVERT the source: parse `content/llms.txt` + `.md.txt` → course graph; `build_page.py` copies the design system from a model AAW page; gating delegated to `apps/jonnify-cms`, no re-impl). 5-file server wiring (folder-routed like `/logic`): `main.go` (`redisPatternsDir` + `REDIS_PATTERNS_DIR` env + 2 `app.Get` + log), `Dockerfile` (1 COPY), `Makefile` (var + 2 recipe exports), **`cmd/sitemap/main.go` add `{"redis-patterns",htmlRoot,"/redis-patterns"}` to the hardcoded `folderRouted` slice (REQUIRED)**. e2e: permanent `apps/e2e/tests/redis-patterns-*.spec.ts` (computed-style, modeled on `elixir-hero.spec.ts`). Entry pages: `html/redis-patterns/index.html` hub + `overview/index.html` + `overview/course.html` (re-themed from the upstream `content/course.html`). cms check: `--routes-from /redis-patterns=html/redis-patterns --chapter-alias r1=caching,…,r8=production-operations --require-refs`.

**Stage 3 — AUTHOR — UNDERWAY (2026-06-06).** Pages are being built. **Server route `/redis-patterns` is ALREADY WIRED in `main.go`** (`redisPatternsDir`/`REDIS_PATTERNS_DIR` + `app.Get` at lines ~467-468 + startup log) — so cms gates + live serve work (after a `make restart`; agents hit stale-instance 404s, harmless). **Built+gated:** R0.1 (home `index.html` = full R1–R8 map + overview landing `overview/index.html` w/ the net-new `.upnext` chapter grid), R0.2 (Redis under Portal hub+3 dives). **R0.3 specced** (full quad `r0.3.{md,stories,llms,prompt}` grounded in the real EchoMQ 4-layer model `docs/echomq/`: L1 data-layer + L2's 53 Lua scripts = the immutable core) — pages pending its agent. **R1 Caching building** via the supervised-spec-first + WAVED fan-out: I authored the chapter landing + 7 per-module specs `specs/caching/r1.0N.md`, then fan out one `redis-expert` per module (hub+3 dives), **rate-safe waves of ≤4**, each bootstrapping from a BUILT redis sibling (R0.2 → then R1.01). W1 (R1.01-04) done+verified+relinked; W2 (R1.05-07) in build. **Live ANSI dashboard = `docs/redis-patterns/redis-patterns.progress.md`** (per-chapter cell bar + COURSE bar + build log + waves table; recount per wave). **Content↔chapter mapping = `docs/redis-patterns/redis-patterns.content-map.md`** (all 30 source files → their primary/applied modules + per-page techniques mined from `content/<section>/<pattern>.md.txt`); agents MINE their mapped content file (redis-expert #6). **Served `llms.txt` REQUIRED in each `html/redis-patterns/` subfolder** (course root + per-chapter + per-module maps, llmstxt.org format, served text/plain by serveDirTree's co-located branch); hand-authored now, a `toolkit/gen_llms.py` generator (cmd/elixir-llms pattern) is the Stage-2 automation. GOTCHAS: (1) the cms **`links` gate does NOT validate cross-mount `/elixir` links** — verify them on disk manually (every R1 agent's /elixir cross-links checked, all real); (2) the **voice rule extends to software components** (a cache/caller/surface must not "see" — fix to receives/touches/waits-on); (3) wave siblings being built concurrently must NOT be linked in each other's "Related" (only-resolving routes), else links-gate FAIL at gate time.

**FEEDBACK (2026-06-06, cache-aside probe) — 3 BINDING CRAFT RULES** now encoded in skill §5a + redis-expert #1/#6 + redis-write Step2/3 + `toolkit/README.md`: (1) **SOURCE IS THE CONTENT SPINE** — the module HUB LEADS with the author source's (`content/<section>/<pattern>.md.txt`) opening summary line as its `.lede`, then follows the source's `##` sections IN ORDER (cache-aside: How It Works→Redis Commands Used→Advantages→Staleness Problem→Mitigating Staleness→When to Use→When to Avoid); jonnify framing/interactives/.bridge/grounding layer ON TOP, never replacing or preceding the source content (the W1/W2 pages were agent-framed with the source mined only for commands = the bug). (2) **References = 2-COLUMN** (`.refs{display:grid;grid-template-columns:1fr 1fr}` + two child `<div>`s Sources|Related — the AAW-derived dive model rendered 1-col stacked). (3) **md-FIRST route-mirror** — author `docs/redis-patterns/markdown/<route>.md` (served route minus `/redis-patterns/` + `.md`) as the source-of-record the HTML reflects; exemplar of depth = user-authored `markdown/caching/branded-cache-aside.md` (multi-runtime Redix/ioredis tabs, stale-set race, branded Snowflake keys). PROVEN on three surfaces: `caching/cache-aside` HUB (hand, re-rooted to source spine) + `caching/cache-aside/miss-fill` DIVE (hand; dives were already aspect-grounded → needed only 2-col refs + md) + `caching/cache-stampede-prevention` MODULE (DELEGATED to a redis-expert — autonomously re-rooted the hub to the source's 5-section spine + ported the comparison table, all 4 pages 2-col refs + md, source `<h2>` order EXACT, 4/4 PASS = **delegation validates the rollout**). SURFACE-AWARE rule set: HUBS get all 3 rules; DIVES get refs+md (content usually already aspect-faithful); LANDINGS get only the md-mirror (already 2-col refs; no single pattern source — the chapter arc is the structure; landing md e.g. `markdown/coordination.md`). **R2 Coordination LANDING built** (`coordination/index.html`, first →EchoMQ chapter; modules R2.01-06 planned). **`/redis-reconcile` ROLLED OUT (2026-06-07): cache-aside module (first run) → then the WHOLE R1 chapter** — all 7 R1 modules + the landing now conformant: write-through/write-behind/client-side-caching/session-management **hub-rerooted** to their source `##` order, workshop **layout-only** (capstone = no single source, hub NOT re-rooted), R1 landing md authored; **28/28 module pages PASS, md bijection 29 served ↔ 29 route-mirror mds** (+ the intentional `branded-cache-aside.md` depth exemplar). **WAVE-SIZE CEILING for HEAVY edit/reconcile redis-expert agents = ≤2 concurrent, NOT the ≤4 the dashboard claims** — a 4-concurrent wave dropped 2 agents on rate-pressure socket closes (~140s in, vs ~650s for the survivors); RECOVER by re-running the dropped modules as a 2-agent wave (partial work survives in the tree → audit on-disk state, tell the resume agent what's already done so it finishes only the remainder). VOICE GREP must include `observes?/notices?/tracks?` (caught "application observes" ×2 — prose + a JS readout string — that BOTH the cms voice gate and the authoring agent missed). Then **R0 reconciled (2026-06-07)** — all layout-only (R0.2/R0.3 are ORIENTATION modules, no `content/*.md.txt` spine → NO hub re-root, like the workshop; home `markdown/index.md` + overview `markdown/overview.md` landing mds authored by orchestrator). **`/redis-reconcile` ROLLOUT NOW COMPLETE: the whole BUILT course (R0 10 + R1 29 + R2 landing 1 = 40 pages) is conformant, with a clean course-wide md bijection (40 served ↔ 40 route-mirror mds + the branded-cache-aside.md exemplar). Remaining work is AUTHORING new modules (R2.01+ via /redis-write), NOT reconciling.** TWO durable learnings from R0: (a) **PRONOUN-subject voice slips** evade the noun-keyed grep — a redis-expert caught the EXEMPLAR `caching/cache-aside/miss-fill` shipping "**it** never sees the hit-or-miss branch" (it=the web app); fixed → "never handles", and the verify set now includes a pronoun spot-check `\b(it|they) (sees?|knows?|wants?|believes?|observes?|notices?)\b` (course-wide CLEAN). (b) **hub md path convention** = `markdown/<chapter>/<module>.md` (BESIDE the dive dir), NEVER `markdown/<chapter>/<module>/index.md` — the stampede hub md had drifted to the `index.md` form and was moved. Socket-drop note refined: even a 2-agent wave dropped 1 (R0.2); LONE agents have held every time → for the riskiest/last module, prefer a single agent.

**R2 AUTHORING (2026-06-07): R2.01–R2.04 BUILT (4/6); next gap R2.05 hash-tag co-location, then R2.06 workshop.** Each module = hub + 3 dives = 4 pages, via `/redis-write` per-page fan-out from the senior-writer narrative `specs/coordination/r2.progress.md` + per-module prompt packs `r2.NN.prompt.md`. **R2.03 + R2.04 authored IN PARALLEL** (user: "Author R2.03 and R2.04 in parallel") as **4 waves of 2 concurrent redis-expert agents** — both module HUBS first (each is its module's dive-pager target + the dives' design-system model), then the three dive-pairs interleaved cross-module; zero socket drops across all 8 (the ≤2-concurrent-heavy ceiling held). The dive pager chain (hub→d1→d2→d3→hub) means each dive's forward-sibling dangles transiently at its own gate — EXPECTED; the orchestrator's authoritative re-gate after ALL 8 land clears every `links` FAIL (8/8 PASS confirmed; 16/16 inline scripts parse). Grounding: **R2.03 redlock = CONTRAST** — verified **ZERO** real `redlock` in `echo/apps/echomq` AND `apps/echomq-go` (the lone Go grep hit is the substring inside the test name `TestStalled_RequeuesExpiredLock`), so the module grounds in the *absence* + the single-Redis lease (`Keys.lock/2` + `LockManager` one-timer + `moveStalledJobsToWait` reclaim = why a queue needs no majority-of-N). **R2.04 cross-shard = PREVENTION** — `apps/echomq-go/pkg/echomq/cluster.go` `GetClusterSlot` = CRC16 % 16384 + `worker_impl.go:413` `validateClusterCompatibility` (builds the 6 sample keys Wait/Active/Prioritized/Meta/Job/Lock → `ValidateHashTags` → the real warning *"Multi-key Lua scripts may fail with CROSSSLOT errors in cluster mode."* at `:432`); detection (token/version/commit-marker) is the FALLBACK when keys cannot share a slot. **NEW GOTCHA — manifest drift the cms gates cannot catch:** the HOME map (`html/redis-patterns/index.html`) tracks PER-MODULE build pills too (R1.01–07 show `built` there), so when flipping `soon→built` relink BOTH manifests (home + chapter landing) AND audit the home's sibling pills for staleness — a prior wave had left R2.01/R2.02 `soon` in the home while the chapter landing had them `built` (repaired this turn → home now 12 built). R2.04's one trap: the cross-shard source's tail links hash-tag-colocation (R2.05, unbuilt) → name it in prose, link the resolving chapter landing `/redis-patterns/coordination`, never the unbuilt module route.

**Authoring craft = the `.claude/skills/redis-course-writer/` skill** (`SKILL.md` + `references/course-map.md`; sibling of `agile-course-writer`; registered). Defers the shared design craft (tokens/visualization/voice/page-anatomy) to `.claude/skills/elixir-technical-writer/references/`; documents only what's different (the spec system, the EchoMQ-grounding boundary + no-invent, the page surfaces + chapter-landing anatomy, the `cms check --routes-from /redis-patterns=html/redis-patterns --chapter-alias r1=caching,…` gate command). Sources of truth = the spec system + `jonnify-cms`. **Fan-out author = the `.claude/agents/redis-expert.md` agent def** (sibling of agile-expert; `tools: Read,Write,Edit,Bash,Grep,Glob,Skill`, opus; loads the redis-course-writer skill, builds ONLY from the page spec, enforces the EchoMQ-grounding + no-invent rule, gates to PASS, never git, doesn't relink the landing). GOTCHA (same as agile-expert): a newly-created agent def is NOT spawnable via `subagent_type` until a session reload → until then fan out with `general-purpose` carrying the redis-expert brief (it's self-contained). **redis-expert ACTIVATED + verified post-reload** (clean read-only R0 review, verdict READY). **Fan-out command = `/redis-write`** (`.claude/commands/redis-write.md`, sibling of `/agile-write`; per-module redis-expert fan-out + orchestrator-only manifest relink + TOC sync). **Reconcile command = `/redis-reconcile`** (`.claude/commands/redis-reconcile.md`, the RETROFIT/conformance engine — sibling of redis-write but for ALREADY-BUILT pages: audits a page/module/chapter against the established pattern [the 3 binding rules + the gate-invisible conventions, encoded as a checklist] then fans out redis-expert REFINERS [refine-don't-rewrite, surface-aware: hubs re-root to source spine, dives refs+md, landings md] + adversarially verifies with the proven greps. Commands load same-session = invokable immediately, unlike agent defs).

**DESIGN-SYSTEM SOURCE = a built REDIS page of the same surface (the course is SELF-CONTAINED — do NOT couple it to other courses' pages as the canonical model).** Another jonnify course (elixir/AAW) is a ONE-TIME BOOTSTRAP for the *first* page of a surface only: lift the shared `<head>`…`</style>` + header + footer + scripts shell, then REWRITE the route-tag/nav/footer to redis and carry over NO other-course content. R0 is built first so its home/hub/dive become the redis models; after R0 every page copies from a redis page. (User caught this cross-course leak in redis-write.md; fixed in `redis-write.md` Step 0.4 + Step 2, `redis-expert.md` non-negotiable #2, and `redis-course-writer/SKILL.md` §3.)

**GROUNDING SOURCE — "OUR CONTEXT IS ELIXIR" (user directive 2026-06-07, MEMORIZE).** The authoritative EchoMQ grounding for this course is the **Elixir** app **`echo/apps/echomq`** (inside the echo umbrella; governed by `echo/CLAUDE.md`; READ-ONLY), NOT (only) the Go port `apps/echomq-go`. It is far richer and is the real "EchoMQ-in-Portal" (Portal is Elixir). What's there (all VERIFIED real): **`priv/scripts/*.lua`** = the ~53 REAL Lua protocol scripts as files, named `scriptName-numberOfKeys.lua` (`moveToActive-11.lua`, `moveToFinished-14.lua`, `extendLock-2.lua`, `extendLocks-1.lua`, `releaseLock-1.lua`, `moveStalledJobsToWait-8.lua`, `addStandardJob-9.lua`, `addPrioritizedJob-9.lua`, `promote-9.lua`, `retryJob-11.lua`, …); **`lib/echomq/scripts.ex`** = `EchoMQ.Scripts` — `execute_raw/4` runs a script via **EVALSHA → on NOSCRIPT → EVAL** (the SHA1 script-cache pattern); `move_to_active/4` (11 keys, one atomic move), `move_to_finished/7` (14 keys), `execute_transaction/2` (**MULTI/EXEC**), `add_standard_jobs_pipelined/3` ("all jobs added or none"), `extend_lock/5`, `extend_locks/5` (renew MANY in one call), `release_lock/4`; **`lib/echomq/keys.ex`** = `EchoMQ.Keys` — `bull:#{name}:*` key builder; `lock/2` = `bull:q:JOBID:lock`; (the `{queue}` hash-tag for cluster colocation is explicit in the Go port `cluster.go`+`keys.go`); **`lib/echomq/lock_manager.ex`** = `EchoMQ.LockManager` — a GenServer with **ONE timer** (fires every `lock_renew_time/2`) that batch-extends all tracked jobs' locks via `Scripts.extend_locks/5` ("instead of a timer per job, a single timer" = the chapter-spec's "one timer per worker"); lost-lock → `on_lock_renewal_failed` + untrack. `extendLock-2.lua` body = `if GET KEYS[1]==ARGV[1] then SET KEYS[1] ARGV[1] PX ARGV[2]; SREM stalled` (token-checked lease renewal). The Go port = a SIBLING RUNTIME / contrast (R2.03 redlock-vs-single-lease; the polyglot lock-renewal contrast: Elixir LockManager one-timer-for-N vs Go per-job heartbeat). Verify in `echo/apps/echomq` before citing; never invent.

**COURSE BOUNDARY — "DOOR, NOT DEPTH" (user directive 2026-06-07, MEMORIZE).** Redis Patterns gives only a **"door"** to EchoMQ: ONE tight real excerpt per module as *proof the pattern is real*, plus a `.bridge` + a `.door` forward-pointer that **prepares the learner to understand the "under the hood" of EchoMQ** — it does NOT exhaustively teach the Lua scripts. The **dedicated EchoMQ course** (built next, same toolkit, parser pointed at `echo/apps/echomq`) is where **EVERY single Lua script in `echo/apps/echomq/priv/scripts` is covered** (the full include-graph, EVALSHA/NOSCRIPT dispatch, the 4-layer protocol). So in R2 (and every →EchoMQ chapter): teach the transferable Redis pattern fully, cite ONE verified EchoMQ excerpt as the bridge, and door-forward the depth. Do not pile multiple scripts onto a redis-patterns page.

**R2.05+R2.06 BUILT + the ECHOMQ-DOOR RECONCILE (2026-06-07, operator-triggered `/reconcile` mid-build, MEMORIZE).** R2 is now **COMPLETE (6/6)**: R2.05 hash-tag-colocation (hub + `the-tag-mechanic`/`crossslot-prevention`/`cluster-auto-detect`) + R2.06 workshop (capstone, no dives). The first R2.05 drafts DRIFTED on the EchoMQ framing (the *second* such drift — R2.04 over-claimed earlier): they grounded the **Go port AS EchoMQ** ("in EchoMQ the Go port computes the slot") and doored "the dedicated EchoMQ course, **built next** with this toolkit". The operator halted the fan-out and ran `/reconcile`. THREE binding corrections, now the standing rule for **every →EchoMQ page**: (1) **The exemplar is the ICONIC ELIXIR app `echo/apps/echomq`** — for R2.05 that's `moveToActive-11.lua` via `EchoMQ.Scripts.move_to_active/4` (eleven `bull:{queue}:*` keys, one EVAL). Foreground it; the Go port is NEVER a stand-in for "EchoMQ". (2) **The Go port `apps/echomq-go` is a LABELLED polyglot contrast** — for cluster colocation (R2.04/R2.05) it is "the **Go runtime** filling the documented **Elixir cluster gap**" (the Elixir app is library-only, `Keys.base/1` returns `bull:my_queue` with NO braces + ships no slot code; the Go `cluster.go` `GetClusterSlot`/`ValidateHashTags`/`CalculateCRC16` % 16384 + `validateClusterCompatibility` add it). The `{queue}` hash-tag form is part of EchoMQ's IMMUTABLE PROTOCOL per **`docs/echomq/echomq.roadmap.md`** (E1.04/E7.01/E8.04 explicitly name the "Elixir cluster gap"). (3) **The EchoMQ course is REAL + DESIGNED, not "built next"** — **`docs/echomq/`** holds a full E0–E8 spec system (`echomq.md`/`echomq.toc.md`/`echomq.roadmap.md` + E0 triad + `content/` corpus), served folder-routed at **`/echomq`** once its build stage ships (html/echomq absent today → `/echomq` 404s). The `→ EchoMQ` door is now a REAL `<a href="/echomq">` forward-link (operator chose LINK over prose; the cms `links` gate flags `/echomq` as dangling **BY DESIGN**, exactly like the home/landing manifests' soon-links — so the hub + cluster-auto-detect + workshop carry that ONE intended `links` FAIL, while non-door pages stay fully clean). The door is BIDIRECTIONAL (R→E `/echomq`, E→R back `/redis-patterns/overview`). Drop "built next"/"has none yet" everywhere. Remediation was **operator-scoped to R2.05+R2.06 ONLY**: hub + `the-tag-mechanic` re-framed in place; `cluster-auto-detect` + `workshop` built correct from the start; both manifests (home + landing) + TOC + chapter `llms.txt` + the progress dashboard relinked. **OPEN TAIL (deliberately left, per the R2.05+R2.06-only scope): the course-wide "built next" door wording on ~10 OTHER built pages** (the canonical `overview/patterns-become-protocol/the-door-to-echomq`, `atomic-updates`, `redlock`, `cross-shard-consistency`, etc.) — a candidate future course-wide door reconcile (link to `/echomq` + drop "built next" + name the designed E0–E8 course).

**R3 STARTED + the LIVING EchoMQ course CROSS-LINK MAP (2026-06-08, MEMORIZE — changes how EVERY `→ EchoMQ` door is authored).** The dedicated EchoMQ course is now a **living, agile-spec course** with its own toolchain (`echomq-course-writer` skill + `echomq-expert` agent + `/echomq-write` + `/echomq-reconcile`). It has a NEW spec system **`docs/echomq/specs/emq/`** (rungs `emq.1`–`emq.6`, each a triad, tracking the real EMQ extension build of `echo/apps/echomq`) + an updated **`docs/echomq/echomq.toc.md`** that CROSS-LINKS redis-patterns. **The cross-link map** (echomq.toc.md "The door from redis-patterns"): **R0.3→E1 · R2→E1/E3 · R3→E2/E5/E6 · R4/R5→E6/E7 · R6→E4/E5 · R8→E8.** **E-chapter routes:** E1 `/echomq/protocol` · E2 `/echomq/core` · E3 `/echomq/substrate` (⇄ emq.1) · E4 `/echomq/groups` (⇄ emq.2) · E5 `/echomq/batches` (⇄ emq.3) · E6 `/echomq/lifecycle` (⇄ emq.4) · E7 `/echomq/cache` (⇄ emq.5) · E8 `/echomq/production` (⇄ emq.6). The `/echomq*` pages are **NOT served yet** (html/echomq absent → 404); a redis-patterns `→ EchoMQ` door should be a REAL `<a href="/echomq/<chapter>">` forward-door cross-linking the SPECIFIC E-chapter(s) per the map (cross-mount; the cms `links` gate flags them dangling BY DESIGN, like a manifest's soon-links — so a door page carries that intended `links` FAIL). KILLER cross-link: R2.05 hash-tag colocation ⇄ **E3 substrate (emq.1)** — `EchoMQ.Ext.Keys` builds `bull:{queue}:emq:…` keys hash-tag-safe (share the queue's slot) so the EMQ extension stays atomic BESIDE the core (emq.1-D1 + INV4 "the atomicity floor"). **R3 BUILT as a STRATEGIC entry (NOT the 6 granular R3.01–R3.06 modules):** the chapter landing `/redis-patterns/queues` (a route manifest, forward-looking — surveys the arc R3→R8) + **3 course-direction dives** — `the-reliable-queue` (`RPOPLPUSH wait→active` via `moveToActive-11.lua` + `de:{id}` via `EchoMQ.Keys.dedup/2` + `moveStalledJobsToWait-8.lua`) · `states-as-locations` (`moveToFinished-14.lua` one EVALSHA via `EchoMQ.Scripts.execute_raw/4` + `bull:{queue}:marker`/`BZPOPMIN`) · `the-road-ahead` (ORIENTATION dive — the arc R3→R8 + the door, grounded in the cross-link map, NOT a Lua script: 0 script-citations + 7 `/echomq` links is the correct fingerprint). Doors cross-link E2/E5/E6. All 4 gated 9/10 (only `/echomq*` forward-doors + the landing's R4–R8 up-next FAIL `links`, by design). Landing's 3 dive cards relinked `soon→built`; TOC + `queues/llms.txt` + dashboard synced. **Structure note:** a "chapter landing hosting 3 dives directly" is NON-STANDARD (chapter→module→dive convention puts dives under module hubs) but was operator-chosen for this strategic build; the granular R3.01–R3.06 modules are now ALL BUILT (R3 COMPLETE — see the two build entries below). **OPEN TAILS:** (1) **R8.07 capstone** (course summary + invite to `/echomq`) DEFERRED to a follow-up batch. (2) **R2.05's doors STILL point at the GENERIC `/echomq`** (from the earlier reconcile), NOT the specific **E1 `/echomq/protocol` + E3 `/echomq/substrate`** cross-links + the emq.1-substrate detail — the operator started that refinement, then declared "author R2.05 [was] not correct" and redirected to R3, so it is unfinished. (3) the course-wide "built next" door wording on the OTHER built pages (the canonical `the-door-to-echomq`, etc.) is still un-reconciled.

**R3.01–R3.03 BUILT via SENIOR-NARRATIVE-then-PERSISTENT-PROMPT-PACK fan-out (2026-06-08, 12 pages — the pattern the operator named, reusable).** Workflow: a **senior redis-expert** (1) EXPLORES + VERIFIES the grounding in real code, (2) authors the build NARRATIVE `specs/queues/r3.progress.md`, (3) injects three **persistent on-disk packs** `specs/queues/r3.01.prompt.md`/`r3.02`/`r3.03` — each a SELF-CONTAINED A+ build brief (routes, exact pager table, crumbs, verified grounding block, source-spine instruction, md-first paths, named hub+dive model pages, the 2 layout rules, the 2-col refs w/ exact vetted URLs, the interactive contract + concrete per-page ideas, voice rules, the gate cmd, no-git/scope). Then **redis-writers fan out per MODULE** (each builds hub+3 dives = 4pp; wave 1 = R3.01+R3.02 concurrent, wave 2 = R3.03 lone; the per-module agent keeps the hub→dive design-copy dependency INSIDE one agent = the right unit). Built: **R3.01 processing-list** (`EchoMQ.Scripts.move_to_active/4`→`moveToActive-11.lua` `RPOPLPUSH wait→active`; dives list-wait-active·lmove-rpoplpush·the-in-flight-list; door→E2), **R3.02 at-least-once** (producer `de:{id}` `EchoMQ.Keys.dedup/2`+`removeDeduplicationKey-1.lua`; idempotent consumers grounded in the REAL `Portal.Engine` enrollment; dives at-least-once-semantics·idempotent-consumers·why-exactly-once-is-a-lie; door→E2/E6), **R3.03 stalled-recovery** (atomic `EchoMQ.Scripts.move_stalled_jobs_to_wait/4`→`moveStalledJobsToWait-8.lua` vs NON-atomic Go `StalledChecker` `checkStalledJobs` in `apps/echomq-go/pkg/echomq/stalled.go` = honest double-recovery-window + client-side `atm`-increment contrast; dives lock-expiry-detection·two-phase-mark-recover·atomic-vs-non-atomic; door→E6/E2). All 12 = 9/10 (only intended `/echomq/*` doors FAIL `links`); no invented surface (only real `Portal.Engine` verified at `echo/apps/portal/lib/portal/engine.ex`); clamps spaced; `data-c` on every select button; 12 route-mirror mds; live 200, `/echomq/core` 404. **TWO REUSABLE LESSONS:** (a) **CALL-SITE beats a drifted COMMENT** — the Lua header of `moveStalledJobsToWait-8.lua` lists KEYS[8]=`event stream` + no `failed`, but the Elixir wrapper `EchoMQ.Scripts.move_stalled_jobs_to_wait/4` (scripts.ex) actually passes `[stalled,wait,active,failed,stalled-check,meta,paused,marker]` — ground in what the WRAPPER passes (the real runtime call), not the stale ported comment; the writer's self-correction (event-stream→failed) was RIGHT. (b) **standardize NOTATION in the PACKS before fan-out, not the output** — orchestrator caught the packs rendering concrete `bull:my_queue:…` while the shipped chapter dives use the `{queue}` placeholder; one SendMessage to the senior fixed all 4 spec files → 12 pages render consistent on first build (cheaper than sweep-fixing output; `{queue}`=queue-name placeholder, distinct from R2.05 cluster hash-tag `{tag}`). Orchestrator-fixed 1 component-voice slip the writer missed ("it assumes the worker died"→"treats the worker as dead", HTML+md parity). Manifests relinked (landing promotes 3 modules to `built` cards; home pills `soon→built`); TOC+llms.txt+dashboard synced (19/53 modules, 36%). Existing on disk: `docs/redis-patterns/README.md` (project rationale, authored), `content/` (30 pattern `.md.txt` in fundamental/community/production + `llms.txt` + upstream `course.html` + a `commands/` mirror to IGNORE), `toolkit/` (empty). Sibling craft: [[agile-course-canonical-route]] (AAW), [[elixir-toolkit-portable-kit]], [[elixir-content-md-tree]]. The user steers the design conversationally and rejects abstract AskUserQuestions in favour of reacting to concrete documents.

**R3.04–R3.06 BUILT → R3 COMPLETE (2026-06-08, 9 pages, the closing reliable-queue modules).** Same senior-narrative-then-persistent-prompt-pack fan-out as R3.01–R3.03, but I played the senior MYSELF (explored + verified all grounding in `echo/apps/echomq` directly, extended `specs/queues/r3.progress.md` build-2, wrote the 3 packs `r3.04/05/06.prompt.md`) — doing the explore-and-verify directly is the senior's whole value-add and avoids a round-trip that re-introduces drift. Then redis-writers fanned out (wave 1 = R3.04 + R3.05 concurrent hub+3-dives each; wave 2 = R3.06 lone, a NO-DIVES capstone). Built: **R3.04 atomic-state-machine** (`EchoMQ.Scripts.move_to_finished/7`→`moveToFinished-14.lua`; the **14 keys in verified order** wait·active·prioritized·events·stalled·limiter·delayed·paused·meta·pc·completed/failed·job·metrics·marker — NO failed-only slot, completed/failed is slot 11; **`fetch_next` ARGV defaults to 1** = finish AND fetch the next in ONE EVALSHA = the centrepiece; the EVALSHA→NOSCRIPT→EVAL fallback is real `EchoMQ.Scripts.execute_raw/4` matching `%Redix.Error{message:"NOSCRIPT"<>_}`; dives states-as-locations·read-decide-write-in-one-evalsha·evalsha-and-noscript; door→E2/E6), **R3.05 blocking-vs-polling** (`EchoMQ.Worker.wait_for_job/2`→`do_wait_for_job/3` both **private** `defp` = the worker's blocking-fetch internals not a public API; `BZPOPMIN` on `bull:{queue}:marker` a **ZSET** "for blocking operations"; wake-up = `addBaseMarkerIfNeeded`→`ZADD marker 0 "0"` in addStandardJob-9/drain-5 = the doorbell; dedicated `EchoMQ.RedisConnection.blocking_connection/1`; Go contrast = `apps/echomq-go/pkg/echomq/worker_impl.go` `time.Sleep` empty-queue poll, labelled the Go runtime; dives the-busy-poll-cost·blocking-pop·the-marker-wake-up; door→E2/E6), **R3.06 workshop** (capstone, no dives like R2.06; a reliable Portal enrollment-job queue assembling R3.01–R3.05; payoff = at-least-once + Portal's already-idempotent enroll = exactly-once-in-effect; door→E2). Model for the workshop = `html/redis-patterns/coordination/workshop/index.html` (R2.06, single page no dives). All 9 = 9/10 (only `/echomq/*` doors FAIL `links`; the workshop also links bare `/echomq` = the EchoMQ course home, matching the shipped R2.06 precedent = also an intended door). Adversarial verify clean: every `Portal.*` real, all EchoMQ Lua/symbols cross-checked in echo, clamps spaced, data-c on every select button (the-busy-poll-cost has 0 buttons by design = slider-driven), 9 route-mirror mds, zero component-voice perceptual-verb hits, live 200, unbuilt R4 404. **KILLER REUSABLE LESSON — the no-invent discipline caught a defect in MY OWN brief:** my packs named `Portal.Learning.enroll/2` (following `echo/CLAUDE.md` §5's table, which my [[echo-data-unify]] memo already flags STALE); the R3.06 writer verified against the BUILT code and refused it — there is **NO `Portal.Learning` module**; the real surface is **`Portal.Enrollment.enroll/2`** with the **`Portal.enroll/2` facade `defdelegate`-ing to `Portal.Enrollment`** (portal.ex). The served page was right; I reconciled the two SPEC docs (spec ← code). Generalized rule: **verify against the code, never against a prose table that claims to describe the code — and the spec/pack is NOT exempt from the same code-truth reconcile the pages get.** Manifests relinked (R3 landing promotes all 6 modules to `built` + drops the in-build note; home pills `soon→built`); `queues/llms.txt` + TOC + dashboard synced (**22/53 modules, 42%, 86 html**). R3 is the FIRST fully-built granular chapter. **Next gap: R4 Time/Delay/Priority.** OPEN TAILS unchanged: R8.07 capstone deferred; R2.05's generic-`/echomq` doors → specific E1/E3 cross-links unfinished; course-wide "built next" door reconcile on ~10 other pages.

**R4 ENTERED (2026-06-08, strategic entry = landing + 3 orientation dives; granular modules R4.01–R4.06 still planned).** Same R3 strategic-entry shape: the chapter **landing** (`/redis-patterns/time-delay-priority`, a route manifest, orchestrator-authored from the R3 landing model with a NEW ZSET-as-clock SVG = a `:delayed` timer wheel + a `:prioritized` priority ladder) + **3 orientation dives hosted directly under it** — **the-sorted-set-as-a-clock** (one ZSET, two readings), **score-as-meaning** (the score IS the semantics), **the-road-ahead** (the arc R4.01→R4.06 + the EchoMQ door). I played the senior MYSELF (verified all grounding in `echo/apps/echomq` before authoring, banked it in `specs/time-delay-priority/r4.progress.md` = the verified-grounding table the dive agents build from, + the door reconcile), then wrote 3 persistent packs `r4.<dive>.prompt.md` and fanned out redis-experts (wave 1 = clock + score, wave 2 = road-ahead lone; ≤2 concurrent held, zero drops). **VERIFIED GROUNDING (read field-by-field, cite verbatim):** `:delayed` score = `(timestamp + delay) × 0x1000` (`addDelayedJob-6.lua` `getDelayedScore`; the ms is shifted left 12 bits so a low-bit discriminator orders within one ms; `getNextDelayedTimestamp` recovers it via `/ 0x1000`) + the `promoteDelayedJobs` **bulk sweep** (`ZRANGEBYSCORE delayed 0 (now+1)*0x1000-1` then `ZREM`); `getPriorityScore` = `priority × 0x100000000 + (INCR pc) % 0x100000000` (a REAL included Lua fn verbatim in SIX scripts — high 32 bits = tier, low 32 = arrival → strict tier + FIFO; read by `ZPOPMIN`); `:repeat` = a **ZSET** (`ZADD repeatKey nextMillis customKey`, upsert via `ZSCORE` — `addRepeatableJob-2.lua` `storeRepeatableJob`); `EchoMQ.Backoff.calculate/4` = `trunc(:math.pow(2, attempt-1) * base)` + `apply_jitter` (the formula OWNER, Elixir) while `retryJob-11.lua` does the reschedule onto `:delayed`. `EchoMQ.Keys.delayed/prioritized/repeat` confirmed real at keys.ex 110/114/184. **SENIOR-PASS CATCHES:** (a) `:repeat` is a ZSET in BEHAVIOUR even though `keys.ex`'s own doc-comment mislabels it a "hash" — the CODE's ZADD/ZSCORE is ground truth, not the comment (an echo-source defect, out of scope to fix); (b) `promoteDelayedJobs` (bulk sweep, an included fn) ≠ `promote-9.lua` (one named job) — cite the sweep for "sweep by score"; (c) the backoff `2^(n-1)` math lives in `EchoMQ.Backoff`, NOT `retryJob` (the spec's shorthand). **DOOR RECONCILE:** the course-level echomq.toc map says coarse "R4/R5 → E6/E7", but E7 = EchoCache (near-cache) is a DISHONEST door for time/priority → wired the honest content fit **E6 · Lifecycle controls** (`/echomq/lifecycle`) + **E4 · Groups** (`/echomq/groups`, whose own header claims `← R4`) + the bare `/echomq` home; recorded the deviation in r4.progress.md (the senior grounds the door honestly, doesn't mechanically copy a coarse pair). **TWO KILLER REUSABLE LESSONS:** (1) **folder-routing makes an `ls <slug>.html` a MISLEADING dangling-link check** — a path is only truly dangling if absent as BOTH `<slug>.html` AND `<slug>/index.html` (the cms `links` gate already encodes this; `R0.2 /…/overview/redis-under-portal` + `/elixir/pragmatic/cqrs` are DIRS that resolve, so they never dangled — only the genuinely-absent `/elixir/pragmatic/genserver-state` did → fixed to the real `/elixir/language/otp/genserver`). (2) **a strategic entry needs NO home-map edit** — the home (`html/redis-patterns/index.html`) tracks per-MODULE pills (all R4.01–R4.06 correctly stay `soon`), and the `Open chapter →` forward-link simply RESOLVES once the landing lands (strictly improves the home's `links` gate, no markup change); do-no-harm = touch nothing on the home. Gate tally: landing FAIL only on intended `/echomq` + R5–R8 forward-links; **the-sorted-set-as-a-clock STATUS: PASS (clean 10/10)**; score-as-meaning + the-road-ahead 9/10 (only the `/echomq` doors). Adversarial sweep clean (no invented surface, clamps spaced, data-c on all select-buttons, 4 route-mirror mds + chapter `time-delay-priority/llms.txt`, zero component-voice hits, all JS parses, live 200, unbuilt R4 module 404). TOC R4 `○→◐`; dashboard 22/53 modules UNCHANGED (orientation dives are NOT modules), pages **86→90 html + 18→19 llms.txt**. **Next gap: R4.01–R4.06 granular modules** (the chapter spec rows + r4.progress.md grounding are ready for `/redis-write time-delay-priority <module>` fan-out).

**DOOR-MAP RECONCILE (2026-06-08, `/reconcile`) — the R4 catch generalized into a CANONICAL bidirectional R↔E map.** The R4→E7 dishonesty was one instance of a course-wide drift: the redis-patterns `→ EchoMQ` doors and the echomq `← redis-patterns` reverse-links were COARSE + INCONSISTENT (the course-level "R4/R5 → E6/E7" pair contradicted the per-chapter reverses E4←R4, E6←R3/R5, E7←R1/R5). Treated BOTH courses' spec surfaces as the two sides of a `/reconcile` diff, judged each door against the CONTENT each E-chapter teaches, and created the single source of truth **`docs/redis-patterns/redis-patterns.echomq-doors.md`** (bidirectional R→E + E→R tables + content rationale + the consistency invariant "every edge appears in both tables"). **Canonical edges:** R0.2→E3 · R0.3→E1 · **R1→E7** (caching → the real near-cache; the most natural door, was MISSING from the forward map) · R2→E1/E3 · R3→E2/E5/E6 · **R4→E6/E4** (the fix, NOT E6/E7) · R5→E6/E7 · R6→E4/E5 · **R7→E1** (secondary, the job-hash data layer) · R8→E8. Refined to agree: `redis-patterns.roadmap.md` Door column (named every E-target, was bare `→ EchoMQ`/`—`/`partial`), `echomq.toc.md` "door from redis-patterns" map + the E6 header (`←R3,R5`→`R3,R4,R5`) + the E1 header (added R7), the R4 chapter-spec door section; all three sources now declare IDENTICAL R→E edges + both relative links resolve. **KEY FINDING — the inverted reconcile direction:** the built R3 + R4 door PAGES already wired the right targets (R3 hrefs = E2/core+E5/batches+E6/lifecycle; R4 = E4/groups+E6/lifecycle) — the drift was purely in the cross-link SPECS, so the fix flowed spec ← as-built (the `/reconcile` "post" direction). PAGE-LEVEL FOLLOW-UPS (recorded in the door-map md, NOT spec drift, retrofit when next touched): R1 built pages carry NO `/echomq` door (should door to E7), R2 doors only to the bare `/echomq` home (should name E1+E3). A DOOR ≠ GROUNDING (R1 grounds in Portal's cache machine yet doors to E7's near-cache). **Future course work (R5–R8 doors, new E-reverse-links) MUST cite + update `redis-patterns.echomq-doors.md` — it's the source of truth both courses point at.**

**R4.01 + R4.02 BUILT (2026-06-08, `/redis-write R4.01 + R4.02`, 8 pages → R4 now 24/53 modules, 45%, 98 html).** The first granular R4 modules, per-module fan-out from the banked `r4.progress.md`: I wrote two prompt packs (`r4.01.prompt.md` delayed-queue · `r4.02.prompt.md` schedulers) extending the verified-grounding bank, then two redis-experts fanned CONCURRENTLY (wave of 2, ≤2 heavy cap, zero drops, ~219k+214k subagent tokens each). **R4.01 the delayed queue** (`/redis-patterns/time-delay-priority/delayed-queue` + dives score-is-fire-time · zrangebyscore-promotion · the-next-wake) grounds the textbook `fundamental/delayed-queue` source in `:delayed` ZSET. **R4.02 schedulers** (`…/schedulers` + cron-vs-interval · upsert-no-duplicates · start-to-start-cadence) grounds in the `:repeat` ZSET upsert. **TWO GROUNDING REFINEMENTS over r4.progress.md (verify-against-code beat the bank):** (1) `promoteDelayedJobs` is an **INCLUDED function** copied identically into `moveToActive-11.lua` + `retryJob-11.lua` + `moveToFinished-14.lua` (sweep body `rcall("ZRANGEBYSCORE", delayedKey, 0, (timestamp+1)*0x1000-1, "LIMIT", 0, 1000)`), NOT a standalone `includes/promoteDelayedJobs.lua` as the bank loosely implied — cite it as an included fn, never a script. (2) The `:repeat` "hash" doc-comment in keys.ex is HONESTLY RESOLVED by the real code: the registry is a ZSET (`ZADD repeatKey nextMillis customKey`), but per-scheduler OPTS live in a SEPARATE companion HASH at `repeat:<customKey>` (`HMSET`, addRepeatableJob-2.lua) — the doc-comment was loosely pointing at the opts hash, not mislabelling the registry; teach both honestly. **KILLER GATE-INVISIBLE CATCH — component-voice personification the `voice` gate is blind to:** both agents passed 9/10, yet two pages carried "the removal **decides** the owner" + "how it computes the next score **decides** whether the cadence holds" — a removal/computation is a software operation, so "decides" personifies it. The `voice` gate scans a FIXED banned-word list (just/simply/blazing/…), NOT subject-verb semantics, so an orchestrator adversarial grep `\b(removal|computation|score|set|…) (decides?|sees?|wants?|knows?)\b` is REQUIRED on every batch (fixed → "establishes"/"governs", in both HTML + the route-mirror md). Gate tally: all 8 = 9/10, the lone `links` FAIL is the intended `/echomq/lifecycle` door — matches the R3 module precedent (every built R3 module page, hubs AND dives, carries its `/echomq/*` door FAIL by design; the door belongs on every page of the module, not hub-only). **CONCURRENT-SIBLING related-link discipline:** the R4.02 agent correctly DEFERRED linking its R4.01 sibling in "Related" during the concurrent build (R4.01's dir didn't exist yet → would have been a 2nd unintended FAIL), substituting the already-built `score-as-meaning` orientation dive; orchestrator RESTORED the R4.01 cross-link post-build once it resolved (the "don't link concurrent wave siblings" rule, with the post-build restore as the completion step). **OPERATOR-STAGED-AM GOTCHA (new):** the operator `git add`'d the agents' output OUT-OF-BAND mid-batch (HEAD → `2b82170a`); my later orchestrator edits (the 2 voice fixes + the schedulers-hub R4.01-link add) then showed as **`AM`** in porcelain (staged copy predates my edit) → a plain `git commit` would commit the PRE-fix staged version and drop my corrections. I ran ZERO git (read-only status only); flagged in the report that those AM files need a re-`add` before the operator commits. Manifests relinked orchestrator-side: home map (`index.html`:368) R4.01/R4.02 pills `soon→built` (cards already `<a>`), R4 landing granular grid `<div class="mod">`→`<a class="mod" href>` + `built`. TOC R4 header + module rows `✓ built`; dashboard 22→24/53 (45%, bar 8/24 cells), pages 90→98 html; `time-delay-priority/llms.txt` R4.01/R4.02 promoted to built with dive routes. **Next gap: R4.03 priority-scores (`getPriorityScore` `priority×0x100000000+pc`, dives ZPOPMIN/FIFO-within-tier) · R4.04 backoff-retry · R4.05 leaderboards (Portal progress, not EchoMQ) · R4.06 workshop.**

**R4.03 BUILT (2026-06-08, `/redis-write R4.03`, 4 pages, LONE agent → R4 now 25/53 modules, 47%, 102 html).** R4.03 priority-scores (`/redis-patterns/time-delay-priority/priority-scores` + dives `packing-two-keys-in-one-score` · `fifo-within-tier` · `zpopmin`) — the chapter's sharpest "Redis Pattern Applied" moment: a naive priority queue needs TWO structures (a set per tier + a FIFO within), but EchoMQ packs BOTH into ONE numeric score, `getPriorityScore` = `priority × 0x100000000 + (INCR pc) % 0x100000000` (tier in the high 32 bits, arrival counter in the low 32; `0x100000000 = 2^32`), so a single `ZPOPMIN` on `:prioritized` yields strict priority then FIFO-within-tier, no 2nd structure. **A LOWER priority number = higher precedence** (smaller score, ZPOPMIN pops smallest). Source = `fundamental/lexicographic-sorted-sets` — its SCORE-based half (composite-keys + "Combining with Score-Based Ordering"); the honest contrast taught on-page = a lexicographic key packs fields into the MEMBER (`ZRANGEBYLEX`), EchoMQ packs them into the SCORE (`ZPOPMIN`). **KILLER REUSABLE LESSON — no-invent caught a defect in MY OWN BANK, AGAIN (3rd time in this course):** `r4.progress.md` cited `EchoMQ.Keys.priority_counter/1` — that fn DOES NOT EXIST; the real key fn is **`EchoMQ.Keys.pc/1`** → `bull:{queue}:pc` (`keys.ex:179`, `@doc "Key for the priority counter"`; passed as KEYS[9] via `Keys.pc(ctx)` in `add_prioritized_job`, `scripts.ex:624`). The grounding-DUTY pattern held: I grepped `def priority_counter` BEFORE briefing, found NOTHING, traced the real `:pc` key, corrected the bank (spec ← code) + briefed the agent on `Keys.pc/1`, and the phantom name is absent from all 4 shipped pages. (Generalizes the standing rule: **a name in your own spec/bank/pack is NOT verified until grepped in echo — `@doc` strings and roadmap tables routinely name a fn that doesn't exist.**) Also corrected the bank's "six scripts" → `getPriorityScore` is an included fn reused across 11 priority-aware scripts. Verified grounding: `:prioritized` (`keys.ex:114`), `getPriorityScore` (`addPrioritizedJob-9.lua:84`, an INCLUDED fn), `ZADD prioritizedKey score jobId`, `EchoMQ.Scripts.add_prioritized_job/4`, consumption `rcall("ZPOPMIN", priorityKey)`→`LPUSH activeKey` (`moveToActive-11.lua:113`), `change_priority`/`changePriority-7.lua` (re-score aside). All 4 pages 9/10 (lone `links` FAIL = the intended `/echomq/groups` E4·Groups door — the honest priority fit per the door-map, NOT lifecycle). Adversarial sweep clean (zero `Portal.*`, phantom `priority_counter` absent, clamps spaced, route-tags segmented, 2-col refs, pager chain, md↔html bijection, component-voice grep clean). The agent SELF-FIXED two voice slips pre-report ("a priority queue *wants* two"→"needs"; "logic to *choose*"→"a selection step") — the lone-agent voice discipline is improving, but I still grep adversarially. **DEV-SERVER-STOPPED-MID-SESSION gotcha:** the live crawl returned `000` (connection refused, NOT 404) because the jonnify dev server had stopped (stale `bin/jonnify.pid`, `make status`=not running) — a `000` means server-down, a `404` means route-missing; restart with `make start` (background, port 8765, rebuilds) then re-crawl. (Authoring + the cms gate are filesystem-based and unaffected by a down server — only the live crawl needs it.) Manifests relinked (home map R4.03 pill `soon→built`, R4 landing granular card `<div>→<a>` + `built`); TOC + dashboard 24→25/53 (47%, bar 12/24 cells), pages 98→102 html; `time-delay-priority/llms.txt` R4.03→built with dive routes. **Next gap: R4.04 backoff-retry (`EchoMQ.Backoff.calculate/4` `base×2^(n-1)` + jitter, reschedule onto `:delayed` via `retryJob-11.lua`) · R4.05 leaderboards (Portal progress rankings, NOT EchoMQ) · R4.06 workshop (capstone, no dives).**

## Archived index line (2026-06-12, index compaction)

/redis-patterns course teaching 30 Redis patterns APPLIED, grounded in real EchoMQ-in-Portal. GROUNDING SOURCE = the ELIXIR app echo/apps/echomq (our-context; priv/scripts/*.lua + EchoMQ.Scripts/Keys/LockManager), preferred over the Go port apps/echomq-go (sibling-runtime/contrast). BOUNDARY = "DOOR NOT DEPTH": redis-patterns cites ONE real excerpt as proof + doors forward to prepare the learner for EchoMQ's under-the-hood; the dedicated EchoMQ course (next) covers EVERY priv/scripts Lua script. STAGED build; Stage 1 spec system DONE; chapter slugs locked to 8 specs/ folders. STAGE 3 AUTHORING: R0–R4 COMPLETE (R4 closed 2026-06-10: all six granular modules incl. R4.04 backoff-retry + R4.05 leaderboards + R4.06 workshop-capstone; EVERY R4 page carries the mandatory forward-framed "Where this is heading — EchoMQ 2.0" note per the operator's 2.0 protocol-break pivot, see [echomq 2.0 protocol break]); next: R5 Streams & Events strategic entry — all reconciled to the 3 binding rules (source-spine hub · 2-col refs · route-mirror md), clean md bijection, 111 html + 19 llms.txt, 28/53 modules (53%). Per-chapter senior-narrative-then-prompt-pack fan-out (r<N>.progress.md = the verified-grounding bank + r<N>.<mod>.prompt.md packs); waves of ≤2 concurrent heavy agents, lone agents safest, resume from the tree. R3=first fully-built granular chapter (6 modules, all grounded in echo/apps/echomq worker loop). R4 grounding banked in specs/time-delay-priority/r4.progress.md (:delayed score=(ts+delay)×0x1000 + promoteDelayedJobs bulk sweep; getPriorityScore=priority×0x100000000+(INCR pc), an included Lua fn across 11 priority-aware scripts (tier in high 32 bits, arrival counter in low 32; LOWER priority number=higher precedence via ZPOPMIN; the :pc counter key = EchoMQ.Keys.pc/1, NOT the phantom priority_counter/1 the bank had — no-invent caught it); :repeat=ZSET upsert — keys.ex doc-comment "hash" is a SOURCE DEFECT, code's ZADD/ZSCORE is truth; EchoMQ.Backoff.calculate/4=base×2^(n-1) owns the math, retryJob-11.lua only reschedules); door=E6·lifecycle + E4·groups (E7=EchoCache EXCLUDED as a dishonest door for time/priority, vs the toc's coarse "E6/E7"). gotchas (cms links gate skips cross-mount /elixir → verify on disk; folder-routing → a path is only dangling if absent as BOTH <slug>.html AND <slug>/index.html — an `ls *.html` alone misleads; voice rule covers software components incl. PRONOUN subjects, grep observes/notices/tracks AND decides/establishes — the `voice` gate scans a FIXED banned-word list, NOT subject-verb semantics, so a removal/computation/score that "decides" passes the gate but is personification → orchestrator grep every batch; don't link concurrent wave siblings (DEFER the cross-link, then RESTORE it post-build once the sibling resolves); module dives carry the `/echomq/*` door FAIL too (it's per-page, NOT hub-only — the R3 precedent); operator may `git add` the batch OUT-OF-BAND mid-build → my later orchestrator edits then show `AM` (staged copy predates the edit) so a plain commit drops them → flag the AM files for a re-`add` (I still run ZERO git); a live-crawl `000` = the dev server is DOWN (stale `bin/jonnify.pid`, `make status`=not running), NOT a `404`=route-missing → `make start` (background, port 8765) to restore then re-crawl (authoring + the cms gate are filesystem-based, unaffected by a down server); HEAVY author/edit agents cap ≤2 concurrent not 4; relink BOTH manifests on soon→built — the HOME tracks per-module pills too and drifts stale, BUT a strategic entry (landing+dives, no granular modules) needs NO home-map edit since the home is module-granular and the Open-chapter forward-link just resolves; the no-invent discipline catches defects in your OWN brief — verify against code, never a prose table, the spec/pack is not exempt (4th catch: the bank's "Portal progress rankings ZADD/ZRANK" was ASPIRATIONAL — Portal has NO Redis ZSET, real surface = Portal.Enrollment.Progress percent/PRG → ground in a clean standalone example + a COUNTERFACTUAL bridge); operator BUILDS AHEAD out-of-band — module HTML can already be committed at HEAD while dashboard/TOC still say "planned" → recount the TREE (find module hubs) before syncing views, and an agent's independent build converging byte-identical on HEAD = validation not error; gate internals decoded (apollo.go): `no-future` = ONLY the literal string `/future` (forward-tense 2.0 prose passes), `voice` = fixed 7-word regex; a `cd` in a compound Bash persists cwd → later relative paths report phantom MISSINGs — recheck with absolute paths)
