---
name: codemojex-tauri-track
description: codemojex Tauri desktop-shell track (cmt.N) — native shell wrapping the Phoenix game; ships /cm-ship; cmt.1–3 + cmt.4.1 shipped; Classic-first re-sequence (cmt.4.2 BoardScreen next, golden→cmt.5); hot-loading HMR entry landed
project: codemojex
metadata: 
  node_type: memory
  type: project
  originSessionId: 8c85ea96-925f-41ca-80a2-7859620110e6
---

**codemojex Tauri (`cmt.N`)** = a native desktop shell (Tauri v2, `mercury/codemojex/apps/game-tauri`) wrapping the running Phoenix codemojex game + the game island `mercury/codemojex/apps/game` (`@codemojex/game`). Canon `docs/codemojex/specs/tauri/` (tauri.design.md · tauri.specs.md ladder). Ships via **/cm-ship** (the Node side; NOT /codemojex-ship). AAW scope `cm-tauri`.

**Shipped 2026-07-01:** cmt.1 (shell run-loop, Director-solo) · cmt.2 (local Vite dev via a `GAME_DEV_URL` override in echo/ GameLive) · **cmt.3 Phase A** — the Effector Phoenix-channel STATE layer: `@mercury/effector` `createChannel` (additive, effector-only) + game `channel/{model,PhoenixGame}` driving `GameEdge` from `$props`. Commits `13652a7f` (game tree + admin.1, Operator out-of-band) + `2d67d8f3` (packages/docs remainder, Director). **Arm A:** the game folded into the mercury workspace (deleted its vestigial nested `pnpm-workspace.yaml` + dropped `!codemojex/apps/game`) since the reorg moved `@echo/phoenix*` → `mercury/packages/`; the built Vite bundle stays self-contained regardless.

**Deferred to /codemojex-ship** (echo/ + live-proof-gated): cmt.3 **D4** (the `RoomChannel` twin of GameLive) + **Phase B** (the Arm-B flip: default `mount`→`PhoenixGame`, GameLive slims to a page host; INV7 = SES rides a socket connect param). Land after the Operator observes the live round-trip (INV5, TCC fallback).

**Follow-ons flagged:** (1) the game dir's standalone edge Docker deploy (`edge.codemoji.games`, Dockerfile + fly.toml + bin/edge-deploy.sh) BREAKS under Arm A (frozen-lockfile + self-containment) — needs a rewrite to build from the `mercury/` workspace context; (2) [[mercury-dual-vitest-jestdom-trap]].

**cmt.4 SPLIT + re-aimed Classic-first (R-classic, 2026-07-02):** Classic Free/Paid ships before ANY golden surface ("Free/Paid" = the existing `view.free`/`view.guess_fee` already in GameProps — no new flow). **cmt.4.1 = the game DS + i18n foundation — SHIPPED `457e0f56` (2026-07-02, Director-verified):** Tailwind v4 (`@tailwindcss/vite`) + the verbatim `@theme` port of tokens.mjs (gold texture EXCLUDED — asset-backed tokens defer with golden) + `cn` + react-i18next (bundled ru/en, `smoke.*` seed) + the `?inline`→`<style data-cmjx-game>` CSS delivery (preflight skipped) + the dev-flagged `GameSmoke` (`VITE_GAME_SMOKE`, off by default — the branch FOLDS out of the shipped artifact; tailwindcss 4.3.2 emits used-only theme vars, `@theme static` = the one-line flip if full emission is wanted). **Pixel proof OWED Operator-observed:** hot-load loop + `VITE_GAME_SMOKE=1` → the bg-app gradient + card + translated string paint, host un-clobbered. **NEXT = `/cm-ship cmt.4.2`** (the Classic `BoardScreen` composition + GameProps→board mapping replacing the plain GameEdge internals; balances omit/neutralize per F3✓; the `board.*`/`game.*` i18n namespaces port there) → cmt.4.3 (Classic finished-state/events) → **cmt.5 = the deferred GOLDEN variant** (the gold.png 1.39MB delivery fork + boost; needs the `/codemojex-ship` `game_props` extension); cmt.6/cmt.8 now depend cmt.4.3. **GameRoom design docs** (`gameroom.{steelman,steward}.design.md`, still untracked) are partially superseded by the re-sequence (cmt.4.2 IS the board migration) — reconcile-or-retire them at cmt.4.2's carve. [[codemojex-program]] [[cm-ship-program]] [[codemojex-tma-edge]]

**Hot-loading + deep Effector BUILT (2026-07-02, Director-solo, design `docs/codemojex/specs/tauri/hotswap-effector.design.md`):** the RETURN-CONTEXT loop shipped as 3 improvements — (I1) the boot (`@codemojex/liveview-boot`) detects a Vite SOURCE bundle (`devOriginOf`: absolute http(s) + `.ts/.tsx` pathname, structural — no echo/ edit) and wires `/@react-refresh` preamble + `/@vite/client` before the entry import (`wireViteDev`, idempotent, warn-and-proceed) + guarded entry import; (I2) `@mercury/effector` gained `bridgeChannel(bridge,{joinReply})` — HostBridge→ChannelLike adapter, join "ok"=initial props, additive barrel; (I3) game mount is model-driven (`BridgeGame` twin of PhoenixGame; `update()`→`propsReceived`; derived `$view/$leaderboard/$history/$me` + typed `events.{guessRejected,revealed,goldenWin}`) with entry-level `import.meta.hot.accept` remounting the NEW module from RETAINED props (LiveMount facade in `hot.data`; page+socket never reload). Dev loop: `GAME_DEV_URL=http://127.0.0.1:5173/src/index.tsx`. Gates green (boot 18, game 32, effector build). **Committed `1c99cfa6` (2026-07-02 02:03 — it also swept cmt.4.1's index.tsx grafts aboard); the committed echo/ app.js still lags until the Operator runs phoenix-modules-build.sh.**

**⚠ F-1 gotcha (load-bearing):** the game's APP-MODE vite build (`rollupOptions.input`, no lib) was silently emitting a MOUNT-LESS bundle — rollup dropped the entry export signature (+ tree-shook the graph); prod survived on an older artifact; any fresh edge deploy = dead island. Fix shipped: `preserveEntrySignatures: "strict"` in game vite.config.ts. GATE RULE: verify the built `game-[hash].js` by `node import()` asserting keys ⊇ {mount, GameEdge} — source greps CANNOT see this. Vitest gotcha: `import.meta.hot` is TRUTHY under vitest but `hot.data` is undefined — guard hot-swap code on `import.meta.hot?.data`.

**Entanglement (2026-07-02) — RESOLVED at ship:** cmt.4.1 built concurrently with the hotswap track; `1c99cfa6` swept the index.tsx grafts; cmt.4.1 shipped `457e0f56` with vite.config.ts committed WHOLE (the tailwindcss plugins hunk + `preserveEntrySignatures` riding together); `mercury/pnpm-lock.yaml` (the game's 6 new deps) left UNCOMMITTED for the Operator's out-of-band lockfile pass (admin.5.x precedent).

**RETURN CONTEXT (2026-07-01, Operator-set, superseded by the 2026-07-02 build above):** after the mercury **mx.10** detour (SPECCED not built, [[mercury-design-system]]), active development resumed at game-tauri hot-loading (cmt.2's `GAME_DEV_URL` + cmt.3 Phase A Effector state). mx.10 is a mercury-island rung (`/mercury-ship`), independent and non-blocking.
