---
name: echomq-course-spec
description: "/echomq course = 'EchoMQ, In Depth' — 2026-06-15 RE-PIVOT to ONE shipped system, THREE PILLARS (Queue/Bus/Cache) + Overview/Protocol/Proof, Elixir-canonical, as-shipped (NO versions), [RECONCILE] md shadow, persistent [chapter].prompt.md fan-out; infra rewritten (echo-mq-writer / echo-mq-expert / echo-mq-reconcile); Overview+Protocol+QUEUE SHIPPED = 45/91 pages ([RECONCILE]=0 on the Queue), now built via the new shell-assembler tool docs/echo_mq/course/tool/build_page.py; NEXT = Bus/Cache/Proof; the old E0–E8 two-movements model is RETIRED (read the top banner; the rest is build history)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 73b81fc7-ffef-4d6d-ac38-349cafb4dda9
---

> **★ 2026-06-15 RE-PIVOT — "EchoMQ, In Depth" (THIS supersedes everything below, incl. the 2.0-PIVOT section).**
> The /echomq COURSE was rebuilt to a new target by Operator directive. **One shipped system, THREE PILLARS** above one
> owned wire — **the Queue** (distribute work), **the Bus** (broadcast signals + a retained, replayable event log),
> **the Cache** (serve reads) — plus **Overview**, a **Protocol** foundation, and a **Proof** chapter. **Named pillar
> routes:** `/echomq/{protocol,queue,bus,cache,proof}` (+ `/echomq`, `/echomq/overview`). Taught **Elixir-canonical**
> ("polyglot" is the thesis, stated in ONE line — supported-in/ported-to, no per-runtime depth). **FOUR authoring
> disciplines** (the heart): (1) **as-shipped, NO version numbers** in main content (no "2.0/3.0", "the break",
> "tracked as it is built"); (2) **extract-and-annotate code, NO `file:line`** — lift the real Elixir fn with teaching
> comments; **Lua in TWO BEATS** (the named handle e.g. `EchoMQ.Jobs @enqueue`, then a separate commented script
> body); (3) **the `[RECONCILE]` md shadow** — the markdown source-of-record flags every claim AHEAD of as-built code
> (chiefly the Bus/streams depth, grounded in canon `emq.roadmap.md` §stream-tier + `emq3.specs.md`); it NEVER leaks
> into the HTML; it is the iteration-2 worklist (the 3.0 streams are upcoming FROM ANOTHER TEAM — write as shipped now,
> reconcile when they land). (4) **no-invent** — real code where it exists (`echo/apps/echo_mq` + `echo_wire` +
> `echo_cache`), design canon where it doesn't; the **frozen, unrelated `echo/apps/echomq` (no underscore) is NOT part
> of this course** (one-way guard: scrub `EchoMQ.Keys|LockManager|Scripts|Worker|moveToActive` → 0 on every page).
> **Per chapter: a persistent `docs/echo_mq/course/<chapter>.prompt.md`** fan-out brief the orchestrator authors before
> spawning one `echo-mq-expert` per module (the "inject persistent [chapter].prompt.md" mechanism).
>
> **RETIRED:** the two movements; teaching the frozen v1 line as a subject; the three-state living-status; ALL version
> framing + the `.fork` "2.0 break" callout; the E0–E8 ⇄ emq.N chapter map; the `examples/`-decoded-file rule.
> **KEPT:** dark-editorial design + the shared craft (`elixir-technical-writer/references/`); two-column refs; md-first;
> the redis door; never git.
>
> **INFRA REWRITTEN (2026-06-15):** `.claude/skills/echo-mq-writer/{SKILL.md,references/course-map.md}` ·
> `.claude/agents/echo-mq-expert.md` · `.claude/commands/echo-mq-reconcile.md` (now **wipe + rebuild to target** via the
> persistent prompt + Step 1.5) + `echo-mq-write.md` (greenfield sibling). `.claude/commands/redis-reconcile.md` got a
> **Step 1.5** (persistent `docs/redis-patterns/specs/<chapter>/<chapter>.prompt.md` + Step-2 point-agents-at-it).
> **Canon/output home = `docs/echo_mq/course/`:** `echo_mq.course.md` (the spine = the content-map), `markdown/<route>.md`
> (the route-mirror + `[RECONCILE]` shadow), `echo_mq.course.progress.md` (dashboard + `[RECONCILE]` ledger),
> `<chapter>.prompt.md` (the per-chapter briefs).
>
> **SHIPPED this run (E0+E1, 23 pp, all gate-PASS A+, fresh-built):** **Overview** = home `html/echomq/index.html` +
> `overview/` (landing + dives `the-three-pillars` · `the-protocol-below-the-line` · `the-door`; stale
> `the-four-layers`/`the-polyglot-thesis` WIPED). **The Protocol** = `protocol/` landing + 4 modules×(hub+3 dives) +
> workshop: `the-owned-keyspace · the-record-hash · the-lua-layer · immutability-and-branded-ids · workshop` (all REAL
> code → zero `[RECONCILE]`). The old `protocol/{four-layer-model,job-hash,lua-scripts,immutability}` WIPED; the old
> `core/substrate/groups/batches/lifecycle/production` dirs survive as **transitional** (superseded as pillars land).
> **As-built grounding verified on disk:** `EchoMQ.Keyspace.queue_key/2`→`emq:{q}:<type>`, **`job_key/2`→`emq:{q}:job:<id>`**
> (NOT the `j:` shorthand — agents corrected my brief from the code), `slot/1` CRC16-XMODEM%16384 (vector
> `slot("123456789")==12739`), `version_key/0`→`{emq}:version`, `reserve/1`; the 3-field job HASH `state/attempts/payload`;
> the inline `@enqueue`/`@claim`/`@complete` Lua (EMQKIND `string.sub(ARGV,1,3)` gate, declared `KEYS[]`) run by
> `EchoMQ.Connector.eval/5` (EVALSHA-first) / `EchoMQ.Script.new/2` (SHA1); the fence value `echomq:2.0.0` (code-constant
> ONLY); branded ids via `EchoData.BrandedId`. **The Bus pillar** = `EchoMQ.Events` (real pub/sub) + the stream tier
> (CANON → `[RECONCILE]`-heavy when built). Telemetry surface = **`EchoMQ.Meter`** (there is NO `EchoMQ.Telemetry`).
>
> **THE QUEUE PILLAR SHIPPED (2026-06-15, 2nd build session):** all **22 pages** gate-PASS A+, `[RECONCILE]` = 0 (all
> real code, scrubbed clean: no version/lineage/frozen-tree leakage). Modules `the-lifecycle · jobs-lanes-consumer ·
> batches · lifecycle-controls · flows · workshop`. Built via the **NEW shell-assembler tool
> `docs/echo_mq/course/tool/build_page.py`** (the token optimization the handoff asked for): the ~14 KB byte-identical
> design shell lives ONCE as fragments (`_head.html` w/ `{{title}}`/`{{meta}}`/`{{page_css}}`, `_header.html` w/
> `{{route_tag}}` generated from the route, `_foot_queue.html` w/ `{{module_nav}}`, `_scripts_common.html` =
> stamp-decoder + reveal), and the author writes only the per-page `<main>` + interactive JS (+ optional page_css). The
> worked example + spec live in `tool/pages/the-operator-plane.*`; round-trip verified (regenerate
> `the-lifecycle/claim-and-the-lease` → STATUS:PASS). **The home + protocol-landing Queue cards flipped soon→built;
> course.md Queue row ✅; progress.md 45/91.** Donor shell = `the-lifecycle/claim-and-the-lease.html` (dive) +
> `the-lifecycle/index.html` (hub); page heads are NOT byte-identical (each appends interactive CSS) → `{{page_css}}`.
> **NEXT:** build the **Bus / Cache / Proof** pillars (each via its own `<chapter>.prompt.md`, fan-out
> echo-mq-expert per module; apply the tool). **redis R1 caching — LIGHT 2nd PASS DONE (2026-06-15):** verified CLEAN
> already (the named surface drifts `Journal.intend`/`Gateway.parse`/`Table.expires` are ABSENT in caching — the prior
> reconcile fixed them; zero BullMQ; all 29 pages gate-PASS A+; TOC+roadmap already "✓ built + reconciled"). So NO
> fan-out engine run was needed (a clean baseline → the heavy `/redis-reconcile B1` would only re-confirm conformance).
> The genuine delta: the 2 prominent `.door` CTAs (caching landing + workshop) were framed around *the coherence/
> write-behind job lane riding EchoMQ's fair lanes* and pointed at the bare `/echomq` — **retargeted to the now-built
> Queue pillar `/echomq/queue`** (a BETTER target than the handoff's interim `/echomq/overview`, which assumed the door
> was a Cache-pillar door — it isn't; EchoCache is already doored to `/bcs`). The Cache-pillar retarget note (R1 →
> `/echomq/cache` when it lands; ~45 inline refs stay on the `/echomq` hub) is recorded in
> `docs/redis-patterns/redis-patterns.echomq-doors.md`. **Follow-up:** the site-wide `html/llms.txt` has NO `/echomq` section (pre-existing gap).
> Build/gate footgun: the cms gate flags need zsh word-split `${=F}` (unquoted `$F` is one arg in zsh → false 0/N fails).
>
> ---
>
> **⚠️ 2026-06-10 SUPERSESSION — read the "EchoMQ 2.0 PIVOT" section at the END first.** Everything below describing emq.1 as the "substrate / opt-in floor / wire-compatible by construction / `EchoMQ.Ext.*` / `bull:{q}:emq:` / `priv/emq_scripts/`" design is the RETIRED pre-2.0 framing (kept as build history). The current truth: emq.1 = the EchoMQ 2.0 protocol break, backed by Valkey.

**`docs/echomq/` = the dedicated EchoMQ course spec system** — a **LIVING agile-spec course** that teaches EchoMQ in depth AND **tracks the implementation as it is built**. RECONCILED 2026-06-07 (2nd pass) after the user revealed the implementation spec system at **`docs/echomq/specs/emq/`** = **EMQ "the library grows the patterns"**: an agile value ladder (emq.1–emq.6) for EXTENDING the real `echo/apps/echomq` library, opt-in + wire-compatible BY CONSTRUCTION (extend beside the core in the `emq` keyspace + a versioned Lua bundle, never edit; byte-identical wire for unextended queues, proven by the wire-compat + interop probes every rung). The EMQ rungs: emq.1 substrate (`EchoMQ.Ext.{Keys,Bundle,ID}` + bundle loader + boot fence + probes) · emq.2 groups (fair lanes, `EchoMQ.Group`, park-don't-poll, `group_{add,pick,finish,stalled}.lua`) · emq.3 batches (bulk consume, group affinity, partitioned finish) · emq.4 lifecycle (TTL/distributed-cancel/`checkpoint/2`) · emq.5 **EchoCache** (L1 ETS / L2 Redis cache-aside near-cache, single-flight, jittered TTL, version-guarded newer-wins, bus invalidation, `via: :job`) · emq.6 conformance + telemetry + benchmark gate (vs `bullmq` hex + Oban, wins AND losses). EMQ is **DRAFTED, none built**.

**The reconciled course = TWO MOVEMENTS, 9 chapters (E0–E8):** **Movement I · The Core** (what EchoMQ IS, the as-built baseline): E0 Overview (thesis + four layers + door + the LIVING nature) · E1 `protocol` (L1/L2 immutable wire) · E2 `core` (lifecycle + components + 3 runtimes). **Movement II · The Extension (EMQ — the LIVING spine, 1:1 with the build):** E3 `substrate` ⇄ emq.1 · E4 `groups` ⇄ emq.2 · E5 `batches` ⇄ emq.3 · E6 `lifecycle` ⇄ emq.4 · E7 `cache` (EchoCache) ⇄ emq.5 · E8 `production` ⇄ emq.6. (Supersedes the 1st-pass static-depth E0–E8.)

**The TRIANGLE every Movement-II chapter teaches (the design's core idea):** the **pattern** (the redis-patterns family the rung cites — R6 groups, R1 EchoCache, etc.) + the **implementation spec** (the `emq.N` triad) + the **as-built code** (real `lib/echomq/ext/*` + `priv/emq_scripts/*` once shipped). LIVING mechanic: a chapter teaches from the SPEC while the rung is `drafted` ("emq.N builds…", never asserted-as-shipped), and `/echomq-reconcile` RE-GROUNDS it in the as-built code when the rung ships. Two master invariants: the Golden Rule (L1/L2 immutable) for the core; wire-compatible-by-construction (extend beside, opt-in) for the extension.

**Grounding (NO INVENTION; verified anchors):** `echo/apps/echomq` (`Scripts.move_to_finished/7`, `Keys.lock/2`, `LockManager.extend_locks/5`, `Champ`=Channel Message Protocol, library-only no-supervision-tree) + `priv/scripts/*.lua` + `priv/emq_scripts/*` (built per rung) + the EMQ specs `docs/echomq/specs/emq/` + corpus `docs/echomq/content/` + Go `apps/echomq-go` (cluster.go `GetClusterSlot`/`ValidateHashTags`, CRC16%16384). Honest cross-runtime status (Go non-atomic + attemptsMade-vs-atm bug, Elixir cluster gap, Node-Pro-only groups). NOTE: `docs/echomq/specs/` is a PORTABLE pack — `reference/` holds the F8 consumer-chapter copies + `specs.approach.md` + the f8.1 exemplar; `../echomq/`-style links are pack-local.

**STATUS: the 3 course spec files (`echomq.toc.md` · `echomq.roadmap.md` w/ the triangle grounding map · `echomq.md` the contract w/ "The spec triad for a course chapter" adaptation) are RECONCILED + gate-verified.** **ALL E0–E8 CHAPTER TRIADS AUTHORED (2026-06-08, `/spec-write echomq` course-chapter mode, effort max; 28 spec files = 9 triads×3 + e0.prompt.md), all gate-clean.** Method = senior-author-then-fan-out: (1) **E0 RECONCILED to the settled 5-page shape** — home + overview landing + 3 dives (the-polyglot-thesis=why · the-four-layers=what · the-door=how, the-door carries the living two-movement frame); e0.{md,stories,llms} rewritten + e0.prompt.md's TWO stale facts FIXED (chapter-alias `e2=core…` not `e2=lua-scripts…`; reconciled door map R3→E2/E5/E6) + TOC E0 synced (the 10-page/2-module 1st-pass shape DISCARDED per user's "landing+overview+3 dives"). (2) **E1 = Movement-I exemplar** (protocol↔runtime bridge, shipped-code grounding, NO emq.N/Tracks, 4 INV). (3) **E3 = Movement-II exemplar** (the TRIANGLE: ← redis-patterns + ⇄ emq.N + as-built code; pattern↔implementation bridge; living-status "emq.N builds…" NEVER asserted-shipped since rungs DRAFTED; footer `· Tracks: specs/emq/emq.N.md`; 5 INV incl. INV2=triangle, INV3=living/no-invent). (4) **Fanned E2(I)/E4/E5/E6/E7/E8(II) in 3 waves of 2 `spec-author` agents** (E2 copies E1; E4–E8 copy E3) — all gate-clean (structure/voice/fences/traceability + gate-blind: no perceptual-verb-on-component, Coverage-complete, headline arities real). Map: E1 protocol·E2 core (Mvt I) / E3 substrate⇄emq.1·E4 groups⇄emq.2·E5 batches⇄emq.3·E6 lifecycle⇄emq.4·E7 cache⇄emq.5·E8 production⇄emq.6 (Mvt II). E5–E8 = 3 teaching modules+workshop (6 D#); E1–E4 = 4+workshop (7 D#). Comprehensive prompts INLINE in each .llms.md (only E0 has a separate .prompt.md pack). Gotchas: my trace-checker truncating long R# at 400 chars → false "missing [US:]" (segment-don't-truncate); real corpus file = `ch02-redis-data-layer.md` NOT ch02-data-structures (link gate caught); E8 separates shipped-core `[:echomq,…]` telemetry from emq.6 drafted `[:echo_mq,…]` extension catalog; E6 keeps shipped `EchoMQ.CancellationToken` distinct from emq.4 distributed cancel-by-id. **THE TOOLING FAMILY IS BUILT + hot-loaded** (echomq-course-writer SKILL+course-map · echomq-expert · /echomq-write · /echomq-reconcile w/ Mode B THE LIVING RECONCILE `--ship emq.N`; §5a=2 binding rules; anchors verified real). **E0 PAGES BUILT (2026-06-08, `/echomq-write overview`, effort max; 5 html + 5 route-mirror md):** `html/echomq/index.html` (home, full E0–E8 map, two-movement layout + course-map interactive) + `html/echomq/overview/index.html` (overview landing, L0–L4 layer-stack framing interactive + 3 dive cards + E1–E8 up-next) — both ROUTE MANIFESTS (links gate FAILs on soon-pill forward-links to unbuilt E1–E8 BY DESIGN, every other gate PASS) — + 3 dives ALL A+ STATUS:PASS: `overview/the-polyglot-thesis` (why, ch00 §0.2/§0.5, the attemptsMade-vs-atm interop bug from ch01 §1.2), `overview/the-four-layers` (what, the L0–L4 model + one-pickup-three-runtimes interactive; **I HAND-AUTHORED this as the dive-surface model**), `overview/the-door` (how, the bidirectional door map R0.3→E1/R2→E1·E3/R3→E2·E5·E6/R4·R5→E6·E7/R6→E4·E5/R8→E8 + the two-movement reading path, living-status "emq.5 builds EchoCache" never asserted-shipped). Method: I authored home+landing+four-layers (manifests are orchestrator-only; four-layers sets the echomq dive surface), then FANNED OUT 2 echomq-expert agents (the-polyglot-thesis + the-door) copying four-layers verbatim. **BUILD LEARNINGS (reuse):** (1) **ECHO ACCENT TOKEN** = teal `--echo:#2aa198; --echo-bright:#6fdccf;` added to `:root` (keep --redis/--elixir shared); rewrite course-accent USES redis→echo (`h1 .ex`, `.math`, `.geo-readout b`, `code.inl`, `.pill.soon`, `.mod.work`, hero gradient `rgba(42,161,152,…)`, `.stack .layer.imm`, `.bridge .cell.elix .lbl`, `pre.code .rdx`, data-c="echo"); route-tag stays GOLD (jonnify chrome). (2) **MULTI-MOUNT GATE** — cms `--routes-from` is REPEATABLE; add `--routes-from /redis-patterns=html/redis-patterns --routes-from /elixir=elixir` so cross-mount links RESOLVE in the gate (else they false-dangle; the known gotcha). (3) **DIVE LINK DISCIPLINE** — a dive must keep EVERY internal link resolving (e0 INV4), so do NOT hard-link unbuilt depth chapters (/echomq/protocol etc.); name them in `<strong>` prose, reader reaches them via the linked home map. Manifests (home+landing) carry the soon-pill forward-link exception. (4) future dives bootstrap from `html/echomq/overview/the-four-layers.html` (built echomq dive surface), NOT redis. **E1 STARTED 2026-06-08 (`/echomq-write E1`, effort max):** the E1 chapter LANDING (`/echomq/protocol`, manifest) + the COMPLETE E1.01 `four-layer-model` module (hub + 3 dives the-layer-stack/queue-level-keys/job-level-keys) BUILT — hub + 3 dives A+ STATUS:PASS, landing = manifest-exception; 5 html + 5 route-mirror md. **Established TWO NEW echomq surfaces:** the **chapter-landing** (copy chrome from a built echomq landing; full E1–E8 anatomy overview→why&when→module cards→how it works→workshop→up-next; `← redis-patterns` back-links) + the **module-hub** (framing interactive + ≥3 dive cards). Method: I authored landing + hub (new surfaces, orchestrator-only), then FANNED OUT the 3 dives copying `html/echomq/overview/the-four-layers.html` in a WAVE OF 2 (the-layer-stack + queue-level-keys) + 1 (job-level-keys, gates clean since its prev exists). **E1 GROUNDING all verified in `echo/apps/echomq/lib/echomq/`:** keys.ex = the FULL key taxonomy (queue-level: `wait`/`active`/`paused` LIST, `delayed`/`prioritized`/`completed`/`failed`/`marker`/`waiting-children` ZSET, `stalled` SET, `events` STREAM, `limiter`/`id`/`pc`, `meta` HASH, `all_queue_keys/1`; job-level: `job/2`, `lock/2`=`…:{jobId}:lock` STRING+TTL, `logs/2`, `dependencies/2`, `processed/2`) + job.ex (`from_redis/4`, compressed fields `atm`/`ats`/`stc`/`rjk`/`deid`/`defa` — NOT `nrjid` (a scheduler field); the `Map.get(data,"attemptsMade")||Map.get(data,"atm")` fallback IS the interop bug) + scripts.ex (`execute_raw/4`, `move_to_active/4`, `move_to_finished/7`, `extend_lock/5`, `extend_locks/5`; `moveToActive-11`/`moveToFinished-14` numkeys=name). **NEW LEARNINGS:** (5) the **svg gate fires on a LANDING too** — a text-only framing interactive FAILs `svg`; add an SVG diagram the interactive highlights (the "seen argument"). (6) a fanned agent **CAUGHT my brief's invented version** `echomq-ex:5.62.0` → real `EchoMQ.Version @bullmq_version "5.65.1"`, format `bullmq:5.65.1` — verify-before-citing applies to the ORCHESTRATOR's brief, not only the agents. (7) parallel dive fan-out: the pager chain cross-refs parallel siblings → expected-pending `links`; run a wave of 2 (the chain heads) then the tail. **E1 COMPLETE 2026-06-08 (`/echomq-write protocol E1.02 E1.03 E1.04 E1.05`, effort max):** the 4 remaining modules BUILT — E1.02 `job-hash` (the-schema·the-compressed-fields·the-field-name-bug) · E1.03 `lua-scripts` (scripts-are-the-protocol·evalsha-dispatch·one-script-many-keys) · E1.04 `immutability` (what-l1-binds·the-field-name-contract·the-cross-runtime-consequence) · E1.05 `workshop` (flat) — 13 pp ALL A+ STATUS:PASS; home+landing relinked (E1.02–E1.05 soon→built), TOC synced (+ stale `nrjid` grounding note fixed), all routes live-crawl 200, md bijection 13/13, all 10 cited `EchoMQ.*` surfaces verified-real (zero invention). Fan-out = 2 waves of 2 echomq-expert. **TURN LEARNINGS:** (8) **the orchestrator's grounding DUTY** — I caught the spec/skill anchor's `nrjid` conflation BEFORE briefing: the real `from_redis/4` compressed fields = `atm`/`ats`/`stc`/**`rjk`**/`deid`/`defa`; `nrjid`=nextRepeatableJobId is a SCHEDULER field (updateJobScheduler-12.lua/addJobScheduler-11.lua `HSET …"nrjid"`), NOT a from_redis job-hash field; also `from_redis` reads `processedBy` LONG not the compressed `pb` (corpus §1.2.3 is the protocol's contract table; the Elixir reader is deliberately inconsistent per field). A spec's anchor-list is a convenience summary that drifts from a moving codebase — read the actual decoder field-by-field at brief time. (9) **md-first CRASH-RECOVERY proven** — E1.03's agent socket-dropped at the report boundary AFTER writing all 4 md (md-first) but BEFORE any HTML; rebuilt the 4 HTML from the surviving md via a FRESH agent (a low-risk mechanical md→HTML transform, grounding already banked). The per-page md→HTML checkpoint makes a mid-batch failure cost minutes, not the module. (10) **corpus §1.5 version strings are ILLUSTRATIVE** (`echomq-ex:5.62.0`/`echomq-go:5.62.0`) — real `meta.version` = `bullmq:5.65.1` (`EchoMQ.Version @bullmq_version "5.65.1"`); inoculated every brief (last turn's trap). (11) **convergent verification = strong no-invent evidence** — two independent agents (E1.02+E1.04) verified the Go cross-runtime status to the SAME conclusion: Go AGREES on `atm` on the Redis hash (queue_impl.go:102 write / script_runner.go:160 read); the only divergence is Go's external JSON tag `attemptsMade` (job.go:22), an L4 surface, NOT a wire bug. **E2 `core` IN PROGRESS (Movement I):** **E2.01 `lifecycle`** (hub + the-eight-states·every-transition-and-its-script·the-lock-protocol) was built before; **E2.02 `jobs-queues-workers`** (the-job-model·the-stateless-queue·the-worker-fetch-loop) + **E2.03 `lock-management`** (one-timer-not-n·stalled-recovery·schedulers-and-events) BUILT this session (2026-06-08, `/echomq-write` per-module fan-out, effort xhigh) — 8 pp ALL A+ STATUS:PASS + 8 route-mirror md; both manifests relinked (chapter landing core/index.html AND home — incl. the stale E2.01 home pill soon→built) + TOC synced (E0 `· route · ✓ built` convention + chapter `◐ pages built` header). **E2.04 `runtimes` (the-elixir-architecture·the-go-gaps·cross-runtime-interop) + E2.05 `workshop` REMAIN.** **E2 GROUNDING all verified in `echo/apps/echomq/lib/echomq/`:** job.ex `Job.new/4` + stateless `Queue.add/4` (binary-queue clause = no process, Redis holds state; atom/pid clause = `GenServer.call` convenience) + worker.ex `@default_concurrency 1`, `available_slots = concurrency − map_size(active_jobs)`, **Task.async = a BEAM process per job**, fetch via `Scripts.move_to_active/4`→`moveToActive-11.lua`; lock_manager.ex = ONE `Process.send_after(:extend_locks)` timer per worker batch-renewing via `Scripts.extend_locks/5`→`extendLocks-1.lua` (numkeys=1, RETURNS the FAILED ids — the scripts.ex "1 for success" @doc is imprecise; timing 30000/15000/7500 ms); stalled_checker.ex = two-phase mark→recover, `LRANGE active`+pipelined `EXISTS lock`, recover via `moveStalledJobsToWait-8.lua` (`Scripts.move_stalled_jobs_to_wait/4`, returns `[recovered, failed]`, `max_stalled_count` default 1); queue_events.ex = Redis Streams `XREAD BLOCK 5000` → `{:echomq_event, type, data}`, last_id default "$"; job_scheduler.ex = `upsert/7`/`calculate_next_millis/2` cron|`:every`. **E2 BATCH LEARNINGS (reuse):** (12) **MOVEMENT-I CORE NEEDS THE 3-MOUNT GATE** — every E2 page carries `← redis-patterns R3` → `/redis-patterns/queues` (spec e2.md INV4 fixes E2=R3/queues), so the gate needs `--routes-from /echomq=… --routes-from /redis-patterns=… --routes-from /elixir=…` or the VALID back-link false-dangles (the model lifecycle pages themselves FAIL on 2 mounts). (13) **GATE-INVISIBLE BACK-LINK DRIFT** — a fan-out agent linked `/redis-patterns/coordination` (`← redis-patterns R2`) on the lock-management hub+one-timer dive (locks≈coordination, topically apt) — but the CHAPTER marker is fixed at R3/queues by INV4+roadmap+the E2.01/E2.02 siblings; `/redis-patterns/coordination` is a REAL route so the `links` gate PASSES the wrong chapter link. Caught only by reading the spec + comparing siblings (the "right-route-vs-resolvable" blind spot); normalized R2→R3 across HTML + all 4 md (header line 3 + related-link). (14) **CIRCULAR HUB↔DIVE LINKS ⇒ the MODULE is the atomic fan-out unit** — a hub links down to its dive cards and each dive links up (crumbs+pager) to the hub, and the `links` gate fails on any unresolved href, so NEITHER reaches PASS until all 4 files exist; one agent per MODULE (hub + 3 dives, md-first) is the only clean unit (splitting hub-from-dives deadlocks the gate). 2 concurrent per-module agents stayed within the heavy-author cap. **(15) THE `.applied` "Redis Patterns Applied" REVERSE-DOOR BLOCK (course-wide convention, 2026-06-08).** Every E1–E8 chapter LANDING carries a `.applied` block = the **inverse** of redis-patterns' "A door, not a depth" `.door` (example `html/redis-patterns/coordination/atomic-updates/index.html`): redis doors FORWARD (red `--redis`, "cite one excerpt; depth is in EchoMQ"); echomq's `.applied` points BACK (teal `--echo`, host-brand, identical geometry, "*this* page is the depth"). Placement = **end of "how it works"** (after `.note`, before "Up next"). It names + links the `/redis-patterns` chapters that door INTO this E-chapter. **SOURCE OF TRUTH = `docs/redis-patterns/redis-patterns.echomq-doors.md`** (canonical bidirectional `R ↔ E` map; map wins on disagreement). Reverse edges: E1←R0.3+R2(+R7 secondary) · E2←R3 · E3←R0.2+R2 · E4←R4+R6 · E5←R3+R6 · E6←R3+R4+R5 · E7←R1+R5 · E8←R8. **E0 = NO block** (course home ≠ door target). **RULE: link only a BUILT redis-patterns chapter; `<strong>`-name an unbuilt one (R5–R8; R7 data-modeling) with NO href** — a prose back-link is gate-checked, unlike a manifest forward-link. DONE: added to the 2 built landings (E1 `/echomq/protocol` R2+R0.3 linked/R7 named; E2 `/echomq/core` R3 linked) + synced protocol route-mirror md; both stay PASS-modulo-the-6-unbuilt-E forward-links. CODIFIED in echomq-course-writer SKILL (§1 table row + §3 anatomy + new §4c door-map subsection), echomq-expert agent def (#1 sub-bullet + gate-invisible self-check), /echomq-write Step 2. **E3 `substrate` STRATEGIC ENTRY BUILT 2026-06-10 (`/echomq-write E3 landing with 3 dives`, Fable 5, effort xhigh) — the FIRST Movement-II chapter built:** the E3 chapter LANDING (`/echomq/substrate`) + 3 chapter-level ORIENTATION dives (`the-opt-in-floor` · `loaded-beside-the-core` · `compatibility-as-a-check`) — 4 html + 4 route-mirror md, ALL A+ (3 dives STATUS:PASS; landing PASS-modulo-the-5-by-design-E4–E8-soon-forward-links); all 4 live-crawl 200 on :8765 (/echomq wired). The granular E3.01–E3.05 modules DEFERRED (still soon on home + landing). **E3 milestones (firsts):** (a) first `.applied` "Redis Patterns Applied" reverse-door on a MOVEMENT-II landing (E3 ← R0.2 + R2 per the doors-doc; both built → both LINKED — no name-only needed); (b) first TRIANGLE-STATEMENT landing via a new scoped `.triad` block (3 cells: pattern `← R0,R2` / spec `⇄ emq.1 · drafted` / the as-built code) = the Movement-II landing signature; (c) the `.bridge` flips to pattern→implementation (`.cell.idea`=the redis pattern, red `--redis-bright` lbl; `.cell.elix`=the emq.1 build, echo lbl). **STRATEGIC-ENTRY PATTERN (reusable for E4–E8 first passes):** a chapter = landing + 3 chapter-level orientation dives (essay slugs), the granular modules deferred — the redis-patterns/E0 "front door" precedent. **Orientation-dive slugs MUST differ from the future module DIR names** (substrate's modules will be dirs extend-beside/versioned-bundle/boot-fence/probes/workshop → a leaf `probes.html` would collide with `probes/index.html` under serveDirTree) — used distinct essay slugs. **FAN-OUT for a CHAINED orientation set:** author the landing FIRST (orchestrator-only manifest); then wave-of-2 = the chain-ENDS that touch the existing landing (the-opt-in-floor [prev=landing✓] + compatibility-as-a-check [next=landing✓], each dangling only on the MIDDLE), then the LONE middle (loaded-beside-the-core, neighbors now exist → clean) — the E1 "chain-heads then tail" precedent, ≤2 heavy concurrent; re-gate the 2 ends after the middle lands. **E3 GROUNDING (all verified at brief time):** emq.1 is DRAFTED — `echo/apps/echomq/lib/echomq/ext/` + `priv/emq_scripts/` ABSENT → every Ext surface written "emq.1 builds …" (0 asserted-shipped; grep clean). emq.1 D1–D7: `EchoMQ.Ext.Keys` (emq segment `bull:{q}:emq:…`, hash-tag-safe), `EchoMQ.Ext.Bundle` (SCRIPT LOAD priv/emq_scripts at init, EVALSHA→EVAL-on-NOSCRIPT MIRRORING the shipped `EchoMQ.Scripts.execute_raw/4` at scripts.ex:250-262, records {major,minor} at `bull:{q}:emq:meta`, failure isolation INV2), the version fence (INV3, hooked into Worker opt-in path, typed boot error), `EchoMQ.Ext.ID` (integer Snowflake epoch 2024-01-01 layout ts(41)<<22|node(10)<<12|seq(12), base62 `BAT…` brand, verified pair **`TSK0KHTOWnGLuC ⇄ 274557032793636864`** = base62 `0KHTOWnGLuC`, INV5 decode(brand(x))==x), the 2 probes (D5 golden capture + D6 interop via shipped `EchoMQ.Queue.add/4` arity-4 binary clause, D7 `emq_ping.lua`), INV4 atomicity floor (pure over KEYS/ARGV, time as ARGV ms). Shipped anchors verified: `EchoMQ.RedisConnection` (NimblePool `use Supervisor`, command/3), `EchoMQ.Scripts.execute_raw/4`, 50 core scripts in `priv/scripts/` (UNTOUCHED), library-only. **E3 LEARNINGS:** (16) the real `EchoMQ.Keys` emits UNBRACED keys (`bull:my_queue:wait`); the braced `bull:{q}:…` hash-tag form the pages use is the Redis-Cluster CONVENTION emq.1 Ext.Keys is specified to preserve (D1 "hash-tag-safe") — documented (spec+roadmap+model pages), NOT invented; an agent flagged it transparently. (17) the home needs NO edit for a strategic entry — the chap-link `Open chapter →` /echomq/substrate just RESOLVES (was a by-design dangle), the granular module cards correctly stay soon, there's no chapter-level pill to flip (confirms the redis-patterns precedent holds for echomq). (18) ADVERSARIAL-GREP must match BOTH the literal glyph AND its HTML entity (`←`+`&larr;`, `·`+`&middot;`) — agents emit `&larr;` so a literal-`←` grep false-reported "0 backlinks" on a page that had them (byte-different, render-identical). **NEXT = `/echomq-write core E2.04 E2.05`** (finish Movement I: runtimes + workshop) OR continue E3 granular modules (E3.01–E3.05) OR `/echomq-write E4 landing with 3 dives` (next Movement-II strategic entry, ← R4+R6); THEN Movement II E3–E8 depth (each NEW chapter landing MUST carry its `.applied` block per the map); THEN verify/complete the server wiring — NOTE `/echomq` is ALREADY wired into `main.go` (live-crawl 200 across all E1 routes), so the 5-file checklist (main.go ECHOMQ_DIR + 2 app.Get serveDirTree, Dockerfile COPY, Makefile, cmd/sitemap folderRouted slice) is largely DONE — verify each before treating as TODO. Served at `/echomq`, letter E, `serveDirTree`. Related: [[redis-patterns-course]] (the near side of the door; the pattern source), [[redlock-spec-system]] (/spec-write+spec-author codification), [[echomq-umbrella-app]] (the grounding app echo/apps/echomq).

---

**EchoMQ 2.0 PIVOT (2026-06-10) — TWO OPERATOR RESOLUTIONS, supersede all earlier emq.1/E3 framing above.** (1) **THE BREAK:** `emq.1` REWRITTEN to "EchoMQ 2.0 — the protocol break": BullMQ wire compat DROPPED (v1 line frozen at `1.3.0`, the last compatible release); `emq:{q}:<type>` keyspace replaces `bull:` (per-queue hashtag applied transparently/idempotently/validated by the core); `{emq}:` reserved for core cross-queue keys + `emq` rejected as a queue name; EVERY Lua key declared in `KEYS[]` (the undeclared-keys flaw — ~24–26 of 50 scripts, `moveToActive-11.lua:148,162`/`scripts.ex:256` — resolved at the root); `meta.version` → `echomq:2.0.0` + two-way typed boot fence; v1→v2 migration (drain-and-switch + at-rest key tool) = emq.1 D1–D7; master invariant INVERTED ("EchoMQ 2.0 owns its protocol — break once, versioned, frozen"); reason = Dragonfly thread-per-shard. **The old `EchoMQ.Ext.{Keys,Bundle,ID}` / `priv/emq_scripts/` / `lib/echomq/ext/` "beside the core" design is RETIRED — cite it NOWHERE** (e3.md's below-the-fold body still carries it; the RECONCILED banner wins — the banner-supersession rule). E3 retitled "EchoMQ 2.0 — the protocol break" (slugs unchanged: `/echomq/substrate`, orientation dives re-aimed: the-opt-in-floor=the owned keyspace · loaded-beside-the-core=the versioned break · compatibility-as-a-check=conformance-not-compatibility). **`docs/echomq/echomq.v2-frame.md` = THE consistency anchor** (locked narrative, verified grounding bank, the `.fork` block: burgundy, label verbatim "The 2.0 fork — EchoMQ leaves the BullMQ wire", ≤1/page, per-chapter reframe checklists, the 3-mount gate command). TOC + roadmap + the 4 substrate pages + E0/E1/E2 fork-frames already reconciled by the operator. (2) **BACKED BY VALKEY (the substrate of record):** conformance truth = **Valkey, current stable line** — BSD-3, foundation-governed, Redis-semantics Lua, AOF durability, hash-field TTL, the most credible neutral target for a library that must outlive any one vendor's licensing mood; **the emq.6 suite runs on Valkey FIRST and documentation claims are phrased against this engine** (never pin a version in prose; emq.6 drafted ⇒ spec-speak only). Dragonfly = the multithreading PERFORMANCE TARGET the v2 key discipline unlocks (--lock_on_hashtags), NOT the truth-of-record; never transfer one engine's capability to the other (hashtag-thread placement is Dragonfly's, never claimed for Valkey). **RECONCILED THIS SESSION (both resolutions):** echomq-course-writer SKILL.md (§0.3 v2-exclusive + §0.4 Valkey + §2 + §4/4a/4b banks + `.fork` in §5 + 3-mount §6 + Sources: BullMQ demoted to lineage-not-upstream, Valkey `https://valkey.io/docs/` + Dragonfly docs added + triad path FIXED to docs/echomq ROOT e0–e8) + references/course-map.md (statuses refreshed: E0✓ E1✓ E2◐ E2.05-left E3◐-reframed; resume point; /echomq wired) + echomq-expert agent def (new "protocol identity" section w/ the 3 rules; same banks/sources/gate; adversarial checks: no EchoMQ.Ext, no present-tense v2, engine-claims placed, fork-label verbatim) + docs/echomq/echomq.md contract (2 stale lines fixed: "wire-compatible by construction" → inverted invariant; E3 map row). **from_redis/4 fields fix propagated**: `atm/ats/stc/rjk/deid/defa` (NOT nrjid) now in skill+def+course-map. **PENDING (operator's spec pass, flagged not edited):** emq.md/emq.1.md/emq.6.md/v2-frame.md still say "Dragonfly as the first-class substrate"/"conformance probe green on Dragonfly" — the Valkey resolution lives in skill+def until the specs adopt it; e3.md body re-derivation (E3.01–E3.05 module ladder from the rewritten emq.1).

## 2026-06-12 — the emq SPEC-RUNG home moved out of this course tree

The implementation-spec system left `docs/echomq/specs/emq/` (DELETED wholesale in commit `7cfba869`; the
referenced emq.1–6 triads with it — recoverable only via `git show 1f03d579:docs/echomq/specs/emq/<file>`)
→ the new home is **`docs/echo_mq/`** (emq.design.md + emq.roadmap.md + emq.references.md + flat
`specs/emq.N.{md,stories.md,llms.md}` triads, movement-numbered: emq.0 = the BCS migration). The program is
"EchoMQ in Three Movements" ([[echo-mq-three-movements]]); the new library is `echo/apps/echo_mq` (the BCS
2.0 code), with `apps/echomq` frozen as the v1 push source. CONSEQUENCE for course work: any
`docs/echomq/specs/emq/*` citation on a page/spec is now stale — a Movement-II chapter's `⇄ emq.N` tracks
`docs/echo_mq/specs/emq.N.md`, and the rung NUMBERING changed (movement-based; the old emq.1-the-fork ↔ new
ladder mapping comes from the rewritten `docs/echo_mq/emq.roadmap.md`). `docs/echomq/specs/{core,trading}/`
(the reverse-mode chapter systems) remain in place. Also gone (staged): `docs/echomq/specs/reference/` —
the F8 pack copies + the specs.approach copy (canonical original = `docs/elixir/specs/specs.approach.md`).
