---
name: courses-memory
description: "Memory index for course writing html/[elixir,redis-patterns,echomq,bcs,art,mesh]"
metadata: 
  node_type: memory
  type: project
  originSessionId: 74619602-0eb8-44e7-9b03-800faa5f2e61
---

# Memory index: Courses

RECALL THIS MEMORY ENTRIES ONLY IF YOU WORK ON COURSE CREATION

- [fsharp course](./fsharp-course.md) — /fsharp "F# In Depth"; **C0+C1+C2+C4 COMPLETE; C3 for-elixir 3/9** + home built; dark-editorial+violet; C0–C5 (50 mods); **C4 Algorithms = parity w/ elixir E4 + an _EchoMQ in F#_ block per dive grounded in the NEW `echo_fs` port** (repo-root F# port — **now 3 packages EchoMQ+EchoConnector+EchoCache** mirroring echo_mq/echo_wire/echo_cache, `dotnet test` **62 green**, net9.0; reverse-spec docs in `docs/echo_fs`; Valkey socket transport = next rung efs.3, "coming soon"); C5=ibbs; /fsharp-write + fsharp-expert; **resume = C3.04–09 then C5 DevOps** — patterns: **senior-writer prompt-pack fan-out** + **per-module agent fan-out in waves of 3** (24–48pp A+ first pass); the **link race** (pin Related to own dives + a built module + a verified /elixir route, never a sibling-in-batch); landing pre-exists/is the dive model; cms links gate = exact-match (contextual footer, mounts incl. `/echomq` for C4); **C4 honesty guardrail** (never claim F# `Map`/echo_fs IS a HAMT/CHAMP — taught as technique/production form); FSI sig form diverges (C0–C2 name-less; C4 uses real-fsi named); 2 F# traps (Result.map FS0030 value-restriction; multi-case active pattern = Choice<unit,unit>)
- [echomq course spec](courses/echomq-course-spec.md) — **2026-06-15 RE-PIVOT (read the top banner): /echomq = "EchoMQ, In Depth"** — ONE shipped system, THREE PILLARS (Queue/Bus/Cache) + Overview/Protocol/Proof, named pillar routes, Elixir-canonical, as-shipped (NO versions), extract-and-annotate code (two-beat Lua, no file:line), [RECONCILE] md shadow, persistent [chapter].prompt.md fan-out; infra rewritten (echo-mq-writer/-expert/-reconcile); **Overview + Protocol + QUEUE SHIPPED A+ = 45/91pp** ([RECONCILE]=0 on the Queue's 22pp); built via the **NEW shell-assembler tool `docs/echo_mq/course/tool/build_page.py`** (shell as fragments, author writes only `<main>`+interactive JS+page_css — the token optimization); frozen echo/apps/echomq NOT taught (one-way guard); Bus streams = canon→[RECONCILE]; old E0–E8 two-movements model RETIRED; next = Bus/Cache/Proof pillars; R1 caching.prompt.md authored, run deferred (door→/echomq/cache blocked till Cache pillar)