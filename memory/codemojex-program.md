---
name: codemojex-program
description: "codemojex = the BCS Telegram emoji-game consumer (echo/apps/codemojex) — its canon docs, the Codemojex Program, the ship skill, and the shipped game engine"
metadata: 
  node_type: memory
  type: project
  originSessionId: 874ee9ef-331a-49fc-82d7-80158cd7f6d8
---

**codemojex** = the worked consumer of the BCS data stack — the Telegram emoji-guessing Mastermind (for money) at `echo/apps/codemojex`, ABOVE `echo_wire`/`echo_data`/`echo_mq`/`echo_store` (it consumes their public surface, never edits it — a change reaching a sibling app is out of bounds). A generic Mastermind engine: the modes are policy on one `games` table with a `type` discriminator — `classic` (live feedback, live settlement) + `golden` (blind/sealed: feedback `none`, sealed top-K, a commit-reveal provably-fair secret).

**Canon (`docs/codemojex/`, reorganized 2026-06-25 to 3 files + a program):** `codemojex.design.md` (the binding design — engine · six-table model · state machine · pragmatic Valkey node · open questions) · `codemojex.roadmap.md` (the `cm.N` ladder + the forward feature catalog) · `codemojex.progress.md` (the dashboard). The **Codemojex Program** is `docs/codemojex/program/codemojex.{program,venus,mars,apollo}.md` (operating manual + role calibrations, mirroring `docs/echo_mq/program/`). Rung specs are `specs/cm.N.{md,stories.md,llms.md}`; run-ledgers + design-phase deliverables archived in `specs/progress/`.

**Ship a rung via `/codemojex-ship`** (the `codemojex-ship` skill = /x-mode with the codemojex facts pre-loaded): GENERIC venus/mars/apollo charters (NO codemojex-* role skills); the topology router (Solo/Duo/Trio/Squad, keyed risk×build-state); a **data-model rung fans out a 2nd architect (Venus-Postgres)** over the relational redesign; Apollo is the MANDATORY high-risk evaluator on a Squad (the generic x-mode Apollo, NOT emq's mentor-only recalibration). Gate (from `echo/apps/codemojex`, `TMPDIR=/tmp`): Valkey 6390 + Postgres, `mix compile --warnings-as-errors`, `mix test --include valkey`; the residual-brand grep to 0 on a rename rung (spare `Kernel.round`); the fresh-schema reinit (`codemojex_dev`/`codemojex_test`) + migration up/down on a schema rung; the ≥100 determinism loop on a mint/process rung; `mix codemojex.stories`.

**Shipped:** the `codemojex-game-rename` rung (HIGH Squad) re-based three brands (`RND`→`GAM`, `RMM`→`ROM`, `USR`→`PLR`), collapsed to one six-table initial migration, removed the bonus-tier economy for linear-only scoring, and landed the blind Golden flow (commit-reveal SHA-256, per-game reduced `cell_codes`, sealed top-K with stored `payout_split`) — **cm.1 (founding core) + cm.3 (blind Golden): the engine WHOLE** (docs reorg `32f73504`). **cm.4 (the auth floor) SHIPPED** (`b6b8c958`: verified Telegram `initData` HMAC → a shared `SES`-in-Valkey session + the `:auth` plug + `POST /api/players` retired — the one pre-launch auth gap closed). **The `codemojex-golden-calibration` design phase COMPLETE + committed** (`3670eb7d` design-close + `5360d7a6` canon reconcile, 2026-06-26): the launch Golden Room is RE-DESIGNED as a **tournament** — NOT the blind type, NOT a boost class — `type:"classic"`+`golden:true` marker, **$1 buy-in = membership** for the room's life, **gather 10 paid members → the live timer**, a self-funding **$1×10=$10 break-even** buy-in-funded pool, two-class settlement (`top_k_split` diamond pool + consolation clips `max_score/10`), a `:gathering` state, never-fills void+refund, and **`gold_multiplier` REMOVED** (`D-16` — backend+canon drift absent from `node/codemoji-app`). Approved model = `docs/codemojex/specs/economy/economy.md` §8 + `echo/apps/codemojex/docs/golden-rooms.md`; the scope ledger (D-1..D-20, Z-1/Z-2) = `specs/progress/codemojex-golden-calibration.progress.md`. **SHIPPED THIS LINE (2026-06-27):** `cm.5` the Golden Room tournament engine (`22cb2cf9`; Apollo BUILD-GRADE — the `:gathering` state machine · `Wallet.buy_in` two-sided + the `transactions(player,ref) WHERE reason='buy_in'` partial unique index · `close_split`/`close_void` · the sweep wired · `gold_multiplier` reconciled out) → **`cm.6` the REVENUE LEDGER** (`2de57202` + roadmap `ae677477`; the BNK bank's first slice, cm-6 D-1..D-12): explicit balanced house-account double-entry — a dedicated `revenue_ledger` (RVL-branded, signed bigint delta, NO non-negative CHECK, multi-source/multi-currency) + `Wallet.house_post/5` / `house_balance/0..1` (by account) / `revenue_breakdown/1` (by game); cm.5's player/pool legs byte-frozen (the additive overlay); conservation **property-tested** (Apollo found the M5 `div(pool,10)→div(pool,9)` floor-divergence survivor a fixed-example suite missed → closed by the spec-mandated property test + wiring `Economy.entry_fee_split_keys/5` for one-floor STRUCTURAL conservation). → **`cm.7` the KEYSHOP SHIPPED** (`0acba290` build + `ab0b8709` Stage-6 fold; cm-7 D-1..D-10; **Apollo BUILD-GRADE**, 2026-06-28; design phase was `e3b62f8e` D-1..D-6): multi-rail PAY-IN replacing the weak `purchase_keys`/`"stars"`-literal **double-mint-on-replay** path — keys via Telegram Stars (XTR, built end-to-end) + TON/USDT/RUB (schema-shaped, adapters forward); a DB-stored `PKG` catalog + `ORD`/`OTX` with the exactly-once `(rail, external_id) WHERE external_id IS NOT NULL` partial-unique gate (the cm.5 buy_in idiom); **`KeyShop.settle_payment/1` = three rows in ONE `Repo.transaction`** (OTX receipt + TXN keys mint `ref=ORD` + RVL gross booking) GATED on the OTX insert (Pattern A count-rose; **NO status fast-path — the index is the sole race-safe authority, L-5c**); gross revenue to the SAME `revenue_ledger` (`account="platform"`, `reason="purchase"`, `currency`=the rail — D-3, the reconciliation bug the dual-architect payments-integrity lens CAUGHT: `account="purchase"` would've hidden it from `house_balance`); rate config-launch + `rate_source`/`rate_quoted_at` pinned on the order (D-4); the pure `Codemojex.KeyShop` pricing + the frozen `Codemojex.Rails` (native minor-units star/nanoTON/micro-USDT/kopeck + a `self_check!` boot vector); the cutover retired the client key-count/ref path. **Verify:** Director A1-exactly-once mutation-proof + harden 150/150 determinism + migration up/down/reinit; Apollo A1-A12 PASS + mutation kill-rate 3/3 + replay/tamper attacks held + the §5 spec-follows-code sync; cm.6 byte-frozen. **FORWARD (Apollo `L-6`, NOT a Stars blocker):** the first push-rail rung (TON/USDT/RUB, unbuilt) must assert `amount_minor >= order.price_minor` in `settle_payment` (no pre_checkout on a push rail). **`cm.8` NAMED** = the cash-out/treasury rung (withdrawable diamonds → TON/USDT/RUB at floating rates · KYC/hold/rate-pinning). NEXT deferred: a full-intake-double-entry rung (itemize the player buy-in keys-debit as a signed `TXN` under a DISTINCT reason — the L-8 link) · the BNK rake · `RMP` membership · growth · analytics. (Left uncommitted, a separate concern: the cm.5-closure `golden-tournament.stories.md` doc-sync + `cm-5.progress.md`/`registry`.)
**The standalone /codemojex course (SCAFFOLDED 2026-07-02, `034d0880` docs + `79428081` html):** the B7-under-/bcs plan is SUPERSEDED — the teaching arc ships standalone at `/codemojex`: nine chapters C0–C8 × 3 dives (the B7 six modules reconciled to as-built linear-only scoring + extended with cm.4 auth → C7.1, cm.6+cm.7 commerce → C6, production → C8; cm.5 threads C2/C4/C5, deliberately NOT a chapter). Canon = `docs/codemojex/specs/course/` — course.{toc,roadmap,progress,landing}.md + course.0–8.md stub manuscripts (the per-chapter fan-out briefs). HTML = `html/codemojex/`: an A+ landing (interactive 100−20·d guess-row hero) + 9 gated real-shell stubs in the **CMX calibration** — contract-sheet basis, ONE blue (links=chrome=`--c-lead` #1c70ac Telegram blue), gold #9a6b08 for Golden-Room money only, teal #1f7a5e diamonds/doors; per-page unique `CMX…` stamps (minted via `mcp__msh__mint`); the 9 stubs share a byte-identical `<style>` block (generated from the C0 template by a scratchpad script). `/bcs/codemojex` stays the BCS B7 and doors into /codemojex (a §3 section filling its §2→§4 numbering gap). Gate = `cms check --require-refs` with mounts /codemojex /bcs /echomq /redis-patterns /mesh /echo-persistence → 11×A+; served by echo-static with zero server changes. The same rung's canon reconcile closed the roadmap tier-`[RECONCILE]`, fixed design.md's stale Golden-Room forward-tense + the false pre-cm.4 auth-gap ¶, and extended the namespace story to 15 brands. **NEXT for the course:** per-chapter authoring (a future `/codemojex-write` fan-out consuming `course.N.md` briefs). Owed follow-ups: the /courses catalog card (`go/echo-courses/content/codemojex.html`), `html/sitemap.xml`+`llms.txt` entries, the /bcs B7 "thirty tiers" card-copy drift (bcs-writer scope), `program/codemojex.program.md:214` still listing `SES` among deferred systems.

**Money-rung craft (cm.5/cm.6):** the branded-id generator `EchoData.Snowflake.next()` is provably COLLISION-FREE (a lock-free `:atomics` compare_exchange CAS, strictly monotonic — same-ms bursts increment the seq, 4096/ms carries into the ts) → a "same-ms mint flake" is a MISDIAGNOSIS; the real debt is the PRE-EXISTING **L-5** `:valkey`-story no-Ecto-Sandbox row accumulation (test-hygiene; slows the suite over many iters, NEVER collides). **NEVER `git checkout`/`git restore` on the UNCOMMITTED overlay** — it WIPES the build (recover from a git dangling blob); revert mutations via Edit/cp. With two adversarial evaluators the truth is the UNION of attacks (a reproduced survivor outweighs a clean verdict that didn't probe that surface). A fixed-worked-example suite can be strong against gross mutations + blind to a one-config arithmetic corner → a money rung's conservation invariant needs a PROPERTY/grid test (the spec mandated it). **OPEN FLAG (`L-15`):** the gameplay canon `node/codemoji-design/gameplay/` still defines the Golden Room as the `gold_multiplier` boost class — it needs the same reconcile on that Operator-owned design surface. Gotchas: six Postgres tables not seven (`NOT` is a Valkey lane); `players.bonus_diamonds` is a wallet bucket NOT a scoring tier; EMS `cell_size`=72 (measured from the real sprite sheets, not the code default 144); a golden timer test needs a long `duration_ms` + explicit `close_now`. [[redis-reframe-echomq]] [[bcs-family-tooling]] [[echo-store-rename]]
