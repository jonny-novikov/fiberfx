---
name: aaw-mcp-server-v2
description: "aaw MCP server v2: design pair CANON (D-11), 8-rung ladder mcp1-mcp8 (mcp1+mcp2 SHIPPED, mcp3 BUILD-GRADE, mcp4 specced, mcp5 PROMOTED to Reconcile tool; D-13 TRIAD LAW; resume=mcp3 build), ledger RENAMED aaw.mcp.progress.md w/ slug-symlink shim (D-14 cutover seam), dashboard specs/mcp.progress.md, corrections D-7/D-8/D-10/D-11/D-13/D-14"
metadata: 
  node_type: memory
  type: project
  originSessionId: ca0c6852-c755-491f-9909-65670b505b2c
---

**aaw MCP server v2 (docs/aaw/mcp/) — state as of 2026-06-11.** The §12 Design Phase ran end-to-end on the live PoC (`go/aaw` 2.0.0-min, 17 tools, streamable-http :8905): Venus-1 ∥ Venus-2 → cross-reviews → Apollo DESIGN-GRADE → synthesis. **Canon (Operator D-11): `aaw.mcp.design.md` + `aaw.mcp.roadmap.md`** — base = venus-1 protocol spine + 14 venus-2 grafts; 22 tools (17 v1 + tool_x_resonance + channel_publish/poll/list + agent_heartbeat; tool_memory_* DROPPED per D-3); tokenless (D-4); go/mcp-go FREE TO MODIFY (D-5, AGENTS.md note in place); stateless transport w/ build-gate probe-as-decider; no env config, policy in tree-visible .aaw/config.json (W-3 gitignore fix = `.aaw/*` + `!.aaw/config.json` — bare negation under dir-ignore is a git no-op); V-SOLO-2 evidence-only (W-1).

**Build ladder mcp1–mcp8** (specs under docs/aaw/mcp/specs/, **D-13 TRIAD LAW shape**: root index `specs/mcp.md` (the emq.md pattern) + per-rung `mcpN.md` + `mcpN.stories.md` + `mcpN.llms.md` [+ optional prompt.md runbook]; the earlier fused `mcpN.specs.md` form is RETIRED — Operator halted mcp4 sharpening on the violation 2026-06-11; the missing index had let two scope drifts pass: drafted mcp3 dropped the §8 EBNF grammar half, drafted mcp4 pulled mcp5's transport/C-1 in; remediated by Venus-4 ccl-aaw-mcp-11): **mcp1 CLOSED `7972859f`** (settled tier; D3's O_APPEND objects deferred via atomic.go:11). **mcp2 CLOSED `f44f0539`+`514d4768`** (18 tools; wire_contract OMITTED until mcp4 computes it — main.go:154; the `model` field LANDED EARLY in the harden — store.go:48/:348/:375, main.go:73/:86/:138/:378 — mcp4-D4 only closes the deferral formally by pinning tests). **mcp3 SHIPPED** (`750bda97`, standard tier COMPLETE: Mars build → Mars-mcp3-2 second context independent DoD re-run CLEAN zero-changes → Director gate green → close P-9; 16 codes verbatim in internal/gates + `aaw: <CODE>:` Errorf; lock.go:44 single-prefix boot fold is CORRECT realization not drift; ledger.go:192 = documented exemption; 2 OPEN observations: live :8905 still pre-MCP1 17-tool binary — restart=Operator's call; L-5 flag-order usage-doc mismatch — natural home mcp4-D5 one-line fix, surfaced not decided) · mcp4=config/ports/wire BUILD-GRADE (Venus-mcp4 grounding reconcile vs 750bda97, ratified D-18; all drift=mcp3 line shifts re-pinned; emission route=gates constants per lock.go:44; Mars fires AFTER the Venus-5 framing pass lands; W-3+F-2 ride here, F-2 held-or-granted) · **mcp5 = "The Reconcile tool" SPECCED** (Operator promotion 2026-06-11, Venus-5 ccl-aaw-mcp-13 triad, Director ratified D-16): ships `aaw reconcile` — deterministic READ-ONLY CLI subcommand (the §10 aaw-audit zero-MCP-tool pattern, D-3 tool-fatigue precedent; tool count stays 18/22), claim grammar = file:line cites + relative md links + backticked paths → MATCH/STALE/MISSING delta table, -json, exit 0/1/2; MCP-tool promotion = named future Operator decision. Displaced transport-posture/C-1-probe rides **mcp8** (zero renumbering; probe=cutover gate; mcp4 prose re-homed). Proposal RETIRED from tree → cite via `git show 74d8a899:docs/aaw/mcp/aaw.mcp.proposal.md`; design canon still carries 2 broken proposal links (~:20/:665, Operator-side fix pending) · **mcp6 = "Interactive aaw — the Bubble Tea console" SPECCED 2026-06-11 (Venus-5 triad, the 3rd D-14-style promotion)**: `aaw tui` — read-only TUI over the file plane (3rd §10 zero-tool CLI app; charm trio bubbletea/bubbles/lipgloss = the ladder's FIRST third-party UI dep, a named D-5-analog seam, pinned at build; tea.Tick mtime-guarded re-read, NO fsnotify; lock-free/unattributed/torn-proof via mcp1 atomics; views = scope list + scope detail [liveness table · gates panel · live-follow ledger tail · parse-health]; `tui` mode word flags-first L-5, non-TTY stdout = exit 2; tool count stays 18; read model REUSES store read paths — readIndex :116/ScopeNames :235/LoadRegistry :261/Liveness :519/ParseHealth ledger.go:101-121/gates rule main.go:421). mcp6 STAYS the MEASUREMENT rung (D-17): mcp1–mcp5 = UPFRONT RUNGS, mcp6's own authoring runs server-coordinated end-to-end, the console RENDERS that coordination live, and the close entry records the productivity TALLY incl. the inverse metric (manual out-of-band interventions → mcp7/mcp8 deliverable | future-rung | non-goal). Displaced message channels ride **mcp7** (zero renumbering): mcp7 = "Message channels, resonance, archival & the aaw audit CLI" — the 18→22 jump in ONE rung, weight SURFACED for a possible Operator split when it approaches (triad unauthored) · mcp8=transport+conformance+cutover. ALSO: mcp4+mcp5 two-audience reconcile DONE 2026-06-11 (Venus-5): stories re-cut US-D[N]=developer / US-A[N]=agent (mcp4 5→7 stories: US3 split D3+A1 verdict-in-band, US5 split D5+A2 probe-payload; mcp5 5→6: US4 split D2+A3 locked-catalog), 5W Who/Why per-audience cost-first, llms [US:]/[implements] re-pointed, mcp.md "Story audiences (from mcp4 on)" conventions bullet; mcp1–mcp3 keep as-shipped US[N] (history not rewritten); gates green, zero old-form ids tree-wide.

**Ledger naming (D-14 → D-15 INVERTED):** real file = SLUG name `docs/aaw/mcp/aaw-mcp.progress.md` (the server's only possible write target — LedgerPath slug-derived store.go:235-237, SlugRe forbids dots); dotted-family alias `aaw.mcp.progress.md` = same-dir SYMLINK to it. D-14 (mine) had it the other way; a Mars finding (ccl-aaw-mcp-8, fence-correct withhold) proved the restart fuse — HEAD's writeFileAtomic os.Rename REPLACES a symlink at the destination → silent ledger fork on first post-restart append — so D-15 inverted: no write ever targets a symlink under EITHER binary. Doc references may use the dotted alias; testdata goldens keep slug-derived names (harness derives scope+".progress.md"). NEW dashboard `docs/aaw/mcp/specs/mcp.progress.md` (stage-point metric: 4 stages × 6 cells/rung, ladder=11/32=34%; Director syncs at every stage transition, agents never touch it).

**Process corrections (Operator-locked, ledger aaw-mcp.progress.md):** D-7 — NO standing Apollo stage in Design Phases (cross-review IS the evaluation; Apollo re-homed to rung-level inter-Mars + post-build; applies to the paused emq-design phase too). D-8 — system-spec synthesis = the REAL `venus` agent type (not general-purpose+charter); Director records its aaw ceremony (venus def lacks mcp__aaw__* tools). D-10 — TIERED ceremony: settled (1 build pass + Director gate re-run) / standard (build + ONE second context) / full (forks/risk/system-spec). D-11 — ceremony at the lawful minimum: one executable gate per rung, ONE P-entry per close, Z-requires-D at scope close, no reports longer than their diff. Protocol docs (x.md §12, x-mode SKILL §1/§2b, apollo.md) still carry the OLD formation — the Δ1–Δ7 edit ledger awaits the Operator grant in `docs/aaw/mcp/x-mode.design.md` §3 (+ fold in D-10/D-11).

**Hot-ledger discipline (earned live):** the scope ledger is a coordination medium the Operator appends to out-of-band mid-session — entries landed between a heading census and the next append THREE times (D-8 minted my D-9; P-6/Z-1/Z-2 taken before my close → P-7/Z-3). Re-read decisions immediately before every write/brief; never pre-number; nextN re-parse + per-scope lock makes races numbering-safe. The Operator also builds WHOLE RUNGS ahead out-of-band (mcp1 closed while its triad was an hour old). PoC quirks until v2 rungs land: aaw_spawn mints duplicate CCL-ids (len+1 race — observed live, ccl-6 twice); registry RMW unlocked → stagger ceremonies.

Related: [[x-mode-cclin-leadteam]].
