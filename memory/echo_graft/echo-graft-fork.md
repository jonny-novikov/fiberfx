---
name: echo-graft-fork
description: "echo_graft = from-scratch Rust durability tier seeded from Graft (NO upstream compat); work tree echo/apps/echo_graft, reference github.local/graft (idea source only); eg.1 carve + eg.2 remote-fence + eg.3 branded-id/change-feed SHIPPED; eg.4 design-reconciled — TWO functional-twin engines COEXIST (Operator D-1=A), Rust backend named echo_graft_backend, native EchoStore.Graft.* untouched"
metadata: 
  node_type: memory
  type: project
  originSessionId: be123e04-08e9-443b-b8c1-351eeaf17f18
---

**echo_graft** = a **from-scratch system seeded from Graft** (`orbitinghail/graft`, MIT/Apache-2.0) re-seamed as the EchoMQ platform's transactional+replicated **durability tier** (the consumer is `echo_store` — "the native Graft replication engine"). A 6-rung epic **eg.1–eg.6** (specs under `docs/graft/`: `graft.roadmap.md`, `graft.{1..6}.md`, `graft.integration.md`, `graft.1.prompt.md`). Integration is a supervised **Rust sidecar** the BEAM drives over EchoMQ (RESP3) — NOT an in-VM NIF (that's a post-eg.6 optimization).

**DEVELOPMENT DIRECTION (binding, set 2026-06-21):** **NO backward compatibility** with upstream Graft — not API, not wire, not cherry-pick. Upstream is a read-only **idea source** (never a sync target); every divergence is **owned**, never re-merged. Agents have explicit allowance to modify any file / break any upstream contract for EchoMQ's efficiency/correctness/DX. The standing brief is `echo/apps/echo_graft/README.md` — **this file supersedes the former `FORK.md`** (renamed; all docs/graft references rebased FORK.md→README.md, and the cherry-pick/mergeability framing across the roadmap + graft.{1,2,3}.md + graft.integration.md was reconciled to the no-compat direction). License attribution (repo-level MIT/Apache-2.0, no per-file headers) is retained as legal provenance, NOT a compat promise.

**Locations (critical — got corrected once):**
- **Work tree:** `echo/apps/echo_graft/` — a Rust Cargo workspace *inside* the Elixir umbrella's `apps/` (Mix ignores it; no `mix.exs`). Crates: `echo_graft` (runtime), `echo_graft_test` (parity harness + integration tests), `echo_graft_tracing`. Homebrew `cargo` 1.93 (workspace `rust-version=1.91`, edition 2024). Build/test: `TMPDIR=/tmp cargo test --workspace`.
- **Reference (READ-ONLY idea source, NOT a cherry-pick target):** `github.local/graft` — pristine upstream checkout @ `b07d9312`. NEVER edit it; read it for ideas / seed code, never sync back. `echo/apps/echo_graft/README.md` (the former `FORK.md`) records provenance + every owned divergence. A vendored `Cargo.lock` pins third-party deps (incl. `opendal@06f088d9`) for reproducibility.

**eg.1 SHIPPED** (the carve, NORMAL→clean): vendored runtime+tracing+test crates, removed the SQLite extension (`graft-ext`/`graft-sqlite` + the rusqlite bank-workload tests `remote_commit_faults`/`workload_sanity`/`sqlite`), renamed `graft`→`echo_graft`. Green on macOS: 67 engine inline + 2 verify_snapshot + 1 verify_soft_truncate + 4 concurrent_commit + 1 doctest = **75 tests**, 0 warnings, **determinism 100/100**. NOT committed (untracked additions on the jonnify `echo_mq` branch).

**Ground truths that diverge from the spec/integration sketch (confirmed against the pinned source — do not re-derive):**
- Remote storage is Apache **opendal** (`RemoteConfig::{Memory,Fs,S3Compatible}` over `opendal::Operator`), NOT `object_store`. The commit fence already exists (`RemoteErr::precondition_failed` ⇒ `opendal::ErrorKind::ConditionNotMatch`); `S3Compatible` already reads `AWS_ENDPOINT_URL` → **eg.2 Tigris is mostly config**, not new structure.
- Local commit OCC is **snapshot-version level, NOT page-level** (`fjall_storage::commit` → `is_latest_snapshot`): a 2nd concurrent commit from the same base aborts with `LogicalErr::VolumeConcurrentWrite` **even on disjoint pages**. So spec acceptance #4 ("disjoint concurrent → both succeed") was reframed to the engine's real behavior; disjoint-merge is a *remote-sync* property (eg.2/eg.3).
- macOS build patch: `reqwest::tcp_user_timeout` is Linux/Android-only → cfg-gated in `remote.rs` (the one owned build divergence, recorded in `README.md`).
- **No `echo_graft_remote` crate** — remote is a module, not an extractable trait (would force a core/remote/runtime split the rung never required).

**eg.2 SHIPPED** (NORMAL, 2026-06-21) — a **verification/reconciliation** rung, NOT an implementation one: the remote seam (opendal `Remote`) was carried whole in eg.1, so eg.2 *proves* it. Key reconciliation (the spec's "implement the `echo_graft_remote` trait against Tigris via a SigV4 client + MinIO" was stale): there is **no `echo_graft_remote` crate** and **no hand-rolled SigV4** — opendal's `S3` service IS the SigV4 Tigris client (`RemoteConfig::S3Compatible` reads `AWS_ENDPOINT_URL`), and the fence already existed as `put_commit`'s `WriteOptions{if_not_exists:true}`⇒`If-None-Match:*`⇒`ConditionNotMatch`⇒`RemoteErr::precondition_failed()`. **MinIO is NOT installed** → proved the fence in-process against opendal `Memory`+`Fs` (both honor `if_not_exists`→`ConditionNotMatch` per pinned `opendal@06f088d`: Memory=CAS, Fs=`O_EXCL` — the same conditional-create contract as S3); the live MinIO/Tigris leg is env-gated (`ECHO_GRAFT_TEST_S3_BUCKET`+`AWS_ENDPOINT_URL`), matching upstream's own no-live-S3 posture. **Engine finding:** the multi-writer model is *sync-then-race* — a never-synced volume may NOT blind-push to a non-empty remote (`plan_commit` asserts it); the real fence race = pull, then push from a stale base. **Added (additive, zero `Remote`/`RemoteCommit` byte changes):** `echo_graft_test/tests/remote_fence.rs` (fence #1 + conformance #6, on Memory AND Fs + env-gated S3) + `remote_sync.rs` (push-rollup→1 segment #2, lazy pull #3, ≤64-page Zstd framing #4, end-to-end sync-then-race fence #1, Fs parity #5) + `testutil`-gated `Remote::testutil_list` + `GraftTestRuntime::remote()` + `tempfile` test dep. Gate: **87 tests** green (75 eg.1 + 12 eg.2), warnings-as-errors clean, determinism 100/100 on fence+race; clippy exits 0 (the `redundant_clone` test-idiom warnings match eg.1's `concurrent_commit.rs` — deferred cosmetic). **LIVE TIGRIS-VERIFIED 2026-06-21** (Operator gave explicit non-sensitive creds at `echo/.env.test` → `fly.storage.tigris.dev`, region `auto`): created test bucket `echo-graft-test-7d4855` via `aws s3api create-bucket`, then ALL env-gated legs pass against real Tigris — the fence leg + the 4 sync legs (rollup/pull/framing/race). The sync suite was REFACTORED into backend-agnostic `run_*` helpers, each with a Memory entrypoint (always) + an env-gated `*_on_tigris` entrypoint; harness gained `with_live_remote`/`live_s3(tag)` (UNPAUSED tokio clock — `start_paused` auto-advances time and trips reqwest's connect/retry timeouts against a real net) + `on_remote(fut)` (inspect on the harness runtime so the live conn-pool/DNS resolver are reused) + liveness-matching `spawn_peer`. GOTCHA: opendal reads `AWS_ENDPOINT_URL` NOT Fly's `AWS_ENDPOINT_URL_S3` (added both to .env.test; bucket recorded in BUCKET_NAME/TIGRIS_BUCKET). Run live: `set -a; . echo/.env.test; set +a; export ECHO_GRAFT_TEST_S3_BUCKET="$TIGRIS_BUCKET"; cargo test -p echo_graft_test tigris`. Spec reconciled in `docs/graft/specs/graft.2.md` (Reconciliation block + acceptance #5 now "verified live against Tigris") + `FORK.md` eg.2 section. NOT committed (untracked, per the eg.1 git posture).

**eg.3 SHIPPED** (NORMAL+, 2026-06-21) — branded-id identity + the in-process change-feed, **additive, the engine action `remote_commit.rs` untouched**. New `echo_graft::identity::BrandedId` (validates `{NS}{base62}` = 3 upper + 11 Base62 = 14 chars; **caller-supplied, NO Rust minter** — criterion #1 is "opened BY a branded id", store+round-trip not generate). New `echo_graft::feed`: byte-frozen `FeedEvent` (bilrost 1 `volume_branded_id`·2 `log_id`·3 `lsn:u64`·4 `ts:u64`, 51-byte fixture), `ChangeFeed` trait + `InMemoryFeed` stub (ordered, idempotent-per-LSN, `events_since` replay), `lane_for`→`egraft:feed:{vol}` (the eg.4 lane string). Mapping = `Volume.branded_id` `#[bilrost(6)] Option<String>` (authoritative, in `volumes` partition) + new Fjall keyspace **`brands`** (branded→VolumeId forward index); `volume_open_branded` writes both in ONE batch (atomic w/ creation). `Runtime` gained `volume_open_branded`/`resolve_branded`/`feed()` (the feed is always-on internal state → `Runtime::new` signature UNCHANGED, zero ripple). **The publish gate = `volume_push`'s remote-LSN advance** (diff sync.remote before/after `run_action(RemoteCommit)`; publish one event per new LSN on the Ok branch) → a lost conditional write advances nothing → no event (#6 by construction, no engine-action edit). The event's `log_id` = remote `LogId` native string (the cursor coordinate), NOT a branded LOG id (the `brands` index is namespace-generic so LOG↔LogId is zero-code, but only VOL is exercised — minting a LOG brand would re-add the omitted minter). Gate: **98 tests** (87 + 6 inline identity/feed + 5 integration `identity_feed`), plain clippy exit 0 (eg.3 adds ZERO warnings; the 39 are carried-idiom deferred), determinism 100/100 on `identity_feed`. **FINDING (pre-existing, surfaced by eg.3 timing):** carried `verify_snapshot.rs` fault tests share PROCESS-GLOBAL precept fault state + race under cargo's default parallelism (`set_pending(1)` consumed by the other test's concurrent push) → BOTH fail; confirmed NOT an eg.3 regression (original one-liner `volume_push` also races ~1/8 parallel) → **fault suites must run `--test-threads=1`; the authoritative gate run is serial**. Spec reconciled: `graft.3.md` (Reconciliation block + verified-acceptance table + declared keys), README eg.3 section. NOT committed (untracked, per eg.1/eg.2 git posture).

**eg.4 DESIGN-RECONCILED 2026-06-21** (Venus design rung + Operator ruling): the umbrella has **TWO functional-twin Graft engines** — Rust `apps/echo_graft` AND a complete **native-BEAM `EchoStore.Graft.*`** (CubDB; `VolumeServer` single-writer = write-lock, `Streamer`→Tigris "native Litestream", `Sync` feed `graft:{vol}:commits`, `Reader` ETS→CubDB→fetch) in `apps/echo_store` that wants "no foreign engine". Each independently owns write-lock+OCC+LSN-log+remote-fence+async-ship+lazy-reads+change-feed → ONE role built twice; the duplicated role = the page/Volume engine. The ONLY hot-path durability need (the LOW-VOLUME `EchoStore.Durability.*` outbox — its moduledoc: "a small, mostly-idle dependency, not the hot path Oban puts every dequeue through") is ALREADY met natively. `graft.4.md` had mis-assumed the BEAM has no Graft and named `EchoStore.Graft` for the sidecar client → collision (build paused). **Operator ruling: D-1 = A COEXIST** (both kept; native canonical + UNTOUCHED) + **D-2 name = `echo_graft_backend`** (Rust EchoMQ-participant crate/binary; wire crate `echo_graft_proto`; Elixir client `EchoStore.GraftBackend` = a coexisting PEER, NOT a replacement; Rust feed lane `egraft:feed:{vol}` kept DISTINCT from native `graft:{vol}:commits`). Design doc `docs/graft/graft.engine-split.design.md`; `graft.4.md` reconciled + unblocked (claim narrowed to "drive the **Rust** page-engine"). **eg.4 builds:** `echo_graft_backend` sidecar + byte-frozen versioned RESP3 `echo_graft_proto` (reuse `EchoMQ.RESP` on the Elixir side) + `EchoStore.GraftBackend` client + DUAL-SIDE conformance (Rust + Elixir assert byte-equality on shared fixtures = the HIGH-risk mitigation); transport in-process/abstract, live Valkey/EchoMQ leg env-gated (eg.2/eg.3 precedent). eg.6 shootout = **per-workload** (D-5; "best engine" is workload-dependent under A). → eg.5 (low-latency write tier) → eg.6 (ship: cross-compile + CI + the durability shootout vs Champ/Oban). Working loop: implement smallest change to the rung's Given/When/Then, run tests + the cross-cutting gates, **stop at the acceptance boundary and report** before the next rung. CI/release YAML + README/AGENTS prose + the `docs/` SQLite tree are deferred to eg.6. Consumer link: [[echo-store-rename]].
