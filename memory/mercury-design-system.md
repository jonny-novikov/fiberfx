---
name: mercury-design-system
description: mercury = React+Vite+Effector pnpm monorepo design system (distinct from the echo_mq/BCS Elixir stack); package/app split rule + showcase rebuild
metadata: 
  node_type: memory
  type: project
  originSessionId: 92a053af-432f-4ffa-a4bd-47ceaf6e3902
---

`/Users/jonny/dev/jonnify/mercury` = a **React + Vite + Effector pnpm monorepo design system** (Node 22, pnpm 10.17). Distinct from the echo_mq/BCS Elixir stack that shares the `jonnify` git root — git root is `jonnify` (the PARENT), so scope commits with a `mercury/...` pathspec.

**Packages (resolve from SOURCE via vite alias + tsconfig `paths` — apps need NO prebuild):**
- `packages/mercury-ui` (`@mercury/ui`): token-driven, className-based (`.mx-*`) React 18 components, ~23 exports (Button, Input/Textarea/Search, Select, Checkbox/Radio/Switch/Segmented/Slider, Chip/Tag/Badge/Avatar, Card, Alert/Progress, Tabs, Modal/Tooltip, Table, AuthCode, Icon). Presentational, light theme default (`.dark-theme` class on root for dark), fonts DM Sans/Mono/Serif via `--font-primary/secondary/display`. Build: `vite build && tsc` → `dist/mercury-ui.{js,css}` + `index.d.ts`. Tokens in `src/styles/tokens.css` (`rgb(var(--token))` triplets).
- `packages/mercury-effector` (`@mercury/effector`): the state plug — `useTheme/setTheme/toggleTheme/initTheme`, `toast`/`<Toaster>` (renders Mercury `Alert`s), `createForm` (Effector-backed `useField`/`useForm`). Components stay presentational; effector lives outside React.

**THE RULE (Operator-stated):** ready-to-use components live ONLY in `packages/mercury-ui`; `apps/*` (showcase, catalogue, docs, echomq) just COMPOSE them + the effector plug — never reimplement, never house reusable components in an app.

**`static/`** = design-tool exports (custom `x-dc`/`DCLogic` runtime, raw `.mx-*` markup + `mercury.css`/`tokens.css`) = the design source of truth (`showcase.html`, `catalogue.html`, `react-showcase.html`, Svelte variants, screens).

**2026-06-27/28 — three static→React app ports** (commits `b62d544f` showcase + `dcee5d66` echomq + `131ef0a2` mobile on `echo_mq`; base is echo_mq NOT main because the whole `mercury/` tree lives unmerged on echo_mq, 264 commits ahead of main — a clean cherry-pick onto main is impossible. **PR #5** showcase+echomq `mercury-static-ports → echo_mq` was MERGED (real merge — both now in echo_mq). **PR #6** mobile `mercury-mobile-port → echo_mq` OPEN. PR-hygiene gotcha: local `echo_mq` can carry unpushed Operator commits (e.g. a `[codemojex]` fix) between the merged base and your new commit, so pushing HEAD to a branch drags them in — cherry-pick ONLY your commit onto `origin/echo_mq` in an isolated worktree for a clean single-commit PR. Also the root `mercury/package.json` working tree held an unrelated Operator `engines` bump (>=20→>=22): revert it → pathspec-commit your one line → restore it, so the rung commit stays pure):
- `apps/showcase` = faithful 16-page React port of `static/showcase.html`: chrome (`src/chrome/` Shell/Sidebar/Topbar/Demo/PropsTable + `showcase.css`), Effector app store (`src/store.ts`: `$route`+localStorage, `$progress` ticker, app-modal stores) + the plug, 16 pages (Overview, Foundations×3, 10 component pages w/ API tables, Patterns SignIn+Dashboard).
- `apps/echomq` = React port of `static/echomq.html` (EchoMQ bus dashboard, **dark-first**): pure `data.ts` (queues/stats/donut arcs/sin-hash throughput series/jobs/groups/batches/processors) + app-specific SVG charts (`charts/Donut`,`ThroughputChart`), Effector store (view/selected/search/run/range/procRunning), 5 views (Overview/Jobs/Groups/Batches/Processors).
- `apps/mobile` (port 5177, `pnpm dev:mobile`) = React port of `static/mobile-app.html` (consumer-fintech phone, **light-first**): Login + 5 screens (Home/Activity/Wallet/Profile/Send) in an Effector store ($authed/$tab/$sending/$filter); **DS consumed = Button/Input/Chip/Card**; the Send screen uses `@mercury/effector` `createForm` + a success `toast`/`<Toaster>`. Bespoke phone chrome (frame/statusbar/bottom-nav/balance/rows/amount field/finance glyphs) lives in `mobile.css`+`icons.tsx`, ported from `static/mercury-app.css`. No mercury-ui changes.
Both gated green: `tsc --noEmit` + `vite build` + Playwright smokes (0 console errors) via `.ds-sync/node_modules/playwright` + chromium-1228 (`~/Library/Caches/ms-playwright`); `vite preview` binds `localhost`/::1 (use `http://localhost:<port>`, NOT 127.0.0.1). The audit found `@mercury/ui` already implements every component/variant both mocks need — **no `mercury-ui` changes required**.

**2026-06-28 — mercurized the auth screens** (commit `d023fdca` on `echo_mq`, **commit-only — no push/PR by Operator choice**). Unlike the three ports above, this task ADDED to the libraries (the "no mercury-ui changes" note above is port-specific, not a universal rule). Extracted the reusable parts of `static/screens/` (six Svelte auth-flow mocks all composing a shared split-screen `AuthLayout`):
- **`@mercury/ui` +5 components:** `Link` (inline link/button affordance, `mx-link`; replaces hand-rolled `<a style>`), `Divider` (`mx-divider`, optional "— or —" label + vertical), `Checklist` (`mx-checklist`, met/unmet rules), `PasswordStrength` (`mx-pwstr`, presentational meter wrapping `Progress`), `AuthLayout` (`mx-al`, branded split-screen shell — prop-driven branding w/ Mercury defaults + a `brand` override slot). CSS appended to `mercury.css`; all exported from `src/index.ts`.
- **`@mercury/effector` +3:** `passwordStrength(pw)` pure scorer → `{score,label,variant,rules}`; `createCooldown()` countdown-timer store (resend "in 30s"); **`createForm` gained an async `onSubmit` + `$submitting`** (backward-compatible — existing `SignInPage`/mobile `sendForm` callers unaffected; `useForm()` now also returns `submitting`, `submit` is async returning `Promise<boolean>`).
- **`apps/showcase`:** new Patterns › **"Auth flow"** page (`patterns/auth`, `AuthFlowPage.tsx`) = 5 screens (Sign in/Register/Forgot/Reset/Verify) on one `AuthLayout`, Segmented-switched, driven entirely by the effector plug; new `components/link` + `components/divider` doc pages; `SignInPage` now uses `Link`+`Divider`; nav/store/App wired for 3 new routes. ui CSS dist is ~524kB (fonts inlined as data URIs — pre-existing, not a regression).
Gated green: tsc (ui+effector+showcase) + vite build (all 3) + Playwright smoke (0 console errors; verified strength meter Weak→Strong, rules checklist, resend cooldown 30s, async sign-in toast, forgot success state).

**Also (prior task):** `@mercury/ui` synced to a claude.ai/design project (`.design-sync/` in-repo holds config + 23 previews + NOTES). That sync needs a load-bearing top-level `"types": "./dist/index.d.ts"` in `mercury-ui/package.json` (the converter ignores `exports['.'].types`); that package.json edit is the design-sync's, not the showcase's.

**2026-06-28 — `docs/mercury/` spec-driven program AUTHORED (mx.0 docs DONE; NOT built/committed).** New canon mirroring the echo_mq pattern: `mercury.roadmap.md` + `mercury.design.md` + `mercury.progress.md` + `program/mercury.program.md` + `specs/mx.1/{mx.1.md,.stories.md,.llms.md}`. Rung prefix `mx`. The modular plan (Operator-confirmed via AskUserQuestion):
- **`@mercury/core` (NEW, UI-free)** = HOIST `mercury-ui/src/{internal,shared,utils}` + `cx.ts`/`date.ts`/`types.ts`/`css.d.ts` into `packages/mercury-core` (source-consumed like `@echo/core`, React a PEER dep for headless hooks useId/use-arrow-navigation). **D-1: utils/types/hooks ONLY** — design tokens STAY in `@mercury/ui`; `@mercury/effector` STAYS separate; both depend on core.
- **Regroup `@mercury/ui` the Claude-Design way** = `src/components/<group>/<Name>/` (8 groups + new `layout`), SPLITTING the 5 aggregate files (`Selection`→Checkbox/Radio/Segmented/Slider/Switch · `Overlay`→Modal/Tooltip · `DataDisplay`→Avatar/Badge/Chip/Tag · `Feedback`→Alert/Progress · `Input`→Input/Textarea/Search). `Card`/`Table` are standalone files w/ NO DataDisplay overlap.
- **`packages/mercury-ds` is EPHEMERAL → DELETE in mx.1 (D-2).** Forensic truth: `mercury-ds/components/<group>/<Name>/` = GENERATED design-sync stubs (`.jsx` re-export window.MercuryUI + `.html`/`.d.ts`/`.prompt.md`, NO runtime `.tsx`). The ONLY real source = `mercury-ds/mercury-components/{Accordion,Toggle,Pagination}.tsx` (3 net-new, salvage→@mercury/ui) + `handoff/tokens.css` (19K token export, diff-fold do-no-harm).
- **Master invariant = `@mercury/ui` public barrel stable** (barrel-diff gate: export NAMES identical before/after; additions OK, removals/renames NOT). Apps alias `@mercury/core` to source in all 5 `apps/*/vite.config.ts`.
- **mx.0 docs ✅ + mx.1 ✅ BUILT gate-green 2026-06-28 (UNCOMMITTED, commit pending).** mx.1 done: `packages/mercury-core` (new, source-consumed: `private` + `exports`/`main`/`types`→`./src/index.ts`, react PEER + `@internationalized/date` dep) holds the hoisted foundation (internal/shared/utils/cx/date/types); `@mercury/ui` regrouped into `src/components/<group>/<Name>/<Name>.tsx`+`index.ts` (9 groups, 5 aggregates SPLIT into per-component files); Accordion/Toggle/Pagination salvaged from (now-`rm -rf`'d, was UNTRACKED) mercury-ds + exported; `@mercury/core` alias added to all 5 apps (vite + tsconfig). **Master invariant PROVEN: barrel 91→103 additive-only, 0 removed/renamed** (mechanical check = TS getExportsOfModule diff vs HEAD baseline; `export *` means a text-diff of index.ts is NOT enough). Gate = `pnpm --filter "./packages/*"` + `"./apps/*"` typecheck+build (scope to mercury, NOT `pnpm -r` — that drags in codemojex-node's known-broken builds). **LANDMINES:** (1) the date chain uses `@/` imports + apps have NO `@` vite alias (only tree-shaking hid it) → converted core's `@/`→relative so vite bundles the boundary; (2) React 19 `useRef<T>(v):RefObject<T|null>` made `.current` nullable → salvaged Accordion needed `if(ref.current)` guards; (3) tokens.css NOT folded (handoff delta = only `@kind` annotation comments, identical values). pnpm-lock.yaml changed (new pkg). Storybook = Movement II (mx.2–mx.5), NOW UNBLOCKED. Origin: user's "Create storybook for Mercury" reframed into a modular roadmap; Storybook is the destination, mx.1 the prerequisite.

**2026-06-28 — mx.2 REFRAMED + AUTHORED (docs + K-1 exemplar; NOT committed).** User redirect: stop treating component contracts as generated files to FIND/port — HAND-AUTHOR them. The deleted `mercury-ds` `<Name>.prompt.md` were design-sync GENERATED stubs ("use via window.MercuryUI") that SURVIVE intact in `mercury/ds-bundle/components/<group>/<Name>/` (23) + `.design-sync/docs/` — nothing lost; seeds not contracts. **Movement II reframed = the authored contract layer (mx.2)**; Storybook shifted to **Movement III (mx.3–mx.6)**. mx.2 = hand-author a co-located `<Name>.prompt.md` for all 33 components via the AAW *contract-set* method (NEW section added to `docs/aaw/aaw.architect-approach.md`: contracts as hypotheses that feed each other, reconciled vs 3 truths = source + real call sites + siblings; exemplar-first fan-out). Authored this turn: mx.2 triad (`docs/mercury/specs/mx.2/`), format note `docs/mercury/contracts.md` (D-7 frozen template, 6 sections), **K-1 exemplar PAIR** `Button.prompt.md`↔`Icon.prompt.md` (grounded in live .tsx + real showcase/economy call sites; resolving cross-link both ways), **4× CLAUDE.md** (`mercury/` top = what-it-is + AAW-loop SUMMARY + laws + gate; 3 packages), canon D-7/D-8 + roadmap/progress re-sequence. **Consolidation AUDIT** (2 bg Explores): showcase + economy ALREADY pure composers — only marginal hoist candidates (showcase Demo/PropsTable, economy Mono) → all KEEP-INTERNAL (D-8); mx.2 payload is CONTRACTS not hoisting. **REMAINING K-2: 31 contracts (2/33 done)**; 5 have NO app call site (Textarea/Search/Toggle/Accordion/Pagination → source-grounded). Per-component grounding inventory embedded in `mx.2.llms.md`. Gate (contract-only, no build perturbation): prompt.md count == 33 folder count + no `window.MercuryUI`/`_ds_bundle` leak + barrel unchanged.

**2026-06-28 — `/mercury-ship` SKILL created** (`.claude/skills/mercury-ship/SKILL.md` at the jonnify root; NOT committed). The Mercury ship/reconcile mechanism, mirroring `/echo-mq-ship` + `/codemojex-ship` but run LIGHT (Duo/Trio, no `mercury-*` role skills — generic venus/mars/apollo charters). It's `/x-mode` with Mercury context pre-loaded. Modes: `mercury-ship mx.N` (ship a rung), `mercury-ship reconcile mx.N [post]` (bind `/reconcile` — the spec↔code differ grounded in Mercury surfaces: barrel exports · prop tables · cross-links · call-site citations), empty (next unshipped per roadmap). **Headline = THE BOUNDARY (the Mercury island):** IN = `mercury/**` + `docs/mercury/**` + `docs/aaw/**` ONLY; OUT (never read/Glob/Grep/find/edit) = everything else in the `jonnify` root — `echo/ html/ elixir/ go/ infra/ node/ tradex/ mcp/ bin/ scripts/ *.local/ memory/`. Enforce: every search roots at `mercury/`/`docs/mercury/`; gate is `pnpm --filter` NEVER blind `pnpm -r` (codemojex-node sub-workspace can be broken); commit = `mercury/…` pathspec; NO `TMPDIR=/tmp` (Elixir-only). Binds the master-invariant (barrel-diff via RESOLVED export set — `export *` makes text-diff insufficient), D-7 contract law, the contract fan-out (exemplar-first, ≤2 heavy authors/wave from `mx.2.llms.md` inventory), topology router (Duo=docs/contract/reconcile · Trio=component/package · Squad=HIGH-risk barrel/topology/delete/token rung, verifier mandatory). `mercury/CLAUDE.md` now points at it (was silent on the ship mechanism). All 21 cited paths verified resolving; 33/9 component fact exact. [[mercury-economy-calibration]]

**2026-06-28 — mx.2 SHIPPED + COMMITTED** (via `/mercury-ship mx.2`). The contract layer is BUILD-GRADE: all **33/33** `@mercury/ui` components carry a hand-authored, grounded, cross-linked co-located `<Name>.prompt.md`. Authored in **3 waves** (≤2 general-purpose author agents/wave, the AAW cadence): A=inputs+selection, B=data-display+feedback, C=navigation+overlay+singletons (Link/Divider/AuthLayout). **Wave C hit a transient API ECONNRESET on first try (wrote 0) → re-launched cleanly.** Gate green: coverage 33/33 · no extractor framing · **75 cross-links resolve (0 broken)** · `pnpm --filter "./packages/*"` typecheck+build + `"./apps/*"` build (5 apps) exit 0 · barrel byte-identical (docs add no exports, INV-1 trivially held). **The contract-set method proved out**: authors repeatedly overrode the `mx.2.llms.md` inventory with `.tsx` source-truth (AuthLayout had 10 MORE props than inventory; Stat/Progress/Alert/Chart richer enums), caught a wrong cross-link I'd instructed (PasswordStrength composes Progress only, NOT Chip), and discriminated real composition from coincidence (AuthLayout composes Alert, excludes Segmented page-chrome). **Two scoped commits on echo_mq**: `93de0d63` (the `/mercury-ship` skill + 4 CLAUDE.md) + `5e229956` (mx.2 = 33 prompt.md + contracts.md + specs/mx.2/ triad + aaw.architect-approach.md method + canon fold). PATHSPEC-only: the Operator's in-flight `#components/` subpath-import migration (4 .tsx: Chip/Tag/PasswordStrength/Search — `"#*":"./src/*"` imports map) + mx.1's 22 package files LEFT UNCOMMITTED (Operator's). [[mercury-economy-calibration]]

**2026-06-28 — mx.3 SHIPPED (Storybook host + foundations) via `/mercury-ship mx.3`.** Movement III opener. Director-led Trio (venus authored the fresh mx.3 triad → mars built → Director independent verify; no Apollo — NORMAL risk). **The Storybook host is a workspace APP `apps/storybook/` (`@mercury/storybook`), NOT a `packages/*` member (D-9)** — a Storybook ships nothing, it composes `@mercury/ui` like the 5 apps; resolves `@mercury/*` from SOURCE via a byte-mirror of the showcase vite alias; **Storybook 10.4.6** (`@storybook/react-vite` — picked as latest stable on Vite 6 + React 19, not the 8/9.x the spec sketched). `.storybook/{main.ts,preview.tsx}`: CSF3, a forward-compatible **3-root stories glob** (`../stories/**` + `../../../packages/mercury-ui/src/**` + `../../*/src/**` apps — last EMPTY until mx.4), a `globalTypes` light/`dark-theme` toolbar + a scoped `dark-theme`-wrapper decorator, `import "@mercury/ui"` for side-effect CSS. **3 foundation stories**, controls a NO-INVENT restatement of each mx.2 `.prompt.md`: co-located `Icon.stories.tsx` (`IconName[]`-typed 38-name select + Gallery) + `Button.stories.tsx` (6 variants × 3 sizes + WithIcon), host-local `stories/Tokens.stories.tsx`. **GATE POLICY (ruling 4/D-9):** host EXCLUDED from the per-rung apps gate → `pnpm --filter "./apps/*" --filter "!@mercury/storybook" build` (the FIVE product apps; combo works in pnpm 10.17.1) + root `sb:build`/`sb:dev` scripts + `build:apps` carries the same exclusion; a separate `pnpm sb:build` smoke is the Director's ship proof (→ `storybook-static/`, registers EXACTLY the 3 titles). **INV-8 (load-bearing, mutation-proven):** co-located `*.stories.tsx` under `packages/mercury-ui/src/` enter the library's own `tsc` (`include:["src"]`, inherited by tsconfig.build.json) and FAIL (`TS2307` — the lib has no Storybook types) → one additive `"exclude":["**/*.stories.tsx","**/*.stories.ts"]` in `mercury-ui/tsconfig.json` fixes it; the vite lib build (entry index.ts) is unaffected. **GOTCHA:** the host `tsconfig.json` needs `"@storybook/react-vite":["./node_modules/@storybook/react-vite/dist/index.d.ts"]` in `paths` so `tsc` can typecheck the co-located package stories (pnpm symlinks the storybook devDep only under `apps/storybook/`; tsc `paths` ignores the package `exports` map; vite's resolver is fine). **`/design-sync`:** `ds-bundle/` relocated to `apps/storybook/ds-bundle/` = the single DesignSync `localDir` (the tool defaults localDir→cwd and REJECTS any uploaded `localPath` outside it; full pipeline re-align = mx.6). Barrel byte-identical (INV-1). **The Operator committed it out-of-band as `26aa7754` "[mercury] storybook"** (author Ivan Novikov, captured my exact 17-file staged surface seconds before my own `git commit` → mine no-op'd; left it as the Operator's commit, did NOT amend). Canon folded: roadmap/progress mx.3→BUILT, design D-9. **OPEN (out-of-island, Operator):** `mercury/.gitignore` is itself repo-ignored by the root bare-`.gitignore` rule ([[jonnify-gitignore-repo-wide-trap]]) → the `apps/storybook/storybook-static/` ignore is LOCAL-only; wants a ROOT-registry entry for fresh-clone/CI durability. **NEXT = mx.4** = component stories across all groups (variants·argTypes·actions) **+ apps-side Pages**: brand-new pages from the apps' REAL composed screens (`apps/{catalogue,echomq,showcase,mobile,docs}` each export an `App`) wiring `@mercury/ui` + `@mercury/effector` — **MANDATORY: mx.4 ENHANCES `@mercury/ui` additively** (master invariant: additions OK) to cover more controls/cases/pages those screens need. Then mx.5 effector / mx.6 build-deploy + design-sync re-align. [[mercury-economy-calibration]]

**mx.4 SHIPPED 2026-06-29 (`5d075477` — MY pathspec commit) via `/mercury-ship mx.4` under ultracode** (venus triad → Workflow build fan-out [enhancement serial + 9 story groups ∥, each adversarially verified] → completeness critic → Director gate + net-zero mutation spot-check → mars harden → Apollo BUILD-GRADE). Q1 ruling: mx.4 = **component stories (all 33) + the focused-trio additive enhancement**; apps-side Pages **RE-SEQUENCED to mx.5** (they wire @mercury/effector = mx.5's domain). Every component now has a co-located CSF3 `<Name>.stories.tsx` (**35 component homes + host Tokens = 36** via `sb:build`); controls restate the mx.2 contracts (typed enum const-arrays off the exported union = the guard). **The trio (new public surface, Q2 ruling):** Card gains optional `title`/`actions` header props (**NO new export**; `CardProps` `Omit<…,'title'>` shadows the native attr — verified 0 call sites use it); new **`ListRow`** (data-display) + **`MoneyInput`** (inputs, composes Input via its `leading` slot) → barrel **+4 EXACT names** (ListRow/ListRowProps/MoneyInput/MoneyInputProps), **additions-only** (this rung's barrel-diff is additions-only, NOT byte-identical like mx.3 — reading it as a byte-diff = false fail). Canon **D-10**. TWO LESSONS: (1) **stories are type-checked ONLY by `@mercury/storybook`'s tsc** — `sb:build`/esbuild STRIPS types without checking → added a root **`sb:typecheck`** script to the gate = the authoritative compile-time NO-INVENT enforcement (mutation proof: an invented `AlertTone` → `TS2322`). (2) **Entangled index:** the Operator pre-stages sibling-program docs (echo-persistence/echo_mq/redis-patterns) in the index out-of-band → a bare `git commit` SWEEPS them in; MUST use `git commit -F msg -- <mx.4 pathspecs>` (pathspec-limited) so their staged work is preserved + only mx.4 commits ([[jonnify-gitignore-repo-wide-trap]] is the sibling .gitignore trap; this is the sibling-index trap). Multi-word group titles Title-Cased hyphen→space (`data-display`→"Data Display"). NEXT = mx.5 (effector stories + apps-side Pages), mx.6 (build/deploy + design-sync re-align).

**mx.5 SHIPPED 2026-06-29 (`63d4d233` — MY pathspec commit) via `/mercury-ship mx.5` under ultracode** (Trio+Apollo: venus triad → mars-1 build → Director independent gate + net-zero mutation spot-check [ZERO findings → NO harden pass] → Apollo BUILD-GRADE). **Operator SPLIT the rung (Q1):** mx.5 = **effector-powered Storybook stories ONLY**; the apps-side Pages RE-SEQUENCED to **mx.6** (Q2 model = Storybook **page-stories** `*.stories.tsx` co-located in `apps/*/src/` — the host glob `../../*/src/**` ALREADY reaches the 5 apps; Q3 = the 5 apps are being **completely rewritten with Mercury DS then RETIRED from the workspace at program end**, `codemojex-node/apps/economy` OUT of scope + NOT glob-reachable), build/deploy + design-sync → **mx.7**. Six host-home CSF3 stories `apps/storybook/stories/effector/{Theme,Toast,Form,Strength,Cooldown,Formatter}.stories.tsx`, one per `@mercury/effector` adapter (roadmap row LAGGED at 4; source + canon §1 = SIX: theme·toast·createForm·strength·createCooldown·formatter), each wiring live Effector state (`effector-react` hooks; models at MODULE SCOPE, hooks INSIDE each render component — Storybook treats `render` as a component) into the real components. `sb:build` 36→**42** homes. **Risk NORMAL + surface FROZEN: the `@mercury/ui` barrel is BYTE-IDENTICAL this rung** (mx.4 grew it; mx.5 does not — the strongest form of the master invariant), and **ZERO host-config edit** (the effector dep/alias/path/glob/scripts were all already present from mx.4 — the whole code diff is the 6 story files). Arms (Director-ratified, all minor/conservative, NOT Operator-level): A=theme story AUGMENTS (a local `${theme}-theme` wrapper, NEVER `initTheme()` — no global `<html>`/localStorage mutation/leak) rather than replace the mx.3 toolbar decorator; B=host home (cross-component patterns, like `Tokens.stories.tsx`; preserves mx.4's 1:1 component↔story count); C=one file per adapter. Canon **D-11**. LESSONS/grounding: (1) venus's lag-1 reconcile CAUGHT a bad task hint — `formatter→MoneyInput` is WRONG (`createFormatterModel` wraps an Intl DATE/locale `Formatter`; `MoneyInput` takes no formatter) → corrected to wire **Stat**; (2) `passwordStrength` is a PURE fn (no store) → the story supplies the effector state via an inline `createStore`+`createEvent`+`useUnit`; (3) Formatter determinism = a FIXED `const SAMPLE=new Date(2026,0,15)` at module scope + locale pinned `en-US` (NEVER `new Date()` at load — and the llms `grep "new Date()"` gate is a substring check that trips on a COMMENT quoting the literal, so reword comments); (4) the story NO-INVENT guard = enum option arrays typed `as const satisfies readonly MonthFormat[]` / `const TONES:AlertTone[]=[…]` enforced by **`sb:typecheck`** (the only tsc that checks stories; mutation proof: an invented `"bogus"` MonthFormat → `TS2322`). Same entangled-index pathspec-commit discipline as mx.4 (Operator pre-staged echo-persistence/echo_mq-course/redis-patterns/codemojex-tma + `.gitignore`/`memory/*`/`vitest.config.ts` — all preserved). NEXT = **mx.6** (apps-side Pages = Storybook page-stories of the 5 rewritten+transient apps), then **mx.7** (static build/deploy + `.design-sync` re-align).

**2026-06-30 — Movement-III tail RE-SCOPED + mx.7.1 SHIPPED.** Operator-ruled "skip apps at all" → **mx.6 (apps-side Pages) DROPPED** (tombstoned; doc value moves to mx.9). New tail, all triads authored + committed (`d4a85a63`): **mx.7** = import the Claude-Design bundle's net-new components into `@mercury/ui` — a **5-batch epic** mx.7.1–mx.7.5 (30 net-new + 2 folds: TextArea→the live Textarea, ToggleGroup→the live Toggle's already-exported ToggleGroup); **mx.8** = enrich the Storybook stories (palette·roundings·variant·actions·real-world scenes); **mx.9** = `apps/showcase/` one comprehensive showcase app (library·docs·API·do/don't·recipes) replacing the retired apps. Each mx.7 batch ships through an **Operator → Agent → Agent loop** (Operator in the seat between batches). **THE IMPORT MODEL (load-bearing): design flows DOWN only** — source = the Operator's **claude.ai/design project** (`claude_design` MCP = the `DesignSync` tool) → regenerated into the untracked, regenerable bundle **`mercury/packages/mercury-ds/`** (53 inline-style `.tsx` prototypes + own tokens/mercAccent + a live showcase engine) → **TRANSLATED** (not dropped-in) into `@mercury/ui`'s `.mx-*` + token idiom. **`/design-sync` + the `DesignSync` MCP are FORBIDDEN** (never push local→remote). The bundle stays UNTRACKED (out of every commit via pathspec; a root-.gitignore entry is the Operator's out-of-band call — [[jonnify-gitignore-repo-wide-trap]]). **mx.7.1 BUILT + COMMITTED (`5f4847f1`, MY pathspec) via `/mercury-ship mx.7.1`** — Director-led Trio-lite (Director did the lag-1 reconcile + verify DIRECT; mars built the 5 homes). Batch 1 = the **5 foundational primitives** `Heading·Text·Label·IconButton·Separator` (+5), each a translated 4-file home. **Fork A → Separator NET-NEW** in `foundations` (richer API orientation·label·size·decorative); `Divider` untouched (never rename). Barrel **107→127** (+20 = 5 components + 5 Props + 10 aux types), 0 removed/renamed; tokens.css byte-unchanged. **TWO REUSABLE LESSONS (every import batch):** **(L1)** a multi-component shared enum (like `accent`) must be a **per-component INLINE union, NEVER a shared `export type`** — duplicate `export type` names via the barrel's `export *` COLLIDE and **silently drop** the name = a gate-invisible barrel REMOVAL; the proof is a collision sweep (`grep` the new export names OUTSIDE the new folders → empty). **(L2)** the **DM Sans 600 `@font-face` is ABSENT and stays absent** — weight 600 already rides `--font-primary` via the live local()/synthesis pattern (.mx-money__ccy etc.); adding a face for the nonexistent SemiBold woff2 is harmful (INV-7 = "fold only if the live layer LACKS it"). Also: `IconButton` shares `.mx-btn--<variant>` tokens (CSS `:hover`, no useState, `aria-label` after `...rest`, no `inverse`); `Heading` on DM Mono (`--font-secondary`) at display sizes 5–9, DM Sans 1–4; the bundle's arbitrary `color?:string` escape hatch is DROPPED (recolor via `accent` only). **Entangled 2-commit slice** (prior-turn spec ladder + this-turn build shared roadmap/progress, no hunk-staging): A=`d4a85a63` (mx.6 tombstone + mx.7/7.2-7.5/8/9 triads), B=`5f4847f1` (mx.7.1 build + triad + roadmap/progress fold). NOT pushed (echo_mq ahead 4). **NEXT = mx.7.2** (+10: Callout·Spinner·Skeleton·Blockquote·DataList·Code·Kbd·AspectRatio·Collapsible·ScrollArea). Open later-batch forks: bundle git-fate · overlay-floor ADR (7.4) · date-lib dep (7.3 — a `@mercury/core` hook; `@mercury/ui` must NOT import `@internationalized/date` directly) · mx.8 palette mechanism. [[mercury-economy-calibration]]

**2026-06-30 — mx.7.2 SHIPPED (`acf538dd`, MY pathspec) via `/mercury-ship mx.7.2`.** Import batch 2 = 10 simple feedback/display/layout primitives (Callout·Spinner·Skeleton·Blockquote·DataList·Code·Kbd·AspectRatio·Collapsible·ScrollArea), built in **2 sequential mars waves of 5** (10 ≈ 2× the connection-death ceiling → split; shared `index.ts`/`additions.css` force the waves SEQUENTIAL, not parallel). Director-led Trio-lite (Director reconcile + interim/final verify direct; 2 mars build waves). Barrel **127→160** (+33 = 10 comp + 10 Props + 13 aux), 0 removed/renamed. Distinctness (Director-ratifiable, both NET-NEW, siblings untouched): Callout≠Alert (inline emphasis intent×variant×size vs status msg), Collapsible≠Accordion (single disclosure vs set). Gate green; tokens.css byte-unchanged (no DM Sans 600). **THREE NEW LESSONS (carry to every import batch):** **(L4)** ground each prop in the **bundle `.tsx`, NOT the spec §3/§4 summary** — §3/§4 drifted 3× this batch (Callout has NO accent [§4 error]; Skeleton has NO variant [§3 stray]; DataList HAS labelWidth=140 [§3 omission]); the bundle source + real tokens win, invent nothing. **(L5)** **bundle glyph names ≠ the live `@mercury/ui` Icon set** (`keyof typeof ICONS`) — remap (`chevDown`→`chevron-down`, `warning`→`alert`, `circleHelp`→`help-circle`); the typed `name` prop means `sb:typecheck` backstops a miss. **(L6)** **translate runtime `<style>` injection OUT into `additions.css`** — the bundle's Skeleton/ScrollArea do `document.head.appendChild`; never in a live component (ScrollArea scrollbar → `.mx-scrollarea` webkit rules with semantic `--border-primary`/`--border-strong`, not raw `--slate`). Also: Code accent solid-ink uses `--slate-1/-12` (contrast swatch, canon-§6-permitted). NEXT = **mx.7.3** (+6 input/selection composites: DateField·Calendar·Checkbox/Radio groups+cards + folds TextArea→Textarea/ToggleGroup; the date-lib fork = a `@mercury/core` hook, `@mercury/ui` must NOT import `@internationalized/date` directly). [[mercury-economy-calibration]]
