---
name: mercury-design-system
description: mercury = React+Vite+Effector pnpm monorepo design system (distinct from the echo_mq/BCS Elixir stack); package/app split rule + showcase rebuild
metadata: 
  node_type: memory
  type: project
  originSessionId: 92a053af-432f-4ffa-a4bd-47ceaf6e3902
---

`/Users/jonny/dev/jonnify/mercury` = a **React + Vite + Effector pnpm monorepo design system** (Node 22, pnpm 10.17). Distinct from the echo_mq/BCS Elixir stack that shares the `jonnify` git root — git root is `jonnify` (the PARENT), so scope commits with a `mercury/...` pathspec.

**Packages (resolve from SOURCE via vite alias + tsconfig `paths` — apps need NO prebuild):**
- `packages/mercury-ui` (`@mercury/ui`): token-driven, className-based (`.mx-*`) React 18 components, ~23 exports (Button, Input/Textarea/Search, Select, Checkbox/Radio/Switch/Segmented/Slider, Chip/Tag/Badge/Avatar, Card, Alert/Progress, Tabs, Modal/Tooltip, Table, AuthCode, Icon). Presentational, light theme default (`.dark-theme` class on root for dark), fonts DM Sans/Mono/Serif via `--font-primary/secondary/display`. Build: `vite build && tsc` → `dist/mercury-ui.{js,css}` + `index.d.ts`. Tokens in `src/styles/tokens.css` (`rgb(var(--token))` triplets).
- `packages/mercury-effector` (`@mercury/effector`): the state plug — `useTheme/setTheme/toggleTheme/initTheme`, `toast`/`<Toaster>` (renders Mercury `Alert`s), `createForm` (Effector-backed `useField`/`useForm`). Components stay presentational; effector lives outside React.

**THE RULE (Operator-stated):** ready-to-use components live ONLY in `packages/mercury-ui`; `apps/*` (showcase, catalogue, docs, echomq) just COMPOSE them + the effector plug — never reimplement, never house reusable components in an app.

**`static/`** = design-tool exports (custom `x-dc`/`DCLogic` runtime, raw `.mx-*` markup + `mercury.css`/`tokens.css`) = the design source of truth (`showcase.html`, `catalogue.html`, `react-showcase.html`, Svelte variants, screens).

**2026-06-27/28 — three static→React app ports** (commits `b62d544f` showcase + `dcee5d66` echomq + `131ef0a2` mobile on `echo_mq`; base is echo_mq NOT main because the whole `mercury/` tree lives unmerged on echo_mq, 264 commits ahead of main — a clean cherry-pick onto main is impossible. **PR #5** showcase+echomq `mercury-static-ports → echo_mq` was MERGED (real merge — both now in echo_mq). **PR #6** mobile `mercury-mobile-port → echo_mq` OPEN. PR-hygiene gotcha: local `echo_mq` can carry unpushed Operator commits (e.g. a `[codemojex]` fix) between the merged base and your new commit, so pushing HEAD to a branch drags them in — cherry-pick ONLY your commit onto `origin/echo_mq` in an isolated worktree for a clean single-commit PR. Also the root `mercury/package.json` working tree held an unrelated Operator `engines` bump (>=20→>=22): revert it → pathspec-commit your one line → restore it, so the rung commit stays pure):
- `apps/showcase` = faithful 16-page React port of `static/showcase.html`: chrome (`src/chrome/` Shell/Sidebar/Topbar/Demo/PropsTable + `showcase.css`), Effector app store (`src/store.ts`: `$route`+localStorage, `$progress` ticker, app-modal stores) + the plug, 16 pages (Overview, Foundations×3, 10 component pages w/ API tables, Patterns SignIn+Dashboard).
- `apps/echomq` = React port of `static/echomq.html` (EchoMQ bus dashboard, **dark-first**): pure `data.ts` (queues/stats/donut arcs/sin-hash throughput series/jobs/groups/batches/processors) + app-specific SVG charts (`charts/Donut`,`ThroughputChart`), Effector store (view/selected/search/run/range/procRunning), 5 views (Overview/Jobs/Groups/Batches/Processors).
- `apps/mobile` (port 5177, `pnpm dev:mobile`) = React port of `static/mobile-app.html` (consumer-fintech phone, **light-first**): Login + 5 screens (Home/Activity/Wallet/Profile/Send) in an Effector store ($authed/$tab/$sending/$filter); **DS consumed = Button/Input/Chip/Card**; the Send screen uses `@mercury/effector` `createForm` + a success `toast`/`<Toaster>`. Bespoke phone chrome (frame/statusbar/bottom-nav/balance/rows/amount field/finance glyphs) lives in `mobile.css`+`icons.tsx`, ported from `static/mercury-app.css`. No mercury-ui changes.
Both gated green: `tsc --noEmit` + `vite build` + Playwright smokes (0 console errors) via `.ds-sync/node_modules/playwright` + chromium-1228 (`~/Library/Caches/ms-playwright`); `vite preview` binds `localhost`/::1 (use `http://localhost:<port>`, NOT 127.0.0.1). The audit found `@mercury/ui` already implements every component/variant both mocks need — **no `mercury-ui` changes required**.

**2026-06-28 — mercurized the auth screens** (commit `d023fdca` on `echo_mq`, **commit-only — no push/PR by Operator choice**). Unlike the three ports above, this task ADDED to the libraries (the "no mercury-ui changes" note above is port-specific, not a universal rule). Extracted the reusable parts of `static/screens/` (six Svelte auth-flow mocks all composing a shared split-screen `AuthLayout`):
- **`@mercury/ui` +5 components:** `Link` (inline link/button affordance, `mx-link`; replaces hand-rolled `<a style>`), `Divider` (`mx-divider`, optional "— or —" label + vertical), `Checklist` (`mx-checklist`, met/unmet rules), `PasswordStrength` (`mx-pwstr`, presentational meter wrapping `Progress`), `AuthLayout` (`mx-al`, branded split-screen shell — prop-driven branding w/ Mercury defaults + a `brand` override slot). CSS appended to `mercury.css`; all exported from `src/index.ts`.
- **`@mercury/effector` +3:** `passwordStrength(pw)` pure scorer → `{score,label,variant,rules}`; `createCooldown()` countdown-timer store (resend "in 30s"); **`createForm` gained an async `onSubmit` + `$submitting`** (backward-compatible — existing `SignInPage`/mobile `sendForm` callers unaffected; `useForm()` now also returns `submitting`, `submit` is async returning `Promise<boolean>`).
- **`apps/showcase`:** new Patterns › **"Auth flow"** page (`patterns/auth`, `AuthFlowPage.tsx`) = 5 screens (Sign in/Register/Forgot/Reset/Verify) on one `AuthLayout`, Segmented-switched, driven entirely by the effector plug; new `components/link` + `components/divider` doc pages; `SignInPage` now uses `Link`+`Divider`; nav/store/App wired for 3 new routes. ui CSS dist is ~524kB (fonts inlined as data URIs — pre-existing, not a regression).
Gated green: tsc (ui+effector+showcase) + vite build (all 3) + Playwright smoke (0 console errors; verified strength meter Weak→Strong, rules checklist, resend cooldown 30s, async sign-in toast, forgot success state).

**Also (prior task):** `@mercury/ui` synced to a claude.ai/design project (`.design-sync/` in-repo holds config + 23 previews + NOTES). That sync needs a load-bearing top-level `"types": "./dist/index.d.ts"` in `mercury-ui/package.json` (the converter ignores `exports['.'].types`); that package.json edit is the design-sync's, not the showcase's.

**2026-06-28 — `docs/mercury/` spec-driven program AUTHORED (mx.0 docs DONE; NOT built/committed).** New canon mirroring the echo_mq pattern: `mercury.roadmap.md` + `mercury.design.md` + `mercury.progress.md` + `program/mercury.program.md` + `specs/mx.1/{mx.1.md,.stories.md,.llms.md}`. Rung prefix `mx`. The modular plan (Operator-confirmed via AskUserQuestion):
- **`@mercury/core` (NEW, UI-free)** = HOIST `mercury-ui/src/{internal,shared,utils}` + `cx.ts`/`date.ts`/`types.ts`/`css.d.ts` into `packages/mercury-core` (source-consumed like `@echo/core`, React a PEER dep for headless hooks useId/use-arrow-navigation). **D-1: utils/types/hooks ONLY** — design tokens STAY in `@mercury/ui`; `@mercury/effector` STAYS separate; both depend on core.
- **Regroup `@mercury/ui` the Claude-Design way** = `src/components/<group>/<Name>/` (8 groups + new `layout`), SPLITTING the 5 aggregate files (`Selection`→Checkbox/Radio/Segmented/Slider/Switch · `Overlay`→Modal/Tooltip · `DataDisplay`→Avatar/Badge/Chip/Tag · `Feedback`→Alert/Progress · `Input`→Input/Textarea/Search). `Card`/`Table` are standalone files w/ NO DataDisplay overlap.
- **`packages/mercury-ds` is EPHEMERAL → DELETE in mx.1 (D-2).** Forensic truth: `mercury-ds/components/<group>/<Name>/` = GENERATED design-sync stubs (`.jsx` re-export window.MercuryUI + `.html`/`.d.ts`/`.prompt.md`, NO runtime `.tsx`). The ONLY real source = `mercury-ds/mercury-components/{Accordion,Toggle,Pagination}.tsx` (3 net-new, salvage→@mercury/ui) + `handoff/tokens.css` (19K token export, diff-fold do-no-harm).
- **Master invariant = `@mercury/ui` public barrel stable** (barrel-diff gate: export NAMES identical before/after; additions OK, removals/renames NOT). Apps alias `@mercury/core` to source in all 5 `apps/*/vite.config.ts`.
- **mx.0 docs ✅ + mx.1 ✅ BUILT gate-green 2026-06-28 (UNCOMMITTED, commit pending).** mx.1 done: `packages/mercury-core` (new, source-consumed: `private` + `exports`/`main`/`types`→`./src/index.ts`, react PEER + `@internationalized/date` dep) holds the hoisted foundation (internal/shared/utils/cx/date/types); `@mercury/ui` regrouped into `src/components/<group>/<Name>/<Name>.tsx`+`index.ts` (9 groups, 5 aggregates SPLIT into per-component files); Accordion/Toggle/Pagination salvaged from (now-`rm -rf`'d, was UNTRACKED) mercury-ds + exported; `@mercury/core` alias added to all 5 apps (vite + tsconfig). **Master invariant PROVEN: barrel 91→103 additive-only, 0 removed/renamed** (mechanical check = TS getExportsOfModule diff vs HEAD baseline; `export *` means a text-diff of index.ts is NOT enough). Gate = `pnpm --filter "./packages/*"` + `"./apps/*"` typecheck+build (scope to mercury, NOT `pnpm -r` — that drags in codemojex-node's known-broken builds). **LANDMINES:** (1) the date chain uses `@/` imports + apps have NO `@` vite alias (only tree-shaking hid it) → converted core's `@/`→relative so vite bundles the boundary; (2) React 19 `useRef<T>(v):RefObject<T|null>` made `.current` nullable → salvaged Accordion needed `if(ref.current)` guards; (3) tokens.css NOT folded (handoff delta = only `@kind` annotation comments, identical values). pnpm-lock.yaml changed (new pkg). Storybook = Movement II (mx.2–mx.5), NOW UNBLOCKED. Origin: user's "Create storybook for Mercury" reframed into a modular roadmap; Storybook is the destination, mx.1 the prerequisite.

**2026-06-28 — mx.2 REFRAMED + AUTHORED (docs + K-1 exemplar; NOT committed).** User redirect: stop treating component contracts as generated files to FIND/port — HAND-AUTHOR them. The deleted `mercury-ds` `<Name>.prompt.md` were design-sync GENERATED stubs ("use via window.MercuryUI") that SURVIVE intact in `mercury/ds-bundle/components/<group>/<Name>/` (23) + `.design-sync/docs/` — nothing lost; seeds not contracts. **Movement II reframed = the authored contract layer (mx.2)**; Storybook shifted to **Movement III (mx.3–mx.6)**. mx.2 = hand-author a co-located `<Name>.prompt.md` for all 33 components via the AAW *contract-set* method (NEW section added to `docs/aaw/aaw.architect-approach.md`: contracts as hypotheses that feed each other, reconciled vs 3 truths = source + real call sites + siblings; exemplar-first fan-out). Authored this turn: mx.2 triad (`docs/mercury/specs/mx.2/`), format note `docs/mercury/contracts.md` (D-7 frozen template, 6 sections), **K-1 exemplar PAIR** `Button.prompt.md`↔`Icon.prompt.md` (grounded in live .tsx + real showcase/economy call sites; resolving cross-link both ways), **4× CLAUDE.md** (`mercury/` top = what-it-is + AAW-loop SUMMARY + laws + gate; 3 packages), canon D-7/D-8 + roadmap/progress re-sequence. **Consolidation AUDIT** (2 bg Explores): showcase + economy ALREADY pure composers — only marginal hoist candidates (showcase Demo/PropsTable, economy Mono) → all KEEP-INTERNAL (D-8); mx.2 payload is CONTRACTS not hoisting. **REMAINING K-2: 31 contracts (2/33 done)**; 5 have NO app call site (Textarea/Search/Toggle/Accordion/Pagination → source-grounded). Per-component grounding inventory embedded in `mx.2.llms.md`. Gate (contract-only, no build perturbation): prompt.md count == 33 folder count + no `window.MercuryUI`/`_ds_bundle` leak + barrel unchanged.

**2026-06-28 — `/mercury-ship` SKILL created** (`.claude/skills/mercury-ship/SKILL.md` at the jonnify root; NOT committed). The Mercury ship/reconcile mechanism, mirroring `/echo-mq-ship` + `/codemojex-ship` but run LIGHT (Duo/Trio, no `mercury-*` role skills — generic venus/mars/apollo charters). It's `/x-mode` with Mercury context pre-loaded. Modes: `mercury-ship mx.N` (ship a rung), `mercury-ship reconcile mx.N [post]` (bind `/reconcile` — the spec↔code differ grounded in Mercury surfaces: barrel exports · prop tables · cross-links · call-site citations), empty (next unshipped per roadmap). **Headline = THE BOUNDARY (the Mercury island):** IN = `mercury/**` + `docs/mercury/**` + `docs/aaw/**` ONLY; OUT (never read/Glob/Grep/find/edit) = everything else in the `jonnify` root — `echo/ html/ elixir/ go/ infra/ node/ tradex/ mcp/ bin/ scripts/ *.local/ memory/`. Enforce: every search roots at `mercury/`/`docs/mercury/`; gate is `pnpm --filter` NEVER blind `pnpm -r` (codemojex-node sub-workspace can be broken); commit = `mercury/…` pathspec; NO `TMPDIR=/tmp` (Elixir-only). Binds the master-invariant (barrel-diff via RESOLVED export set — `export *` makes text-diff insufficient), D-7 contract law, the contract fan-out (exemplar-first, ≤2 heavy authors/wave from `mx.2.llms.md` inventory), topology router (Duo=docs/contract/reconcile · Trio=component/package · Squad=HIGH-risk barrel/topology/delete/token rung, verifier mandatory). `mercury/CLAUDE.md` now points at it (was silent on the ship mechanism). All 21 cited paths verified resolving; 33/9 component fact exact. [[mercury-economy-calibration]]

**2026-06-28 — mx.2 SHIPPED + COMMITTED** (via `/mercury-ship mx.2`). The contract layer is BUILD-GRADE: all **33/33** `@mercury/ui` components carry a hand-authored, grounded, cross-linked co-located `<Name>.prompt.md`. Authored in **3 waves** (≤2 general-purpose author agents/wave, the AAW cadence): A=inputs+selection, B=data-display+feedback, C=navigation+overlay+singletons (Link/Divider/AuthLayout). **Wave C hit a transient API ECONNRESET on first try (wrote 0) → re-launched cleanly.** Gate green: coverage 33/33 · no extractor framing · **75 cross-links resolve (0 broken)** · `pnpm --filter "./packages/*"` typecheck+build + `"./apps/*"` build (5 apps) exit 0 · barrel byte-identical (docs add no exports, INV-1 trivially held). **The contract-set method proved out**: authors repeatedly overrode the `mx.2.llms.md` inventory with `.tsx` source-truth (AuthLayout had 10 MORE props than inventory; Stat/Progress/Alert/Chart richer enums), caught a wrong cross-link I'd instructed (PasswordStrength composes Progress only, NOT Chip), and discriminated real composition from coincidence (AuthLayout composes Alert, excludes Segmented page-chrome). **Two scoped commits on echo_mq**: `93de0d63` (the `/mercury-ship` skill + 4 CLAUDE.md) + `5e229956` (mx.2 = 33 prompt.md + contracts.md + specs/mx.2/ triad + aaw.architect-approach.md method + canon fold). PATHSPEC-only: the Operator's in-flight `#components/` subpath-import migration (4 .tsx: Chip/Tag/PasswordStrength/Search — `"#*":"./src/*"` imports map) + mx.1's 22 package files LEFT UNCOMMITTED (Operator's). [[mercury-economy-calibration]]

**2026-06-28 — mx.3 SHIPPED (Storybook host + foundations) via `/mercury-ship mx.3`.** Movement III opener. Director-led Trio (venus authored the fresh mx.3 triad → mars built → Director independent verify; no Apollo — NORMAL risk). **The Storybook host is a workspace APP `apps/storybook/` (`@mercury/storybook`), NOT a `packages/*` member (D-9)** — a Storybook ships nothing, it composes `@mercury/ui` like the 5 apps; resolves `@mercury/*` from SOURCE via a byte-mirror of the showcase vite alias; **Storybook 10.4.6** (`@storybook/react-vite` — picked as latest stable on Vite 6 + React 19, not the 8/9.x the spec sketched). `.storybook/{main.ts,preview.tsx}`: CSF3, a forward-compatible **3-root stories glob** (`../stories/**` + `../../../packages/mercury-ui/src/**` + `../../*/src/**` apps — last EMPTY until mx.4), a `globalTypes` light/`dark-theme` toolbar + a scoped `dark-theme`-wrapper decorator, `import "@mercury/ui"` for side-effect CSS. **3 foundation stories**, controls a NO-INVENT restatement of each mx.2 `.prompt.md`: co-located `Icon.stories.tsx` (`IconName[]`-typed 38-name select + Gallery) + `Button.stories.tsx` (6 variants × 3 sizes + WithIcon), host-local `stories/Tokens.stories.tsx`. **GATE POLICY (ruling 4/D-9):** host EXCLUDED from the per-rung apps gate → `pnpm --filter "./apps/*" --filter "!@mercury/storybook" build` (the FIVE product apps; combo works in pnpm 10.17.1) + root `sb:build`/`sb:dev` scripts + `build:apps` carries the same exclusion; a separate `pnpm sb:build` smoke is the Director's ship proof (→ `storybook-static/`, registers EXACTLY the 3 titles). **INV-8 (load-bearing, mutation-proven):** co-located `*.stories.tsx` under `packages/mercury-ui/src/` enter the library's own `tsc` (`include:["src"]`, inherited by tsconfig.build.json) and FAIL (`TS2307` — the lib has no Storybook types) → one additive `"exclude":["**/*.stories.tsx","**/*.stories.ts"]` in `mercury-ui/tsconfig.json` fixes it; the vite lib build (entry index.ts) is unaffected. **GOTCHA:** the host `tsconfig.json` needs `"@storybook/react-vite":["./node_modules/@storybook/react-vite/dist/index.d.ts"]` in `paths` so `tsc` can typecheck the co-located package stories (pnpm symlinks the storybook devDep only under `apps/storybook/`; tsc `paths` ignores the package `exports` map; vite's resolver is fine). **`/design-sync`:** `ds-bundle/` relocated to `apps/storybook/ds-bundle/` = the single DesignSync `localDir` (the tool defaults localDir→cwd and REJECTS any uploaded `localPath` outside it; full pipeline re-align = mx.6). Barrel byte-identical (INV-1). **The Operator committed it out-of-band as `26aa7754` "[mercury] storybook"** (author Ivan Novikov, captured my exact 17-file staged surface seconds before my own `git commit` → mine no-op'd; left it as the Operator's commit, did NOT amend). Canon folded: roadmap/progress mx.3→BUILT, design D-9. **OPEN (out-of-island, Operator):** `mercury/.gitignore` is itself repo-ignored by the root bare-`.gitignore` rule ([[jonnify-gitignore-repo-wide-trap]]) → the `apps/storybook/storybook-static/` ignore is LOCAL-only; wants a ROOT-registry entry for fresh-clone/CI durability. **NEXT = mx.4** = component stories across all groups (variants·argTypes·actions) **+ apps-side Pages**: brand-new pages from the apps' REAL composed screens (`apps/{catalogue,echomq,showcase,mobile,docs}` each export an `App`) wiring `@mercury/ui` + `@mercury/effector` — **MANDATORY: mx.4 ENHANCES `@mercury/ui` additively** (master invariant: additions OK) to cover more controls/cases/pages those screens need. Then mx.5 effector / mx.6 build-deploy + design-sync re-align. [[mercury-economy-calibration]]

**mx.4 SHIPPED 2026-06-29 (`5d075477` — MY pathspec commit) via `/mercury-ship mx.4` under ultracode** (venus triad → Workflow build fan-out [enhancement serial + 9 story groups ∥, each adversarially verified] → completeness critic → Director gate + net-zero mutation spot-check → mars harden → Apollo BUILD-GRADE). Q1 ruling: mx.4 = **component stories (all 33) + the focused-trio additive enhancement**; apps-side Pages **RE-SEQUENCED to mx.5** (they wire @mercury/effector = mx.5's domain). Every component now has a co-located CSF3 `<Name>.stories.tsx` (**35 component homes + host Tokens = 36** via `sb:build`); controls restate the mx.2 contracts (typed enum const-arrays off the exported union = the guard). **The trio (new public surface, Q2 ruling):** Card gains optional `title`/`actions` header props (**NO new export**; `CardProps` `Omit<…,'title'>` shadows the native attr — verified 0 call sites use it); new **`ListRow`** (data-display) + **`MoneyInput`** (inputs, composes Input via its `leading` slot) → barrel **+4 EXACT names** (ListRow/ListRowProps/MoneyInput/MoneyInputProps), **additions-only** (this rung's barrel-diff is additions-only, NOT byte-identical like mx.3 — reading it as a byte-diff = false fail). Canon **D-10**. TWO LESSONS: (1) **stories are type-checked ONLY by `@mercury/storybook`'s tsc** — `sb:build`/esbuild STRIPS types without checking → added a root **`sb:typecheck`** script to the gate = the authoritative compile-time NO-INVENT enforcement (mutation proof: an invented `AlertTone` → `TS2322`). (2) **Entangled index:** the Operator pre-stages sibling-program docs (echo-persistence/echo_mq/redis-patterns) in the index out-of-band → a bare `git commit` SWEEPS them in; MUST use `git commit -F msg -- <mx.4 pathspecs>` (pathspec-limited) so their staged work is preserved + only mx.4 commits ([[jonnify-gitignore-repo-wide-trap]] is the sibling .gitignore trap; this is the sibling-index trap). Multi-word group titles Title-Cased hyphen→space (`data-display`→"Data Display"). NEXT = mx.5 (effector stories + apps-side Pages), mx.6 (build/deploy + design-sync re-align).

**mx.5 SHIPPED 2026-06-29 (`63d4d233` — MY pathspec commit) via `/mercury-ship mx.5` under ultracode** (Trio+Apollo: venus triad → mars-1 build → Director independent gate + net-zero mutation spot-check [ZERO findings → NO harden pass] → Apollo BUILD-GRADE). **Operator SPLIT the rung (Q1):** mx.5 = **effector-powered Storybook stories ONLY**; the apps-side Pages RE-SEQUENCED to **mx.6** (Q2 model = Storybook **page-stories** `*.stories.tsx` co-located in `apps/*/src/` — the host glob `../../*/src/**` ALREADY reaches the 5 apps; Q3 = the 5 apps are being **completely rewritten with Mercury DS then RETIRED from the workspace at program end**, `codemojex-node/apps/economy` OUT of scope + NOT glob-reachable), build/deploy + design-sync → **mx.7**. Six host-home CSF3 stories `apps/storybook/stories/effector/{Theme,Toast,Form,Strength,Cooldown,Formatter}.stories.tsx`, one per `@mercury/effector` adapter (roadmap row LAGGED at 4; source + canon §1 = SIX: theme·toast·createForm·strength·createCooldown·formatter), each wiring live Effector state (`effector-react` hooks; models at MODULE SCOPE, hooks INSIDE each render component — Storybook treats `render` as a component) into the real components. `sb:build` 36→**42** homes. **Risk NORMAL + surface FROZEN: the `@mercury/ui` barrel is BYTE-IDENTICAL this rung** (mx.4 grew it; mx.5 does not — the strongest form of the master invariant), and **ZERO host-config edit** (the effector dep/alias/path/glob/scripts were all already present from mx.4 — the whole code diff is the 6 story files). Arms (Director-ratified, all minor/conservative, NOT Operator-level): A=theme story AUGMENTS (a local `${theme}-theme` wrapper, NEVER `initTheme()` — no global `<html>`/localStorage mutation/leak) rather than replace the mx.3 toolbar decorator; B=host home (cross-component patterns, like `Tokens.stories.tsx`; preserves mx.4's 1:1 component↔story count); C=one file per adapter. Canon **D-11**. LESSONS/grounding: (1) venus's lag-1 reconcile CAUGHT a bad task hint — `formatter→MoneyInput` is WRONG (`createFormatterModel` wraps an Intl DATE/locale `Formatter`; `MoneyInput` takes no formatter) → corrected to wire **Stat**; (2) `passwordStrength` is a PURE fn (no store) → the story supplies the effector state via an inline `createStore`+`createEvent`+`useUnit`; (3) Formatter determinism = a FIXED `const SAMPLE=new Date(2026,0,15)` at module scope + locale pinned `en-US` (NEVER `new Date()` at load — and the llms `grep "new Date()"` gate is a substring check that trips on a COMMENT quoting the literal, so reword comments); (4) the story NO-INVENT guard = enum option arrays typed `as const satisfies readonly MonthFormat[]` / `const TONES:AlertTone[]=[…]` enforced by **`sb:typecheck`** (the only tsc that checks stories; mutation proof: an invented `"bogus"` MonthFormat → `TS2322`). Same entangled-index pathspec-commit discipline as mx.4 (Operator pre-staged echo-persistence/echo_mq-course/redis-patterns/codemojex-tma + `.gitignore`/`memory/*`/`vitest.config.ts` — all preserved). NEXT = **mx.6** (apps-side Pages = Storybook page-stories of the 5 rewritten+transient apps), then **mx.7** (static build/deploy + `.design-sync` re-align).

**2026-06-30 — Movement-III tail RE-SCOPED + mx.7.1 SHIPPED.** Operator-ruled "skip apps at all" → **mx.6 (apps-side Pages) DROPPED** (tombstoned; doc value moves to mx.9). New tail, all triads authored + committed (`d4a85a63`): **mx.7** = import the Claude-Design bundle's net-new components into `@mercury/ui` — a **5-batch epic** mx.7.1–mx.7.5 (30 net-new + 2 folds: TextArea→the live Textarea, ToggleGroup→the live Toggle's already-exported ToggleGroup); **mx.8** = enrich the Storybook stories (palette·roundings·variant·actions·real-world scenes); **mx.9** = `apps/showcase/` one comprehensive showcase app (library·docs·API·do/don't·recipes) replacing the retired apps. Each mx.7 batch ships through an **Operator → Agent → Agent loop** (Operator in the seat between batches). **THE IMPORT MODEL (load-bearing): design flows DOWN only** — source = the Operator's **claude.ai/design project** (`claude_design` MCP = the `DesignSync` tool) → regenerated into the untracked, regenerable bundle **`mercury/packages/mercury-ds/`** (53 inline-style `.tsx` prototypes + own tokens/mercAccent + a live showcase engine) → **TRANSLATED** (not dropped-in) into `@mercury/ui`'s `.mx-*` + token idiom. **`/design-sync` + the `DesignSync` MCP are FORBIDDEN** (never push local→remote). The bundle stays UNTRACKED (out of every commit via pathspec; a root-.gitignore entry is the Operator's out-of-band call — [[jonnify-gitignore-repo-wide-trap]]). **mx.7.1 BUILT + COMMITTED (`5f4847f1`, MY pathspec) via `/mercury-ship mx.7.1`** — Director-led Trio-lite (Director did the lag-1 reconcile + verify DIRECT; mars built the 5 homes). Batch 1 = the **5 foundational primitives** `Heading·Text·Label·IconButton·Separator` (+5), each a translated 4-file home. **Fork A → Separator NET-NEW** in `foundations` (richer API orientation·label·size·decorative); `Divider` untouched (never rename). Barrel **107→127** (+20 = 5 components + 5 Props + 10 aux types), 0 removed/renamed; tokens.css byte-unchanged. **TWO REUSABLE LESSONS (every import batch):** **(L1)** a multi-component shared enum (like `accent`) must be a **per-component INLINE union, NEVER a shared `export type`** — duplicate `export type` names via the barrel's `export *` COLLIDE and **silently drop** the name = a gate-invisible barrel REMOVAL; the proof is a collision sweep (`grep` the new export names OUTSIDE the new folders → empty). **(L2)** the **DM Sans 600 `@font-face` is ABSENT and stays absent** — weight 600 already rides `--font-primary` via the live local()/synthesis pattern (.mx-money__ccy etc.); adding a face for the nonexistent SemiBold woff2 is harmful (INV-7 = "fold only if the live layer LACKS it"). Also: `IconButton` shares `.mx-btn--<variant>` tokens (CSS `:hover`, no useState, `aria-label` after `...rest`, no `inverse`); `Heading` on DM Mono (`--font-secondary`) at display sizes 5–9, DM Sans 1–4; the bundle's arbitrary `color?:string` escape hatch is DROPPED (recolor via `accent` only). **Entangled 2-commit slice** (prior-turn spec ladder + this-turn build shared roadmap/progress, no hunk-staging): A=`d4a85a63` (mx.6 tombstone + mx.7/7.2-7.5/8/9 triads), B=`5f4847f1` (mx.7.1 build + triad + roadmap/progress fold). NOT pushed (echo_mq ahead 4). **NEXT = mx.7.2** (+10: Callout·Spinner·Skeleton·Blockquote·DataList·Code·Kbd·AspectRatio·Collapsible·ScrollArea). Open later-batch forks: bundle git-fate · overlay-floor ADR (7.4) · date-lib dep (7.3 — a `@mercury/core` hook; `@mercury/ui` must NOT import `@internationalized/date` directly) · mx.8 palette mechanism. [[mercury-economy-calibration]]

**2026-06-30 — mx.7.2 SHIPPED (`acf538dd`, MY pathspec) via `/mercury-ship mx.7.2`.** Import batch 2 = 10 simple feedback/display/layout primitives (Callout·Spinner·Skeleton·Blockquote·DataList·Code·Kbd·AspectRatio·Collapsible·ScrollArea), built in **2 sequential mars waves of 5** (10 ≈ 2× the connection-death ceiling → split; shared `index.ts`/`additions.css` force the waves SEQUENTIAL, not parallel). Director-led Trio-lite (Director reconcile + interim/final verify direct; 2 mars build waves). Barrel **127→160** (+33 = 10 comp + 10 Props + 13 aux), 0 removed/renamed. Distinctness (Director-ratifiable, both NET-NEW, siblings untouched): Callout≠Alert (inline emphasis intent×variant×size vs status msg), Collapsible≠Accordion (single disclosure vs set). Gate green; tokens.css byte-unchanged (no DM Sans 600). **THREE NEW LESSONS (carry to every import batch):** **(L4)** ground each prop in the **bundle `.tsx`, NOT the spec §3/§4 summary** — §3/§4 drifted 3× this batch (Callout has NO accent [§4 error]; Skeleton has NO variant [§3 stray]; DataList HAS labelWidth=140 [§3 omission]); the bundle source + real tokens win, invent nothing. **(L5)** **bundle glyph names ≠ the live `@mercury/ui` Icon set** (`keyof typeof ICONS`) — remap (`chevDown`→`chevron-down`, `warning`→`alert`, `circleHelp`→`help-circle`); the typed `name` prop means `sb:typecheck` backstops a miss. **(L6)** **translate runtime `<style>` injection OUT into `additions.css`** — the bundle's Skeleton/ScrollArea do `document.head.appendChild`; never in a live component (ScrollArea scrollbar → `.mx-scrollarea` webkit rules with semantic `--border-primary`/`--border-strong`, not raw `--slate`). Also: Code accent solid-ink uses `--slate-1/-12` (contrast swatch, canon-§6-permitted). NEXT = **mx.7.3** (+6 input/selection composites: DateField·Calendar·Checkbox/Radio groups+cards + folds TextArea→Textarea/ToggleGroup; the date-lib fork = a `@mercury/core` hook, `@mercury/ui` must NOT import `@internationalized/date` directly). [[mercury-economy-calibration]]

**2026-06-30 — mx.7.3.1 (DateField) + mx.7.3.2 (Calendar) SHIPPED; the workspace went MULTI-PROGRAM.** mx.7.3 Operator-SPLIT → 7.3.1/7.3.2/7.3.3. **The date pair, A2 ruled arm (a) BOTH** (`74021ebe` DateField; Calendar absorbed into the Operator's cleanup commits `e18a9fc7..67348fcd`): they COMPOSE Mercury's owned date foundation — `@mercury/core/src/internal/date-time/*` over `@internationalized/date` (a **core-only** dep) — via sibling composables **`useDateField`** (segment keystroke reducer) + **`useCalendar`** (42-cell month grid via `getLastFirstDayOfWeek`+`createFormatter`+month paging), surfaced on the @mercury/core barrel with the value kit (DateValue/CalendarDate/parseDate). `@mercury/ui` consumes dates THROUGH @mercury/core — **INV-6** (never `import @internationalized/date` in ui; the typed half + the picked half share ONE DateValue model). barrel 50→51 (7.3.1) then +1 Calendar (7.3.2). [[mercury-compose-mature-foundation]] confirmed. **FINDING (Apollo, INV-5 illusory — pre-existing Icon foundation):** `Icon.tsx` = `const ICONS: Record<string,ReactNode>` ⇒ `IconName=keyof typeof ICONS` widens to **`string`** → `<Icon name="anything"/>` type-checks clean; the "typed `name` backstops glyph validity" claim is a NO-OP. Glyphs verified MANUALLY (chevron-right real; **no chevron-left exists** → prev nav = chevron-right CSS-`rotate(180deg)`). Operator-fork pending (tighten ICONS via `satisfies`). venus.md gained the guardrail "a type-level gate must actually NARROW — `:Record<string,_>` widens `keyof` to string = false-green". **THE WORKSPACE IS NOW MULTI-PROGRAM (load-bearing):** a parallel **echo/codemojex backend** branch SHARES `mercury/` — added `packages/{core=@echo/core, fx=@echo/fx, echo-core, phoenix, phoenix_live_view}` + `codemojex-node/*` (workspace globs = `packages/* apps/* codemojex-node/{packages,apps}/*`). Mercury (mercury-core/ui/effector + apps/*) is INDEPENDENT. **The root `build`(`./packages/*`) + `typecheck`(`-r`) globs are now CONTAMINATED** — they sweep the @echo backend, whose node_modules are uninstalled → false "Cannot find module" failures. **VERIFY MERCURY VIA `@mercury/*` SCOPE** — added root scripts **`typecheck:mercury` / `build:mercury` / `verify:mercury`** (`pnpm --filter "@mercury/*"`, excludes @echo/@codemojex). **GIT LESSON:** `git commit -F msg -- <pathspec>` commits the **working-tree** state of those paths — committing while an external process is mid-deleting files records SPURIOUS deletions (commit A `0086ca9a` deleted 4 shipped files this way during the Operator's restructure; harmless — the Operator absorbed/fixed it building on top). **Recover-from-tree held a 3rd time:** Apollo's verify died to a parent-process exit, but its write-first §9 + mentor ledger survived on disk → recovered, BUILD-GRADE. NEXT = mx.7.3.3 (4 selection composites + 2 folds). [[mercury-compose-mature-foundation]]

**2026-06-30 — the Component Registry & Developer Reference doc created** (`be1d5a9a`, MY pathspec; Director-solo Duo via `/mercury-ship` with a custom docs arg — NOT a rung). New canon **`docs/mercury/mercury-ui.registry.md`** = the **as-built index** the other canon docs point at (roadmap Map gains the pointer). Three payloads: **§1** categorized catalogue — **as-built count = 52 shipped components across 9 groups** (NOT the "33" still cited across mercury.design/contracts/older memory — the 33 original + mx.4 ListRow/MoneyInput + mx.7.1 ×5 + mx.7.2 ×10 + DateField + Calendar = 52; each verified on disk with `.tsx`+`.stories.tsx`+`.prompt.md`), attributed to origin rung; +14 planned rows (mx.7.3.3/7.4/7.5). **§2** Storybook status, **2 columns** read from the `.stories.tsx` exports — *Surface* (variants·colors·actions = the axis stories Variants/Sizes/Accents/Tones/States) | *Usage stories* (the scenario stories WithHeader/KpiRow/Confirm/SettingsList…); every component has a `Playground` + axis + usage stories. **§4** the future **`apps/showcase` (mx.9, Squad-tier, NOT yet scaffolded** — current apps = echomq·fx-demo·marketing-site·mobile·storybook·website): **scaffolded from `mercury/packages/mercury-ds/project/showcase`** (the live-`.tsx` prototype: `index.html`+`library.jsx`+`loader.js` = a component browser using @babel/standalone IN-BROWSER, lazy-compiles each `.stories.tsx` via `window.MercuryLoader`, renders **Stories + the `.prompt.md` Docs tab**; `guides.html`+`app.jsx`/`pages.jsx` = Foundations colors/type/spacing + Patterns form/dashboard) → translated to a real Vite/React app resolving `@mercury/*` from SOURCE (no in-browser babel). The broad-audience reference surfaces **API·Props·Usage·Forms·Do-Don't·Composition map 1:1 onto the D-7 contract's 6 sections** (title/import · ## Props · ## Examples+stories · inputs+selection groups & the Form pattern · ## Notes · ## Composition) — the showcase is a PROJECTION of the registry, not parallel hand-docs. Docs-only, no code/barrel/gate touch. [[mercury-compose-mature-foundation]]

**2026-07-01 — mx.8.1 (Storybook enrichment · foundations slice) SHIPPED (`422ba2c6`, MY pathspec) via `/mercury-ship mx.8.1` — the FIRST production use of the new `venus-mercury` + `mars-mercury` role skills** (the prior-session calibration: two Mercury role skills on the shared `.claude/skills/mercury-program.md` floor, routed from the venus/mars charters on an `mx.*` slug — those skill files + `docs/aaw/aaw.specs-approach.md` stay UNCOMMITTED as a SEPARATE concern, not swept into the rung). **mx.8 opened as an EPIC** (sliced by component group, like mx.7): the cross-cutting Palette+Roundings globals build **host-wide** on the first slice (proven book-wide); per-component audit + scenes scope per group; mx.8.2+ as mx.7.4/7.5 land the library. mx.8.1 = **Palette + Roundings brand-only toolbar globals** (Operator-ruled Fork-1 arm a + brand-only: re-point ONLY the `--bg-brand` family + `--radius-2…-32`; NEVER `--bg-active` [interaction stays indigo] or `--radius-full` [circular affordances]) generalizing the mx.3 `withTheme` decorator on the ONE wrapper (3 globals: theme·palette·radius); the **foundations variant audit** (only Heading's `as`/HeadingTag was a real gap — Icon/Text/Divider/Separator already full-union); **2 foundations-in-context scenes** (`Scenes/Profile`·`Scenes/Article`, host-home `apps/storybook/stories/scenes/`, real @mercury/ui exports, cited screens). Barrel BYTE-IDENTICAL (mx.8 freezes the surface — the only `mercury-ui/src` edits are `foundations/*.stories.tsx`; rest is host config + scenes). **K-4 (actions) DEFERRED**, grounded: the 5 foundations `.tsx` declare zero `on[A-Z]` handlers (presentational primitives) → no actions to spy, no host dep; actions activates on a later interactive-group slice. **LESSONS:** (L-mx8.1a) **venus-mercury's lag-1 reconcile caught 2 grounding errors in the Director's brief** — (a) foundations `accent` (`iris|…|red`, NO "brand") reads `--<ramp>-11` DIRECTLY, ORTHOGONAL to the Palette global → a bare Heading shows no Palette change BY DESIGN, so INV-6 probes a real `--bg-brand` surface (Button/Card/Avatar, book-wide), NOT a foundations primitive; (b) the `IconName=string` illusory gate reconfirmed (the mx.7.3.2 finding) → Icon `name` enforced by a MANUAL set-equality grep, not the type. (L-mx8.1b) **INV-6 "globals-drive-tokens" (the no-op trap) headless proof pattern:** no browser in the Mercury island (no Playwright/`@storybook/test`), so PROVE the decorator EMITS the correct override maps (green→`rgb(var(--green-9))`, sharp→`0px`, never `--radius-full`/`--bg-active`) by executing paletteVars/radiusVars via tsx/node + the CSS-cascade reasoning (same mechanism as the gate-proven theme decorator); state the unmeasured browser-pixel step HONESTLY (live check = `pnpm sb:dev`). (L-mx8.1c) partial status ramps `green/orange/plum/red` ship ONLY `-3/-9/-11` (no `-10`/`-4`) → the remap uses all-six-safe steps + a `-10→-9` fallback gated by `HAS_10=[iris,indigo]`; a `--<status>-(10|4)` grep guards it. **Commit rule (Operator-clarified):** a verified+tested rung commits SOLO by the Director with NO `Co-Authored-By` trailer ([[director-commits-every-rung]]). Formation = Trio (venus-mercury architect → mars-mercury implementor → Director independent verify incl. LAW-1a mutation [invented `"h7"` → sb:typecheck TS2322 → revert net-zero]; clean first-pass = NO harden). sb:build 162 entries/61 homes (both scenes registered; Foundations still 6 — enriched in place). NEXT = mx.8.2+ (remaining groups' audit + scenes) or the parked mx.7.3.3/7.4/7.5 import batches. [[mercury-compose-mature-foundation]]

**2026-07-01 — mx.8.2 (Storybook enrichment · actions slice) SHIPPED (`41ca635a`, MY pathspec) via `/mercury-ship mx.8.2` (Trio: venus-mercury + mars-mercury; 2nd production use of the role skills).** The `actions` group (Button·IconButton·Link) — the FIRST interactive group, where mx.8's **K-4 (actions) ACTIVATES** (mx.8.1 deferred it as N/A). **D1 variant audit = a VERIFY** (all three ALREADY full-union from mx.4 → no top-up, unlike mx.8.1's Heading `as` gap; the only per-story edit is the spy). **D2 K-4 = a ZERO-DEP `fn()` spy** (`import { fn } from "storybook/test"` + `args:{onClick:fn()}` on meta) — the headline: **mx.8 §A Fork-5 (the actions host-dep, forward-authored as an Operator dependency-fork) DISSOLVED at ship** — SB **10.4.6** ships the fn spy + `action` logger + Actions panel in **CORE** (`storybook/test`+`storybook/actions` resolve as core subpaths; `main.ts addons:[]` fine), so NO package.json/pnpm-lock/main.ts edit (INV-6). Operator ruled zero-dep over a `@storybook/test-runner` headless-play-assertion (INV-7 = live-inspection residual, the mx.8.1 browser-pixel pattern). **D3 scene = `Scenes/Confirm`** (Director-ratified over `Scenes/Wallet`): actions-led confirmation sheet — icon-only IconButton toolbar + a Button variant-spread action bar + Links, cited Send.tsx:56-63/Home.tsx:33. Palette/Roundings globals **INHERITED host-wide from mx.8.1** → `preview.tsx` **UNEDITED** (the key contrast with mx.8.1, which BUILT them); barrel **BYTE-IDENTICAL**. **LESSONS:** (L-mx8.2a) **the host-tsc `paths`-alias gotcha for a NEW `storybook/*` core-subpath import from a mercury-ui story** — `apps/storybook/tsconfig.json`'s `include` glob pulls the mercury-ui stories, but `tsc` resolves a bare specifier from the SOURCE-FILE's dir upward, where `storybook` isn't symlinked (pnpm strict) → TS2307; fix = a `paths` alias mirroring the existing line-12 `@storybook/react-vite` entry (`"storybook/test":["./node_modules/storybook/dist/test/index.d.ts"]`) — a compile-time TYPE-ALIAS, NOT a dep (INV-6 holds), **group-agnostic so mx.8.3+ inherit it**. **The asymmetry:** `require.resolve('storybook/test')` was GREEN (node resolves from the consuming app dir) while `tsc` was RED (from the source-file dir) — a runtime resolve is NOT a proxy for tsc reachability on a core-subpath import. (L-mx8.2b) **the `IconName=string` untyped landmine re-bit** (the mx.7.3.2/mx.8.1 finding) — the scene's Icon glyphs (copy·cog·close·check) pass `sb:typecheck` regardless (`keyof Record<string,ReactNode>` widens to string), so NO-INVENT on glyphs is a MANUAL set-equality vs `Icon.tsx`'s ICONS keys (all 4 real). Director independent verify: gate re-run + **LAW-1a mutation** (bogus `ButtonVariant`→TS2322→net-zero revert) + the glyph set-equality; **clean first-pass ⇒ NO harden** (right-sized — no ceremonial Mars-2). **Apollo NOT spawned** — 2 known findings (L-2 + the `@mercury/*` gate-pin) folded direct by the Director (cost-discipline, [[right-size-formation-and-write-only-artifacts]]). **`@mercury/*` gate-pin reconfirmed:** the raw `./packages/*` sweep is multi-program (12 projects incl. `@echo/fx` Rust/WASM, which fails from HEAD) → verify via `--filter "@mercury/*"`. Entangled tree (pre-existing codemojex/admin + phoenix + fx, + the aaw ledger) left UNCOMMITTED as separate concerns; the commit is the 10-file measured surface only, NO Co-Authored-By ([[director-commits-every-rung]]). NEXT = mx.8.3+ (remaining interactive groups selection·inputs·feedback·data-display·navigation·overlay, one per slice, inheriting the zero-dep fn() pattern) or the parked mx.7.3.3/7.4/7.5 import batches.

**2026-07-01 — mx.7.4 (the overlay-floor + the dialog family + the strong effector bridge + the showcase foundation) SHIPPED in TWO sequenced commits via `/mercury-ship mx.7.4` (Squad-tier — the first HIGH-risk Mercury rung, mandatory Apollo verifier).** Commit #1 `e6cb9b68` = the packages; commit #2 `3d9c90a4` = the showcase foundation (the Operator ruled SEQUENCED — packages first, then the app). Formation: venus-mercury (triad reconcile + §E/§F authoring) → **mars ×4 waves** (W1 core floor · W2 ui overlays · W3 effector bridge = commit #1; W4 showcase = commit #2) → Director independent verify (full `@mercury/*` gate + LAW-1a mutation) → **Apollo adversarial a11y** → 2 pathspec commits. **Forks (Operator-ruled at Bootstrap via ONE AskUserQuestion):** §A overlay-floor home = **a shared headless floor in `@mercury/core`** (D-5 barrel widening, the useDateField precedent): `useFocusTrap` (real Tab-cycle + escaped-focus pull-back + focus-return via `previouslyFocused`) · `useDismiss` (pointerdown+Escape, composing `dom.isClickTrulyOutside` + an `ignore` list) · `useAnchoredPosition` (**measurement-based** `getBoundingClientRect`+`position:fixed`, viewport-clamped, +pointer case for 7.5 → returns `{style}`, so anchored panels MUST portal) + `useId`/`useArrowNavigation` published; the `<Portal>` lands in `@mercury/ui` (core is **react-peer-only, NO react-dom**). §C Dialog↔Modal = **add Dialog net-new** (monolithic; Modal byte-untouched; reuses `.mx-modal` + composes mx.7.1 Heading/IconButton + a description slot + the focus-trap the prototype lacked). §D positioning = **hand-rolled** (no npm dep). §E the effector bridge (Operator chose the RICHER option) = a NEW 8th `@mercury/effector` adapter `disclosure.ts`: `createDisclosure()` factory (open/close/toggle + `$open` + `useOpen`, the `createCooldown` idiom) + a GLOBAL overlay-stack + **padding-compensated body-scroll-lock singleton** (`pushOverlay`/`popOverlay`/`$openOverlays` LIFO/`$anyOverlayOpen`/`$topOverlay`/idempotent reversible `initOverlayLock`, the `initTheme` idiom) — the overlays stay PRESENTATIONAL, `@mercury/ui` gains NO effector dep (**INV-EFFECTOR**), the bridge drives them from OUTSIDE (proven live by the `Effector/Overlay` story + the showcase Overlays demo). §F the showcase = a NEW `@mercury/showcase` Vite/React app (source-resolving `@mercury/*` mirroring apps/mobile; shell + theme toggle via the effector `theme` adapter + an Overlays demo driving the mx.7.4 overlays through §E — two modals stack+lock, the Popover doesn't; the full per-component Reference stays **mx.9**). Barrels: `@mercury/ui` +3 · `@mercury/core` +floor hooks · `@mercury/effector` +`disclosure` (0 removed/renamed, Modal/Tooltip byte-present). **LESSONS:** (L-mx7.4a) **recover-from-tree held a 4th time + caught INCOMPLETE work** — mars-w1 went idle WITHOUT surfacing its report (the ECONNRESET final-report-loss), AND had left the D-5 barrel UN-widened (the floor hooks existed as files but weren't exported → W2 couldn't import them); recover-from-tree (read the files + gate) IS the mandatory Director verify, and I folded the 6-line barrel fix into W2's Step 0 (a peer writes it — LAW-1a) rather than spin a cycle. (L-mx7.4b) **verify-the-verifier cuts BOTH ways** — venus-mercury's reconcile flagged the registry §4 apps list "stale" (fx-demo/marketing-site/website "don't exist"); a `find` proved all 6 apps exist → the FLAG was the false positive; applying it on trust would have DELETED real apps from the registry (a peer's reconcile FINDING can be wrong, not just its build). (L-mx7.4c) **Apollo's adversarial a11y caught the class of defect the Squad gate exists for** — the Dialog `A11yTrap` play proved role+aria-modal + trap-wrap but NOT focus-RETURN; INV-A11Y's charter is "a check counts only if it RUNS" (behavior implemented but unasserted = the invariant unsatisfied); remediated STORY-ONLY (open from a real trigger → `{Escape}` → `waitFor(()=>expect(trigger).toHaveFocus())`). No test-runner in the island → play fns use `storybook/test` (typecheck + interactions-panel/browser residual, the mx.8.1/8.2 posture); mars-harden correctly **REFUSED to add the Popover focus-return assertion** (the outside `<button>` grabs focus, racing the effect-cleanup — a flaky proof is worse than an honest gap; Popover's required clause aria-expanded+outside-dismiss already MATCH). (L-mx7.4d) **TWO commit-hygiene entanglements excluded (no `git add -p` non-interactively):** (a) `channel.ts` + its barrel line = a pre-existing UNTRACKED effector adapter sharing `index.ts` with my `disclosure` line (committed HEAD barrel = **6**, not 7 — venus's "prior 7 byte-present" was imprecise, Apollo caught it) → I removed the channel line for the commit then RESTORED it after (net-zero on index.ts; channel stays the Operator's uncommitted concern); (b) `pnpm-lock.yaml` was pre-modified at session start + mars-w4's workspace `pnpm install` restored ~1500 entangled lines on top → EXCLUDED (the Operator committed it themselves in `a908c882`, interleaved between my two commits — same pattern as the codemojex `echo/**` pre-stage). Both commits = the measured surface only, NO Co-Authored-By ([[director-commits-every-rung]]). Gate: `@mercury/*` typecheck+build + apps build + sb:typecheck + sb:build (all 4 new homes) + `@mercury/showcase` typecheck+build; INV-2/3/4/5/FLOOR/EFFECTOR clean; LAW-1a mutation (bogus PopoverPlacement→TS2322→net-zero). NEXT = **mx.7.5** (menus/hover/nav — Dropdown·ContextMenu·HoverCard·LinkPreview·Menubar·TabNav, CONSUME this floor via useArrowNavigation + the remaining useAnchoredPosition cases; Squad-tier) OR mx.8.3+ (interactive-group enrich — the overlay group now shippable) OR mx.9 (grow the showcase into the full per-component Reference). [[mercury-compose-mature-foundation]] [[spawn-resilience-effective-messaging]]

**2026-07-01 — mx.7.5 (menus·hover·nav — CONSUME the 7.4 overlay-floor) SHIPPED (`84a7204d`, MY pathspec) via `/mercury-ship mx.7.5` (Squad-tier, HIGH-risk, mandatory Apollo; the LAST overlay/menu/nav import batch).** Import batch 5 = the **6 keyboard-navigated overlays** `Dropdown·ContextMenu·HoverCard·LinkPreview` (overlay/) + `Menubar·TabNav` (navigation/ — the ONE fork, Operator-ruled `navigation/` over `overlay/`: the barrel encodes the export NAME, group = folder + story-title only). Formation: venus-mx75 (reconcile BUILD-GRADE zero-drift + a write-ready build brief) → **mars ×2 waves** (W1 = the 3 menus sharing the menu machinery; W2 = the 2 cards + TabNav) → Director full verify → **Apollo adversarial-a11y = BUILD-GRADE, zero findings**. Barrel **+6/−0 → 61 folders** (overlay 5→9, navigation 3→5); scoping = purely `@mercury/ui`-additive, **NO `@mercury/core` change** (the 7.4 floor already published all it needs). **The 7.4 floor COMPOSED not re-rolled** (INV-FLOOR-CONSUMED grep-proven): menus use `useDismiss`/`useAnchoredPosition`/`useArrowNavigation`/`useId`+`<Portal>`; hover cards are non-modal (`useAnchoredPosition`+local hover/focus timers, NO trap); TabNav uses NONE of the floor (inline `<a aria-current>`) + restores the `:focus-visible` ring the prototype dropped. accent class-driven (`.mx-<name>--accent-<id>`→`--<ramp>-9/-11`, NO mercAccent — L1 inline-union held); static `@keyframes`; ContextMenu scroll-dismiss = a LOCAL effect (useDismiss owns pointer/Escape — spec-sanctioned). **LESSONS:** (L-mx7.5a) **`useArrowNavigation` is NOT a React hook** despite the `use*` name — a pure event handler `(e,currentEl,parentEl,{candidateSelector,focus:true})→HTMLElement|null` CALLED inside a menu-panel `onKeyDown`, never at render top-level (as-a-hook breaks rules-of-hooks). (L-mx7.5b) **Menubar rules-of-hooks: floor hooks can't be called inside `menus.map()`** → each top menu is an internal `MenubarTop` component (one file) for stable call-sites; and the `--accent-<id>` class must ride BOTH the bar AND each **portaled** panel (a `<Portal>`ed submenu is not a DOM descendant of the bar → the accent custom-property won't cascade — Mars caught this). (L-mx7.5c) **the a11y `play` MUST query the portaled panel via `within(canvasElement.ownerDocument.body)`, not `within(canvasElement)`** — the panel renders at `document.body`, OUTSIDE the story canvas; a canvas-scoped query finds nothing and INV-A11Y silently passes proving nothing. (L-mx7.5d) **"import complete" was spec/Apollo overstatement** — the 7.5 spec (authored 2026-06-29, PRE the 06-30 7.3.3 carve-out) says "closes the import"; the Director folded the truthful status: overlay/menu/nav/foundations/feedback/display batches complete, **only mx.7.3.3 (4 selection composites + 2 folds) remains**. Apollo's a11y-runner mentoring diff (label a "fires/runs" INV *verifier-executed* absent a test-runner) is **PROPOSED to venus-mercury but the harness DENIED the Director applying it** — a peer-proposed `.claude/skills/` config edit needs Operator approval (correctly fenced; surfaced to rule). 30-file measured surface only (Operator's staged cm-*/memory/node preserved), NO Co-Authored-By ([[director-commits-every-rung]]). **mx.9 PRE-BUILD BLOCKERS ANALYSIS on file (Operator-requested this session):** the retired `apps/showcase` foundation (`3d9c90a4`) was intentionally RETIRED (`0a5e59a1`) → mx.9 is a from-scratch build; the mx.9 spec triad exists (SOLID-FORWARD, authored 2026-06-29) + rules the architecture as forks — **Fork C→B: vite source-alias NOT the bundle `loader.js` @babel/standalone in-browser transpiler; INV-6: DERIVE the registry by glob over `@mercury/ui`, NOT the bundle `registry.ts` (which renders the bundle's OWN inline-styled components); Fork A→MVP-first (the browser spine serves all 5 surfaces as contract cuts)**. **THE SHARPEST BLOCKER (spec now stale): 11/61 `@mercury/ui` stories RUNTIME-import `storybook/test`** (`{fn}` mx.8.2 + `{expect,userEvent,…}` plays mx.7.5) — the spec's K-3/INV-7 "no Storybook runtime, type-only erased" is FALSE now → `import.meta.glob`-ing the stories pulls `storybook/test` into the showcase build → FIX = a `storybook/test`→no-op-shim vite alias (plays never run in the showcase; keeps INV-7 zero-dep). Other blockers: recover the `3d9c90a4` scaffold (discard its hardcoded routes); lazy glob (`{eager:false}`); contract cross-link `.md`→in-app-route rewrite in the ported `renderMarkdown`; Foundations/Patterns (pages.jsx 816 + mercurized*.jsx 895) DEFERRED by MVP-first. NEXT = **mx.9 (showcase; rule the `storybook/test`-shim first) OR mx.8.3+ (overlay/nav groups now shippable) OR parked mx.7.3.3.** [[mercury-compose-mature-foundation]] [[spawn-resilience-effective-messaging]]

**2026-07-01 — mx.10 AUTHORED (SPECCED, NOT built) — a cross-cutting TOOLCHAIN rung (spec-authoring only, Director-authored Duo, no build).** Not a Movement III feature rung: the workspace **dependency reconciliation + the vite 6→7 lift** via a pnpm **`catalog:`**. Triad `docs/mercury/specs/mx.10/{mx.10.md,.stories.md,.llms.md}` (6 aaw gates green: voice/structure/traceability/fences/links/format; K-/INV-/S-/R-/AS- id scheme matching the mx.9 exemplar); roadmap row + orthogonality note folded (`mercury.roadmap.md`). **3 shaping forks Operator-ruled (AskUserQuestion):** (1) scope = `packages/*`+`apps/*`+root, **NOT `codemojex/**`** — a sibling `/cm-ship` rung migrates codemojex; the `catalog:` block lives in the SHARED `pnpm-workspace.yaml` (so it's available to codemojex) but only mercury manifests flip to `"catalog:"`; the transitional vite-6-codemojex beside vite-7-mercury is SAFE (per-package build-time concern, each ships a built artifact); (2) mechanism = pnpm `catalog:` — **7 entries**: typescript `~5.9.3` (**TILDE** — TS≠semver, last-session principle), vite `^7`, vitest `^4`, react/react-dom `^19`, jsdom `^26`, @vitejs/plugin-react (v7-compat); (3) shape = ONE rung, **3 internal phases** (hygiene on vite6 → vite7 major → build proof). **Grounded landscape (the reconcile subject):** TS declared **6 ways** (`^5.5.4`·`^5.6.3`·`^5.7.0`·`^5.7.2`·`^5.8.3`·`~5.9.3` — resolves to a single `5.9.3` by pnpm dedup LUCK, not policy); vitest a **LIVE dual-major** (`3.2.6` root + `4.1.9` in 4 leaves game/liveview-boot/phoenix/phoenix_live_view = the jest-dom trap live in the lockfile); vite uniform `6.4.3`; react `19.2.7`; `splitVendorChunkPlugin` UNUSED (one fewer v7 hazard); @vitejs/plugin-react uniform `^4.3.3`. **Top risk = `@storybook/react-vite ^10.4.6` on vite 7** (the largest vite consumer — a Storybook bump joins the rung or a NAMED blocker; never silently downgrade vite→6). Gate = `verify:mercury` + `pnpm --filter "./apps/*" build` + `sb:build` on vite7/vitest4 + barrel byte-identical (INV-1, a dep rung moves no export) + boundary (no codemojex manifest edited) + `@echo/fx` pre-existing red BASELINED (not attributed to the lift). **Ship via `/mercury-ship mx.10`** (NORMAL-to-elevated; a verifier optional unless Storybook forces a wider change). phoenix/phoenix_live_view manifests are working-tree-dirty (in-flight) — attribute against the pre-spawn baseline at commit. [[mercury-dual-vitest-jestdom-trap]] [[codemojex-tauri-track]]

**2026-07-01 — mx.7.3.3 SHIPPED (via `/mercury-ship mx.7.3.3`) — CLOSES the mx.7 import epic.** The last import batch: **+4 net-new selection composites** (`CheckboxGroup·CheckboxCards·RadioGroup·RadioCards`, barrel 61→**65 folders**) + **2 additive folds, 0 new export** (`Textarea`+=`size`; `ToggleGroup`+=`accent`+group-`disabled`, in `selection/Toggle/Toggle.tsx`). Commits `3062d5ed` (rung, 27 files) + `fc18a6c4` (registry — SEPARATED because it still carried the prior mx.9 §4 refresh; a single file can't be split into two commits, so scope-per-concern = leave it out of the rung commit). **Duo+ formation** (Director + Mars two-pass; NORMAL → no in-pipeline Apollo; the architect leg folded into Bootstrap — the spec was build-ready + the Director did the lag-1 reconcile; right-size-formation over the spec's suggested full Trio). **Operator forks:** A1 = compose the live primitive in a card shell; A3 = fold the ToggleGroup enrichment (FORCED by the master invariant — a rename or 2nd export is illegal, so widen the existing interface). **Craft lessons:** (L-a) the `*Cards` **nested-`<label>` hazard** — the live `Checkbox`/`Radio` ARE `<label>`s, so the card shell is a `<div>` and the card body rides the primitive's `label` slot (native label toggles → no double-fire, native keyboard, no invalid nested labels); (L-b) **`accent` realized at the GROUP wrapper class** (`.mx-<group>--accent-<id>`, the mx.7.1 6-ramp), NEVER forwarded to the primitive (they carry no `accent` prop — INV-5 `grep '<(Checkbox|Radio)[^>]*accent='` proves it); (L-c) a **fold widens the exported interface → 0 barrel-name delta** (`size?`/`accent?` become properties on `TextareaProps`/`ToggleGroupProps`; `getExportsOfModule` unchanged — this is why "fold, don't add-distinct" is the only invariant-legal move for a name-collision); (L-d) the `IconName` NO-INVENT gate is **illusory** (`keyof typeof ICONS` widens to `string`) → verify glyphs by grep vs the real ICONS keys, NOT tsc (the mutation spot-check confirmed tsc DOES catch a bad `accent` union but would NOT catch a fake glyph). **GATE FINDING (reinforce the standing note):** the mercury-ship skill + `mercury/CLAUDE.md` state the gate as `pnpm --filter "./packages/*"`, but that path-glob catches **`@echo/fx`** (Rust/WASM at `packages/fx`, fails from HEAD on a missing `wasm32-unknown-unknown` target) → the CORRECT ship gate is the scoped `typecheck:mercury`/`build:mercury` (`--filter "@mercury/*"`). A skill/CLAUDE.md wording correction is PROPOSED (Director finding, Operator to rule). **The mx.7 import epic is COMPLETE** (30 net-new + 2 folds across 5 batches 7.1/7.2/7.3.{1,2,3}/7.4/7.5). Frontier → **mx.9** (showcase) per the Operator sequence `mx.7.3.3 → mx.9 → mx.8.3+`.

**2026-07-01 — theme posture + the dark-`indigo` limitation (audit finding, Operator-confirmed).** Mercury is **light-primary, dark-as-toggle**: the design bundle's showcase (`packages/mercury-ds/project/showcase`) serves `<html class="light-theme">` with a persisted dark toggle (`app.jsx` sets `documentElement.className`); 19/20 preview stages default `"light"`. Consequence — the `.dark-theme` block in `packages/mercury-ui/src/styles/tokens.css` is **best-effort**: it darkens the semantic tokens + brand `iris` (full 1–12) + the four status families (`green/red/orange/plum` at `-3`/`-11`), but the secondary accent **`indigo` is darkened only at `-9..-12`** — its `-1..-8` (incl. the soft-bg `-3`) carry the LIGHT values into dark. So `accent="indigo"` on a soft-bg surface (e.g. the `ToggleGroup` on-state, which reads `--<id>-3`) renders light-in-dark. **This is INHERITED from the bundle** (its own `.dark-theme` in `colors_and_type.css` omits dark `indigo-1..-8` too), and there is **NO local groundable source** (`@radix-ui/colors` is not installed; Mercury's dark scales are custom-tuned, not verbatim Radix — dark `iris-3 = 32 34 72`). **DECISION: log + defer** — dark is a secondary toggle, no groundable value, and design-flows-DOWN says color is authored upstream, not net-new in `@mercury/ui`. Do NOT invent the RGB. Revisit only if **mx.9 makes dark first-class**, or the designer backfills the bundle's dark `indigo` (then it flows down on sync). The other 5 accent families are dark-complete.

**2026-07-02 — mx.9 SPLIT into the mx.9.1–mx.9.5 SUB-EPIC (`8ebf2442`, docs-only, via `/mercury-ship mx.9`).** Operator directive "decompose mx.9 onto 5 rungs"; 3 forks ruled by AskUserQuestion BEFORE authoring: **axis = layered engine** · **all 5 full triads** (9.1/9.2 BUILD-READY, 9.3–9.5 SOLID-FORWARD re-sharpened at each ship) · **stewards ratified** (B `apps/showcase`/`@mercury/showcase`; C vite source-alias + bundle-shell chrome, `loader.js` REJECTED; D prototypes = untracked seeds incl. `fx-demo`; E zero new dep; Fork A resolved BY the split = staged spine-first). **The ladder:** 9.1 spine (echomq-mirror scaffold + the `storybook/test` no-op shim in vite alias + sanity page + `dev:showcase` :5176 + apps gate 2→3, NORMAL/Trio) → 9.2 derived registry + shell (two lazy `import.meta.glob`s `../../../` from `src/`, GROUP_ORDER over the 9 real segments, tab stubs, theme MECHANISM, keys `mx-showcase.{route,theme}.v1`, NORMAL/Trio) → 9.3 live-stories (CSF interpreter, `play` never runs, **THE SHIM LIVENESS GATE**: all 65 modules awaited, the 11 `storybook/test` importers = the proof surface, stub-throw = ESCALATE, ELEVATED/Trio) → 9.4 contract cuts (zero-dep renderer, no `dangerouslySetInnerHTML`; **heading census: Props/Notes/Examples/Composition 65/65, `## The enum language` 56/65** → missing-section-renders-EMPTY is a LIVE 9-contract case, NORMAL/Trio) → 9.5 closer (seed-skinned token-expressed chrome, dual-theme acceptance w/ the indigo-3 limitation EXPECTED, whole-epic INV re-run, **Squad+Apollo — the epic's verifier mandate relocated here**; Movement III closes). **Lag-1 corrections baked (21 `[RECONCILED 2026-07-02]` marks in the epic):** `dev:showcase` is an ADD (script deleted with the retired apps); the 11 VALUE-imports (the "type-only erased" claim was stale); a new workspace package ALWAYS writes a pnpm-lock importer block (the epic's "lockfile untouched" was wrong); the stylesheet arrives via the barrel side-effect import (`index.ts:12` → no explicit css import in apps); ~66→65. **Duo formation** (Director + venus-mx9 = the NEW `venus-mercury` skill's first outing — performed excellently: byte-exact write-ready briefs, honest MANUAL-gate marking, the census strengthening) in **2 write-ready waves**; one crossed heartbeat/report message handled by tree-probe + reference-not-repaste + a fixed reply token. Records folded (roadmap SPLIT row + 5 sub-rows; progress bar; registry §4; the mx.10 note gained ONE sentence: 9.1 pins vite ^6, later sub-rungs inherit the `catalog:` if mx.10 ships mid-epic). Pre-existing dead link mx.3.md:69 (→ the retired showcase vite.config) SELF-HEALS at 9.1's build — left alone. **NEXT = `/mercury-ship mx.9.1`.**

**2026-07-02 — mx.9.1 (the showcase spine) SHIPPED (`1b8f3cb6`, 12 files, via `/mercury-ship mx.9.1`).** First build rung of the mx.9 sub-epic; **Duo+ pass-1 clean — the Mars-2 harden pass COLLAPSED (zero findings)**, the architect leg discharged at the split (the write-ready brief built byte-exact, zero usage adjustments). As-built: `apps/showcase` (7 files — echomq-mirror alias+`paths` diff-IDENTICAL; the 6-export `storybook/test` shim; `light-theme` sanity page, NO css import — the barrel side-effect delivers the stylesheet, dist CSS ≈87.6 kB) + root `dev:showcase` :5176. Gate: typecheck:mercury/build:mercury 0 · showcase 0 · apps gate **Scope 3 of 21** (`showcase·echomq·mobile`) · barrel 0-diff · LAW-1a alias-mutation net-zero · dev-smoke proved `/@fs/…/src/index.ts` source rewrite with core's `dist/` ABSENT. `pnpm install` importer-only (downloaded 0, added 0 external). **Two ship findings:** (F-a) the consume-down grep needs `--exclude-dir=node_modules` — post-install pnpm SYMLINKS workspace packages under the app, so the raw grep follows links into `packages/mercury-ui/CLAUDE.md` prose quoting the design-sync law (2 false hits; folded into mx.9.1 INV-6; **apply to the 9.2–9.5 ladders at dispatch/re-sharpen**); (F-b) **the lockfile was EXCLUDED from the rung commit** — it carried a ±588-line uncommitted sibling delta (one file ≠ two concerns); the importer block regenerates deterministically on `pnpm install` and ships with the sibling's commit (Operator-surfaced). Mars-mercury skill first outing: excellent (honest AS-3 posture, the symlink diagnosis, clean server teardown). **NEXT = `/mercury-ship mx.9.2`** (the derived registry + shell; its dispatch inherits the scoped-gate + grep corrections).

**2026-07-02 — mx.9.2 (the derived registry + shell) SHIPPED (`38a55e46`, 12 files, via `/mercury-ship mx.9.2`).** Second build rung of the mx.9 sub-epic; **Duo+ pass-1 clean, Mars-2 collapsed** — the architect leg collapsed at Bootstrap (WRITE-READY triad + an all-MATCH Director ground-truth reconcile: 9 groups, 65/65 story↔prompt, tokens, `index.ts:12`, theme classes at `tokens.css:84/:303`). NOTE: the Operator asked `/mercury-ship mx.9.3` but 9.3 HARD-GATES on 9.2 (unbuilt) — AskUserQuestion ruled "ship mx.9.2 only". As-built: `registry.ts` (brief-transcribed: two lazy `import.meta.glob` three-up, segment-parsed, 9-group as-const order, unknown groups appended, 65-parity DERIVED) + `shell/{Sidebar,Topbar,ComponentPage,Home}` + persisted `mx-showcase.{route,theme}.v1` (theme boot-applied pre-mount) + token-only `showcase.css`. INV-5 held (zero loader calls; every story/prompt chunk code-split lazy). **Three Mars realizations Director-ACCEPTED (recorded in the spec §7):** (1) NEW in-src `vite-env.d.ts` — the 9.1 scaffold's tsconfig `"types":["react","react-dom"]` makes `import.meta.glob` TS2339; the vite/client triple-slash is the in-boundary fix (**a scaffold rung that pins `types` should carry vite-env.d.ts from birth**); (2) the brief's `parse()` literal fails strict indexed access → strict-safe realization (**brief-carried code must compile under the app's strict tsconfig — a venus-mercury craft note**); (3) Home `main`→`section` (App owns the landmark). Verify: `@mercury/*`-scoped gate green (raw `./packages/*` build still trips pre-existing `@echo/fx` wasm) · S-1 probe run TWICE (Mars `foundations/__Probe__` + Director adversarial `zz-probe/` UNKNOWN-group pass proving the appended-derived path) · every token name in showcase.css grep-verified to exist (a `var(--typo)` fails silently) · LAW-1a `REGISTRY`-rename → TS2724 → net-zero. Trailer convention reconfirmed: rung commits carry NO Co-Authored-By (amended to match 9.1). Apollo mentor pass folded direct by the Director (2 craft notes above; cost-discipline per [[right-size-formation-and-write-only-artifacts]]). **NEXT = `/mercury-ship mx.9.3`** (the live-stories surface — NOW unblocked: first loader invocation, the CSF interpreter, THE SHIM LIVENESS GATE across all 65 modules with the 11 `storybook/test` importers as the proof surface; ELEVATED/Trio + deepened verify; read the untracked seed `packages/mercury-ds/project/showcase/library.jsx` at ITS ship per the re-sharpen).

**2026-07-02 — mx.9.3 (the live-stories surface + THE SHIM LIVENESS GATE) SHIPPED (`1dffadf9`, 8 files, via `/mercury-ship mx.9.3`).** Third build rung; **full Trio** (the SOLID-FORWARD triad mandated a ship-time re-sharpen, so the architect leg did NOT collapse) — Venus re-sharpen → FORK-1 Operator-ruled → Mars pass-1 clean → Director deepened verify zero findings → harden collapsed. **The re-sharpen census over all 65 story files KILLED 3 authoring assumptions:** (1) `parameters.summary` = bundle-INVENTED (0/65 — the seed's own world); (2) `render` is DOMINANT (65/65 files, 115 sites, 3 META-level: Tabs/Pagination/TabNav → resolution `story.render ?? meta.render`); (3) the seed's `story.args`-only read renders every `Playground: Story = {}` (65/65) EMPTY → **the merge law `{...meta.args, ...story.args}` is binding**; also resolved: ZERO non-story exports tree-wide (named exports = stories), title = `story.name ?? key` (8 a11y stories; never `args.name`). **FORK-1 (5 meta-level decorator files: Separator/Divider/Progress/PasswordStrength 320px frames + AuthLayout 760px `Frame`) RULED Arm A** — support exactly the censused `(Story) => JSX` length-1 meta-level shape; beyond it renders unwrapped. As-built: `src/lib/storyRender.tsx` (~160 lines; resolution inside a `ResolvedStory` component so decorator calls + mount + missing-component throw all land INSIDE the per-story boundary; cards keyed `entryKey/storyKey` against stale boundary state; StoriesPanel entry-keyed lazy effect, alive-guard, NO MOD_CACHE port) + stub swap + `showcase-story*` tokens-only css. **INV-2 PROVEN TWICE:** Mars transient vitest sweep + Director independent STRONGER sweep (65/65 boundary-catch-free, 11 importers by name, merged-args/meta-render/both-decorator DOM witnesses, `fn()` click no-op, containment). Probes ×2 each (`spyOn`+`screen` → LOUD at vite import-analysis; contained render throw at jsdom). **LAW-1a mutation was BEHAVIORAL, not type-level** (an interpreter defect is type-invisible): dropping the merge failed 4/5 Director tests INCL. the liveness sweep itself (`inputs/Select` `undefined.map`) → net-zero revert. **LESSONS:** (L-mx9.3a) **vitest include paths resolve against the RUN CWD, not the config file's dir** — a root-run `--config apps/showcase/vitest.X.config.ts` needs ROOT-RELATIVE `include`; the sweep config merges the APP's vite.config (that's what routes `storybook/test` → the shim); vitest/jsdom/@testing-library resolve as ROOT devDeps only (pnpm strict — never add app devDeps, ruling E + the dual-vitest trap). (L-mx9.3b) **the verify sweep must carry CONTENT witnesses, not just mounts** — a mount-only sweep misses seed-class defects (empty renders pass); witness assertions (Button meta-args children, the 320px/760px decorator wrappers in the DOM) are what made the mutation catchable. (L-mx9.3c) the Operator pre-staged a 40+-file codemojex batch mid-session — the pathspec COMMIT (`git commit -- <paths>`) cleanly selected only the rung; the batch stays staged untouched. Manual residuals stated honestly: the `:5176` visual pass + the network-panel lazy observation. **NEXT = `/mercury-ship mx.9.4`** (the contract surface — the Docs stub + the `loadPrompt` loaders await; SOLID-FORWARD, re-sharpen at ship incl. the 56/65 `## The enum language` census; then mx.9.5 closes Movement III, Squad + Apollo).

**2026-07-02 — mx.9.4 (the contract surface) SHIPPED (`289db2a5`, 9 files, via `/mercury-ship mx.9.4`).** Fourth build rung; **full Trio** (SOLID-FORWARD → ship-time re-sharpen) — venus-94 census → FORK-1 + the xref rule Operator-ruled → mars-94 pass-1 clean → Director 8-witness corpus sweep → harden collapsed. **The construct inventory over ALL 65 contracts KILLED 5 seed assumptions:** (1) 475 continuation lines across 65/65 files — the seed's flat list loop = structural corruption of EVERY contract → the 1–3-space JOIN-into-li law; (2) ~200 links, 100% relative `.prompt.md` xrefs → non-navigating spans (title=target), `http(s)` real anchors (Operator-confirmed); (3) `*italic*` real (16 files); (4) bold WRAPS code in 34/65 → the mask-first sentinel inline pass (code atomic; the seed only survives this via its banned innerHTML); (5) `\|` load-bearing in 50/65 table cells. Out-of-scope constructs → the paragraph-fallback law (content never silently drops). **FORK-1 (epic §7-A, the four-view grain) RULED Arm A** — nested sub-tabs inside the Docs tab; route/`Tab` union/`App.tsx` untouched; mx.9.5 owes the two rows distinct skinning. As-built: `lib/markdown.tsx` (303 lines — typed React-element renderer + exact-depth `section()` (`###` never terminates) + census-bound `DOC_CUTS`) + `shell/DocsPanel.tsx` (124 — 4-state union, keyed remount per entry, ONE fetch four selections) + 6-line ComponentPage wire + 132 append-only `showcase-md-*` css lines; packages untouched, barrel byte-identical, package.json unchanged. Evidence: Mars 13/13 + the Director 8/8 corpus sweep with RAW-DERIVED per-file expectations (block counts outside fences; leakage outside code; EVERY continuation fragment canonicalized-contained in a li; the cutter law Props 65/65 + EXACTLY the census-9 enum-less; the real `Switch` empty state; single-fetch; loaderless no-contract); THREE mutations net-zero (Mars join-break + naive pipe-split; Director `###`-terminates-cutter — CONTENT-verified revert since the file is untracked and `git diff` cannot witness it). **LESSONS:** (L-mx9.4a) **the NUL-byte trap struck THREE times this rung** — authoring the U+0000 sentinel via Write lands a LITERAL NUL; in an Edit old_string AND in any Bash command the JSON-escape form of backslash-u0000 IS the NUL codepoint (double the backslash); guard: perl -ne 'print if /\x00/' after ANY sentinel write, and spell the escape as visible text everywhere. (L-mx9.4b) **question the witness before the build** — 3 consecutive Director-sweep failures were all WITNESS bugs (stripping `*` inside code spans; a grep matching `**`-BETWEEN-code-spans; a continuation line SPLITTING a code span shifts backtick pairing in isolation) — canonicalize BOTH sides for content-containment witnesses. (L-mx9.4c) 5 contracts (Stat:19 · Alert:18 · PasswordStrength:16 · Progress:17 · Label:23) carry `\|` inside code spans in PROSE — CommonMark-faithful literal render (GitHub identical); a contract-HYGIENE candidate for a later packages-side rung, NOT a renderer gap. Residual: the `:5176` four-view visual pass. **NEXT = `/mercury-ship mx.9.5`** (the closer: seed-skinned chrome + dual-theme + the whole-epic INV-1..9/S-9/S-10 re-run; ELEVATED — **Squad + Apollo**; Movement III closes).
