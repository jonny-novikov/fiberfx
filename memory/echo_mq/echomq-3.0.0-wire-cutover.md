---
name: echomq-3-0-0-wire-cutover
description: "echomq:3.0.0 wire bump + codemojex PROD deploy on echo-valkey — DEPLOYED 2026-06-26; codemojex→echo-valkey wiring (per-client inet6 tuple); prod-build gotchas (no :kernel compile config · Dockerfile COPY must mirror mix.exs deps · umbrella releases: block); fly-valkey.sh"
project: echo_mq
metadata:
  node_type: memory
  type: project
  originSessionId: dfc9537d-f90d-4421-9f93-ca7810fa2c6b
---

EchoMQ wire `echomq:2.4.2` → **`echomq:3.0.0`** — code, local, AND production codemojex deploy all **DONE / DEPLOYED (2026-06-25/26).**

- **Code (DONE):** `@wire_version` at `apps/echo_wire/lib/echo_mq/connector.ex:35` — the ONLY source change; everything else reads `Connector.wire_version/0` dynamically (conformance/tests assert the *shape*, never a literal). Fence: `connector.ex` `fence/2` = `claim | verify | refuse` against `{emq}:version` (`keyspace.ex:11`) at every connect/reconnect. **The cutover is TWO-SIDED:** code constant + the persisted `{emq}:version` on *every* Valkey (the key out-votes the constant until `SET`/`DEL`).
- **LOCAL `:6390` db-0 (DONE):** `SET '{emq}:version' echomq:3.0.0`. Full umbrella green (conformance 79/79).
- **PRODUCTION `echo-valkey` (DEPLOYED 2026-06-26):** fence = `echomq:3.0.0`; codemojex deployed + verified — `https://codemojex.fly.dev/api/health` → 200, **2 machines** each with **9 stable lanes** on echo-valkey, fence verified. echo-valkey = Valkey 9.1.0, `:6390`, `requirepass` via `VALKEY_EXTRA_FLAGS` secret, persistent `/data`.

**codemojex → echo-valkey WIRING (was the real gap — codemojex used to dial `127.0.0.1:6390` no-auth):** `config/runtime.exs` reads `VALKEY_HOST`/`VALKEY_PORT`/`VALKEY_PASSWORD` (prod) → `Codemojex.Application.conn_opts/0` threads host/password to ALL lanes (Bus + 4 consumers + 3 tables; `store.ex` `Codemojex.Bus` + `tables.ex` forward opts verbatim to `EchoMQ.Connector`, which already supports `:host`/`:password`/`HELLO 3 AUTH`). IPv6 (Fly 6PN is v6-only): `valkey_host/0` resolves the host to an **inet6 ADDRESS TUPLE** via `:inet.getaddr(host, :inet6)` — gen_tcp infers inet6 from an 8-tuple. **Per-client** (only the Valkey connector), no frozen-wire change. Caveat: tuple resolved once at boot → if echo-valkey's machine is *replaced*, restart codemojex to re-resolve.

**PROD-BUILD GOTCHAS (each caused a deploy failure; all fixed — check these BEFORE the next codemojex deploy):**
1. **NEVER `config :kernel, ...` in compile-time `config/*.exs`** — a release errors `Cannot configure base applications: [:kernel]` (kernel starts before config loads) and cascades to `gen_tcp :badarg`. So IPv6 is NOT a global kernel default (which also collides with clients choosing their own family) — it's the per-client inet6 tuple above.
2. **The Dockerfile hand-picks a COPY subset of umbrella apps — it MUST mirror codemojex's `mix.exs` dep closure.** `echo_bot` (a REAL dep: `EchoBot.deliver/3` + `ingest/1` for Telegram I/O) was omitted, incl. its `vendor/ex_gram` (on `elixirc_paths`) + `priv/`. A *local* compile can't catch this (full umbrella on disk); only the Docker build warns `missing dependency echo_bot`.
3. **An umbrella has NO default release** — root `echo/mix.exs` needs `releases: [codemojex: [applications: [codemojex: :permanent]]]` or `mix release codemojex` → `Unknown release`.
- **Reproduce the Docker build locally to surface prod-env bugs a dev compile hides:** `MIX_ENV=prod mix compile` + `MIX_ENV=prod mix release codemojex` (the two heavy Docker stages).

- **SECRETS:** nothing reads `.env.production` at runtime — the release gets them via `fly secrets import --stage -a codemojex < .env.production` (then `fly deploy`). `DATABASE_URL` + `SECRET_KEY_BASE` are boot-critical (raise if missing). echo/.env.production is gitignored.
- **TOOL `scripts/fly-valkey.sh`** (jonnify repo root): `fly proxy` + authed valkey-cli, password from `infra/valkey/.env.production`, secret never printed. `CLIENT LIST` gotcha: `addr=` is a substring of `laddr=` (the server addr) → naive `grep addr=` double-counts. Healthy codemojex footprint = **9 lanes/instance** (4 `blpop` consumers + 1 `client|tracking` + 4 idle `ping`), all RESP3, stable (no churn/leak).
- **DOCS:** `echo/docs/echomq-3.0.0-upgrade.md` (runbook) + `echo/docs/wire-version-fence.dev-guide.md`.
- **Operator commits + deploys out-of-band:** all my changes were committed by the Operator (the `[codemojex] deploy`/`mix`/`first deploy` commits) as part of deploying; tree goes clean between turns — verify HEAD rather than assume the tree is dirty. [[user-commits-elixir-batches]]
- **Dormant/optional:** Graft→Tigris (the `codemojex-graft` Tigris bucket + creds + `BUCKET_NAME` are wired, but activation is an UNBUILT feature — no codemojex write path into a volume, committer live-notice push-gap needs an echo_store change; the default `EchoStore.Durability.SQLite` is a LOCAL journal/outbox, **not** Litestream/replication — codemojex's real durability is Postgres). Pre-existing `echo_data/champ_view.ex` `-Werror` warning (inverted layering) is harmless noise. Open nit: `fly.toml` `min_machines_running = 1` but 2 machines ran.

- **EVERY Fly 6PN backend needs PER-CLIENT inet6** (lesson 2026-06-26): echo-**postgres** had the SAME gap as Valkey — `config/runtime.exs` `Codemojex.Repo` had `url`+`pool_size` but NO `socket_options: [:inet6]`, so Postgrex silently never connected to the v6-only `echo-postgres.internal` (Ecto retries forever, NO crash; `/api/health` is DB-less + the Valkey hot-path works → **liveness ≠ connectivity**, it looked healthy). Found via `pg_stat_activity` (0 codemojex conns though the `codemojex` db exists). Fix: `maybe_ipv6 = if System.get_env("ECTO_IPV6") in ~w(true 1), do: [:inet6], else: []` + `socket_options: maybe_ipv6` (ECTO_IPV6=true was already in fly.toml, just unread) — but inet6 was only the FIRST of TWO STACKED issues. The SECOND: a WRONG `echo_mesh` password in DATABASE_URL — once inet6 let it reach the server, TCP connected but PG **auth failed** (server `pg_hba` ALLOWED it via `host all all all scram-sha-256`; echo-postgres logged repeating `FATAL: password authentication failed for user "echo_mesh"`). **RESOLVED 2026-06-26** by reconciling the password + redeploy → **10 pool conns connect, Repo.query works**. (DB user is `echo_mesh`, db `codemojex`.) Diagnose stacked 6PN-backend failures with the **DNS→TCP→auth ladder** (`:inet.getaddr`/`:gen_tcp.connect` via `fly ssh console -a <app> -C "bin/<app> rpc '…'"`) + the SERVER's FATAL log; verify via `pg_stat_activity`/`CLIENT LIST` + a live `Repo.query`, NOT the HTTP health check (it's DB-less). Postgrex `:queue_timeout` masks the real connect error — go to the layers.
- **Machine count:** codemojex scaled **2→1** (`fly scale count 1`) — Fly's HA default provisions 2 for an app with an HTTP service; `fly.toml min_machines_running=1` is only the autostop floor, not a cap. echo-valkey healthy at 1 machine = 9 lanes (4 blpop + 1 client-tracking + 4 ping) + probes.

**Why:** a major wire bump is a deliberate, coordination-free lockout of old-protocol nodes.

Related: [[echo-mq-three-movements]] [[operator-runs-deploys]] [[codemojex-program]] [[user-commits-elixir-batches]]
