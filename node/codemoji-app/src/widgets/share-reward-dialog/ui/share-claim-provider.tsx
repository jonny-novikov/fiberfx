/**
 * Temporarily disabled — claim logic is no longer called from the frontend.
 * The component is kept for future re-enablement.
 *
 * Previously: headless-компонент для автоматического вызова POST /share/claim
 * при возвращении пользователя в приложение.
 */
export const ShareClaimProvider = () => {
  return null
}
