{
  "name": "codemojex-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright end-to-end stories for the codemojex three-tier render (welcome -> LiveView lobby -> LiveReact game). Drives the real Telegram-initData auth handshake against a running dev server.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.10.0"
  }
}
