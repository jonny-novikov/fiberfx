# The pnpm workspace for @codemojex/edge. The members are the vendored Phoenix
# client packages (@echo/phoenix, @echo/phoenix_live_view) under packages/, so the
# edge build resolves them from this self-contained assets/ tree — no umbrella, no
# echo/deps/, no file:../ escape.
packages:
  - "packages/*"
