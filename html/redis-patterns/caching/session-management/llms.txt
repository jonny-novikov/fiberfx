# R1.06 · Session management

> Sessions are the canonical TTL-expiry use of Valkey: per-user state that vanishes on logout or timeout. The
> encoding choice, correct expiry, and the tie to authentication. Grounded in `EchoCache.Table.put` (`SET … PX`) with
> a branded `SES` key, the kind gate `gate/2` binding it to one namespace, and the Exchange Platform's opaque account
> id (`Exchange.Gateway.parse_account/1`); real Valkey SET PX / PTTL / DEL. Served at
> /redis-patterns/caching/session-management.

## Pages

- [Hub](/redis-patterns/caching/session-management): sessions as the canonical TTL-expiry use.
- [Hash vs String vs framed value](/redis-patterns/caching/session-management/encodings): the encoding trade-off — field-level access vs the `version<>value` frame EchoCache writes.
- [TTL expiry](/redis-patterns/caching/session-management/ttl-expiry): absolute vs sliding (refresh-on-access) `SET … PX`, the jittered `expires_at`, and the `:sweep` eviction that makes it real.
- [The auth-session tie-in](/redis-patterns/caching/session-management/auth-session): a `SES`-keyed row via `Table.put`, dropped by `Table.invalidate`, refused at the kind gate — keyed to the account id, inventing no auth surface.

## Up

- [R1 · Caching](/redis-patterns/caching) · [Course home](/redis-patterns)
