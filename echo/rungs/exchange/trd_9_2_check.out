# trd_9_2_check.out -- committed transcript of the TRD.9.2 rung gate (DoD artifact).
# Produced by: cd echo && mix run --no-start rungs/exchange/trd_9_2_check.exs 2>/dev/null
# Exit: 0 (PASS 5/5). The mix compile-progress lines on stderr are elided -- stdout
# from the `header:` line is the gate record. Reproducible: two runs are byte-identical
# (the generators are self-contained seeded :rand, no StreamData; the runner dials nothing).
# Gates (Tier 1, network-free): G1-scaffold@48 parity (48 mapped + 24 pending = 72; the
#        5 touched services) | G2 money-read-shapes (2000 seeded {units,nano} in the
#        read-response field shapes; Money exercised-not-extended) | G-FID pass-through-
#        fidelity (all 41 reads delegate to their identically-named &Stub.<fun>/3; a
#        mutated pairing turns it red) | G5 network-free (the :sandbox exclusion is
#        structural) | G7 no-token (the secret scan, INV-9).
# The live representative-subset round-trip (G6, 9.2 scope: open -> Instruments read ->
# Money-decode last-prices -> Money-decode portfolio -> close) is the Operator's SEPARATE
# hard gate, run with INVEST_TOKEN on `mix test --include sandbox`, NOT part of this
# network-free transcript. Spec: docs/exchange/trd.9.2.specs.md ("Acceptance gates").
#
header: Investex read services (lib-only, no dial) | Elixir 1.18.4 OTP 28 | grpc 0.11.5 protobuf 0.17.0
G1-scaffold@48 parity ok -- the proto enumerates 72 RPCs across 10 services; the 48 implemented (Users 4 + the sandbox trio 3 + Instruments 27 + MarketData 7 + Operations 7) each map to a real, exported Investex function; the 24 unimplemented are an explicit pending list; the touched services are exactly {Users, Sandbox, Instruments, MarketData, Operations} (the count-prints-72 full assertion is 9.5)
G2 money-read-shapes ok -- across 2000 seeded {units, nano} pairs in the read-response field shapes from_quotation . to_quotation is identity, from_money_value decodes the integer pair + the non-empty ISO currency, and NO value is a float; Investex.Money is exercised-not-extended (its public surface is exactly the 3 codec functions)
G-FID pass-through-fidelity ok -- all 41 read functions (Instruments 27 + MarketData 7 + Operations 7) delegate via &Stub.<fun>/3 to their identically-named stub fun (snake(RPC) == def-name == stub-fun-name), no def bypasses the Caller.unary seam, and each module's def-name set equals its service's declared RPC set; a mutated pairing (def shares -> &Stub.bonds/3) turns it red (the wrong-stub class the parity scaffold cannot catch)
G5 network-free ok -- test_helper.exs starts ExUnit with exclude: [:sandbox] so the default suite (and this --no-start gate) is network-free; the live read subset module is @moduletag :sandbox (excluded by default, run only on --include sandbox with INVEST_TOKEN); this gate dials nothing by construction
G7 no-token ok -- no token-shaped literal in the app lib, tests, or generated modules (28 files scanned); the token is read from INVEST_TOKEN at call time only -- never a struct default, a config literal, a log line, a fixture, or this transcript
PASS 5/5
