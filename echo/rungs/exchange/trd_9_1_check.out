# trd_9_1_check.out -- committed transcript of the TRD.9.1 rung gate (DoD artifact).
# Produced by: cd echo && mix run --no-start rungs/exchange/trd_9_1_check.exs 2>/dev/null
# Exit: 0 (PASS 5/5). The mix compile-progress lines on stderr are elided -- stdout
# from the `header:` line is the gate record. Reproducible: two runs are byte-identical
# (the generators are self-contained seeded :rand, no StreamData; the runner dials nothing).
# Gates (Tier 1, network-free): G1-scaffold parity (7 mapped + 65 pending = 72) |
#        G2 money-round-trip (2000 seeded {units,nano}) | G4 retry-pure (the three
#        branches + the forbidden-effect grep) | G5 network-free (the :sandbox
#        exclusion is structural) | G7 no-token (the secret scan, INV-9).
# The live sandbox round-trip (G6, 9.1 scope) is the Operator's SEPARATE hard gate,
# run with INVEST_TOKEN on `mix test --include sandbox`, NOT part of this network-free
# transcript. Spec: docs/exchange/trd.9.1.specs.md ("Acceptance gates").
#
header: Investex transport spine (lib-only, no dial) | Elixir 1.18.4 OTP 28 | grpc 0.11.5 protobuf 0.17.0
G1-scaffold parity ok -- the proto enumerates 72 RPCs across 10 services; the 7 implemented (UsersService 4 + the sandbox trio 3) each map to a real, exported Investex function; the 65 unimplemented are carried as an explicit pending list (rows move pending->asserted across 9.2-9.5; the count-prints-72 full assertion is 9.5)
G2 money-round-trip ok -- across 2000 seeded {units, nano} pairs from_quotation . to_quotation is identity, from_money_value decodes the integer pair + the ISO currency, and NO value is a float (the Go ToFloat/FloatToQuotation bridge is deliberately not ported)
G4 retry-pure ok -- decide/3+/4 returns {:retry, 500} on :unavailable/:internal under the cap, {:retry, wait} honoring x-ratelimit-reset (floored 500) on :resource_exhausted, and :give_up past max_retries / RE-disabled / disable_all_retry / an unretryable status; retry.ex (comments stripped) contains no clock/sleep/Process.*/network/IO
G5 network-free ok -- test_helper.exs starts ExUnit with exclude: [:sandbox] so the default suite (and this --no-start gate) is network-free; the live round-trip module is @moduletag :sandbox (excluded by default, run only on --include sandbox with INVEST_TOKEN); this gate dials nothing by construction
G7 no-token ok -- no token-shaped literal in the app lib, tests, or generated modules (24 files scanned); the token is read from INVEST_TOKEN at call time only -- never a struct default, a config literal, a log line, a fixture, or this transcript
PASS 5/5
