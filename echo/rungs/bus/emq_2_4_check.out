# emq_2_4_check.out -- committed transcript of the emq.2.4 gate-ladder harness (DoD artifact).
# Produced by: cd /Users/jonny/dev/jonnify/echo && bash rungs/bus/emq_2_4_check.sh
# Exit: 0 (PASS 7/7). Run 2026-06-14, 18:04:07Z -> 18:11:03Z (~7 min, LOOP_N=100, owning the machine).
# The mix compile-progress lines on stderr + the lone watch-plane reconnect-kill
# captured-log line are elided -- the board (the seven gate lines + the PASS/FAIL
# verdict) is the gate record. Reproducible: re-running yields the same board (the
# same commands, the same pinned counts 250/43/100/5 + the 182/72 column split); only
# timing-jitter wall seconds vary, and the dbsize ABSOLUTE number floats run-to-run
# (Valkey residue -- only the before==after DELTA is asserted, and it is flat).
#
# This is the structural fix the Operator directed ("Harness required"): the
# determinism proof that EVAPORATED when the prior build cycle crashed mid-Stage-4
# (a hand-run loop tee'd to a /tmp file that did not survive) is now a committed,
# re-runnable artifact. The emq.2.4 CODE is already committed + Director-gate-verified
# GREEN (92a8f042 obliterate-fix, 7c2f2405 docs, 3298e4bc closer); this harness
# RE-PROVES the gate ladder + the >=100 determinism loop, so a crash cannot evaporate
# the green-board again.
#
# Gates (the program gate ladder, bound to emq.2.4): toolchain (erlang 28.5.0.1
#   re-probed from apps/echo_mq + Valkey :6390 PONG) | compile (--warnings-as-errors
#   clean) | suite (mix test --include valkey -> 4 doctests / 250 tests / 0 failures,
#   + the pure column 182 excluded / wire column 72 excluded, all 0 failures) |
#   conformance (Conformance.run/2 over a live :6390 -> {:ok, 43}) | determinism (the
#   >=100 loop over watch_depth + admin_depth + conformance_run -> 100/100) | leak
#   (dbsize before == after the loop) | seed-sweep (seeds 0 1 42 312540 999999 over
#   metrics_depth + admin_depth + rate_consult + dedup_bound -> 5/5).
# Spec: docs/echo_mq/specs/emq.2.4.md ; the green-board: docs/echo_mq/emq.testing.md.
#
header: emq.2.4 parity-closer gate ladder | 1.18.4 Elixir / OTP 28 | Valkey :6390 | LOOP_N=100 | 2026-06-14T18:04:08Z
----------------------------------------------------------------------
toolchain        ok   -- asdf resolves erlang 28.5.0.1 inside apps/echo_mq (the .tool-versions pin, the build dir -- not the repo-root global) and Valkey answers PING -> PONG on :6390 (the live engine, not the 6379 default)
compile          ok   -- TMPDIR=/tmp mix compile --warnings-as-errors is clean in apps/echo_mq (no warning, no undefined same-app verb -- an INV2 floor: the 5 depth suites call no surface that does not compile)
suite            ok   -- TMPDIR=/tmp mix test --include valkey -> [4 doctests, 250 tests, 0 failures] in apps/echo_mq (the lone captured-log GenServer-killed line is the watch-plane reconnect-kill drill, not a failure -- ExUnit still reports 0 failures). Pure column (no :valkey) [4 doctests, 250 tests, 0 failures, 182 excluded] ; wire column (--only valkey) [4 doctests, 250 tests, 0 failures, 72 excluded]
conformance      ok   -- EchoMQ.Conformance.run/2 over a live :6390 returns {:ok, 43} -- the 18 founding+emq.1 + 6 read + 8 ops + 5 watch + 5 emq.2.4 depth (unknown_state, rate_consult, dedup_release, extend_locks_batch, stalled_group) + obliterate_grouped (the emq.2.2 fix's proving scenario); the prior 37 byte-unchanged, the 6 new probe-registered
determinism      ok   -- the >=100 determinism loop is 100/100 green over the process-touching depth suites (watch_depth + admin_depth + conformance_run -- the timer/mint surface); no same-ms mint collision, no timer flake, no order race surfaced across 100 owning-the-machine iterations
leak             ok   -- redis-cli -p 6390 dbsize is FLAT across the loop: 1 before == 1 after (the depth suites purge what they mint; no key accumulation -- obliterate clears grouped rows, the locks/stalled/events keys are cleaned). The absolute count floats run-to-run (Valkey residue); only the delta is asserted
seed-sweep       ok   -- the multi-seed sweep is 5/5 green over the synchronous read/ops depth suites (metrics_depth, admin_depth, rate_consult, dedup_bound) across seeds 0 1 42 312540 999999 -- no order-dependent bug in the deterministic round-trips
----------------------------------------------------------------------
PASS 7/7 -- emq.2.4 gate ladder GREEN + harness-reproducible
