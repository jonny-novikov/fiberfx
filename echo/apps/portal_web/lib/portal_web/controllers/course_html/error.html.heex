<%!--
  An expected domain failure (F6.1-D4, F6.1-INV4). Renders @error.message from the
  closed %Portal.Error{} set at HTTP 422. Reads only the assign.
--%>
<h1>Error</h1>
<p data-error-message>{@error.message}</p>
