<%!--
  An expected domain failure (F6.5-D0; relocated from course_html/error.html.heex).
  Renders @error.message from the closed %Portal.Error{} set at HTTP 422. Reads only
  the assign — the enrolled read is the sole producer of a %Portal.Error{} now.
--%>
<h1>Error</h1>
<p data-error-message>{@error.message}</p>
